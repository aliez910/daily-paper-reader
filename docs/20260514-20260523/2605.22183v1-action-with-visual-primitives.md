---
title: Action with Visual Primitives
title_zh: 基于视觉基元的动作
authors: "Weilong Guo, Yuchen Wang, Renping Zhou, Yunfeng Zhang, Rui Fang, Yue Meng, Wenda Xu, Yuan He, Gao Huang"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22183v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 在VLA中使用视觉基元作为动作接口
tldr: "针对VLA模型将语言理解、场景感知与运动控制纠缠导致效率低的问题，提出AVP，让VLM输出视觉原始标记，条件化流匹配动作专家，解耦高层推理与低层控制。在真实机器人拾取放置任务中，成功率比pi_0.5提升27.61%，数据效率、空间组合泛化和物体迁移能力均提升。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22183v1/fig-001.webp\", \"caption\": \"Figure 3: OOD object gallery for cross-domain generalization. We evaluate zero-shot transfer on 45 unseen objects spanning multiple categories beyond the Chinese chess training domain.\", \"page\": 15, \"index\": 1, \"width\": 820, \"height\": 431}, {\"url\": \"assets/figures/arxiv/2605.22183v1/fig-002.webp\", \"caption\": \"Figure 1: Different visual primitives of our AVP.\", \"page\": 2, \"index\": 2, \"width\": 829, \"height\": 962}, {\"url\": \"assets/figures/arxiv/2605.22183v1/fig-003.webp\", \"caption\": \"Figure 2: Overview of the AVP framework.\", \"page\": 4, \"index\": 3, \"width\": 829, \"height\": 464}, {\"url\": \"assets/figures/arxiv/2605.22183v1/fig-004.webp\", \"caption\": \"Figure 4: Qualitative results for zero-shot cross-domain generalization. The leftmost column displays unseen OOD test objects, and the right columns illustrate the successful grasping trajectories executed by AVP.\", \"page\": 16, \"index\": 4, \"width\": 826, \"height\": 952}]"
motivation: 现有VLA模型将指令理解、场景感知与运动控制耦合在同一目标中，限制学习效率与泛化能力。
method: AVP通过VLM推断下一阶段目标并生成视觉原始标记，条件化流匹配动作专家，以末端执行器运动学为监督进行端到端训练。
result: "在通用拾取放置任务上，成功率比pi_0.5提高27.61%，并在数据效率、空间组合泛化和物体级迁移上表现更优。"
conclusion: AVP通过视觉原始解耦框架有效提升了机器人操作性能，兼顾效率与泛化。
---

## 摘要
视觉-语言-动作（VLA）模型已成为通用机器人操作领域的一种有前景的范式。当前架构中的常见设计是将语言指令和视觉观测映射为单次前向传递中的动作。虽然概念上简单，但这种表述将指令理解、空间场景感知和运动控制纠缠在单一学习目标中。结果，动作专家必须隐式地重新学习预训练VLM中已存在的认知和感知能力，这可能限制学习效率和泛化能力。我们提出AVP（基于视觉基元的动作），一种端到端架构，实现了这种以视觉基元为中心的接口：VLM推断下一阶段目标并发出视觉基元标记，这些标记条件化一个流匹配动作专家，其监督来自末端执行器运动学。在通用拾取与放置任务上的真实机器人实验表明，AVP相比pi_0.5将成功率提高了27.61%，并优于其他近期方法，在数据效率、空间组合泛化和物体级迁移方面表现出一致性提升。

## Abstract
Vision-Language-Action (VLA) models have emerged as a promising paradigm for generalist robotic manipulation. A common design in current architectures maps language instructions and visual observations to actions in a single forward pass. While conceptually simple, this formulation entangles instruction comprehension, spatial scene understanding, and motor control within a single learning objective. As a result, the action expert must implicitly relearn cognitive and perceptual capabilities already present in the pretrained VLM, which can limit both learning efficiency and generalization. We introduce AVP (Action with Visual Primitives), an end-to-end architecture that implements this visual-primitive-centric interface: the VLM infers the next-stage target and emits visual-primitive tokens that condition a flow-matching action expert, with supervision derived from end-effector kinematics. Real-robot experiments on general pick-and-place tasks show that AVP improves the success rate by 27.61% over pi_0.5 and outperforms other recent methods, with consistent gains in data efficiency, spatial-compositional generalization, and object-level transfer.

---

## 论文详细总结（自动生成）

好的，这是对论文《Action with Visual Primitives》的结构化、深入、客观的中文总结。

### 1. 核心问题与整体含义（研究动机和背景）

*   **核心问题**：当前主流的视觉-语言-动作（VLA）模型（如RT-2, OpenVLA等）将**语言理解、视觉感知、场景推理和动作生成**等多项任务纠缠在一个单一的学习目标中。这导致动作专家（Action Expert）必须隐式地重新学习VLM已经具备的认知和感知能力，不仅学习效率低下，而且在面对物体或场景变化时泛化能力不足。
*   **研究背景**：研究者们已经尝试引入中间表征来解决此问题，例如：
    *   基于**规划**的方法：将指令分解为子任务，但语言接口难以表达精细空间区分。
    *   基于**未来帧预测**的方法：提供子目标图像，但需要动作专家在稠密像素中重新发现关键信息。
    *   基于**视觉提示**的方法：将VLM输出转为图像上的空间标记（点、框等），但这依赖于外部模型，形成多阶段管线，易累积误差并增加延迟。
*   **总体含义**：本论文旨在重新定义VLM和动作专家之间的职责划分，提出一种更清晰、高效的通信协议。核心思想是让VLM负责“做什么”和“在哪里”，而动作专家只需专注于“如何做”。

### 2. 方法论：核心思想与关键技术细节

*   **核心思想**：提出一种名为**AVP (Action with Visual Primitives)** 的端到端架构，在VLM和动作专家之间建立一个以**视觉基元（Visual Primitives）** 为核心的显式通信接口。VLM不再直接输出隐式的特征，而是生成显式的、空间化的视觉标记（如点、边界框）来指导动作。

*   **关键技术细节与流程**：
    1.  **VLM推理**：接收语言指令`l`和当前观测`o_t`，输出下一个子任务的目标及其对应的视觉基元`p_t`。
    2.  **视觉基元投影**：将预测的视觉基元`p_t`投影回原始图像`o_t`的视觉令牌空间，生成增强的视觉令牌`zvp_t`。这相当于在原始图像上“绘制”了VLM认为的关键位置。
    3.  **动作专家预测**：增强后的多模态表示`zaug_t`（包含原始视觉、语言和视觉基元信息）与机器人状态`s_t`一起，作为条件输入到**流匹配动作专家（Flow-Matching Action Expert）** 中，生成最终的动作序列`a_{t:t+h}`。
    4.  **监督信号**：
        *   视觉基元预测损失：使用交叉熵损失`Lvp`来监督基元解码器。其**真值标签`yvp_t`直接来自机器人的末端执行器运动学**（通过手眼标定投影到图像平面），无需人工标注或外部感知模型。
        *   动作预测损失：标准动作预测损失`Lact`。
    5.  **联合优化**：总损失函数为 `L = Lact + λLvp`，实现了基元预测和动作生成的端到端联合优化。

### 3. 实验设计

*   **实验场景与数据集**：在**真实机器人**（AgileX Piper桌面双臂平台）上进行了三个任务，全部使用自采的专家演示数据训练。
    *   **场景A：中国象棋操作**：主要基准测试，评估空间精确的序列操作。11.2小时数据。
    *   **场景B：多米诺骨牌放置**：评估高精度双臂操作，需对齐方向。1.7小时数据。
    *   **场景C：通用物体拾取与放置**：评估对不同外形和外观物体的泛化能力。1.7小时数据。
*   **评测指标**：主要报告**指令跟随成功率、拾取成功率、放置成功率**，对于多米诺任务还额外报告**方向成功**率。
*   **对比方法**：
    *   **主要基线**：`π0.5`（当前最强vla基线之一）。
    *   **其他基线**：DM0, LDA, π0, StarVLA, 以及基于外部API的Point-VLA变体。所有基线在相同硬件和数据上复现。

### 4. 资源与算力

论文中**明确提到了训练步数和批量大小**，但**未明确说明使用的GPU型号、数量和总训练时长**。具体信息如下：
*   中国象棋任务批量大小64，训练40k步。
*   其他任务批量大小64，训练10k步。

### 5. 实验数量与充分性

实验设计较为充分和客观，主要体现在以下几点：
*   **任务多样性**：覆盖了从高精度（象棋）、到涉及方向（多米诺）、再到一般化（通用拾取）的多种真实操作任务，验证了方法的普适性。
*   **基线对比公平**：所有基线方法（包括作者的AVP）都在相同的机器人本体、训练数据和评估协议下进行复现，保证了比较的公平性。
*   **严格的消融实验**：
    *   **视觉基元类型**：对比了无提示、点、框、框+掩码、框+掩码+记忆等多种设计。
    *   **泛化实验**：
        *   **空间组合泛化**：在从未训练过的直接“棋盘到棋盘”过渡任务上评估，AVP表现远优于基线。
        *   **跨域零样本泛化**：在训练域外的全新物体、不同背景和指令协议下测试，AVP表现出强大的迁移能力，而基线性能大幅下降。
    *   这些实验有效验证了**视觉基元作为解耦中间表示**的核心假设。

### 6. 主要结论与发现

*   **核心结论**：通过引入视觉基元作为VLM和动作专家之间的显式通信接口，AVP成功解耦了“感知推理”与“运动执行”的责任，显著提升了VLA模型的性能。
*   **关键发现**：
    *   **性能提升显著**：在三个任务上，AVP均优于所有对比方法。相比最强基线`π0.5`，总体成功率**提升27.61%**（在中国象棋任务上，AVP平均成功率90.28% vs. π0.5的62.67%）。
    *   **泛化能力卓越**：在空间组合泛化和跨域零样本泛化测试中，AVP表现鲁棒，而基线方法严重退化，证明了该框架在应对分布外场景中的巨大优势。
    *   **数据效率高**：通过引入结构化空间提示，减少了动作专家为理解任务所需的学习负担，从而在较少数据下也能有效学习。

### 7. 优点

1.  **方法简洁优雅**：核心思想明确（解耦），技术实现为端到端，避免了复杂的多阶段管线或外部依赖。
2.  **实用性强**：视觉基元的**真值标签可以从机器人自身的运动学数据直接导出**，无需耗时费力的人工标注或昂贵的外部模型，这使得该方法在数据收集成本上极具优势，易于扩展到新平台。
3.  **性能与泛化能力强**：在主任务和泛化测试中均取得显著领先效果，验证了方法的有效性。
4.  **实验设计完整深入**：不仅报告了多任务的结果，还通过精心设计的泛化和消融实验，深入分析并验证了核心设计理念，使结论更有说服力。

### 8. 不足与局限

1.  **推理延迟**：VLM先预测基元、再将其输入给动作专家的**顺序两步推理**过程，相比单次前向传递的策略（如π0.5），引入了额外的计算和延迟。论文报告AVP的延迟为0.27s，略高于π0.5的0.16s。
2.  **对标定精度敏感**：视觉基元的监督信号依赖于**精确的手眼标定**来将3D末端执行器位置投影到2D图像平面。这意味着系统对相机外参的漂移或机械结构的物理扰动比较敏感，在实际部署中可能是一个维护挑战。
3.  **实验环境相对受限**：虽然覆盖了多种操作，但所有实验都在一个相对结构化的**桌面双臂平台**上进行，未在更复杂、更动态的真实场景（如家庭、仓库）中验证。可能存在对特定环境（如特定棋盘、特定光照）的过拟合风险。
4.  **未明确提及计算资源**：缺少GPU型号、数量等详细信息，对计算成本和可复现性评估带来一定困难。
5.  **零样本泛化实验对象数量有限**：虽然在跨域泛化中展示了45个未见物体，但零样本实验结果的详细表格（表4b）仅展示了有限组合，缺乏对所有45个物体的完整成功率统计。

（完）
