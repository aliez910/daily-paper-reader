---
title: "Good in Bad (GiB): Sifting Through End-user Demonstrations for Learning a Better Policy"
title_zh: 劣中取优 (GiB)：筛选终端用户演示以学习更好的策略
authors: "Noushad Sojib, Momotaz Begum"
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01529v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 筛选用于模仿学习的终端用户演示数据
tldr: 针对非专家用户提供的模仿学习数据中包含错误子任务的问题，本文提出GiB算法。该方法通过自监督学习提取潜在特征，并利用马氏距离自动识别并剔除演示中的低质量片段，保留高质量部分。实验证明，GiB能有效利用混合质量数据，在Franka机器人的仿真和真实多步任务中显著提升了策略性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01529v1/fig-001.webp\", \"caption\": \"Fig. 6. Success rate under different ρ values for Square and Mug tasks.\", \"page\": 7, \"index\": 1, \"width\": 529, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2605.01529v1/fig-002.webp\", \"caption\": \"Fig. 1. GiB subtask evaluation pipeline. (A) A self-supervised model learns latent embeddings zi and assigns binary weights wi ∈ {0, 1} to trajectories, with wi = 1 indicating high-quality demonstrations. (B) Trajectories are segmented into k subtasks (e.g. k = 3), and for each subtask j, a Gaussian distribution of the latent trajectory N (µj ,Σj) is estimated using high-quality trajectories only. (C) Subtasks are evaluated by computing the Mahalanobis distance between each timestep’s latent embedding and the corresponding subtask distribution. The top ρ subtasks with the largest deviation scores are labeled as bad (shown in red), while the remaining ones are labeled as good (shown in green).\", \"page\": 3, \"index\": 2, \"width\": 1004, \"height\": 566}]"
motivation: 传统的模仿学习假设演示数据均为高质量，但非专家用户的演示常包含错误，直接学习会导致不安全行为，而全盘舍弃又会浪费数据。
method: GiB通过自监督模型学习潜在特征，并利用马氏距离对演示片段进行评估，从而自动过滤掉错误的子任务并保留优质片段。
result: 在Franka机器人的仿真和真实世界多步任务中，GiB成功从混合质量的演示中学习到了更鲁棒的策略。
conclusion: GiB提供了一种高效的数据筛选机制，能够从包含噪声的非专家演示中提取有用信息，显著增强了模仿学习的实用性。
---

## 摘要
模仿学习为机器人从人类用户那里获取多样化技能提供了一个极具前景的框架。然而，大多数模仿学习算法假设可以获得高质量的演示，但在从非专家用户收集数据时，这往往是一个不切实际的期望，因为他们的演示通常包含无意的错误。盲目地从这些演示中学习可能导致不安全的策略行为，而由于偶尔的错误就丢弃整个演示则会浪费宝贵的数据，尤其是在低数据量的情况下。在这项工作中，我们提出了 GiB (Good-in-Bad)，这是一种能够自动识别并丢弃演示中错误子任务，同时保留高质量子任务的算法。过滤后的数据随后可被任何策略学习算法用于训练更鲁棒的策略。GiB 首先训练一个自监督模型来学习潜在特征，并分配二进制权重将每个演示标记为好或坏。接着，它对高质量片段的潜在特征分布进行建模，并利用马氏距离 (Mahalanobis distance) 来检测和评估低质量子任务。我们在 Franka 机器人上通过模拟和真实世界的多步任务验证了 GiB，证明了在从质量参差不齐的人类演示中学习时，该算法能够提升策略性能。

## Abstract
Imitation learning offers a promising framework for enabling robots to acquire diverse skills from human users. However, most imitation learning algorithms assume access to high-quality demonstrations an unrealistic expectation when collecting data from non-expert users, whose demonstrations often contain inadvertent errors. Naively learning from such demonstrations can result in unsafe policy behavior, while discarding entire demonstrations due to occasional mistakes wastes valuable data, especially in low-data settings. In this work, we introduce GiB (Good-in-Bad), an algorithm that automatically identifies and discards erroneous subtasks within demonstrations while preserving high-quality subtasks. The filtered data can then be used by any policy learning algorithm to train more robust policies. GiB first trains a self-supervised model to learn latent features and assigns binary weights to label each demonstration as good or bad. It then models the latent feature distribution of high-quality segments and uses the Mahalanobis distance to detect and evaluate poor-quality subtasks. We validate GiB on the Franka robot in both simulated and real-world multi-step tasks, demonstrating improved policy performance when learning from mixed-quality human demonstrations.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **GiB (Good-in-Bad)** 的算法，旨在解决模仿学习中非专家用户演示数据包含错误的问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的模仿学习（IL）算法通常假设训练数据由专家提供且高质量。但在现实场景（Field Imitation Learning, FIL）中，非专家用户（如普通家庭用户）提供的演示往往包含无意间的错误或失败片段。
*   **研究动机**：如果直接学习这些包含错误的数据，会导致机器人策略不安全；如果因为局部错误而丢弃整个演示序列，则会造成宝贵数据的巨大浪费（尤其是在数据稀缺的情况下）。
*   **整体含义**：论文提出了一种“劣中取优”的思路，即自动识别并剔除演示中的错误子任务（Segments），同时保留其中的高质量片段，从而利用不完美的数据训练出更鲁棒的策略。

### 2. 方法论：核心思想与技术细节
GiB 算法分为两个主要阶段：
*   **阶段一：自监督特征学习与轨迹初步筛选**
    *   基于 BED（Behavior Cloning for Error Discovery）框架，训练一个自监督模型。
    *   通过最小化“不一致性损失”（包括动作、状态演化和目标状态的不一致性），为每条轨迹分配一个初步权重 $w_i \in \{0, 1\}$。
    *   **核心假设**：高质量演示在数量上占主导地位，因此不一致的演示被视为潜在的错误数据。
*   **阶段二：子任务级别的评估与过滤**
    *   **子任务分割**：将长时程任务划分为 $k$ 个子任务（仿真中使用配置文件，现实中使用基于启发式的脚本，如检测夹爪状态）。
    *   **分布建模**：利用阶段一识别出的“好”轨迹，为每个子任务 $j$ 在潜在特征空间中建立多元高斯分布 $N(\mu_j, \Sigma_j)$。
    *   **异常检测**：计算所有演示片段中每个时间步的潜在特征与对应分布之间的**马氏距离（Mahalanobis distance）**。
    *   **筛选机制**：根据平均偏差得分对所有子任务进行排序，剔除得分最高的 $\rho$ 个子任务（即最偏离正常分布的片段），其余保留用于策略训练。

### 3. 实验设计
*   **实验场景**：
    *   **仿真环境**：使用 MimicGen 框架，包含四个任务：Square（套圈）、Coffee（放咖啡胶囊）、Mug Cleaning（洗杯子）、Kitchen（长时程厨房任务）。
    *   **真实世界**：使用 Franka 机器人执行“放西兰花进抽屉”的三步任务。
*   **数据集**：结合了专家演示和由 5 名非专家用户（通过 SpaceMouse 操作）提供的真实、包含自然错误的数据集。
*   **对比方法（Benchmarks）**：
    *   **Oracle**：基于真值标签过滤。
    *   **BED**：仅在轨迹级别进行过滤。
    *   **DemInf、S2I、LOF**：其他先进的数据筛选或异常检测基准方法。
    *   **All data / Only Good**：全量数据或仅用专家数据。
*   **下游策略**：所有过滤后的数据均使用 **Diffusion Policy** 进行训练。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号和数量。
*   **训练细节**：提到在仿真中训练了 200 个 epoch，在真实机器人任务中训练了 800 个 epoch。视觉编码器使用了 ResNet-18。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2 步到 7 步的不同复杂度的任务。
*   **充分性**：
    *   进行了多种子任务过滤参数 $\rho$ 的消融实验。
    *   针对多模态任务（如从左侧或右侧抓取）进行了专门的鲁棒性测试。
    *   所有实验均使用 3 个不同的随机种子取平均值，确保了结果的客观性。
*   **公平性**：对比方法均在相同的 Diffusion Policy 框架下运行，确保了筛选算法本身效果的对比是公平的。

### 6. 主要结论与发现
*   **性能提升**：GiB 筛选后的数据集在所有任务中均显著优于未过滤的数据，且在大多数情况下优于仅在轨迹级别过滤的方法（如 BED）。
*   **数据效率**：通过保留“坏”演示中的“好”片段，GiB 在复杂任务（如 Kitchen）中表现尤为突出，证明了细粒度筛选的必要性。
*   **可解释性**：马氏距离的波动能够直观反映机器人何时偏离正常轨迹，以及何时从错误中恢复。
*   **多模态兼容**：实验证明 GiB 能够处理具有多种正确执行方式（多模态）的任务，而不会将其误判为错误。

### 7. 优点
*   **细粒度筛选**：突破了“全盘接收或全盘舍弃”的局限，实现了子任务级别的精度。
*   **策略无关性**：GiB 作为一个数据预处理框架，可以适配任何模仿学习算法（如 BC、Diffusion Policy 等）。
*   **现实实用性**：直接针对非专家用户的自然错误进行建模，非常符合家庭机器人等实际应用场景。
*   **可视化能力**：利用马氏距离提供了良好的诊断工具，帮助理解演示质量。

### 8. 不足与局限
*   **分割依赖性**：目前依赖于固定的子任务数量 $k$ 和基于启发式的分割方法。如果用户在演示中反复重试或步骤乱序，启发式分割可能会失效。
*   **分布假设**：假设“好”数据占多数。如果采集到的数据中错误率极高（超过 50%），该方法可能会失效。
*   **超参数敏感**：参数 $\rho$（剔除子任务的数量）需要根据经验或先验知识调整，缺乏完全自动化的确定机制。
*   **实时性限制**：目前主要针对离线（Offline）模仿学习，尚未讨论在线实时纠错的可能性。

（完）
