---
title: "$π_0$-EqM: Equilibrium Matching for Closed-Loop Vision-Language-Action Control"
title_zh: $\pi_0$-EqM：用于闭环视觉-语言-动作控制的均衡匹配
authors: "Huanming Liu, Congsheng Xu, Jianmin Ji, Yao Mu"
date: 2026-05-22
pdf: "https://arxiv.org/pdf/2605.23128v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 使用平衡匹配实现闭环视觉-语言-动作控制
tldr: "当前视觉-语言-动作（VLA）模型采用固定采样步数的流匹配动作解码器，限制了状态依赖计算和时间重用。本文提出π0-EqM，将平衡匹配解码器替换流匹配专家，在不改变上游VLA结构下，在相同预算内提升任务成功率。在RoboTwin的19个任务上平均成功率从40.4%提升至50.2%，LIBERO-10达87.0%。实验揭示残差与成功率存在任务依赖的非单调关系，表明推理深度是策略设计的一部分，为可组合动作生成提供了新视角。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.23128v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 891, \"height\": 1033}, {\"url\": \"assets/figures/arxiv/2605.23128v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 861, \"height\": 564}, {\"url\": \"assets/figures/arxiv/2605.23128v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 855, \"height\": 657}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.23128v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1563, \"height\": 517}, {\"url\": \"assets/tables/arxiv/2605.23128v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 679, \"height\": 298}]"
motivation: 现有VLA模型使用固定采样步数，无法根据状态动态调整计算资源，限制了闭环控制中的时间重用和效率。
method: 提出π0-EqM，用平衡匹配（EqM）解码器替换π0中的流匹配专家，保留上游VLA栈不变，通过优化能量函数实现闭环动作生成。
result: "在300步预算下，RoboTwin成功率从40.4%升至50.2%，LIBERO-10达87.0%，且发现残差与成功率的非单调关系。"
conclusion: 迭代VLA控制的推理深度是策略设计关键，平衡匹配为不同任务和本体的可组合动作生成提供了基于能量的视角。
---

## 摘要
目前，视觉-语言-动作（VLA）模型因其在任务泛化方面的巨大潜力，已成为机器人操作最常用的范式。尽管大多数用于VLA控制的生成式流匹配动作解码器通常采用固定的采样步长，这限制了状态相关计算和跨控制周期的时间复用。我们提出$\pi_0$-EqM，它用均衡匹配（EqM）解码器替换了$\pi_0$中的流匹配专家，同时保持上游VLA栈不变。在匹配的300步预算下，$\pi_0$-EqM在19个任务上将RoboTwin的平均成功率从40.4%提升至50.2%，在LIBERO上保持竞争力，其中在LIBERO-10上提升最为明显（87.0%）。两次阈值扫描揭示了残差与成功率之间存在任务依赖的非单调关系，我们将其称为平稳性-可执行性差距。结果表明，迭代VLA控制中的推理深度是策略设计的一部分，并引入了基于能量的VLA视角，这可能为未来跨任务和具身的可组合动作生成工作提供启示。

## Abstract
Currently, Vision-Language-Action (VLA) models have become the most adopted paradigm for robotic manipulation for its great potential for task generalization. While most generative flow-matching action decoders for VLA control are often deployed with fixed sampling horizons, limiting state-dependent compute and temporal reuse across control cycles. We present $π_0$-EqM, which replaces the flow-matching expert in $π_0$ with an Equilibrium Matching (EqM) decoder while leaving the upstream VLA stack unchanged. Under a matched 300-step budget, $π_0$-EqM improves RoboTwin average success from 40.4% to 50.2% across 19 tasks and remains competitive on LIBERO, with its clearest gain on LIBERO-10 (87.0%). Two threshold scans reveal a task-dependent non-monotonic relation between residual and success, which we term the stationarity--executability gap. The results suggest that inference depth in iterative VLA control is part of policy design and introduce an energy-based VLA perspective that may inform future work on composable action generation across tasks and embodiments.

---

## 论文详细总结（自动生成）

# 论文详细中文总结：π0-EqM：用于闭环视觉-语言-动作控制的均衡匹配

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前主流的视觉-语言-动作（VLA）模型（如π0）使用**固定采样步数**的流匹配（Flow Matching）动作解码器，导致两个限制：一是无法根据机器人当前状态动态调整计算量（state-dependent compute）；二是无法在相邻控制周期之间有效复用先前的迭代中间结果（temporal reuse）。
- **整体含义**：该论文提出将解码器替换为**均衡匹配（EqM）**，使得迭代求解过程不再依赖显式的噪声时间步，从而将推理深度（迭代次数）作为可控的策略设计变量，而非固定的采样预算。这引入了**基于能量的VLA视角**，为跨任务和跨本体的可组合动作生成奠定了基础。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：学习一个**时间无关（自治）的条件向量场** \( f_\theta(A; c_t) \)，其根（零点）对应于目标动作块。通过迭代求解 \( f_\theta(A; c_t)=0 \) 来解码动作，从而实现自适应停止和跨周期热启动。
- **关键技术细节**：
  - **训练目标（EqM损失）**：给定演示动作块 \( A \) 和噪声 \( \varepsilon \)，定义插值 \( A_\gamma = \gamma A + (1-\gamma)\varepsilon \)。训练向量场 \( f_\theta \) 逼近目标向量 \( w(\gamma)(A-\varepsilon) \)，其中 \( w(\gamma) \) 在 \( \gamma \to 1 \) 时衰减至0，强制数据流形成为稳定平衡点。
  - **推理求解器**：采用**Nesterov加速梯度法**：
    \[
    \tilde{A}_t^{(k)} = A_t^{(k)} + \mu (A_t^{(k)} - A_t^{(k-1)})
    \]
    \[
    A_t^{(k+1)} = \tilde{A}_t^{(k)} - \eta f_\theta(\tilde{A}_t^{(k)}; c_t)
    \]
  - **自适应停止**：定义归一化残差 \( r_k = \|f_\theta(\tilde{A}_t^{(k)}; c_t)\|_2 / \sqrt{Hd} \)，当 \( r_k \leq \tau \) 或达到最大步数时停止。这使得测试时可调节计算量。
  - **结构化热启动**：利用相邻控制周期的相关性，将上一时刻输出的前一半动作块复制到当前初始化的前一半，后一半填充随机噪声：\( A_t^{(0)} = [A_{t-1,\text{copy}}^{(\text{out})}, \varepsilon_{\text{tail}}] \)，减少初始迭代次数。

## 3. 实验设计：数据集、基准、对比方法

- **数据集/场景**：
  - **RoboTwin**：19个任务，每个任务500条演示（100条干净+400条随机化），共9500条。
  - **LIBERO**：四个子套件（LIBERO-Spatial、LIBERO-Object、LIBERO-Goal、LIBERO-10），评估长期任务泛化。
- **基准**：原始 π0（流匹配解码器）作为直接对比基线。
- **对比方法**：仅在动作解码器层面替换（π0 vs π0-EqM），上游VLA栈（视觉编码器、语言条件、骨干网络）完全相同。推理预算匹配为300步（即300次迭代）。

## 4. 资源与算力

- **文中未明确说明**：未提及训练所用的GPU型号、数量、训练总时长。仅在推理时指定了300步的迭代预算。实验部分未涉及训练成本对比，故无法从论文中获取算力信息。

## 5. 实验数量与充分性

- **实验数量**：
  - 主要实验：19个RoboTwin任务的成功率对比（表1）；4个LIBERO套件成功率对比（表2）。
  - 阈值扫描实验：2个RoboTwin任务（place_dual_shoes和click_bell）的残差阈值-成功率曲线（图2）。
  - 定性分析：1个任务（click_alarmclock）的推理轨迹拆解（图3）。
- **充分性与公平性**：
  - **优点**：控制变量法严格——仅更换解码器，保证公平。匹配推理步数（300步），避免计算优势。
  - **不足**：阈值扫描仅覆盖2个任务，未对所有19个任务进行扫描，因此“非单调关系”的普遍性有待验证。未进行消融实验分离EqM目标、停止规则、热启动三个因素的各自贡献。缺乏对计算效率（实际耗时）的对比，因为EqM可能每步计算量不同。

## 6. 论文的主要结论与发现

- **主要结论**：
  - π0-EqM在300步推理预算下，19个RoboTwin任务平均成功率从40.4%提升至50.2%，提升9.8个百分点；12个任务提升，5个下降，2个持平。
  - LIBERO上整体保持竞争力（平均94.15% → 94.35%），尤其在长时任务LIBERO-10上提升1.8%（85.2% → 87.0%）。
  - 发现**平稳性-可执行性差距**（stationarity–executability gap）：残差最低的迭代步并不一定对应最佳执行效果；不同任务的最佳残差阈值不同（如place_dual_shoes在τ=1.0最优，click_bell在τ=0.1最优）。
  - 推理深度（迭代次数）是闭环VLA策略设计的一部分，而非纯粹的计算附随。

## 7. 优点

- **方法创新**：提出将推断深度作为可控策略变量，取代固定采样步数，赋予VLA模型自适应计算能力，更符合实际闭环控制需求。
- **能量视角**：将动作生成视为隐式能量最小化，为跨任务/本体的组合动作生成提供理论框架。
- **热启动机制**：利用时序相关性降低初始化距离，减少收敛所需迭代次数（Proposition 1给出理论解释），工程实用性强。
- **实验设计清晰**：严格保持上游不变，仅替换解码器，归因性强。匹配计算预算，避免不公平比较。

## 8. 不足与局限

- **阈值扫描覆盖不全**：仅对2个任务进行分析，无法确认“非单调关系”是否普遍存在于所有任务，降低了结论的泛化性。
- **理论分析局部化**：Proposition 1假设局部势能下降和局部收缩，但真实学习的向量场可能非保守、非全局凸，因此实际停止阈值选择仍需经验调节。
- **未分离贡献因素**：改进效果可能来自EqM目标本身、自适应停止、热启动三者共同作用，但未设计消融实验进行量化分离。
- **计算效率缺失**：虽然推理步数匹配，但EqM每步的向量场计算开销可能与流匹配不同，论文未比较实际运行时间或FLOPs，难以评估工程可行性。
- **未在真实机器人上验证**：仅在仿真环境（RoboTwin、LIBERO）测试，缺乏真实世界的泛化性和鲁棒性评估。

（完）
