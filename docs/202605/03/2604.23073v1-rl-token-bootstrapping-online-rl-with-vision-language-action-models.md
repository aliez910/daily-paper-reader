---
title: "RL Token: Bootstrapping Online RL with Vision-Language-Action Models"
title_zh: "RL Token: 利用视觉-语言-动作模型引导在线强化学习"
authors: "Charles Xu, Jost Tobias Springenberg, Michael Equi, Ali Amin, Adnan Esmail, Sergey Levine, Liyiming Ke"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.23073v1"
tags: ["query:rob-il"]
score: 10.0
evidence: 微调预训练的视觉-语言-动作（VLA）模型用于操纵任务
tldr: 本研究针对视觉-语言-动作（VLA）模型在现实任务中精度和速度不足的问题，提出了RL Token（RLT）方法。该方法通过在预训练VLA中引入紧凑的“RL token”作为特征接口，并结合小型Actor-Critic头部进行在线强化学习微调。实验表明，RLT仅需数小时的真实练习即可显著提升模型性能，在多项复杂机器人任务中实现了3倍的速度提升，甚至在部分任务中超越了人类的操作水平，为大型VLA的高效微调提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23073v1/fig-001.webp\", \"caption\": \"Fig. 3: The tasks in our experiments: each task contains a critical phase that requires high precision: (top) using a screwdriver to install a screw, (middle) fastening a zip tie, (bottom) plugging in an Ethernet cable and plugging in a charger.\", \"page\": 6, \"index\": 1, \"width\": 1074, \"height\": 514}, {\"url\": \"assets/figures/arxiv/2604.23073v1/fig-002.webp\", \"caption\": \"Fig. 4: RLT increases throughput significantly over the base VLA policy, improving both the speed and consistency of the critical phase of each task. The improvement is especially pronounced for the harder tasks where the VLA policy is prone to making mistakes.\", \"page\": 8, \"index\": 2, \"width\": 1061, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2604.23073v1/fig-003.webp\", \"caption\": \"Fig. 5: RLT can boost success rates across multiple tasks. Where the VLA is already competent (e.g., the Ethernet task) it maintains success rate and increases throughput. For tasks that are challenging for the base VLA policy (screwdriver and zip tie) RLT leads to a significant improvement in success.\", \"page\": 8, \"index\": 3, \"width\": 1061, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2604.23073v1/fig-004.webp\", \"caption\": \"Fig. 6: Comparison to other RL algorithms. We compare RLT against several baselines from the recent RL literature. Methods that consider only single actions, rather than action chunks, (HIL-SERL, PLD) perform poorly. DSRL leads to high success but significantly lags behind in throughput.\", \"page\": 8, \"index\": 4, \"width\": 512, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2604.23073v1/fig-005.webp\", \"caption\": \"Fig. 2: Details on RL token extraction. RLT adds an encoder-decoder transformer to a pretrained VLA. It produces a compressed embedding of the VLA representation (the RL token). This representation then enables data and parameter efficient fine-tuning during online RL.\", \"page\": 4, \"index\": 5, \"width\": 533, \"height\": 496}]"
motivation: 现有的视觉-语言-动作（VLA）模型在处理高精度和高速度要求的现实任务时，仍需通过强化学习进行进一步微调以提升性能。
method: 提出RL Token（RLT）方法，通过在VLA中引入紧凑的“RL token”表示并训练小型Actor-Critic头部，实现高效的在线强化学习微调。
result: 在螺丝安装、扎带固定等四项真实机器人任务中，RLT将任务速度提升了高达3倍，并在数小时内显著提高了成功率。
conclusion: RLT证明了即使是大型VLA模型，也能通过轻量化的在线强化学习在短时间内达到甚至超越人类的操作水平。
---

## 摘要
视觉-语言-动作（VLA）模型能够“开箱即用”地学习执行多种操作技能，但要达到现实世界任务所需的精度和速度，还需要进一步的微调——例如，通过强化学习（RL）。我们提出了一种轻量级方法，仅需数小时的现实世界练习，即可实现预训练 VLA 的高效样本在线强化学习微调。我们（1）对 VLA 进行调整以暴露一个“RL token”，这是一种紧凑的读取表示，在保留与任务相关的预训练知识的同时，作为在线强化学习的高效接口；（2）在此 RL token 上训练一个小型 actor-critic 头部以优化动作，同时将学习到的策略锚定在 VLA 上。使用 RL token（RLT）进行在线强化学习使得快速且高效地微调大型 VLA 成为可能。在四项真实机器人任务（螺丝安装、扎带紧固、充电器插入和以太网线插入）中，RLT 在任务最难部分的执行速度提高了多达 3 倍，并在几分钟到几小时的练习内显著提升了成功率。在某些任务上，它的速度甚至超过了人类远程操作。

## Abstract
Vision-language-action (VLA) models can learn to perform diverse manipulation skills "out of the box," but achieving the precision and speed that real-world tasks demand requires further fine-tuning -- for example, via reinforcement learning (RL). We introduce a lightweight method that enables sample-efficient online RL fine-tuning of pretrained VLAs using just a few hours of real-world practice. We (1) adapt the VLA to expose an "RL token," a compact readout representation that preserves task-relevant pretrained knowledge while serving as an efficient interface for online RL, and (2) train a small actor-critic head on this RL token to refine the actions, while anchoring the learned policy to the VLA. Online RL with the RL token (RLT) makes it possible to fine-tune even large VLAs with RL quickly and efficiently. Across four real-robot tasks (screw installation, zip tie fastening, charger insertion, and Ethernet insertion), RLT improves the speed on the hardest part of the task by up to 3x and raises success rates significantly within minutes to a few hours of practice. It can even surpass the speed of human teleoperation on some of the tasks.

---

## 论文详细总结（自动生成）

这篇论文由 Physical Intelligence 团队发表，提出了一种名为 **RL Token (RLT)** 的方法，旨在解决大型视觉-语言-动作（VLA）模型在处理高精度机器人任务时速度慢、精度不足的问题。

以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：虽然通用的 VLA 模型（如 $\pi_0$ 系列）具备强大的泛化能力，但在执行需要毫米级精度的任务（如插拔、拧螺丝）时，往往表现得动作迟缓、容易出错或需要反复尝试。
*   **核心挑战**：直接对拥有数十亿参数的 VLA 进行在线强化学习（Online RL）微调在计算上极其昂贵，且样本效率低下；而从头训练小型 RL 模型又会丢失 VLA 预训练的丰富先验知识。
*   **整体含义**：论文寻求一种“折中”方案，既能保留 VLA 的感知和行为先验，又能利用轻量级 Actor-Critic 架构实现快速的在线适应。

### 2. 论文提出的方法论
核心思想是**将 VLA 作为一个特征提取器和行为引导器**，通过引入一个紧凑的接口进行 RL 训练。
*   **RL Token 提取**：
    *   在冻结的 VLA 内部特征层之后添加一个轻量级的 **Encoder-Decoder Transformer**。
    *   Encoder 将 VLA 的高维 token 序列压缩成一个单一的、紧凑的向量——**RL Token**。
    *   通过重构 VLA 原始特征的损失函数进行预训练，确保 RL Token 保留了任务相关的关键信息。
*   **轻量级 Actor-Critic 头部**：
    *   在 RL Token 之上构建小型多层感知机（MLP）作为 Actor 和 Critic。
    *   **动作块细化（Action Chunk Refinement）**：Actor 不从零生成动作，而是接收 VLA 预测的动作块（Action Chunk）作为参考，并对其进行微调。
*   **关键技术细节**：
    *   **正则化（BC Regularizer）**：在 RL 目标函数中加入正则项，使 Actor 产生的动作不偏离 VLA 参考动作太远，确保探索的稳定性。
    *   **参考动作丢弃（Reference Action Dropout）**：训练时随机丢弃 VLA 的参考动作，强制 Actor 建立独立的生成路径，防止其只会简单地“复制”VLA。
    *   **异步更新**：采用高更新数据比（Update-to-Data Ratio = 5），在极少量的机器人交互数据下实现快速收敛。

### 3. 实验设计
*   **实验场景**：四项极具挑战性的真实机器人任务：
    1.  **螺丝安装**：需要亚毫米级对齐。
    2.  **扎带固定**：双臂协作处理柔性物体。
    3.  **以太网线插入**：对位置和角度极其敏感。
    4.  **充电器插入**：在低观测下进行盲插对齐。
*   **Benchmark 与对比方法**：
    *   **Base VLA**：未经 RL 微调的原始模型。
    *   **HIL-SERL**：不使用 VLA 先验，直接从 ResNet 特征进行 RL 训练。
    *   **PLD (Probe-Learn-Distill)**：学习单步残差动作的方法。
    *   **DSRL**：在扩散模型的潜空间进行引导的方法。
    *   **DAgger**：基于人类干预数据的模仿学习微调。

### 4. 资源与算力
*   **机器人练习时长**：论文强调了极高的样本效率，每个任务仅需 **15 分钟到 5 小时** 的实际机器人交互数据。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号和数量。但提到 Actor-Critic 头部是轻量级的 MLP（隐藏层维度 256-512），且 VLA 主干是冻结的，这暗示了该方法对实时计算的要求相对较低，可以在普通的机器人工作站上运行。

### 5. 实验数量与充分性
*   **实验规模**：每个任务评估了 50 个回合，涵盖了“关键阶段评估”和“全任务评估”。
*   **消融实验**：非常充分。作者分别验证了 **RL Token 的必要性**（对比 ResNet）、**动作块（Chunking）的作用**、**正则化项的影响**以及**参考动作输入（Pass-Through）的贡献**。
*   **客观性**：通过“吞吐量”（每 10 分钟成功次数）这一指标客观衡量了速度与成功率的综合提升，并对比了人类专家远程操作的速度，证明了其超越性。

### 6. 论文的主要结论与发现
*   **性能飞跃**：RLT 在所有任务中均显著提升了成功率。在最难的螺丝安装任务中，成功率从 20% 提升至 65%。
*   **速度提升**：在关键阶段，执行速度提升了 **3 倍**。
*   **超越人类**：在以太网线插入任务中，RLT 学习到了比人类演示更流畅、更快速的策略（如利用物理顺应性进行“晃动插入”），中位执行时间仅为人类的一半。
*   **高效性**：证明了通过压缩 VLA 特征作为 RL 接口是实现大型模型快速在线适应的有效路径。

### 7. 优点
*   **样本效率极高**：能在数小时内完成复杂任务的微调，非常适合现实世界的机器人部署。
*   **架构优雅**：通过 RL Token 解决了大模型微调的计算瓶颈，同时保留了预训练的语义理解能力。
*   **涌现行为**：RL 能够发现人类演示中不存在的高效策略（如利用接触力学），突破了模仿学习的上限。

### 8. 不足与局限
*   **人类干预依赖**：训练过程仍需要人类提供成功/失败的标签，以及在必要时进行干预纠错，尚未实现完全自主的强化学习。
*   **任务切分**：实验主要集中在任务的“关键阶段”（Critical Phase），虽然有全任务评估，但如何自动识别并触发 RL 策略与基础策略的切换仍有优化空间。
*   **泛化性验证**：虽然 VLA 本身泛化性强，但经过 RL 微调后的轻量级头部在面对未见过的环境扰动时，其鲁棒性是否会下降仍需更多验证。

（完）
