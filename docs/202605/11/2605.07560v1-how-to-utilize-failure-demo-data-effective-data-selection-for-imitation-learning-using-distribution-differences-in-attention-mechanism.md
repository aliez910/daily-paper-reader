---
title: "How to utilize failure demo data?: Effective data selection for imitation learning using distribution differences in attention mechanism"
title_zh: 如何利用失败演示数据？：利用注意力机制中的分布差异进行模仿学习的有效数据选择
authors: "Kana Miyamoto, Kanata Suzuki, Tetsuya Ogata"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07560v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 利用失败演示数据的机器人任务模仿学习
tldr: 针对机器人模仿学习中难以直接利用失败演示数据的问题，本文提出一种利用注意力机制分布差异的方法。通过学习成功与失败样本间的潜在表示差异，并在推理时选择合适的潜在模式，提高了动作稳定性。同时，引入一种后训练指标来筛选有益的失败数据，在不增加额外处理负担的情况下，显著提升了任务成功率和数据利用效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.07560v1/fig-001.webp\", \"caption\": \"Fig. 4. PB selection during inference using nearest-neighbor retrieval in the initial observation embedding space. Several points are shown with their corresponding initial observation images.\", \"page\": 11, \"index\": 1, \"width\": 717, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2605.07560v1/fig-002.webp\", \"caption\": \"Fig. 6. PCA visualization of PBs learned by the proposed method under {DS ,DF } (left) and {DS ,Dlow F } (right). Red and blue points denote PBs of successful and failed demonstrations, respectively.\", \"page\": 13, \"index\": 2, \"width\": 725, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2605.07560v1/fig-003.webp\", \"caption\": \"Fig. 5. Overview of failure data selection based on the KL metric. The left panel shows the PCA projection of all PBs, with red for successful PBs and blue-to-green for failure PBs according to their KL metric values. The blue-to-green gradient indicates increasing KL metric values and corresponds to the colors in the right panel, where failure samples are sorted by the metric averaged over five training runs relative to the minimum value.\", \"page\": 12, \"index\": 3, \"width\": 718, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2605.07560v1/fig-004.webp\", \"caption\": \"Fig. 2. Overview of the proposed method. The boxed area indicates the proposed modules added to the baseline ACT.\", \"page\": 5, \"index\": 4, \"width\": 688, \"height\": 684}]"
motivation: 传统的模仿学习主要依赖成功演示，而收集过程中产生的失败数据往往被浪费或需要复杂的额外处理才能利用。
method: 提出一种学习成功与失败差异潜在表示的方法，并结合注意力机制及一种基于注意力差异的数据筛选指标。
result: 仿真实验证明，该方法在包含失败数据的情况下提升了任务成功率，且所提指标能有效识别出对训练有益的失败样本。
conclusion: 该研究为机器人数据采集流程中更高效地利用各类演示数据提供了一种稳定且有效的技术方案。
---

## 摘要
机器人任务的模仿学习主要依赖于仅在成功演示上训练的策略，尽管在人类数据收集过程中失败是不可避免的。许多现有的利用失败数据的方法需要额外的数据处理或通过自主展开进行迭代策略更新，这使得直接且稳定地利用数据收集过程中积累的失败数据变得困难。在这项工作中，我们提出了一种学习成功与失败差异的潜在表示并将其整合到注意力机制中的方法。在推理过程中，从初始观察中选择合适的潜在模式以提高动作稳定性。此外，我们引入了一种训练后指标，该指标量化了每个失败样本与成功演示之间的注意力差异，以筛选失败数据。仿真结果表明，所提方法在利用失败数据训练时提高了任务成功率，且所提指标能够识别出与成功演示结合后对学习有益的失败样本。这些结果表明，所提方法可以支持在机器人数据收集流程中更有效地利用收集到的演示数据。

## Abstract
Imitation learning for robotic tasks has relied primarily on policies trained only on successful demonstrations, although failures are unavoidable during human data collection. Many existing approaches for exploiting failure data require additional data processing or iterative policy updates through autonomous rollouts, making it difficult to directly and stably utilize failure data accumulated during data collection. In this work, we propose a method that learns latent representations of success-failure discrepancies and incorporates them into the attention mechanism. During inference, an appropriate latent mode is selected from the initial observation to improve action stability. Furthermore, we introduce a post-training metric that quantifies the attention discrepancy between each failure sample and successful demonstrations to select failure data. Simulation results show that the proposed method improves task success rates when trained with failure data and that the proposed metric identifies failure samples that are beneficial for learning when combined with successful demonstrations. These results suggest that the proposed method can support more efficient use of collected demonstrations in robotic data collection pipelines.

---

## 论文详细总结（自动生成）

以下是对论文《How to utilize failure demo data?: Effective data selection for imitation learning using distribution differences in attention mechanism》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在机器人模仿学习（IL）中，如何直接且稳定地利用数据采集过程中不可避免产生的“失败演示数据”，而无需复杂的后期编辑、奖励函数设计或额外的自主探索迭代。
*   **研究动机**：传统的模仿学习高度依赖高质量的成功数据，收集成本高昂。虽然失败数据包含环境信息，但由于其动作分布与成功数据冲突，直接加入训练往往会导致模型性能下降或不稳定。因此，需要一种机制来区分成功/失败的潜在表示，并筛选出对学习有益的失败样本。

### 2. 方法论
该方法基于 **ACT (Action Chunking with Transformers)** 架构，提出了两个阶段的训练流程：

*   **核心思想**：利用**参数偏差（Parametric Bias, PB）**和**注意力机制的分布差异**来捕捉成功与失败样本之间的特征。
*   **关键技术细节**：
    1.  **参数偏差 (PB)**：为每个训练样本分配一个可学习的潜在向量（5维），并将其注入到 Transformer 解码器的自注意力层中，通过线性投影修改 Query 和 Value。
    2.  **KL 正则化 (KL Regularization)**：在训练损失中加入一个 KL 散度项。该项旨在拉大“当前样本的注意力分布”与“随机选择的异类标签样本注意力分布”之间的距离，从而迫使 PB 捕捉成功与失败在注意力模式上的差异。
    3.  **两阶段流程**：
        *   **阶段一（全量训练）**：在包含成功和失败的所有数据上训练，学习 PB 表示。
        *   **阶段二（数据筛选与重训）**：计算每个失败样本与所有成功样本之间的平均 KL 散度指标。筛选出 KL 指标较低（即与成功案例注意力模式较接近、可能是“差一点就成功”的近邻失败样本）的数据，与成功数据合并后重新训练模型。
    4.  **推理阶段**：通过 ResNet18 提取初始观察图像特征，利用最近邻检索找到最相似的成功训练样本，并使用其对应的 PB 引导动作生成。

### 3. 实验设计
*   **场景与任务**：使用 **robosuite** 仿真环境中的 **Lift（方块提升）** 任务，操作对象为 7 自由度的 Panda 机器人手臂。
*   **数据集**：共 100 条演示数据（50 条成功，50 条失败）。
*   **Benchmark 与对比方法**：
    *   **Baseline**：标准的 ACT 模型（仅在成功数据上训练，或在全量数据上直接训练）。
    *   **对比策略**：随机选择失败数据 vs. 基于 KL 指标选择失败数据（低 KL、中 KL、高 KL 组）。
*   **评估指标**：任务成功率（Success Rate）。

### 4. 资源与算力
*   **训练参数**：使用 AdamW 优化器，Batch Size 为 10，训练 10,000 个 epoch。
*   **算力说明**：论文中**未明确指出**具体的 GPU 型号和数量，也未提及具体的训练时长。但考虑到 ACT 模型和仿真环境的规模，通常单张消费级 GPU（如 RTX 3090/4090）即可完成此类实验。

### 5. 实验数量与充分性
*   **实验规模**：每个模型配置均使用了 **5 个不同的随机种子**进行训练，并且每个种子进行 **100 次 Rollout（测试）**，最终报告平均成功率和标准差。
*   **充分性评价**：
    *   **客观性**：通过对比随机选择和不同 KL 梯度的选择，验证了所提指标的有效性。
    *   **公平性**：实验涵盖了消融实验（如仅使用 PB 而不使用失败数据的情况），证明了改进不仅来自于模型容量的微增，更来自于对失败数据的有效利用。
    *   **局限性**：实验仅在一个相对简单的仿真任务（Lift）上完成，缺乏多任务或复杂长程任务的验证。

### 6. 主要结论与发现
*   **性能提升**：所提方法在利用全量数据时，成功率（77.8%）显著高于仅用成功数据的 ACT（71.0%）和直接混合数据的 ACT（71.6%）。
*   **筛选价值**：基于 KL 指标筛选出的“低 KL”失败样本对性能提升最明显（达到 79.4%），而“高 KL”样本甚至会损害性能。
*   **PB 的作用**：PB 能够有效区分成功和失败的注意力模式，且在推理时通过初始状态检索 PB 是一种有效的引导策略。

### 7. 优点
*   **非侵入性**：不需要修改机器人控制器的底层逻辑，也不需要人工介入进行数据修复。
*   **数据效率**：提供了一种量化指标来识别“高质量失败数据”，这对于大规模离线数据集的预处理具有重要参考价值。
*   **稳定性**：通过 KL 正则化解决了模仿学习中加入失败数据可能导致的动作分布偏移问题。

### 8. 不足与局限
*   **推理依赖检索**：推理时依赖于初始图像的最近邻检索来选择 PB，如果初始状态在训练集中未覆盖，检索到的 PB 可能不适用。
*   **任务复杂度有限**：仅在仿真环境的单一任务中验证，尚未在真实机器人或具有高度随机性的复杂场景中测试。
*   **计算开销**：两阶段训练流程（先全量训练以获得指标，再重训）增加了整体的训练时间成本。

（完）
