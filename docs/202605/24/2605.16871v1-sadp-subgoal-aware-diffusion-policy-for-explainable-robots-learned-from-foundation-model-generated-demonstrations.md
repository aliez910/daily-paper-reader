---
title: "SADP: Subgoal-Aware Diffusion Policy for Explainable Robots Learned from Foundation Model Generated Demonstrations"
title_zh: "SADP: 面向可解释机器人的子目标感知扩散策略——从基础模型生成的演示中学习"
authors: "Site Hu, Takato Horii"
date: 2026-05-16
pdf: "https://arxiv.org/pdf/2605.16871v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 子目标感知扩散策略用于模仿学习
tldr: 针对现有模仿学习缺乏子目标结构导致机器人可解释性不足的问题，本文提出SADP框架，利用基础模型自动生成带子目标标注的演示数据，训练以子目标为条件的扩散策略，并通过轻量级辅助头预测子目标完成状态，使机器人能在长时程操作中展示执行阶段。实验表明SADP在仿真和真实场景中均取得更高成功率，同时提供子目标级执行信号用于监控和故障诊断，证明内建可解释性与高性能可共存。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.16871v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of the SADP. SADP autonomously collects demonstrations paired with structured subgoal descriptions and completion signals to train an subgoal-aware diffusion policy with a completion prediction head.\", \"page\": 1, \"index\": 1, \"width\": 524, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2605.16871v1/fig-002.webp\", \"caption\": \"Fig. 5. Predicted subgoal completion scores for a representative successful trajectory of ItemInDrawer. The upper row shows the active subgoal over time, and the lower row shows the predicted completion score for the current subgoal. Lower scores indicate higher confidence that the current subgoal has been completed. The predicted scores decrease around the transitions between execution stages, indicating that the subgoal completion predictor is temporally aligned with the progress of the long-horizon task.\", \"page\": 6, \"index\": 2, \"width\": 512, \"height\": 436}, {\"url\": \"assets/figures/arxiv/2605.16871v1/fig-003.webp\", \"caption\": \"Fig. 6. Predicted subgoal completion scores for a representative failed trajectory of ItemInDrawer. The robot successfully completes the initial subgoals for aligning with and grasping the drawer handle, but the execution stalls during the drawer-opening subgoal. Because the gripper disengages from the handle during the opening motion, the completion of the third subgoal is not detected, and the active subgoal remains unchanged. This example illustrates how the predicted subgoal-level signal can help identify the stage at which the policy becomes stuck.\", \"page\": 7, \"index\": 3, \"width\": 512, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2605.16871v1/fig-004.webp\", \"caption\": \"Fig. 2. Subgoal-aware diffusion policy with completion prediction head. The completion prediction head shares the same encoded observation features with the diffusion policy and outputs a binary classification score indicating subgoal completion probability.\", \"page\": 4, \"index\": 4, \"width\": 874, \"height\": 496}, {\"url\": \"assets/figures/arxiv/2605.16871v1/fig-005.webp\", \"caption\": \"Fig. 4. Real-world experiment setup. The red circles indicate the RGB-D camera used in each task. The tasks range from pick-and-place to multi-stage manipulation involving object placement followed by box or drawer closing.\", \"page\": 5, \"index\": 5, \"width\": 529, \"height\": 460}]"
motivation: 现有模仿学习仅基于任务级演示训练，缺乏子目标建模，限制了机器人在长时程操作中展示和监控子任务执行的能力。
method: 提出SADP，利用基础模型自动生成子目标标注的演示数据，训练以任务级和子目标级描述为条件的扩散政策，并添加辅助头预测子目标完成状态。
result: 在RLBench仿真和UR5e真实机器人上，SADP实现了高于强任务条件扩散基线的任务成功率，并提供子目标级执行信号用于进度监控和故障诊断。
conclusion: 内建可解释性（子目标感知）可与高任务性能并存，通过显式建模子目标结构和执行状态实现可解释且高效的机器人学习。
---

## 摘要
可解释机器人不仅需要成功完成任务，还需要以用户友好的方式展示其内部决策过程。然而，大多数模仿学习方法仅基于任务级演示进行训练，没有显式建模子目标结构或执行进度。标准机器人学习数据集中子目标级监督的稀缺进一步加剧了这一限制，阻碍了能够传达其在长时程操作过程中正在执行的子任务的机器人的发展。为了解决这个问题，本文提出了子目标感知扩散策略（SADP），该框架利用基础模型自主生成带子目标标注的演示，并基于这些数据集训练扩散策略。SADP通过将动作生成条件设置为任务级和子目标级描述，将策略执行围绕可人类理解的子目标进行组织。一个轻量级的辅助头进一步预测子目标完成状态，使机器人能够暴露其当前执行阶段并监控子目标进展。在RLBench仿真和UR5e机器人上的真实世界评估实验表明，SADP相比强大的任务条件扩散基线取得了更高的任务成功率，同时提供了用于监控进展和诊断故障的子目标级执行信号。这些结果突显了内建的可解释性（而非事后可解释性）可以与高任务性能共存。

## Abstract
Explainable robots require not only successful task execution but also the ability to expose internal decision-making process in a user-friendly manner. However, most imitation learning methods are trained solely on task-level demonstrations, without explicitly modeling subgoal structure or execution progress. This limitation is further exacerbated by the scarcity of subgoal-level supervision in standard robot learning datasets, which restricts the development of robots that can convey the subtasks they are executing during long-horizon manipulation. To address this issue, this paper proposes Subgoal-Aware Diffusion Policy (SADP), a framework that leverages foundation models to autonomously generate subgoal-annotated demonstrations and trains diffusion policies on these datasets. SADP structures policy execution around human-interpretable subgoals by conditioning action generation on both task-level and subgoal-level descriptions. A lightweight auxiliary head further predicts subgoal completion states, allowing the robot to expose its current execution stage and monitor subgoal progression. Experiments in RLBench simulations and real-world evaluations on a UR5e robot demonstrate that SADP achieves higher task success rates than strong task-conditioned diffusion baselines, while providing subgoal-level execution signals for monitoring progress and diagnosing failures. These results highlight that built-in, rather than post-hoc, interpretability can coexist with high task performance.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：机器人需要通过模仿学习掌握长时程操作任务，但现有方法仅基于任务级演示训练，缺乏对子目标（subgoal）结构的显式建模和执行进度监控，导致机器人内部决策过程不透明，用户无法理解其当前执行阶段。
- **研究动机**：可解释性是人机信任的关键，尤其在长时程任务中。事后解释（post-hoc）方法往往与真实决策过程脱节；而内建可解释性（interpretable-by-design）要求策略本身具有人类可理解的结构。但机器人学习数据集普遍缺少子目标级标注，手工标注成本高、难以扩展。
- **整体含义**：本文探索如何在不牺牲任务性能的前提下，将人类可解释的自然语言子目标结构嵌入扩散策略，使机器人能够在执行过程中实时暴露当前子目标及其完成状态，实现透明且高性能的长时程操作。

## 2. 论文提出的方法论

### 2.1 核心思想
- 利用基础模型（LLM + VLM）自动将任务指令分解为有序的子目标列表，并执行这些子目标以生成带子目标标注和完成信号的演示数据。
- 训练子目标感知扩散策略（SADP），动作生成同时以任务级指令和当前子目标描述为条件；同时联合训练一个轻量级完成预测头，输出当前子目标是否完成的二分类信号。

### 2.2 关键技术细节
1. **子目标标注演示生成**：
   - 使用LLM将自然语言任务指令分解为子目标序列（如“抓取垃圾→复位→移动到垃圾箱上方→松开夹爪”）。
   - 采用基于affordance的自动数据采集框架：LLM+VLM提取目标物体点云和体素值映射，LLM生成启发式运动规划器逐子目标执行。
   - 每条轨迹按子目标序列分割，每个子目标的最后一帧标注为完成（0），其他帧标注为进行中（1）。仅保留成功执行的数据。
2. **子目标感知扩散策略**：
   - 观测编码：将机器人本体位姿历史、目标物体3D点云特征（通过轻量MLP编码器DP3）、任务和子目标文本嵌入（CLIP B/32）拼接为条件向量c。
   - 扩散过程：从高斯噪声开始，经K步去噪得到动作。条件通过FiLM机制融入。
   - 动作预测损失：标准噪声预测MSE损失（公式2）。
3. **完成预测头**：
   - 共享条件向量c，通过全连接层+sigmoid输出完成概率p。
   - 采用Focal Loss缓解正负样本不平衡（进行中样本远多于完成样本），参数β=0.25, γ=2。
   - 总损失：L_total = L_action + λ L_completion，λ从0逐渐增加至0.1。
4. **推理时执行**：
   - 在线通过LLM生成子目标序列（同数据生成阶段）。
   - 当完成概率p低于阈值τ（设为0.2）时，触发子目标切换。

## 3. 实验设计

### 3.1 数据集 / 场景
- **仿真**：RLBench中的6个长时程操作任务：
  - PutRubbishInBin（拾取-放置）
  - SlideBlockToTarget（推动）
  - MeatOffGrill（拾取-放置）
  - OpenDrawer（拉抽屉）
  - CloseDrawer（关抽屉）
  - PutItemInDrawer（开抽屉+放置物体）
- **真实世界**：UR5e机械臂+RealSense D435相机，3个任务：
  - SpongeInPlate（拾取-放置）
  - ToyInBoxClose（放置+关盒）
  - ToyInDrawerClose（放置+关抽屉）
- **数据量**：每个任务自动收集30个演示。

### 3.2 Benchmark与对比方法
- **基线1**：DP3（仅点云+任务条件扩散策略）
- **基线2**：TARAD（基于affordance的扩散策略，任务条件）
- **消融版本**：
  - SADP去掉子目标条件（保留完成预测头）
  - SADP使用oracle完成信号（代替预测信号）

### 3.3 评估指标
- 任务成功率（%）。仿真中：每个任务3个随机种子，每200epoch评估20个episode，取top3成功率平均；真实世界：最终检查点评估30个episode。

## 4. 资源与算力

- **论文未明确说明使用的GPU型号、数量、训练时长**。仅提及每个任务训练2000个epoch，未提供硬件配置细节。因此无法从文中获知具体算力投入。

## 5. 实验数量与充分性

- **实验数量**：
  - 仿真6个任务 × 3个种子，共18组独立训练；每个种子20 episode评估，共计360 episode（每个seed每200epoch评估）。
  - 真实世界3个任务 × 30 episode，共90 episode评估。
  - 消融实验：2种消融版本（去掉子目标条件、oracle完成信号）在6个仿真任务上重复（3 seed × 6 task）。
- **客观性与公平性**：
  - 对比基线均为强任务条件扩散策略，且在同一自动收集数据集上训练，控制变量较好。
  - 随机种子多次重复，报告均值标准差，符合标准。
  - 消融设计合理，能验证子目标条件和完成预测头的各自贡献。
- **充分性评价**：仿真任务覆盖了不同时间结构（拾取-放置、推动、抽屉交互、复合任务），具有一定多样性。真实世界3个任务验证了泛化性。但数据量偏小（每个任务仅30个演示），且仅使用一个仿真平台（RLBench）和一个真实平台（UR5e）。未在更复杂、更多样化的场景（如多物体、杂乱桌面、动态环境）中评估，可能存在偏差风险。

## 6. 论文的主要结论与发现

1. **集成子目标结构可提升长时程任务性能**：SADP在6个仿真任务上平均成功率82.8%，高于DP3（61.8%）和TARAD（82.0%），尤其在含顺序依赖的任务中提升显著。
2. **子目标级执行信号可以监控进度和诊断故障**：成功轨迹中完成分数随子目标切换下降；失败轨迹中信号可定位到卡住的子目标（如抓取手柄后脱离导致“打开抽屉”子目标无法完成）。
3. **子目标条件对性能至关重要**：去掉子目标条件后性能大幅下降（69.1%），甚至低于TARAD基线，说明子目标信息必须融入动作生成而非仅作为辅助信号。
4. **可靠的子目标转换估计是关键**：使用oracle完成信号可将平均成功率提升至87.1%，表明完成预测的准确性是改进方向。
5. **内建可解释性与高性能可以共存**：SADP在取得更高成功率的同时，能够输出人类可理解的子目标级执行状态。

## 7. 优点

1. **自动化数据生成**：利用基础模型自动分解子目标并标注完成信号，消除了昂贵的手工标注，具备良好可扩展性。
2. **内建可解释性**：子目标结构直接嵌入策略（条件生成+完成预测），而非事后解释，保证了忠实性。
3. **轻量高效**：SADP在基础模型生成的数据上训练轻量扩散策略，推理时不需依赖大模型，减少了非确定性。
4. **性能与透明兼得**：实验证明显式子目标结构反而提升了长时程任务成功率，反驳了“可解释会牺牲性能”的常见观点。
5. **消融实验设计完整**：清晰分离了“子目标条件”和“完成预测”各自的贡献，并分析了完成预测误差的影响。

## 8. 不足与局限

1. **实验覆盖有限**：
   - 仅使用RLBench仿真和UR5e真实平台，未在更多样化的基准（如MetaWorld、CALVIN）或不同机器人（如Franka、KUKA）上验证。
   - 任务数量较少（6仿真+3真实），且每个任务仅30个演示，对数据量变化和复杂场景的鲁棒性未知。
2. **完成预测可靠性不足**：
   - 不同子目标持续步数差异大，短子目标训练样本少，导致预测不准。文中也提到短子目标可能被跳过或重复。
   - 固定阈值τ=0.2可能无法适应所有任务，需手动调整。
3. **依赖基础模型**：数据生成依赖LLM和VLM（如GPT-4o、GroundingDINO），这些模型可能引入错误（如分解不准、感知失败），但论文未分析基础模型错误对最终性能的影响。
4. **子目标序列完全由LLM生成**：在推理时仍需调用LLM生成子目标列表，增加了延迟和不确定性；未评估子目标生成错误的鲁棒性。
5. **仅使用二分类完成标签**：无法提供连续进度（如完成百分比），未来可改进为连续回归。
6. **缺少用户研究**：论文虽强调“可解释性”，但未进行人机交互实验验证用户是否能真正理解子目标信号并建立信任。

（完）
