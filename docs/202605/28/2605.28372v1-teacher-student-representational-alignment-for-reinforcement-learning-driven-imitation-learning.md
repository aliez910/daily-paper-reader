---
title: Teacher-Student Representational Alignment for Reinforcement Learning-Driven Imitation Learning
title_zh: 基于强化学习驱动的模仿学习的师生表征对齐
authors: "Meraj Mammadov, Pedro Zuidberg Dos Martires, Johannes Andreas Stork"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28372v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 机器人模仿学习中师生表示对齐
tldr: 模仿学习利用强化学习教师策略，但教师依赖学生无法访问的特权状态信息，导致模仿差距。本文提出一种新算法，通过自监督对比学习构建共享嵌入空间，隐藏智能体特定观测，从设计上训练可模仿的教师策略。该空间限制梯度更新防止提取私有信息，并行训练教师策略。实验表明，该方法在多个领域显著缩小模仿差距，提升学生策略性能，无需微调。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.28372v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 842, \"height\": 838, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28372v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 645, \"height\": 528, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28372v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 875, \"height\": 296, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28372v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 654, \"height\": 334, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28372v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1814, \"height\": 555, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.28372v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 872, \"height\": 253, \"label\": \"Table\"}]"
motivation: 克服教师依赖特权状态信息导致的模仿差距，避免模仿学习后需强化学习微调的额外成本。
method: 基于自监督对比学习构建共享嵌入空间，隐藏智能体特定观测，限制梯度防止私有信息提取，并行训练教师策略。
result: 在多个示例领域，学生策略性能显著高于基线，模仿差距大幅减小。
conclusion: 该方法从设计上保证教师策略的可模仿性，有效缩小模仿差距，提升学生性能。
---

## 摘要
从基于状态的强化学习策略进行模仿学习是克服机器人技术中复杂高维观测空间维数灾难的常用方法。本文解决了当教师和学生策略分别学习时出现的不可约模仿鸿沟问题——教师策略可以自由依赖学生无法从其观测中推断出的特权状态信息。不同于在模仿学习后使用强化学习微调来改善学生性能（这通常需要全新的训练设置），我们提出了一种新算法，该算法学习一个隐藏智能体特定观测的共享嵌入空间，从而通过构造训练出可模仿的教师策略。我们与教师策略并行地使用自监督对比学习训练共享嵌入空间，并通过限制其梯度更新编码器网络来防止其提取私有信息。我们在多个示例领域上进行了评估，并与最先进的基线方法进行比较，结果表明我们的算法能够实现更高的学生性能，并大幅缩小模仿鸿沟。

## Abstract
Imitation learning (IL) from a state-based reinforcement learning (RL) policy is a common approach to overcome the curse of dimensionality in complex and high-dimensional observation spaces prevalent in robotics. This paper addresses the irreducible imitation gap that emerges when teacher and student are learned in isolation, and the teacher policy has the liberty to rely on privileged state information that the student cannot infer from its observations. Instead of improving poor student performance with RL finetuning after IL, which often requires a whole new training setup, we propose a novel algorithm which learns a shared embedding space that hides agent-specific observations and thus trains imitable teacher policies by construction. We train the shared embedding space with self-supervised contrastive learning in parallel to the teacher policy and prevent it from extracting private information by limiting its gradients from updating the encoder networks. We perform evaluations on several example domains and compare to state-of-the-art baselines showing that our algorithm enables higher student performance with substantially reduced imitation gap.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：在基于强化学习（RL）的模仿学习（IL）框架中，通常先训练一个使用低维状态观测的教师策略，再通过IL将其行为蒸馏到使用高维观测的学生策略。但当教师拥有学生无法获取的“特权状态信息”（如目标位置、障碍物精确坐标）时，教师会依赖这些私有信息做出决策，导致学生无法完美模仿——即出现不可约的**模仿鸿沟**。
- **现有方法不足**：常见的解决方案是在IL后对学生进行RL微调，但微调本身需要重新搭建RL训练环境，且容易出现策略退化。另一种方法修改奖励函数（如SITT），但引入难以调优的权衡系数，可能破坏任务目标。
- **本文目标**：从训练源头出发，通过构造共享表示空间，使教师策略**天然只使用学生也能观测到的信息**，从而让教师学会的行为自动可被学生模仿，无需事后微调或修改奖励函数。

## 2. 方法论

- **核心思想**：将教师和学生的观测视为同一环境状态的“多视图”表示，包含公共部分 \(c_t\) 和私有部分 \(p_t^T, p_t^S\)。使用自监督对比学习从两种观测中提取仅包含公共信息的共享嵌入空间 \(z_t\)。教师策略在该共享嵌入上训练RL任务，且梯度不反向传播到编码器，防止教师从私有信息中获利。学生则直接使用同一嵌入空间进行模仿。

- **关键技术细节**：
  - **共享嵌入学习**：采用InfoNCE损失，将同一时间步的教师和学生嵌入作为正样本对，不同时间步的嵌入作为负样本，迫使共享信息被保留，私有信息被丢弃。
  - **对齐损失**：额外添加显式的余弦相似度最大化损失，直接拉近正样本对嵌入距离，增强模仿的稳定性。
  - **稳定性损失**：为防止编码器更新导致策略网络的输入分布剧烈变化，保持当前嵌入与前一阶段嵌入的相似性（基于策略网络原始logits的余弦相似度），梯度仅更新编码器。
  - **训练流程**：两阶段交替进行——阶段1：用收集的教师轨迹数据集训练嵌入空间（损失函数为对比+对齐+稳定性）；阶段2：用RL训练策略网络在共享嵌入上最大化任务奖励，策略梯度仅更新策略网络，不更新编码器。

## 3. 实验设计

- **数据集/场景**：两个自定义仿真环境：
  - **TunnelVision**：网格世界，教师能观察到周围8个单元格，学生只能看到朝向方向的3个单元格，需频繁转向揭示障碍物。
  - **CollectHealth**：3D房间，教师知道所有健康包精确位置，学生只有有限视野的第一人称图像，需不断调整方向搜索。
- **基准方法**：
  - **Behavior Cloning (BC)**：教师单独训练，然后用教师轨迹对学生进行监督学习。
  - **SITT**（Student-Informed Teacher Training）：联合训练教师和学生，用KL散度惩罚教师进入学生无法模仿的状态，并修改奖励函数。
- **对比设置**：所有方法使用相同网络架构和相同环境交互步数。每个实验重复至少5个随机种子，报告平均成功率。

## 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量、训练时长等算力资源。仅提及使用PPO算法和标准网络结构。需要指出这一信息缺失。

## 5. 实验数量与充分性

- **实验数量**：在2个环境上对比了3种方法（BC、SITT、本文方法），并做了2组消融实验（去掉对齐损失、去掉稳定性损失）。共计 \(2 \times (1+1+1+2) = 10\) 个条件，每个5个随机种子，总约50次运行。还提供了成功率曲线（图5）和表格数据（表I）。
- **充分性**：实验设计较为充分——对比了最相关基线，消融实验验证了各损失项贡献，随机种子保证了统计可靠性。但环境数量较少（仅2个），且均为离散动作、低维观测空间的简化任务，对连续控制和高维（如图像输入）的泛化性尚未验证。因此实验在广度上略显不足。

## 6. 主要结论与发现

- 本文方法在TunnelVision上学生成功率**达到1.00**（模仿鸿沟0.00），在CollectHealth上学生成功率0.98（鸿沟0.01），远优于BC（鸿沟0.81/0.53）和SITT（鸿沟0.19/0.43）。
- 对齐损失和稳定性损失均对性能有贡献：去掉对齐损失在CollectHealth上导致成功率从0.98降至0.73；去掉稳定性损失在TunnelVision上学生成功率降至0.78（鸿沟增至0.01）。
- 教师性能几乎不受影响（成功率≥0.99），说明方法不牺牲教师任务表现。

## 7. 优点

- **任务不可知**：无需任何领域知识或人工特征工程，对比学习的正负样本自动从时间对齐的观测对中构造。
- **即插即用**：可直接集成到现有RL+IL框架，无需修改奖励函数或引入复杂超参数（仅温度参数τ可自动学习）。
- **从表示层面解决问题**：避免了奖励函数修改带来的风险（如reward hacking），直接从信息流限制教师依赖私有信息。
- **稳定性设计**：引入对齐和稳定性损失，有效缓解了并行训练中嵌入漂移对策略学习的影响。

## 8. 不足与局限

- **实验覆盖有限**：仅在两个离散动作、低维观测的仿真环境上验证，未在连续控制任务（如机器人操纵、无人机飞行）或真实机器人上测试，泛化性存疑。
- **计算资源未报告**：缺少训练时间和硬件信息，难以评估方法的实际效率。
- **对比基线不全面**：未与其他表示对齐方法（如VRIL、DIPC）或更先进的IL方法（如GAIL、SQIL）比较，仅与BC和SITT对比。
- **稳定性损失调参依赖**：稳定性损失系数未显式说明，可能对某些环境敏感；文中仅展示去除该损失的效果，未提供参数灵敏度分析。
- **理论保证有限**：虽然对比学习理论上能恢复共享信息，但实际中私有信息可能仍通过统计相关性泄漏，论文未分析该风险。
- **环境假设较强**：假设教师和学生的观测在时间上完全对齐（t时刻对应同一状态），实际部署中可能因延迟或异步导致对齐偏差。

（完）
