---
title: "Good in Bad (GiB): Sifting Through End-user Demonstrations for Learning a Better Policy"
title_zh: Good in Bad (GiB)：从终端用户演示中筛选以学习更优策略
authors: "Noushad Sojib, Momotaz Begum"
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01529v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 模仿学习使机器人能够从人类演示中获得多种技能
tldr: 针对非专家用户提供的模仿学习数据中包含错误的问题，本文提出GiB（Good-in-Bad）算法。该方法通过自监督学习提取潜在特征，并利用马氏距离自动识别并剔除演示中的错误子任务，同时保留高质量片段。在Franka机器人的仿真和真实多步任务中，GiB显著提升了从混合质量数据中学习策略的鲁棒性和性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01529v1/fig-001.webp\", \"caption\": \"Fig. 6. Success rate under different ρ values for Square and Mug tasks.\", \"page\": 7, \"index\": 1, \"width\": 529, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2605.01529v1/fig-002.webp\", \"caption\": \"Fig. 1. GiB subtask evaluation pipeline. (A) A self-supervised model learns latent embeddings zi and assigns binary weights wi ∈ {0, 1} to trajectories, with wi = 1 indicating high-quality demonstrations. (B) Trajectories are segmented into k subtasks (e.g. k = 3), and for each subtask j, a Gaussian distribution of the latent trajectory N (µj ,Σj) is estimated using high-quality trajectories only. (C) Subtasks are evaluated by computing the Mahalanobis distance between each timestep’s latent embedding and the corresponding subtask distribution. The top ρ subtasks with the largest deviation scores are labeled as bad (shown in red), while the remaining ones are labeled as good (shown in green).\", \"page\": 3, \"index\": 2, \"width\": 1004, \"height\": 566}]"
motivation: 非专家用户的演示常包含偶然错误，直接学习会导致不安全行为，而全盘舍弃整段演示则会造成数据浪费。
method: GiB利用自监督模型学习潜在特征，并通过马氏距离建模高质量片段的分布，从而自动识别并过滤掉演示中的低质量子任务。
result: 在Franka机器人的仿真和真实环境多步任务中，GiB显著提高了从混合质量人类演示中学习到的策略性能。
conclusion: GiB证明了通过细粒度地筛选演示中的高质量子任务，可以在不浪费数据的前提下训练出更鲁棒的机器人策略。
---

## 摘要
模仿学习为机器人从人类用户处获取多样化技能提供了一个极具前景的框架。然而，大多数模仿学习算法假设能够获取高质量的演示，但在从非专家用户收集数据时，这往往是一个不切实际的期望，因为他们的演示通常包含无意的错误。盲目地从这类演示中学习可能导致不安全的策略行为，而由于偶尔的错误就丢弃整个演示则会浪费宝贵的数据，尤其是在数据稀缺的情况下。在这项工作中，我们提出了 GiB (Good-in-Bad)，这是一种能够自动识别并剔除演示中错误子任务，同时保留高质量子任务的算法。过滤后的数据可用于任何策略学习算法，以训练更稳健的策略。GiB 首先训练一个自监督模型来学习潜在特征，并分配二进制权重将每个演示标记为“好”或“坏”。随后，它对高质量片段的潜在特征分布进行建模，并利用马氏距离（Mahalanobis distance）来检测和评估低质量子任务。我们在 Franka 机器人上通过仿真和真实世界的多步任务验证了 GiB，证明了在从质量参差不齐的人类演示中学习时，该算法能显著提升策略性能。

## Abstract
Imitation learning offers a promising framework for enabling robots to acquire diverse skills from human users. However, most imitation learning algorithms assume access to high-quality demonstrations an unrealistic expectation when collecting data from non-expert users, whose demonstrations often contain inadvertent errors. Naively learning from such demonstrations can result in unsafe policy behavior, while discarding entire demonstrations due to occasional mistakes wastes valuable data, especially in low-data settings. In this work, we introduce GiB (Good-in-Bad), an algorithm that automatically identifies and discards erroneous subtasks within demonstrations while preserving high-quality subtasks. The filtered data can then be used by any policy learning algorithm to train more robust policies. GiB first trains a self-supervised model to learn latent features and assigns binary weights to label each demonstration as good or bad. It then models the latent feature distribution of high-quality segments and uses the Mahalanobis distance to detect and evaluate poor-quality subtasks. We validate GiB on the Franka robot in both simulated and real-world multi-step tasks, demonstrating improved policy performance when learning from mixed-quality human demonstrations.

---

## 论文详细总结（自动生成）

这是一份关于论文《Good in Bad (GiB): Sifting Through End-user Demonstrations for Learning a Better Policy》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
在现实世界的“现场模仿学习”（Field Imitation Learning, FIL）中，非专家用户提供的演示数据往往包含无意间的错误（如手抖、误操作、疲劳导致的偏差）。
*   **研究动机**：现有的模仿学习算法大多假设数据是高质量的。如果直接学习包含错误的数据，会导致策略不安全；如果因为局部错误就丢弃整段演示，在数据稀缺的情况下又会造成极大浪费。
*   **核心问题**：如何从质量参差不齐的演示中，自动识别并保留高质量的“子任务片段”（Subtasks），同时剔除有害的错误片段，从而提升策略的鲁棒性。

### 2. 方法论：核心思想与技术细节
GiB 算法分为两个主要阶段，旨在实现细粒度的片段级数据过滤：

*   **第一阶段：自监督特征学习与轨迹加权**
    *   基于 BED（Behavior Cloning for Error Discovery）框架，通过最小化“不一致性损失”来学习潜在特征。
    *   **损失函数**包含三部分：动作一致性（预测动作与实际动作的偏差）、目标一致性（末端状态与平均末端状态的距离）、路径一致性（潜在轨迹与平均轨迹的偏差）。
    *   该阶段会为每条完整轨迹分配一个初步权重 $w_i \in [0, 1]$，识别出整体表现较好的演示。

*   **第二阶段：子任务评估与过滤**
    *   **分段（Segmentation）**：将轨迹划分为 $k$ 个子任务（基于物体交互或启发式脚本）。
    *   **分布建模**：利用第一阶段识别出的“好”轨迹，为每个子任务 $j$ 的潜在特征建立多元高斯分布 $\mathcal{N}(\mu_j, \Sigma_j)$。
    *   **异常检测**：计算所有演示片段中每个时间步的潜在特征与对应分布的**马氏距离（Mahalanobis distance）**。
    *   **筛选逻辑**：根据平均偏差得分对子任务进行排序，剔除得分最高的 $\rho$ 个片段（标记为坏），保留其余片段（标记为好）。

### 3. 实验设计
*   **实验场景**：
    *   **仿真环境（MimicGen）**：包含 Square（方块套圈）、Coffee（放咖啡胶囊）、Mug Cleaning（清理杯子）、Kitchen（长程厨房任务，含7个子任务）。
    *   **真实世界**：使用 Franka 机器人执行“将西兰花放入抽屉并关上”的任务。
*   **数据集**：混合了专家演示和 5 名非专家用户的真实演示（包含自然发生的错误）。
*   **对比基准（Baselines）**：
    *   **Oracle**：基于真值的完美过滤。
    *   **BED**：仅进行轨迹级过滤。
    *   **DemInf、S2I、LOF**：其他先进的数据筛选或异常检测方法。
    *   **All data**：不进行任何过滤。
*   **下游策略**：统一使用 **Diffusion Policy** 作为策略学习算法。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或总训练时长。
*   **模型细节**：视觉编码器使用 ResNet-18，低维输入使用 MLP，Diffusion Policy 训练了 200（仿真）到 800（真实）个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2 步到 7 步的不同复杂度任务。每个模型使用 **3 个不同的随机种子**进行训练，并取平均成功率。
*   **充分性评估**：
    *   **多维度验证**：不仅对比了成功率，还通过可视化马氏距离曲线展示了算法的解释性（如机器人如何从错误中恢复）。
    *   **消融实验**：对超参数 $\rho$（剔除片段的数量）进行了灵敏度分析。
    *   **多样性测试**：专门设计了多模态实验，证明 GiB 在处理多种正确操作方式时不会误判。
    *   **客观性**：对比了多种 SOTA 过滤算法，实验设计较为公平。

### 6. 主要结论与发现
*   **片段级过滤优于轨迹级**：GiB 通过保留“坏演示中的好片段”，比直接丢弃整条轨迹（如 BED）保留了更多有效信息，显著提升了成功率。
*   **复杂任务优势明显**：在长程任务（如 Kitchen）中，GiB 的表现接近或达到了 Oracle 水平。
*   **鲁棒性强**：在真实世界实验中，GiB 过滤后的数据使机器人成功率从 15%（全数据）提升至 70%。
*   **可解释性**：马氏距离的波动能准确反映操作过程中的错误发生及随后的恢复过程。

### 7. 优点
*   **数据效率高**：最大化利用了非专家用户提供的数据，减少了数据采集的成本。
*   **策略无关性**：GiB 作为一个数据预处理框架，可以适配任何模仿学习算法（如 BC、Diffusion Policy）。
*   **无需人工标注**：通过自监督学习和启发式分段，实现了自动化的质量评估。

### 8. 不足与局限
*   **分段依赖性**：目前依赖于固定数量的子任务划分和启发式脚本，如果用户在演示中反复尝试（如失败后重试），分段可能会错位。
*   **假设限制**：算法假设“好演示占多数”，如果数据集中错误演示占绝大多数，模型可能会学习到错误的分布。
*   **超参数敏感**：需要手动调节剔除比例 $\rho$，这在完全未知的任务中可能需要经验调试。

（完）
