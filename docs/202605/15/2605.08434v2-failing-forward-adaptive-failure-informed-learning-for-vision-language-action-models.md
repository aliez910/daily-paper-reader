---
title: "Failing Forward: Adaptive Failure-Informed Learning for Vision-Language-Action Models"
title_zh: 败中求进：针对视觉-语言-动作模型的自适应失败告知学习
authors: "Meng Zheng, Samhita Marri, Anwesa Choudhuri, Benjamin Planche, Zhongpai Gao, Van Nguyen Nguyen, Terrence Chen, Girish Chowdhary, Ziyan Wu"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08434v2"
tags: ["query:rob-il"]
score: 10.0
evidence: VLA 模型的失败告知学习
tldr: 针对视觉-语言-动作（VLA）模型在行为克隆中因缺乏纠错信号而导致的鲁棒性不足问题，本文提出了自适应失败告知学习（AFIL）框架。该框架利用预训练模型在线生成失败轨迹，并训练共享骨干网络的双重动作生成器以提供负向引导。在采样阶段，AFIL根据成功与失败分布的差异自适应地调整动作，显著提升了机器人在复杂任务中的成功率和抗干扰能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08434v2/fig-001.webp\", \"caption\": \"Figure 3: Visualization of in-domain (left) and out-of-domain (right) manipulation task setup. Row 1: exterior camera image, row 2: wrist camera image. See supplementary for more visualizations.\", \"page\": 8, \"index\": 1, \"width\": 738, \"height\": 219}, {\"url\": \"assets/figures/arxiv/2605.08434v2/fig-002.webp\", \"caption\": \"Figure 2: Dual Action Generator (DAG)-VLA with Adaptive Failure-Informed Learning (AFIL).\", \"page\": 5, \"index\": 2, \"width\": 824, \"height\": 234}, {\"url\": \"assets/figures/arxiv/2605.08434v2/fig-003.webp\", \"caption\": \"Table 4: Performance comparison on out-of-domain manipulation tasks. Success rates (%) reported over 25 configs × 3 runs for each task.\", \"page\": 9, \"index\": 3, \"width\": 764, \"height\": 191}, {\"url\": \"assets/figures/arxiv/2605.08434v2/fig-004.webp\", \"caption\": \"Figure 1: Overview of the proposed Adaptive Failure-Informed Learning (AFIL) pipeline. AFIL uses online-generated failure trajectories as adaptive negative guidance, steering action generation away from failure-prone regions and toward more reliable success modes.\", \"page\": 2, \"index\": 4, \"width\": 782, \"height\": 293}]"
motivation: 现有的VLA模型主要依赖成功案例的克隆，导致其在面对微小执行误差时容易陷入不可恢复的失败状态。
method: 提出AFIL框架，通过在线生成失败轨迹并训练双重动作生成器，在采样时利用失败分布作为负向引导来修正动作。
result: 在多种域内和跨域的机器人操纵任务中，AFIL相比现有基准模型显著提高了任务成功率和鲁棒性。
conclusion: AFIL证明了利用失败经验作为负向反馈能有效增强VLA模型的泛化能力和执行可靠性。
---

## 摘要
视觉-语言-动作（VLA）模型为可扩展的机器人操控提供了一种极具前景的范式，然而它们对仅包含成功案例的行为克隆的依赖使其变得脆弱；由于缺乏纠正性训练信号，微小的执行误差会迅速累积成不可恢复的分布外失败。为了解决这一局限性，我们提出了自适应失败告知学习（AFIL），这是一个端到端框架，利用失败轨迹作为基于扩散和流的 VLA 策略的自适应负向引导。AFIL 使用预训练的 VLA 在线生成失败展开，避免了对手工设计失败模式或人工在环恢复的需求。随后，它在共享通用视觉-语言主干网络的同时，联合训练针对成功和失败行为的双动作生成器（DAGs），从而以有限的参数开销实现高效的失败感知策略学习。在采样过程中，失败生成器自适应地引导动作生成远离易出错区域，并转向更可靠的成功模式，引导强度由每个扩散步骤中成功与失败分布之间的距离决定。在域内和跨域机器人操控任务（涵盖短时程和长时程设置）上的实验表明，AFIL 相比现有的 VLA 基准模型一致地提高了任务成功率和鲁棒性，证明了其有效性、效率和通用性。

## Abstract
Vision-language-action (VLA) models provide a promising paradigm for scalable robotic manipulation, yet their reliance on success-only behavioral cloning leaves them brittle; lacking corrective training signals, minor execution errors rapidly compound into unrecoverable, out-of-distribution failures. To address this limitation, we propose Adaptive Failure-Informed Learning (AFIL), an end-to-end framework that leverages failure trajectories as adaptive negative guidance for diffusion- and flow-based VLA policies. AFIL uses a pretrained VLA to generate failure rollouts online, avoiding the need for handcrafted failure-mode design or human-in-the-loop recovery. It then jointly trains Dual Action Generators (DAGs) for successful and failed behaviors while sharing a common vision-language backbone, enabling efficient failure-aware policy learning with limited parameter overhead. During sampling, the failure generator adaptively steers action generation away from failure-prone regions and toward more reliable success modes, with guidance strength determined by the per-diffusion-step distance between success and failure distributions. Experiments across in-domain and out-of-domain robotic manipulation tasks, covering both short- and long-horizon settings, show that AFIL consistently improves task success rates and robustness over existing VLA baselines, demonstrating its effectiveness, efficiency, and generality.

---

## 论文详细总结（自动生成）

这是一份关于论文《Failing Forward: Adaptive Failure-Informed Learning for Vision-Language-Action Models》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型主要依赖“仅成功案例”的行为克隆（Behavioral Cloning）。这种方式导致模型在面对微小执行误差时非常脆弱，因为它们从未学习过如何从错误状态中恢复，误差会迅速累积导致“分布外”（OOD）失败。
*   **研究背景**：虽然学习失败经验（Learning from Failure）已被证明能提高鲁棒性，但现有方法通常依赖手工设计的失败模式（如故意让抓取滑落）或需要人工干预纠错，这限制了模型在大规模场景下的扩展性和通用性。

### 2. 方法论：自适应失败告知学习（AFIL）
*   **核心思想**：利用在线生成的失败轨迹作为“负向引导”（Negative Guidance），在推理采样阶段动态地将动作生成导向成功区域，远离失败区域。
*   **关键技术细节**：
    *   **双重动作生成器（DAG）**：在共享同一个预训练 VLM（如 $\pi_{0.5}$）骨干网络的基础上，设计了两个轻量级的动作生成头：一个学习成功分布 $p_{succ}$，另一个学习失败分布 $p_{fail}$。
    *   **在线失败数据生成**：不依赖手工设计，而是直接运行预训练的 VLA 模型，收集其在任务中的失败尝试，并利用运动规划器（Motion Planner）对部分失败进行修正，形成“成功+失败+修正”的混合数据集。
    *   **自适应负向引导采样**：在扩散（Diffusion）或流匹配（Flow-matching）的每一步采样中，计算成功头和失败头预测的噪声/速度向量。
    *   **公式逻辑**：最终动作方向由成功预测减去加权的失败预测决定。权重 $\hat{\lambda}_\eta$ 是自适应的，基于成功与失败预测向量之间的**余弦相似度**：当两者方向差异大（即失败信号明确）时，增强负向引导强度。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **ManiSkill**：涵盖短时程（如叠方块、提木栓）和长时程（如摆放碗筷、分类水果）任务。
    *   **LIBERO**：标准的机器人学习基准测试，包含 Spatial、Object、Goal 和 Long 四个子集。
*   **对比基准（Benchmarks）**：
    *   主流 VLA 模型：OpenVLA、Octo、$\pi_{0.5}$、CoT-VLA。
    *   消融变体：仅成功训练、成功+失败修正训练、静态失败引导（固定权重）。
*   **评估指标**：任务成功率（Success Rate）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确给出**具体的 GPU 型号、数量及总训练时长。
*   **模型规模**：实验基于 $\pi_{0.5}$ 架构（通常为数十亿参数级别），提到 DAG 架构仅增加了极少的参数开销（因为共享了 VLM 骨干）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 ManiSkill 中，每个任务评估 150 次运行（50 个配置 $\times$ 3 次重复）。
    *   在 LIBERO 中，遵循标准协议，跨 40 个任务进行 500 次评估。
    *   包含针对不同引导尺度 $\alpha$ 的消融实验。
*   **充分性评价**：实验设计较为充分。不仅涵盖了域内（In-domain）任务，还专门设置了**分布外（OOD）**测试（如未见过的物体几何形状、杂乱环境），验证了模型在复杂现实场景下的泛化能力。对比方法涵盖了从传统扩散策略到最新的大模型基准，具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：AFIL 在所有任务中均优于仅使用成功数据的基准模型。在长时程任务中提升尤为显著（例如在 ManiSkill 某任务中比静态引导高出 12%）。
*   **自适应的重要性**：实验证明，动态调整负向引导权重比固定权重更有效。在动作初期（成功与失败路径重合时）减小引导，在关键分歧点（失败模式显现时）加大引导，能有效避免误导。
*   **鲁棒性增强**：在面对未见过的物体和干扰物时，AFIL 表现出更强的纠错和恢复能力。

### 7. 优点与亮点
*   **无需人工干预**：通过在线 rollout 自动获取失败数据，解决了失败数据获取难、成本高的问题。
*   **参数高效**：通过共享骨干网络，仅需训练微小的动作头即可获得显著的性能增益。
*   **模型无关性**：该方法可无缝集成到任何基于扩散或流匹配的 VLA 模型中。
*   **数学优雅性**：利用余弦相似度进行自适应引导，直观且计算开销低。

### 8. 不足与局限
*   **推理开销**：虽然参数增加不多，但推理时需要同时运行两个动作生成头，可能会略微增加推理延迟（Latency），这对于高频实时控制（如 100Hz 以上）可能存在挑战。
*   **失败数据的质量依赖**：如果在线生成的失败轨迹过于单一，可能无法覆盖所有潜在的失败模式。
*   **算力细节缺失**：缺乏具体的训练资源消耗说明，不便于其他研究者评估复现成本。

（完）
