---
title: "FrameSkip: Learning from Fewer but More Informative Frames in VLA Training"
title_zh: FrameSkip：在 VLA 训练中从更少但更具信息量的帧中学习
authors: "Bin Yu, Shijie Lian, Xiaopeng Lin, Zhaolong Shen, Yuliang Wei, Changti Wu, Hang Yuan, Haishan Liu, Bailing Wang, Cong Huang, Kai Chen"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13757v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 改进VLA训练以应对操纵关键转折点
tldr: "针对视觉-语言-动作（VLA）模型训练中密集轨迹导致的样本不平衡问题，本文提出FrameSkip框架。该方法通过动作变化、视觉一致性等指标对帧进行评分，筛选出关键动作转换帧（如抓取、释放）进行训练。FrameSkip在不改变模型架构的前提下，仅保留20%的帧便在多个基准测试中显著提升了任务成功率，实现了更高效的机器人策略学习。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.13757v1/fig-001.webp\", \"caption\": \"Figure 3: FRAMESKIP pipeline. FRAMESKIP scores frames in each demonstration trajectory, retains highimportance frames under a target ratio, and injects the compressed trajectory view into VLA training through dataloader index remapping while leaving the model and inference procedure unchanged.\", \"page\": 4, \"index\": 1, \"width\": 761, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2605.13757v1/fig-002.webp\", \"caption\": \"Table 7: Results of evaluating the VLA models with the GR1 robot in the RoboCasa-GR1 Tabletop simulation environment. The results for Isaac-GR00T N1.5 and Isaac-GR00T N1.6 are sourced from the official Isaac-GR00T GitHub repository (NVIDIA et al., 2025b). We highlight the best results in bold and the second-best results with underline.\", \"page\": 12, \"index\": 2, \"width\": 950, \"height\": 1067}]"
motivation: 传统的VLA训练对所有帧等同视之，导致低变化片段占据主导，而关键的操纵转换环节样本稀疏。
method: 提出一种数据层框架，利用动作变化、视觉-动作相干性及任务进度先验等指标筛选高信息量帧。
result: "在RoboCasa-GR1等三个基准测试中，仅保留20%帧的情况下，平均成功率从66.50%提升至76.15%。"
conclusion: 通过智能筛选关键帧，可以在大幅减少训练样本的同时，显著提高VLA策略的学习效率和性能。
---

## 摘要
视觉-语言-动作（VLA）策略通常从密集的机器人演示轨迹中训练，这些轨迹通常通过远程操作收集，并采样每个记录的帧，就好像它们提供了同等有用的监督。我们认为这种惯例造成了时间监督的不平衡：长时间的低变化片段主导了训练流，而诸如对齐、接触、抓取和释放等关键操作转换则出现得非常稀疏。我们引入了 FrameSkip，这是一个数据层帧选择框架，它利用动作变化、视觉-动作一致性、任务进度先验和夹具转换保留来对轨迹帧进行评分，然后在目标保留率下将训练样本重新映射到高重要性帧。由于 FrameSkip 仅在数据加载器中运行，它保持了 VLA 架构、动作头、训练目标和推理过程不变。在 RoboCasa-GR1、SimplerEnv 和 LIBERO 上的实验表明，FrameSkip 在成功率与保留率的权衡上优于全帧训练和更简单的帧选择变体，在三个基准测试中实现了 76.15% 的宏平均成功率，而全帧训练为 66.50%，同时在主要设置中使用了仅保留 20% 唯一帧的压缩轨迹视图。

## Abstract
Vision-Language-Action (VLA) policies are commonly trained from dense robot demonstration trajectories, often collected through teleoperation, by sampling every recorded frame as if it provided equally useful supervision. We argue that this convention creates a temporal supervision imbalance: long low-change segments dominate the training stream, while manipulation-critical transitions such as alignment, contact, grasping, and release appear only sparsely. We introduce FrameSkip, a data-layer frame selection framework that scores trajectory frames using action variation, visual-action coherence, task-progress priors, and gripper-transition preservation, then remaps training samples toward high-importance frames under a target retention ratio. Because FrameSkip operates only in the dataloader, it leaves the VLA architecture, action head, training objective, and inference procedure unchanged. Across RoboCasa-GR1, SimplerEnv, and LIBERO, FrameSkip improves the success-retention trade-off over full-frame training and simpler frame selection variants, achieving a macro-average success rate of 76.15% across the three benchmarks compared with 66.50% for full-frame training while using a compressed trajectory view that retains 20% of unique frames in the main setting.

---

## 论文详细总结（自动生成）

这篇论文提出了 **FrameSkip**，一种针对视觉-语言-动作（VLA）模型训练的帧选择框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：传统的 VLA 训练通常对机器人演示轨迹中的所有帧进行均匀采样。然而，机器人轨迹在时间上存在严重的“监督不平衡”：大部分时间是低变化的冗余动作（如接近物体、平稳运输），而关键的操纵转折点（如对齐、接触、抓取、释放）仅占极小比例。
*   **研究动机**：均匀采样会导致模型在简单的平滑运动上过度拟合，而在关键的交互阶段由于样本稀疏而表现脆弱。论文旨在通过重新分配时间监督，让模型从更少但更具信息量的帧中学习，从而提高训练效率和任务成功率。

### 2. 方法论
FrameSkip 是一个位于数据加载层（Dataloader）的框架，其核心思想是**根据重要性分数对帧进行剪枝和重映射**。
*   **重要性评估指标**：
    1.  **动作变化重要性 (AVI)**：计算当前动作与前一动作的差异，并结合未来几步的动作方差，捕捉动作剧烈变化的时刻。
    2.  **视觉-动作相干性 (VAC)**：利用 DINOv2 提取视觉特征，计算视觉变化与动作变化的比例，识别那些动作虽小但引起环境显著变化的时刻（如接触物体）。
    3.  **任务进度先验 (TPI)**：利用高斯混合模型 (GMM) 拟合关键阶段（如抓取点）在轨迹进度中的分布，为关键阶段提供更高的基础权重。
    4.  **夹具转换保留**：强制保留夹具状态改变（开/关）的帧。
*   **训练策略**：
    *   **索引重映射**：不改变模型架构，仅在数据加载时将原始索引映射到保留的高重要性帧索引。
    *   **热身与锚点**：训练初期使用全帧（Warmup）以建立基础感知；随后切换到剪枝模式（如保留 20% 的帧），并定期插入全帧作为“锚点”以保持全局上下文。

### 3. 实验设计
*   **数据集与场景**：
    1.  **RoboCasa-GR1**：双臂灵巧手操作，包含 24 个桌面任务。
    2.  **SimplerEnv**：WidowX 机械臂，测试在 BridgeV2 数据集训练后的跨域泛化能力。
    3.  **LIBERO**：Franka 机械臂，包含 Spatial、Object、Goal、Long 四个任务套件。
*   **对比方法**：
    *   **全帧训练 (Full-Frame Training)**：基准线。
    *   **随机剪枝 (Random)**：作为对照，验证重要性采样的必要性。
    *   **SOTA 模型参考**：对比了 OpenVLA、$\pi_0$、GR00T 等现有先进 VLA 系统的性能。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H100 GPU**。
*   **训练细节**：
    *   使用 DeepSpeed ZeRO-2 优化器。
    *   RoboCasa-GR1 训练 100K 步，SimplerEnv 训练 60K 步，LIBERO 训练 30K 步。
    *   全局 Batch Size 统一为 128。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个不同的机器人平台和仿真环境上进行了广泛测试，涵盖了单臂、双臂、灵巧手等多种形态。
*   **消融实验**：非常充分。作者研究了：
    *   **保留率 (r)**：从 10% 到 60% 的不同压缩比例对性能的影响。
    *   **指标贡献**：分别验证了 AVI、VAC、TPI 等不同评分组件的增益。
    *   **热身步数**：测试了不同 Warmup 长度对稳定性的影响。
*   **公平性**：所有对比实验均在相同的模型架构（Qwen2-VL + Diffusion Transformer）和训练预算下进行，确保了结果的客观性。

### 6. 主要结论与发现
*   **性能提升**：在仅保留 20% 唯一帧的情况下，FrameSkip 在三个基准测试中的宏平均成功率从 **66.50% 提升至 76.15%**。
*   **“少即是多”**：实验证明，消除 80% 的冗余帧不仅不会降低性能，反而能通过集中监督关键转换点来显著增强策略的鲁棒性。
*   **泛化能力**：在 SimplerEnv 的跨域测试中，FrameSkip 表现出比全帧训练更强的泛化性能。

### 7. 优点
*   **架构无关性**：作为数据层插件，它可以直接应用于任何 VLA 模型（如基于 Transformer 或 Diffusion 的模型），无需修改模型代码。
*   **高效性**：显著减少了训练时的有效数据吞吐量，同时提升了模型在关键操作环节（如抓取）的成功率。
*   **多模态融合评分**：结合了动作、视觉和任务进度三个维度的信息，比单纯的动作差分采样更科学。

### 8. 不足与局限
*   **预处理开销**：虽然训练变快了，但需要提前计算 DINOv2 特征和 GMM 先验，增加了一定的离线预处理负担。
*   **超参数敏感性**：虽然论文给出了推荐值，但针对全新的机器人任务，可能仍需要微调不同评分指标的权重（$\alpha, \beta, \gamma$）。
*   **长程依赖风险**：极端的帧剪枝（如 10% 以下）可能会破坏轨迹的连续性，导致模型难以学习需要极长时序上下文的任务。

（完）
