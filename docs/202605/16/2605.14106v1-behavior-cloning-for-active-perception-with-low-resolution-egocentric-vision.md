---
title: Behavior Cloning for Active Perception with Low-Resolution Egocentric Vision
title_zh: 基于低分辨率第一人称视角视觉的主动感知行为克隆
authors: "Anthony Bilic, Chen Chen, Ladislau Bölöni"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.14106v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 基于第一视角RGB图像的闭环控制行为克隆
tldr: 本研究探讨了行为克隆在低分辨率第一视角视觉下实现主动感知的有效性。研究使用配备腕部相机的低成本机械臂，通过闭环控制直接从低分辨率RGB图像预测关节指令，完成寻找并居中目标植物的任务。实验证明，相对关节增量预测优于绝对位置预测，且低分辨率视觉足以支持可靠的任务执行，展示了主动感知能力可从行为克隆中涌现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.14106v1/fig-001.webp\", \"caption\": \"Fig. 2: Model architecture (left) and joint configuration update (right).\", \"page\": 2, \"index\": 1, \"width\": 1052, \"height\": 206}]"
motivation: 验证行为克隆是否足以在结构化物体寻找任务中产生主动感知能力。
method: 采用低成本机械臂和腕部相机，通过闭环控制模型直接从低分辨率RGB图像预测关节指令。
result: 实验发现低分辨率视觉足以完成任务，且预测相对关节增量比预测绝对位置效果更好。
conclusion: 结果表明在可重复的设置下，视觉驱动的主动感知能力可以通过行为克隆成功训练出来。
---

## 摘要
我们研究了在结构化物体寻找任务中，行为克隆是否足以产生主动感知。一台配备腕部第一人称视角 RGB 摄像头的低成本机械臂必须重新定位，使部分可见的植物居中，然后触发抓取信号，这需要能够改善未来观测的动作。该模型在闭环控制下直接从低分辨率 RGB 图像预测关节指令。我们表明，低分辨率第一人称视角视觉足以可靠地完成任务，并且在我们的设置中，预测相对关节增量显著优于预测绝对关节位置。这些结果表明，在可重复的设置中，基于视觉的主动感知可以从行为克隆中产生。

## Abstract
We investigate whether behavior cloning is sufficient to produce active perception in a structured object-finding task. A low-cost robot arm equipped with a wrist-mounted egocentric RGB camera must reposition to center a partially visible plant before triggering a grasp signal, requiring actions that improve future observations. The model predicts joint commands directly from low-resolution RGB images under closed-loop control. We show that low-resolution egocentric vision is sufficient for reliable task completion and that predicting relative joint deltas substantially outperforms absolute joint position prediction in our setting. These results demonstrate that visually grounded active perception can emerge from behavior cloning in a reproducible setting.

---

## 论文详细总结（自动生成）

这篇论文探讨了在低成本机器人平台上，通过行为克隆（Behavior Cloning, BC）实现主动感知（Active Perception）的可能性。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的主动感知通常需要显式地优化信息获取动作，而行为克隆仅通过模仿专家演示来学习策略。作者试图验证：在目标物体初始仅部分可见的任务中，简单的行为克隆是否足以让机器人产生“为了更好地观察而移动”的主动感知行为。
*   **背景**：研究聚焦于低成本硬件和低分辨率视觉输入，旨在探索实现复杂机器人行为的极简条件。

### 2. 方法论
*   **核心思想**：使用端到端的深度学习模型，将第一人称视角的图像序列直接映射为机器人关节指令。
*   **关键技术细节**：
    *   **视觉编码器**：采用四层卷积神经网络（CNN）处理 64×64 的低分辨率 RGB 图像。
    *   **时序控制器**：使用长短期记忆网络（LSTM）处理图像特征序列（历史长度 $H=20$），以捕捉单帧图像无法提供的时序依赖信息。
    *   **动作表示**：对比了两种表示方法：
        1.  **绝对关节位置**：直接预测下一时刻的关节角度。
        2.  **相对关节增量（Joint Deltas）**：预测当前位置与下一位置的差值（$\Delta a_t = a_{t+1} - a_t$）。
    *   **闭环控制**：模型在推理时实时接收反馈，预测增量并更新关节状态，形成闭环。

### 3. 实验设计
*   **场景设置**：使用 Lynxmotion AL5D 六自由度机械臂，手腕安装单目 RGB 摄像头。任务是寻找并居中一个放置在左侧或右侧的植物，然后关闭夹持器。
*   **数据集**：通过 Xbox 控制器进行远程操作收集演示数据。
*   **对比实验**：
    *   **消融/对比**：对比了“关节增量预测”与“绝对位置预测”在不同训练样本量（4, 8, 16, 32, 64 组演示）下的表现。
    *   **基准指标**：使用关节空间下的均方误差（MSE）和任务成功率作为评价标准。
    *   **泛化性测试**：测试了植物被放置在训练集未覆盖的中间位置时的表现。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **硬件成本**：强调了使用低成本、现成的商业组件（Off-the-shelf components），体现了该方法对计算资源和硬件的要求较低。

### 5. 实验数量与充分性
*   **实验规模**：针对 6 种不同的训练集规模进行了测试，每种设置下进行 5 次自主测试实验。
*   **充分性评价**：作为一篇短篇论文（3页），其实验设计较为精简。虽然涵盖了核心对比（增量 vs 绝对位置）和样本效率分析，但测试样本量（每组 5 次）较小，统计显著性略显不足。实验环境高度结构化（白色背景），缺乏复杂背景下的鲁棒性验证。

### 6. 主要结论与发现
*   **主动感知的涌现**：行为克隆足以产生主动感知行为，机器人学会了通过移动摄像头来改善视野以完成任务。
*   **低分辨率可行性**：仅 64×64 的低分辨率图像就足以支持可靠的闭环控制。
*   **动作表示的重要性**：预测**相对关节增量**显著优于绝对位置。增量模型在测试集上的 MSE 更低，动作更平滑，且能更好地泛化到未见过的物体位置；而绝对位置模型容易出现超调（Overshoot）现象。

### 7. 优点
*   **简洁高效**：证明了无需复杂的奖励函数或显式的信息增益计算，仅靠 BC 即可实现主动感知。
*   **可重复性强**：采用低成本硬件和简单的网络结构，易于其他研究者复现。
*   **见解深刻**：明确了动作表示（增量 vs 绝对）对行为克隆性能和泛化能力的重大影响。

### 8. 不足与局限
*   **环境过于理想化**：实验在纯白背景下进行，目标物体单一，未测试遮挡、光照变化或复杂背景下的表现。
*   **任务复杂度低**：任务仅限于简单的居中和抓取，尚未验证在更复杂的多阶段操作任务中的有效性。
*   **评估规模有限**：测试次数较少，可能存在一定的偶然性。

（完）
