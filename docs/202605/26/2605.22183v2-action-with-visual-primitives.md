---
title: Action with Visual Primitives
title_zh: 基于视觉基元的动作
authors: "Weilong Guo, Yuchen Wang, Renping Zhou, Yunfeng Zhang, Rui Fang, Yuyang Pang, Wenda Xu, Gao Huang"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22183v2"
tags: ["query:rob-il"]
score: 9.0
evidence: 端到端VLA使用视觉基元实现机器人操纵
tldr: "提出了AVP（Vision-Action with Primitives）架构，将视觉-语言模型（VLM）与流匹配动作专家解耦：VLM输出视觉原语token作为中间表示，条件化生成末端执行器轨迹。在通用抓取放置任务中，相比pi_0.5成功率提升27.61%，并在数据效率、空间组合泛化和物体迁移上取得一致改进。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22183v2/fig-001.webp\", \"caption\": \"Figure 3: Overview of the AVP framework.\", \"page\": 6, \"index\": 1, \"width\": 829, \"height\": 464}, {\"url\": \"assets/figures/arxiv/2605.22183v2/fig-002.webp\", \"caption\": \"Figure 4: OOD object gallery for cross-domain generalization. We evaluate zero-shot transfer on 45 unseen objects spanning multiple categories beyond the Chinese chess training domain.\", \"page\": 15, \"index\": 2, \"width\": 820, \"height\": 431}, {\"url\": \"assets/figures/arxiv/2605.22183v2/fig-003.webp\", \"caption\": \"Figure 5: Qualitative results for zero-shot cross-domain generalization. The leftmost column displays unseen OOD test objects, and the right columns illustrate the successful grasping trajectories executed by AVP.\", \"page\": 16, \"index\": 3, \"width\": 826, \"height\": 950}]"
motivation: 现有VLA模型将语言理解、场景感知和运动控制耦合在同一目标中，导致动作专家需重新学习VLM已具备的认知能力，限制了学习效率和泛化性。
method: 采用端到端架构，VLM推断下一阶段目标并输出视觉原语token，以此为条件通过流匹配生成动作，监督来自末端执行器运动学。
result: "在真实机器人抓取放置任务上，AVP相比现有方法成功率提升27.61%，并在数据效率、空间组合泛化和物体级迁移上表现更优。"
conclusion: 通过视觉原语解耦认知与动作，可提升学习效率和泛化能力，为通用机器人操作提供新范式。
---

## 摘要
视觉-语言-动作（VLA）模型已成为通用机器人操作领域一种有前景的范式。当前架构的常见设计是将语言指令和视觉观测映射为单次前向传递中的动作。虽然概念简单，但这种表述将指令理解、空间场景感知和运动控制纠缠在单一学习目标中。因此，动作专家必须隐式地重新学习预训练VLM中已有的认知和感知能力，这可能限制学习效率和泛化能力。我们提出AVP（基于视觉基元的动作），这是一种端到端架构，实现了这种以视觉基元为中心的接口：VLM推断下一阶段目标并发射视觉基元标记，这些标记条件化一个流匹配动作专家，其监督来自末端执行器运动学。在通用拾取与放置任务上的真实机器人实验表明，AVP相比pi_0.5提升了27.61%的成功率，并优于其他近期方法，在数据效率、空间组合泛化和对象级迁移方面均取得一致改进。

## Abstract
Vision-Language-Action (VLA) models have emerged as a promising paradigm for generalist robotic manipulation. A common design in current architectures maps language instructions and visual observations to actions in a single forward pass. While conceptually simple, this formulation entangles instruction comprehension, spatial scene understanding, and motor control within a single learning objective. As a result, the action expert must implicitly relearn cognitive and perceptual capabilities already present in the pretrained VLM, which can limit both learning efficiency and generalization. We introduce AVP (Action with Visual Primitives), an end-to-end architecture that implements this visual-primitive-centric interface: the VLM infers the next-stage target and emits visual-primitive tokens that condition a flow-matching action expert, with supervision derived from end-effector kinematics. Real-robot experiments on general pick-and-place tasks show that AVP improves the success rate by 27.61% over pi_0.5 and outperforms other recent methods, with consistent gains in data efficiency, spatial-compositional generalization, and object-level transfer.

---

## 论文详细总结（自动生成）

# 基于视觉基元的动作（AVP）论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：当前视觉-语言-动作（VLA）模型通常将语言理解、空间场景感知和运动控制耦合在单一前向传递中，动作专家必须隐式重新学习预训练VLM已具备的认知和感知能力，导致学习效率低、泛化能力弱。
- **核心问题**：如何更好地划分VLM与动作专家之间的职责，使VLM专注于“做什么、在哪做”，动作专家专注于“怎么做”，从而实现更高效、更鲁棒的机器人操作。
- **整体含义**：本文提出一种以视觉基元为中心的通信协议，作为VLM与动作专家之间的显式接口，提升空间推理和泛化能力，为通用机器人操作提供新范式。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：通过视觉基元（如点、边界框、掩码、内存标记等）作为显式中间表示，解耦高级推理与低级运动控制。
- **架构组成**：预训练VLM + 自回归解码器 + 动作专家（流匹配模型）。
- **流程**：
    1. VLM接收语言指令和多视角观测，解读当前子任务，预测下一阶段目标及其对应的视觉基元。
    2. 视觉基元解码器将VLM输出的上下文令牌映射为离散的视觉基元标记。
    3. 将视觉基元标记投影到视觉令牌空间，与原始多模态令牌融合，形成增强表示。
    4. 动作专家基于增强表示和机器人状态，通过流匹配生成动作序列。
- **关键公式**：
    - 视觉基元预测：\( p_t = D_\psi(\text{VLM}(o_t, l)) \)
    - 投影：\( z^{vp}_t = \text{Proj}(p_t, o_t) \)
    - 动作生成：\( a_{t:t+h} = \pi_\theta(z^{aug}_t, s_t) \)
    - 损失函数：\( \mathcal{L} = \mathcal{L}_{\text{act}} + \lambda \mathcal{L}_{\text{vp}} \)，其中 \( \mathcal{L}_{\text{vp}} \) 为视觉基元预测的交叉熵损失。
- **监督生成**：直接从末端执行器运动学获得，无需人工标注：通过检测夹爪状态变化提取关键帧，获取3D末端位置，投影到图像平面，并离散化为训练标签。

## 3. 实验设计

- **任务场景**：
    - 中国象棋操作（精确空间操作，视觉相似棋子密集排列）
    - 多米诺骨牌放置（高精度双灵巧手操作，含方向约束）
    - 通用物体抓取放置（多样外观和形状的物体）
- **数据集**：真实机器人专家演示数据，时长分布：中国象棋11.2小时，多米诺1.7小时，通用物体1.7小时。
- **基准与对比方法**：
    - 主基线：π0.5（同一框架）
    - 其他对比：Point-VLA（级联视觉提示）、DM0、LDA
    - 所有方法在相同的双机械臂（AgileX Piper，6-DOF ×2）和14维动作空间下训练和评估。
- **评估指标**：指令遵循成功率、拾取成功率、放置成功率；多米诺任务额外报告方向成功率。

## 4. 资源与算力

- 文中未明确说明训练使用的GPU数量和总训练时长，仅提到**单张NVIDIA RTX 3090 GPU用于推理**，推理延迟0.27秒（中国象棋任务）。
- 训练配置：中国象棋策略训练40k步，批量大小64；多米诺和通用物体策略训练10k步。其他基线（DM0、LDA）使用32批量大小、50k步骤直至收敛。
- 算力资源描述不够详尽，但可推测是单卡或多卡RTX 3090，具体未披露。

## 5. 实验数量与充分性

- **主要实验**：在三个真实任务上评估，每个任务均有详细成功率统计（表1-3）。
- **泛化实验**：
    - 空间组合泛化（未见的直接板对板迁移）50条指令评估（表4a）。
    - 跨域零样本泛化（45个未见物体，不同背景和指令模式）8×8测试（表4b）。
- **消融实验**：
    - 视觉基元类型消融（无提示、点、边界框、边界框+掩码、掩码+记忆）——表5。
    - 提示类型对比（原始、点、框、框掩码）——表6。
    - 掩码透明度影响（α=0, 0.7, 0.9）——表7。
- **充分性评价**：实验覆盖了主任务效果、泛化能力（空间组合、跨域零样本）和设计消融，对比基线多样，实验设计较为全面。但所有实验仅在同一机器人平台进行，未在多个不同机器人或模拟环境验证，存在一定局限性。另外，泛化实验仅使用了50或8个指令/物体，样本量偏小，需谨慎解读稳定性。

## 6. 论文的主要结论与发现

- 使用视觉基元作为显式接口可以显著解耦感知与控制，提升学习效率和泛化能力。
- 在主任务上，AVP整体成功率比π0.5提升27.61%（平均：90.28% vs 62.67%）。
- 在多米诺任务上，AVP在拾取和方向成功率上优于基线，放置精度持平。
- 在通用物体抓取任务上，AVP比π0.5提升18.45%（拾取）和45.21%（放置）。
- 空间组合泛化：AVP能组合未见过的直接路径，而π0.5完全失败。
- 跨域零样本泛化：AVP在45个未见物体上表现稳定，π0.5严重退化。
- 消融证实：视觉基元（尤其是记忆增强的掩码+框）是性能提升的关键。

## 7. 优点

- **方法创新**：提出视觉基元作为VLM与动作专家间的显式通信媒介，无需外部感知模块，实现端到端学习。
- **监督高效**：直接从末端执行器运动学生成基元标签，避免人工标注和外部模型依赖。
- **泛化能力强**：在空间组合和零样本跨域场景下表现突出，证明基元接口有助于学习可迁移的动作模式。
- **实验扎实**：真实机器人多任务验证，包含主实验、泛化测试和多种消融，对比基线包括当前主流VLA模型。
- **推理速度快**：级联式方法（如Point-VLA）因外部分段流水线延迟高达37秒，AVP仅需0.27秒，适合实时操作。

## 8. 不足与局限

- **推理延迟**：两阶段推理（基元预测→动作生成）比单阶段策略增加额外时间，文中已承认。
- **标定敏感**：监督生成依赖精确的手眼标定，摄像头外参漂移或物理扰动可能降低性能。
- **实验规模有限**：训练数据总量（约14.6小时）相对较小，主要针对特定任务；泛化测试数量偏少（50/8条指令），统计显著性有待进一步验证。
- **平台单一**：仅在一个双机械臂平台（AgileX Piper）上评估，未在多平台或仿真中验证通用性。
- **未讨论失败案例**：文中未详细分析失败模式（如放置精度为何未提升或多米诺放置持平的原因）。
- **超参数未充分公开**：λ（平衡两损失）的选择、视觉基元形态（点、框、掩码）的设计依赖启发式，缺乏更系统的搜索。

（完）
