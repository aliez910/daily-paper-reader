---
title: Action with Visual Primitives
title_zh: 基于视觉基元的动作
authors: "Weilong Guo, Yuchen Wang, Renping Zhou, Yunfeng Zhang, Rui Fang, Yue Meng, Wenda Xu, Yuan He, Gao Huang"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22183v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 基于视觉基元的端到端VLA
tldr: "针对视觉-语言-动作模型在机器人操作中将指令理解、空间感知和运动控制混为一谈导致学习效率低和泛化差的问题，本文提出AVP（视觉基元动作）架构，让VLM推断下一阶段目标并输出视觉基元标记，条件化流匹配动作专家，从末端执行器动力学获得监督。在通用拾取放置任务中，AVP相比pi_0.5成功率提升27.61%，并在数据效率、空间组合泛化和物体级迁移上持续领先。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22183v1/fig-001.webp\", \"caption\": \"Figure 3: OOD object gallery for cross-domain generalization. We evaluate zero-shot transfer on 45 unseen objects spanning multiple categories beyond the Chinese chess training domain.\", \"page\": 15, \"index\": 1, \"width\": 820, \"height\": 431}, {\"url\": \"assets/figures/arxiv/2605.22183v1/fig-002.webp\", \"caption\": \"Figure 1: Different visual primitives of our AVP.\", \"page\": 2, \"index\": 2, \"width\": 829, \"height\": 962}, {\"url\": \"assets/figures/arxiv/2605.22183v1/fig-003.webp\", \"caption\": \"Figure 2: Overview of the AVP framework.\", \"page\": 4, \"index\": 3, \"width\": 829, \"height\": 464}, {\"url\": \"assets/figures/arxiv/2605.22183v1/fig-004.webp\", \"caption\": \"Figure 4: Qualitative results for zero-shot cross-domain generalization. The leftmost column displays unseen OOD test objects, and the right columns illustrate the successful grasping trajectories executed by AVP.\", \"page\": 16, \"index\": 4, \"width\": 826, \"height\": 952}]"
motivation: 现有VLA模型将指令理解、场景感知和运动控制耦合在同一学习目标中，限制了学习效率和泛化能力。
method: AVP架构通过VLM输出视觉基元标记，条件化流匹配动作专家进行端到端学习，监督来自末端执行器动力学。
result: "在通用拾取放置任务中，AVP相比pi_0.5成功率提升27.61%，并优于其他近期方法。"
conclusion: AVP通过解耦感知与动作，显著提升了数据效率、空间组合泛化和物体级迁移能力。
---

## 摘要
视觉-语言-动作（VLA）模型已成为通用机器人操作的一种有前景的范式。当前架构的常见设计是将语言指令和视觉观察映射到单个前向传播中的动作。虽然概念上简单，但这种表述将指令理解、空间场景理解和运动控制纠缠在单一学习目标中。因此，动作专家必须隐式地重新学习预训练VLM中已具备的认知和感知能力，这可能限制学习效率和泛化能力。我们提出AVP（基于视觉基元的动作），一种端到端架构，实现了这种以视觉基元为中心的接口：VLM推断下一阶段目标，并生成视觉基元标记，这些标记条件化一个流匹配动作专家，其监督来自末端执行器运动学。在通用拾取和放置任务上的真实机器人实验表明，AVP相比pi_0.5提高了27.61%的成功率，并优于其他近期方法，在数据效率、空间组合泛化和对象级迁移方面具有一致的优势。

## Abstract
Vision-Language-Action (VLA) models have emerged as a promising paradigm for generalist robotic manipulation. A common design in current architectures maps language instructions and visual observations to actions in a single forward pass. While conceptually simple, this formulation entangles instruction comprehension, spatial scene understanding, and motor control within a single learning objective. As a result, the action expert must implicitly relearn cognitive and perceptual capabilities already present in the pretrained VLM, which can limit both learning efficiency and generalization. We introduce AVP (Action with Visual Primitives), an end-to-end architecture that implements this visual-primitive-centric interface: the VLM infers the next-stage target and emits visual-primitive tokens that condition a flow-matching action expert, with supervision derived from end-effector kinematics. Real-robot experiments on general pick-and-place tasks show that AVP improves the success rate by 27.61% over pi_0.5 and outperforms other recent methods, with consistent gains in data efficiency, spatial-compositional generalization, and object-level transfer.

---

## 论文详细总结（自动生成）

# 基于视觉基元的动作（AVP）详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现有视觉-语言-动作（VLA）模型通常将语言指令、视觉观察与机器人动作在一个前向传播中直接映射。这种设计将指令理解、空间场景感知和运动控制耦合在同一学习目标中，导致动作专家需要隐式地重新学习VLM已经具备的认知和感知能力，限制了学习效率和泛化性能。尤其是当物体、布局或环境发生变化时，泛化能力往往较弱。
- **核心问题**：如何合理划分VLM与动作专家的职责？理想的划分应该是VLM负责“做什么”和“在哪里”，动作专家专注于“怎么执行”。当前方法要么使用语言接口（如pi_0.5的子任务分解）但无法表达精细空间信息，要么依赖外部视觉模型生成空间标记（如Point-VLA）但引入延迟和错误传播。
- **论文含义**：提出AVP（Action with Visual Primitives），一种端到端架构，通过在VLM和动作专家之间建立显式的**视觉基元通信协议**——VLM内部生成空间化的视觉基元（点、边界框等），作为动作专家的直接视觉化指令，从而明确划分二者的学习责任，提升数据效率、空间组合泛化和物体级迁移能力。

## 2. 论文提出的方法论：核心思想、关键技术细节（用文字说明即可）

- **核心思想**：将通信接口由隐式的多模态特征转变为显式的视觉基元。VLM根据语言指令和视觉观测预测下一阶段目标及其对应的视觉基元（如目标点的位置、边界框等），这些基元被投影到图像token空间，作为动作专家的条件输入。动作专家基于这些显式的空间标记生成动作，不再需要从全局视觉-语言特征中隐式学习空间定位。
- **关键技术细节**：
  - **三组件架构**：预训练VLM + 自回归解码器（用于生成视觉基元） + 流匹配动作专家。
  - **前向流程**：
    1. VLM处理语言指令和当前观测，产生多模态上下文token。
    2. 视觉基元解码器自回归预测离散化的视觉基元\( p_t \)（代表下一阶段的子任务及其空间目标）。
    3. 将预测的基元投影到视觉token空间，得到\( z^{vp}_t \)。
    4. 融合原始多模态token与\( z^{vp}_t \)得到增强的多模态表示\( z^{aug}_t \)。
    5. 动作专家基于\( z^{aug}_t \)和机器人状态\( s_t \)预测动作序列\( a_{t:t+h} \)。
  - **监督信号**：
    - 视觉基元解码器的损失\( \mathcal{L}_{vp} \)：使用从末端执行器运动学导出的真实视觉基元标签（通过手眼标定将3D末端位置投影到图像平面，离散化得到目标）。
    - 动作预测损失\( \mathcal{L}_{act} \)：标准动作预测损失。
    - 总损失\( \mathcal{L} = \mathcal{L}_{act} + \lambda \mathcal{L}_{vp} \)。
  - **关键创新**：视觉基元完全由VLM内部生成，无需外部检测器或分割模型；基元标签自动来自机器人自身运动学，无需人工标注；端到端联合优化。

## 3. 实验设计：数据集/场景、基准、对比方法

- **任务与数据集**：
  - 中国象棋操作（主要基准）：11.2小时专家演示，训练40k步。
  - 多米诺骨牌放置：1.7小时演示，训练10k步。
  - 通用物体拾取放置：1.7小时演示，训练10k步。
  - 所有任务在真实机器人平台（AgileX Piper双6-DoF机械臂）上采集。
- **基准方法与指标**：
  - 主要对比基线：pi_0.5（在相同机器人本体、训练数据、评估协议下复现）。
  - 额外对比：Point-VLA（使用Qwen3.6-Plus和Kimi-K2.5 API）、DM0、LDA、StarVLA（部分方法未报告结果）。
  - 评估指标：指令跟随成功率、拾取成功率、放置成功率、平均成功率。多米诺任务额外报告定向成功率。
- **实验场景**：
  - 中国象棋：72条指令序列，评估精细空间顺序操作。
  - 多米诺：48条指令，评估位置与定向精度。
  - 通用物体：多种外观和形状的物体，评估泛化。
- **泛化实验**：
  - **空间组合泛化**：训练时只使用间接转移（A→C→B），测试直接转移（A→B）。使用12.8小时演示数据，训练50k步，评估50条未见指令。
  - **跨域泛化**：仅用中国象棋数据训练，测试45个未见物体（包括不同背景和指令协议——“直接”与“间接”设置）。
- **消融实验**：视觉基元类型（无、点、框、框+掩码）、掩码透明度、记忆增强等，在中国象棋任务上进行。

## 4. 资源与算力

- **文中未明确说明使用的GPU型号、数量以及训练耗时**。仅提及训练步数和数据量：象棋40k步、其他10k步，批量大小为64。未提及训练时间或硬件资源。因此，本总结指出：论文未提供详细的算力信息，读者无法直接从论文中评估训练成本。

## 5. 实验数量与充分性

- **实验数量**：论文包含三组主要任务实验（象棋、多米诺、通用物体），每组均有量化成功率比较。此外进行了一组空间泛化实验（Table 4a）、一组跨域泛化实验（Table 4b）、一组视觉基元消融实验（Table 5，五组设置）。附录中还有额外的基元类型和掩码透明度消融（Table 6、Table 7）。总实验数量较为充分。
- **充分性与公平性**：
  - 主要基线pi_0.5在相同机器人本体、训练数据和评估协议下复现，对比方法如Point-VLA也适配了双机械臂控制，尽量保持公平。
  - 实验覆盖了不同任务类型（精细操作、定向约束、通用抓取）和泛化场景（未见转移、未见物体、不同背景）。
  - 消融实验系统性地验证了视觉基元各组件的贡献。
  - 不足：部分对比方法（如DM0、LDA）未报告所有指标或未在相同条件下完全复现；另一些方法（如StarVLA）没有结果。此外，实验主要集中在自采数据，未在公开数据集（如Open X-Embodiment）上进行跨数据集评估，泛化实验的规模（45个物体）有限。整体上公平性较好，但对比广度可进一步提升。

## 6. 论文的主要结论与发现

1. **主要性能提升**：在中国象棋任务上，AVP的平均成功率达到90.28%，相比pi_0.5的62.67%提升27.61%；在多米诺和通用物体任务上也分别提升6.25%和21.22%。
2. **空间组合泛化**：仅在间接转移上训练，AVP能零样本泛化到直接转移（成功率100%），而pi_0.5完全失败（0/8）。这验证了视觉基元使策略能够解耦子任务并组合新行为。
3. **跨域物体泛化**：仅用象棋数据训练的AVP，在45个未见物体上（不同背景和指令协议）仍能保持80-100%成功率，而pi_0.5严重退化（尤其直接设置下0/8）。
4. **视觉基元有效性**：消融表明，引入视觉基元（尤其点或框+掩码）显著优于无基元设置；附加记忆机制进一步提升性能（91% vs 78%）。
5. **可扩展性**：基元标签自动来自末端运动学，无需人工标注；基元形式简洁，易于扩展（如加入记忆或顺序索引），为规模化提供了低成本的路径。

## 7. 优点：方法或实验设计上的亮点

- **方法创新性**：首次在端到端VLA内部建立显式的视觉基元通信接口，明确划分VLM与动作专家的职责，避免依赖外部视觉模型。
- **监督自动化**：基元标签直接从末端执行器运动学投影得到，消除了人工标注和外部感知模块的依赖，使训练数据收集成本接近于标准模仿学习。
- **强泛化能力**：在未见空间转移和未见物体上均展现了出色的零样本泛化，证明了空间推理与动作执行的解耦。
- **实验设计全面**：覆盖多种操作任务、泛化场景和消融分析，且在同一机器人平台、统一动作空间下进行对比，保证了可比性。
- **实际部署可行性**：推理延迟仅0.27秒（象棋任务），远低于基于API的Point-VLA（37秒），适合实时控制。

## 8. 不足与局限

- **实验覆盖与对比广度**：
  - 对比方法不够全面：部分方法（DM0、LDA）未完整报告结果，且未与最新大模型方法（如OpenVLA、RT-2）在相同条件下对比。
  - 所有实验基于自建数据集和单一机器人平台，未在公开基准（如LIBERO、BridgeData）上验证，泛化结论可能受限于数据分布。
  - 跨域泛化实验仅在45个物体上进行，规模偏小；具体如何选择物体、是否包含困难案例未详细说明。
- **偏差风险**：训练数据全部来自专家演示，未涉及探索性学习或失败数据，策略对演示质量敏感；此外，基元标签依赖精确的手眼标定，标定误差会直接传导。
- **应用限制**：
  - 需要两阶段推理（基元预测+动作生成），相比单次策略增加了延迟（文中已承认）。
  - 系统对相机外参漂移或物理扰动敏感，实际部署时可能需要定期重新标定。
  - 当前任务仅限于拾取放置类操作，未验证在更复杂的非抓取操作（如推动、组装）或长时间跨任务场景下的表现。
  - 未研究策略在长时序或部分可观测环境中的记忆机制（附录虽有初步记忆消融，但未系统评估）。
- **资源信息缺失**：未报告训练硬件、时间等，影响可复现性和对计算成本的评估。
- **方法局限**：视觉基元的形式目前仅限于点、边界框等简单几何标记；对于需要更丰富空间信息（如物体方向、深度）的任务，可能需要更复杂的基元设计。

（完）
