---
title: "CorridorVLA: Explicit Spatial Constraints for Generative Action Heads via Sparse Anchors"
authors: "Dachong Li, ZhuangZhuang Chen, Jin Zhang, Jianqiang Li"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21241v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 具有显式空间约束的VLA模型用于动作生成
tldr: 针对视觉-语言-动作（VLA）模型中空间引导通常隐含在潜变量中的问题，本文提出CorridorVLA。该方法通过预测稀疏空间锚点（增量物理变化）来构建显式的容差区域（走廊），并利用流匹配动作头引导轨迹生成。在LIBERO-Plus基准测试中，该方法显著提升了SmolVLA和GR00T的成功率，证明了显式空间约束在生成式动作策略中的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21241v1/fig-001.webp\", \"caption\": \"Fig. 2. Framework. (A) The backbone predicts a small set of future key spatial increments, while the action output is augmented with the corresponding end-effector displacement fields. These key increments are then used to constrain action generation, requiring only a few additional prediction slots with minimal changes to the original VLA pipeline. (B) Spatial-change guidance provides a simple prior: manipulation trajectories tend to evolve smoothly, so sparse key increments can offer a safe, structured signal that reduces unstructured exploration under stochastic generation.\", \"page\": 3, \"index\": 1, \"width\": 512, \"height\": 410}]"
motivation: 现有的VLA模型主要依赖隐含的潜特征进行空间引导，缺乏对动作生成的显式物理约束。
method: 提出预测稀疏空间锚点以定义“走廊”区域，并结合流匹配动作头，对超出该区域的轨迹施加纠正梯度。
result: "在LIBERO-Plus基准上，CorridorVLA将基线模型的成功率提升了3.4%至12.4%，其中GR00T-Corr变体达到了83.21%的成功率。"
conclusion: 动作对齐的物理线索能为生成式动作策略提供直接且可解释的约束，是视觉或潜变量空间引导的有效补充。
---

## Abstract
Vision--Language--Action (VLA) models often use intermediate representations to connect multimodal inputs with continuous control, yet spatial guidance is often injected implicitly through latent features. We propose $CorridorVLA$, which predicts sparse spatial anchors as incremental physical changes (e.g., $Δ$-positions) and uses them to impose an explicit tolerance region in the training objective for action generation. The anchors define a corridor that guides a flow-matching action head: trajectories whose implied spatial evolution falls outside it receive corrective gradients, while minor deviations from contacts and execution noise are permitted. On the more challenging LIBERO-Plus benchmark, CorridorVLA yields consistent gains across both SmolVLA and GR00T, improving success rate by $3.4\%$--$12.4\%$ over the corresponding baselines; notably, our GR00T-Corr variant reaches a success rate of $83.21\%$. These results indicate that action-aligned physical cues can provide direct and interpretable constraints for generative action policies, complementing spatial guidance encoded in visual or latent forms. Code is available at https://github.com/corridorVLA.

---

## 论文详细总结（自动生成）

这是一份关于论文《CorridorVLA: Explicit Spatial Constraints for Generative Action Heads via Sparse Anchors》的结构化分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在连接多模态输入与连续控制时，空间引导信息通常是以隐含的方式注入到潜特征（latent features）中的。这种方式缺乏直观的物理约束，导致模型在生成动作轨迹时可能出现不符合物理逻辑的偏差。
*   **研究动机**：作者探索是否可以将空间引导表达为**直接的、文本风格的物理量**（如末端执行器的位置增量），并利用这些量在目标函数（Loss）层面为动作生成施加显式的“容差区域”约束，从而提高机器人操作的精准度和鲁棒性。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出 **CorridorVLA**，通过预测未来的稀疏空间锚点（Sparse Anchors）来定义一条“走廊”（Corridor）。生成的动作轨迹必须保持在走廊定义的容差范围内，超出范围将受到惩罚。
*   **关键技术细节**：
    *   **稀疏锚点预测**：在VLA骨干网络中引入少量可学习的槽位（Slots），预测未来 $K$ 个关键时间步的末端执行器（EE）3D位置增量（$\Delta$-positions）。
    *   **扩展动作空间（extra-A）**：将动作向量从单纯的控制指令扩展为包含对应的物理位移信息，使动作头监督信号与骨干网络预测的锚点对齐。
    *   **走廊约束目标函数**：
        *   **走廊缓冲损失 ($L_{buf}$)**：定义一个基于地面真值（GT）的容差带 $\delta$。当流匹配（Flow Matching）生成的预测轨迹偏离锚点超过 $\delta$ 时，施加纠正梯度。
        *   **走廊内一致性损失 ($L_{cons}$)**：在轨迹进入走廊后，通过阶段性累积进度进一步精细化调整。
        *   **噪声感知加权**：根据流匹配的噪声水平 $t$ 动态调整约束权重，在接近真实数据分布（低噪声）时施加更强的空间约束。
*   **算法流程**：骨干网络同时输出融合特征和稀疏锚点 $\rightarrow$ 动作头基于流匹配生成轨迹 $\rightarrow$ 计算 FM 损失、锚点预测损失及走廊约束损失 $\rightarrow$ 联合优化。

### 3. 实验设计
*   **数据集/场景**：
    *   **LIBERO**：标准的机器人学习基准。
    *   **LIBERO-Plus**：更具挑战性的变体，包含更强的扰动和多样性。
*   **Benchmark**：主要针对 4-in-1 任务（Long, Goal, Object, Spatial）。
*   **对比方法**：
    *   **基线模型**：SmolVLA (0.45B), GR00T。
    *   **外部对比**：$\pi_0$ (3.3B), GraspVLA, NORA, UniVLA, OpenVLA-OFT 等。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到 SmolVLA 基于 LeRobot 框架实现，GR00T 遵循 StarVLA 的公开实现。由于 SmolVLA 较小（0.45B），作者强调该模型允许快速迭代和细粒度消融。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流基准（LIBERO 和 LIBERO-Plus）上进行了完整测试。
    *   针对 SmolVLA 和 GR00T 两种不同架构的 VLA 进行了验证，证明了方法的通用性。
    *   **消融实验**：非常详尽，涵盖了走廊损失组件的必要性、锚点选择算法（RDP算法 vs 均匀采样）、预测目标（绝对位置 vs 增量位置）以及骨干网络交互方式（因果掩码 vs 双向注意力）。
*   **客观性与公平性**：实验采用了相同的训练协议和超参数，对比了多个 SOTA 模型，且在不同分辨率（256 vs 512）下进行了鲁棒性验证，实验设计较为客观公平。

### 6. 主要结论与发现
*   **性能提升**：CorridorVLA 在 LIBERO 上比 SmolVLA 基线提升了 4.45%；在更难的 LIBERO-Plus 上，SmolVLA-Corr 提升了 **12.4%**，GR00T-Corr 提升了 **7.98%**（达到 83.21% 的成功率）。
*   **物理线索的有效性**：预测 $\Delta$-positions（增量）比预测绝对位置更有效，因为增量更能捕捉控制的本质且对相机校准不敏感。
*   **显式约束的优势**：在目标函数层面施加物理约束，比单纯在潜空间进行特征融合能提供更直接、更可解释的引导。

### 7. 优点（亮点）
*   **轻量且高效**：仅需增加极少量（如3个）预测 token，几乎不增加推理延迟和计算负担。
*   **可解释性强**：通过物理意义明确的“走廊”来约束动作，比黑盒化的潜特征更易理解。
*   **即插即用**：可以方便地集成到现有的生成式动作头（如 Flow Matching 或 Diffusion）中。
*   **几何感知**：利用 RDP 算法选择几何显著的锚点，比简单的均匀采样更能捕捉轨迹特征。

### 8. 不足与局限
*   **缺乏真机验证**：所有实验均在仿真环境（LIBERO）中完成，尚未在真实机器人平台上验证其在复杂物理接触和传感器噪声下的表现。
*   **对比范围有限**：未与基于图像生成（如视频预测）的重型空间引导方法进行直接的端到端对比。
*   **超参数敏感性**：走廊宽度 $\delta$ 和损失权重 $\lambda$ 的选择可能需要针对不同任务进行微调，论文未深入讨论其自适应调整机制。

（完）
