---
title: "GCImOpt: Learning efficient goal-conditioned policies by imitating optimal trajectories"
title_zh: GCImOpt：通过模仿最优轨迹学习高效的目标条件策略
authors: "Jon Goikoetxea, Jesús F. Palacián"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22724v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 通过在轨迹优化生成的数据集上训练进行模仿学习
tldr: GCImOpt是一种通过模仿最优轨迹来学习高效目标条件策略的方法。针对模仿学习中演示数据获取难且质量不一的问题，该方法利用轨迹优化快速生成高质量数据集，并通过数据增强扩大规模。实验证明，该策略在倒立摆、无人机和机械臂任务中表现优异，具有参数量小、推理速度快（比优化器快6000倍）的特点，适用于资源受限的嵌入式系统。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.22724v1/fig-001.webp\", \"caption\": \"Figure 1: Schematic summary of GCIMOPT, a simple method to train and evaluate near-optimal goal-conditioned policies. A policy πθ, parameterized by a neural network, is trained by supervised learning on a large dataset of trajectories generated by trajectory optimization. The policy is then evaluated in simulation to measure its success rate and efficiency.\", \"page\": 2, \"index\": 1, \"width\": 722, \"height\": 553}, {\"url\": \"assets/figures/arxiv/2604.22724v1/fig-002.webp\", \"caption\": \"Table 1: Final validation error Jval, success rate s and average cost relative error δr for all tasks. Results are shown as mean ± standard deviation over 5 random seeds (see detailed results in Appendix A.4). Lower Jval and δr are better (↓); higher s is better (↑).\", \"page\": 9, \"index\": 2, \"width\": 898, \"height\": 387}, {\"url\": \"assets/figures/arxiv/2604.22724v1/fig-003.webp\", \"caption\": \"Table 5: Final validation error Jval, success rate s and average cost relative error δr for all tasks, for 5 random seeds for each task/MLP hidden layer size (dMLP) combination.\", \"page\": 16, \"index\": 3, \"width\": 785, \"height\": 535}]"
motivation: 传统的模仿学习依赖昂贵且可能次优的人类演示，限制了其在复杂控制任务中的应用。
method: 利用轨迹优化快速生成最优轨迹，并通过将中间状态视为目标的增强方案扩大数据集，训练轻量级目标条件神经网络。
result: 在多种控制任务中实现了高成功率和近乎最优的控制效果，且推理速度比传统轨迹优化求解器快数千倍。
conclusion: GCImOpt证明了结合轨迹优化与模仿学习可以有效训练出适用于资源受限硬件的高性能、实时控制策略。
---

## 摘要
模仿学习是一种成熟的基于机器学习的控制方法。然而，其适用性取决于演示数据的获取，而这些数据通常收集成本高昂且/或对于解决任务而言并非最优。在这项工作中，我们提出了 GCImOpt，这是一种通过在轨迹优化生成的数据集上进行训练来学习高效目标条件策略的方法。我们的数据集生成方法计算效率高，可以在笔记本电脑上几分钟内生成数千条最优轨迹，并产生高质量的演示。此外，通过将中间状态视为目标的数据增强方案，我们能够将训练数据集的大小提高一个数量级。利用生成的数据集，我们训练了目标条件神经网络策略，能够控制系统趋向任意目标。为了证明该方法的通用性，我们为各种控制任务生成了数据集并训练了策略，包括倒立摆稳定、平面和三维四旋翼飞行器稳定，以及使用 6 自由度机械臂的点到达任务。我们展示了训练后的策略可以实现高成功率和近乎最优的控制曲线，同时模型体积小（神经网络参数少于 80,000 个）且速度足够快（比轨迹优化求解器快 6,000 倍以上），可以部署在资源受限的控制器上。我们在自由软件许可下提供了视频、代码、数据集和预训练策略；详见项目网站 https://jongoiko.github.io/gcimopt/。

## Abstract
Imitation learning is a well-established approach for machine-learning-based control. However, its applicability depends on having access to demonstrations, which are often expensive to collect and/or suboptimal for solving the task. In this work, we present GCImOpt, an approach to learn efficient goal-conditioned policies by training on datasets generated by trajectory optimization. Our approach for dataset generation is computationally efficient, can generate thousands of optimal trajectories in minutes on a laptop computer, and produces high-quality demonstrations. Further, by means of a data augmentation scheme that treats intermediate states as goals, we are able to increase the training dataset size by an order of magnitude. Using our generated datasets, we train goal-conditioned neural network policies that can control the system towards arbitrary goals. To demonstrate the generality of our approach, we generate datasets and then train policies for various control tasks, namely cart-pole stabilization, planar and three-dimensional quadcopter stabilization, and point reaching using a 6-DoF robot arm. We show that our trained policies can achieve high success rates and near-optimal control profiles, all while being small (less than 80,000 neural network parameters) and fast enough (up to more than 6,000 times faster than a trajectory optimization solver) that they could be deployed onboard resource-constrained controllers. We provide videos, code, datasets and pre-trained policies under a free software license; see our project website https://jongoiko.github.io/gcimopt/.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **GCImOpt**，一种结合轨迹优化（Trajectory Optimization, TO）与模仿学习（Imitation Learning, IL）的方法，旨在为动态系统学习高效且具备目标条件（Goal-conditioned）的控制策略。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在计算资源受限的嵌入式设备上，实现既能处理多种目标任务、又具备近乎最优性能的实时闭环控制？
*   **研究背景**：
    *   **模型预测控制（MPC）** 虽然性能优异，但在高频运行时计算开销巨大。
    *   **强化学习（RL）** 往往需要复杂的奖励函数设计和大量的环境交互。
    *   **传统模仿学习** 依赖昂贵的人类演示，且数据质量往往难以保证。
*   **整体含义**：论文提出通过“离线生成最优数据 + 在线快速推理”的思路，利用快速轨迹优化器生成专家数据，再通过行为克隆（Behavioral Cloning）训练轻量级神经网络，从而将复杂的优化过程转化为极速的函数映射。

### 2. 方法论
*   **核心思想**：将最优控制问题（OCP）的解作为专家演示，训练一个以当前状态和目标为输入的神经网络策略 $\pi_\theta(x, g)$。
*   **关键技术细节**：
    1.  **数据集生成**：使用 **FATROP**（一种专为机器人设计的快速约束优化求解器），通过直接多重射击法（Direct Multiple Shooting）求解不同初始状态和目标下的最优轨迹。
    2.  **代价函数设计**：代价函数平衡了控制能量消耗和轨迹时间（Time-optimal vs. Effort-optimal）。
    3.  **数据增强（后验目标重标记）**：借鉴 Hindsight Experience Replay (HER) 的思想，将一条最优轨迹中的任何中间状态都视为一个“已达成的目标”，从而将原始轨迹切分为多个子任务样本，使数据量提升一个数量级。
    4.  **策略训练**：采用简单的多层感知机（MLP）和行为克隆（监督学习），最小化策略输出与最优控制量之间的均方误差（MSE）。

### 3. 实验设计
*   **实验场景**：
    1.  **倒立摆（Cart-pole）**：4维状态，非线性平衡任务。
    2.  **平面四旋翼（Planar Quadrotor）**：6维状态，2维空间内的稳定与移动。
    3.  **三维四旋翼（3D Quadrotor）**：12维状态，高度非线性且对控制误差敏感。
    4.  **Franka Emika Panda 机械臂**：6自由度，末端执行器点位到达任务。
*   **Benchmark（基准）**：以 **FATROP 求解器**生成的理论最优解作为性能上限。
*   **对比维度**：成功率（Success Rate）、最优性差距（Relative Cost Error）、推理延迟（Inference Time）。

### 4. 资源与算力
*   **硬件环境**：笔记本电脑（Intel i9-13th Gen CPU, 24核；NVIDIA RTX 4090 Laptop GPU, 16GB VRAM）。
*   **训练时长**：
    *   **数据生成**：非常高效，所有任务的数据集生成均在 **12分钟内** 完成。
    *   **策略训练**：每个任务的训练（含验证）大约耗时 **30分钟**。
*   **模型规模**：极小，参数量少于 80,000 个。

### 5. 实验数量与充分性
*   **实验规模**：每个任务生成了 **20,000 条** 原始最优轨迹，经过数据增强后，训练样本量达到 **1100万至1400万** 个。
*   **充分性**：
    *   针对每个任务测试了不同规模的 MLP（隐藏层神经元分别为 32, 64, 128）。
    *   每个配置运行了 **5个随机种子** 以评估鲁棒性。
    *   涵盖了从低维线性到高维非线性的多种动力学系统，实验设计较为全面且客观。

### 6. 主要结论与发现
*   **高性能**：在大多数任务中，轻量级 MLP 达到了 90% 以上的成功率，且控制代价与最优解的差距较小。
*   **极速推理**：相比于 FATROP 求解器，神经网络策略实现了 **97倍至6278倍** 的加速。在 3D 四旋翼任务中，推理仅需约 0.015 毫秒。
*   **模型敏感性**：在 3D 四旋翼等复杂任务中，简单的回归误差（MSE）下降并不直接等同于闭环成功率的提升，模型需要更深的层数（5层）来捕捉复杂的动力学特性。
*   **数据增强的价值**：通过重标记中间状态，极大地提高了策略在目标空间内的泛化能力。

### 7. 优点
*   **计算效率极高**：从数据生成到训练完成仅需不到一小时，且推理速度足以支持超高频实时控制。
*   **通用性强**：目标条件（Goal-conditioned）的设计使得一个模型就能处理该系统下的任意到达任务，无需为每个目标重新优化。
*   **开源贡献**：提供了完整的代码、数据集和预训练模型，具有很高的透明度和复现价值。

### 8. 不足与局限
*   **模型依赖性**：该方法属于基于模型（Model-based）的范畴，需要精确的系统动力学方程（ODE）。
*   **全状态观测假设**：实验假设系统状态是完全可观测的，未考虑传感器噪声或部分观测（POMDP）的情况。
*   **分布偏移风险**：采用简单的行为克隆（BC）可能面临复合误差问题，即策略一旦偏离专家轨迹，可能无法自我纠正（虽然实验中表现良好，但在更复杂的动态环境下可能需要 DAgger 等算法）。
*   **约束处理**：目前主要处理了执行器限制，对于复杂的时变环境约束（如动态避障）探讨较少。

（完）
