---
title: Health-Conditioned Vision-Language-Action Models for Malfunction-Aware Robot Control
title_zh: 针对故障感知机器人控制的健康调节视觉-语言-动作模型
authors: "Hüseyin Arslan, Özgür Erkent"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.16056v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 用于机器人控制的健康调节视觉-语言-动作模型
tldr: 针对现有视觉-语言-动作（VLA）模型在面对机器人关节退化或执行器故障等物理损伤时表现不佳的问题，本文提出了一种健康感知的VLA模型。通过引入健康向量输入和Health Projector模块，模型能够根据机器人当前的关节角度和扭矩能力动态调整动作策略。在LIBERO环境下的实验表明，该方法仅需极小的计算开销，即可使机器人在硬件受损的情况下成功完成任务，显著提升了机器人的鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.16056v1/fig-001.webp\", \"caption\": \"TABLE II SUCCESS RATE (%) COMPARISON: BASELINE VS. HEALTH-CONDITIONED MODEL\", \"page\": 3, \"index\": 1, \"width\": 481, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2605.16056v1/fig-002.webp\", \"caption\": \"TABLE III PER-TASK SUCCESS RATE (%) UNDER ALL JOINT MALFUNCTIONS ON LIBERO-SPATIAL (T0–T9). B = BASELINE, O = OURS. J1 w=0.9 AND J3 w=0.9 OMITTED (BOTH MODELS 0% ON ALL TASKS). BOLD INDICATES OUR MODEL OUTPERFORMS THE BASELINE.\", \"page\": 4, \"index\": 2, \"width\": 831, \"height\": 490}, {\"url\": \"assets/figures/arxiv/2605.16056v1/fig-003.webp\", \"caption\": \"Fig. 1. Health-conditioned VLA architecture. The Health Projector (orange) maps the 7D health vector into the model’s latent space and adds it to the proprioceptive features. Only the Health Projector and action queries are trained; all other components remain frozen.\", \"page\": 2, \"index\": 3, \"width\": 527, \"height\": 289}]"
motivation: 现有的VLA模型缺乏对机器人自身物理故障（如关节退化或执行器失效）的感知与自适应能力。
method: 在VLA-Adapter架构中引入Health Projector模块，将描述关节状态的健康向量作为输入以指导动作生成。
result: 在LIBERO-Spatial任务中，该模型能够成功应对多种关节退化配置，而预训练的标准模型则无法完成任务。
conclusion: 通过引入健康状态感知机制，可以显著增强VLA模型在真实世界硬件故障场景下的任务执行成功率和适应性。
---

## 摘要
近年来，视觉-语言-动作（VLA）模型的研究迅速增加。尽管其中一些研究侧重于检测、预防和从任务失败中恢复，但它们通常不涉及适应机器人的物理故障。在现实场景中，大多数机器人面临各种形式的物理退化，例如关节退化、执行器故障或夹持器无力。我们引入了故障感知（健康调节）的 VLA，它将健康向量作为输入，提供有关机器人关节操作角度和扭矩能力的信息，并调整其预测以利用退化的关节完成任务。为实现这一目标，我们在 VLA-Adapter 架构中注入了一个健康投影仪（Health Projector）模块，并在我们在 LIBERO 环境中收集的故障机器人数据上对其进行训练。我们在 Libero-Spatial 任务上收集了 128 个远程操作情节。我们的结果表明，通过非常轻量级的添加，该模型可以学会在不同退化关节配置下成功运行，而默认的预训练 VLA-Adapter 的 Libero-Spatial-Pro 模型则无法做到。代码和数据集将很快在 https://github.com/h-arslan/health-aware-vla 发布。

## Abstract
Research on Vision Language Action (VLA) models has been increasing rapidly in recent years. Although some of them focus on detecting, preventing, and recovering from task failures, they usually don't deal with adapting to robot's physical failures. In real-life scenarios, most robots face physical degradations in various ways such as joint degradation, actuator failure, or weak gripper. We introduce malfunction-aware (health-conditioned) VLA that takes a health vector as an input that gives information about robots' joints' operation angle and torque capability, and adapts its predictions to complete the tasks with the degraded joints. To achieve this, we inject a Health Projector module to the VLA-Adapter architecture and train it on malfunction robot data we collected on the LIBERO environment [1]. We collect 128 teleoperated episodes on Libero-Spatial tasks. Our results show that, with a very lightweight addition, the model can learn to operate successfully with different configurations of degraded joints which the default pretrained VLA-Adapter's Libero-Spatial-Pro model cannot. The code and dataset will be available soon at https://github.com/h-arslan/health-aware-vla

---

## 论文详细总结（自动生成）

这是一份关于论文《Health-Conditioned Vision-Language-Action Models for Malfunction-Aware Robot Control》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型（如 RT-2, OpenVLA 等）通常假设机器人硬件处于完美状态。然而，在现实世界中，机器人经常面临关节退化、执行器部分失效或电机扭矩损失等物理故障。
*   **研究动机**：目前的 VLA 研究多关注语义层面的任务失败检测与恢复，缺乏针对机器人自身物理损伤的自适应机制。当训练于健康状态的策略在受损硬件上运行时，往往会导致任务彻底失败。本文旨在填补这一空白，使 VLA 模型能够感知硬件“健康度”并调整动作策略。

### 2. 方法论
*   **核心思想**：引入一个显式的“健康向量”（Health Vector），通过一个轻量级的投影模块将其注入预训练 VLA 模型的潜在空间，使模型能够根据硬件限制调整运动规划。
*   **关键技术细节**：
    *   **健康向量 ($h$)**：定义为一个 $J$ 维向量（$J$ 为关节数），每个元素 $h_j \in [0, 1]$。$1.0$ 代表完全健康，$0.0$ 代表关节完全锁定。中间值按比例缩放执行器增益和可达到的角度范围。
    *   **Health Projector 模块**：一个两层 MLP，将 7 维健康向量映射到主干 LLM 的嵌入空间（$\mathbb{R}^{896}$）。
    *   **架构集成**：基于 **VLA-Adapter Pro** 架构。健康特征被逐元素加到本体感受（Proprioceptive）特征上。
    *   **零初始化策略**：MLP 的最后一层初始化为零，确保在训练开始时不会干扰预训练动作头的原始表现。
    *   **训练策略**：冻结 VLM 主干、视觉编码器和适配器，仅训练 Health Projector 和 Action Queries（总计约 900K 参数）。

### 3. 实验设计
*   **数据集与场景**：在 **LIBERO-Spatial** 仿真环境下进行。该任务集要求机器人理解空间关系（如将碗放到盘子上）。
*   **数据收集**：
    *   通过鼠标和键盘远程操作收集了 128 个故障状态下的情节（Episodes）。
    *   设置了四个故障等级（$w = 1 - h$）：0.3, 0.5, 0.7, 0.9。
    *   包含 50 个由预训练模型生成的健康状态情节。
*   **Benchmark 与对比方法**：
    *   **基准模型**：未经过健康调节的预训练 VLA-Adapter Libero-Spatial-Pro 模型。
    *   **对比维度**：在 7 个关节、4 种故障等级、10 个空间任务的组合下测试成功率。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA L40S GPU** 进行微调。
*   **训练细节**：
    *   训练步数：15,000 步。
    *   Batch Size：8（梯度累积 2 步）。
    *   优化器：AdamW，学习率 2e-4。
    *   可训练参数量：极低，仅约 **0.9M (900K)**，这使得该方法非常轻量且易于部署。

### 5. 实验数量与充分性
*   **实验规模**：总计使用了 178 个情节进行训练。在评估阶段，针对每个“任务 x 关节 x 故障等级”组合运行了 10 次实验（基准模型为 4 次）。
*   **充分性评价**：
    *   **客观性**：实验覆盖了机械臂的所有关节和多种故障程度，对比清晰。
    *   **局限性**：由于数据是通过人工远程操作收集的，样本量相对较小（178 个），且目前仅限于单关节故障的场景。对于多关节同时故障的复杂情况，实验尚不充分。

### 6. 主要结论与发现
*   **基准模型的脆弱性**：预训练模型在肩部（J1）和肘部（J3）等关键运动关节出现轻微退化（$w=0.3$）时，成功率会从 97.5% 骤降至 40% 左右；当退化严重时，成功率直接归零。
*   **健康调节的有效性**：
    *   在关键关节受损时，新模型表现显著优于基准。例如，肩部在 $w=0.3$ 时成功率从 45% 提升至 89%。
    *   模型学会了补偿策略，通过改变运动轨迹来绕过受限关节的物理极限。
*   **性能保持**：在完全健康的状态下，新模型保持了 99% 的成功率，证明了该方法不会损害原始性能。

### 7. 优点
*   **参数高效**：仅需不到 1M 的额外参数即可实现显著的自适应能力提升。
*   **即插即用**：通过零初始化投影仪，可以无缝集成到现有的预训练 VLA 模型中。
*   **现实意义强**：直接针对机器人长期运行中不可避免的硬件磨损问题，提高了系统的鲁棒性。

### 8. 不足与局限
*   **数据依赖**：目前依赖显式的健康向量输入，但在现实中，准确获取每个关节的实时健康参数可能需要额外的传感器或估计器。
*   **泛化限制**：实验仅限于单关节故障和特定的 LIBERO 任务集，尚未在真实物理机器人或多关节同时故障的复杂任务中验证。
*   **泛化成本**：在某些基准模型表现良好的关节上，新模型出现了轻微的性能下降（如基座关节 J0），这可能是由于训练数据分布不均导致的“遗忘”现象。

（完）
