---
title: Spacetime Optimal-Transport Attention for Visuo-Haptic Imitation Learning of Contact-Rich Manipulation
title_zh: 用于接触丰富操作视觉-触觉模仿学习的时空最优传输注意力
authors: "Yue Feng, Weicheng Huang, I-Ming Chen"
date: 2026-05-19
pdf: "https://arxiv.org/pdf/2605.20433v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 触觉-视觉模仿学习用于接触式操纵
tldr: "针对接触丰富的机器人操作任务（如紧配合插入、连接器配对等），现有单模态或双模态策略难以应对不连续动力学和部分观测性。本文提出时空最优传输注意力（SO-TA），一种三模态（视觉、力/力矩、本体感）融合骨干，利用熵正则化最优传输对齐力-位姿子查询与视觉补丁，提供结构化归纳偏置，并配合扩散序列策略。在三个真实机器人任务上，SO-TA在紧配合销孔任务中达到100%成功率，且在光照、干扰物等扰动下保持82.5%成功率，远超基线，并具有可解释性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.20433v1/fig-001.webp\", \"caption\": \"Fig. 1. Overall forward pass of the tri-modal imitation-learning pipeline: an observation window is preprocessed, fused into a single encoding, and consumed by a diffusion policy that emits a pose-action chunk; the chunk is integrated and clamped before being sent to an impedance loop.\", \"page\": 3, \"index\": 1, \"width\": 500, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2605.20433v1/fig-002.webp\", \"caption\": \"Fig. 2. End-to-end SO-TA fusion pipeline. Red-bordered blocks are learnable, gray blocks are tensor variables. The OT-based attention is detailed in Fig. 3.\", \"page\": 3, \"index\": 2, \"width\": 470, \"height\": 498}, {\"url\": \"assets/figures/arxiv/2605.20433v1/fig-003.webp\", \"caption\": \"Fig. 3. Compact view of the OT-based attention node inside SO-TA. The subqueries and the row-marginal supply γ are both generated from the force–pose conditioning vector; the column marginal is uniform; transport is solved by log-domain Sinkhorn.\", \"page\": 3, \"index\": 3, \"width\": 516, \"height\": 341}, {\"url\": \"assets/figures/arxiv/2605.20433v1/fig-004.webp\", \"caption\": \"TABLE I KEY HYPERPARAMETERS USED IN ALL EXPERIMENTS\", \"page\": 5, \"index\": 4, \"width\": 458, \"height\": 398}, {\"url\": \"assets/figures/arxiv/2605.20433v1/fig-005.webp\", \"caption\": \"Fig. 9. Peg-in-hole: SO-TA interpretability across three phases. Top: OTbased patch heatmap from (17). Bottom: modality ratios (16). Vision share peaks before contact; F/T and pose grow during search and insertion.\", \"page\": 6, \"index\": 5, \"width\": 516, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2605.20433v1/fig-006.webp\", \"caption\": \"Fig. 16. Erasing: SO-TA interpretability across three phases. Vision peaks before contact; visual and force shares are jointly emphasized during wiping; pose dominates during retreat.\", \"page\": 7, \"index\": 6, \"width\": 516, \"height\": 283}]"
motivation: 接触丰富操作任务中，现有单模态或双模态策略局限性明显，多模态融合缺乏针对接触任务的结构化归纳偏置。
method: 提出时空最优传输注意力（SO-TA），用熵正则化最优传输对齐力-位姿子查询与视觉补丁，替代传统softmax注意力，并配合扩散模型进行序列策略学习。
result: "在三个真实机器人任务（紧配合销孔、连接器插入、曲面擦除）中，SO-TA达到100%成功率（紧配合销孔），在扰动下保持82.5%，远超融合基线（43.5%）。"
conclusion: SO-TA通过最优传输注意力提供任务相关的归纳偏置，实现了鲁棒、可解释的三模态融合策略，显著提升接触丰富操作性能。
---

## 摘要
接触丰富的操作任务，如紧公差插入、连接器配接、抛光以及表面贴合擦拭，对于数据驱动的控制器来说仍然困难，因为它们耦合了不连续的接触动力学、部分可观测性以及严格的安全约束。没有单一的传感模态足够：视觉在接触前提供全局背景，力/扭矩反馈在接触后控制交互，本体感觉姿态提供一致的运动学骨架。大多数先前用于接触丰富任务的模仿学习策略在单或双模态信号上运作，少数融合三种模态的策略通常采用现成的注意力模块，而没有关于注意力质量应如何在任务相关区域分布的明确先验。我们提出时空最优传输注意力（SO-TA），一种三模态融合骨干网络，用熵正则化的最优传输对齐力-姿态派生子查询与视觉补丁，替代了softmax归一化的补丁注意力。显式的边际约束作为接触丰富任务的结构化归纳偏置，鼓励条件感知的空间选择，在光照、干扰物和部分遮挡下保持稳定。SO-TA与基于扩散的序列策略配对，将观察窗口映射到姿态-动作块。我们在三个真实机器人任务上评估SO-TA：紧配合插销-孔组装、BCM线束连接器插入以及曲面标记擦除。在每种条件下约200次 rollout，SO-TA在紧配合插销-孔上达到100%成功率，而同等容量的交叉注意力为93%，并且在光照、干扰物和部分遮挡扰动下保持82.5%的成功率，而拼接基线下降到43.5%。OT派生的补丁热图和留一模态影响比率提供了可解释的、依赖于阶段的诊断。

## Abstract
Contact-rich manipulation tasks such as tight-clearance insertion, connector mating, polishing, and surface-conforming wiping remain difficult for data-driven controllers because they couple discontinuous contact dynamics, partial observability, and strict safety constraints. No single sensing modality suffices: vision supplies global context before contact, force/torque (F/T) feedback governs interaction after contact, and proprioceptive pose provides a consistent kinematic backbone. Most prior imitation-learning policies for contact-rich tasks operate on uni- or bi-modal signals, and the few that fuse three modalities typically adopt off-the-shelf attention modules with no explicit prior on how attention mass should be distributed across task-relevant regions. We present Spacetime Optimal-Transport Attention (SO-TA), a tri-modal fusion backbone that replaces softmax-normalized patch attention by an entropy-regularized Optimal Transport (OT) alignment between force-pose-derived sub-queries and visual patches. Explicit marginal constraints act as a structured inductive bias for contact-rich tasks, encouraging conditioning-aware spatial selection that is stable across illumination, distractors, and partial occlusion. SO-TA is paired with a diffusion-based sequence policy mapping observation windows to pose-action chunks. We evaluate SO-TA on three real-robot tasks: tight peg-in-hole assembly, BCM wiring-connector insertion, and curved-surface mark erasing. With ~200 rollouts per condition, SO-TA reaches 100% success on tight peg-in-hole versus 93% for cross-attention at matched capacity, and retains 82.5% success under illumination, distractor, and partial-occlusion perturbations where a concatenation baseline drops to 43.5%. OT-derived patch heatmaps and leave-one-out modality-influence ratios provide interpretable, phase-dependent diagnostics.

---

## 论文详细总结（自动生成）

# 详细中文论文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题领域**：接触丰富的机器人操作任务（如紧公差销孔插入、连接器配接、曲面擦拭等）对于数据驱动的控制器仍然困难，因为这类任务同时具有不连续的接触动力学、部分可观测性和严格的安全约束。
- **现有局限**：
  - 单一传感模态（视觉、力/力矩、本体感应）均不足以单独完成任务，需要三模态融合。
  - 已有三模态融合方法多采用现成的注意力模块（如softmax归一化的交叉注意力），缺乏针对接触任务的结构化归纳偏置，无法明确控制注意力质量在任务相关区域中的分布。
  - 面对光照变化、干扰物、部分遮挡等视觉扰动，传统融合方法（如拼接）和softmax注意力易受虚假显著性影响，鲁棒性不足。
- **本文贡献**：提出时空最优传输注意力（SO-TA），用熵正则化最优传输（OT）对齐力-位姿子查询与视觉补丁，提供结构化归纳偏置，提升融合的稳定性和可解释性，并与扩散策略结合形成完整的模仿学习系统。

## 2. 方法论

### 核心思想
- 将三模态（视觉、力/扭矩、本体感应）融合建模为力-位姿条件化的空间聚合问题，通过**熵正则化最优传输**（Optimal Transport）代替传统softmax归一化注意力。
- 引入**显式边际约束**：行边际（供应）由力-位姿令牌生成，列边际（容量）均匀分布，从而施加结构化先验，使注意力分配基于当前触觉状态。
- 配合**扩散策略**（Diffusion Policy）作为动作生成模块，预测未来动作块。

### 关键技术细节
- **令牌编码与条件化**：
  - 力和姿态观测通过MLP编码为令牌序列，拼接并投影得到条件嵌入 \( z_{fp} \in \mathbb{R}^{B \times T_w \times d_{att}} \)。
  - 视觉RGB帧通过ResNet-18提取补丁令牌 \( Z_{img} \)，加入2D位置编码。
- **OT注意力模块**（见图3）：
  - 从 \( z_{fp} \) 生成子查询 \( z_{qry} \in \mathbb{R}^{N_{sub} \times d_{att}} \) 和行边际供应 \( \gamma \in \mathbb{R}^{N_{sub}} \)（通过softmax归一化）。
  - 视觉补丁投影为键和值，计算余弦相似度成本。
  - 求解熵正则化OT问题：\( \min_{\Pi \geq 0} \langle \Pi, C \rangle - \varepsilon_{ot} H(\Pi) \)，约束行边际 \( \gamma \)、列边际 \( \beta = 1/N_{patch} \)，使用Sinkhorn算法（对数域迭代）求解。
  - 聚合：子查询收集消息 \( z_{sub} = \sum_p \Pi_{\ell,p} z_{val,p} \)，再以行边际加权合并得到 \( z_{sota} \)。
- **帧级与时间融合**：
  - 每帧将力、姿态、SO-TA输出令牌序列送入轻量级Transformer，通过softmax门控加权组合。
  - 加入时间编码后，经时间Transformer产生最终融合特征 \( Z_{fused} \)，供扩散策略使用。
- **可解释性**：
  - **模态影响比**：留一模态零掩码后计算输出差异，归一化得到各模态贡献比例。
  - **OT热图**：将有效补丁权重乘视觉影响比，重塑为热图叠加到图像上。

### 公式化流程
- 训练：行为克隆目标 + 扩散模型的ϵ预测损失。
- 推理：短窗口观测 → SO-TA融合 → 扩散逆过程 → 动作块 → 阻抗控制器。

## 3. 实验设计

### 任务与场景
| 实验任务 | 数据量 | 描述 |
|---------|--------|------|
| 紧配合销孔插入（tight peg-in-hole） | 198次演示，32,974步（3,297秒） | 间隙<0.2mm，初始位置随机扰动 |
| BCM线束连接器插入（wiring-connector insertion） | 396次演示，29,126步（2,916秒） | 视觉更杂乱，背景干扰大 |
| 曲面标记擦除（curved-surface mark erasing） | 199次演示，35,374步（3,437秒） | 需要定位、接触、控制法向力、持续滑动 |

### 基准方法
- **拼接基线**：视觉、力、姿态特征直接拼接。
- **交叉注意力基线**：用标准缩放点积注意力替换OT步骤。
- **模态消融**：保持拼接骨干，分别掩码力/扭矩、姿态或视觉。

### 评估协议
- 每种条件约200次rollout，设置5N安全阈值和100秒超时。
- 报告成功率和成功完成时间密度（核密度估计）。
- 视觉扰动测试：改变光照、添加干扰物、部分遮挡（不重新训练）。
- 可解释性分析：在演示回放中观察OT热图和模态比。

## 4. 资源与算力

- **训练硬件**：单个NVIDIA RTX 6000 Pro GPU
- **训练配置**：AdamW优化器，余弦预热，混合精度训练，EMA权重
- **训练时长**：约22小时（4,000 epoch，每epoch 8,192样本）
- **推理硬件**：RTX 4070 Ti SUPER（用于延迟测试）
- **参数规模**：SO-TA和交叉注意力均为61M总参数（DP 40M + 融合21M）；拼接81M（无独立融合参数）。
- **延迟**：在RTX 4070 Ti SUPER上，推理时间9.6-16.2 ms（1-10步逆扩散），满足10 Hz控制频率。

## 5. 实验数量与充分性

- **实验数量充足**：每个条件约200次rollout，三个任务共约1800次物理实验，统计显著性较好。
- **消融实验充分**：
  - 模态消融（掩码力/扭矩、姿态、视觉）证明了各模态贡献。
  - 融合方法对比（SO-TA vs 交叉注意力 vs 拼接）在相同参数预算下公平比较。
  - 扩散步数对性能与延迟的影响分析。
  - 视觉扰动泛化实验（无再训练）验证鲁棒性。
- **公平性**：所有融合方法使用相同扩散策略骨干，超参数一致（表I）。参数量对齐（SO-TA与交叉注意力均为61M）。
- **局限性**：未比较不同骨干网络（如Transformer-based policy变体）；未在更多样化任务（如抛光、密封）上验证；仅评估了三种基线，未包含更先进的多模态融合方法。

## 6. 主要结论与发现

- **性能提升**：
  - 紧配合销孔插入：SO-TA达100%成功率，交叉注意力93%，拼接99.5%（仅一例失败）。
  - 视觉扰动下：SO-TA保持82.5%成功率，拼接降至43.5%。
  - 连接器插入：SO-TA均值接近人类，但存在长尾超时（通过截断-重置包装器缓解）。
  - 曲面擦除：SO-TA成功率166/200（83%），人类平均17.6s vs 策略19.3s。
- **可解释性验证**：
  - 模态比随任务阶段动态变化：接触前视觉主导，接触后力/扭矩和姿态主导。
  - OT热图聚焦视觉显著区域，但略偏扩散（缺乏空间先验）。
- **泛化能力**：SO-TA对光照、干扰物、遮挡的鲁棒性显著优于基线，结构化融合是关键。

## 7. 优点

- **方法创新**：
  - 首次将熵正则化最优传输应用于接触丰富任务的视觉-触觉融合，替代softmax归一化，提供结构化归纳偏置。
  - 行边际由力-位姿条件化生成，无需显式阶段标签，自动实现阶段感知。
  - 提供内在可解释性信号（OT热图、模态影响比），有助于诊断和调试。
- **实验严谨性**：
  - 三个真实的、挑战性不同的任务，覆盖插入、连接、擦拭。
  - 物理机器人实验，约200次rollout/条件，统计可靠。
  - 参数对齐的公平比较，消融实验完整。
  - 视觉扰动测试无再训练，验证真实泛化能力。
- **实用价值**：
  - 所有硬件（RTX 4070 Ti SUPER）可满足10 Hz实时控制，具有部署潜力。
  - 截断-重置包装器实用技巧，解决长尾超时问题，无需修改策略。

## 8. 不足与局限

- **方法局限**：
  - OT热图仍较为扩散，因为成本函数（负余弦相似度）没有显式空间先验，未来可引入位置编码或空间正则项。
  - 结构化融合虽减少但未消除接触条件下的复合误差长尾，截断-重置包装器仅在运行时缓解。
  - 子查询数量 \( N_{sub}=2 \) 可能不是最优，需进一步调参。
- **实验覆盖**：
  - 仅测试了三种任务（插入、连接、擦拭），未涵盖抛光、密封、打磨等典型接触任务。
  - 未评估大规模多任务泛化或零样本迁移。
  - 基线仅包括拼接和交叉注意力，未与更先进的融合方法（如多模态Transformer变体、图网络）比较。
- **资源与重现性**：
  - 训练需单GPU约22小时，但未提供开源代码或预训练模型，复现困难（论文为预印本）。
  - 超参数（如OT温度 \( \varepsilon_{ot}=0.2 \)、Sinkhorn迭代5次）未进行系统灵敏度分析。
- **偏差风险**：
  - 演示收集通过遥操作（leader-follower），操作员展示风格可能引入偏差。
  - 视角变化有限（仅手腕相机），可能低估域偏移影响。

（完）
