---
title: "$π_0$-EqM: Equilibrium Matching for Closed-Loop Vision-Language-Action Control"
title_zh: "$π_0$-EqM: 闭环视觉-语言-动作控制的均衡匹配"
authors: "Huanming Liu, Congsheng Xu, Jianmin Ji, Yao Mu"
date: 2026-05-22
pdf: "https://arxiv.org/pdf/2605.23128v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 闭环视觉-语言-动作模型与均衡匹配
tldr: "当前视觉-语言-动作(VLA)模型广泛用于机器人操作，但其流匹配解码器固定采样步长，限制了状态相关的计算和跨周期时序复用。本文提出π₀-EqM，将π₀中的流匹配专家替换为均衡匹配(EqM)解码器，保持上游VLA栈不变。在300步预算下，π₀-EqM在RoboTwin 19个任务上将平均成功率从40.4%提升至50.2%，在LIBERO上保持竞争力，尤其在LIBERO-10上达87.0%。阈值扫描揭示残差与成功率之间存在任务依赖的非单调关系，即平稳-可执行性差距。结果表明迭代VLA控制中的推理深度是策略设计的一部分，并引入了基于能量的VLA视角，为未来跨任务和本体的可组合动作生成提供参考。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.23128v1/fig-001.webp\", \"caption\": \"TABLE I DEPLOYMENT SUCCESS RATES ON THE 19-TASK ROBOTWIN BENCHMARK UNDER A MATCHED 300-STEP BUDGET. THE TWO METHODS SHARE THE SAME UPSTREAM VLA STACK; ONLY THE ACTION DECODER DIFFERS.\", \"page\": 4, \"index\": 1, \"width\": 894, \"height\": 288}, {\"url\": \"assets/figures/arxiv/2605.23128v1/fig-002.webp\", \"caption\": \"Fig. 3. Qualitative EqM inference trajectory on click alarmclock, showing early attraction, semantic shaping, and over-refinement.\", \"page\": 4, \"index\": 2, \"width\": 484, \"height\": 369}, {\"url\": \"assets/figures/arxiv/2605.23128v1/fig-003.webp\", \"caption\": \"Fig. 2. Threshold scans on two RoboTwin tasks. The preferred threshold differs across tasks, so the same residual threshold changes both solver cost and closed-loop behavior.\", \"page\": 3, \"index\": 3, \"width\": 485, \"height\": 315}, {\"url\": \"assets/figures/arxiv/2605.23128v1/fig-004.webp\", \"caption\": \"Fig. 1. Overview of π0-EqM. We replace only the action decoder in π0 and cast action generation as iterative equilibrium solving, enabling adaptive stopping and warm starts. An executable intermediate action may appear before full numerical convergence.\", \"page\": 1, \"index\": 4, \"width\": 511, \"height\": 591}]"
motivation: 现有VLA模型的流匹配动作解码器固定采样步长，限制了状态相关的计算和时序复用，需要更灵活的解码方式。
method: 将π₀中的流匹配专家替换为均衡匹配(EqM)解码器，其余VLA部分保持不变，实现自适应推理深度。
result: "在300步预算下，RoboTwin平均成功率从40.4%提升至50.2%，LIBERO-10达到87.0%，并发现任务依赖的残差-成功率非单调关系。"
conclusion: 迭代VLA控制中的推理深度是策略设计的一部分，平衡匹配解码器提供了能量视角，有助于未来可组合动作生成。
---

## 摘要
当前，视觉-语言-动作（VLA）模型由于其强大的任务泛化潜力，已成为机器人操作中最常用的范式。然而，大多数用于VLA控制的生成式流匹配动作解码器通常采用固定的采样步长，限制了依赖于状态的算力分配以及控制周期之间的时间重用。我们提出了$π_0$-EqM，它将$π_0$中的流匹配专家替换为均衡匹配（EqM）解码器，同时保持上游VLA堆栈不变。在匹配的300步预算下，$π_0$-EqM在19个任务中将RoboTwin的平均成功率从40.4%提高到50.2%，并在LIBERO上保持竞争力，在LIBERO-10上增益最为明显（87.0%）。两次阈值扫描揭示了任务依赖的残差与成功率之间的非单调关系，我们称之为平稳性-可执行性差距。结果表明，迭代VLA控制中的推理深度是策略设计的一部分，并引入了一种基于能量的VLA视角，这可能为未来跨任务和本体的可组合动作生成研究提供参考。

## Abstract
Currently, Vision-Language-Action (VLA) models have become the most adopted paradigm for robotic manipulation for its great potential for task generalization. While most generative flow-matching action decoders for VLA control are often deployed with fixed sampling horizons, limiting state-dependent compute and temporal reuse across control cycles. We present $π_0$-EqM, which replaces the flow-matching expert in $π_0$ with an Equilibrium Matching (EqM) decoder while leaving the upstream VLA stack unchanged. Under a matched 300-step budget, $π_0$-EqM improves RoboTwin average success from 40.4% to 50.2% across 19 tasks and remains competitive on LIBERO, with its clearest gain on LIBERO-10 (87.0%). Two threshold scans reveal a task-dependent non-monotonic relation between residual and success, which we term the stationarity--executability gap. The results suggest that inference depth in iterative VLA control is part of policy design and introduce an energy-based VLA perspective that may inform future work on composable action generation across tasks and embodiments.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有视觉-语言-动作（VLA）模型中的动作解码器通常采用固定的采样步长（如扩散或流匹配的时间索引），无法根据机器人当前状态动态调整计算量，也无法在连续控制周期之间复用中间迭代结果。这种固定设计限制了测试时的计算灵活性和时序相干性。
- **研究动机**：希望将推理深度（即迭代步数）视为策略设计的一部分，而非固定的采样器参数。通过引入时间无关的均衡匹配（EqM）解码器，实现自适应停止和热启动，从而在相同计算预算下获得更好的闭环控制性能。
- **整体含义**：该工作不但提升了VLA策略在多个基准上的成功率，还揭示了一个新现象——“平稳性-可执行性差距”（残差最小的迭代不一定执行效果最好），并提出了基于能量的VLA视角，为未来跨任务和本体的可组合动作生成铺路。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将π₀中的流匹配动作专家替换为均衡匹配（Equilibrium Matching, EqM）解码器，其余VLA堆栈（视觉编码器、语言条件、动作语义头）保持不变。EqM学习一个与时间无关的条件向量场，其根对应目标动作块。推理时通过迭代求解向量场零点来解码动作，而非沿固定时间路径采样。
- **关键技术细节**：
  - **训练目标**：给定演示动作块A、结构噪声ε和插值因子γ，定义插值样本A_γ = γA + (1-γ)ε。训练EqM向量场f_θ(A_γ; c_t)匹配目标向量w(γ)(A-ε)。损失函数为L_EqM = E[||f_θ(A_γ; c_t) - w(γ)(A-ε)||²]。其中w(γ)设计为在γ→1时衰减至零，确保数据流形成为稳定平衡点。
  - **推理过程**：从初始点A_t^(0)出发，使用Nesterov加速梯度下降求解f_θ(A; c_t)=0。迭代公式：
    - Ẵ_t^(k) = A_t^(k) + μ (A_t^(k) - A_t^(k-1))
    - A_t^(k+1) = Ẵ_t^(k) - η f_θ(Ẵ_t^(k); c_t)
  - **自适应停止**：定义归一化前瞻残差 r_k = ||f_θ(Ẵ_t^(k); c_t)||₂ / √(Hd)。当r_k ≤ τ或达到最大迭代次数K_max时停止。阈值τ是可控策略变量。
  - **结构热启动**：由于EqM时间无关，可以将上一控制周期的前半段输出动作块直接复制到当前初始化的前半段，后半段填充随机噪声：A_t^(0) = [A_{t-1, copy}^{(out)}, ε_tail]。这利用了时序相干性，减少所需迭代次数。
- **理论解释（命题1）**：在局部范围内，若向量场可视为梯度场，则迭代等价于能量下降。给出残差下界和所需迭代次数的上界，说明热启动可以降低初始化距离，从而减少达到目标阈值所需的步数。

### 3. 实验设计：数据集、Benchmark、对比方法

- **数据集与Benchmark**：
  - **RoboTwin**：19个任务，每个任务500条示范（100条干净+400条随机化），共9500条示范。这是一个双臂操作基准，包含多种操作场景。
  - **LIBERO**：四个套件（LIBERO-Spatial、LIBERO-Object、LIBERO-Goal、LIBERO-10），标准终身机器人学习基准，重点报告套件级成功率。
- **对比方法**：主要与原始π₀对比，保持上游VLA堆栈完全一致，仅替换动作解码器（π₀使用流匹配，π₀-EqM使用EqM）。
- **主要比较设置**：匹配300步推理总预算（即两种方法在相同计算开销下比较）。
- **额外实验**：阈值扫描实验，在RoboTwin的`place_dual_shoes`和`click_bell`任务上允许最多1000次EqM迭代，改变停止阈值τ观察成功率变化。

### 4. 资源与算力

- **未明确说明**：论文未提及使用的GPU型号、数量、训练时长等算力信息。仅提及“300步推理预算”和“最多1000次迭代”，但未给出训练阶段的算力消耗。读者无法据此评估训练成本或实际部署效率。

### 5. 实验数量与充分性

- **实验数量**：主要实验包括：
  - RoboTwin 19个任务的成功率对比（表I）。
  - LIBERO 4个套件的结果（表II）。
  - 两个任务的阈值扫描（图2）。
  - 单个任务`click_alarmclock`的定性迭代轨迹（图3）。
- **充分性与公平性**：
  - **充分性**：涵盖了通用VLA基准（LIBERO）和更具挑战性的双臂操作基准（RoboTwin），任务多样。但阈值扫描仅覆盖两个任务，样本量较少，无法全面评估“平稳性-可执行性差距”的普遍性。
  - **公平性**：对比方法在同一上游框架下仅改变解码器，预算匹配，公平合理。
  - **缺点**：缺少消融实验（如：单独分析EqM目标 vs. 自适应停止 vs. 热启动的贡献）；未在同一任务上进行多次运行报告方差（仅报告单次成功率）；未将π₀-EqM与其他前沿VLA模型（如RT-2、Octo）对比，仅与同源基线对比。

### 6. 论文的主要结论与发现

- **性能提升**：在300步预算下，π₀-EqM 在RoboTwin 19个任务上将平均成功率从40.4%提升至50.2%（+9.8个百分点），在LIBERO-10上达到87.0%（+1.8个百分点），其他LIBERO套件基本持平或微增。
- **任务依赖的非单调性**：阈值扫描显示，残差阈值与成功率呈非单调关系，不同任务的最优阈值不同。这表明较低的残差并不总是对应更好的执行效果，作者称其为“平稳性-可执行性差距”。
- **策略设计启示**：推理深度（停止阈值）是闭环VLA策略设计的一部分，而非纯粹的数值收敛问题。EqM的时间无关性使得部署时计算分配成为可控变量。
- **能量视角**：EqM将动作生成视为能量最小化过程，为未来组合式（composable）动作生成（跨任务、跨本体）提供了新视野。

### 7. 优点：方法或实验设计上的亮点

- **方法创新性**：将EqM（一种生成模型）引入VLA动作解码，替代标准的扩散/流匹配解码器，实现了时间无关、可自适应停止和热启动的迭代推理，思路新颖。
- **计算可控性**：EqM解码器允许在测试时动态分配算力，而不改变上游模型，这为部署中的实时性调整和资源受限场景提供了便利。
- **理论联动**：提供了局部能量下降的命题，为残差阈值作为停止准则提供了数学依据，并与热启动的加速效果建立了联系。
- **实验验证清晰**：在匹配计算预算的条件下进行对比，避免了公平性争议；阈值扫描直观揭示了residual-performance之间的复杂关系。
- **发现新现象**：“平稳性-可执行性差距”是一个值得关注的新观察，提醒研究者迭代解码中数值收敛与任务执行之间存在不一致。

### 8. 不足与局限

- **实验覆盖有限**：
  - 阈值扫描仅两个任务，不能说明“平稳性-可执行性差距”的普遍性。
  - 缺少消融实验：无法量化EqM目标函数、自适应停止、热启动各自贡献多少性能提升。
  - 未报告多次运行的统计量（均值、方差），单次结果可能受随机性影响。
- **理论局部性**：命题1仅适用于局部梯度场假设，而实际学习的EqM向量场未必是保守的（可视为广义的），因而该理论不能全局保证收敛性。
- **未对基线进行充分扩展**：仅与π₀对比，未与其他VLA模型（如扩散策略、OpenVLA、Octo等）比较，难以定位π₀-EqM在更广泛方法中的相对地位。
- **算力未报告**：缺乏训练成本信息，不利于判断方法实用性。
- **应用限制**：方法目前仅在仿真基准上验证，未在真实机器人上评估。EqM在高维、复杂动作空间上的收敛性和稳定性尚未在真实物理系统中验证。

（完）
