---
title: "GesVLA: Gesture-Aware Vision-Language-Action Model Embedded Representations"
title_zh: GesVLA：手势感知的视觉-语言-动作模型嵌入表示
authors: "Wenxuan Guo, Ziyuan Li, Meng Zhang, Yichen Liu, Yimeng Dong, Chuxi Xu, Yunfei Wei, Ze Chen, Erjin Zhou, Jianjiang Feng"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22812v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 将手势作为并行指令模态的通用视觉-语言-动作操控模型
tldr: 现有视觉-语言-动作（VLA）模型依赖文本指令，在复杂场景中难以解决空间歧义。本文提出手势感知VLA模型（GesVLA），将手势特征编码到潜空间，采用双VLM架构实现紧耦合，并构建可扩展的手势数据生成管道。实验表明，手势输入能显著提升目标定位精度和人机交互效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22812v1/fig-001.webp\", \"caption\": \"Fig. 1: Comparison between language-only VLA and our gesture-aware GesVLA. Left: Text-only instructions can be ambiguous in cluttered scenes, requiring complex descriptions. Right: By incorporating pointing gestures, GesVLA enables direct and intuitive target specification, leading to more accurate and efficient task execution.\", \"page\": 1, \"index\": 1, \"width\": 504, \"height\": 439}, {\"url\": \"assets/figures/arxiv/2605.22812v1/fig-002.webp\", \"caption\": \"Fig. 4: Experimental setup and tasks in real-world environments. The manipulation tasks include Pick-and-Place Block, Select Jelly, and Select Fruit/Vegetable under multimodal gesture-language instructions. We also independently evaluate gesture-conditioned intent reasoning tasks, where the model predicts target objects from pointing gestures.\", \"page\": 6, \"index\": 2, \"width\": 1054, \"height\": 352}, {\"url\": \"assets/figures/arxiv/2605.22812v1/fig-003.webp\", \"caption\": \"TABLE I: Real-robot manipulation experiment results. For Pick-and-Place Block, simple/hard subsets are divided by the number of blocks on the table: simple contains fewer than 5 blocks, while hard contains 5 or more blocks. For Select Jelly and Select Fruit/Vegetable, simple denotes single-object pick-and-place tasks, while hard denotes sequential multi-object pick-and-place tasks. Avg values are success rates over 20 trials per task. Total Avg is the mean of the three task averages.\", \"page\": 6, \"index\": 3, \"width\": 1014, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2605.22812v1/fig-004.webp\", \"caption\": \"Fig. 2: Overview of the proposed gesture-aware dual-VLM architecture. VLMint performs gesture-conditioned intent reasoning from multimodal instructions, producing textual and visual reasoning outputs. VLMper performs online perception by attending to the latent representations of VLMint via cross-attention. The action expert generates action trajectories through iterative denoising. The asymmetric attention design enables efficient inference by enforcing a unidirectional information flow, where VLMint does not attend to downstream modules, allowing it to be computed once and reused throughout execution.\", \"page\": 3, \"index\": 4, \"width\": 1046, \"height\": 500}, {\"url\": \"assets/figures/arxiv/2605.22812v1/fig-005.webp\", \"caption\": \"Fig. 3: Scalable gesture-aware data engine and two-stage training pipeline of GesVLA. Left: The semi-synthetic data engine synthesizes gesture trajectories on real RGB-D scenes, producing gesture data and paired language instructions with precise spatial annotations. Right: In Stage 1, VLMint is trained for gesture-conditioned intent reasoning using the generated data. In Stage 2, the dual-VLM policy is trained on real robot demonstrations with flow matching for action generation.\", \"page\": 4, \"index\": 5, \"width\": 1052, \"height\": 393}]"
motivation: 解决VLA模型在复杂场景中因文本指令空间模糊导致的定位困难。
method: 将手势作为并行指令模态，通过双VLM架构编码到潜空间，并构建渲染手模型到真实场景的数据生成管道。
result: 在多种真实机器人任务中，手势输入提升了目标定位精度和交互效率。
conclusion: 手势作为辅助模态能有效改善复杂场景下的机器人操作性能。
---

## 摘要
视觉-语言-动作模型通过统一感知和行动，展现出在通用机器人操作中的强大潜力。然而，现有的VLA系统主要依赖文本指令，在包含多个相似物体的复杂场景中难以解决空间歧义问题。为解决这一局限，我们引入手势作为并行指令模态，并提出一种手势感知的视觉-语言-动作模型（GesVLA）。我们的方法将手势特征直接编码到潜在空间中，使其同时参与高层推理和低层动作生成，并采用双VLM架构实现手势表示与动作策略之间的紧密耦合。在数据层面，我们通过将手部模型渲染到真实场景图像上，构建了一个可扩展的手势数据生成流水线。这减少了模拟到真实的视觉差距，同时生成了具有多样运动模式和对应指向标注的丰富数据。此外，我们采用两阶段训练策略，使模型同时具备手势感知和动作预测能力。我们在多个真实世界机器人任务上评估了该方法，包括用于验证的受控积木操作任务，以及更实际的场景，如产品与农产品挑选。实验结果表明，融入手势能够持续提升目标定位准确性和人机交互效率，特别是在复杂和杂乱环境中。项目页面：https://gwxuan.github.io/GesVLA/。

## Abstract
Vision-Language-Action (VLA) models have shown strong potential for general-purpose robot manipulation by unifying perception and action. However, existing VLA systems primarily rely on textual instructions and struggle to resolve spatial ambiguity in complex scenes with multiple similar objects. To address this limitation, we introduce gesture as a parallel instruction modality and propose a Gesture-aware Vision-Language-Action model (GesVLA). Our approach encodes gesture features directly into the latent space, enabling them to participate in both high-level reasoning and low-level action generation, and adopts a dual-VLM architecture to achieve tight coupling between gesture representations and action policies. At the data level, we construct a scalable gesture data generation pipeline by rendering hand models onto real-world scene images. This reduces the sim-to-real visual gap while producing rich data with diverse motion patterns and corresponding pointing annotations. In addition, we employ a two-stage training strategy to equip the model with both gesture perception and action prediction capabilities. We evaluate our approach on multiple real-world robotic tasks, including a controlled block manipulation task for validation and more practical scenarios such as product and produce selection. Experimental results show that incorporating gesture consistently improves target grounding accuracy and human-robot interaction efficiency, especially in complex and cluttered environments. Project page: https://gwxuan.github.io/GesVLA/.

---

## 论文详细总结（自动生成）

# GesVLA: Gesture-Aware Vision-Language-Action Model 论文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：现有 Vision-Language-Action (VLA) 模型主要依赖文本指令，在包含多个相似物体的杂乱场景中难以解决空间歧义。例如，指令“把这个拿起来”或“放在那里”缺乏精确的空间参照，导致目标定位失败。
- **研究动机**：人类自然通过指向手势（deictic gestures）来消除歧义，而现有方法要么将手势作为后处理信号，要么转化为离散文本导致信息损失，缺乏统一、紧耦合的融合机制。
- **整体含义**：本文提出 GesVLA，将手势作为与语言平级的并行指令模态，直接编码到潜空间，使模型同时具备意图推理和动作生成能力，显著提升复杂场景中的人机交互效率。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- **手势作为一等模态**：不同于将手势转换为文本或后处理，GesVLA 将手部关键点特征通过 MLP 投影到连续潜空间，与视觉、语言特征共同参与推理和动作生成。
- **双 VLM 架构**：将意图推理（VLM_int）与在线感知（VLM_per）解耦，并通过跨注意力和 KV 缓存实现紧耦合单向信息流（VLM_int → VLM_per → 动作专家），提高推理效率。
- **可扩展数据生成**：通过将手部网格渲染到真实场景 RGB-D 图像上，合成带精确指向标注的手势数据，降低 sim-to-real 差距。
- **两阶段训练**：阶段1预训练 VLM_int 意图推理；阶段2冻结 VLM_int，联合训练 VLM_per 和基于流匹配的动作专家。

### 关键技术细节

#### 手势表示与编码
- **关键帧提取**：基于手部运动停滞捕捉指向瞬间，提取手腕和食指三关节的 4 个关键点，每个点表示 (x,y,d) → 12 维向量。
- **编码**：通过多层 MLP（φ）投影到潜空间：$z_{gi} = \phi(\text{Pose}(g_i))$。

#### 双 VLM 架构
- **VLM_int**：输入手势 G 和语言 T，输出推理结果 y（文本+视觉提示）及 KV 缓存 (K_int, V_int)。
- **VLM_per**：输入场景观测 O、语言 T 和推理结果 y，通过交叉注意力关注 VLM_int 的潜表示，产生用于动作的潜特征。
- **动作专家 F_θ**：基于流匹配，从噪声 $x_0 \sim N(0,I)$ 迭代去噪生成动作块 $a_{1:K} = F_θ(x_0, s_t, K_{per}, V_{per})$。

#### 数据生成引擎
- **目标选择**：使用 GroundingDINO 检测真实场景候选物体，随机选取目标并施加抖动模拟指向偏差。
- **3D 标注**：利用深度图和相机内参恢复 3D 目标点 $p = ((u-c_x)z/f_x, (v-c_y)z/f_y, z)$。
- **手部运动生成**：从随机方向逐步接近目标，多目标间采用抛物线式移动（提升项），生成逼真手势视频。
- **输出**：约 16k 个半合成样本，每样本包含手势视频、语言指令、推理监督信号（坐标及文本）和元数据。

#### 训练流程（算法级文字说明）
- **阶段1 意图推理预训练**：使用半合成数据，对 VLM_int 进行自回归交叉熵训练，将目标坐标离散化为特殊 token，联合学习语义推理和空间定位。
- **阶段2 联合训练**：在真实机器人演示数据上，对 VLM_per 和动作专家 F_θ 进行流匹配训练，目标是最小化速度预测误差 $\mathcal{L}_{action} = E[\|v_θ(x_t,t,c)-(x_1-x_0)\|^2]$。VLM_int 冻结。

#### 推理算法（Algorithm 1）
1. 输入：初始观测 O0、语言 T、手势 G、机器人状态 s0。
2. 手势关键帧提取与编码。
3. VLM_int 生成推理结果 y 及 KV 缓存。
4. 循环：每次控制步调用 VLM_per 并复用 KV 缓存，动作专家去噪生成动作块，执行并更新观测和状态。

## 3. 实验设计

### 数据集与场景
- **意图推理实验**：基于 300 张 RGB-D 真实场景构建半合成训练集（积木和盘子），88 个真实测试样本（≥7 物体），评估指向目标预测准确率。
- **操控实验**：三类真实任务各 20 次，共 60 次实验：
  - **Pick-and-Place Block**：从凌乱积木中抓取指定积木放至指定盘子，按物体数分简单（<5）和困难（≥5）。
  - **Select Jelly**：从多个盘子中的果冻杯按顺序抓取指定果冻并放置。
  - **Select Fruit/Vegetable**：从两个农产品箱中按顺序抓取指定水果/蔬菜。

### Benchmark 方法
- **Text-only VLA**：将手势替换为描述性文本，类似 π0。
- **MLLM + VLA**：先用 Qwen3.5-plus 推理手势目标生成视觉提示，再用 VLA 执行。
- **Geometric pipeline + VLA**：MediaPipe 提取手部关键点 + GroundingDINO + DepthAnythingV2 构建射线求交，再生成视觉提示给 VLA。
- **GesVLA (decoupled VLMs)**：去除跨注意力，仅用视觉提示接口。
- **GesVLA (ours)**：完整模型。

### 主要结果
- **意图推理**：GesVLA 准确率 94.3%（83/88），远超 MLLM（38.6%）和几何流水线（59.1%）。
- **操控实验**：GesVLA 总平均成功率 83.3%，显著高于 Text-only VLA（31.7%）、MLLM+VLA（31.7%）、几何+VLA（41.7%）、解耦VLM（61.7%）。在困难子集上优势更明显（如积木困难 9/10 vs 3/10）。

## 4. 资源与算力
- 硬件：4 块 H20 GPU 进行训练，单张 RTX 4090 GPU 进行推理。
- 框架：PaliGemma-2B 作为 VLM 骨干，π0 作为 VLM_per 初始化。
- 优化器：AdamW + 余弦 warmup + EMA。
- 未明确说明训练总时长或具体迭代步数。

## 5. 实验数量与充分性

### 实验数量
- 意图推理：88 个测试样本，2 个基线。
- 操控任务：3 个场景 × 2 难度（简单/困难）各 10 次，共 60 次真实机器人运行。
- 消融实验：5 组（数据引擎操作、训练设计、训练策略、模型架构、定性比较）。

### 充分性与客观性
- 充分性：覆盖了核心设计维度（数据增广、手势 MLP、训练策略、注意力机制），并对比了多种替代方案。
- 客观性：所有基线均保持相同训练数据与设置，并报告了单次成功次数而非平均值，结果透明。
- 局限性：测试场景数量有限（每任务 20 次），缺乏跨机器人或跨环境泛化验证，可能存在随机性影响。

## 6. 主要结论与发现
1. **手势显著提升目标定位**：在杂乱场景中，纯语言指令成功率仅 31.7%，加入手势后提升至 83.3%。
2. **端到端潜空间融合优于管道式**：GesVLA（含跨注意力）优于解耦版本（仅视觉提示，61.7%），说明离散化会造成信息瓶颈。
3. **半合成数据有效支持 sim-to-real**：VLM_int 在合成数据上预训练可在真实场景达到 94.3% 准确率，且冻结后效果更优。
4. **两阶段训练策略关键**：联合训练会因合成数据主导而损害动作学习（45.0%），两阶段冻结 VLM_int 最佳（83.3%）。
5. **数据增强和抖动很重要**：移除坐标抖动导致准确率暴跌至 42.0%，移除手部外观增强降至 76.1%。

## 7. 优点
- **创新性架构**：同时将手势作为连续模态融合，并通过不对称注意力和 KV 缓存实现高效推理（1: T: T×N 计算比），可复用。
- **可扩展数据生成**：半合成策略结合真实背景与合成手势，精确标注，易扩展至新物体和新任务，填补了手势-机器人数据空白。
- **全面的实验验证**：从意图推理到真实操控，进行了多任务、多难度、多基线对比和充分消融，结果可靠。
- **实用性强**：使用常见硬件（单卡 4090 推理）和开源组件，代码和项目页面公开，具备可复现性。

## 8. 不足与局限
- **仅支持指向手势**：未涉及更复杂手势（如握拳、抓取、示意）或动态连续手势，应用范围有限。
- **测试规模有限**：每任务仅 20 次运行，统计稳定性不足；未在多个机器人平台或跨场景验证泛化能力。
- **依赖深度图**：数据生成需要真实深度图；手部关键点提取依赖 MediaPipe，在极端遮挡或光照下可能失效。
- **缺少人机交互实际延迟分析**：未报告端到端推理延迟（尤其多步执行时）或用户满意度指标。
- **未讨论失败案例**：仅在定性部分展示成功案例，未深入分析失败原因（如 94.3% 的 5 个错误样本）。
- **消融中“去掉视觉提示”性能下降明显**：表明视觉提示仍是关键，但论文应进一步讨论为何文本提示未能提升性能（已解释跨注意力已足够）。

（完）
