---
title: "VAMPO: Policy Optimization for Improving Visual Dynamics in Video Action Models"
title_zh: VAMPO：通过策略优化提升视频动作模型中的视觉动力学
authors: "Zirui Ge, Pengxiang Ding, Baohua Yin, Qishen Wang, Zhiyong Xie, Yemin Wang, Jinbo Wang, Hengtao Li, Runze Suo, Wenxuan Song, Han Zhao, Shangke Lyu, Zhaoxin Fan, Haoang Li, Ran Cheng, Cheng Chi, Huibin Ge, Yaozhi Luo, Donglin Wang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19370v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 改进视频动作模型中的视觉动力学以用于机器人控制
tldr: 针对视频动作模型在机器人控制中存在的视觉动力学精度不足问题（如物体位姿、接触时机等误差），本文提出了VAMPO框架。该方法将扩散模型的去噪过程建模为序列决策过程，通过策略优化（结合GRPO和欧拉混合采样器）直接优化潜空间中的视觉动力学奖励。实验证明，VAMPO显著提升了视频预测的准确性，进而增强了下游动作生成的成功率和泛化能力。
selection_source: fresh_fetch
motivation: 现有的基于扩散的视频预测模型采用似然代理目标函数，导致其在处理对操作至关重要的精确视觉动力学（如空间关系和接触时机）时存在偏差。
method: 将多步去噪过程表述为序列决策任务，并引入欧拉混合采样器与GRPO算法，通过在潜空间定义专家视觉动力学奖励来优化去噪策略。
result: 在模拟和真实世界的多种操作任务中，VAMPO有效改善了任务相关的视觉动力学，提升了动作生成的质量和模型的泛化性能。
conclusion: 通过策略优化直接对齐视觉动力学，是提升视频动作模型在复杂机器人操作任务中表现的一种有效且实用的后训练方案。
---

## 摘要
视频动作模型是视觉-语言-动作（VLA）系统极具吸引力的基础，因为它们可以从大规模视频数据中学习视觉动力学，并将这些知识迁移到下游机器人控制中。然而，目前的基于扩散的视频预测器通常采用似然代理目标进行训练，这虽然能产生全局合理的预测，但未能显式优化对操作任务至关重要的精确视觉动力学。这种目标不匹配往往会导致物体姿态、空间关系和接触时机方面的细微误差，并可能被下游策略放大。我们提出了 VAMPO，这是一个通过策略优化直接改进视频动作模型中视觉动力学的后训练框架。我们的核心思想是将多步去噪过程建模为一个序列决策过程，并利用在潜空间中定义的专家视觉动力学奖励来优化去噪策略。为了使这种优化具有可行性，我们引入了一种欧拉混合采样器（Euler Hybrid sampler），它仅在第一个去噪步骤中注入随机性，从而在保持后续去噪轨迹连贯性的同时，实现了可处理的低方差策略梯度估计。我们进一步将该设计与 GRPO 和可验证的非对抗性奖励相结合。在多种模拟和现实世界的操纵任务中，VAMPO 显著改进了与任务相关的视觉动力学，从而实现了更优的下游动作生成和更强的泛化能力。项目主页：https://vampo-robot.github.io/VAMPO/。

## Abstract
Video action models are an appealing foundation for Vision--Language--Action systems because they can learn visual dynamics from large-scale video data and transfer this knowledge to downstream robot control. Yet current diffusion-based video predictors are trained with likelihood-surrogate objectives, which encourage globally plausible predictions without explicitly optimizing the precision-critical visual dynamics needed for manipulation. This objective mismatch often leads to subtle errors in object pose, spatial relations, and contact timing that can be amplified by downstream policies. We propose VAMPO, a post-training framework that directly improves visual dynamics in video action models through policy optimization. Our key idea is to formulate multi-step denoising as a sequential decision process and optimize the denoising policy with rewards defined over expert visual dynamics in latent space. To make this optimization practical, we introduce an Euler Hybrid sampler that injects stochasticity only at the first denoising step, enabling tractable low-variance policy-gradient estimation while preserving the coherence of the remaining denoising trajectory. We further combine this design with GRPO and a verifiable non-adversarial reward. Across diverse simulated and real-world manipulation tasks, VAMPO improves task-relevant visual dynamics, leading to better downstream action generation and stronger generalization. The homepage is https://vampo-robot.github.io/VAMPO/.

---

## 论文详细总结（自动生成）

以下是对论文《VAMPO: Policy Optimization for Improving Visual Dynamics in Video Action Models》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视频动作模型（Video Action Models）在训练视频预测器（VPM）时，通常采用似然代理目标（如 ELBO）。这种目标函数倾向于生成“全局合理”的图像，但在机器人操作所需的**高精度视觉动力学**（如物体位姿、空间关系、接触时机）上缺乏显式优化。
*   **研究动机**：视频预测中的细微误差（如抓取位置偏离几毫米）会被下游动作生成模型（AGM）放大，导致任务失败。作者希望通过强化学习（RL）直接优化预测质量，使模型生成的视觉特征更符合物理真实和操作需求。

### 2. 方法论
VAMPO 将多步去噪过程视为一个序列决策过程，并引入后训练（Post-training）框架进行优化：
*   **核心思想**：将扩散模型的去噪步骤建模为 MDP。从噪声潜变量开始，每一步去噪动作由策略（模型）决定，最终状态是预测的未来潜变量。通过奖励函数引导模型生成与专家轨迹一致的视觉动力学。
*   **关键技术：欧拉混合采样器（Euler Hybrid Sampler）**：
    *   为了解决策略梯度估计的方差问题和长时程信用分配难题，该采样器**仅在去噪的第一步注入随机性（SDE 采样）**，产生可微的概率分布。
    *   后续步骤采用确定性 ODE 采样，确保生成轨迹的连贯性。
*   **优化算法：GRPO**：采用组相对策略优化（Group Relative Policy Optimization），通过一组样本的相对奖励来估计优势函数，无需额外的 Critic 网络，提高了训练稳定性。
*   **可验证奖励（Verifiable Reward）**：在潜空间（Latent Space）中计算预测值与专家真实值之间的 **L1 距离**和**余弦相似度**，直接对齐视觉动力学特征。

### 3. 实验设计
*   **数据集与场景**：
    *   **模拟环境**：使用 CALVIN 基准测试（ABC→D 协议），评估长时程（Long-horizon）语言条件下的机器人操作。引入 L-CALVIN 将任务序列从 5 步扩展到 10 步。
    *   **真实世界**：在 Agibot Genie 01（双臂机器人）、Flexiv 和 VidowX 平台上进行验证，涵盖杂乱环境抓取、单臂精确放置和双臂协同任务。
*   **对比方法**：
    *   **VLM-based**：OpenVLA, $\pi_0$, $\pi_{0.5}$ 等。
    *   **VPM-based**：UniPi (像素级), Seer, VPP (潜变量级) 等。

### 4. 资源与算力
*   **算力投入**：
    *   **VPM 后训练**：使用 **64 台 NVIDIA H20 GPU**，训练约 1.5k 步。
    *   **AGM 训练**：使用 **8 台 NVIDIA H20 GPU**，训练约 10 个 epoch。
    *   **推理/评估**：在 NVIDIA RTX 5880 GPU 上执行。
*   **数据规模**：使用 CALVIN ABC 数据集的 18,033 条轨迹，切分为约 12.9 万个视频样本。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了模拟环境的大规模测试（1-10 步连续任务）和 3 类真实世界复杂任务。
*   **消融实验**：非常充分，分别针对**训练步数、优化算法（GRPO vs DDPO）、采样策略（1步 SDE vs 多步 SDE）以及奖励类型（潜空间 vs 像素空间）**进行了对比。
*   **客观性**：通过 SVD 分解计算雅可比矩阵的有效秩（Effective Rank），从数学层面证明了 VAMPO 增强了视觉特征与动作生成之间的耦合度，论证较为严谨。

### 6. 主要结论与发现
*   **性能提升**：VAMPO 在 CALVIN ABC→D 任务上的成功率显著超过基准模型（VPP），尤其在 5 步连续任务中表现优异。
*   **长时程优势**：在 L-CALVIN（10 步任务）中，VAMPO 的领先优势扩大，证明了精确视觉动力学对复杂任务的重要性。
*   **视觉对齐**：定性分析显示，VAMPO 有效抑制了视频预测中的幻觉，纠正了物体位姿和接触逻辑。
*   **泛化性**：在未见过的真实场景和背景下，模型依然能保持较高的操作精度。

### 7. 优点与亮点
*   **即插即用**：作为一种后训练框架，VAMPO 不需要修改原有的模型架构，具有很强的通用性。
*   **采样器创新**：欧拉混合采样器巧妙平衡了“探索（随机性）”与“稳定性（确定性）”，解决了扩散模型结合 RL 时的训练难题。
*   **效率优化**：通过在潜空间进行奖励计算和优化，避免了昂贵的像素级解码开销。

### 8. 不足与局限
*   **RL 训练不稳定性**：实验中观察到训练过程中性能存在波动（如 600 步时出现下降），反映了强化学习固有的不稳定性。
*   **依赖专家数据**：奖励函数依赖于预先存在的专家视频潜变量，在缺乏高质量演示数据的领域可能受限。
*   **计算成本**：虽然推理高效，但后训练阶段需要 64 块 H20 GPU，对于资源受限的团队门槛较高。

（完）
