---
title: "DexGrasp-Zero: A Morphology-Aligned Policy for Zero-Shot Cross-Embodiment Dexterous Grasping"
title_zh: DexGrasp-Zero：一种用于零样本跨本体灵巧抓取的形态对齐策略
authors: "Yuliang Wu, Yanhan Lin, WengKit Lao, Yuhao Lin, Yi-Lin Wei, Wei-Shi Zheng, Ancong Wu"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16806v2"
tags: ["query:rob-il"]
score: 8.0
evidence: 用于零样本跨具身灵巧抓取的形态对齐策略
tldr: "针对多样化灵巧手硬件的跨具身抓取难题，本文提出DexGrasp-Zero框架。该方法通过形态对齐的图表示将不同手的运动学关键点映射到统一节点，并利用MAGCN网络结合物理属性注入机制，实现了通用抓取技能的学习。实验表明，该策略在未见过的灵巧手上达到了85%的零样本迁移成功率，显著优于现有方法，并成功应用于真实世界机器人平台。"
selection_source: fresh_fetch
motivation: 现有的跨具身抓取方法在处理异构运动学和物理约束时容易产生误差，难以实现高效的零样本迁移。
method: 提出一种形态对齐的图卷积网络（MAGCN），通过统一的图表示和物理属性注入机制来编码不同灵巧手的结构与运动约束。
result: "在仿真中对未见过的硬件实现了85%的零样本抓取成功率，在真实世界实验中达到82%的平均成功率。"
conclusion: DexGrasp-Zero通过结构与语义对齐，成功实现了灵巧手抓取策略在异构硬件间的零样本泛化。
---

## 摘要
为了满足日益多样化的灵巧手硬件需求，开发一种无需冗余重新学习即可实现零样本跨本体抓取的策略至关重要。由于异构手的运动学和物理约束，跨本体对齐具有挑战性。现有方法通常预测中间运动目标并将其重定向到每个本体，这可能会引入误差并违反特定本体的限制，从而阻碍在不同手之间的迁移。为了克服这些局限性，我们提出了 DexGrasp-Zero，这是一种从多样化本体中学习通用抓取技能的策略，能够实现向未知手的零样本迁移。我们首先引入了一种形态对齐的图表示，将每只手的运动学关键点映射到具有解剖学基础的节点，并为每个节点配备三轴正交运动原语，从而实现不同形态之间的结构和语义对齐。依靠这种基于图的表示，我们设计了一个形态对齐图卷积网络（MAGCN）来对图进行编码以进行策略学习。MAGCN 结合了物理属性注入机制，将特定于手的物理约束融合到图特征中，从而能够针对不同的连杆长度和驱动限制进行自适应补偿，实现精确且稳定的抓取。我们在 YCB 数据集上进行的广泛仿真评估表明，我们的策略在四种异构手（Allegro、Shadow、Schunk、Ability）上进行了联合训练，在未知硬件（LEAP、Inspire）上实现了 85% 的零样本成功率，比现有最先进方法高出 59.5%。真实世界实验进一步在三个机器人平台（LEAP、Inspire、Revo2）上评估了我们的策略，在未知物体上实现了 82% 的平均成功率。

## Abstract
To meet the demands of increasingly diverse dexterous hand hardware, it is crucial to develop a policy that enables zero-shot cross-embodiment grasping without redundant re-learning. Cross-embodiment alignment is challenging due to heterogeneous hand kinematics and physical constraints. Existing approaches typically predict intermediate motion targets and retarget them to each embodiment, which may introduce errors and violate embodiment-specific limits, hindering transfer across diverse hands. To overcome these limitations, we propose DexGrasp-Zero, a policy that learns universal grasping skills from diverse embodiments, enabling zero-shot transfer to unseen hands. We first introduce a morphology-aligned graph representation that maps each hand's kinematic keypoints to anatomically grounded nodes and equips each node with tri-axial orthogonal motion primitives, enabling structural and semantic alignment across different morphologies. Relying on this graph-based representation, we design a Morphology-Aligned Graph Convolutional Network (MAGCN) to encode the graph for policy learning. MAGCN incorporates a Physical Property Injection mechanism that fuses hand-specific physical constraints into the graph features, enabling adaptive compensation for varying link lengths and actuation limits for precise and stable grasping. Our extensive simulation evaluations on the YCB dataset demonstrate that our policy, jointly trained on four heterogeneous hands (Allegro, Shadow, Schunk, Ability), achieves an 85% zero-shot success rate on unseen hardware (LEAP, Inspire), outperforming the state-of-the-art method by 59.5%. Real-world experiments further evaluate our policy on three robot platforms (LEAP, Inspire, Revo2), achieving an 82% average success rate on unseen objects.

---

## 论文详细总结（自动生成）

以下是对论文《DexGrasp-Zero: A Morphology-Aligned Policy for Zero-Shot Cross-Embodiment Dexterous Grasping》的深度结构化总结：

### 1. 论文的核心问题与整体含义
随着灵巧手硬件的快速迭代，不同设备间的形态差异（如关节数量、连杆长度、自由度分布）导致针对特定硬件训练的抓取策略难以通用。
*   **研究动机**：现有的跨具身（Cross-Embodiment）抓取方法通常依赖于中间运动目标（如指尖位置或MANO模型）再进行重定向（Retargeting），这往往会违反特定硬件的物理约束，导致动作不可行。
*   **核心目标**：开发一种通用的抓取策略，能够实现从已知灵巧手到未知灵巧手的**零样本（Zero-Shot）**迁移，无需针对新硬件进行任何重新训练或微调。

### 2. 论文提出的方法论
论文提出了 **DexGrasp-Zero** 框架，其核心思想是通过形态对齐的图表示和物理属性注入来实现跨硬件的语义统一。
*   **形态对齐的图表示（Morphology-Aligned Graph）**：将任何灵巧手抽象为具有解剖学意义的图结构。节点分为6类语义类型（如指尖、近端、腕部等），边表示运动学连接。
*   **通用运动原语空间（Hand-Agnostic Motion Primitives）**：为每个图节点定义了三个正交的运动原语：**屈曲（Flexion）**、**外展（Abduction）**和**轴向旋转（Axial Rotation）**。这统一了不同手的动作空间。
*   **MAGCN 网络架构**：
    *   **物理属性注入（Physical Property Injection）**：从 URDF 文件中提取连杆长度、关节限制、阻尼系数等静态物理参数，并将其注入到 GCN 的每一层中，使策略能感知硬件限制。
    *   **层级融合**：通过多层图卷积处理节点特征，并结合全局特征（物体位置、速度等）输出动作。
*   **确定性映射 $M_h$**：通过一个固定的、特定于硬件的线性算子，将通用的运动原语直接映射为物理关节指令，避免了复杂的重定向过程。

### 3. 实验设计
*   **仿真环境**：使用 RaiSim 物理引擎。
*   **数据集与 Benchmark**：
    *   **CrossDex/YCB 协议**：包含 45 个 YCB 物体。
    *   **GraspXL 协议**：使用 ShapeNet 和 PartNet 物体进行大规模训练和测试。
*   **对比硬件**：
    *   **训练手（Seen）**：Allegro, Shadow, Ability, Schunk。
    *   **测试手（Unseen）**：LEAP, Inspire, Revo2（真实世界）。
*   **对比方法**：主要对比了 CrossDex（SOTA 跨具身方法）和 GraspXL（单手专家模型）。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 3090 GPU** 进行训练和推理。
*   **训练时长**：在仿真中进行了 **6000 轮（rounds）** 的 PPO 强化学习训练。
*   **并行度**：为每个（手，物体）对实例化了 3 个并行仿真环境，控制频率为 20Hz。

### 5. 实验数量与充分性
论文进行了多维度的广泛实验，体现了高度的客观性：
*   **跨具身迁移实验**：在 4 种训练手上训练，在 2 种完全未见的仿真手上测试。
*   **单手迁移实验**：验证了即使只在一种手上训练，该架构也具备一定的泛化能力。
*   **消融实验**：分别验证了运动原语、物理属性注入、激活掩码（Activation Mask）等组件的必要性。
*   **真实世界部署**：在 3 个不同的机器人平台（Kinova/Piper 机械臂 + 不同灵巧手）上对 10 种未见物体进行了 150 次试验。
*   **扩展性测试**：在附录中展示了该方法甚至能迁移到非类人结构（如 3 指的 Barrett Hand）。

### 6. 论文的主要结论与发现
*   **性能突破**：在未见过的硬件（LEAP, Inspire）上实现了 **85%** 的零样本抓取成功率，比之前的 SOTA 方法（CrossDex）提升了 **59.5%**。
*   **真实世界有效性**：在真实场景中达到了 **82%** 的平均成功率，证明了从仿真到现实（Sim-to-Real）的良好迁移性。
*   **物理感知的重要性**：消融实验证明，注入 URDF 物理先验能显著提升策略在处理不同连杆长度和驱动限制时的稳定性。

### 7. 优点
*   **端到端一致性**：通过统一的图表示和运动原语，消除了传统方法中“预测+重定向”带来的误差累积。
*   **物理约束感知**：MAGCN 的层级物理注入机制让模型能够“量体裁衣”，自动补偿不同硬件的机械差异。
*   **极强的泛化性**：不仅支持不同型号的类人灵巧手，还展现了向非类人夹爪迁移的潜力。

### 8. 不足与局限
*   **触觉反馈缺失**：目前主要依赖视觉和本体感受，在处理极小物体时，由于缺乏触觉反馈，容易出现“空抓”或抓取不稳的情况。
*   **硬件尺寸限制**：对于尺寸极小的灵巧手（如 Revo2），在抓取大型物体时受限于物理包络范围，成功率会有所下降。
*   **静态物体假设**：真实世界实验中假设物体初始是静止的，尚未充分探讨动态环境下的交互。

（完）
