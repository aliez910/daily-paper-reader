---
title: "AerialVLA: A Vision-Language-Action Model for UAV Navigation via Minimalist End-to-End Control"
title_zh: AerialVLA：一种通过极简端到端控制实现无人机导航的视觉-语言-动作模型
authors: "Peng Xu, Zhengnan Deng, Jiayan Deng, Zonghua Gu, Shaohua Wan"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14363v1"
tags: ["query:rob-il"]
score: 8.0
evidence: 用于连续控制的端到端视觉-语言-动作框架
tldr: 本研究针对无人机视觉语言导航中存在的外部引导依赖和语义鸿沟问题，提出了AerialVLA框架。这是一种极简的端到端视觉-语言-动作模型，能将原始视觉观测和模糊指令直接映射为连续的物理控制信号。通过双视图感知策略、基于机载传感器的模糊定向提示以及集成了起降信号的统一控制空间，该模型实现了真正的自主导航。实验证明，AerialVLA在TravelUAV基准上表现优异，尤其在未知场景下的泛化能力显著提升。
selection_source: fresh_fetch
motivation: 现有无人机导航方法过度依赖密集外部引导和辅助检测器，限制了其在复杂动态环境中的真实自主性。
method: 提出一种端到端VLA框架，结合双视图感知、模糊定向提示和统一控制空间，实现从原始输入到3自由度运动及着陆信号的直接映射。
result: 在TravelUAV基准测试中取得SOTA性能，且在未知环境中的成功率达到领先基准模型的三倍。
conclusion: 极简且以自主性为中心的端到端范式比复杂的模块化系统具有更强的鲁棒性和泛化能力。
---

## 摘要
无人机（UAV）的视觉语言导航（VLN）要求在动态 3D 环境中进行复杂的视觉解释和连续控制。现有的分层方法依赖于密集的先验指导或辅助目标检测器，从而产生了语义鸿沟并限制了真正的自主性。我们提出了 AerialVLA，这是一个极简的端到端视觉-语言-动作框架，可将原始视觉观测和模糊语言指令直接映射为连续的物理控制信号。首先，我们引入了一种精简的双视图感知策略，在减少视觉冗余的同时保留了前向导航和精确落地的关键线索，这进一步促进了未来的仿真到现实迁移。为了恢复真正的自主性，我们部署了一种仅源自机载传感器的模糊方向提示机制，完全消除了对密集先验指导的依赖。最后，我们构建了一个统一的控制空间，将连续的 3 自由度（3-DoF）运动学指令与内在降落信号相结合，使智能体无需外部目标检测器即可实现精准降落。在 TravelUAV 基准测试上的大量实验表明，AerialVLA 在已知环境中达到了最先进的性能。此外，它在未知场景中表现出卓越的泛化能力，成功率接近领先基线模型的三倍，验证了以自主为中心的极简范式比复杂的模块化系统能捕获更稳健的视觉-运动表征。

## Abstract
Vision-Language Navigation (VLN) for Unmanned Aerial Vehicles (UAVs) demands complex visual interpretation and continuous control in dynamic 3D environments. Existing hierarchical approaches rely on dense oracle guidance or auxiliary object detectors, creating semantic gaps and limiting genuine autonomy. We propose AerialVLA, a minimalist end-to-end Vision-Language-Action framework mapping raw visual observations and fuzzy linguistic instructions directly to continuous physical control signals. First, we introduce a streamlined dual-view perception strategy that reduces visual redundancy while preserving essential cues for forward navigation and precise grounding, which additionally facilitates future simulation-to-reality transfer. To reclaim genuine autonomy, we deploy a fuzzy directional prompting mechanism derived solely from onboard sensors, completely eliminating the dependency on dense oracle guidance. Ultimately, we formulate a unified control space that integrates continuous 3-Degree-of-Freedom (3-DoF) kinematic commands with an intrinsic landing signal, freeing the agent from external object detectors for precision landing. Extensive experiments on the TravelUAV benchmark demonstrate that AerialVLA achieves state-of-the-art performance in seen environments. Furthermore, it exhibits superior generalization in unseen scenarios by achieving nearly three times the success rate of leading baselines, validating that a minimalist, autonomy-centric paradigm captures more robust visual-motor representations than complex modular systems.

---

## 论文详细总结（自动生成）

### AerialVLA：基于极简端到端控制的无人机视觉-语言-动作导航模型总结

#### 1. 核心问题与研究动机
无人机（UAV）的视觉语言导航（VLN）要求在动态3D环境中实现复杂的视觉理解与连续物理控制。现有的方法普遍存在**“双重拐杖”依赖**问题：
*   **依赖密集先验指导（Oracle Guidance）：** 现有基准通常在输入中注入精确的每一步指令（如“向右转”），使智能体沦为被动的指令跟随者，而非主动的路径规划者。
*   **依赖外部检测器：** 许多系统需要额外的目标检测模型（如Grounding DINO）来触发降落，导致感知与控制链路断裂，在开放世界中鲁棒性差。
**研究目标：** 提出一种极简的端到端视觉-语言-动作（VLA）框架，直接将原始视觉和模糊指令映射为物理控制信号，实现真正的自主导航。

#### 2. 方法论核心思想与技术细节
AerialVLA 基于 **OpenVLA-7B** 架构，结合了 Llama-2 语言模型和混合视觉编码器（SigLIP + DINOv2）。其核心技术包括：
*   **极简双视图感知（Minimalist Dual-View Perception）：** 放弃冗余的多摄像头阵列，仅将前视（避障/识别）和下视（对准/降落）图像垂直拼接（Vertical Mosaic），调整为 224x224 分辨率。这种设计减少了计算开销，并利用 ViT 的 Patch 结构避免了跨视图语义干扰。
*   **模糊定向提示（Fuzzy Directional Prompting）：** 仅利用机载传感器（IMU/GPS）获取目标的大致方位，并将其映射为模糊的语义词汇（如“在你右后方”），强迫模型进行主动视觉接地（Visual Grounding），而非机械跟随精确指令。
*   **数值动作标记化（Numerical Action Tokenization）：** 将连续的 3 自由度动作空间（前向位移 $\Delta x$、垂直位移 $\Delta z$、偏航角变化 $\Delta \psi$）离散化为 99 个分箱，并直接映射到 LLM 词表中的标准数字 Token。这利用了预训练模型对数值大小和顺序的理解，加速了收敛。
*   **内在降落策略（Intrinsic Landing）：** 在训练中将终端帧与 `LAND` 文本 Token 及零位移标签对齐，使模型能根据视觉收敛情况自主触发降落，无需外部检测器。
*   **几何一致性监督：** 通过侧向深度图过滤掉专家数据中因人类反应延迟导致的歧义样本，确保模型学习到正确的避障逻辑。

#### 3. 实验设计与对比
*   **数据集/基准：** 使用 **TravelUAV** 基准（UAV-Need-Help 任务），包含约 1.2 万条人类驾驶轨迹。
*   **测试场景：** 分为三个子集：**Seen**（已知环境）、**Unseen Object**（未知目标类别）、**Unseen Map**（全新地图）。
*   **对比方法：** 
    *   启发式基线（随机、固定动作）。
    *   传统 VLN 模型（CMA）。
    *   模块化/分层模型（TravelUAV-DA、NavFoM）。
    *   最新的时空编码模型（LongFly）。

#### 4. 资源与算力
*   **硬件：** 使用了 **4 张 NVIDIA RTX 4090 (24GB)** GPU。
*   **训练参数：** 采用 LoRA 微调（秩 $r=64$），可训练参数占比约 2.98%。
*   **时长：** 训练 5 个 Epoch，耗时约 **35 小时**。
*   **推理效率：** 总延迟为 0.38s（RTX 4090），优于 TravelUAV 的 0.63s，主要得益于去除了外部检测模块。

#### 5. 实验数量与充分性
*   **实验规模：** 在三个不同维度的测试集上进行了详尽的定量评估。
*   **消融实验：** 针对数据清洗（几何过滤）、感知视图数量（2视图 vs 5视图）、动作标记化方式（标准数字 vs 自定义 Token）进行了对比实验。
*   **充分性评价：** 实验设计覆盖了从已知到未知的泛化性测试，且通过消融实验验证了每个核心组件的有效性，对比实验均采用相同的数据分割，具有较高的客观性和公平性。

#### 6. 主要结论与发现
*   **性能领先：** 在已知环境中，AerialVLA 的成功率（SR）达到 47.96%，显著超过 LongFly（36.39%）。
*   **泛化能力极强：** 在**未知地图**测试中，AerialVLA 的成功率（37.58%）是领先基线模型（11.27%）的 **3 倍以上**。
*   **端到端优势：** 实验证明，相比于复杂的模块化系统，极简的端到端范式能捕获更稳健的视觉-运动表征，尤其是在处理环境变化时表现出更强的鲁棒性。

#### 7. 优点与亮点
*   **高度自主：** 摆脱了对密集 Oracle 指令和外部目标检测器的依赖，更符合真实无人机部署场景。
*   **感知高效：** 双视图拼接策略在保留关键导航信息的同时，极大地降低了计算复杂度。
*   **控制平滑：** 通过数值 Token 映射和速度-时间控制接口，实现了比位置控制更稳定的飞行轨迹。

#### 8. 不足与局限
*   **缺乏全局记忆：** 由于采用反应式控制（Reactive Control）而非显式的历史记忆机制，在高度重复的城市结构中可能会出现路径回溯困难。
*   **行为保守：** 在极端分布外（OOD）场景（如目标被茂密树冠严重遮挡）下，模型倾向于保守的悬停，而非执行多阶段的主动探索。
*   **仿真依赖：** 目前主要在 AirSim 仿真环境中验证，尚未在真实物理环境中进行大规模迁移测试。

（完）
