---
title: "MG-Grasp: Metric-Scale Geometric 6-DoF Grasping Framework with Sparse RGB Observations"
title_zh: MG-Grasp：基于稀疏 RGB 观测的度量尺度几何 6-DoF 抓取框架
authors: "Kangxu Wang, Siang Chen, Chenxing Jiang, Shaojie Shen, Yixiang Dai, Guijin Wang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16270v1"
tags: ["query:rob-il"]
score: 8.0
evidence: 基于RGB观测的六自由度机器人抓取与操纵
tldr: 针对传统RGB-D抓取依赖深度传感器及现有纯RGB方法几何表示不准确的问题，本文提出MG-Grasp框架。该框架利用双视图3D大模型，结合相机内外参，从稀疏RGB图像中重建出具有度量尺度且多视图一致的稠密点云，进而生成稳定的6-DoF抓取位姿。实验证明其在GraspNet-1Billion及真实场景中达到了纯RGB抓取领域的领先水平。
selection_source: fresh_fetch
motivation: 现有的纯RGB 6-DoF抓取方法由于几何表示不精确，难以实现物理上可靠的机器人操作。
method: 利用双视图3D基础模型结合相机参数，从稀疏RGB图像重建度量尺度的稠密点云并生成抓取位姿。
result: 在GraspNet-1Billion数据集和真实世界实验中，该方法在纯RGB 6-DoF抓取任务上取得了当前最先进的性能。
conclusion: MG-Grasp证明了通过高质量几何重建可以显著提升无深度传感器条件下的机器人抓取可靠性。
---

## 摘要
单视图 RGB-D 抓取检测仍然是 6-DoF 机器人抓取系统中的常用选择，这通常需要深度传感器。虽然最近对仅使用 RGB 的 6-DoF 抓取方法进行了研究，但它们不准确的几何表示并不直接适用于物理上可靠的机器人操作，从而阻碍了可靠抓取的生成。为了解决这些局限性，我们提出了 MG-Grasp，这是一种新型的无深度 6-DoF 抓取框架，可实现高质量的物体抓取。利用结合相机内参/外参的双视图 3D 基础模型，我们的方法从稀疏 RGB 图像中重建出度量尺度且多视图一致的稠密点云，并生成稳定的 6-DoF 抓取。在 GraspNet-1Billion 数据集和真实世界中的实验表明，MG-Grasp 在基于 RGB 的 6-DoF 抓取方法中达到了最先进的（SOTA）抓取性能。

## Abstract
Single-view RGB-D grasp detection remains a com- mon choice in 6-DoF robotic grasping systems, which typically requires a depth sensor. While RGB-only 6-DoF grasp methods has been studied recently, their inaccurate geometric repre- sentation is not directly suitable for physically reliable robotic manipulation, thereby hindering reliable grasp generation. To address these limitations, we propose MG-Grasp, a novel depth- free 6-DoF grasping framework that achieves high-quality object grasping. Leveraging two-view 3D foundation model with camera intrinsic/extrinsic, our method reconstructs metric- scale and multi-view consistent dense point clouds from sparse RGB images and generates stable 6-DoF grasp. Experiments on GraspNet-1Billion dataset and real world demonstrate that MG-Grasp achieves state-of-the-art (SOTA) grasp performance among RGB-based 6-DoF grasping methods.

---

## 论文详细总结（自动生成）

### MG-Grasp: 基于稀疏 RGB 观测的度量尺度几何 6-DoF 抓取框架论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：传统的 6-DoF 机器人抓取高度依赖 RGB-D 传感器（深度相机），但在实际应用中，深度传感器成本高、在强光或透明/反光物体上表现差。现有的纯 RGB 抓取方法（如基于 NeRF 或 3D 大模型的方法）往往存在几何表示不精确、缺乏绝对度量尺度（Metric-scale）以及多视图不一致的问题，导致生成的抓取位姿在物理上不可靠。
*   **研究动机**：利用先进的 3D 基础模型（如 MASt3R），结合已知的相机内外参，从极少数（稀疏）的 RGB 图像中重建出具有物理度量意义、且多视图一致的高质量点云，从而实现高精度的 6-DoF 抓取。

#### 2. 方法论
MG-Grasp 框架主要由两个核心模块组成：
*   **度量尺度深度图生成器 (Metric-Scale Depth Map Generator)**：
    *   **深度聚合与匹配**：利用 MASt3R 模型提取两两视图间的点图（Pointmaps）和特征匹配，通过置信度加权聚合得到初始深度图。
    *   **基于三角测量的尺度恢复**：这是本文的关键创新。由于 3D 大模型输出的深度通常是“比例缩放”的（Up-to-scale），作者利用已知的相机位姿，通过三角测量计算匹配点的真实物理深度，并以此为基准校准初始深度图，将其转换到真实的度量空间。
*   **多视图引导抓取生成 (Multi-View Guided Grasp Generation)**：
    *   **两阶段多视图优化**：为了解决视图间的对齐误差，设计了优化算法。第一阶段优化 3D 一致性（最小化匹配点在 3D 空间距离）；第二阶段优化 2D 重投影一致性（最小化重投影误差）。
    *   **抓取感知引导与解码**：利用 MobileSAM 进行背景剔除，并根据置信度过滤点云。最后将精炼后的点云输入局部抓取模型（LoG），通过 FPS 采样区域中心并解码出最终的 6-DoF 抓取位姿。

#### 3. 实验设计
*   **数据集与 Benchmark**：主要在 **GraspNet-1Billion** 大规模抓取数据集上进行评估，涵盖了 RealSense 和 Kinect 两种相机的测试集，并分为 Seen（已见）、Similar（相似）、Novel（新颖）三个难度等级。
*   **对比方法**：
    *   **RGB-D 基准**：GraspNet, TransGrasp, HGGD, FlexLoG（作为性能上限）。
    *   **纯 RGB 方法**：GraspNeRF, VG-Grasp。
    *   **几何后端对比**：对比了基于 VGGT、MASt3R-SLAM 和 MVSAnywhere 的不同几何重建方案。
*   **真实世界实验**：使用 UR5e 机械臂和 Robotiq 2F-85 夹爪，针对 25 种日常物体（包括透明物体）进行杂乱场景下的抓取测试。

#### 4. 资源与算力
*   **硬件配置**：实验在一台配备 **NVIDIA RTX 4090 GPU** 和 Intel Core i9-13900K CPU 的台式机上运行。
*   **运行效率**：系统实现了秒级端到端延迟。在 5 视图输入下，单次推理平均耗时 **2.86 秒**；在 4 视图下为 2.13 秒。其中几何处理（匹配与优化）占总耗时的 77.3%。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 GraspNet 的数千个场景上进行了详尽测试，并进行了 40 次真实机器人抓取尝试。
*   **消融实验**：针对“尺度恢复”和“多视图精炼”两个核心模块进行了消融对比，验证了各部分的必要性。
*   **视图数量分析**：研究了输入视图数量（2 到 9 个）对抓取性能的影响，发现 5-6 个视图是性能与效率的最佳平衡点。
*   **评价**：实验设计客观、公平，对比了多种 SOTA 方法，且涵盖了从仿真数据集到真实物理环境的迁移验证，实验充分性较高。

#### 6. 主要结论与发现
*   **性能领先**：MG-Grasp 在纯 RGB 6-DoF 抓取领域达到了 SOTA 水平，显著优于 GraspNeRF 和 VG-Grasp。
*   **缩小差距**：该方法极大地缩小了纯 RGB 方法与高性能 RGB-D 方法（如 FlexLoG）之间的性能差距。
*   **几何一致性的重要性**：实验证明，显式的多视图几何约束和度量尺度恢复对于提高抓取成功率至关重要。
*   **透明物体潜力**：在深度相机失效的透明物体抓取中，MG-Grasp 表现出较强的鲁棒性（成功率 55%）。

#### 7. 优点与亮点
*   **无需深度传感器**：仅靠稀疏 RGB 图像即可实现高质量抓取，降低了硬件成本和部署难度。
*   **度量尺度恢复**：提出了一种简单有效的三角测量校准方法，解决了 3D 大模型输出深度无法直接用于物理操作的痛点。
*   **端到端集成**：将复杂的几何重建、多视图优化与抓取检测整合进一个统一且高效的流水线中。

#### 8. 不足与局限
*   **实时性限制**：虽然达到了秒级延迟，但对于需要快速闭环反馈的动态抓取任务，2.86 秒的推理时间仍然偏慢。
*   **特殊物体挑战**：对于高反射（镜面）或近乎球形的物体，由于难以提取可靠的稠密对应关系，重建几何可能出现偏差，导致抓取失败。
*   **依赖相机参数**：该框架需要准确的相机内外参，在相机未经校准或位姿估计不准的情况下，性能会受影响。

（完）
