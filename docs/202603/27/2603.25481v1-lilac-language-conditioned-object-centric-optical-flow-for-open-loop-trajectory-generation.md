---
title: "LILAC: Language-Conditioned Object-Centric Optical Flow for Open-Loop Trajectory Generation"
title_zh: LILAC：用于开环轨迹生成的语言条件、以物体为中心的光流模型
authors: "Motonari Kambara, Koki Seno, Tomoya Kaichi, Yanan Wang, Komei Sugiura"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25481v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 用于机器人轨迹生成的基于流的视觉-语言-动作模型
tldr: LILAC是一个基于光流的视觉-语言-动作模型（VLA），旨在通过自然语言指令生成机器人操作轨迹。它利用语义对齐损失和提示条件跨模态适配器，从单张RGB图像和文本指令中生成以物体为中心的2D光流，并将其转换为6自由度机械臂动作。该方法减少了对特定机器人数据的依赖，在光流质量和实际操作成功率上均优于现有方法。
selection_source: fresh_fetch
motivation: 现有的机器人操作任务在从预操作图像和自然语言指令生成准确的物体轨迹时，面临指令与光流对齐困难的问题。
method: 提出了LILAC模型，通过语义对齐损失强化语言调节，并利用跨模态适配器融合视觉提示与图文特征，生成2D光流并转换为6自由度轨迹。
result: 实验表明，LILAC在多个基准测试中的光流生成质量以及在真实物理操作实验中的任务成功率均显著优于现有方法。
conclusion: LILAC证明了基于光流的开环轨迹生成在处理自由形式指令和跨模态对齐方面的有效性，为机器人操作提供了一种高效的VLA方案。
---

## 摘要
我们利用基于流的轨迹生成来解决语言条件的机器人操纵问题，该方法支持在人类和网络物体操纵视频上进行训练，且仅需极少量的具身特定数据。这一任务具有挑战性，因为从操纵前的图像和自然语言指令中生成物体轨迹需要适当的指令-流对齐。为了应对这一挑战，我们提出了基于流的语言指令引导开环动作生成器（LILAC）。这种基于流的视觉-语言-动作模型（VLA）从 RGB 图像

## Abstract
We address language-conditioned robotic manipulation using flow-based trajectory generation, which enables training on human and web videos of object manipulation and requires only minimal embodiment-specific data. This task is challenging, as object trajectory generation from pre-manipulation images and natural language instructions requires appropriate instruction-flow alignment. To tackle this challenge, we propose the flow-based Language Instruction-guided open-Loop ACtion generator (LILAC). This flow-based Vision-Language-Action model (VLA) generates object-centric 2D optical flow from an RGB image and a natural language instruction, and converts the flow into a 6-DoF manipulator trajectory. LILAC incorporates two key components: Semantic Alignment Loss, which strengthens language conditioning to generate instruction-aligned optical flow, and Prompt-Conditioned Cross-Modal Adapter, which aligns learned visual prompts with image and text features to provide rich cues for flow generation. Experimentally, our method outperformed existing approaches in generated flow quality across multiple benchmarks. Furthermore, in physical object manipulation experiments using free-form instructions, LILAC demonstrated a superior task success rate compared to existing methods. The project page is available at https://lilac-75srg.kinsta.page/.

---

## 论文详细总结（自动生成）

以下是对论文《LILAC: Language-Conditioned Object-Centric Optical Flow for Open-Loop Trajectory Generation》的结构化深入总结：

### 1. 核心问题与研究背景
*   **研究动机**：现有的视觉-语言-动作模型（VLA）通常需要海量的具身特定数据（机器人演示），且在处理未见过的指令或场景时泛化能力有限。
*   **核心问题**：如何利用互联网上丰富的人类操作视频（非具身数据）来训练机器人，并解决自然语言指令与底层像素级运动（光流）之间的对齐难题。
*   **整体含义**：论文提出了一种名为 **LILAC** 的框架，将操纵任务分解为“从指令生成以物体为中心的光流”和“将光流转换为机械臂轨迹”两个阶段，旨在实现高效、泛化性强的开环轨迹生成。

### 2. 方法论
LILAC 由两个核心模块组成：**光流生成模块**和**动作去标记化器（Action De-Tokenizer）**。
*   **提示条件跨模态适配器（Prompt-Conditioned Multi-Modal Adapter）**：
    *   利用多模态大模型（如 GPT-4o）根据图像和指令生成“视觉提示”（在图像上绘制代表运动趋势的箭头）和目标物体的边界框。
    *   使用 Transformer 编码器融合原始图像、文本指令和视觉提示，缩小高层语义与底层运动模式之间的差距。
*   **语义对齐解码器（Semantic Alignment Decoder）**：
    *   采用 Transformer 解码器自回归生成 2D 光流坐标。
    *   **关键技术：语义对齐损失（Semantic Alignment Loss, $L_{sem}$）**。该损失函数强制光流序列的最后一个 Token 与 CLIP 文本嵌入对齐，确保生成的运动轨迹严格遵循指令语义。
*   **动作去标记化器**：
    *   将生成的 2D 光流结合深度图和相机参数，通过刚体变换估计初步生成 6 自由度（6-DoF）轨迹。
    *   使用 Transformer 解码器对轨迹进行精细化处理，最终输出机械臂的执行动作。

### 3. 实验设计
*   **数据集与基准测试**：
    *   构建了 **Robot Flow Benchmark**：基于 Fractal 和 BridgeData V2 数据集，利用 CoTracker-3 提取物体中心光流作为真值。
    *   包含 Fractal 子集（约 2.6 万个片段）和 BridgeData V2 子集（约 1 万个片段）。
*   **对比方法**：
    *   光流生成对比：Im2Flow2Act、FLIP。
    *   物理实验对比：Im2Flow2Act、FLIP 以及最先进的 VLA 模型 $\pi_0$。
*   **物理实验场景**：使用丰田 HSR 机器人，执行移动放置、移动移动、移动倾倒、抽纸巾、开抽屉 5 类任务，共 100 次试验。

### 4. 资源与算力
*   **硬件**：论文明确提到使用 **NVIDIA H200 GPU** 进行训练。
*   **软件/模型**：视觉提示生成使用了 GPT-4o，光流标注使用了 Qwen-2.5-VL 和 CoTracker-3。
*   **时长**：文中未明确给出具体的训练总时长，但提到采用分类损失（Cross-Entropy）而非回归损失，加速了网络收敛。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **定量评估**：在两个大规模数据集子集上进行了详尽的指标测试（ADE, AUC, P@K）。
    *   **消融实验**：针对“语义对齐损失”和“视觉提示”分别做了剔除实验，验证了各组件的必要性。
    *   **物理实验**：100 组真实机器人测试，涵盖了未见过的物体和环境，具有较强的说服力。
    *   **错误分析**：随机抽取 100 个样本对视觉提示的失败案例进行了分类统计（如终点错误、方向反向等）。
*   **评价**：实验设计较为全面，对比了当前主流的流式模型和端到端 VLA 模型，客观展示了 LILAC 在精度和成功率上的优势。

### 6. 主要结论与发现
*   **性能领先**：LILAC 在光流生成质量上显著优于 Im2Flow2Act 和 FLIP，在 Fractal 数据集上将平均距离误差（ADE）降低了约 17.4 点。
*   **物理成功率高**：在真实机器人任务中，LILAC 的平均成功率比表现最好的基准方法高出 14%。
*   **推理速度快**：作为开环模型，LILAC 的推理速度约为 376.6ms/action，比闭环运行的 $\pi_0$ 快约 1.4 倍，且能有效避免闭环执行中的误差累积。
*   **视觉提示的重要性**：消融实验证明，视觉提示是提升性能最关键的因素。

### 7. 优点
*   **数据效率**：通过光流作为中间表示，能够利用非机器人视频（如人类操作视频）进行预训练，降低了对昂贵具身数据的依赖。
*   **语义对齐增强**：引入语义对齐损失，解决了模型容易过度依赖视觉信息而忽略语言指令的通病。
*   **开环鲁棒性**：相比于需要频繁重规划的闭环模型，LILAC 的一次性轨迹生成在处理长程任务时表现出更好的稳定性。

### 8. 不足与局限
*   **静态环境假设**：模型基于开环生成，假设环境在执行过程中不发生突变，无法应对动态障碍物或意外干扰。
*   **2D 流的局限性**：2D 光流难以表达复杂的 3D 空间运动（如绕过障碍物或工具旋转），对深度信息的依赖仍是瓶颈。
*   **提示词敏感性**：模型性能高度依赖于 MLLM 生成的视觉提示质量，若 GPT-4o 输出的箭头位置错误，会直接导致后续光流生成失败。
*   **复杂场景失效**：在物体密集或空间关系极其复杂的场景下，视觉提示的生成仍存在较高错误率。

（完）
