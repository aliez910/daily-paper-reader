---
title: "S-VAM: Shortcut Video-Action Model by Self-Distilling Geometric and Semantic Foresight"
title_zh: S-VAM：通过自蒸馏几何与语义预见实现的快捷视频-动作模型
authors: "Haodong Yan, Zhide Zhong, Jiaguan Zhu, Junjie He, Weilin Yuan, Wenxuan Song, Xin Gong, Yingjie Cai, Guanyi Zhao, Xu Yan, Bingbing Liu, Ying-Cong Chen, Haoang Li"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16195v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 用于复杂操纵任务的快捷视频-动作模型
tldr: 针对机器人学习中视频动作模型（VAM）在实时推理与高保真预测之间的权衡难题，本文提出S-VAM模型。该模型通过单次前向传播即可预见连贯的几何与语义表示，并以此作为动作预测的蓝图。核心创新在于引入自蒸馏策略，将多步去噪的生成先验压缩至单步推理，利用视觉基础模型提取的特征作为教师信号指导轻量级解耦器学习。实验证明，S-VAM在仿真和实操中均优于现有SOTA方法，实现了高效精准的复杂任务操控。
selection_source: fresh_fetch
motivation: 旨在解决现有视频动作模型在多步生成的低效性与单步提取的噪声干扰之间难以平衡的问题。
method: 采用自蒸馏策略将多步扩散模型的生成先验压缩为单步推理，并利用轻量级解耦器预测几何与语义表示。
result: 实验结果显示，S-VAM在仿真和真实世界任务中均超越了现有最先进方法，显著提升了操控效率与精度。
conclusion: S-VAM通过高效的单步前瞻表示简化了动作预测流程，为实时、高保真的机器人视觉学习提供了新方案。
---

## 摘要
视频-动作模型（VAMs）因其在复杂操作任务中强大的视觉预见能力，已成为机器人学习中一种极具前景的范式。然而，目前的 VAMs 通常依赖于缓慢的多步视频生成或带有噪声的单步特征提取，无法同时保证实时推理和高保真预见。为解决这一局限性，我们提出了 S-VAM，这是一种快捷视频-动作模型，通过单次前向传递即可预见连贯的几何与语义表示。作为稳定的蓝图，这些预见的表示显著简化了动作预测。为了实现这一高效的快捷方式，我们引入了一种新颖的自蒸馏策略，将多步去噪的结构化生成先验压缩到单步推理中。具体而言，从扩散模型自身多步生成的视频中提取的视觉基础模型（VFM）表示提供了教师目标。轻量级解耦器作为学生，学习将带有噪声的单步特征直接映射到这些目标。在仿真和真实世界中的广泛实验表明，我们的 S-VAM 优于现有最先进的方法，能够在复杂环境中实现高效且精确的操作。我们的项目页面位于 https://haodong-yan.github.io/S-VAM/。

## Abstract
Video action models (VAMs) have emerged as a promising paradigm for robot learning, owing to their powerful visual foresight for complex manipulation tasks. However, current VAMs, typically relying on either slow multi-step video generation or noisy one-step feature extraction, cannot simultaneously guarantee real-time inference and high-fidelity foresight. To address this limitation, we propose S-VAM, a shortcut video-action model that foresees coherent geometric and semantic representations via a single forward pass. Serving as a stable blueprint, these foreseen representations significantly simplify the action prediction. To enable this efficient shortcut, we introduce a novel self-distillation strategy that condenses structured generative priors of multi-step denoising into one-step inference. Specifically, vision foundation model (VFM) representations extracted from the diffusion model's own multi-step generated videos provide teacher targets. Lightweight decouplers, as students, learn to directly map noisy one-step features to these targets. Extensive experiments in simulation and the real world demonstrate that our S-VAM outperforms state-of-the-art methods, enabling efficient and precise manipulation in complex environments. Our project page is https://haodong-yan.github.io/S-VAM/

---

## 论文详细总结（自动生成）

以下是对论文《S-VAM: Shortcut Video-Action Model by Self-Distilling Geometric and Semantic Foresight》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视频-动作模型（VAMs）在机器人操控中面临**“推理延迟”与“预见保真度”**之间的权衡。
    *   **多步生成范式**：通过扩散模型生成未来视频来指导动作，虽保真度高但推理极慢，难以满足闭环控制的实时性要求。
    *   **单步特征范式**：直接提取扩散模型的单步去噪特征，虽速度快但特征高度纠缠且多噪，缺乏准确的几何和语义信息，导致动作预测不精准。
*   **研究背景**：VLA（视觉-语言-动作）模型通常缺乏时空预见能力。利用互联网规模视频预训练的视频扩散模型（VDM）可以提供物理动力学先验，但如何高效提取这些先验是关键挑战。

### 2. 方法论
S-VAM 提出了一种“快捷方式”，通过单次前向传播预见连贯的几何与语义表示。
*   **核心思想**：利用**自蒸馏（Self-Distillation）**策略，将扩散模型多步去噪产生的“高质量视觉先验”压缩到单步推理的“轻量级解耦器”中。
*   **关键技术细节**：
    1.  **单步特征提取**：从 Stable Video Diffusion (SVD) 的上采样层提取初始去噪特征。
    2.  **几何与语义解耦器**：设计两个轻量级的时空 Transformer 分支。
        *   **几何分支**：目标是预测 DPAv3（视觉几何基础模型）的表示，提供动态几何结构。
        *   **语义分支**：目标是预测 DINOv2 的表示，提供补丁级的语义区分度。
    3.  **自蒸馏机制**：在训练阶段，让扩散模型进行完整的多步去噪生成视频，从中提取 VFM 特征作为“教师信号”，指导解耦器将单步噪声特征映射到这些目标。这避免了使用真实标签（GT）可能导致的轨迹不匹配问题。
    4.  **Uni-Perceiver 与动作专家**：使用类 Q-Former 结构聚合解耦后的预见特征与原始特征，输入到基于 Diffusion Transformer (DiT) 的动作策略中预测执行动作。

### 3. 实验设计
*   **数据集与场景**：
    *   **CALVIN**：长程桌面操作基准，评估 ABC→D 的泛化能力。
    *   **MetaWorld**：包含 50 个不同难度的操控任务（Easy, Middle, Hard）。
    *   **真实世界**：使用 AgileX/Mobile ALOHA 双臂机器人，执行包括“倒水”、“移动透明杯子”、“双臂抬锅”等复杂任务。
*   **对比方法**：
    *   **直接动作学习**：OpenVLA, RT-1, Diffusion Policy, $\pi_0$, Spatial Forcing 等。
    *   **预测性方法**：VPP (直接竞争对手), SuSIE, HiF-VLA, Uni-VLA 等。

### 4. 资源与算力
论文详细列出了训练配置：
*   **算力设备**：主要使用 **NVIDIA H100 GPU**。
*   **训练阶段**：
    *   第一阶段（微调 SVD）：在 MetaWorld 上训练 100k 步，真实世界 40k 步（4 张 H100）。
    *   第二阶段（自蒸馏）：仅需 50k 步（单张 H100），效率极高。
    *   第三阶段（动作专家）：CALVIN 60k 步，其他 40k 步（4 张 H100）。
*   **推理设备**：在单张 **NVIDIA RTX 3090 (24GB)** 上即可运行。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 50 个 MetaWorld 任务和 CALVIN 的长程任务，实验组数充足。
*   **消融实验**：针对几何蒸馏、语义蒸馏、自蒸馏机制、Uni-Perceiver 模块以及不同 VFM 目标（如 CLIP, SigLIP, DINOv3, VGGT 等）进行了详尽的对比。
*   **客观性**：通过可视化“注意力中心轨迹”证明了 S-VAM 相比 VPP 能更准确地锁定任务目标，实验设计逻辑严密，对比公平（均采用单目 RGB 输入）。

### 6. 主要结论与发现
*   **性能领先**：S-VAM 在 CALVIN 上达到了 4.16 的平均完成长度，在 MetaWorld 硬任务上胜率显著超过 VPP（68.4% vs 52.6%）。
*   **效率平衡**：S-VAM 的推理延迟约为 307.6ms，仅比单步特征模型（VPP）增加 15.8% 的开销，但性能大幅提升，且远快于多步生成模型。
*   **自蒸馏的优越性**：发现使用模型自身生成的视频作为教师信号，比使用真实未来帧效果更好，因为前者保持了扩散轨迹的一致性。

### 7. 优点
*   **创新性强**：通过自蒸馏解决了 VAM 模型中“快而不准”或“准而太慢”的顽疾。
*   **鲁棒性高**：在处理透明物体（深度估计难点）时，通过几何与语义的协同预见展现了极强的稳定性。
*   **即插即用**：解耦器架构轻量，易于集成到现有的视频生成管线中。

### 8. 不足与局限
*   **硬件依赖**：虽然推理可在 3090 上运行，但 300ms+ 的延迟对于极高频（如 100Hz 以上）的精细操作仍有挑战。
*   **单目限制**：尽管引入了几何预见，但在极端遮挡或缺乏深度参考的场景下，单目视觉固有的局限性依然存在。
*   **数据依赖**：模型性能高度依赖于预训练视频扩散模型的质量，若基础模型在某些特定物理交互上存在偏差，S-VAM 也会受限。

（完）
