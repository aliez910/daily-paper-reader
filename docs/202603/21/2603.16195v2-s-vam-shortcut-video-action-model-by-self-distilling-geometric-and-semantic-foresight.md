---
title: "S-VAM: Shortcut Video-Action Model by Self-Distilling Geometric and Semantic Foresight"
title_zh: S-VAM：通过自蒸馏几何与语义预见实现的快捷视频-动作模型
authors: "Haodong Yan, Zhide Zhong, Jiaguan Zhu, Junjie He, Weilin Yuan, Wenxuan Song, Xin Gong, Yingjie Cai, Guanyi Zhao, Xu Yan, Bingbing Liu, Ying-Cong Chen, Haoang Li"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16195v2"
tags: ["query:rob-il"]
score: 10.0
evidence: 用于复杂操纵任务的快捷视频动作模型
tldr: 针对机器人学习中视频动作模型（VAM）在推理速度与预测质量之间的权衡问题，本文提出了S-VAM。该模型通过单次前向传播即可预测连贯的几何与语义表示，作为动作预测的蓝图。核心贡献在于引入了一种自蒸馏策略，将多步扩散生成过程中的结构化先验压缩至单步推理中，在保证高保真视觉预见的同时实现了实时推理，显著提升了复杂操作任务的效率与精度。
selection_source: fresh_fetch
motivation: 现有视频动作模型在多步生成的慢速推理与单步提取的噪声特征之间存在矛盾，难以同时满足实时性和高保真度。
method: 采用自蒸馏策略，利用轻量级解耦器学习将多步扩散生成的视觉基础模型表示压缩为单步推理的几何与语义特征。
result: 在仿真和真实世界实验中，S-VAM 的表现均优于现有最先进方法，实现了在复杂环境下的高效精确操控。
conclusion: S-VAM 通过单步视觉预见简化了动作预测流程，为构建兼顾速度与性能的机器人视觉动作模型提供了有效方案。
---

## 摘要
视频-动作模型（VAMs）因其在复杂操作任务中强大的视觉预见能力，已成为机器人学习中一种极具前景的范式。然而，目前的 VAMs 通常依赖于缓慢的多步视频生成或带有噪声的单步特征提取，无法同时保证实时推理和高保真预见。为解决这一局限性，我们提出了 S-VAM，这是一种快捷视频-动作模型，通过单次前向传递即可预见连贯的几何与语义表示。作为稳定的蓝图，这些预见的表示显著简化了动作预测。为了实现这一高效的快捷方式，我们引入了一种新颖的自蒸馏策略，将多步去噪的结构化生成先验压缩到单步推理中。具体而言，从扩散模型自身多步生成的视频中提取的视觉基础模型（VFM）表示提供了教师目标。轻量级解耦器作为学生，学习将带有噪声的单步特征直接映射到这些目标。在仿真和真实世界中的广泛实验表明，我们的 S-VAM 优于现有最先进的方法，能够在复杂环境中实现高效且精确的操作。我们的项目页面位于 https://haodong-yan.github.io/S-VAM/。

## Abstract
Video action models (VAMs) have emerged as a promising paradigm for robot learning, owing to their powerful visual foresight for complex manipulation tasks. However, current VAMs, typically relying on either slow multi-step video generation or noisy one-step feature extraction, cannot simultaneously guarantee real-time inference and high-fidelity foresight. To address this limitation, we propose S-VAM, a shortcut video-action model that foresees coherent geometric and semantic representations via a single forward pass. Serving as a stable blueprint, these foreseen representations significantly simplify the action prediction. To enable this efficient shortcut, we introduce a novel self-distillation strategy that condenses structured generative priors of multi-step denoising into one-step inference. Specifically, vision foundation model (VFM) representations extracted from the diffusion model's own multi-step generated videos provide teacher targets. Lightweight decouplers, as students, learn to directly map noisy one-step features to these targets. Extensive experiments in simulation and the real world demonstrate that our S-VAM outperforms state-of-the-art methods, enabling efficient and precise manipulation in complex environments. Our project page is https://haodong-yan.github.io/S-VAM/

---

## 论文详细总结（自动生成）

以下是对论文《S-VAM: Shortcut Video-Action Model by Self-Distilling Geometric and Semantic Foresight》的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视频-动作模型（VAM）在机器人操纵中面临**推理延迟与预见保真度之间的矛盾**。
    *   **多步生成范式**：通过扩散模型生成未来视频帧来指导动作，虽预见性强但推理极慢，无法满足实时闭环控制。
    *   **单步特征范式**：直接提取扩散模型的单步去噪特征，虽速度快但特征噪声大、时空纠缠严重（如夹爪位置漂移），缺乏精确的几何和语义引导。
*   **研究动机**：利用互联网规模视频数据预训练的扩散模型拥有强大的物理动力学先验，如何既保留这种先验，又能以单步推理的效率获得高质量的“视觉蓝图”？

### 2. 论文提出的方法论
S-VAM 的核心思想是建立一条“快捷路径（Shortcut）”，通过**自蒸馏（Self-Distillation）**将多步生成的结构化知识压缩到单步推理中。
*   **几何与语义解耦器（Decouplers）**：设计了两个轻量级的时空 Transformer 分支，分别负责从纠缠的单步扩散特征中提取“几何预见”（目标为 DPAv3 表示）和“语义预见”（目标为 DINOv2 表示）。
*   **自蒸馏策略**：
    *   **教师信号**：在训练阶段，让扩散模型进行完整的多步去噪生成视频，并从中提取 VFM（视觉基础模型）的特征作为真值。
    *   **学生网络**：解耦器作为学生，学习将单步去噪特征映射到上述教师信号上。这种方式避免了与真实未来帧之间的轨迹不匹配问题。
*   **Uni-Perceiver 与动作专家**：
    *   使用 **Uni-Perceiver**（类似 Q-Former 结构）将解耦后的几何/语义特征与原始扩散特征进行融合与压缩。
    *   最终由 **Diffusion Policy**（基于 DiT 架构）结合任务文本指令，根据压缩后的特征预测动作序列。

### 3. 实验设计
*   **实验场景**：
    *   **仿真环境**：CALVIN（考察长程任务泛化，ABC→D设置）和 MetaWorld（包含 50 个不同难度的操纵任务）。
    *   **真实世界**：使用 AgileX 移动 ALOHA 双臂机器人，执行包括“倒水”、“放置透明杯子”、“双臂抬锅”等 4 类复杂任务。
*   **Benchmark 与对比方法**：
    *   **直接动作学习类**：RT-1, Diffusion Policy, OpenVLA, π0, Spatial Forcing 等。
    *   **预测/预见类**：SuSIE, VPP（主要基准）, Uni-VLA, HiF-VLA 等。

### 4. 资源与算力
论文详细列出了训练所需的算力资源：
*   **训练阶段**：
    1.  **SVD 微调**：使用 4 张 NVIDIA H100 GPU，MetaWorld 训练 100k 步，真实世界 40k 步。
    2.  **解耦器蒸馏**：使用 1 张 NVIDIA H100 GPU，训练 50k 步。
    3.  **动作专家训练**：使用 4 张 NVIDIA H100 GPU，CALVIN 训练 60k 步，其余 40k 步。
*   **推理阶段**：使用单张 NVIDIA RTX 3090 (24GB) GPU。单次前向传播耗时约 307.6ms，支持 25Hz 的有效控制频率。

### 5. 实验数量与充分性
*   **实验规模**：在 MetaWorld 上测试了全部 50 个任务，在 CALVIN 上进行了连续长程任务评估，真实世界中每个任务进行了 25 次测试。
*   **消融实验**：针对几何蒸馏、语义蒸馏、自蒸馏机制、Uni-Perceiver 模块以及不同 VFM 目标（如 CLIP, SigLIP, DINOv3, VGGT 等）进行了详尽的对比实验。
*   **充分性评价**：实验设计覆盖了从仿真到现实、从简单到复杂的梯度，对比了当前最先进的 VLA 和 VAM 模型，数据量和对比维度较为充分，结果具有客观性。

### 6. 论文的主要结论与发现
*   **性能领先**：S-VAM 在 CALVIN 平均完成任务数（4.16）和 MetaWorld 平均成功率（72.8%）上均刷新了 SOTA 记录。
*   **解耦的价值**：显式提取几何和语义特征比直接使用纠缠的扩散特征更能有效指导动作，尤其在处理透明物体和精细装配任务时优势明显。
*   **自蒸馏的优越性**：相比于直接预测真实未来帧的特征，蒸馏扩散模型自身的生成结果能保持更好的轨迹一致性，降低学习难度。

### 7. 优点（亮点）
*   **效率与质量兼顾**：通过单步推理实现了接近多步生成的预见质量，解决了 VAM 落地实时控制的痛点。
*   **自蒸馏机制创新**：巧妙地利用扩散模型“自己教自己”，将生成先验转化为感知先验。
*   **多模态融合**：通过解耦几何（DPAv3）和语义（DINOv2），为机器人提供了更全面的物理世界理解。

### 8. 不足与局限
*   **模型体量**：虽然解耦器是轻量级的，但底层仍依赖 1.5B 参数的 SVD 骨干网络，对于算力极度受限的嵌入式设备仍有压力。
*   **单目局限性**：尽管引入了几何预见，但在极端遮挡或深度信息缺失严重的单目视角下，仍可能存在感知瓶颈。
*   **VFM 依赖**：模型的上限部分取决于所选 VFM（如 DPAv3）的表征能力，若 VFM 在某些特定工业场景下表现不佳，S-VAM 的性能也会受限。

（完）
