---
title: "3D-Mix for VLA: A Plug-and-Play Module for Integrating VGGT-based 3D Information into Vision-Language-Action Models"
title_zh: 3D-Mix for VLA：一种用于将基于 VGGT 的 3D 信息集成到视觉-语言-动作模型中的即插即用模块
authors: "Bin Yu, Shijie Lian, Xiaopeng Lin, Zhaolong Shen, Yuliang Wei, Haishan Liu, Changti Wu, Hang Yuan, Bailing Wang, Cong Huang, Kai Chen"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24393v1"
tags: ["query:rob-il"]
score: 10.0
evidence: 将3D信息集成到用于操纵的视觉-语言-动作模型中
tldr: 针对视觉-语言-动作（VLA）模型在3D空间感知能力的不足，本文提出了一种名为3D-Mix的即插即用模块。通过系统研究九种VGGT集成方案，研究发现语义调节门控融合效果最佳。3D-Mix能在不修改现有模型核心组件的情况下，有效整合3D几何特征，在多个基准测试中显著提升了机器人的操作性能和泛化能力。
selection_source: fresh_fetch
motivation: 现有的VLA模型主要基于2D数据训练，缺乏处理复杂机器人操作任务所需的3D空间智能。
method: 提出3D-Mix模块，采用语义调节门控融合机制，将VGGT提供的3D几何特征自适应地集成到多种VLA架构中。
result: "在SIMPLER和LIBERO基准测试中，3D-Mix在9个模型变体上实现了性能的持续提升，其中在域外测试中平均提升了7.0%。"
conclusion: 3D-Mix为增强VLA系统的空间智能提供了一种原则性的集成方法，证明了3D几何信息对提升机器人操作任务的重要性。
---

## 摘要
视觉-语言-动作（VLA）模型利用多模态大语言模型（MLLM）进行机器人控制，但近期研究表明，由于主要在 2D 数据上进行训练，MLLM 表现出有限的空间智能，导致其在操作任务中的 3D 感知能力不足。虽然最近的方法引入了如 VGGT 等专门的 3D 视觉模型来增强空间理解，但它们采用了多种集成机制且缺乏系统性研究，使得最优融合策略尚不明确。我们在标准化基准上进行了一项全面的初步研究，对比了九种 VGGT 集成方案，并发现语义条件门控融合（semantic-conditioned gated fusion）在所有评估的九种融合方案中表现最强，该方案能根据任务上下文自适应地平衡 2D 语义特征和 3D 几何特征。我们提出了 3D-Mix，这是一个即插即用模块，可以集成到多种 VLA 架构（如 GR00T 风格和 π 风格）中，而无需修改现有的 MLLM 或动作专家组件。在 SIMPLER 和 LIBERO 基准上针对六个 MLLM 系列（九个模型变体，参数量为 2B-8B）进行的实验表明，3D-Mix 带来了持续的性能提升，在所有九个 GR00T 风格变体中，其在分布外（OOD）SIMPLER 基准上的平均提升达 +7.0%，为增强 VLA 系统的空间智能建立了一种原则性方法。

## Abstract
Vision-Language-Action (VLA) models leverage Multimodal Large Language Models (MLLMs) for robotic control, but recent studies reveal that MLLMs exhibit limited spatial intelligence due to training predominantly on 2D data, resulting in inadequate 3D perception for manipulation tasks. While recent approaches incorporate specialized 3D vision models such as VGGT to enhance spatial understanding, they employ diverse integration mechanisms without systematic investigation, leaving the optimal fusion strategy unclear. We conduct a comprehensive pilot study comparing nine VGGT integration schemes on standardized benchmarks and find that semantic-conditioned gated fusion, which adaptively balances 2D semantic and 3D geometric features based on task context, achieved the strongest performance among all nine evaluated fusion schemes in our pilot study. We present 3D-Mix, a plug-and-play module that integrates into diverse VLA architectures (GR00T-style and $π$-style) without modifying existing MLLM or action expert components. Experiments across six MLLM series (nine model variants, 2B--8B parameters) on SIMPLER and LIBERO show that 3D-Mix delivers consistent performance gains, averaging +7.0% on the out-of-domain (OOD) SIMPLER benchmark across all nine GR00T-style variants, establishing a principled approach for enhancing spatial intelligence in VLA systems.

---

## 论文详细总结（自动生成）

以下是对论文《3D-Mix for VLA: A Plug-and-Play Module for Integrating VGGT-based 3D Information into Vision-Language-Action Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）模型主要依赖于在海量 2D 图像-文本数据上预训练的多模态大语言模型（MLLM）。由于缺乏显式的 3D 几何监督，这些模型在处理需要精确空间推理的机器人操纵任务（如深度感知、抓取姿态估计、空间关系理解）时表现欠佳。
*   **研究背景**：虽然已有研究尝试引入专门的 3D 视觉模型（如 VGGT）来增强空间智能，但集成方式多种多样且缺乏系统性对比，导致社区并不清楚哪种融合策略是最优的。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：通过一项包含 9 种融合方案的初步研究（Pilot Study），确定了**语义调节门控融合（Semantic-conditioned Gated Fusion）**为最优策略，并据此设计了名为 **3D-Mix** 的即插即用模块。
*   **关键技术细节**：
    *   **特征提取与投影**：使用冻结的 VGGT-1B 提取几何特征，并通过线性层投影到 MLLM 的隐藏维度。
    *   **语义调节门控机制**：
        1.  对 MLLM 的隐藏状态进行均值池化，提取全局语义上下文（Global Semantic Summary）。
        2.  将全局语义与局部几何特征拼接，通过一个可学习的门控网络（Sigmoid 激活）计算每个 token 位置的权重。
        3.  根据权重动态平衡 2D 语义和 3D 几何信息，生成融合特征。
    *   **多架构适配**：
        *   **GR00T 风格**：在 MLLM 最后一层输出后插入 3D-Mix，作为动作专家（DiT）的增强条件。
        *   **$\pi$ 风格**：采用层级融合，在 MLLM 的多个中间层独立应用 3D-Mix，实现深度的语义-动作对齐。
*   **即插即用特性**：无需修改 MLLM 或动作专家（Action Expert）的内部结构，仅作为中间桥接模块。

### 3. 实验设计
*   **数据集与场景**：
    *   **SIMPLER**：Real-to-Sim 的分布外（OOD）基准测试，评估模型在视觉变化和物体配置改变下的泛化能力。
    *   **LIBERO**：包含 Spatial、Object、Goal、Long-Horizon 四个子集，评估域内多任务学习能力。
*   **对比基准**：
    *   **基础模型**：涵盖 6 个 MLLM 系列（Qwen2.5-VL, Qwen3-VL, RoboBrain2/2.5, MimoEmbodied, RynnBrain），参数量从 2B 到 8B。
    *   **融合方案对比**：在初步研究中对比了 AE-Fusion、Early Fusion、Concat、CrossAttn、3D-Tokens、Middle Layer Injection、Spatial Forcing、Visual Fusion 等 9 种策略。

### 4. 资源与算力
*   **算力配置**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练细节**：采用 DeepSpeed ZeRO-2 优化器。训练步数为 60,000 步。
*   **学习率**：MLLM 主干为 $10^{-5}$，动作专家及融合模块为 $10^{-4}$。

### 5. 实验数量与充分性
*   **实验规模**：
    *   初步研究对比了 9 种不同的架构设计。
    *   主实验覆盖了 9 个 GR00T 风格变体和 5 个 $\pi$ 风格变体。
    *   进行了多项消融实验：VGGT 冻结 vs. 微调、3D 信息敏感性测试（替换为噪声或零向量）、$\pi$ 风格中的稀疏层融合深度。
*   **充分性评价**：实验设计非常充分且客观。通过在多个不同家族、不同规模的 MLLM 上验证，证明了 3D-Mix 的普适性；通过 OOD 测试（SIMPLER）验证了其真正的空间推理提升而非简单的过拟合。

### 6. 主要结论与发现
*   **最优策略**：语义调节的自适应门控优于固定比例融合或简单的特征拼接。
*   **性能提升**：3D-Mix 在所有测试的模型上均实现了性能增长。在 SIMPLER 任务中，GR00T 风格模型平均成功率提升了 **7.0%**，部分模型（如 RynnBrain-8B）提升高达 **12.51%**。
*   **3D 信息的真实性**：消融实验证明，性能提升确实源于 VGGT 提供的 3D 几何信息，而非仅仅因为增加了参数量或特征维度。
*   **冻结优势**：冻结预训练的 VGGT 效果往往优于微调，这降低了训练成本并保持了特征的鲁棒性。

### 7. 优点（亮点）
*   **系统性研究**：首次对 VLA 模型集成 3D 信息的方式进行了详尽的分类和对比实验，填补了行业空白。
*   **高兼容性**：模块化的设计使其能够轻松适配当前主流的两种 VLA 架构（模块化 vs. 层级耦合）。
*   **轻量化与高效**：作为即插即用模块，它对原有复杂系统的侵入性极小，且在 $\pi$ 风格中支持稀疏融合以节省显存。

### 8. 不足与局限
*   **推理开销**：虽然 3D-Mix 模块本身很轻量，但引入 VGGT 作为额外的特征提取器会增加推理时的计算延迟和显存占用。
*   **实验环境限制**：主要评估集中在仿真环境（SimplerEnv 和 LIBERO），虽然包含 Real-to-Sim 测试，但缺乏在真实物理机器人上的大规模部署数据。
*   **依赖预训练 3D 模型**：3D-Mix 的效果高度依赖于 VGGT 的预训练质量，如果 3D 编码器本身存在感知盲区，该模块的上限将受限。

（完）
