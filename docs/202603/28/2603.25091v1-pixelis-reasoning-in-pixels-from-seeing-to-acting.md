---
title: "Pixelis: Reasoning in Pixels, from Seeing to Acting"
title_zh: Pixelis：像素级推理，从观看到行动
authors: Yunpeng Zhou
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25091v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 通过模仿损失从动作后果中学习的像素空间智能体
tldr: 针对视觉语言系统仅作为静态观察者、缺乏行动能力且难以适应环境变化的问题，本文提出 Pixelis。这是一个在像素空间运行的智能体，通过缩放、分割、跟踪等可执行操作直接处理图像和视频。它采用三阶段训练：监督微调学习工具语法，奖励微调优化好奇心与连贯性，以及测试时强化学习实现无标签自适应。实验表明，Pixelis 在多个基准测试中显著提升了性能，实现了更具物理落地感的视觉推理。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型多为静态观察者，缺乏通过行动在物理世界中进行推理和自我优化的能力。
method: 提出一种三阶段训练框架，结合了像素级工具链的监督微调、基于好奇心与连贯性的奖励优化，以及测试时的无标签强化学习自适应。
result: "在六个图像和视频基准测试中，Pixelis 相比 8B 参数基线模型平均提升了 4.08%，并生成了更短且可审计的工具链。"
conclusion: 将推理过程置于像素级的可执行操作中，能有效增强多模态感知的物理落地性，并支持在无外部反馈下的具身自适应。
---

## 摘要
大多数视觉语言系统是静态观察者：它们描述像素，但不采取行动，且在分布偏移下无法安全地改进。这种被动性限制了可泛化的、具有物理基础的视觉智能。在精选数据之外，通过行动而非静态描述进行学习至关重要。我们提出了 Pixelis，这是一个像素空间智能体，它通过一组紧凑的可执行操作（缩放/裁剪、分割、跟踪、OCR、时间定位）直接作用于图像和视频，并从其结果中学习。Pixelis 的训练分为三个阶段：(1) 有监督微调，通过带有掩码模仿损失的思维-行动链（Chain-of-Thought-Action）轨迹学习像素工具语法，该损失增加了操作/参数标记的权重，并利用辅助头来稳定像素落地的参数；(2) 好奇心-连贯性奖励微调，优化了一个结合了预测误差好奇心、相邻步骤连贯性以及在 KL 锚点下的轻度效率先验的双驱动目标，从而产生简短、有效且结构化的工具链；(3) 像素测试时强化学习，通过检索邻居、对完整轨迹而非答案进行投票，并向简短、高保真的样本进行更新，同时利用 KL-to-EMA 安全控制来约束漂移，从而实现无标签自适应。在六个公开的图像和视频基准测试中，Pixelis 取得了持续的改进：与相同的 8B 基线相比，平均相对增益为 +4.08%（在 VSI-Bench 上达到峰值 +6.03%），同时生成了更短、可审计的工具链，并在测试时学习期间保持了合理的 KL 散度。在像素内而非抽象标记中采取行动，使多模态感知落地于物理世界，将视觉推理与可执行的结果联系起来，并实现了无需外部反馈的具身自适应。

## Abstract
Most vision-language systems are static observers: they describe pixels, do not act, and cannot safely improve under shift. This passivity limits generalizable, physically grounded visual intelligence. Learning through action, not static description, is essential beyond curated data. We present Pixelis, a pixel-space agent that operates directly on images and videos via a compact set of executable operations (zoom/crop, segment, track, OCR, temporal localization) and learns from its consequences. Pixelis trains in three phases: (1) Supervised Fine-Tuning learns a pixel-tool grammar from Chain-of-Thought-Action traces with a masked imitation loss that upweights operation/argument tokens and auxiliary heads to stabilize pixel-grounded arguments; (2) Curiosity-Coherence Reward Fine-Tuning optimizes a dual-drive objective marrying prediction-error curiosity with adjacent-step coherence and a mild efficiency prior under a KL anchor, yielding short, valid, structured toolchains; (3) Pixel Test-Time RL performs label-free adaptation by retrieving neighbors, voting over complete trajectories rather than answers, and updating toward short, high-fidelity exemplars while constraining drift with a KL-to-EMA safety control. Across six public image and video benchmarks, Pixelis yields consistent improvements: the average relative gain is +4.08% over the same 8B baseline (peaking at +6.03% on VSI-Bench), computed as (ours-baseline)/baseline, while producing shorter, auditable toolchains and maintaining in-corridor KL during test-time learning. Acting within pixels, rather than abstract tokens, grounds multimodal perception in the physical world, linking visual reasoning with actionable outcomes, and enables embodied adaptation without external feedback.

---

## 论文详细总结（自动生成）

以下是对论文《Pixelis: Reasoning in Pixels, from Seeing to Acting》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
现有的视觉语言模型（VLM）大多是“被动观察者”：它们仅能描述像素，却无法通过主动操作（如缩放、分割、跟踪）来验证推理过程。这种被动性导致模型在面对领域偏移（Domain Shift）时，容易出现定位、读取和时间定位错误，且无法在测试阶段自我改进。
**核心动机**：构建一个能在像素空间直接采取行动、通过可执行工具链进行推理，并能在测试阶段通过行动后果进行安全自适应的智能体（Pixelis），从而将多模态感知与物理世界的行动结果联系起来。

### 2. 方法论：核心思想与关键技术
Pixelis 采用三阶段训练框架，将推理过程转化为可审计、可执行的像素工具链：

*   **阶段 1：监督微调 (SFT)**
    *   **核心思想**：学习工具调用的语法。
    *   **技术细节**：使用“思维-行动链”（CoTA）轨迹进行训练。采用掩码模仿损失，提高操作和参数标记（Token）的权重。引入辅助头（Auxiliary Heads）预测边界框、掩码等，以稳定像素落地的参数。
*   **阶段 2：好奇心-连贯性奖励微调 (CC-RFT)**
    *   **核心思想**：优化探索效率与逻辑结构。
    *   **技术细节**：使用 GRPO 风格的策略梯度优化。
        *   **好奇心奖励**：基于预测误差，鼓励探索未知的视觉状态。
        *   **连贯性奖励**：计算相邻步骤嵌入向量的余弦相似度，惩罚无意义的工具切换。
        *   **效率先验**：惩罚过长的工具链，引导模型生成简短、有效的推理路径。
*   **阶段 3：像素测试时强化学习 (Pixel TTRL)**
    *   **核心思想**：在无标签情况下实现测试阶段的在线自适应。
    *   **技术细节**：
        *   **轨迹投票**：检索相似邻居，对完整的推理轨迹（而非仅最终答案）进行不确定性加权投票。
        *   **行为对齐**：选择高保真、短路径的样本作为更新目标。
        *   **安全控制**：引入 **KL-to-EMA** 锚点机制，通过 PID 控制器动态调整 KL 惩罚，防止模型在自适应过程中产生灾难性漂移。

### 3. 实验设计
*   **数据集/场景**：涵盖图像和视频的六大基准测试，包括 V* Bench（视觉搜索）、MMBench（综合能力）、MVBench（视频理解）、InfoVQA（文档问答）、Video-MMMU（专业视频知识）和 VSI Bench（空间推理）。
*   **对比方法**：
    *   **基线模型**：Qwen3-VL-8B-Instruct。
    *   **闭源模型**：GPT-5 (minimal)、Gemini-2.5-Pro。
    *   **开源模型**：Seed-1.5-VL、InternVL3.5。
    *   **变体与消融**：Pixel Reasoner、仅 SFT/RFT 的变体、以及多种测试时自适应方法（如 StatA、RA-TTA）。
*   **评估指标**：除了准确率，还引入了 **RaPR**（像素推理率）和 **RaCPR**（复合像素推理率）来衡量工具链的有效性与连贯性。

### 4. 资源与算力
*   **模型规模**：主要基于 8B 参数规模的 Qwen3-VL。
*   **训练时长**：总训练成本为 **805.2 GPU·h**。
*   **推理延迟**：中位延迟为 5.8 秒（包含模型前向传播和像素工具执行），其中像素工具（如分割和跟踪）占据了约 87% 的耗时。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个主流基准上进行了全面测试，并针对 1500 个样本进行了详细的错误分析和工具调用审计。
*   **消融实验**：非常充分。涵盖了每个训练阶段的贡献、不同奖励项（好奇心 vs 连贯性）的影响、以及测试时自适应的安全性分析。
*   **客观性**：通过严格的去重审计（SHA-256 和感知哈希）确保训练集与测试集无泄漏（重叠率为 0.00%）。使用了相同的 8B 骨干网络进行公平对比，并报告了 95% 的置信区间。

### 6. 主要结论与发现
*   **性能提升**：Pixelis 在 8B 规模下，相比基线模型平均取得了 **+4.08%** 的相对增益，在 VSI-Bench 上最高提升 **+6.03%**。
*   **推理质量**：生成的工具链更短（平均从 6 步缩短至 3.7 步），且更具可审计性。
*   **自适应能力**：Pixel TTRL 能在无标签偏移环境下稳定提升准确率，且 KL 散度保持在安全区间（<0.2），有效防止了模型崩溃。
*   **工具价值**：实验证明，像素级工具（而非纯文本启发式）是性能提升的核心驱动力，尤其在需要精确空间定位和时间跟踪的任务中。

### 7. 优点
*   **闭环推理**：将视觉感知、逻辑思考与像素级行动整合进统一的强化学习框架。
*   **可解释性**：通过可回放的工具执行轨迹，使模型的推理过程透明、可验证。
*   **安全性设计**：KL-to-EMA 机制为在线学习提供了坚实的护栏，解决了测试时强化学习容易漂移的顽疾。
*   **过程导向评估**：提出的 RaPR/RaCPR 指标为评估视觉智能体的推理质量提供了新维度。

### 8. 不足与局限
*   **工具依赖性**：模型性能受限于底层非微分工具（如 SAM2、OCR）的准确性，工具端的错误会传播至推理链。
*   **计算开销**：由于涉及多次工具调用和图像处理，推理延迟显著高于单次前向传播的模型。
*   **阶段性耦合**：三个训练阶段虽然互补，但并非完全端到端的联合优化，可能存在局部最优风险。
*   **复杂场景挑战**：在极细微结构、艺术化字体或剧烈运动模糊的场景下，工具链的鲁棒性仍有待提高。

（完）
