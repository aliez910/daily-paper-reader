---
title: Knowledge-Guided Manipulation Using Multi-Task Reinforcement Learning
title_zh: 基于多任务强化学习的知识引导操纵
authors: "Aditya Narendra, Mukhammadrizo Maribjonov, Dmitry Makarov, Dmitry Yudin, Aleksandr Panov"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24083v1"
tags: ["query:rob-il"]
score: 8.0
evidence: 通过场景图统一感知与策略的多任务机器人操纵
tldr: 本研究提出KG-M3PO框架，旨在解决部分可观测环境下的多任务机器人操作问题。该框架通过将自我中心视觉与在线3D场景图结合，构建包含空间、包含和功能关系的动态关系表示。通过端到端强化学习训练图神经网络编码器，使关系特征直接服务于控制决策。实验证明，该方法在处理遮挡、干扰和布局变化时，比基准模型具有更高的成功率、样本效率和泛化能力。
selection_source: fresh_fetch
motivation: 针对部分可观测和复杂环境下的多任务机器人操作，现有的纯视觉策略难以有效利用结构化知识和长程关系。
method: 提出KG-M3PO框架，利用动态3D场景图整合多模态信息，并通过端到端强化学习优化关系特征以指导控制策略。
result: 在包含遮挡和干扰的多种操作任务中，该方法在成功率、学习效率以及对新物体和场景的泛化性上均优于强基准模型。
conclusion: 结构化且持续维护的世界知识是实现可扩展、通用化机器人操作的强大归纳偏置，能显著提升复杂环境下的控制鲁棒性。
---

## 摘要
本文介绍了基于知识图谱的大规模多任务模型化策略优化（KG-M3PO），这是一个在部分可观测环境下统一了感知、知识与策略的多任务机器人操纵框架。该方法通过在线 3D 场景图增强了第一视角视觉，将开放词汇检测落地为度量化的关系表示。动态关系机制在每一步更新空间、包含和可操作性（affordance）边，并且图神经网络编码器通过强化学习（RL）目标进行端到端训练，使得关系特征直接由控制性能塑造。多种观测模态（视觉、本体感受、语言和基于图的）被编码到一个共享的潜空间中，RL 智能体在此基础上运行以驱动控制循环。策略以轻量级图查询以及视觉和本体感受输入为条件，为决策提供紧凑且具有语义信息的决策状态。在包含遮挡、干扰物和布局变化的一系列操纵任务上的实验表明，该方法相对于强基线模型具有持续的提升：知识条件化智能体实现了更高的成功率、更高的样本效率，以及对新物体和未见场景配置更强的泛化能力。这些结果支持了这样一个前提：结构化、持续维护的世界知识是实现可扩展、可泛化操纵的强大归纳偏置：当知识模块参与 RL 计算图时，关系表示与控制保持一致，从而在部分可观测性下实现稳健的长程行为。

## Abstract
This paper introduces Knowledge Graph based Massively Multi-task Model-based Policy Optimization (KG-M3PO), a framework for multi-task robotic manipulation in partially observable settings that unifies Perception, Knowledge, and Policy. The method augments egocentric vision with an online 3D scene graph that grounds open-vocabulary detections into a metric, relational representation. A dynamic-relation mechanism updates spatial, containment, and affordance edges at every step, and a graph neural encoder is trained end-to-end through the RL objective so that relational features are shaped directly by control performance. Multiple observation modalities (visual, proprioceptive, linguistic, and graph-based) are encoded into a shared latent space, upon which the RL agent operates to drive the control loop. The policy conditions on lightweight graph queries alongside visual and proprioceptive inputs, yielding a compact, semantically informed state for decision making.   Experiments on a suite of manipulation tasks with occlusions, distractors, and layout shifts demonstrate consistent gains over strong baselines: the knowledge-conditioned agent achieves higher success rates, improved sample efficiency, and stronger generalization to novel objects and unseen scene configurations. These results support the premise that structured, continuously maintained world knowledge is a powerful inductive bias for scalable, generalizable manipulation: when the knowledge module participates in the RL computation graph, relational representations align with control, enabling robust long-horizon behavior under partial observability.

---

## 论文详细总结（自动生成）

以下是对论文《Knowledge-Guided Manipulation Using Multi-Task Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：机器人在非结构化日常环境中进行操纵时，面临严重的**部分可观测性（Partial Observability）**问题（如物体被遮挡、置于容器内）。
*   **研究动机**：
    *   **反应式策略的局限**：纯视觉（像素到扭矩）的策略容易产生状态混淆，且在长程任务中难以进行信用分配。
    *   **传统方法的不足**：传统的基于模型的方法依赖于脆弱的手调几何流水线，难以扩展到多任务场景。
    *   **知识集成的必要性**：需要一种能够跨时间步持久化、支持关系推理并能与强化学习（RL）策略紧密耦合的结构化世界知识表示。

### 2. 方法论
论文提出了 **KG-M3PO** 框架，其核心思想是将“感知、知识、策略”统一在一个端到端的训练体系中。
*   **核心思想**：利用动态更新的 3D 场景图（3DSG）作为智能体的持久化信念状态，并通过 RL 目标直接优化图神经网络（GNN）编码器。
*   **关键技术细节**：
    *   **在线场景图构建**：基于 BBQ 框架，利用机器人传感数据生成包含物体 3D 边界框、CLIP 语义嵌入及空间关系（如“在……之上”、“在……之后”）的图。
    *   **动态关系机制**：每 10 个步骤刷新受影响的子图，实时更新空间、包含和功能（Affordance）边缘。
    *   **多模态融合**：将视觉嵌入（腕部摄像头）、知识嵌入（GNN 处理后的图特征）、语言目标嵌入和本体感受数据融合进一个共享的潜空间。
    *   **端到端训练**：GNN 编码器不是预训练或独立训练的，而是作为 RL 计算图的一部分，其参数通过策略梯度（如 M3PO 的损失函数）直接更新，使关系特征与控制性能对齐。

### 3. 实验设计
*   **实验场景**：基于 NVIDIA Isaac Lab 构建的物理仿真环境，使用 Franka Emika Panda 和 UR5 两种机器人臂。
*   **任务设置**：
    *   **完全可观测（FO）**：抓取方块、放置方块、开柜子。
    *   **部分可观测（PO）**：抓取被墙遮挡的物体、移除遮挡物（圆锥）后抓取并放置物体。
*   **基准模型（Benchmarks）**：对比了 PPO、SAC、DreamerV3、TD-MPC2、IMPALA 以及不带知识图谱的 M3PO（纯视觉）。
*   **评估指标**：采用归一化得分（0-1000 分，锚定随机策略和专家水平）及成功率。

### 4. 资源与算力
*   **硬件配置**：单台工作站，配备 **NVIDIA RTX 5070 Ti (16 GB VRAM)** 和 64 GB RAM。
*   **并行规模**：利用 Isaac Lab 的矢量化 API，在单个进程中并行运行 **1024 个环境**。
*   **训练时长**：
    *   纯视觉 M3PO 训练 4 亿步约需 0.610 GPU 天。
    *   KG-M3PO（带知识图谱）训练 4 亿步约需 **0.793 GPU 天**。
    *   虽然 KG 增加了每步计算开销，但在达到相同性能（如 600 分）时，KG-M3PO 所需的总步数和时间远少于基准模型。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 种不同难度的操纵任务，并在两种不同的机器人平台上进行了验证。
*   **消融实验**：进行了针对**场景图噪声**的鲁棒性消融实验，测试了边界框中心位移、尺寸缩放和旋转噪声对策略的影响。
*   **充分性评价**：实验设计较为充分，通过多任务与单任务、FO 与 PO 环境的对比，客观地证明了知识图谱的价值。使用 1024 个并行环境确保了统计结果的稳定性。

### 6. 主要结论与发现
*   **知识引导的优越性**：在完全可观测任务中，加入知识图谱显著提升了样本效率；在部分可观测任务中，KG-M3PO 能够解决纯视觉模型几乎无法完成的任务（成功率从接近 0% 提升至 60% 以上）。
*   **端到端的重要性**：将 GNN 纳入 RL 损失函数进行训练，使得关系表示能更好地服务于长程控制决策。
*   **多任务扩展性**：单一策略能够通过融合状态表示同时处理多种异构任务，且在不同机器人平台上具有通用性。

### 7. 优点
*   **架构统一**：成功将高层语义关系（场景图）与底层连续控制（RL）无缝结合。
*   **动态更新**：提出的在线关系更新机制解决了静态场景图容易过时的问题。
*   **鲁棒性强**：消融实验证明，即使在存在一定几何测量误差的情况下，策略性能仍能优雅降级，优于纯视觉方案。

### 8. 不足与局限
*   **缺乏实机验证**：所有实验均在仿真环境（Isaac Sim）中完成，尚未在真实物理机器人上进行验证，存在“仿真到现实（Sim-to-Real）”的迁移风险。
*   **计算开销**：虽然样本效率高，但每一步的检测、图更新和 GNN 推理增加了计算延迟，可能影响实时控制频率。
*   **环境动态性限制**：实验主要针对静态或准静态环境，对于物体快速运动或频繁接触导致的拓扑结构剧烈变化，场景图的维护可能面临挑战。

（完）
