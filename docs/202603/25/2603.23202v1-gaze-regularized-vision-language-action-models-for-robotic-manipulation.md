---
title: Gaze-Regularized Vision-Language-Action Models for Robotic Manipulation
title_zh: 机器人操作中基于注视正则化的视觉-语言-动作模型
authors: "Anupam Pani, Yanchao Yang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23202v1"
tags: ["query:rob-il"]
score: 10.0
evidence: 用于操纵的视线正则化视觉-语言-动作 (VLA) 模型
tldr: 针对视觉-语言-动作（VLA）模型在精细操作任务中缺乏主动视觉注意力的问题，本文提出一种注视正则化训练框架。该方法通过KL散度将人类注视热图转化为补丁级分布，引导模型内部注意力对齐人类视觉模式。在不改变架构和推理开销的前提下，该框架显著提升了模型性能、收敛速度及鲁棒性，并增强了决策的可解释性。
selection_source: fresh_fetch
motivation: 现有的VLA模型在处理精细操作时缺乏有效的视觉注意力分配机制，而人类注视数据蕴含了丰富的任务意图和规划信息。
method: 利用KL散度将时间聚合的人类注视热图转化为补丁级分布，以此正则化Transformer的注意力权重，使模型学习到人类的感知偏置。
result: "在多个操作基准测试中实现了4-12%的性能提升，且在光照变化和传感器噪声下表现出更强的鲁棒性。"
conclusion: 引入人类感知先验能有效加速机器人学习并提升任务表现，同时通过生成类人注意力模式增强了系统的可解释性。
---

## 摘要
尽管视觉-语言-动作（VLA）模型取得了进展，但由于当前模型缺乏主动视觉注意力分配机制，机器人操作在处理细粒度任务时仍面临挑战。人类注视自然地编码了意图、规划和执行模式，为引导机器人感知提供了强大的监督信号。我们提出了一种注视正则化训练框架，在不改变架构或增加推理开销的情况下，将 VLA 模型的内部注意力与人类视觉模式对齐。我们的方法将时间聚合的注视热图转换为补丁级（patch-level）分布，并通过 KL 散度对 Transformer 的注意力进行正则化，在保持部署效率的同时，建立了对任务相关特征的归纳偏置。当集成到现有的 VLA 架构中时，我们的方法在各项操作基准测试中实现了 4-12% 的提升。注视正则化模型能以更少的训练步数达到同等性能，并在光照变化和传感器噪声下保持鲁棒性。除了性能指标外，学习到的注意力模式还产生了反映人类策略的可解释可视化结果，从而增强了对机器人系统的信任。此外，我们的框架不需要眼动追踪设备，并可直接应用于现有数据集。这些结果表明，人类感知先验可以显著加速机器人学习，同时提高任务性能和系统可解释性。

## Abstract
Despite advances in Vision-Language-Action (VLA) models, robotic manipulation struggles with fine-grained tasks because current models lack mechanisms for active visual attention allocation. Human gaze naturally encodes intent, planning, and execution patterns -- offering a powerful supervisory signal for guiding robot perception. We introduce a gaze-regularized training framework that aligns VLA models' internal attention with human visual patterns without architectural modifications or inference-time overhead. Our method transforms temporally aggregated gaze heatmaps into patch-level distributions and regularizes the transformer's attention through KL divergence, creating an inductive bias toward task-relevant features while preserving deployment efficiency. When integrated into existing VLA architectures, our approach yields 4-12% improvements across manipulation benchmarks. The gaze-regularized models reach equivalent performance with fewer training steps and maintain robustness under lighting variations and sensor noise. Beyond performance metrics, the learned attention patterns produce interpretable visualizations that mirror human strategies, enhancing trust in robotic systems. Moreover, our framework requires no eye-tracking equipment and applies directly to existing datasets. These results demonstrate that human perceptual priors can significantly accelerate robot learning while improving both task performance and system interpretability.

---

## 论文详细总结（自动生成）

### 机器人操作中基于注视正则化的视觉-语言-动作模型 (VLA) 总结

#### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型在处理精细机器人操作时存在“被动感知”的局限性。模型通常需要同时学习“看哪里”和“如何做”，这导致训练效率低下、收敛缓慢，且在复杂环境下表现不佳。相比之下，人类在操作时会通过视觉注视（Gaze）主动分配注意力，注视模式中蕴含了任务意图、路径规划和执行的关键信息。
本文的研究动机是利用人类的视觉注意力先验，将 VLA 模型从被动观察者转变为主动感知者，通过引导模型内部注意力对齐人类注视模式，提升其在精细操作任务中的性能和鲁棒性。

#### 2. 方法论：核心思想与关键技术
论文提出了一种**注视正则化（Gaze-Regularized）训练框架**，其核心思想是在训练阶段引入人类注视分布作为辅助监督信号，而不改变模型架构，也不增加推理时的开销。

*   **注视先验生成**：由于现有机器人数据集缺乏眼动追踪数据，作者使用预训练的注视预测模型（如 GLC 网络）在视频演示上生成合成注视热图（Heatmaps）。
*   **时间聚合**：为了捕捉人类注视的动态性和预测性，对连续帧的注视热图进行加权时间平均，生成更稳定的注视分布。
*   **补丁级分布转换**：将连续的热图投影到与 Transformer 视觉 Token 相同的网格上，转化为离散的概率分布 $G_{i,t}$。
*   **注意力正则化**：提取 Transformer 最后一层中语言查询（Language Query）对视觉 Token 的交叉注意力分布 $S_{i,t}$。通过最小化 $G_{i,t}$ 与 $S_{i,t}$ 之间的 **KL 散度（Kullback-Leibler Divergence）**，将注视先验作为“软约束”引入训练目标函数。
*   **推理过程**：训练完成后，注视分支被移除，模型仅依靠学习到的内部参数进行推理，保持了实时性和部署的简便性。

#### 3. 实验设计
*   **数据集与基准测试**：
    *   **LIBERO Suite**：包括 LIBERO-Spatial（空间位置）、LIBERO-Object（细粒度物体识别）、LIBERO-Goal（顺序推理）和 LIBERO-10（多任务泛化）。
    *   **ALOHA-Sim**：用于验证跨域泛化能力（如立方体转移、挂钩插入）。
    *   **真实世界实验**：在 Aloha 机器人上进行了抓取放置、多杯子连续操作等任务。
*   **对比方法**：
    *   **Baseline**：标准的 VLA 模型（如 Pi-0, OpenVLA），不含注视正则化。
    *   **消融变体**：不同正则化强度（$\lambda$）、不同注视预测模型（DeepGaze vs. GLC）、均匀分布注意力等。

#### 4. 资源与算力
论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但文中提到了训练步数（如 10k, 20k, 30k steps），并指出注视正则化模型在 20k 步时即可达到基准模型 30k 步以上的性能，强调了其样本效率。

#### 5. 实验数量与充分性
实验设计非常充分且具有客观性：
*   **多维度验证**：涵盖了空间、物体、目标等多种任务类型。
*   **架构迁移性**：在 Pi-0 和 OpenVLA 两种不同的 VLA 架构上均验证了有效性。
*   **鲁棒性测试**：专门设计了光照变化、传感器噪声、相机视角缺失以及语言指令扰动等极端情况下的对比实验。
*   **消融实验**：系统地分析了正则化系数的影响，证明了“软引导”优于“硬约束”。
*   **真实感验证**：通过真实机器人部署和人类眼动仪数据的相关性分析（IoU 评估），验证了合成注视信号的可靠性。

#### 6. 主要结论与发现
*   **性能提升**：注视正则化使模型在 LIBERO 各项任务中实现了 4-12% 的成功率提升（例如在 LIBERO-Spatial 上从 85.9% 提升至 95.5%）。
*   **收敛加速**：模型能以更少的训练步数达到更高的性能，展现出极高的样本效率。
*   **鲁棒性增强**：在光照变化和传感器噪声下，正则化模型表现出更强的稳定性，因为它学会了关注语义上更稳定的特征（如物体边界）。
*   **可解释性**：可视化结果显示，正则化后的模型注意力更集中在任务相关区域（如目标物体及其周围环境），而非背景杂质，这增强了人类对系统的信任。

#### 7. 优点
*   **即插即用**：无需修改现有 VLA 架构，仅在训练阶段增加一个辅助 Loss，具有极强的通用性。
*   **推理零开销**：推理时不依赖眼动设备或注视预测模型，不影响实时控制。
*   **解决数据稀缺**：通过预训练模型生成合成注视数据，巧妙解决了机器人领域缺乏大规模眼动标注的问题。
*   **显著提升效率**：通过引入人类感知偏置，降低了模型从零探索视觉相关性的难度。

#### 8. 不足与局限
*   **合成数据依赖**：虽然验证了合成注视的有效性，但其本质上仍是人类注视的近似值，可能无法完全捕捉到专家操作中极细微的视觉策略。
*   **任务局限性**：目前的实验主要集中在桌面操作任务，对于需要全身协调或更广阔视野的移动操作任务，注视模式的建模可能更加复杂。
*   **潜在偏置风险**：如果注视预测模型本身存在偏置（例如在某些光照下失效），可能会误导 VLA 模型的注意力学习。

（完）
