---
title: "VTAM: Video-Tactile-Action Models for Complex Physical Interaction Beyond VLAs"
title_zh: VTAM：超越 VLA 的复杂物理交互视频-触觉-动作模型
authors: "Haoran Yuan, Weigang Yi, Zhenyu Zhang, Wendi Chen, Yuchen Mo, Jiashi Yin, Xinzhuo Li, Xiangyu Zeng, Chuan Wen, Cewu Lu, Katherine Driggs-Campbell, Ismini Lourentzou"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23481v1"
tags: ["query:rob-il"]
score: 10.0
evidence: 用于复杂物理交互的视频-触觉-动作模型
tldr: 针对视频动作模型（VAM）在处理复杂物理交互和接触密集型任务时视觉观测不足的问题，本文提出了视频-触觉-动作模型（VTAM）。该模型通过轻量化模态迁移微调，将触觉感知整合进预训练视频Transformer中，并引入触觉正则化损失以平衡跨模态注意力。实验表明，VTAM在需要精细力控的任务中表现优异，显著提升了具身智能在物理交互中的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的视频动作模型在接触密集型场景中因缺乏触觉反馈，难以实现精细的力度调节和接触状态感知。
method: 提出VTAM框架，通过轻量化微调将触觉流引入预训练视频模型，并利用触觉正则化损失防止视觉主导，实现高效的跨模态表征学习。
result: "VTAM在接触密集型操作中达到90%的平均成功率，在薯片抓取等高精度力控任务中比pi 0.5基准模型提升了80%。"
conclusion: 将触觉反馈整合进世界动作模型是纠正视觉估计误差、实现物理扎根的具身基础模型的关键。
---

## 摘要
视频-动作模型 (VAMs) 已成为具身智能领域一种极具前景的框架，它通过从原始视频流中学习隐式世界动力学，从而生成时间一致的动作预测。尽管此类模型通过视觉推理在长程任务中表现出强大性能，但在接触密集型场景中仍存在局限性，因为在这些场景中，关键的交互状态仅凭视觉只能部分观察到。特别是，细粒度的力调制和接触转换无法可靠地编码在视觉 token 中，导致行为不稳定或不精确。为了弥补这一差距，我们引入了视频-触觉动作模型 (VTAM)，这是一种将触觉感知作为补充对齐信号的多模态世界建模框架。VTAM 通过轻量级的模态迁移微调，为预训练的视频 Transformer 增加了触觉流，从而在无需触觉-语言配对数据或独立触觉预训练的情况下，实现了高效的跨模态表示学习。为了稳定多模态融合，我们引入了触觉正则化损失，以强制实现平衡的跨模态注意力，防止动作模型中视觉潜变量占据主导地位。VTAM 在接触密集型操作中表现出卓越的性能，平均保持了 90% 的稳健成功率。在诸如需要高保真力感知的薯片拾取与放置等挑战性场景中，VTAM 的表现优于 pi 0.5 基线模型 80%。我们的研究结果表明，集成触觉反馈对于纠正世界动作模型中的视觉估计误差至关重要，为物理对齐的具身基础模型提供了一种可扩展的方法。

## Abstract
Video-Action Models (VAMs) have emerged as a promising framework for embodied intelligence, learning implicit world dynamics from raw video streams to produce temporally consistent action predictions. Although such models demonstrate strong performance on long-horizon tasks through visual reasoning, they remain limited in contact-rich scenarios where critical interaction states are only partially observable from vision alone. In particular, fine-grained force modulation and contact transitions are not reliably encoded in visual tokens, leading to unstable or imprecise behaviors. To bridge this gap, we introduce the Video-Tactile Action Model (VTAM), a multimodal world modeling framework that incorporates tactile perception as a complementary grounding signal. VTAM augments a pretrained video transformer with tactile streams via a lightweight modality transfer finetuning, enabling efficient cross-modal representation learning without tactile-language paired data or independent tactile pretraining. To stabilize multimodal fusion, we introduce a tactile regularization loss that enforces balanced cross-modal attention, preventing visual latent dominance in the action model. VTAM demonstrates superior performance in contact-rich manipulation, maintaining a robust success rate of 90 percent on average. In challenging scenarios such as potato chip pick-and-place requiring high-fidelity force awareness, VTAM outperforms the pi 0.5 baseline by 80 percent. Our findings demonstrate that integrating tactile feedback is essential for correcting visual estimation errors in world action models, providing a scalable approach to physically grounded embodied foundation models.

---

## 论文详细总结（自动生成）

### 论文总结：VTAM (Video-Tactile-Action Models)

#### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作模型（VLA）在处理“接触密集型”（Contact-rich）任务（如抓取易碎品、擦拭、剥皮）时表现不佳。
*   **研究动机**：
    *   **视觉局限性**：视觉无法直接感知瞬时接触动力学，且在精细操作中常被机械臂遮挡。
    *   **触觉集成难点**：触觉信号频率高、空间特征细微，简单地将其作为语义 Token 注入预训练模型会导致“模态崩溃”（Modality Collapse），即模型过度依赖视觉而忽略触觉。
    *   **缺乏预测性**：现有方法多为反应式（Reactive），缺乏对物理交互过程的预测建模。

#### 2. 方法论
VTAM 的核心思想是将触觉感知整合进一个**预测性世界模型**框架中，通过联合预测未来视觉和触觉流来学习物理动力学。
*   **多模态潜空间建模**：
    *   使用预训练的视频 VAE 将多视角视频和高分辨率触觉图像（GelSight）投影到统一的连续潜空间。
    *   采用 Transformer 架构，交替进行“视点内自注意力”和“跨视点交叉注意力”，建模视觉与触觉的联合演化。
*   **变形感知虚拟力正则化**：
    *   为了防止视觉主导，提出从触觉图像的光流中提取“虚拟力”代理信号（切向剪切力和法向压缩力）。
    *   在动作头中引入虚拟力预测任务，作为辅助监督信号，确保模型对触觉变化保持敏感。
*   **两阶段训练流程**：
    *   **阶段 I（世界模型微调）**：仅训练模型预测未来的视觉和触觉潜变量（Flow Matching），建立物理直觉。
    *   **阶段 II（动作头训练）**：冻结或微调骨干网络，训练动作预测头，同时联合预测动作、机器人状态和虚拟力。

#### 3. 实验设计
*   **实验场景**：
    1.  **薯片拾取与放置**：极易碎，需精确控制抓取力并检测接触。
    2.  **黄瓜剥皮**：需维持稳定的剪切力和接触。
    3.  **白板擦拭**：需在不同倾斜角度下调节法向力。
*   **基准模型（Benchmarks）**：
    *   **Genie Envisioner**：纯视觉的视频动作模型。
    *   **$\pi_{0.5}$ (Vision-Only)**：最先进的开源 VLA 模型。
    *   **$\pi_{0.5}$ + Naive Tactile**：将触觉作为额外视觉输入简单拼接的 VLA。
*   **评估指标**：真实机器人实验的成功率（每项任务 20 次测试）。

#### 4. 资源与算力
*   **硬件**：使用了 **4 张 NVIDIA A100 (40GB)** GPU。
*   **软件框架**：DeepSpeed ZeRO Stage 2，混合精度（bf16）。
*   **训练时长**：
    *   阶段 I（世界模型）：50,000 步。
    *   阶段 II（动作头）：20,000 步。
    *   推理频率：1 Hz。

#### 5. 实验数量与充分性
*   **实验规模**：总计进行了约 80 次真实机器人测试（3 个主任务 + 消融实验）。
*   **消融实验**：针对薯片任务进行了消融，对比了“无触觉”、“后期融合触觉”和“无虚拟力正则化”的情况。
*   **充分性评价**：实验设计较为客观，涵盖了易碎、变形、变角度等多种物理挑战。对比基准涵盖了当前最强的 VLA 模型，证明了触觉在特定任务中的不可替代性。但受限于真机实验成本，样本量（20次/任务）虽符合机器人论文惯例，但统计显著性仍有提升空间。

#### 6. 主要结论与发现
*   **触觉是关键**：在薯片抓取任务中，VTAM 成功率达 90%，而纯视觉基准几乎全部失败（0-10%），证明触觉对纠正视觉误差至关重要。
*   **预测建模优于静态融合**：将触觉嵌入预测性世界模型（阶段 I）比简单的后期特征拼接效果显著更好。
*   **正则化必不可少**：引入虚拟力预测能有效缓解模态崩溃，使模型在视觉信息充足时依然关注触觉反馈。

#### 7. 优点
*   **物理扎根**：通过预测未来触觉状态，使模型具备了“预判”物理失败（如滑移、破碎）的能力。
*   **无需配对数据**：通过轻量化模态迁移，无需大规模“触觉-语言”标注数据即可利用预训练视频模型的知识。
*   **正则化创新**：利用光流导出的虚拟力解决了多模态训练中常见的梯度失衡问题。

#### 8. 不足与局限
*   **推理延迟**：推理频率仅为 1 Hz，对于需要极高频反馈（如 100Hz 以上）的瞬时防滑任务可能仍显不足。
*   **任务多样性**：虽然在 3 个任务上表现优异，但尚未在大规模通用操作数据集上验证其泛化能力。
*   **硬件依赖**：高度依赖 GelSight 这类高分辨率视觉触觉传感器，对于低分辨率或仅有力矩反馈的机器人适配性未知。
*   **环境假设**：实验环境相对受控，未探讨在极端光照或复杂背景下视觉噪声对触觉预测的影响。

（完）
