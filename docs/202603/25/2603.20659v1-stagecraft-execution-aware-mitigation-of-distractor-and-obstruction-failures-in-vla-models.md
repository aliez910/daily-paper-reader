---
title: "StageCraft: Execution Aware Mitigation of Distractor and Obstruction Failures in VLA Models"
title_zh: StageCraft：在 VLA 模型中缓解干扰物和障碍物导致执行失败的执行感知方法
authors: "Kartikay Milind Pangaonkar, Prabin Rath, Omkar Patil, Nakul Gopalan"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20659v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 缓解VLA模型在机器人泛化中的失效问题
tldr: "针对视觉语言动作模型（VLA）在面对干扰物和物理障碍时容易失败的问题，本文提出了 StageCraft。这是一种无需训练的即插即用方法，利用大语言模型（VLM）的上下文推理能力，通过分析策略执行视频和成功标签，识别并移除初始环境中的干扰因素。实验表明，该方法在多种真实任务中将 VLA 性能提升了 40%，且能根据策略强度自动调整干预程度。"
selection_source: fresh_fetch
motivation: 现有的 VLA 模型在存在干扰物或物理障碍的复杂环境中容易失效，且传统的微调方法在处理未见过的干扰场景时效果有限。
method: 提出一种名为 StageCraft 的免训练方法，利用 VLM 对策略执行视频进行推理，在任务开始前识别并清理可能导致失败的干扰物或障碍。
result: "在真实世界任务中实现了 40% 的绝对性能提升，并在仿真实验中证明了其干预程度能随策略强度自适应调整。"
conclusion: StageCraft 证明了利用互联网规模预训练的 VLM 进行环境预处理是提升机器人策略鲁棒性的一种高效且通用的途径。
---

## 摘要
在文本和图像数据上进行的大规模预训练，结合多样化的机器人演示，已帮助视觉-语言-动作模型 (VLA) 泛化到新的任务、物体和场景。然而，当机器人工作空间中存在执行时的障碍（如干扰物和物理障碍物）时，这些模型仍然容易发生失败。现有的策略改进方法通过微调基础 VLA 来提高泛化能力，但在未见过的干扰物设置中仍然表现不佳。为了解决这一问题，我们研究了是否可以利用互联网规模预训练的大型视觉语言模型 (VLM) 来对这些障碍进行推理并缓解策略失败。为此，我们提出了 StageCraft，这是一种无需训练的方法，通过使用基于 VLM 的上下文推理来操纵环境的初始状态，从而提高预训练 VLA 策略的性能。StageCraft 以策略展开视频和成功标签作为输入，并利用 VLM 的推理能力来推断初始状态中哪些物体需要被操纵，以避免预期的执行失败。StageCraft 是一个可扩展的即插即用模块，不会对底层策略引入额外的约束，且仅需少量的策略展开即可工作。我们评估了结合 StageCraft 的最先进 VLA 模型的性能，结果显示在涉及多种干扰物和障碍物的三个真实世界任务领域中，性能绝对提升了 40%。我们在 RLBench 中的仿真实验表明，StageCraft 会根据底层策略的强度调整其干预程度，并随着上下文样本的增加而提高性能。StageCraft 的效果视频可在 https://stagecraft-decorator.github.io/stagecraft/ 查看。

## Abstract
Large scale pre-training on text and image data along with diverse robot demonstrations has helped Vision Language Action models (VLAs) to generalize to novel tasks, objects and scenes. However, these models are still susceptible to failure in the presence of execution-time impediments such as distractors and physical obstructions in the robot's workspace. Existing policy improvement methods finetune base VLAs to improve generalization, yet they still struggle in unseen distractor settings. To address this problem, we investigate whether internet-scale pretraining of large vision-language models (VLMs) can be leveraged to reason about these impediments and mitigate policy failures. To this end, we propose StageCraft, a training-free approach to improve pretrained VLA policy performance by manipulating the environment's initial state using VLM-based in-context reasoning. StageCraft takes policy rollout videos and success labels as input and leverages VLM's reasoning ability to infer which objects in the initial state need to be manipulated to avoid anticipated execution failures. StageCraft is an extensible plug-and-play module that does not introduce additional constraints on the underlying policy, and only requires a few policy rollouts to work. We evaluate performance of state-of-the-art VLA models with StageCraft and show an absolute 40% performance improvement across three real world task domains involving diverse distractors and obstructions. Our simulation experiments in RLBench empirically show that StageCraft tailors its extent of intervention based on the strength of the underlying policy and improves its performance with more in-context samples. Videos of StageCraft in effect can be found at https://stagecraft-decorator.github.io/stagecraft/ .

---

## 论文详细总结（自动生成）

### StageCraft：在 VLA 模型中缓解干扰物和障碍物导致执行失败的执行感知方法

#### 1. 核心问题与研究动机
*   **核心问题**：尽管视觉-语言-动作模型（VLA）在多样化数据上进行了预训练，但在面对下游任务中未见过的**干扰物（Distractors）**和**物理障碍（Obstructions）**时，其鲁棒性依然较差。
*   **研究动机**：现有的策略改进方法（如强化学习微调或适配器学习）通常成本高昂，且容易导致“灾难性遗忘”。受人类在执行任务前会先整理环境（如清理桌面）的启发，本文旨在研究是否能利用大视觉语言模型（VLM）的推理能力，在策略执行前通过**修改环境初始状态**来提高成功率。

#### 2. 方法论
StageCraft 是一种**无需训练（Training-free）**、**即插即用**的模块，其核心流程如下：
*   **核心思想**：利用 VLM 的上下文学习（In-context Learning）能力，通过分析过去几次策略执行的成败经验，推断出当前场景中哪些物体是导致失败的潜在因素，并提前将其移除。
*   **关键技术细节**：
    1.  **物体集合创建（Object-Set Creation）**：收集少量策略在不同干扰物配置下的执行视频和成功标签。VLM 识别每组实验中的物体，并计算不同物体组合下的经验成功率。
    2.  **物体集合转换（Object-Set Transition）**：给定一个新场景，VLM 会对比当前物体与历史上高成功率的物体组合，确定一个**最小干预集**（即需要移除的最少物体数量），以确保环境状态回到策略能够处理的分布内。
    3.  **执行闭环**：VLM 输出需移除物体的描述 -> **SAM3 模型**进行目标检测 -> 机器人通过**运动原语（如抓取和放置）**将干扰物移至回收箱 -> 执行底层 VLA 策略。

#### 3. 实验设计
*   **实验场景**：
    *   **真实世界**：使用 Franka FR3 机器人，设置了三个任务：叠杯子（Stack Cups）、摆盘（Setup Plate）、碗中放块（Block in Bowl）。引入了 8 种在训练数据中从未见过的干扰物体。
    *   **仿真环境**：在 RLBench 中进行“抓取红杯子”任务，设置了不同数量（0-3个）干扰物的变体环境。
*   **Benchmark 与对比方法**：
    *   **基础策略**：Pi0.5 和 SmolVLA（当前最先进的开源 VLA）。
    *   **对比设置**：Base（无干扰）、Distractor（有干扰但无干预）、StageCraft（有干扰且应用本文方法）。
    *   **消融实验**：对比了不同 VLM 模型（如 Gemini 3.1 Pro, GPT 5.2 Pro 等）、不同上下文样本数量（1 vs 20）以及是否有“物体集合”推理策略的效果。

#### 4. 资源与算力
*   **训练算力**：由于 StageCraft 是**免训练**方法，文中未提及大规模 GPU 训练时长。
*   **推理资源**：使用了预训练的 VLA 模型（Pi0.5, SmolVLA）和商业 VLM API（Gemini, GPT 系列）。
*   **模型实现**：基于 LeRobot 开源库进行 VLA 的微调（用于准备基础策略），使用了 Intel D435 摄像头进行视觉输入。

#### 5. 实验数量与充分性
*   **实验规模**：真实世界中每个任务评估了 15 次迭代；仿真实验中进行了 100 次评估以获得统计显著性。
*   **充分性**：实验设计较为全面，涵盖了不同强度的底层策略（弱策略 vs 强策略）、多种 VLM 后端、以及针对推理策略的消融研究。
*   **客观性**：通过对比不同 VLM 的“指令遵循准确率”，客观展示了该方法对高级推理模型的依赖性。

#### 6. 主要结论与发现
*   **性能提升**：在真实世界任务中，StageCraft 将 VLA 的成功率平均**绝对提升了 40%**。
*   **自适应干预**：StageCraft 能根据底层策略的强弱自动调整干预程度。对于较弱的策略，它会移除更多物体；对于较强的策略，它表现得更保守，仅进行最小化修改。
*   **样本效率**：随着上下文样本（In-context samples）的增加，VLM 对策略鲁棒性的估计更准确，干预效果也随之提升。
*   **推理必要性**：简单的“开放式推理”容易误删任务相关物体，而本文提出的“物体集合转换策略”能显著提高干预的准确性和一致性。

#### 7. 优点
*   **通用性强**：作为“装饰器（Decorator）”模式，可应用于任何现有的 VLA 模型，无需修改其内部参数。
*   **部署成本低**：不需要为每个新环境重新收集大量数据或进行微调。
*   **拟人化逻辑**：模拟了人类“先清理工作区再干活”的行为逻辑，具有很强的直观解释性。

#### 8. 不足与局限
*   **VLM 依赖性**：性能高度依赖于 VLM 的推理能力和上下文窗口长度（视频 Token 较多，容易触及窗口上限）。
*   **干预手段单一**：目前仅限于“移除物体”这一种干预方式，无法处理需要增加物体或改变物体姿态的复杂前置条件。
*   **实时性挑战**：在执行任务前增加了一系列推理和清理动作，增加了完成任务的总时长。
*   **物体识别限制**：如果干扰物无法被离散化为明确的物体集合（如散装流体或粉末），该方法将失效。

（完）
