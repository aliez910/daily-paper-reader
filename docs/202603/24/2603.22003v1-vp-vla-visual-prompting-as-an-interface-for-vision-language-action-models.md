---
title: "VP-VLA: Visual Prompting as an Interface for Vision-Language-Action Models"
title_zh: VP-VLA：视觉提示作为视觉-语言-动作模型的接口
authors: "Zixuan Wang, Yuxin Chen, Yuqi Liu, Jinhui Ye, Pengguang Chen, Changsheng Lu, Shu Liu, Jiaya Jia"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22003v1"
tags: ["query:rob-il"]
score: 10.0
evidence: 视觉-语言-动作模型的视觉提示接口
tldr: 针对传统VLA模型在空间精度和鲁棒性上的不足，本文提出VP-VLA框架。该框架采用双系统架构，通过视觉提示接口解耦高层规划与底层执行。System 2规划器将指令分解并生成视觉锚点（如十字准星和边界框），System 1控制器则依据这些提示生成精确动作。实验证明，该方法在多个基准测试中显著提升了任务成功率，为提升机器人操作的精准度提供了新思路。
selection_source: fresh_fetch
motivation: 传统的端到端VLA模型因黑盒映射机制，在处理复杂指令时往往缺乏空间精度和对分布外场景的鲁棒性。
method: 提出一种双系统框架，利用视觉提示（如边界框和准星）作为接口，将高层任务规划与底层动作控制进行解耦。
result: "在Robocasa和SimplerEnv基准测试中，VP-VLA的成功率分别提升了5%和8.3%，表现优于QwenOFT和GR00T-N1.6等基准模型。"
conclusion: 通过引入结构化视觉提示接口，可以有效增强VLA模型的空间感知能力和复杂任务的执行可靠性。
---

## 摘要
视觉-语言-动作（VLA）模型通常将视觉观测和语言指令直接映射为机器人控制信号。这种“黑盒”映射迫使单次前向传播同时处理指令解释、空间定位和底层控制，往往导致空间精度较差，且在分布外（out-of-distribution）场景中的鲁棒性有限。为了解决这些局限性，我们提出了 VP-VLA，这是一个双系统框架，通过结构化视觉提示接口将高层推理与底层执行解耦。具体而言，“系统 2 规划器”将复杂指令分解为子任务，并识别相关的目标物体和目标位置。这些空间锚点随后作为结构化视觉提示（如十字准星和边界框）直接叠加在视觉观测上。在这些提示的引导下，并通过训练过程中引入的一种新型辅助视觉定位目标进行增强，“系统 1 控制器”能够可靠地生成精确的底层执行动作。在 Robocasa-GR1-Tabletop 基准测试和 SimplerEnv 仿真中的实验表明，VP-VLA 将成功率分别提高了 5% 和 8.3%，超越了包括 QwenOFT 和 GR00T-N1.6 在内的竞争基准模型。

## Abstract
Vision-Language-Action (VLA) models typically map visual observations and linguistic instructions directly to robotic control signals. This "black-box" mapping forces a single forward pass to simultaneously handle instruction interpretation, spatial grounding, and low-level control, often leading to poor spatial precision and limited robustness in out-of-distribution scenarios. To address these limitations, we propose VP-VLA, a dual-system framework that decouples high-level reasoning and low-level execution via a structured visual prompting interface. Specifically, a "System 2 Planner" decomposes complex instructions into sub-tasks and identifies relevant target objects and goal locations. These spatial anchors are then overlaid directly onto visual observations as structured visual prompts, such as crosshairs and bounding boxes. Guided by these prompts and enhanced by a novel auxiliary visual grounding objective during training, a "System 1 Controller" reliably generates precise low-level execution motions. Experiments on the Robocasa-GR1-Tabletop benchmark and SimplerEnv simulation demonstrate that VP-VLA improves success rates by 5% and 8.3%, surpassing competitive baselines including QwenOFT and GR00T-N1.6.

---

## 论文详细总结（自动生成）

以下是对论文《VP-VLA: Visual Prompting as an Interface for Vision-Language-Action Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
传统的视觉-语言-动作（VLA）模型通常采用端到端的“黑盒”映射，将视觉观测和文本指令直接转换为机器人控制信号。这种方式存在三大缺陷：
*   **空间精度差**：模型难以在单次前向传播中同时完美处理语义理解、空间定位和底层控制。
*   **鲁棒性不足**：在面对分布外（OOD）的物体或位置时，模型往往表现脆弱，容易产生幻觉或定位偏差。
*   **推理与执行耦合**：高层逻辑推理与底层电机控制混在一起，导致任务分解能力弱，且难以解释。
**研究动机**：通过引入“视觉提示（Visual Prompting）”作为中间接口，将高层推理与底层执行解耦，提升机器人在复杂、动态环境中的操作精度和泛化能力。

### 2. 方法论
VP-VLA 提出了一个**双系统架构**，模拟人类的“快慢思考”机制：
*   **System 2 规划器（慢思考/高层推理）**：
    *   利用预训练 VLM（如 Qwen3-VL）将复杂指令分解为子任务列表。
    *   **事件驱动机制**：仅在检测到特定事件（如夹爪状态改变）时触发规划器，识别当前目标物体和目标位置。
    *   **视觉提示生成**：调用分割模型（SAM3）在原始图像上叠加结构化视觉提示——**十字准星（Crosshair）**表示交互锚点（如抓取点），**边界框（Bounding Box）**表示空间约束（如放置区域）。
*   **System 1 控制器（快思考/底层执行）**：
    *   以原始图像、视觉提示图像和文本指令为输入，生成连续的动作序列（Action Chunking）。
    *   **辅助定位目标（Auxiliary Grounding Objective）**：在训练中引入辅助任务，要求模型预测提示框/准星的离散化坐标（1000个bin），强制模型内化空间感知，确保其真正“看懂”并利用视觉提示。

### 3. 实验设计
论文在仿真和现实世界中进行了广泛评估：
*   **仿真基准**：
    *   **Robocasa-GR1-Tabletop**：包含24个复杂的厨房操作任务，涉及多步交互。
    *   **SimplerEnv**：基于 BridgeDataV2 和 Fractal 数据集，评估在不同环境下的泛化性。
*   **对比方法**：包括 QwenOFT、GR00T-N1.5/N1.6、$\pi_0$、$\pi_{0.5}$、OpenVLA、Octo 等当前最强的 VLA 基准。
*   **现实世界场景**：
    *   **垃圾分类**：评估在杂乱环境下的物体识别与放置。
    *   **属性引用抓取**：根据颜色指令抓取特定物体（评估属性绑定能力）。
    *   **空间网格放置**：根据坐标指令（如“第2行第4列”）放置物体（评估精确空间推理）。

### 4. 资源与算力
*   **硬件**：所有模型训练均在 **8 张 GPU** 上完成（具体型号如 A100/H100 未在文中明确标注，但提到是 8-GPU 并行）。
*   **训练细节**：
    *   SimplerEnv 任务微调了 70k 步（Batch Size 256）。
    *   现实世界任务在 10k 步时进行评估。
    *   使用了 Qwen3-VL-4B-Instruct 作为基础模型。

### 5. 实验数量与充分性
*   **实验规模**：仿真实验中每个任务进行 50 次独立试验以确保统计显著性。
*   **消融实验**：针对定位损失函数、视觉提示设计（准星 vs 点）、提示叠加方式、任务分解必要性等进行了详细消融。
*   **充分性评价**：实验设计非常充分且客观。它不仅对比了平均成功率，还专门设置了 OOD（分布外）测试集（如新物体、新位置、新颜色组合），直接验证了该方法在解决 VLA 模型“脆弱性”方面的有效性。

### 6. 主要结论与发现
*   **性能提升**：在 Robocasa 基准上比基准模型提升了 **5%** 的成功率；在 SimplerEnv 上实现了 **8.3%** 的绝对提升，超越了 $\pi_{0.5}$ 等模型。
*   **泛化能力强**：在现实世界 OOD 实验中，VP-VLA 的性能下降远小于基准模型（例如在颜色引用任务中，基准模型性能从 58% 崩塌至 29%，而 VP-VLA 保持在 75% 左右）。
*   **解耦的价值**：视觉提示有效地将复杂的语义理解转化为简单的“视觉追踪”任务，降低了底层控制器的学习难度。

### 7. 优点（亮点）
*   **接口直观**：将抽象的语言指令转化为图像空间的几何锚点，符合机器人操作的直觉。
*   **双系统解耦**：通过事件驱动触发 System 2，平衡了高层推理的计算开销与底层执行的实时性。
*   **辅助任务设计**：引入 Grounding Loss 解决了模型可能忽略视觉提示的“捷径学习”问题。
*   **即插即用**：该框架逻辑可以适配不同的 VLM 底座和分割模型。

### 8. 不足与局限
*   **依赖前端模型精度**：如果 System 2 规划器或 SAM 分割模型出现错误（如识别错物体或分割偏移），错误会直接传播给控制器。
*   **推理延迟**：虽然是事件驱动，但在触发 System 2 的那一帧，调用大型 VLM 和分割模型会产生瞬时延迟，可能影响高速动态任务。
*   **视觉遮挡风险**：视觉提示是叠加在图像上的，在某些极端视角下，提示信息可能会遮挡关键的视觉特征（尽管消融实验显示分离输入可缓解此问题）。

（完）
