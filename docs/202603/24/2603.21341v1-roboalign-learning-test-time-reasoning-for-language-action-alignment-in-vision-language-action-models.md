---
title: "RoboAlign: Learning Test-Time Reasoning for Language-Action Alignment in Vision-Language-Action Models"
title_zh: RoboAlign：在视觉-语言-动作模型中学习用于语言-动作对齐的测试时推理
authors: "Dongyoung Kim, Sumin Park, Woomin Song, Seungku Kim, Taeyoung Kim, Huiwon Jang, Jinwoo Shin, Jaehyung Kim, Younggyo Seo"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21341v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 通过语言-动作对齐推理提升VLA性能
tldr: "本研究针对多模态大模型（MLLM）在具身推理中语言与底层动作对齐不稳定的问题，提出了RoboAlign框架。该框架通过零样本自然语言推理采样动作，并利用强化学习（RL）优化推理过程，从而显著提升动作准确性。实验表明，在仅使用不到1%数据进行RL对齐后，模型在LIBERO、CALVIN及真实环境中的表现较SFT基线分别提升了17.5%、18.9%和106.6%，有效增强了VLA模型的知识迁移能力。"
selection_source: fresh_fetch
motivation: 现有的视觉问答式监督在提升具身推理时表现不稳定，难以有效将多模态理解转化为精确的底层动作。
method: 提出RoboAlign框架，通过零样本自然语言推理生成动作，并结合强化学习对推理逻辑进行微调以对齐语言与动作空间。
result: "在LIBERO、CALVIN和真实场景中，RoboAlign相比SFT基线分别实现了17.5%、18.9%和106.6%的显著性能提升。"
conclusion: RoboAlign通过强化学习优化推理链条，成功弥合了语言与动作间的模态鸿沟，为构建高性能具身智能模型提供了有效路径。
---

## 摘要
提升多模态大语言模型（MLLMs）中的具身推理能力，对于在其基础上构建视觉-语言-动作模型（VLAs）以将多模态理解转化为底层动作至关重要。因此，最近的研究探索了通过视觉问答类型的监督来增强 MLLMs 的具身推理能力。然而，据报道这些方法会导致 VLA 性能不稳定，通常只能产生微小甚至负面的收益。在本文中，我们提出了一种更系统的 MLLM 训练框架 RoboAlign，能够可靠地提升 VLA 的性能。我们的核心思想是通过零样本自然语言推理采样动作标记（action tokens），并利用强化学习（RL）优化该推理过程，从而提高动作准确性。因此，RoboAlign 弥合了 MLLMs 中语言与底层动作之间的模态鸿沟，并促进了从 MLLM 到 VLA 的知识迁移。为了验证 RoboAlign 的有效性，我们在 MLLM 骨干网络上添加了一个基于扩散（diffusion-based）的动作头来训练 VLAs，并在主要的机器人基准测试中对其进行了评估。值得注意的是，通过在有监督微调（SFT）后使用不到 1% 的数据进行基于强化学习的对齐，RoboAlign 在 LIBERO、CALVIN 和真实世界环境中相比 SFT 基准分别实现了 17.5%、18.9% 和 106.6% 的性能提升。

## Abstract
Improving embodied reasoning in multimodal-large-language models (MLLMs) is essential for building vision-language-action models (VLAs) on top of them to readily translate multimodal understanding into low-level actions. Accordingly, recent work has explored enhancing embodied reasoning in MLLMs through supervision of vision-question-answering type. However, these approaches have been reported to result in unstable VLA performance, often yielding only marginal or even negative gains. In this paper, we propose a more systematic MLLM training framework RoboAlign that reliably improves VLA performance. Our key idea is to sample action tokens via zero-shot natural language reasoning and refines this reasoning using reinforcement learning (RL) to improve action accuracy. As a result, RoboAlign bridges the modality gap between language and low-level actions in MLLMs, and facilitate knowledge transfer from MLLM to VLA. To validate the effectiveness of RoboAlign, we train VLAs by adding a diffusion-based action head on top of an MLLM backbone and evaluate them on major robotics benchmarks. Remarkably, by performing RL-based alignment after SFT using less than 1\% of the data, RoboAlign achieves performance improvements of 17.5\%, 18.9\%, and 106.6\% over SFT baselines on LIBERO, CALVIN, and real-world environments, respectively.

---

## 论文详细总结（自动生成）

### 论文总结：RoboAlign —— 学习用于语言-动作对齐的测试时推理

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是：**多模态大语言模型（MLLM）的具身推理能力与其在机器人控制（VLA）中的实际表现之间存在脱节。**
虽然现有的研究通过视觉问答（VQA）等任务提升了 MLLM 的空间和逻辑推理能力，但这种基于纯语言的改进往往无法转化为精确的底层动作执行，甚至可能导致性能下降（即“模态鸿沟”）。论文旨在提出一种系统性的训练框架，使 MLLM 的推理过程能够直接服务于底层动作的生成，从而实现更可靠的知识迁移。

#### 2. 方法论：核心思想与关键技术
RoboAlign 提出了一个两阶段的训练流程，旨在将推理链条（Chain-of-Thought）与底层动作标记（Action Tokens）对齐：
*   **核心思想**：让模型在输出动作前先进行“思考”（`<think>` 标签），并利用强化学习（RL）根据最终动作的准确性来优化这段推理逻辑。
*   **关键技术细节**：
    *   **FAST 动作标记化**：将连续的机器人动作（位置、姿态、夹爪状态）通过离散余弦变换（DCT）和字节对编码（BPE）转化为离散的 Token，使其能像文本一样被 MLLM 处理。
    *   **第一阶段：机器人感知 SFT**：使用混合数据集（包括通用的 VQA、自定义的机器人 VQA 和带有推理过程的动作数据）进行有监督微调，使模型具备初步的零样本推理和动作生成能力。
    *   **第二阶段：基于 GRPO 的强化学习对齐**：使用组相对策略优化（GRPO）算法。模型针对同一指令生成多个包含推理和动作的响应，通过**格式奖励**（是否符合标签规范）和**准确性奖励**（生成的动作 Token 与真实值的匹配度）来优化模型。
    *   **VLA 适配**：在冻结的 MLLM 骨干网络上添加一个扩散式（Diffusion-based）动作头，利用 MLLM 提取的特征进行最终的控制。

#### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：包含空间、物体、目标和长程（Long-horizon）四个类别的模拟操纵任务。
    *   **CALVIN**：评估模型在连续指令链下的泛化能力（ABC 训练，D 环境测试）。
    *   **真实世界机器人**：使用 Franka Research 3 机械臂进行四种不同的拾取与放置任务。
*   **对比方法（Benchmark）**：
    *   基础模型：Qwen2.5-VL-7B-Instruct。
    *   基准线：仅语言 SFT、仅动作 SFT、不带 RL 的 RoboAlign。
    *   外部对比：RoboBrain 2.0、Cosmos-Reason1、OpenVLA、Octo 等。

#### 4. 资源与算力
论文明确列出了算力消耗：
*   **MLLM 训练**：使用 **8 张 NVIDIA H200 GPU**。SFT 阶段耗时约 30 小时，RL 阶段耗时约 1 小时。
*   **VLA 适配训练**：使用 **2 张 NVIDIA A100 GPU**。每 1 万步训练耗时约 1 小时（LIBERO 训练 6 万步，CALVIN 10 万步，真实机器人 3 万步）。

#### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的模拟实验（LIBERO 500 次试验/类别，CALVIN 1000 条指令链）和 96 次真实机器人试验。
*   **充分性与公平性**：
    *   **消融实验**：对比了不同的对齐策略（高层语言动作 vs 2D 轨迹预测 vs 底层动作）。
    *   **模型泛化性**：在 Qwen2.5-VL 和 Qwen3-VL 两种不同架构上验证了有效性。
    *   **表示分析**：通过 KNN 分类实验分析了模型内部表示的区分度。
    *   **客观性**：所有对比模型均在统一的 VLA 转换框架（Gr00t-N1.5）下测试，确保了骨干网络改进的纯粹性。

#### 6. 主要结论与发现
*   **性能显著提升**：RoboAlign 在 LIBERO 上提升 17.5%，CALVIN 上提升 18.9%，真实世界提升 106.6%。
*   **长程任务优势**：在复杂、长步骤的任务中，RoboAlign 的推理对齐表现出极强的稳定性，而传统 SFT 方法在长程任务中往往失效。
*   **数据效率极高**：仅需不到 1% 的额外数据进行 RL 对齐，即可获得巨大的性能增益。
*   **通用能力保持**：在提升机器人能力的同时，模型在通用视觉理解（MMStar）和空间推理（BLINK）等基准测试中依然保持甚至提升了性能。

#### 7. 优点
*   **弥合鸿沟**：通过将动作准确性作为 RL 奖励，直接解决了“推理好但控制差”的痛点。
*   **测试时推理（Test-Time Reasoning）**：引入了类似 DeepSeek-R1 的推理机制，使模型在执行动作前具备自我修正和逻辑规划的能力。
*   **高效迁移**：证明了从大规模预训练 MLLM 到特定机器人任务的高效知识迁移路径。

#### 8. 不足与局限
*   **动作空间限制**：依赖于特定的 FAST Token 编码方式，对于需要极高频率或极高精度的微操任务，Token 化的离散损失可能仍是瓶颈。
*   **安全风险**：论文提到，如果训练数据包含不安全轨迹，RL 可能会强化这些不安全行为，需在数据筛选上投入更多精力。
*   **推理开销**：在推理阶段生成 `<think>` 标签会增加计算延迟，对于实时性要求极高的机器人场景可能存在挑战。

（完）
