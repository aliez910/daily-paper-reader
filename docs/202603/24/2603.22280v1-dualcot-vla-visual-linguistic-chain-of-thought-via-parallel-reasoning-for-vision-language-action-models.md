---
title: "DualCoT-VLA: Visual-Linguistic Chain of Thought via Parallel Reasoning for Vision-Language-Action Models"
title_zh: DualCoT-VLA：基于并行推理的视觉-语言思维链视觉-语言-动作模型
authors: "Zhide Zhong, Junfeng Li, Junjie He, Haodong Yan, Xin Gong, Guanyi Zhao, Yingjie Cai, Jiantao Gao, Xu Yan, Bingbing Liu, Yingcong Chen, Liuqing Yang, Haoang Li"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22280v1"
tags: ["query:rob-il"]
score: 10.0
evidence: 用于复杂多步任务和细粒度空间感知的VLA模型
tldr: 针对视觉-语言-动作（VLA）模型在复杂任务中逻辑规划不足及推理延迟高的问题，本文提出DualCoT-VLA。该方法通过整合视觉思维链（底层空间理解）与语言思维链（高层任务规划）实现多模态推理，并引入并行推理机制，利用可学习查询标记替代传统的自回归解码，显著降低了推理延迟并减少了误差累积。实验证明该模型在多个基准测试和真实场景中均取得领先性能。
selection_source: fresh_fetch
motivation: 现有VLA模型难以同时兼顾高层逻辑规划与底层空间感知，且自回归推理机制导致了严重的推理延迟和误差累积。
method: 提出一种结合视觉与语言双思维链的并行推理框架，利用可学习查询标记将自回归推理转变为单步前向推理。
result: 在LIBERO、RoboCasa GR1基准测试以及真实世界机器人平台上，DualCoT-VLA均实现了当前最先进的性能表现。
conclusion: 该研究通过双模态并行思维链机制，有效解决了VLA模型在复杂多步任务中的感知与效率瓶颈。
---

## 摘要
视觉-语言-动作（VLA）模型将视觉观测和语言指令直接映射为机器人动作。虽然标准 VLA 模型在简单任务中表现出色，但在需要逻辑规划的复杂多步任务以及需要细粒度空间感知的精确操作中往往表现不佳。最近的研究引入了思维链（CoT）推理，赋予 VLA 模型“先思后行”的能力。然而，目前的基于 CoT 的 VLA 模型面临两个关键局限：1) 由于依赖孤立的单模态 CoT，无法同时捕捉底层视觉细节和高层逻辑规划；2) 逐步自回归解码导致的高推理延迟和复合误差。为了解决这些局限，我们提出了 DualCoT-VLA，这是一种具有并行推理机制的 VLA 模型视觉-语言 CoT 方法。为了实现全面的多模态推理，我们的方法集成了用于底层空间理解的视觉 CoT 和用于高层任务规划的语言 CoT。此外，为了克服延迟瓶颈，我们引入了一种并行 CoT 机制，该机制结合了两组可学习的查询标记（query tokens），将自回归推理转变为单步前向推理。广泛的实验表明，我们的 DualCoT-VLA 在 LIBERO 和 RoboCasa GR1 基准测试以及真实世界平台中均达到了最先进的性能。

## Abstract
Vision-Language-Action (VLA) models map visual observations and language instructions directly to robotic actions. While effective for simple tasks, standard VLA models often struggle with complex, multi-step tasks requiring logical planning, as well as precise manipulations demanding fine-grained spatial perception. Recent efforts have incorporated Chain-of-Thought (CoT) reasoning to endow VLA models with a ``thinking before acting'' capability. However, current CoT-based VLA models face two critical limitations: 1) an inability to simultaneously capture low-level visual details and high-level logical planning due to their reliance on isolated, single-modal CoT; 2) high inference latency with compounding errors caused by step-by-step autoregressive decoding. To address these limitations, we propose DualCoT-VLA, a visual-linguistic CoT method for VLA models with a parallel reasoning mechanism. To achieve comprehensive multi-modal reasoning, our method integrates a visual CoT for low-level spatial understanding and a linguistic CoT for high-level task planning. Furthermore, to overcome the latency bottleneck, we introduce a parallel CoT mechanism that incorporates two sets of learnable query tokens, shifting autoregressive reasoning to single-step forward reasoning. Extensive experiments demonstrate that our DualCoT-VLA achieves state-of-the-art performance on the LIBERO and RoboCasa GR1 benchmarks, as well as in real-world platforms.

---

## 论文详细总结（自动生成）

### DualCoT-VLA 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
传统的视觉-语言-动作（VLA）模型通常采用直接映射的方式，将视觉观测和语言指令转换为机器人动作。这种方式在处理复杂、多步任务时存在两个主要缺陷：
*   **感知与规划的失衡**：现有的思维链（CoT）方法多为单模态。纯语言 CoT 擅长高层逻辑规划但缺乏精确的空间感知；纯视觉 CoT 虽能捕捉空间细节，却难以进行长程逻辑规划。
*   **推理效率低下**：目前的 CoT-VLA 模型大多依赖自回归（Autoregressive）解码，即逐个 token 生成推理过程。这不仅导致了严重的推理延迟，还容易产生误差累积（一步错步步错），难以满足机器人高频实时控制的需求。

#### 2. 论文提出的方法论
DualCoT-VLA 提出了一种**并行隐式双模态思维链**框架，核心思想是将推理过程从显式的自回归解码转变为连续潜空间中的单步前向推理。
*   **并行推理机制**：引入两组可学习的查询标记（Query Tokens）——视觉 CoT 查询（$Q_{vis}$）和语言 CoT 查询（$Q_{lin}$）。这些标记与视觉/语言输入一同输入 VLM 主干网络，通过一次前向传播同时提取空间和逻辑特征。
*   **视觉 CoT（几何蒸馏）**：利用冻结的视觉基础模型（Depth Anything 3）作为教师，通过交叉注意力机制将 $Q_{vis}$ 的输出状态与教师模型的稠密空间特征对齐，使模型获得底层 3D 空间感知能力。
*   **语言 CoT（步骤级监督）**：利用冻结的轻量级 LLM（如 Qwen3-0.6B）作为解码器，监督 $Q_{lin}$ 的输出状态生成人类可读的任务规划文本。这确保了模型在潜空间中内化了高层逻辑规划，同时避免了推理时的显式解码开销。
*   **动作生成**：将富含推理信息的隐藏状态输入到基于流匹配（Flow-Matching）的扩散 Transformer（DiT）动作头中，预测连续的动作序列。

#### 3. 实验设计
论文在模拟环境和真实世界中进行了广泛评估：
*   **数据集/场景**：
    *   **LIBERO**：包含 Spatial、Object、Goal 和 Long 四个任务套件，重点测试空间关系和长程规划。
    *   **RoboCasa GR1**：涉及 29 自由度灵巧手的复杂桌面任务，测试细粒度操作。
    *   **真实世界**：使用 AgileX Cobot 双臂机器人（基于 Mobile ALOHA 设计），执行抓取面包、移动方块、收集水果等任务。
*   **对比方法**：
    *   **非 CoT 模型**：OpenVLA, $\pi_0$, PD-VLA, GR00T 系列等。
    *   **自回归 CoT 模型**：CoT-VLA, ThinkAct, DeepThinkVLA, Fast-ThinkAct, LaRA-VLA 等。

#### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA H100 GPU** 进行训练。
*   **模型配置**：VLM 主干采用 Qwen3-VL-4B，辅助语言解码器为 Qwen3-0.6B。
*   **训练细节**：LIBERO 任务的学习率为 2.5e-5，Batch Size 为 48；RoboCasa 任务的学习率为 3e-5，Batch Size 为 256。
*   **未明确说明的信息**：文中未明确给出具体的 GPU 数量以及总训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：在 LIBERO 上进行了 500 个回合的评估，在 RoboCasa 上进行了 50 次 rollouts，真实世界中每个任务进行 25 次试验。
*   **消融实验**：专门设计了消融实验来验证视觉 CoT 和语言 CoT 的独立贡献，证明了双模态结合的必要性。
*   **推理延迟分析**：对比了非 CoT、自回归 CoT 与并行 CoT 的推理时间，数据详实。
*   **充分性评价**：实验设计覆盖了从简单到复杂、从模拟到现实的多种维度，对比了当前最先进的多个基准模型，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能领先**：DualCoT-VLA 在 LIBERO（平均成功率 98.8%）和 RoboCasa GR1 上均刷新了 SOTA 记录。
*   **效率提升**：相比自回归 CoT 模型（延迟约 3178ms），DualCoT-VLA 的总推理延迟仅为 **83.2ms**，与非 CoT 模型（76.2ms）接近，完全满足实时控制需求。
*   **模态互补**：视觉 CoT 显著提升了空间感知类任务的成功率，而语言 CoT 则在长程规划任务中起到了关键作用。

#### 7. 优点
*   **架构创新**：通过并行 Query Tokens 解决了 CoT 推理速度慢的顽疾，实现了“边想边做”的高效并行。
*   **隐式推理**：利用潜空间对齐而非显式文本生成，既保留了推理能力又避免了信息冗余和解码错误。
*   **多模态融合**：成功将底层几何特征与高层语义逻辑统一在一个框架内。

#### 8. 不足与局限
*   **数据依赖**：训练过程需要高质量的显式 CoT 标注数据（虽然推理时不需要），这在扩展到更大规模数据集时可能面临标注成本问题。
*   **教师模型依赖**：模型的上限在一定程度上受限于辅助教师模型（DA3 和 Qwen3）的性能。
*   **动态环境适应性**：虽然在真实世界做了测试，但对于极度动态或未见过的开放场景，其隐式推理的鲁棒性仍有待进一步验证。

（完）
