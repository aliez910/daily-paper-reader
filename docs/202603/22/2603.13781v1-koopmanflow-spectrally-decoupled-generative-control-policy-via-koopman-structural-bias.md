---
title: "KoopmanFlow: Spectrally Decoupled Generative Control Policy via Koopman Structural Bias"
title_zh: KoopmanFlow：通过 Koopman 结构偏置实现的频谱解耦生成式控制策略
authors: "Chengsi Yao, Ge Wang, Kai Kang, Shenhao Yan, Jiahao Yang, Fan Feng, Honghao Cai, Xianxian Zeng, Rongjun Chen, Yiming Zhao, Yatong Han, Xi Li"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13781v1"
tags: ["query:rob-il"]
score: 8.0
evidence: 用于稳定高频机器人操纵的生成式控制策略
tldr: 针对生成式控制策略在处理全局稳定运动与高频局部修正时的矛盾，本文提出 KoopmanFlow。该方法引入 Koopman 结构归纳偏置，在统一多模态潜空间中将动力学解耦为宏观和瞬态两个分支。宏观分支通过一致性训练锚定慢变轨迹，瞬态分支利用流匹配捕捉高频残差，并通过非对称一致性目标进行协同训练。实验证明其在接触密集型任务中表现优异，显著提升了控制精度与参数效率。
selection_source: fresh_fetch
motivation: 现有的生成式控制策略采用统一的时间积分方案，难以在实时控制中同时捕捉低频稳态运动和高频瞬态修正。
method: 引入 Koopman 结构偏置，将生成过程解耦为负责慢变轨迹的宏观分支和负责高频残差的瞬态分支，并采用非对称一致性目标进行协同优化。
result: 在多个接触密集型机器人操作任务中，KoopmanFlow 在控制精度、抗干扰能力和参数效率方面均显著优于现有最先进基准。
conclusion: 通过频谱解耦和结构化先验，KoopmanFlow 有效解决了复杂动力学环境下的实时生成式控制难题。
---

## 摘要
生成式控制策略 (GCPs) 在机器人操作中展现出巨大潜力，但在同时建模稳定的全局运动和高频局部修正方面仍面临困难。虽然现代架构能够提取多尺度空间特征，但其底层的概率流常微分方程 (Probability Flow ODEs) 采用了统一的时间积分调度。当为了实时收缩时界控制 (RHC) 而压缩至单步时，统一的 ODE 求解器在数学上会平滑掉纠缠在低频稳态中的稀疏高频瞬态。为了在不累积流水线误差的情况下解耦这些动力学，我们引入了 KoopmanFlow，这是一种受 Koopman 启发的结构归纳偏置引导的参数高效生成策略。KoopmanFlow 在具有视觉上下文的统一多模态潜空间中运行，并在终端阶段将生成过程分叉。由于视觉条件作用发生在频谱分解之前，两个分支都受到视觉引导，但在时间特性上各具专长。宏观分支通过单步一致性训练 (Consistency Training) 锚定慢变轨迹，而瞬态分支则利用流匹配 (Flow Matching) 来隔离由突发视觉线索（如接触或遮挡）激发的高频残差。在显式频谱先验的引导下，并通过一种新型的非对称一致性目标进行优化，KoopmanFlow 建立了一种融合的协同训练机制。这使得变体分支能够吸收局部动力学，而不会产生多阶段误差累积。大量实验表明，在需要敏捷抗扰能力的富接触任务中，KoopmanFlow 的表现显著优于最先进的基准模型。通过用多余的延迟缓冲换取更丰富的结构先验，KoopmanFlow 在实时部署限制内实现了卓越的控制保真度和参数效率。

## Abstract
Generative Control Policies (GCPs) show immense promise in robotic manipulation but struggle to simultaneously model stable global motions and high-frequency local corrections. While modern architectures extract multi-scale spatial features, their underlying Probability Flow ODEs apply a uniform temporal integration schedule. Compressed to a single step for real-time Receding Horizon Control (RHC), uniform ODE solvers mathematically smooth over sparse, high-frequency transients entangled within low-frequency steady states. To decouple these dynamics without accumulating pipelined errors, we introduce KoopmanFlow, a parameter-efficient generative policy guided by a Koopman-inspired structural inductive bias. Operating in a unified multimodal latent space with visual context, KoopmanFlow bifurcates generation at the terminal stage. Because visual conditioning occurs before spectral decomposition, both branches are visually guided yet temporally specialized. A macroscopic branch anchors slow-varying trajectories via single-step Consistency Training, while a transient branch uses Flow Matching to isolate high-frequency residuals stimulated by sudden visual cues (e.g., contacts or occlusions). Guided by an explicit spectral prior and optimized via a novel asymmetric consistency objective, KoopmanFlow establishes a fused co-training mechanism. This allows the variant branch to absorb localized dynamics without multi-stage error accumulation. Extensive experiments show KoopmanFlow significantly outperforms state-of-the-art baselines in contact-rich tasks requiring agile disturbance rejection. By trading a surplus latency buffer for a richer structural prior, KoopmanFlow achieves superior control fidelity and parameter efficiency within real-time deployment limits.

---

## 论文详细总结（自动生成）

### KoopmanFlow：通过 Koopman 结构偏置实现的频谱解耦生成式控制策略

#### 1. 核心问题与整体含义
论文针对机器人操纵中的**生成式控制策略（GCPs）**面临的实时性与高保真度之间的矛盾。现有的基于扩散模型或流匹配的方法在实时部署时（如收缩时界控制 RHC），通常需要将复杂的常微分方程（ODE）求解压缩至单步（NFE=1）。然而，这种做法会导致“频谱混淆”：模型会为了维持全局运动的稳定性而抹除掉关键的高频反应性修正（如抓取瞬间的微调、接触力反馈）。论文的核心动机是利用 **Koopman 算子理论**引入结构化归纳偏置，将动作序列在频谱上解耦，从而在单步推理下同时兼顾全局平滑性和局部灵敏度。

#### 2. 方法论
KoopmanFlow 的核心思想是在生成网络的末端引入**频谱解耦机制**，将动力学分为宏观（低频）和瞬态（高频）两个分支：
*   **架构基础**：采用扩散 Transformer（DiT）作为骨干，并在终端层设计了**混合 Koopman 前馈网络（HKFFN）**。
*   **频谱解耦**：利用一维实数快速傅里叶变换（1D RFFT）作为代理，根据能量阈值 $\alpha$ 将潜空间特征分为时间不变（Time-invariant）和时间变体（Time-variant）两部分。
*   **双分支处理**：
    *   **宏观分支**：受随机 Koopman 算子（SKO）启发，通过一致性训练（Consistency Training）锚定慢变的全局轨迹，确保运动的惯性与稳定性。
    *   **瞬态分支**：利用局部动态模式分解（Localized DMD）处理滑动窗口内的特征，捕捉由视觉线索（如接触、遮挡）激发的反应性残差。
*   **融合协同训练（Fused Co-Training）**：提出一种非对称一致性目标函数，将训练批次分为流匹配部分（构建生成基础）和一致性蒸馏部分（实现单步推理），避免了多阶段训练带来的误差累积。

#### 3. 实验设计
论文在仿真和现实世界中进行了广泛评估：
*   **仿真基准**：
    *   **Adroit & DexArt**：高自由度灵巧手操作及关节类物体操作。
    *   **MetaWorld**：包含 48 个任务的语言引导多任务学习。
    *   **RoboTwin 2.0**：复杂的双臂协同任务。
*   **对比方法（Baselines）**：包括 Diffusion Policy (DP), Flow Matching, DP3, $\pi_0$, RDT, 以及最新的 ManiFlow。
*   **现实场景**：使用 AIRBOT 移动/固定平台，执行包括双臂交接、倒水、物体分类、烧烤翻面等 8 类任务。

#### 4. 资源与算力
*   **算力细节**：论文**未明确说明**具体的训练硬件（如 GPU 型号及数量）和总训练时长。
*   **推理性能**：文中提到模型参数量约为 224M，在单步推理下延迟为 **33.7 ms**（约 29.7 FPS），能够满足实时控制（< 50ms）的要求。

#### 5. 实验数量与充分性
实验设计非常充分且具有客观性：
*   **多维度评估**：涵盖了从单臂到双臂、从视觉（RGB/点云）到语言引导、从仿真到现实的多种配置。
*   **消融实验**：针对解耦惩罚项系数 $\lambda_{dec}$、一致性批次比例 $r_{ct}$、频谱能量阈值 $\alpha$ 以及损失函数组件进行了详尽的测试。
*   **统计显著性**：所有仿真实验均基于多个随机种子（如 3 个）并报告了均值与标准差，确保了结果的可重复性。

#### 6. 主要结论与发现
*   **性能领先**：KoopmanFlow 在富接触任务中显著优于所有基准模型，尤其在长程任务（如 Faucet, Bucket）中，性能提升近一倍。
*   **单步高效性**：其单步推理（NFE=1）的成功率甚至超过了某些基准模型的 10 步推理效果，证明了频谱解耦在减少计算开销的同时保留了关键动力学特征。
*   **语义对齐**：通过可视化发现，瞬态分支的能量激增与语义视觉事件（如接触瞬间）高度相关（相关性提升 6 倍），证明了其并非盲目的数学滤波，而是视觉驱动的反应机制。

#### 7. 优点
*   **结构化先验**：巧妙地将经典的 Koopman 算子理论与现代生成式流匹配结合，解决了纯数据驱动模型难以处理的高频非平稳动力学问题。
*   **参数效率**：在不显著增加参数量的情况下，通过终端层的结构改进实现了性能跨越。
*   **鲁棒性**：在现实实验中表现出极强的抗干扰能力和对未见物体的泛化能力。

#### 8. 不足与局限
*   **视觉依赖**：反应性修正高度依赖视觉线索，在极端遮挡或视觉传感器失效时，瞬态分支（$v_{var}$）可能无法有效触发。
*   **频谱假设**：使用 1D RFFT 作为频谱分解的代理虽然高效，但在处理极端非线性冲击或极其复杂的非平稳信号时，线性算子的近似可能存在精度瓶颈。
*   **超参数敏感性**：虽然实验给出了推荐值，但能量阈值 $\alpha$ 等参数在不同动力学特性的任务间可能需要微调。

（完）
