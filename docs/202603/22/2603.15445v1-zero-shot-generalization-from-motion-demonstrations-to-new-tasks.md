---
title: Zero-Shot Generalization from Motion Demonstrations to New Tasks
title_zh: 从运动演示到新任务的零样本泛化
authors: "Kilian Freitag, Alvin Combrink, Nadia Figueroa"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15445v1"
tags: ["query:rob-il"]
score: 8.0
evidence: 从运动演示到新任务的泛化
tldr: 本研究针对机器人运动策略学习中动力系统（DS）方法难以重用演示数据的问题，提出了“高斯图”模型。该方法将学习到的运动基元空间分量转化为图节点，通过“缝合”和“链式”两种框架，将连续控制与离散图搜索相结合。这使得机器人能够利用少量孤立任务的演示，在无需额外训练的情况下，实现对全新任务的零样本泛化，并保持了控制的实时性和稳定性。
selection_source: fresh_fetch
motivation: 传统的动力系统学习方法在处理复杂任务时缺乏灵活性，难以将不同任务的演示数据有效整合以应对未见过的场景。
method: 引入高斯图将运动基元建模为图顶点，并开发了缝合（Stitching）和链式（Chaining）框架来构建时间不变或序列化的动力系统。
result: 实验结果表明，该方法在模拟和真实机器人平台上均能成功完成新任务，表现优于现有的基准方法。
conclusion: 该研究通过融合离散逻辑与连续动力学，为实现机器人运动技能的模块化重用和零样本任务泛化提供了有效途径。
---

## 摘要
从专家演示中学习运动策略是现代机器人学中的一个重要范式。虽然端到端模型旨在实现广泛的泛化，但它们需要大规模数据集和计算量巨大的推理。相反，学习动力系统（DS）可以从极少数演示中提供快速、反应式且具有可证明稳定性的控制。然而，现有的 DS 学习方法通常对孤立的任务进行建模，难以将演示重用于新的行为。在这项工作中，我们将共享工作空间内组合孤立演示的问题形式化，以实现对未知任务的泛化。本文引入了高斯图（Gaussian Graph），它将学习到的运动基元（motion primitives）的空间分量重新解释为相互连接的离散顶点。这种表述使我们能够将连续控制与离散图搜索联系起来。我们提出了两个利用该图的框架：用于构建时不变 DS 的“缝合”（Stitching），以及为复杂运动提供基于序列的 DS 并保留收敛性保证的“链接”（Chaining）。仿真和真实机器人实验表明，这些方法成功地泛化到了基准方法失败的新任务中。

## Abstract
Learning motion policies from expert demonstrations is an essential paradigm in modern robotics. While end-to-end models aim for broad generalization, they require large datasets and computationally heavy inference. Conversely, learning dynamical systems (DS) provides fast, reactive, and provably stable control from very few demonstrations. However, existing DS learning methods typically model isolated tasks and struggle to reuse demonstrations for novel behaviors. In this work, we formalize the problem of combining isolated demonstrations within a shared workspace to enable generalization to unseen tasks. The Gaussian Graph is introduced, which reinterprets spatial components of learned motion primitives as discrete vertices with connections to one another. This formulation allows us to bridge continuous control with discrete graph search. We propose two frameworks leveraging this graph: Stitching, for constructing time-invariant DSs, and Chaining, giving a sequence-based DS for complex motions while retaining convergence guarantees. Simulations and real-robot experiments show that these methods successfully generalize to new tasks where baseline methods fail.

---

## 论文详细总结（自动生成）

以下是对论文《Zero-Shot Generalization from Motion Demonstrations to New Tasks》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：在机器人运动控制中，端到端大模型虽然泛化性强但计算开销大且依赖海量数据；而基于动力系统（Dynamical Systems, DS）的方法（如 LPV-DS）虽然数据效率高、反应快且具备稳定性证明，但通常只能处理孤立任务，无法将不同任务的演示数据有效重用来解决未见过的全新任务。
*   **核心问题**：如何在共享工作空间内，利用少量、孤立的专家演示，在不进行大规模重新训练的情况下，实现对新初始点-目标点对（Initial-Target Pairs）的**零样本（Zero-Shot）泛化**。

### 2. 方法论：核心思想与关键技术
论文提出了**高斯图（Gaussian Graph, GG）**框架，将连续控制与离散图搜索相结合：
*   **高斯图 (GG) 构建**：
    *   利用 LPV-DS 对每个演示任务建模，提取其高斯混合模型（GMM）中的高斯分量作为图的**顶点**。
    *   根据空间距离、数据重叠度（Bhattacharyya 系数）和动力学方向一致性定义**边**及其权重。
*   **演示缝合 (Demonstration Stitching)**：
    *   **核心思想**：在 GG 中寻找从起点到终点的最短路径或最短路径树。
    *   **技术细节**：根据路径筛选相关的高斯分量和数据点，重新拟合一个全局时不变的 LPV-DS 策略，确保系统严格收敛至新目标点。
*   **演示链接 (Demonstration Chaining)**：
    *   **核心思想**：将复杂运动分解为一系列简单的 DS 片段，通过混合自动机（Hybrid Automaton）顺序执行。
    *   **技术细节**：定义了触发器（Trigger）和计时器（Timer）逻辑，并给出了**全局渐近稳定性（GAS）的理论证明**。这种方法能处理时不变 DS 无法表示的复杂、交叉轨迹。

### 3. 实验设计
*   **数据集/场景**：
    *   **2D Small/Large**：包含多个起始点和目标点的二维合成轨迹数据集。
    *   **3D PC-GMM**：基于真实抓取演示的 3D 轨迹数据集（C 形路径、过路点等）。
    *   **真实机器人实验**：使用 UR3e 机械臂执行物体抓取与放置任务，涉及跨任务的路径组合与反向运动。
*   **Benchmark 与对比方法**：
    *   **基准方法**：标准的 LPV-DS（直接在聚合后的全量数据上训练）。
    *   **对比维度**：成功率（Success %）、均方根误差（RMSE）、数据支持度（Data Support，衡量轨迹与原始演示的贴合度）以及推理/计算时间。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号或 CPU 核心数。
*   **时间开销**：
    *   **离线预计算**：Chaining (DS) 平均需 4.2 秒，Chaining (All) 需 44.3 秒。
    *   **在线推理**：推理速度极快，Chaining (DS) 的在线计算时间仅为 0.01-0.02 秒，满足实时反应式控制需求。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 2D Large 数据集中测试了多达 182 个随机初始-目标组合。
    *   所有仿真实验均运行了 4 个不同的随机种子以确保结果的可重复性。
*   **充分性评价**：实验设计较为充分。涵盖了从简单 2D 到复杂 3D 空间，再到真实物理环境的迁移；对比了“缝合”与“链接”两种变体，并进行了消融分析（如不同级别的 DS 重用）。实验结果显示，在基准方法几乎全部失败的复杂场景下，该方法仍能保持高成功率。

### 6. 主要结论与发现
*   **零样本能力**：通过高斯图重组现有技能，机器人能够完成从未演示过的复合任务。
*   **稳定性保证**：提出的 Chaining 方法在理论上证明了稳定性，解决了复杂轨迹下的收敛问题。
*   **性能优势**：相比于直接在混合数据上训练 DS，基于图搜索的方法能有效避免不同任务间的动力学干扰，RMSE 更低且更贴合原始演示区域。
*   **双向性假设**：引入演示的可逆性（Bi-directionality）显著增强了工作空间的覆盖范围和任务灵活性。

### 7. 优点（亮点）
*   **理论与实践结合**：不仅提出了算法，还提供了严谨的动力系统稳定性证明。
*   **模块化与可扩展性**：支持增量学习，引入新任务只需在图中添加节点和边，无需重训整个网络。
*   **高效性**：将复杂计算转移至离线阶段，在线控制极其轻量，适合嵌入式机器人系统。

### 8. 不足与局限
*   **超参数依赖**：高斯图的连通性高度依赖于用户定义的超参数（如距离阈值、方向权重），缺乏自动调优机制。
*   **环境动态性**：目前主要关注空间轨迹的合成，未显式考虑实时避障或动态障碍物的干扰。
*   **空间限制**：当前实现主要基于欧几里得空间，虽然提到可扩展至 SE(3)（末端执行器姿态），但文中未给出具体实验验证。
*   **数据分布依赖**：如果演示数据过于稀疏，高斯图可能无法找到连通路径，导致泛化失败。

（完）
