---
title: "MMaDA-VLA: Large Diffusion Vision-Language-Action Model with Unified Multi-Modal Instruction and Generation"
title_zh: MMaDA-VLA：具有统一多模态指令与生成的扩散视觉-语言-动作大模型
authors: "Yang Liu, Pengxiang Ding, Tengyue Jiang, Xudong Wang, Wenxuan Song, Minghui Lin, Han Zhao, Hongyin Zhang, Zifeng Zhuang, Wei Zhao, Siteng Huang, Jinkui Shi, Donglin Wang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25406v2"
tags: ["query:rob-il"]
score: 10.0
evidence: 用于操纵的原生预训练大型扩散 VLA 模型
tldr: 针对现有视觉-语言-动作（VLA）模型在长程任务中存在的时间不一致性和缺乏环境动力学建模等问题，本文提出了MMaDA-VLA。这是一个原生预训练的大规模离散扩散VLA模型，通过将语言、图像和机器人控制嵌入统一的离散标记空间，利用掩码标记去噪技术并行生成未来目标观测和动作块。该方法不仅提高了长程一致性，还实现了无需额外模块的环境动态感知，在LIBERO和CALVIN等基准测试中达到了SOTA性能。
selection_source: fresh_fetch
motivation: 现有的VLA模型在处理长程任务时容易出现时间不一致和误差累积，且难以在不增加额外模块的情况下捕捉环境动态。
method: 提出一种原生的离散扩散框架，将多模态输入统一到离散标记空间，通过掩码去噪并行生成未来视觉目标和动作序列。
result: "在LIBERO模拟基准上实现了98.0%的平均成功率，在CALVIN任务中达到了4.78的平均完成长度，均创下新纪录。"
conclusion: MMaDA-VLA通过统一多模态理解与生成，有效提升了机器人操作任务的鲁棒性和长程一致性。
---

## 摘要
视觉-语言-动作（VLA）模型旨在根据视觉观测和自然语言指令控制机器人进行操作。然而，现有的分层和自回归范式通常会引入架构开销，面临时间不一致性和长时程误差累积的问题，并且缺乏在不增加额外模块的情况下捕获环境动力学的机制。为此，我们提出了 MMaDA-VLA，这是一个完全原生的预训练扩散 VLA 大模型，它在单一框架中统一了多模态理解与生成。我们的核心思想是一种原生的离散扩散公式，它将语言、图像和连续机器人控制嵌入到一个离散的 token 空间中，并使用掩码 token 去噪训练单个骨干网络，以并行地联合生成未来的目标观测和动作块（action chunk）。迭代去噪实现了全局、无序的细化，在不使用辅助世界模型的情况下，将动作锚定在预测的未来视觉结果中，从而提高了长时程一致性。在仿真基准测试和真实世界任务中的实验表明，该模型达到了最先进的性能，在 LIBERO 上实现了 98.0% 的平均成功率，在 CALVIN 上实现了 4.78 的平均长度。

## Abstract
Vision-Language-Action (VLA) models aim to control robots for manipulation from visual observations and natural-language instructions. However, existing hierarchical and autoregressive paradigms often introduce architectural overhead, suffer from temporal inconsistency and long-horizon error accumulation, and lack a mechanism to capture environment dynamics without extra modules. To this end, we present MMaDA-VLA, a fully native pre-trained large diffusion VLA model that unifies multi-modal understanding and generation in a single framework. Our key idea is a native discrete diffusion formulation that embeds language, images, and continuous robot controls into one discrete token space and trains a single backbone with masked token denoising to jointly generate a future goal observation and an action chunk in parallel. Iterative denoising enables global, order-free refinement, improving long-horizon consistency while grounding actions in predicted future visual outcomes without auxiliary world models. Experiments across simulation benchmarks and real-world tasks show state-of-the-art performance, achieving 98.0% average success on LIBERO and 4.78 average length on CALVIN.

---

## 论文详细总结（自动生成）

以下是对论文 **《MMaDA-VLA: Large Diffusion Vision-Language-Action Model with Unified Multi-Modal Instruction and Generation》** 的结构化深度总结：

### 1. 核心问题与整体含义（研究动机与背景）
现有的视觉-语言-动作（VLA）模型主要采用分层架构（外挂策略头）或自回归生成（离散化动作序列）。这些范式存在以下痛点：
*   **架构冗余与信息损失：** 分层设计增加了训练成本，且模块间的接口可能导致信息保真度下降。
*   **时间不一致性：** 自回归解码在处理长程任务时容易产生累积误差，且强制性的顺序生成不符合动作维度的无序本质。
*   **缺乏动力学建模：** 多数模型无法预测环境变化，通常需要额外的“世界模型”模块来实现。

**MMaDA-VLA** 旨在通过一个**原生的离散扩散框架**，将多模态理解（指令）与生成（目标图像预测+动作序列生成）统一在单个骨干网络中，从而提升机器人操作的精确度和长程一致性。

### 2. 方法论：核心思想与技术细节
*   **统一离散 Token 空间：**
    *   **语言：** 使用 LLaDA 分词器。
    *   **视觉：** 使用 MAGVIT-v2 量化器将图像转化为离散 token。
    *   **动作：** 将连续动作的每个维度离散化为 256 个 bin。
    *   所有模态共享一个词表，将 VLA 任务转化为统一的掩码 token 预测问题。
*   **原生离散扩散公式：**
    *   模型通过**掩码 token 去噪（Masked Token Denoising）**进行训练。
    *   在推理时，从全掩码状态开始，通过迭代去噪并行生成**未来目标观测（Goal Observation）**和**动作块（Action Chunk）**。
*   **混合注意力机制（Hybrid Attention）：**
    *   **模态内：** 采用双向全注意力（Bidirectional Attention），允许 token 间全局信息交换，尊重动作维度的无序性。
    *   **模态间：** 采用因果注意力（Causal Attention），确保信息从指令流向生成目标。
*   **并行生成与细化：** 动作生成与未来视觉目标的预测是同步进行的。这种设计让模型在生成动作时能隐式地参考预测的视觉演变，起到类似“内置世界模型”的作用。

### 3. 实验设计
*   **仿真基准测试：**
    *   **LIBERO：** 包含 Spatial、Object、Goal、Long 四个子集，测试多任务泛化能力。
    *   **CALVIN：** 重点测试长程语言条件下的操作，采用 ABC→D 设置（在环境 ABC 训练，在环境 D 测试泛化性）。
*   **真实世界评估：** 使用 AgileX Piper 6-DoF 机械臂，涵盖简单取放、精准堆叠、复杂抽屉存储和长程桌面整理四类任务。
*   **对比方法：** 包括 OpenVLA、RT-1、Diffusion Policy、π0、VLA-Adapter 以及带有世界模型属性的 UniVLA、DreamVLA 等 SOTA 模型。

### 4. 资源与算力
*   **骨干网络：** 基于 8B 参数的 MMaDA-8B-Base。
*   **算力投入：** 使用了 **8 台 Nvidia H800 GPU 节点**（每台 8 卡，共 64 张 GPU），每张显卡拥有 80GB 显存。
*   **训练时长：** 大规模预训练阶段耗时约 **30 小时**。
*   **推理优化：** 引入了训练无关的 KV Cache 缓存框架和选择性刷新策略，以满足机器人操作的实时性需求。

### 5. 实验数量与充分性
*   **数据规模：** 预训练使用了来自 DROID、Fractal、Bridge V2 等多个数据集的 **6100 万步** 跨化身（cross-embodiment）数据。
*   **实验覆盖：**
    *   仿真实验：在 CALVIN 上进行了 1000 次 rollout 评估。
    *   真实实验：每类任务收集 300 个演示进行微调，评估时每项任务进行 30 次测试。
    *   **消融实验：** 针对“有无世界模型预测”、“是否并行去噪”、“不同注意力机制”以及“预训练的作用”进行了详细对比。
*   **评价：** 实验设计全面，涵盖了从仿真到现实、从短程到长程、从单一任务到多任务泛化的多个维度，对比基线均为当前领域最强模型，结果具有较高的说服力和客观性。

### 6. 主要结论与发现
*   **性能领先：** MMaDA-VLA 在 LIBERO 上达到 98.0% 成功率，在 CALVIN ABC→D 任务中平均完成长度达到 4.78（接近满分 5.0），刷新了纪录。
*   **长程一致性：** 相比自回归模型，离散扩散机制显著提升了长序列任务的稳定性，CALVIN 第五项子任务的成功率从 80% 以下提升至约 90%。
*   **动力学增益：** 消融实验证明，加入未来视觉观测预测（世界模型属性）能显著提升动作预测的质量（CALVIN 平均长度提升 0.48）。

### 7. 优点与亮点
*   **架构优雅：** 无需外挂复杂的策略头或独立的世界模型，在单一 Transformer 骨干中实现了理解与生成的闭环。
*   **并行解码优势：** 迭代去噪避免了自回归的顺序偏差，能够全局细化动作轨迹，减少了长程误差累积。
*   **强大的泛化力：** 通过大规模跨化身预训练，模型在面对未见过的环境和物体时表现出极强的鲁棒性。

### 8. 不足与局限
*   **视觉生成质量有限：** 预测的未来图像虽然在语义和动力学上一致，但像素级保真度较低（如夹持器细节模糊），这限制了其在极高精度操作中的引导作用。
*   **推理延迟挑战：** 尽管使用了缓存优化，但扩散模型的多次迭代去噪在实时性要求极高的场景下仍可能面临计算压力。
*   **失败案例：** 在真实实验中，面对极小开口的容器或需要精细力控的场景（如抽屉卡住），模型仍存在失败概率。

（完）
