---
title: "Emergent Neural Automaton Policies: Learning Symbolic Structure from Visuomotor Trajectories"
title_zh: 涌现神经自动机策略：从视觉运动轨迹中学习符号结构
authors: "Yiyuan Pan, Xusheng Luo, Hanjiang Hu, Peiqi Yu, Changliu Liu"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25903v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 从视觉运动轨迹中学习神经符号策略
tldr: "针对长程机器人学习中端到端模型缺乏结构先验及神经符号方法依赖手工设计的问题，本文提出ENAP框架。该框架通过自适应聚类和扩展L*算法从视觉运动演示中自动推断Mealy状态机作为高层规划器，并结合底层残差网络实现连续控制。ENAP在无需任务标签的情况下，显著提升了样本效率和可解释性，在复杂任务中性能优于现有SOTA模型。"
selection_source: fresh_fetch
motivation: 旨在解决端到端策略在长程任务中缺乏结构推理能力，以及传统神经符号方法过度依赖手工符号先验的局限性。
method: "提出一种双层神经符号策略，利用自适应聚类和L*算法从数据中学习离散状态机，并以此指导底层残差网络进行行为克隆。"
result: "在复杂操作和长程任务中，ENAP在低数据量情况下的表现比现有SOTA视觉语言动作（VLA）策略高出多达27%。"
conclusion: ENAP证明了通过自动学习离散任务结构与连续控制相结合，可以有效提升机器人学习的效率、泛化能力和可解释性。
---

## 摘要
将机器人学习扩展到长程任务仍然是一个巨大的挑战。虽然端到端策略通常缺乏有效长期推理所需的结构先验，但传统的神经符号方法又过度依赖手工设计的符号先验。为了解决这一问题，我们提出了 ENAP（Emergent Neural Automaton Policy，涌现神经自动机策略），这是一个允许双层神经符号策略从视觉运动演示中自适应涌现的框架。具体而言，我们首先采用自适应聚类和 L* 算法的扩展版本，从视觉运动数据中推断出 Mealy 状态机，作为捕获潜在任务模式的可解释高层规划器。然后，这种离散结构引导低层反应式残差网络通过行为克隆（BC）学习精确的连续控制。通过利用离散转移和连续残差显式建模任务结构，ENAP 在不需要任务特定标签的情况下实现了高样本效率和可解释性。在复杂操作和长程任务上的广泛实验表明，ENAP 在低数据量情况下的性能优于最先进的（SoTA）端到端 VLA 策略高达 27%，同时提供了机器人意图的结构化表示。

## Abstract
Scaling robot learning to long-horizon tasks remains a formidable challenge. While end-to-end policies often lack the structural priors needed for effective long-term reasoning, traditional neuro-symbolic methods rely heavily on hand-crafted symbolic priors. To address the issue, we introduce ENAP (Emergent Neural Automaton Policy), a framework that allows a bi-level neuro-symbolic policy adaptively emerge from visuomotor demonstrations. Specifically, we first employ adaptive clustering and an extension of the L* algorithm to infer a Mealy state machine from visuomotor data, which serves as an interpretable high-level planner capturing latent task modes. Then, this discrete structure guides a low-level reactive residual network to learn precise continuous control via behavior cloning (BC). By explicitly modeling the task structure with discrete transitions and continuous residuals, ENAP achieves high sample efficiency and interpretability without requiring task-specific labels. Extensive experiments on complex manipulation and long-horizon tasks demonstrate that ENAP outperforms state-of-the-art (SoTA) end-to-end VLA policies by up to 27% in low-data regimes, while offering a structured representation of robotic intent (Fig. 1).

---

## 论文详细总结（自动生成）

### 论文总结：涌现神经自动机策略 (ENAP)

#### 1. 核心问题与研究动机
*   **研究背景**：长程（Long-horizon）机器人任务要求高层逻辑推理与底层连续控制的协同。目前的端到端（E2E）模型（如 VLA）虽然在短程任务表现出色，但在长程任务中由于缺乏结构先验，往往面临数据效率低、黑盒不可解释等问题。
*   **核心矛盾**：传统的神经符号方法（Neuro-symbolic）虽然引入了结构，但高度依赖人工设计的符号谓词（如 PDDL），难以从原始数据中自适应学习。
*   **核心问题**：如何让“系统 2”（符号推理）的逻辑结构从“系统 1”（感知运动流）中自然涌现，而无需人工标注？

#### 2. 方法论：ENAP 框架
ENAP 提出了一种双层神经符号策略，将任务分解为离散的状态机规划和连续的残差控制。
*   **自适应符号抽象**：利用视觉编码器（如 DINOv2）提取特征，通过 **HDBSCAN** 聚类算法将连续观测空间离散化为符号字母表 $\Sigma$。
*   **扩展 L* 算法提取结构**：
    *   引入一种针对连续轨迹的 **L* 算法扩展版**，从演示数据中推断 **概率 Mealy 状态机 (PMM)**。
    *   **RNN 历史编码**：使用 RNN 将变长的历史轨迹映射为固定维度的嵌入，作为状态节点的连续代理。
    *   **自监督成员查询 (MQ)**：在数据集中检索相似的历史嵌入，构建状态转移。
    *   **非确定性等价查询 (EQ)**：通过在现有状态机中进行路径搜索来验证假设，若失败则引入反例并细化状态。
*   **双层控制流水线**：
    *   **高层**：PMM 根据当前状态和观测符号提供一个粗略的动作先验 $a_t^{base}$。
    *   **底层**：一个轻量级的残差网络 $\pi_\psi$ 根据实时观测对先验进行修正，生成最终动作 $\hat{a}_t = a_t^{base} + \Delta a_t$。
*   **迭代残差学习 (EM 风格)**：交替进行结构提取（E-step）和策略优化（M-step），通过行为克隆（BC）微调编码器和残差网络。

#### 3. 实验设计与 Benchmark
*   **实验场景**：
    1.  **复杂操作 (ManiSkill)**：PegInsert（插拔）、DualStackCube（双块堆叠）、MultiGoalPushT（多目标推 T）。
    2.  **长程 TAMP (CALVIN)**：包含 5 个阶段的顺序任务和具有依赖关系的层次任务。
    3.  **现实世界**：使用 Kinova Gen3 机械臂进行 StackLego（乐高堆叠）、MultiPickPlace（颜色匹配分类）、Hanger（挂衣架）。
*   **对比基准 (Baselines)**：
    *   **生成式/VLA 策略**：Diffusion Policy, OpenVLA, $\pi_0$, $\pi_{0.5}$。
    *   **传统模仿学习**：Transformer BC, GMM。
    *   **神经符号变体**：LATMOS, Serialized FSM 等。

#### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   但提到了模型规模：ENAP 显著比 VLA 模型更轻量。例如，ENAP (DINO) 约为 **23M** 参数，而对比的 OpenVLA 为 **7.6B**，$\pi_0$ 为 **3.3B**。ENAP 在参数量减少约 99% 的情况下实现了更优性能。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了仿真环境和真实世界，总计 8 种不同的任务类型。
*   **数据效率实验**：专门做了数据缩放（Data-scaling）实验，测试了在 25%、50%、100% 演示数据下的表现。
*   **消融实验**：针对 RNN 损失函数（对比损失、预测损失）和相似度阈值 $\tau_{sim}$ 进行了详细消融。
*   **结构度量**：引入了 6 个维度（成功率、节点效率、动作先验保真度等）的雷达图分析。
*   **评价**：实验设计较为全面，不仅关注成功率，还深入分析了学习到的自动机结构的语义合理性，具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能领先**：在低数据量情况下，ENAP 的成功率比 SOTA VLA 模型高出 **8% 到 27%**。
*   **涌现结构**：ENAP 能自动发现任务中的逻辑分支（如不同初始位置的处理）和循环依赖（如失败后的重试逻辑）。
*   **鲁棒性**：通过状态机中的“自循环”结构，机器人展现出了自然的**错误恢复能力**（如乐高对齐失败后自动重试）。
*   **可解释性**：学习到的状态机节点可以与人类语义（如“抓取”、“对齐”）对应，且决策过程透明。

#### 7. 优点与亮点
*   **无需标签**：完全从原始视觉运动数据中学习符号结构，摆脱了对专家预定义谓词的依赖。
*   **双层架构优势**：将全局任务逻辑（离散）与局部精确控制（连续）解耦，降低了神经网络的学习难度。
*   **高效率**：在极小规模的参数量下达到了大规模预训练模型的性能，适合实时部署。
*   **理论支撑**：论文从 POMDP 和贝尔曼方程的角度为双层架构提供了理论证明。

#### 8. 不足与局限
*   **超参数敏感性**：结构的质量高度依赖于聚类阈值 $\tau_{sim}$ 和 RNN 的训练效果，参数设置不当可能导致状态爆炸或过于简化。
*   **视觉编码器依赖**：虽然是自适应的，但初始特征空间的质量（如 DINOv2 的预训练效果）对符号抽象的成败至关重要。
*   **跨任务泛化**：目前主要针对单一或多模态特定任务，如何将学习到的自动机结构迁移到完全不同的任务或机器人形态上仍需探索。

（完）
