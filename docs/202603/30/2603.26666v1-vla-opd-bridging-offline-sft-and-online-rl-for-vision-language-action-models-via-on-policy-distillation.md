---
title: "VLA-OPD: Bridging Offline SFT and Online RL for Vision-Language-Action Models via On-Policy Distillation"
title_zh: VLA-OPD：通过同策略蒸馏桥接视觉-语言-动作模型的离线监督微调与在线强化学习
authors: "Zhide Zhong, Haodong Yan, Junfeng Li, Junjie He, Tianran Zhang, Haoang Li"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26666v1"
tags: ["query:rob-il"]
score: 10.0
evidence: 机器人操纵中VLA模型的在线策略蒸馏
tldr: 针对视觉-语言-动作（VLA）模型在部署时面临的离线微调分布偏移和在线强化学习效率低下的问题，本文提出了VLA-OPD框架。该框架通过在线策略蒸馏，利用专家教师对学生自生成轨迹进行密集的Token级监督。采用Reverse-KL目标函数，在纠正策略偏差的同时保留预训练能力，显著提升了模型的鲁棒性与采样效率。
selection_source: fresh_fetch
motivation: 传统的离线SFT存在分布偏移和灾难性遗忘，而在线RL受限于稀疏奖励和低采样效率，难以实现高效的VLA模型后训练。
method: 提出VLA-OPD框架，利用专家教师在学生生成的轨迹上提供密集监督，并结合Reverse-KL目标函数实现稳定的在线策略蒸馏。
result: 在LIBERO和RoboTwin2.0基准测试中，VLA-OPD在采样效率上优于RL，在鲁棒性上优于SFT，并有效缓解了灾难性遗忘。
conclusion: VLA-OPD成功桥接了SFT的效率与RL的鲁棒性，为VLA模型提供了一种稳定且高效的在线对齐与性能提升方案。
---

## 摘要
尽管预训练的视觉-语言-动作（VLA）模型在机器人操控中表现出令人印象深刻的泛化能力，但后期训练对于确保部署期间的可靠性能仍然至关重要。然而，标准的离线监督微调（SFT）面临分布偏移和预训练能力灾难性遗忘的问题，而在线强化学习（RL）则受困于稀疏奖励和低样本效率。在本文中，我们提出了同策略 VLA 蒸馏（VLA-OPD），这是一个桥接了 SFT 的效率与 RL 的鲁棒性的框架。VLA-OPD 不依赖于稀疏的环境奖励，而是利用专家教师在学生自生成的轨迹上提供密集的、标记级（token-level）的监督。这使得模型能够在策略诱导的状态下进行主动纠错，同时通过温和的对齐保留预训练的通用能力。至关重要的是，我们通过逆向 KL（Reverse-KL）目标来构建 VLA-OPD。与导致模式覆盖（mode-covering）熵爆炸的标准正向 KL，或导致过早熵崩溃的硬交叉熵（Hard-CE）不同，我们的有界模式寻求（mode-seeking）目标通过过滤教师的认知不确定性并保持动作多样性，确保了稳定的策略学习。在 LIBERO 和 RoboTwin2.0 基准测试上的实验表明，VLA-OPD 相比 RL 显著提高了样本效率，相比 SFT 显著提高了鲁棒性，同时有效缓解了后期训练过程中的灾难性遗忘。

## Abstract
Although pre-trained Vision-Language-Action (VLA) models exhibit impressive generalization in robotic manipulation, post-training remains crucial to ensure reliable performance during deployment. However, standard offline Supervised Fine-Tuning (SFT) suffers from distribution shifts and catastrophic forgetting of pre-trained capabilities, while online Reinforcement Learning (RL) struggles with sparse rewards and poor sample efficiency. In this paper, we propose On-Policy VLA Distillation (VLA-OPD), a framework bridging the efficiency of SFT with the robustness of RL. Instead of relying on sparse environmental rewards, VLA-OPD leverages an expert teacher to provide dense, token-level supervision on the student's self-generated trajectories. This enables active error correction on policy-induced states while preserving pre-trained general capabilities through gentle alignment. Crucially, we formulate VLA-OPD via a Reverse-KL objective. Unlike standard Forward-KL that induces mode-covering entropy explosion, or Hard-CE that causes premature entropy collapse, our bounded mode-seeking objective ensures stable policy learning by filtering out the teacher's epistemic uncertainty while maintaining action diversity. Experiments on LIBERO and RoboTwin2.0 benchmarks demonstrate that VLA-OPD significantly improves sample efficiency over RL and robustness over SFT, while effectively mitigating catastrophic forgetting during post-training.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《VLA-OPD: Bridging Offline SFT and Online RL for Vision-Language-Action Models via On-Policy Distillation》** 的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何高效且稳健地对预训练的视觉-语言-动作（VLA）模型进行后期训练（Post-training），以提升其在特定机器人操控任务中的表现。
*   **研究背景**：
    *   **离线监督微调（SFT/行为克隆）**：虽然收敛快，但存在“分布偏移”问题（一旦偏离专家轨迹就无法纠错）和“灾难性遗忘”（损害预训练的通用能力）。
    *   **在线强化学习（RL）**：通过环境交互解决分布偏移，但受限于“稀疏奖励”，导致采样效率极低且训练不稳定。
*   **整体含义**：论文提出了 **VLA-OPD** 框架，旨在结合 SFT 的高效率和 RL 的高鲁棒性，通过“同策略蒸馏”实现两者的桥接。

### 2. 方法论：核心思想与技术细节
VLA-OPD 的核心是将对齐过程重新表述为在**学生自生成轨迹**上的**密集监督**。其流程分为三个阶段：
*   **阶段 1：同策略采样（Student Sampling）**：学生模型在环境中执行任务并收集轨迹。这使得模型能暴露在自己诱导的分布下，捕捉到“错误状态”。
*   **阶段 2：教师密集标注（Teacher Labeling）**：对于学生访问的每一个状态，调用一个冻结的专家教师模型（Teacher）提供 Token 级的动作概率分布（Logits），作为密集监督信号。
*   **阶段 3：逆向 KL 优化（Reverse-KL Optimization）**：
    *   **核心公式**：定义内在奖励 $r_t = -(\log \pi_{\theta}(a_t|s_t) - \log \pi_{tea}(a_t|s_t))$。
    *   **模式寻求（Mode-seeking）**：采用 **Reverse-KL** 目标函数。相比于标准 SFT 的 Forward-KL（会导致“模式覆盖”和熵爆炸，使模型犹豫不决）或 Hard-CE（会导致熵崩溃，失去多样性），Reverse-KL 允许学生在教师确定的动作模式中保持决策的果断性，同时过滤掉教师在分布外状态下的不确定性。
    *   **组基梯度估计**：借鉴 GRPO 的思想，通过组采样（Group Sampling）来降低策略梯度的方差。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO 基准测试**：包含 Spatial、Object、Goal、Long 四个子集，用于测试单臂操控。
    *   **RoboTwin2.0 基准测试**：选择 4 个具有挑战性的双臂协同任务。
*   **实验设置**：
    *   **极度数据稀缺场景**：在 LIBERO 上仅使用 1 条演示轨迹（1-traj）初始化学生模型。
    *   **复杂任务场景**：在 RoboTwin2.0 上使用 1000 条轨迹初始化。
*   **对比方法（Baselines）**：
    *   **教师模型**：SimpleVLA-RL（性能上限）。
    *   **离线方法**：Octo、OpenVLA、Nora、$\pi_0$、RDT 等。
    *   **在线方法**：基于稀疏奖励的 GRPO。

### 4. 资源与算力
*   **论文提及**：文中提到了训练时的超参数设置，如 Batch Size 为 64 或 32，组采样大小 $G=8$。
*   **未明确说明**：论文**未详细列出**具体的 GPU 型号（如 A100/H100）、GPU 数量以及总训练时长（Wall-clock time）。这在学术论文中较为常见，但对于评估实际部署成本略显不足。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了单臂与双臂、简单与长程任务，实验维度较为全面。
    *   **消融实验**：深入对比了 Reverse-KL、Forward-KL 和 Hard-CE 三种目标函数的熵变化与成功率，验证了 Reverse-KL 的优越性。
    *   **遗忘分析**：专门设计了“已见任务 vs 未见任务”的权衡实验，验证模型在微调时是否保留了通用能力。
*   **充分性评价**：实验设计非常充分且具有针对性。通过 1-traj 的极端设置证明了方法的效率，通过双臂任务证明了复杂场景的适用性，逻辑闭环完整。

### 6. 主要结论与发现
*   **高采样效率**：在 LIBERO-Long 任务中，VLA-OPD 仅需 50 步即可达到 GRPO 150 步的效果，实现 **3 倍加速**。
*   **性能跨越**：在仅有 1 条演示轨迹的情况下，VLA-OPD 的成功率（87.4%）接近甚至超过了使用 50 条轨迹训练的完整数据集基准。
*   **缓解遗忘**：相比离线 SFT 在微调后对未见任务性能的断崖式下跌，VLA-OPD 能有效维持预训练模型的通用泛化能力。
*   **稳定性**：Reverse-KL 能够维持健康的策略熵，避免了训练过程中的性能剧烈波动。

### 7. 优点与亮点
*   **理论深刻**：对 Reverse-KL 的“模式寻求”特性在机器人动作预测中的应用做了精辟的分析，解决了模仿学习中常见的熵爆炸/崩溃问题。
*   **实用性强**：通过引入专家教师（可以是开源模型或 API），避开了强化学习中最头疼的奖励函数设计和稀疏奖励问题。
*   **对齐温和**：同策略蒸馏确保了梯度更新锚定在学生当前的动作流形上，实现了更“温和”的对齐，保护了预训练知识。

### 8. 不足与局限
*   **教师依赖性**：该框架高度依赖于一个高性能“专家教师”的存在。如果对于某些新任务没有现成的专家模型，该方法的启动门槛较高。
*   **算力开销**：虽然采样效率高，但“同策略”意味着需要频繁进行环境渲染和教师模型推理，这在实际训练中可能产生较大的计算延迟。
*   **泛化边界**：虽然缓解了遗忘，但对于教师模型本身也无法处理的极端分布外状态，蒸馏的效果可能受限。

（完）
