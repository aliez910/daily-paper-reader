---
title: "LIBERO-Para: A Diagnostic Benchmark and Metrics for Paraphrase Robustness in VLA Models"
title_zh: LIBERO-Para：VLA 模型中释义鲁棒性的诊断基准与指标
authors: "Chanyoung Kim, Minwoo Kim, Minseok Kang, Hyunwoo Kim, Dahuin Jung"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28301v1"
tags: ["query:rob-il"]
score: 9.0
evidence: VLA模型在操纵任务中指令改写鲁棒性的诊断基准
tldr: "针对视觉-语言-动作（VLA）模型在机器人操作中对指令改写鲁棒性不足的问题，本文提出了LIBERO-Para基准测试。通过对7种不同规模的VLA模型进行评估，发现改写会导致性能大幅下降（22-52%），且失败主要源于规划层面的任务识别错误。研究还提出了PRIDE指标来量化改写难度，为提升模型的语义理解能力提供了诊断工具。"
selection_source: fresh_fetch
motivation: VLA模型在下游任务微调时容易过拟合特定的指令表述，导致其对同义改写指令的泛化能力和鲁棒性尚不明确。
method: 开发了LIBERO-Para基准测试以独立分析动作和物体的词汇变化，并提出PRIDE指标通过语义和语法因素量化改写难度。
result: "实验发现改写导致模型性能下降22-52%，且绝大部分失败源于规划层面的轨迹偏离而非执行错误。"
conclusion: VLA模型目前高度依赖表面词汇匹配而非语义理解，未来需重点解决物体级词汇变化带来的泛化挑战。
---

## 摘要
视觉-语言-动作（VLA）模型通过利用预训练的视觉-语言骨干网络，在机器人操作中表现出强大的性能。然而，在下游机器人场景中，这些模型通常使用有限的数据进行微调，导致对特定指令表述的过拟合，而对释义指令的鲁棒性仍缺乏深入研究。为了研究这一差距，我们引入了 LIBERO-Para，这是一个受控基准，通过独立改变动作表达和物体引用，对语言泛化进行细粒度分析。在七种 VLA 配置（0.6B-7.5B）中，我们观察到在释义情况下性能一致下降了 22-52 个百分点。这种性能下降主要由物体层面的词汇变化驱动：即使是简单的同义词替换也会导致大幅下降，这表明模型依赖于表层匹配而非语义接地（semantic grounding）。此外，80-96% 的失败源于规划层面的轨迹偏离而非执行错误，这表明释义干扰了任务识别。二元成功率对所有释义一视同仁，掩盖了模型是在不同难度级别下表现一致，还是仅仅依赖于较简单的案例。为解决这一问题，我们提出了 PRIDE，这是一种利用语义和句法因素量化释义难度的指标。我们的基准和相应代码可在以下网址获取：https://github.com/cau-hai-lab/LIBERO-Para

## Abstract
Vision-Language-Action (VLA) models achieve strong performance in robotic manipulation by leveraging pre-trained vision-language backbones. However, in downstream robotic settings, they are typically fine-tuned with limited data, leading to overfitting to specific instruction formulations and leaving robustness to paraphrased instructions underexplored. To study this gap, we introduce LIBERO-Para, a controlled benchmark that independently varies action expressions and object references for fine-grained analysis of linguistic generalization. Across seven VLA configurations (0.6B-7.5B), we observe consistent performance degradation of 22-52 pp under paraphrasing. This degradation is primarily driven by object-level lexical variation: even simple synonym substitutions cause large drops, indicating reliance on surface-level matching rather than semantic grounding. Moreover, 80-96% of failures arise from planning-level trajectory divergence rather than execution errors, showing that paraphrasing disrupts task identification. Binary success rate treats all paraphrases equally, obscuring whether models perform consistently across difficulty levels or rely on easier cases. To address this, we propose PRIDE, a metric that quantifies paraphrase difficulty using semantic and syntactic factors. Our benchmark and corresponding code are available at: https://github.com/cau-hai-lab/LIBERO-Para

---

## 论文详细总结（自动生成）

以下是对论文《LIBERO-Para: A Diagnostic Benchmark and Metrics for Paraphrase Robustness in VLA Models》的详细总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉-语言-动作（VLA）模型虽然在机器人操作中表现出色，但在针对特定环境进行“数据稀缺型微调”后，容易过拟合于训练集中见过的特定指令表述。
*   **研究动机**：现有的基准测试（如 LIBERO）主要关注视觉泛化，而忽略了语言鲁棒性。当用户使用同义改写（Paraphrase）但意思不变的指令时，模型往往会失效。
*   **整体含义**：本文旨在填补这一空白，通过构建受控的诊断基准和量化指标，深入分析 VLA 模型在面对指令改写时的脆弱性及其背后的失败机理。

### 2. 论文提出的方法论
*   **LIBERO-Para 基准测试**：
    *   **双轴设计**：独立改变“动作表达”（Action）和“物体引用”（Object）。
    *   **动作轴**：涵盖词汇变化（同义词）、结构变化（从句/协作）和语用变化（间接请求，如“我需要...”或“你能...吗？”）。
    *   **物体轴**：侧重词汇层面的变化，如添加描述词或同义词替换（如将“stove”换为“range”）。
    *   **自动生成与验证**：利用 LLM 生成 4,092 条改写指令，并通过人工和模型双重验证确保语义一致性。
*   **PRIDE 指标（核心算法）**：
    *   **关键词相似度 ($S_K$)**：利用 Sentence-BERT 嵌入计算指令中核心动词和名词的语义匹配度。
    *   **结构相似度 ($S_T$)**：利用依存树编辑距离（Tree Edit Distance）衡量句法结构的偏离程度。
    *   **PRIDE 分数**：结合 $S_K$ 和 $S_T$ 得到改写难度权重，并与任务成功与否挂钩，从而区分模型是解决了“简单改写”还是“困难改写”。

### 3. 实验设计
*   **数据集/场景**：基于 LIBERO-Goal 场景，该场景要求模型必须完全依赖语言指令来区分任务（初始视觉状态相同）。
*   **对比方法**：评估了 7 种主流 VLA 模型配置，涵盖不同架构：
    *   **并行解码类**：OpenVLA-OFT (7.5B)。
    *   **流匹配/专家模型类**：$\pi_{0.5}$ (3.3B)、Xiaomi-Robotics-0 (4.7B)。
    *   **轻量级适配器类**：VLA-Adapter (0.6B)、X-VLA (0.9B)。
*   **分析维度**：成功率下降幅度、PRIDE 分数分布、物体 vs. 动作敏感度、轨迹偏离分析。

### 4. 资源与算力
*   **硬件使用**：使用了 NVIDIA RTX A6000 和 NVIDIA L40S GPU。
*   **算力消耗**：
    *   **评估阶段**：总计约 **194 个 GPU 小时**。
    *   **训练阶段**：对 $\pi_{0.5}$ (expert-only) 进行了 30,000 步的微调。
*   **显存占用**：不同模型在推理时占用 3GB 到 38GB 不等的显存。

### 5. 实验数量与充分性
*   **实验规模**：针对 10 个原始任务生成了 **4,092 条改写指令**，每种配置在 5 个随机种子下运行。
*   **充分性**：实验覆盖了从 0.6B 到 7.5B 的多种参数规模和架构，具有广泛的代表性。
*   **客观性**：引入了 15 名标注者进行人工评估（Gwet’s AC1 系数为 0.854，显示高度一致性），并使用动态时间规整（DTW）算法对失败轨迹进行客观分类。

### 6. 论文的主要结论与发现
*   **普遍的脆弱性**：所有模型在改写指令下性能均大幅下降（**22-52 个百分点**），即使是表现最好的模型也无法幸免。
*   **物体接地是瓶颈**：物体层面的词汇变化（如同义词替换）对性能的影响远大于动作表达的变化。模型高度依赖“表面词汇匹配”而非真正的语义理解。
*   **规划层面的失败**：**80-96% 的失败属于“规划级偏离”**（Far-GT），即模型从一开始就找错了目标或理解错了任务，而非动作执行不精准。
*   **微调的负面影响**：联合微调 VLM 和动作专家虽然能提高性能，但无法消除对特定表述的依赖。

### 7. 优点
*   **细粒度诊断**：通过双轴设计，成功分离了物体和动作对鲁棒性的不同影响。
*   **创新的指标**：PRIDE 指标比传统的 NLP 指标（如 BLEU, BERTScore）更能反映机器人任务中的语言难度。
*   **轨迹级分析**：不仅关注“是否成功”，还通过轨迹分析揭示了失败发生在“感知/规划”阶段而非“控制”阶段。

### 8. 不足与局限
*   **仿真与现实的差距**：所有实验均在 LIBERO 仿真环境中完成，尚未在物理机器人上验证。
*   **复合改写的局限**：虽然包含了一些复合改写，但主要分析仍集中在单一维度的变化，现实中语言的复杂性可能更高。
*   **缺乏缓解策略**：论文重点在于“诊断”问题，虽然提到了数据增强的潜力，但未深入探讨如何系统性地修复这些鲁棒性漏洞。

（完）
