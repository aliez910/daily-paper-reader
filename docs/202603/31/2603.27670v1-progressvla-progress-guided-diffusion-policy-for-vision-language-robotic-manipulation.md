---
title: "ProgressVLA: Progress-Guided Diffusion Policy for Vision-Language Robotic Manipulation"
title_zh: ProgressVLA：面向视觉-语言机器人操控的进度引导扩散策略
authors: "Hongyu Yan, Qiwei Li, Jiaolong Yang, Yadong Mu"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27670v1"
tags: ["query:rob-il"]
score: 10.0
evidence: 用于视觉语言机器人操纵的进度引导扩散策略
tldr: ProgressVLA 针对现有视觉-语言-动作（VLA）模型在长程任务中缺乏进度感知的问题，提出了一种进度引导的扩散策略。该模型包含一个在大规模无监督数据集上预训练的鲁棒进度估计器，以及一个通过逆动力学世界模型实现的微分进度引导机制。通过最大化预期进度来优化动作生成，显著提升了机器人在复杂任务中的成功率和泛化能力。
selection_source: fresh_fetch
motivation: 现有 VLA 模型缺乏对任务进度的感知，难以在长程任务中准确判断任务终止或子目标完成情况。
method: 提出 ProgressVLA，结合了在大规模数据集上预训练的进度估计器和基于逆动力学世界模型的微分进度引导机制。
result: 在 CALVIN、LIBERO 基准测试及真实机器人部署中，该方法在成功率和泛化性能上均显著优于现有基准模型。
conclusion: 引入进度感知和微分引导机制能有效提升 VLA 模型在复杂长程机器人操控任务中的表现。
---

## 摘要
大多数现有的用于机器人操控的视觉-语言-动作（VLA）模型缺乏进度感知能力，通常依赖于手工设计的启发式方法来终止任务。这种局限性在涉及级联子目标的长程任务中尤为严重。在这项工作中，我们研究了任务进度的估计与集成，并提出了一种名为 ProgressVLA 的新型模型。我们的技术贡献包括两个方面：（1）鲁棒的进度估计：我们在大规模、无监督的视频-文本机器人数据集上预训练了一个进度估计器。该估计器在仿真中实现了较低的预测残差（在 [0, 1] 尺度上为 0.07），并展示了对未见过的真实世界样本的零样本泛化能力；（2）可微进度引导：我们引入了一个逆动力学世界模型，将预测的动作 token 映射到未来的潜在视觉状态。这些潜在状态随后由进度估计器处理；通过应用最大进度正则化，我们建立了一个可微流水线，提供进度引导的指导以优化动作 token。在 CALVIN 和 LIBERO 基准测试上的广泛实验以及真实世界的机器人部署一致表明，与强基线模型相比，该方法在成功率和泛化能力方面都有显著提升。

## Abstract
Most existing vision-language-action (VLA) models for robotic manipulation lack progress awareness, typically relying on hand-crafted heuristics for task termination. This limitation is particularly severe in long-horizon tasks involving cascaded sub-goals. In this work, we investigate the estimation and integration of task progress, proposing a novel model named {\textbf \vla}. Our technical contributions are twofold: (1) \emph{robust progress estimation}: We pre-train a progress estimator on large-scale, unsupervised video-text robotic datasets. This estimator achieves a low prediction residual (0.07 on a scale of $[0, 1]$) in simulation and demonstrates zero-shot generalization to unseen real-world samples, and (2) \emph{differentiable progress guidance}: We introduce an inverse dynamics world model that maps predicted action tokens into future latent visual states. These latents are then processed by the progress estimator; by applying a maximal progress regularization, we establish a differentiable pipeline that provides progress-piloted guidance to refine action tokens. Extensive experiments on the CALVIN and LIBERO benchmarks, alongside real-world robot deployment, consistently demonstrate substantial improvements in success rates and generalization over strong baselines.

---

## 论文详细总结（自动生成）

以下是对论文《ProgressVLA: Progress-Guided Diffusion Policy for Vision-Language Robotic Manipulation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在执行机器人操控任务时，普遍缺乏对**任务进度（Progress）**的显式感知。
*   **研究动机**：
    *   **终止机制脆弱**：现有模型通常依赖手工设计的启发式规则来判断任务是否结束，在长程（Long-horizon）任务中表现不佳。
    *   **缺乏目标导向**：生成式策略（如扩散策略）虽然能产生合理的动作，但往往缺乏对“当前动作是否有效推进任务”的评估，导致执行效率低下或陷入无效循环。
*   **整体含义**：论文旨在通过引入一个可微的进度估计器，引导扩散策略向“进度最大化”的方向生成动作，从而提升任务成功率和执行效率。

### 2. 方法论：核心思想与关键技术
ProgressVLA 框架由三个核心组件构成：
*   **进度估计器（Progress Estimator）**：
    *   输入语言指令 $l$、初始观察 $o_0$ 和当前观察 $o_t$，输出归一化的进度得分 $p \in [0, 1]$。
    *   利用大规模机器人数据集（OXE）进行预训练，将归一化的时间步作为进度的代理标签。
*   **动作导向的世界模型（World Model）**：
    *   包含逆动力学编码器（将状态转移映射到潜在动作空间）和前向动力学解码器（根据当前状态和潜在动作预测未来视觉特征）。
    *   其作用是建立动作与未来状态之间的可微关联，使得进度得分可以对动作求导。
*   **进度引导的扩散策略（Progress-Guided Diffusion）**：
    *   在扩散模型的去噪过程中，利用分类器引导（Classifier Guidance）技术。
    *   **公式逻辑**：通过反向传播进度估计器的梯度 $\nabla_{x_\tau} \hat{p}_{t+N}$，在每一个去噪步中修正潜在动作，使其向预测进度更高的方向偏移。
*   **强化学习微调（RL Finetuning）**：
    *   利用在线收集的轨迹，通过单调性损失（Monotonicity Loss）约束进度估计器，并通过 KL 正则化改进策略，增强模型对执行噪声的鲁棒性。

### 3. 实验设计
*   **数据集与基准测试**：
    *   **CALVIN**：模拟长程操控环境，采用 ABC $\to$ D 泛化评估协议（在环境 A、B、C 训练，在 D 测试）。
    *   **LIBERO**：包含 LIBERO-Spatial, Object, Goal, Long 四个子集，重点测试多任务和长程交互。
    *   **真实世界机器人**：使用 ARX AC-One 双臂机器人，设计了“捡桃子”、“开抽屉”、“叠碗”等 5 个任务。
*   **对比方法**：
    *   包括 RoboFlamingo, GR-1, 3D Diffuser, OpenVLA, Octo, Dita 等 SOTA（先进）模型。
    *   设置了多个消融变体：无引导（w/o CG）、无预训练引导、全功能版本（Full）。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H20 GPU**。
*   **训练细节**：
    *   预训练阶段：Batch Size 为 2048。
    *   进度估计器预训练耗时约 **400 H20-hours**。
    *   联合微调与蒸馏耗时约 **160 H20-hours**。
    *   基础学习率设定为 $1 \times 10^{-4}$。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **仿真实验**：在 CALVIN 上进行了 1000 条唯一序列的测试；在 LIBERO 的四个子集上进行了全面评估。
    *   **真实世界**：每个任务进行 20 次试验，共计 100 次真实机器人测试。
    *   **泛化性测试**：专门针对进度估计器在“光照变化”和“新物体”场景下的表现进行了定量分析。
*   **充分性评价**：实验设计较为充分且客观。不仅对比了成功率，还引入了“平均完成步数”和“末端执行器路径长度”等效率指标，证明了进度引导不仅能做对，还能做得更快。

### 6. 主要结论与发现
*   **性能提升**：在 CALVIN 基准上，ProgressVLA 在连续完成 5 个子任务的成功率上显著超过基线。
*   **效率优化**：进度引导显著减少了完成任务所需的动作步数（例如在真实机器人实验中，平均步数从 100.8 降至 53.3）。
*   **泛化能力**：预训练的进度估计器表现出极强的零样本泛化能力，能够处理光照变化和未见过的物体，这对于真实场景部署至关重要。
*   **终止判定**：进度得分提供了一个自然的、基于阈值的任务终止准则，比手工规则更鲁棒。

### 7. 优点
*   **创新的引导机制**：将进度估计与扩散策略结合，通过可微世界模型实现闭环的动作优化。
*   **鲁棒的进度感知**：通过大规模预训练解决了从原始像素直接学习进度信号难的问题。
*   **高效的推理**：引入了两阶段推理（Latent warm-start），平衡了生成质量与计算开销。

### 8. 不足与局限
*   **潜在的累积误差**：世界模型在长程预测时可能存在特征漂移，影响梯度引导的准确性。
*   **计算开销**：虽然优化了流程，但扩散模型本身的多步迭代加上梯度回传，在实时性要求极高的场景下可能仍有压力。
*   **任务依赖性**：进度估计器仍依赖于专家演示数据的质量，若演示数据本身非最优，学习到的进度信号可能存在偏差。

（完）
