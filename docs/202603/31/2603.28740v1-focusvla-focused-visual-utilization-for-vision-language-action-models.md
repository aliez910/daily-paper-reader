---
title: "FocusVLA: Focused Visual Utilization for Vision-Language-Action Models"
title_zh: FocusVLA：视觉-语言-动作模型的聚焦视觉利用
authors: "Yichi Zhang, Weihao Yuan, Yizhuo Zhang, Xidong Zhang, Jia Wan"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28740v1"
tags: ["query:rob-il"]
score: 10.0
evidence: 研究VLA模型中用于动作生成的视觉表示
tldr: FocusVLA 针对视觉语言动作（VLA）模型中存在的架构偏差、视觉 Token 过多及任务无关噪声等瓶颈，提出了一种增强视觉利用率的新范式。通过模态级联注意力和聚焦注意力机制，该模型能够动态筛选任务相关的视觉细节并抑制噪声。实验证明，FocusVLA 在模拟和真实机器人任务中显著提升了灵巧操作性能和收敛速度。
selection_source: fresh_fetch
motivation: 现有 VLA 模型因架构设计缺陷和视觉信息冗余，导致模型难以精准聚焦于对动作生成至关重要的视觉细节。
method: 引入模态级联注意力以消除学习捷径，并结合聚焦注意力机制动态选择关键视觉区域并抑制无关噪声。
result: 实验结果显示，FocusVLA 在多种机器人基准测试中均表现出更强的操作能力和更快的训练收敛速度。
conclusion: 研究表明，优化视觉信息的利用方式而非单纯提升视觉表示质量，是提升 VLA 模型性能的关键。
---

## 摘要
视觉-语言-动作（VLA）模型通过将策略建立在丰富的视觉-语言信息之上来改进动作生成。然而，当前的自回归策略受限于三个瓶颈：（1）架构偏差导致模型忽视视觉细节；（2）过多的视觉标记（tokens）使得注意力难以聚焦在正确的区域；（3）与任务无关的视觉信息引入了大量噪声——这些因素共同严重损害了动作质量。在本文中，我们研究了如何有效地利用不同的视觉表示进行动作生成。为此，我们首先通过实验验证了上述问题，并表明 VLA 的性能主要受限于视觉信息的利用方式，而非视觉表示的质量。基于这些见解，我们提出了 FocusVLA，这是一种引导模型注意力转向任务相关视觉区域的新型范式，从而有效地连接视觉与动作。具体而言，我们首先提出了模态级联注意力（Modality Cascaded Attention）以消除捷径路径，从而迫使 VLA 模型依赖任务相关的视觉细节进行动作生成。此外，我们提出了聚焦注意力（Focus Attention），它动态选择任务相关的视觉补丁（patches）以控制信息量，同时显式调节其影响力以抑制任务无关的噪声。在模拟和真实世界机器人基准测试上的广泛实验表明，FocusVLA 不仅能有效利用视觉细节执行灵巧操作，还能在各种任务中显著提高性能并加速收敛。

## Abstract
Vision-Language-Action (VLA) models improve action generation by conditioning policies on rich vision-language information. However, current auto-regressive policies are constrained by three bottlenecks: (1) architectural bias drives models to overlook visual details, (2) an excessive number of visual tokens makes attention difficult to focus on the correct regions, and (3) task-irrelevant visual information introduces substantial noise - together severely impairing the quality of action. In this paper, we investigate how to effectively utilize different visual representations for action generation. To this end, we first empirically validate the above issues and show that VLA performance is primarily limited by how visual information is utilized, rather than by the quality of visual representations. Based on these insights, we introduce FocusVLA, a novel paradigm that directs the model's attention to task-relevant visual regions to effectively bridge vision to action. Specifically, we first propose Modality Cascaded Attention to eliminate shortcut pathways, thereby compelling VLA models to rely on task-relevant visual details for action generation. Furthermore, we propose Focus Attention, which dynamically selects task-relevant visual patches to control information quantity while explicitly modulating their influence to suppress task-irrelevant noise. Extensive experiments on both simulated and real-world robotic benchmarks demonstrate that FocusVLA not only effectively leverages visual details to perform dexterous manipulations, but also substantially improves performance and accelerates convergence across a variety of tasks.

---

## 论文详细总结（自动生成）

以下是对论文《FocusVLA: Focused Visual Utilization for Vision-Language-Action Models》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的自回归视觉-语言-动作（VLA）模型在处理精细操作任务时表现不佳，作者认为瓶颈不在于视觉特征的“质量”，而在于模型对视觉信息的“利用效率”。
*   **研究背景**：当前的 VLA 模型存在三个主要缺陷：
    1.  **架构偏差（Architectural Bias）**：混合注意力机制允许模型通过学习捷径（如仅依赖动作查询 Action Queries）而绕过具体的视觉细节。
    2.  **信息过载（Information Overload）**：过多的视觉 Token 分散了模型的注意力，使其难以锁定关键操作区域。
    3.  **任务无关噪声（Task-Irrelevant Noise）**：背景等无关信息引入了大量噪声，降低了信噪比，干扰了动作生成的准确性。

### 2. 方法论
FocusVLA 提出了一种引导模型关注任务相关视觉区域的新范式，核心技术包括：
*   **模态级联注意力（Modality Cascaded Attention）**：
    *   **核心思想**：将原本混合在一起的多种模态（动作潜变量、动作查询、视觉特征）进行解耦。
    *   **流程**：动作潜变量依次与自身（Self-Attn）、动作查询（Cross-Attn）和视觉特征（Focus-Attn）进行交互，最后通过融合 MLP 整合。这种设计消除了结构性捷径，迫使模型必须从视觉特征中提取关键细节。
*   **聚焦注意力（Focus Attention）**：
    *   **补丁级聚焦（Patch-level Focus）**：基于交叉注意力分数进行 Top-K 筛选，剔除冗余和无关的视觉补丁，仅保留最相关的 Token（如机械臂末端和目标物体区域）。
    *   **通道级聚焦（Channel-level Focus）**：引入元素级门控机制（Element-wise Gate），动态抑制特征通道中的背景噪声，增强指令遵循能力。
*   **特征融合**：结合了 VLM 的高层语义特征和原始视觉骨干（DINOv2+SigLIP）的底层空间细节，实现互补。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个任务套件，评估模型的空间感知、物体识别、目标达成和长程推理能力。
    *   **RoboTwin 2.0**：双臂协作基准测试，包含 Easy（域内）和 Hard（域随机化）设置。
    *   **真实世界机器人**：在 Realman 平台上进行，涉及抓取水果、叠杯子、放置方块等任务，测试背景、空间和目标的变体。
*   **对比方法**：包括 OpenVLA、OpenVLA-OFT、UniVLA、Spatial Forcing、VLA-Adapter-Pro、Pi0、NORA-1.5、EVO-1、DP 等 SOTA 模型。

### 4. 资源与算力
*   **GPU 型号与数量**：
    *   **LIBERO 训练**：使用 4 台 NVIDIA A100 GPU。
    *   **RoboTwin 训练**：使用 8 台 NVIDIA A100 GPU。
*   **训练时长/步数**：
    *   LIBERO 单权重模型训练 100k 步；多权重模型根据任务套件不同训练 10k 至 80k 步不等。
    *   RoboTwin Easy 任务 20k 步，Hard 任务 100k 步。
*   **参数量**：模型基于 Qwen2.5-0.5B，总可训练参数约为 342.7M。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了模拟环境（LIBERO 的 40 个任务，RoboTwin 的多种双臂任务）和真实世界测试（100 次试验）。
*   **消融实验**：非常充分。作者针对注意力模式（混合 vs 级联）、Token 数量（128 到 512）、门控粒度（单参数 vs 元素级）以及视觉表示类型（VLM、DS、VGGT）进行了详尽的对比。
*   **客观性**：通过可视化注意力图（Attention Maps）直观展示了模型聚焦点的变化，并对比了不同视觉骨干在相同架构下的表现，证明了结论的普适性。

### 6. 主要结论与发现
*   **利用胜过表示**：VLA 的性能瓶颈主要在于视觉信息的利用方式，而非视觉编码器的质量。只要利用得当，多种视觉表示都能取得好结果。
*   **聚焦是关键**：通过级联注意力和 Token 剪枝，模型注意力从分散变得集中，显著提升了操作精度。
*   **训练效率提升**：FocusVLA 相比 VLA-Adapter 实现了平均 1.5 倍的收敛加速，在特定任务（LIBERO-Spatial）上甚至达到了 5 倍加速。
*   **SOTA 性能**：在 LIBERO 平均成功率达到 98.7%，在 RoboTwin 的复杂任务（如叠方块、挂杯子）中大幅领先现有模型。

### 7. 优点
*   **架构创新**：通过简单的级联设计解决了复杂的结构性偏差问题，逻辑清晰且有效。
*   **高效性**：在保持轻量级参数（0.5B）的同时，性能超越了许多 7B 规模的大模型。
*   **可视化支撑**：提供了丰富的注意力图对比，有力地证明了方法论中“聚焦”的实际效果。

### 8. 不足与局限
*   **VLM 内部利用**：目前的研究主要集中在策略（Policy）端的视觉利用，VLM 内部的视觉处理仍有优化空间。
*   **规模化验证**：尚未在更大规模的数据集或更大参数量的模型上进行验证。
*   **真实世界鲁棒性**：尽管表现优于基准，但在极端环境变化（如背景颜色和纹理同时剧变）下，模型仍存在性能下降的风险。

（完）
