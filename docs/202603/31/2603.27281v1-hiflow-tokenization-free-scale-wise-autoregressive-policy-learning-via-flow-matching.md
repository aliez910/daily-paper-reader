---
title: "HiFlow: Tokenization-Free Scale-Wise Autoregressive Policy Learning via Flow Matching"
title_zh: HiFlow：基于流匹配的免分词跨尺度自回归策略学习
authors: "Daichi Yashima, Koki Seno, Shuhei Kurita, Yusuke Oda, Komei Sugiura"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27281v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 通过流匹配进行机器人动作的视觉运动策略学习
tldr: HiFlow 提出了一种无需分词的、基于流匹配的从粗到细自回归策略学习框架。针对机器人动作低维连续的特性，该方法摒弃了传统离散分词器带来的量化误差和复杂多阶段训练，通过时间池化构建多尺度连续动作目标，实现了端到端的单阶段训练。实验证明其在模拟和真实环境中的表现均优于现有的扩散模型和基于分词的自回归策略。
selection_source: fresh_fetch
motivation: 现有的从粗到细自回归策略依赖离散分词器，这在处理低维连续机器人动作时会引入不必要的量化误差并增加训练复杂度。
method: 提出 HiFlow 框架，通过时间池化生成多尺度连续动作目标，并利用流匹配技术直接对原始连续动作进行端到端的自回归建模。
result: 在 MimicGen、RoboTwin 2.0 及真实世界环境中的实验结果显示，HiFlow 的性能一致优于扩散模型和基于分词的自回归策略。
conclusion: HiFlow 证明了在机器人策略学习中，直接对连续动作进行多尺度自回归建模比使用离散分词器更高效且性能更强。
---

## 摘要
由粗到细的自回归建模最近在视觉运动策略学习中展现出强大的潜力，它结合了自回归方法的推理效率与基于扩散策略的全局轨迹连贯性。然而，现有方法依赖于离散动作分词器，将连续动作序列映射到码本索引，这种设计继承自图像生成领域，在处理高维像素数据时，学习压缩是必要的。我们观察到机器人动作本质上是低维连续向量，对此类数据进行分词会引入不必要的量化误差和多阶段训练流程。在这项工作中，我们提出了分层流策略（HiFlow），这是一种无需分词的由粗到细自回归策略，直接作用于原始连续动作。HiFlow 通过简单的时域池化，从每个动作块中构建多尺度连续动作目标。具体而言，它对连续的动作窗口取平均值以生成粗略摘要，并在更细的时域分辨率下进行细化。整个模型在单阶段内进行端到端训练，消除了对独立分词器的需求。在 MimicGen、RoboTwin 2.0 和真实环境中的实验表明，HiFlow 一贯优于现有方法，包括基于扩散和基于分词的自回归策略。

## Abstract
Coarse-to-fine autoregressive modeling has recently shown strong promise for visuomotor policy learning, combining the inference efficiency of autoregressive methods with the global trajectory coherence of diffusion-based policies. However, existing approaches rely on discrete action tokenizers that map continuous action sequences to codebook indices, a design inherited from image generation where learned compression is necessary for high-dimensional pixel data. We observe that robot actions are inherently low-dimensional continuous vectors, for which such tokenization introduces unnecessary quantization error and a multi-stage training pipeline. In this work, we propose Hierarchical Flow Policy (HiFlow), a tokenization-free coarse-to-fine autoregressive policy that operates directly on raw continuous actions. HiFlow constructs multi-scale continuous action targets from each action chunk via simple temporal pooling. Specifically, it averages contiguous action windows to produce coarse summaries that are refined at finer temporal resolutions. The entire model is trained end-to-end in a single stage, eliminating the need for a separate tokenizer. Experiments on MimicGen, RoboTwin 2.0, and real-world environments demonstrate that HiFlow consistently outperforms existing methods including diffusion-based and tokenization-based autoregressive policies.

---

## 论文详细总结（自动生成）

### 论文结构化总结：HiFlow

#### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了机器人视觉运动策略（Visuomotor Policy）学习中的生成模型范式。现有的方法主要分为两类：**自回归模型（AM）**推理效率高但缺乏全局连贯性；**扩散模型（DM）**全局连贯性强但推理延迟高。
近期研究（如 CARP）尝试通过“由粗到细”的跨尺度自回归建模来结合两者优点，但这些方法大多借鉴图像生成领域，使用 **VQ-VAE 离散分词器**将连续动作映射为离散索引。作者指出，机器人动作是低维连续向量（通常 7-14 维），使用离散分词会引入不必要的**量化误差**，且导致**多阶段训练**流程复杂。HiFlow 的动机在于实现一种无需分词、直接在连续空间内进行多尺度建模的端到端策略。

#### 2. 方法论：核心思想与关键技术
HiFlow 提出了一个**免分词（Tokenization-Free）**的框架，核心思想是利用**流匹配（Flow Matching）**在连续动作空间内实现从粗到细的逐步细化。

*   **多尺度动作表示**：不使用学习的分词器，而是通过简单的**时间池化（Temporal Averaging）**构建目标。将 $T$ 步动作块按不同粒度（如 1, 2, 4, 8 步）取平均值，形成从全局摘要到局部细节的连续目标序列。
*   **ScaleAR Transformer**：一个跨尺度的自回归 Transformer。它接收视觉、状态和任务特征，并根据粗尺度动作预测细尺度的条件特征。通过因果掩码确保细尺度只能观察到粗尺度信息。
*   **ActionFlowNet（流匹配模块）**：这是一个轻量级的共享 Transformer。它接收高斯噪声、流时间步和 ScaleAR 提供的条件特征，学习一个速度场（Velocity Field），将噪声平滑地传输到目标连续动作分布。
*   **单阶段训练**：整个模型（视觉编码器 + ScaleAR + ActionFlowNet）使用流匹配的均方误差（MSE）损失进行端到端训练，消除了预训练分词器的步骤。

#### 3. 实验设计
论文在三个具有代表性的基准上进行了评估：
*   **MimicGen**：单臂多任务操作（8 个任务，如 Hammer, Threading 等），包含 1K-10K 演示数据。对比方法：TCD、SDP、CARP。
*   **RoboTwin 2.0**：双臂协同操作（3 个任务，如 Click Alarmclock, Place Can），评估高维动作空间的扩展性。对比方法：ACT、Diffusion Policy (DP)。
*   **真实世界实验**：使用 Toyota HSR 移动机器人完成 5 类任务（抓取、重定位、精准放置）。对比方法：ACT、DP、CARP。

#### 4. 资源与算力
*   **训练算力**：使用了 **8 台 NVIDIA H200 SXM GPU** (每张显卡 141GB VRAM)。
*   **推理算力**：在单张 **GeForce RTX 4090** (24GB VRAM) 上运行。
*   **训练参数**：Batch size 为 128，使用 AdamW 优化器，学习率为 $1 \times 10^{-4}$。
*   **推理步数**：流匹配采样步数 $N_{steps} = 25$。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从仿真到现实、从单臂到双臂、从简单抓取到高精度插拔（Threading）的多种场景，实验设置较为全面。
*   **消融实验**：作者针对“时间下采样尺度配置”进行了敏感性分析（对比了 {1,8}, {1,4,8}, {1,2,4,8} 等不同组合），验证了四尺度配置的优越性。
*   **客观性**：对比基准涵盖了当前主流的扩散策略（DP）、自回归策略（ACT）和最新的跨尺度策略（CARP），且在 MimicGen 等标准数据集上遵循了严格的成功率统计标准。

#### 6. 主要结论与发现
*   **性能领先**：HiFlow 在所有基准测试中均优于现有方法。在 MimicGen 上平均成功率达到 88%（超过 CARP 的 85%）；在复杂的双臂任务（Place Can Basket）中，成功率比 Diffusion Policy 翻了一倍（39% vs 18%）。
*   **精度优势**：在需要极高精度的任务（如 Threading 和实机精准放置）中，HiFlow 表现尤为突出，证明了连续空间建模比离散分词更能捕捉细微动作。
*   **多尺度有效性**：可视化结果证实，模型确实遵循了从“全局运动趋势”到“局部轨迹细节”的生成逻辑。

#### 7. 优点（亮点）
*   **简化流程**：摒弃了复杂的 VQ-VAE 训练，实现了真正的端到端单阶段学习。
*   **消除量化误差**：直接在连续空间操作，保留了动作的欧几里得几何特性，对精细操作更友好。
*   **架构通用性**：ActionFlowNet 在不同尺度间共享参数，模型轻量且高效。
*   **实机表现稳健**：在存在感知噪声和物理接触的真实环境中，生成的轨迹比基准方法更平滑、连贯。

#### 8. 不足与局限
*   **推理开销**：虽然比标准扩散模型步数少，但由于是跨尺度自回归，总的前向传播次数为 $|S| \times (1 + N_{steps})$，在实时性要求极高的场景下可能仍有压力。
*   **尺度依赖性**：虽然对尺度配置有一定鲁棒性，但如果下采样过于激进（如直接从 1 步跳到 16 步），性能会有所下降，需要针对不同频率的动作序列微调尺度参数。
*   **长程任务**：实验主要集中在短时域（Action Chunking）内的生成，对于需要极长时序依赖的任务，其自回归压力可能会增大。

（完）
