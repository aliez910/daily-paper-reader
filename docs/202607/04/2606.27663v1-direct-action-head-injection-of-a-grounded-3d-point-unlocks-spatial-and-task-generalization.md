---
title: Direct Action-Head Injection of A Grounded 3D Point Unlocks Spatial and Task Generalization
title_zh: 接地3D点的直接动作头注入解锁空间与任务泛化
authors: "Shiang-Feng Tsai, Jin-Cheng Jhang, Yen-Ling Tai, Jia-Hong Lai, Shih-Yun Wong, KangTung-Hsu, Yi-Ting Chen"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.27663v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 直接将局部3D点注入VLA动作头以实现空间和任务泛化
tldr: 现有VLA模型在空间和任务泛化上脆弱，2D表示不足。本文提出轻量级模块，利用3D点表示地面真值，计算与夹爪的相对位移并通过自适应层归一化直接注入动作头。在LIBERO-PRO基准上，GR00T-N1.6在任务扰动下成功率提升46.3个百分点（31.2→77.5），位置扰动提升32.1个百分点（28.1→60.2）。结果表明，3D点直接注入动作头是提升泛化的关键，且模块通用轻量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27663v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 665, \"height\": 878}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1439, \"height\": 629}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1233, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 706, \"height\": 420}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 777, \"height\": 637}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 733, \"height\": 484}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 731, \"height\": 407}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1449, \"height\": 1260}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1440, \"height\": 1611}, {\"url\": \"assets/figures/arxiv/2606.27663v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1445, \"height\": 1270}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27663v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1301, \"height\": 594}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1083, \"height\": 363}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 489, \"height\": 236}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 491, \"height\": 162}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1213, \"height\": 1857}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1080, \"height\": 367}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1083, \"height\": 515}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1438, \"height\": 222}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1439, \"height\": 223}, {\"url\": \"assets/tables/arxiv/2606.27663v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1435, \"height\": 222}]"
motivation: 现有VLA模型在测试时对位置和指令变化泛化弱，2D信号不足以提供鲁棒引导。
method: 提出轻量模块，利用3D点表示地面真值，计算相对位移后通过自适应层归一化直接注入动作头。
result: "在LIBERO-PRO上，GR00T-N1.6任务扰动成功率从31.2%升至77.5%，位置扰动从28.1%升至60.2%。"
conclusion: 将3D点地面真值注入动作头是提升VLA泛化的关键，且方法轻量、架构无关。
---

## 摘要
视觉-语言-动作（VLA）模型利用大规模视觉-语言预训练实现灵活的机器人操作，但在测试时，它们在两个维度上仍然脆弱：当物体位置与训练时不同时（空间泛化），以及当熟悉场景与训练时不同的语言指令配对时（任务泛化）。越来越多的方法通过赋予策略空间和任务感知信息（如用于物体定位和放置的2D像素坐标）来解决这一脆弱性。然而，我们发现通过语言提示或视觉提示的现有表示并未解决这些局限性；相反，利用基于3D点的表示并将其直接馈送到动作头带来了实质性的改进——表明接地信号如何表示并注入VLA才是真正的游戏规则改变者。因此，我们提出了一个轻量级、模型无关的模块，该模块以3D形式表示接地信号，计算其相对于夹爪的位移，并通过自适应层归一化将得到的空间嵌入直接注入动作头。整个模块是一个两层MLP，无需对VLA骨干网络或预训练流水线进行任何更改。在LIBERO-PRO上，我们的方法在任务扰动下将GR00T-N1.6的平均成功率从31.2提高到77.5个百分点，在位置扰动下从28.1提高到60.2个百分点（分别提升46.3和32.1个百分点）。对于$π_{0.5}$也获得了类似的提升，表明该机制与骨干网络无关。这些结果共同支持了我们的核心发现：当充分的接地信号被提升到3D时，将其直接注入动作头即可解锁VLA在空间和任务两个维度上的泛化能力——仅需在预训练骨干网络之上添加一个轻量级模块即可实现。

## Abstract
Vision-Language-Action (VLA) models leverage large-scale vision-language pretraining for flexible robot manipulation, yet at test time they remain brittle along two axes: spatial generalization, when object positions differ from those seen during training, and task generalization, when a familiar scene is paired with a different language instruction than the one seen in training. A growing family of methods addresses this brittleness by endowing a policy with the spatial and task-aware information such as 2D pixel-coordinate for object localization and placement. However, we find that existing representation through language prompting or visual prompting does not address the limitations; in contrast, exploiting a 3D point-based representation and feeding it directly to the action head leads to substantial improvements-revealing that how the grounding signal is represented and injected into the VLA is the true game changer. Thus, we propose a lightweight, model-agnostic module that represents the grounding signal in 3D, computes its relative displacement to the gripper, and injects the resulting spatial embedding directly into the action head through adaptive layer normalization. The entire module is a two-layer MLP that requires no changes to the VLA backbone or pretraining pipeline. On LIBERO-PRO, our method improves the average success rate of GR00T-N1.6 from 31.2 to 77.5 points under task perturbation and from 28.1 to 60.2 points under position perturbation (gains of 46.3 and 32.1 points). Comparable gains are achieved for $π_{0.5}$ as well, demonstrating that the mechanism is backbone-agnostic. Together, these results support our central finding: given adequate grounding lifted into 3D, injecting it directly into the action head is what unlocks both spatial and task generalization in VLAs-achievable with nothing more than a lightweight module on top of a pretrained backbone.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义
- **研究动机**：现有视觉‑语言‑动作（VLA）模型虽然通过大规模预训练获得了丰富的语义和语言理解能力，但在测试时仍然表现出两个维度的脆弱性：  
  - **空间泛化**（spatial generalization）：物体位置与训练时不同时，策略容易失败；  
  - **任务泛化**（task generalization）：场景不变但指令变为新任务时，策略倾向于“记忆”训练轨迹而非按指令执行。  
- **背景**：已有许多工作尝试通过提供显式的空间接地信号（如目标物体的2D像素坐标）来提升泛化能力，但这些信号通常通过语言提示、视觉提示或链式推理等方式注入策略。  
- **整体含义**：本文指出，关键瓶颈不在于信号来源，而在于 **如何表示和注入** 接地信号。作者发现：将2D点通过深度提升为3D坐标，并直接注入到动作头（action head）中，可以同时大幅提升空间和任务泛化。这个简单、模型无关的机制带来了显著的效果改善。

## 2. 论文提出的方法论
- **核心思想**：利用现成的2D接地信号（例如来自VLM或检测器的目标点），将其通过深度和相机参数**升维到3D空间**，计算与夹爪的相对位移，然后通过一个轻量的两层MLP编码为空间嵌入，最后通过**自适应层归一化（AdaLN）** 直接注入到扩散Transformer（DiT）动作头的条件中。该方法不改变VLA骨干网络或预训练流程，只增加一个2层MLP。
- **关键技术细节**：
  1. **从2D到3D空间表示**：  
     - 给定2D目标点 \((u, v)\) 和对应的深度值 \(z\)，利用相机内参和外参将其投影到机器人基坐标系下的3D点 \(p_t\)。  
     - 从机器人状态获取夹爪的3D位置 \(p_g\)，计算相对位移：  
       \[
       d = p_t - p_g
       \]
  2. **编码空间嵌入**：  
     - 使用两层MLP（隐藏维度和输出维度与动作头隐藏大小匹配，均为1024）将 \(d\) 映射为空间嵌入 \(z_{\text{spatial}}\)。
  3. **注入动作头**：  
     - 动作头原有条件仅包含扩散时间步嵌入 \(z_{\text{time}}\)。本文将其与空间嵌入相加：  
       \[
       \gamma, \beta = \text{Linear}(z_{\text{time}} + z_{\text{spatial}})
       \]  
     - 这些 \(\gamma, \beta\) 作为AdaLN的尺度与偏移参数，调制每个DiT块的归一化特征。  
     - **零初始化技巧**：将MLP第二层权重和偏置初始化为零，使得开始训练时 \(z_{\text{spatial}}=0\)，不干扰预训练的 \(z_{\text{time}}\) 条件，然后逐渐学习。
- **公式/流程**：  
  - \(d = p_t - p_g\)（式1）  
  - \(z_{\text{spatial}} = \text{MLP}(d)\)（式2）  
  - \(\gamma, \beta = \text{Linear}(z_{\text{time}} + z_{\text{spatial}})\)（式3）  
- **算法流程（文字）**：  
  ① 从任意现成视觉接地模块获得2D目标点；  
  ② 用深度图+相机参数将该点提升到3D基坐标系，得到 \(p_t\)；  
  ③ 从机器人状态获取夹爪位置 \(p_g\)，计算 \(d = p_t - p_g\)；  
  ④ 通过2层MLP编码为空间嵌入 \(z_{\text{spatial}}\)；  
  ⑤ 在VLA的动作头中，将 \(z_{\text{spatial}}\) 与时间步嵌入相加后回归AdaLN参数，从而条件化动作生成。

## 3. 实验设计
- **数据集/场景**：
  - **仿真**：LIBERO（LIBERO‑Object、LIBERO‑Spatial）及LIBERO‑PRO（在原任务上施加任务扰动和位置扰动）。训练集从原始的20个任务扩展到69个（增加49个来自LIBERO‑90的pick‑and‑place任务）。  
  - **真实世界**：Franka Emika Panda机器人，配备两个RealSense D435相机（前视RGB‑D、腕装RGB）；8个pick‑and‑place任务，每个任务40条演示。
- **Benchmark**：  
  - 仿真主benchmark为**LIBERO‑PRO**，包含两种扰动：  
    - **Task Perturbation**：指令中的目标/放置区被同场景中的其他物体替换。  
    - **Position Perturbation**：物体初始位置改变。  
  - 同时报告LIBERO原任务（In‑Distribution）的成功率。
- **对比方法**：  
  - 开源VLA基线：OpenVLA、π0、π0.5、GR00T‑N1.6、MolmoAct。  
  - 不同接地表示与注入方式的变体（在GR00T‑N1.6上控制对比）：  
    - 2D via Text Prompt：坐标直接追加到语言提示。  
    - 2D via Visual Prompt：在图像上叠加十字标记。  
    - 2D via AdaLN：用2D坐标+2D位移经MLP通过AdaLN注入。  
    - 3D via Text Prompt：3D坐标追加到语言提示。  
    - 3D via AdaLN（本文方法）。  
  - 真实实验中额外对比了2D Text/Visual Prompt变体。

## 4. 资源与算力
- **文中未明确给出GPU型号、数量和总训练时长**。  
- 仅在附录C.3列出了训练超参数（如batch size、优化器、学习率、训练步数）：
  - GR00T‑N1.6：全局batch size=384，20,000步，AdamW，峰值学习率1×10⁻⁴。  
  - π0.5：全局batch size=256，30,000步，峰值学习率5×10⁻⁵。  
- 真实实验使用GR00T‑N1.6，batch size=256，20,000步。  
- 没有提及使用的GPU型号或数量，这是论文的一个遗漏。

## 5. 实验数量与充分性
- **实验丰富性**：  
  - 在LIBERO‑PRO上评测了两个骨干（GR00T‑N1.6和π0.5）及其带有本方法的版本（表1）。  
  - 专门设计了表示与注入的交叉消融（表2），使用完全相同的地面真值2D点来控制变量。  
  - 探究训练数据规模影响（图3）：20任务 vs 69任务，本文方法在20任务上即超越其他所有方法在69任务上的表现。  
  - 深度噪声鲁棒性实验（图4）：分别训练/测试时加入σ=0.01/0.03m的高斯噪声。  
  - 消融空间嵌入输入成分（pt、pg、d的组合，表3）。  
  - 消融注入机制（AdaLN vs. 通过机器人状态token，表4）。  
  - 真实世界实验（图5）：8个任务，每项10次rolling，有In‑distribution、Task Perturbation、Position Perturbation三种评估条件。
- **充分性与公平性**：  
  - 仿真实验中每任务执行50次rolling，足够统计可靠。  
  - 对比方法均在相同数据上训练后评测（除了OpenVLA、π0等使用已有权重，但已说明公平）。  
  - 控制变量严谨：表示与注入的对比中所有变体使用相同的oracle 2D点，唯一区别是表示维度与注入路径。  
  - 使用两个不同骨干证明机制“模型无关”。  
  - 不足：实验主要集中在pick‑and‑place任务，未覆盖其他操作类型（如推动、抓取+放置之外的复合任务），但已在Limitation中说明。总体来说，实验数量充分、设计客观。

## 6. 论文的主要结论与发现
- **核心结论**：在获得足够准确的2D接地信号后，**将其提升为3D并直接注入动作头**是解锁VLA空间泛化和任务泛化的关键。具体而言：  
  - 相比于通过文本提示或视觉提示传递2D坐标，3D相对位移通过AdaLN注入带来的提升最大。  
  - 在GR00T‑N1.6上，LIBERO‑PRO的任务扰动平均成功率从31.2%提升到77.5%（+46.3%），位置扰动从28.1%提升到60.2%（+32.1%）。  
  - 在π0.5上同样效果显著（任务扰动37.3%→75.9%；位置扰动47.0%→72.2%）。  
  - 真实世界中，2D变体接近零成功率，而本文方法在两种扰动下仍保持>60%的成功率。
- **其他发现**：  
  - 2D信号不管如何注入（文本、视觉、AdaLN）都远不如3D表示，因为动作空间是3D的，2D强制策略内部学习投影映射。  
  - 仅是3D表示还不够，必须直接注入动作头（通过AdaLN）而非经过语言提示（文本中3D坐标会被VLM骨干混淆）。  
  - 更好的“设计选择”比单纯扩大训练数据更有效（本文方法用20任务超越其他方法用69任务）。

## 7. 优点
- **简单高效**：整体模块只有2层MLP，无需修改VLA骨干，无需重新设计预训练目标。  
- **模型无关**：在π0.5和GR00T‑N1.6上均取得一致提升，可推广到其他使用DiT动作头的VLA。  
- **实用性强**：在真实世界中使用消费级RGB‑D传感器和现成VLM作为接地源，仍获得有效泛化。  
- **设计巧妙**：零初始化MLP第二层，使得空间嵌入从零开始逐渐学习，不破坏预训练的扩散时间步条件。  
- **一石二鸟**：同时提升了空间泛化和任务泛化，且保持了分布内性能。  
- **实验规范**：控制了接地来源（所有变体用同一2D点），排除了接地质量差异的干扰，聚焦于表示和注入方式的影响。

## 8. 不足与局限
- **应用范围**：  
  - 方法假设每个子目标可以关联到一个具体的目标物体或区域，无法处理与物体无关的运动（如挥手、推动空区域），作者提到可通过将2D轨迹升维来扩展，但未验证。  
  - 实验仅在单臂机器人上进行，未推广到双臂或人形机器人。  
- **接地依赖**：  
  - 方法依赖于外部2D接地信号的质量（在仿真中使用oracle点，真实中使用VLM），在复杂场景或模糊指令下接地失败会影响整体性能。该问题与本文贡献正交，但仍需注意。  
- **任务类型限制**：  
  - 所有实验都是pick‑and‑place类型的任务，其他操纵类型（如推、旋转、开柜门等）没有测试，泛化性尚不清楚。  
- **深度噪声处理**：  
  - 虽然证明了抗噪能力，但在训练时加入深度噪声会带来一定性能下降（相比清洁深度）；在实际部署中，深度传感器的噪声仍可能造成误差。  
- **资源信息缺失**：未报告具体的GPU型号、数量和训练耗时，不利于复现评估。  
- **其他**：  
  - 环境布局相对固定（LIBERO为桌面操作），更开放、杂乱场景下的表现有待探索。  
  - 接地信号的时间一致性（如跨帧抖动）未专门分析。

（完）
