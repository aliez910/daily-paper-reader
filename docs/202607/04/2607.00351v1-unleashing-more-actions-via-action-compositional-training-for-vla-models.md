---
title: Unleashing More Actions via Action Compositional Training for VLA Models
title_zh: 通过动作组合训练释放VLA模型的更多动作
authors: "Kai Peng, Jie Lu, Xiaojiang Peng"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00351v1"
tags: ["query:rob-il"]
score: 10.0
evidence: 通过动作组合训练提升VLA模型在机器人操作中的泛化能力
tldr: 针对VLA模型在机器人操作中因标准训练范式导致严重过拟合，无法泛化到子技能新组合的问题，提出ACT-VLA离线数据增强框架。该框架利用模型潜在任务表示自动合成新颖且物理有效的演示数据，从而扩展训练分布，无需额外数据采集。在仿真操作任务上，使用ACT-VLA合成数据进行训练的政策，其成功率显著高于仅在原始分布上训练的基线模型。该方法为提升VLA模型的泛化能力提供了有效、可扩展且数据高效的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00351v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 893, \"height\": 486}, {\"url\": \"assets/figures/arxiv/2607.00351v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1871, \"height\": 1027}, {\"url\": \"assets/figures/arxiv/2607.00351v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 841, \"height\": 528}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.00351v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1512, \"height\": 318}, {\"url\": \"assets/tables/arxiv/2607.00351v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 928, \"height\": 244}]"
motivation: VLA模型在标准训练下严重过拟合特定行为模式，无法泛化到仅需子技能新组合的场景，且扩大数据集成本高昂，亟需无需额外数据即可提升泛化能力的方法。
method: ACT-VLA离线数据增强框架通过利用模型潜在任务表示自动合成新颖且物理有效的演示数据来扩展训练分布，无需额外数据采集。
result: 在仿真操作任务中，使用ACT-VLA合成数据进行训练的政策，其成功率显著高于仅在原始分布上训练的基线模型。
conclusion: ACT-VLA验证了自动演示合成可有效提升VLA模型的泛化能力，缓解过拟合与数据稀缺问题，为降低数据依赖提供了可扩展途径。
---

## 摘要
视觉-语言-动作模型在机器人操作中表现出色，这得益于演示数据的规模和多样性。然而，标准的训练范式常常导致VLA模型严重过拟合于特定的行为模式，使其无法泛化到分布外场景，即使这些场景仅需相同子技能的新颖组合。虽然扩展数据集可以缓解过拟合，但获取高质量机器人数据仍以劳动密集和成本高昂著称。为了在不依赖昂贵人类遥操作的情况下解决这一僵局，并真正释放更多动作——即让VLA模型将已知子技能组合成远超原始演示的可执行行为集——我们提出ACT-VLA（VLA模型的动作组合训练），这是一种离线数据增强框架，利用模型的潜在任务表征直接从现有任务中合成新颖且物理有效的演示用于策略训练。通过消除额外的手动数据收集，我们的方法自动扩展训练分布并缓解过拟合。我们在仿真中的挑战性操作任务上评估了该方法。实验表明，基线VLA模型因原始分布过拟合而泛化能力差，而使用合成数据训练的策略取得了显著更高的成功率，验证了利用现有任务进行自动演示合成为扩展VLA泛化提供了一条有效、可扩展且数据高效的途径。

## Abstract
Vision-Language-Action models excel at robotic manipulation, driven by the scale and diversity of demonstration data. However, standard training paradigms often cause VLA models to severely overfit to specific behavioral patterns, rendering them unable to generalize to out-of-distribution scenarios even when those scenarios merely require novel combinations of identical sub-skills. While expanding datasets can mitigate this overfitting, acquiring high-quality robot data remains notoriously labor-intensive and cost-prohibitive. To resolve this impasse without expensive human teleoperation and to truly unleash more actions,i.e., enable VLA models to compose known sub-skills into a much broader set of executable behaviors beyond the original demonstrations-we propose ACT-VLA (Action Compositional Training for VLA Models), an offline data augmentation framework that leverages the model's latent task representations to synthesize novel, physically valid demonstrations directly from existing tasks for policy training. By eliminating additional manual data collection, our method automatically expands the training distribution and mitigates overfitting. We evaluate our approach on challenging manipulation tasks in simulation. Experiments demonstrate that while baseline VLA models generalize poorly due to original distribution overfitting, policies trained with our synthesized data achieve substantially higher success rates, validating that leveraging existing tasks for automated demonstration synthesis provides an effective, scalable, and data-efficient route to broadening VLA generalization.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）
- **组合泛化失败**：Vision-Language-Action (VLA) 模型在标准行为克隆训练下严重过拟合训练集中的特定行为模式，导致当任务仅由已学习的子技能重新配对构成时，模型仍无法泛化（例如将“杯子→圆盘”与“方块→方盘”重新配对为“杯子→方盘”和“方块→圆盘”）。
- **数据获取瓶颈**：通过扩大数据集缓解过拟合虽有效，但机器人演示数据依赖遥操作，成本高昂且难以扩展到指数级增长的任务组合空间。
- **核心目标**：提出**无需额外人工数据采集**、仅利用已有演示自动合成新组合轨迹的训练框架，以释放 VLA 模型执行“更多动作”的能力。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：ACT-VLA 是一种离线数据增强框架，将已有的**文本潜在插值（Text Latent Interpolation, TLI）** 从推理时控制手段转化为离线数据生成引擎，自动合成原始数据中缺失的组合任务演示，并用于策略训练。
- **关键技术三要素**：
  - **指令设计**：合成轨迹需对应组合语言指令，通过拆分并重组原始任务指令的文本成分自动生成。
  - **质量控制**：利用物理模拟器进行**双重过滤**：① 任务完成度检查；② 步数预算限制（排除过长或无效轨迹），确保只有物理有效的高质量轨迹进入训练集。
  - **训练动态平衡**：保持原始数据与合成数据在套件级别上的体积一致，避免合成分布过度主导导致灾难性遗忘。
- **实现流程**：
  1. **表示引导的轨迹合成**：对两个基任务的文本潜在表示 \(T_1, T_2\) 进行插值，修改每步文本隐藏状态：  
     \(h_T(i) \leftarrow h_T(i) + (1-\alpha)T_1 + \alpha T_2 - [(1-\alpha)T_2 + \alpha T_1]\)，其中 \(\alpha = i/\lambda\) 线性增加。
  2. **数据记录与处理**：记录多视角 RGB 帧、机器人本体状态、执行动作，以及组合语言指令，格式与原始数据集一致。
  3. **增强策略训练**：使用标准行为克隆损失（同基线 VLA 训练）在原始+合成数据集上训练，架构无任何修改。

## 3. 实验设计：数据集 / 场景、基准、对比方法
- **基准与套件**：使用 **LIBERO** 仿真基准，包含四个标准套件（Spatial, Object, Goal, Long），并构建两个 OOD 套件（**LIBERO-Spatial-OOD** 和 **LIBERO-Goal-OOD**），通过重组物体与目标位置形成训练中未出现的组合。
- **对比方法**：
  - 基线 VLA 模型：UniVLA, OpenVLA-OFT, GR00T-N1, π0.5（均仅在原始数据上训练）。
  - 推理时方法：π0 + TLI（推理时刻意操纵隐空间，不使用合成训练数据）。
- **主实验参数**：所有模型训练 30k 步，每任务评估 3 个随机种子各 50 个 episode（共 150 episode / 任务），报告套件内平均成功率。

## 4. 资源与算力
论文原文**未明确说明**使用的 GPU 型号、数量或训练时长。仅提及所有模型采用“相同训练设置（30k steps）”，但未给出硬件配置信息。

## 5. 实验数量与充分性
- **实验数量**：
  - 主实验结果（表 I）：覆盖 6 个套件，对比 4 种基线 VLA + 1 种推理时方法。
  - 消融研究（表 II）：在 π0 骨干上对比原始数据、推理时 TLI、原始+合成数据，控制骨干不变。
  - 额外讨论：对合成数据规模、质量过滤等设计选择进行了定性分析。
- **充分性与公平性**：
  - 实验覆盖了多个标准套件和两种 OOD 场景，验证了在不同复杂度下的泛化提升。
  - 主实验中 π0.5 作为骨干，消融中 π0 作为骨干，保证对比架构一致。
  - 所有方法均采用相同评估协议，公平性有保障。
  - 局限：所有实验均在**仿真环境**中进行，未在真实机器人上验证，且 OOD 场景仅局限于两技能顺序组合。

## 6. 主要结论与发现
- **ACT-VLA 大幅提升 OOD 泛化**：在 Spatial-OOD 上成功率从 π0.5 的 35.5% 提升至 88.2%（+52.7）；Goal-OOD 从 46.6% 提升至 95.6%（+49.0）。
- **训练时合成优于推理时操纵**：在相同 π0 骨干下，训练时合成比推理时 TLI 在 Spatial-OOD 高 4.9%，在 Goal-OOD 高 5.5%，且消除了推理时的额外计算开销。
- **平均性能最优**：在所有 6 个套件的非加权平均（AVG）中达到 95.6%，超越所有基线（包括推理时 TLI 的 90.4%）。
- **自动合成演示是有效的训练信号**：消融证实仅依赖原始数据几乎无法泛化（OOD 成功率低至 2%~16%），添加合成数据后效果飞跃。

## 7. 优点：方法或实验设计亮点
- **零额外人工成本**：完全通过现有数据自动生成新任务演示，无需遥操作。
- **即插即用**：不改变 VLA 架构或推理流程，可直接应用于现有训练管线。
- **推理时零开销**：组合能力内化至模型权重，无需推理时的隐空间操纵或外部规划器。
- **严格质量控制**：通过模拟器过滤和步数预算确保训练数据物理有效性，避免错误关联。
- **保持分布内性能**：平衡合成数据规模，未损害原始任务表现。

## 8. 不足与局限
- **组合复杂度有限**：仅在两技能顺序组合（如物体-目标重配对）上验证，未探索更长技能链或更复杂的组合结构。
- **依赖骨干解耦能力**：合成质量受预训练 VLA 模型语义解耦能力的制约，在更弱骨干上可能效果下降。
- **模拟环境与真实差距**：所有实验在 LIBERO 仿真中进行，未在实体机器人上部署，存在 sim-to-real 泛化风险（虽提及双过滤策略可缓解分布偏移）。
- **算力细节缺失**：未报告训练所需的 GPU 型号、数量及时间，不利于复现与成本评估。

（完）
