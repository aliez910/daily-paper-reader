---
title: "SA-VLA: State-aware tokenizer for improving Vision-Language-Action Models' performance"
title_zh: SA-VLA：用于提升视觉-语言-动作模型性能的状态感知分词器
authors: "Tengyue Jiang, Chunpu Xu, Jiayue Kang, Yao Mu"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30113v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 针对VLA模型的状态感知分词器以提升操纵性能
tldr: 当前离散动作tokenization在VLA策略中广泛应用，但现有方法将每个离散码映射到固定连续动作原型，忽略了机器人当前的本体感受状态如关节配置、物体位姿等，导致复杂操作中动作恢复不准确。为此本文提出SA-VLA，一种状态感知的动作tokenizer，通过交叉注意力机制或轻量级状态适配器将机器人状态注入动作解码过程，使每个离散token能表示一组状态依赖的连续动作，从而扩展代码本有效支持。在12个RoboTwin操作任务上，SA-VLA将平均成功率从0.29提升至0.56；在三个真实世界任务的零样本sim-to-real实验中，平均成功率从0.15提升至0.33。本工作证明了状态条件动作解码是减少离散VLA策略压缩差距的简单有效机制，提升了VLA策略的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.30113v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1449, \"height\": 874}, {\"url\": \"assets/figures/arxiv/2606.30113v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 949, \"height\": 858}, {\"url\": \"assets/figures/arxiv/2606.30113v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 865, \"height\": 522}, {\"url\": \"assets/figures/arxiv/2606.30113v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 862, \"height\": 531}, {\"url\": \"assets/figures/arxiv/2606.30113v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 867, \"height\": 656}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.30113v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1408, \"height\": 647}, {\"url\": \"assets/tables/arxiv/2606.30113v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 445, \"height\": 277}, {\"url\": \"assets/tables/arxiv/2606.30113v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1003, \"height\": 311}, {\"url\": \"assets/tables/arxiv/2606.30113v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 667, \"height\": 490}, {\"url\": \"assets/tables/arxiv/2606.30113v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1207, \"height\": 474}, {\"url\": \"assets/tables/arxiv/2606.30113v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 769, \"height\": 424}, {\"url\": \"assets/tables/arxiv/2606.30113v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 552, \"height\": 258}, {\"url\": \"assets/tables/arxiv/2606.30113v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1000, \"height\": 224}]"
motivation: 现有离散动作tokenizer将每个离散码映射到固定动作原型，忽略机器人当前本体感受状态，导致动作恢复不准确，限制VLA策略在复杂操作中的性能。
method: 提出SA-VLA状态感知动作tokenizer，通过交叉注意力或轻量适配器将机器人状态注入动作解码，使每个离散token表示一组状态依赖的连续动作，扩展代码本支持。
result: 在12个RoboTwin操作任务上平均成功率从0.29提升至0.56；零样本sim-to-real实验上平均成功率从0.15提升至0.33。
conclusion: 状态条件动作解码是减少离散VLA策略压缩差距的简单有效机制，显著提升了VLA策略的性能。
---

## 摘要
离散动作分词化为自回归VLA策略提供了紧凑的接口，但从离散编码中准确恢复连续机器人动作仍然具有挑战性。现有的分词器通常将每个离散编码映射到固定的连续动作原型，忽略了机器人当前的本体感觉状态。这种局限性在操作任务中尤为明显，同一动作标记在不同关节配置、物体姿态和接触条件下可能需要不同的连续控制。因此，我们提出了SA-VLA，一种状态感知的动作分词器，它使动作解码依赖于机器人状态。我们研究了两种用于基于VQ的动作分词的状态注入机制：状态与动作特征之间的交叉注意力，以及一个轻量级状态适配器，该适配器预测动作层面的调制因子，用于状态条件动作调制和重建。适配器公式通过允许每个离散标记表示一族依赖于状态的连续动作，扩展了有限码本的有效支持，同时保持了离散动作建模的效率和兼容性。集成到基于LLM的VLA策略中后，SA-VLA支持自回归和并行动作标记解码，且对模型接口改动最小。在12个RoboTwin操作任务上，SA-VLA将平均成功率从0.29提升至0.56，超过了最强的分词器基线。在三个真实世界任务的零样本仿真到现实实验中，它进一步将平均成功率从0.15提升至0.33，超过了最强的分词器基线。这些结果表明，状态条件动作解码是一种简单且有效的机制，能够缩小离散VLA策略中的压缩差距。

## Abstract
Discrete action tokenization provides a compact interface for autoregressive VLA policies, but accurately recovering continuous robot actions from discrete codes remains challenging. Existing tokenizers typically map each discrete code to a fixed continuous action prototype, ignoring the robot's current proprioceptive state. This limitation is particularly pronounced in manipulation, where the same action token may require different continuous controls under different joint configurations, object poses, and contact conditions. We therefore propose SA-VLA, a state-aware action tokenizer that conditions action decoding on robot state. We study two state-injection mechanisms for VQ-based action tokenization: cross-attention between state and action features, and a lightweight state adapter that predicts action-wise modulation factors for state-conditioned action modulation and reconstruction. The adapter formulation expands the effective support of a finite codebook by allowing each discrete token to represent a family of state-dependent continuous actions, while preserving the efficiency and compatibility of discrete action modeling. Integrated into an LLM-based VLA policy, SA-VLA supports both autoregressive and parallel action-token decoding with minimal changes to the model interface. On 12 RoboTwin manipulation tasks, SA-VLA improves the average success rate from 0.29 to 0.56 over the strongest tokenizer baseline. In zero-shot sim-to-real experiments on three real-world tasks, it further improves average success from 0.15 to 0.33 over the strongest tokenizer baseline. These results demonstrate that state-conditioned action decoding is a simple and effective mechanism for reducing the compression gap in discrete VLA policies.

---

## 论文详细总结（自动生成）

以下是根据给定论文内容生成的结构化中文总结。

## 论文总结：SA-VLA: 面向状态感知的视觉-语言-动作模型分词器

### 1. 核心问题与整体含义（研究动机与背景）

- **问题**：现有离散动作分词器（如 OpenVLA 的 uniform binning、FAST、VQ-VLA 的 residual VQ-VAE）将每个离散 token 映射到固定的连续动作原型，**忽略了机器人当前的本体感觉状态**（如关节角度、末端位姿、与物体的接触状态等）。这种静态映射导致同一离散 token 在不同状态下需要不同的连续控制，**动作恢复精度受限**，成为 VLA 策略性能的瓶颈。
- **意义**：将状态信息纳入动作 tokenizer 的解码过程，能够**扩展有限码本的表达能力**，使每个 token 代表一族状态依赖的连续动作，从而**缩小离散编码的“压缩差距”**，提升 VLA 策略在复杂操作任务上的成功率和泛化能力。

### 2. 方法论

- **核心思想**：在动作 tokenizer 中引入机器人状态条件，使解码过程能够根据当前状态自适应地生成连续动作，而不是使用固定的码本输出。
- **关键技术细节**：
  - 基座模型：VQ-VAE（CNN + Transformer，对连续动作序列进行编码、量化、解码）。
  - 提出两种状态注入机制：
    - **方法 A（Cross-Attention）**：在编码器和解码器的 Transformer 层中，使用状态特征作为 Key 和 Value，与动作特征做交叉注意力，使动作表示感知状态。
    - **方法 B（Lightweight Adapter）**：使用一个轻量 MLP（+ Sigmoid 激活）将状态映射到动作每个维度的**缩放因子 \(w\)**，先对输入动作做 \(a_{\text{trans}} = a / w\)，量化后再乘以 \(w\) 恢复。这种方法将离散码本扩展为连续空间：同一 token 乘以不同状态依赖的缩放因子，产生不同连续动作。
  - 训练损失：VQ-VAE 的标准三项损失（重建、码本、承诺），权重 \(\lambda_1, \lambda_2\)。
- **算法流程**（Method B）：
  1. 输入连续动作序列 \(a\)。
  2. 状态适配器输出缩放因子 \(w\)，计算变换后的动作 \(a_{\text{trans}} = a / w\)。
  3. 标准 VQ-VAE（不含状态交叉注意力）对 \(a_{\text{trans}}\) 进行编码、量化、解码，得到 \(\hat{a}_{\text{trans}}\)。
  4. 最终重建动作 \(\hat{a} = \hat{a}_{\text{trans}} \times w\)。
  5. 优化器最小化重建误差与量化损失。
- **集成到 VLA**：将 image tokens、language tokens、state tokens 与 SA-VLA 输出的 action tokens 串接，使用 LLM（Qwen2.5）进行自回归或并行动作预测。并行动作解码时采用双向注意力和占位符 embedding，可一次性生成整个动作块。

### 3. 实验设计

- **数据集与仿真场景**：
  - 仿真：RoboTwin 平台，12 个操作任务（如 Click Bell、Place Container Plate、Pick Diverse Bottles 等），每个任务 1600 条轨迹，总共 **19,200 条轨迹**。
  - 真实世界：Allen 移动平台 + Aloha 设置（四臂 AgileX Piper），使用 Realsense D435 摄像头，零样本 sim-to-real 迁移，3 个任务（Click Bell、Place Container Plate、Pick Diverse Bottles），每任务 20 个 rollout。
- **对比方法**：
  - Binning（OpenVLA 风格）
  - FAST（DCT + BPE）
  - VQ-BET（VQ-VLA 使用的 residual VQ-VAE）
  - 方法 A（SA-VLA with cross-attention）
  - 方法 B（SA-VLA with adapter）
- **评价指标**：成功率（success rate），仿真中每任务 100 次 rollout，真实世界每任务 20 次。
- **消融实验**：
  - 状态信息的作用（w/o state vs. with state）。
  - 模型泛化能力（未见任务 + 未见场景）。
  - Tokenization 粒度（相同动作在不同状态下是否被区分为不同 token）。

### 4. 资源与算力

- 论文明确提及：**训练在单个 RTX 4090 GPU** 上完成。
  - 阶段 1（tokenizer 训练）：batch size 1024，200 epochs。
  - 阶段 2（VLA 训练）：batch size 64，10 epochs。
- 未给出总训练时长或 GPU 小时数，也未说明数据加载或分布式训练细节。
- 评估：单卡 RTX 4090 上运行，具体参考 RoboTwin 官方评估流程。

### 5. 实验数量与充分性

- **实验数量**：仿真中 12 任务 × 100 次 = 1200 次评估；真实世界 3 任务 × 20 次 = 60 次评估；消融实验 4+4 组（状态消融、泛化、粒度）；完整 result table 覆盖 6 种方法 × 2 种解码模式。
- **充分性与客观性**：
  - 对比了当前主流的三种离散动作 tokenizer（binning、FAST、VQ-BET），并保持 VLM backbone（Qwen2.5）相同，控制变量做得好。
  - 仿真与真实世界均展示显著提升（+27% 平均成功率 vs. 最强基线）。
  - 消融实验验证了状态引入的必要性、泛化能力（未见任务和场景）、以及 tokenization 粒度的细化（cosine similarity 差异 0.001）。
  - 论文也坦诚了局限性（仅在小型数据集验证，扩展性未证实）。

### 6. 主要结论与发现

- **方法 B（Adapter）效果最佳**，在仿真上平均成功率达 56%（自回归）和 56%（并行），相比最强基线 VQ-BET 提升 27 个百分点。
- **状态条件动作解码是一种简单有效的机制**，能够显著缩小离散 VLA 策略中的压缩差距，无需大幅修改模型架构。
- 两种注入方式均优于无状态基线，且 Adapter 由于扩展了码本表示空间（连续动作族）而优于 Cross-Attention。
- 真实世界零样本迁移实验中，Method B 获得最高平均成功率 33%（自回归），但绝对值仍较低（最强基线为 15%），表明 sim-to-real 差距依然存在。

### 7. 优点

- **方法创新点清晰**：将机器人本体状态显式引入动作 tokenizer 解码，解决了现有工作忽略状态的问题，具有强物理直觉。
- **简单实用**：Method B 仅需一个轻量 MLP adapter，不改变 VQ-VAE 基本结构，便于集成到现有 VLA 框架。
- **双解码支持**：既能自回归也能并行解码（固定长度动作 token），并行模式显著提升推理效率且保持性能。
- **实验比较全面**：在仿真（12 任务）、真实世界（3 任务）、多种 tokenizer 基线、两种解码模式、多种消融（状态、泛化、粒度）上进行了验证，结果一致。
- **开放共享**：提供了详细训练和评估参数（附录 A），有利于复现。

### 8. 不足与局限

- **规模验证不足**：仅在少量数据集（19,200 条）上测试，未在大规模数据集（如 Open X-Embodiment）上验证，**scale law 尚未验证**（论文中也指出这一局限）。
- **模型架构限制**：仅使用了 VQ-VAE 作为 tokenizer 基座，未探索扩散模型（如 π0 的 flow matching）或其他离散生成模型在 tokenization 中的应用。
- **本体适配范围窄**：仅测试了机械臂（Piper 6-DoF ），未评估灵巧手（dexterous hand）等更复杂本体，实际生活和生产中的通用性待验证。
- **真实世界性能上限低**：即使有提升，零样本真实世界平均成功率仅 33%，距离实用仍有较大差距；领域迁移后的 fine-tuning 效果未报告。
- **计算开销报告不足**：未给出完整的训练时间、参数量、推理速度等指标，难以评估效率。

（完）
