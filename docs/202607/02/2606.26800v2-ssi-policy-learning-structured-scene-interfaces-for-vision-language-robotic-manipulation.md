---
title: "SSI-Policy: Learning Structured Scene Interfaces for Vision-Language Robotic Manipulation"
title_zh: SSI-Policy：学习面向视觉-语言机器人操作的结构化场景接口
authors: "Kaijun Wang, Zikai Ouyang, Xuping Wu, Jinyi Hong, Wei Pan, Haibo Lu, Jia Pan, Wei Zhang, Linfang Zheng"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26800v2"
tags: ["query:rob-il"]
score: 9.0
evidence: 提出统一的RGB仅中间表征用于视觉-语言机器人操纵，实现了精确的空间定位与任务感知推理。
tldr: "现实机器人操作需要空间推理与任务意识，低数据训练尤为困难。SSI-Policy围绕结构化场景接口（SSI）构建统一RGB中间表示，融合单目深度特征、语言锚定对象布局和指令条件2D轨迹。SSI为机器人无关，可从无动作视频学习。在LIBERO基准上，每个任务仅10个演示，SSI-Policy比最强先前方法提升近15%，与使用50个演示的方法竞争。消融和真实实验表明几何与运动线索共享互补，实现了高效跨实体泛化与接触丰富操作。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26800v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 857, \"height\": 420}, {\"url\": \"assets/figures/arxiv/2606.26800v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 876, \"height\": 347}, {\"url\": \"assets/figures/arxiv/2606.26800v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 726, \"height\": 516}, {\"url\": \"assets/figures/arxiv/2606.26800v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 878, \"height\": 657}, {\"url\": \"assets/figures/arxiv/2606.26800v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 890, \"height\": 882}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26800v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 431}, {\"url\": \"assets/tables/arxiv/2606.26800v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 894, \"height\": 694}, {\"url\": \"assets/tables/arxiv/2606.26800v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 896, \"height\": 672}, {\"url\": \"assets/tables/arxiv/2606.26800v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 898, \"height\": 132}, {\"url\": \"assets/tables/arxiv/2606.26800v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 901, \"height\": 280}]"
motivation: 现有方法在低数据场景下难以兼容量化任务推理与几何结构，且依赖深度传感器或仅关注运动信号，缺乏统一的RGB-only中间表示。
method: 提出SSI-Policy，基于结构化场景接口（SSI），一种仅RGB的中间表示，联合编码单目深度特征、语言锚定2D对象布局和指令条件运动轨迹，且机器人无关，可从无动作视频预训练。
result: "在LIBERO上每任务仅10个演示即比前方法提升近15%平均成功率，媲美50演示方法；在13个真实世界任务上验证了跨实体操作、空间推理和接触丰富操作。"
conclusion: SSI-Policy通过统一场景接口实现数据高效的机器人操作，证明几何与运动线索互补，且能泛化到不同实体和真实任务。
---

## 摘要
真实世界的机器人操作需要空间基础、任务感知推理和精确控制。在低数据量情况下学习这些能力变得尤为困难。先前的方法往往在可扩展的任务级推理和显式物理结构之间权衡：基于视频的方法在长时间跨度上可能会出现几何漂移，3D方法通常需要深度感应，而许多流/轨迹接口强调运动，但缺乏显式的仅RGB几何表示。我们提出了SSI-Policy，这是一个围绕结构化场景接口（SSI）构建的模块化框架——SSI是一种统一的、仅基于RGB的中间表示，联合编码了单目深度特征、语言基础的对象布局以及指令条件的2D运动轨迹。关键的是，SSI与机器人无关，并且可以从无动作视频中训练，将感知与控制解耦，使得下游策略能够从少量演示中学习。在LIBERO基准测试中，每项任务仅使用10个演示，SSI-Policy相比最强以前方法提升了近15%，并且与使用大规模外部预训练的50个演示方法保持竞争力。消融实验表明，几何和运动线索在共享接口内提供了互补的好处。我们进一步在13个真实世界任务上进行了验证，涵盖了空间推理、跨实体转移和接触丰富的操作。

## Abstract
Real-world robotic manipulation demands spatial grounding, task-aware reasoning, and precise control. Learning such capabilities becomes particularly challenging in the low-data regime. Prior methods often trade off scalable task-level reasoning and explicit physical structure: video-based approaches can drift geometrically over long horizons, 3D approaches often require depth sensing, and many flow/trajectory interfaces emphasize motion without an explicit RGB-only geometric representation. We introduce SSI-Policy, a modular framework built around a Structured Scene Interface (SSI) -- a unified, RGB-only intermediate representation that jointly encodes monocular depth features, language-grounded object layouts, and instruction-conditioned 2D motion trajectories. Critically, SSI is robot-agnostic and trainable from action-free video, decoupling perception from control so that the downstream policy can learn from few demonstrations. On the LIBERO benchmark with only 10 demonstrations per task, SSI-Policy improves over the strongest prior method by nearly 15\% and remains competitive with 50-demo methods that leverage large-scale external pretraining. Ablations show that geometric and motion cues provide complementary benefits within the shared interface. We further validate on 13 real-world tasks spanning spatial reasoning, cross-embodiment transfer, and contact-rich manipulation.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的详细中文总结。

### 论文总结：SSI-Policy：学习面向视觉-语言机器人操作的结构化场景接口

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：真实世界中的机器人操作不仅需要语义识别，更需要空间定位、任务感知推理和精确控制。在**低数据（few-shot）**场景下，学习这类能力尤为困难。
- **背景与已有方法局限**：
    - **基于视频的方法**（如UniPi, VPT）：受益于大规模RGB预训练，但长期推理中易出现几何漂移和计算成本高。
    - **几何中心方法**（如3D表示）：利用RGB-D进行精确空间推理，但依赖深度传感器，在多任务语言条件设定下困难。
    - **流/轨迹接口**（如ATM, Im2Flow2Act）：提供紧凑运动抽象，但要么隐含处理几何，要么依赖深度/点云初始化或测试时视频生成。
- **核心问题**：现有方法缺少一种**统一的、仅基于RGB的中间表示**，该表示能同时编码几何结构、任务条件运动，并且是**机器人无关**、**可从无动作视频训练**的——这些属性对于数据高效的**多任务操作**至关重要。

#### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：提出**SSI-Policy**，一种围绕**结构化场景接口（Structured Scene Interface, SSI）**构建的模块化框架。关键在于定义一个介于感知和控制之间的**中间表示层**，该层：
    - 机器人无关（可跨实体）
    - 可从无动作RGB视频训练
    - 下游策略仅需极少量演示即可学习
- **SSI的组成**：从RGB观测和语言指令中联合编码三个互补信号：
    1.  **单目深度特征（Monocular Depth Features, MDE）**：通过DepthAnything V2提取相对深度特征，捕获几何关系。
    2.  **语言引导的对象布局图（Language-Guided Object Localization, LGOL）**：利用Grounding DINO根据指令检测对象和机械臂，生成空间显著图。
    3.  **指令条件运动轨迹（Instruction-Conditioned Motion Prediction, ICMP）**：预测像素轨迹，建模对象与机械臂的预期动力学。通过混合采样（均匀采样+高置信度布局区域采样）初始化轨迹点。
- **关键技术细节**：
    - **感知模块（Perception Composer）**：独立预训练三个子模块，可从无动作视频数据训练。
    - **扩散动作规划器（Diffusion Action Planner, DAP）**：
        1.  **多模态特征融合**：将多视图RGB、深度特征、布局图和运动轨迹通过模态特定CNN和Transformer融合，形成条件向量。
        2.  **动作扩散**：基于条件扩散策略，将高斯噪声逐步去噪为动作序列，目标函数为噪声预测的MSE。
    - **公式**：策略分解为 \(S_t = f_{\text{percept}}(I_t, \ell)\) 和 \(A_t = f_{\text{action}}(S_t, I_t, P_t)\)，其中 \(S_t\) 为SSI表示。训练目标为 \(\mathcal{L} = \text{MSE}(\epsilon_k - \epsilon_\theta(s_t, A^k_t, k))\)。

#### 3. 实验设计：数据集/场景、Benchmark、对比方法
- **仿真环境**：**LIBERO**基准套件，包含：
    - **四个子套件**：Spatial（空间推理）、Object（对象操作）、Goal（目标条件）、Long（长序列），每任务50个演示。
    - **低数据协议**：每任务仅**10个演示**训练策略（运动预测器预训练使用全部50个视频的RGB和语言）。
- **真实世界**：使用6-DoF ARX-5手臂和两个RGB相机（侧视+眼在手），评估**13个任务**，包括空间推理、变形物体操作、接触丰富操作、跨实体转移等。
- **对比方法**：
    - **基线**：R3M-finetune, VPT, UniPi, Diffusion Policy, ATM, ATM-DP, MaIL, FLIP。
    - **可扩展对比**：MDT, DepthHelps, OFT；以及使用外部数据的模型（Octo, OpenVLA, SpatialVLA, \pi0等）。
    - **核心指标**：成功率（Success Rate）。

#### 4. 资源与算力
- **训练硬件**：文中明确说明**使用四张NVIDIA A800 GPU**进行训练和仿真评估。
- **训练细节**：每任务训练，未明确总训练时长。但提到输入分辨率128×128，扩散步数100，优化器AdamW (lr=5e-4)，训练400轮（LIBERO-Long为600轮）。

#### 5. 实验数量与充分性
- **实验数量与种类**：
    - **LIBERO low-data**（10演示，表1）：4个套件，每套件10个任务，3个随机种子×20次评估。
    - **LIBERO 50演示**（表2）：4个套件，对比了使用外部数据的众多SOTA。
    - **真实世界空间推理**（表3）：6个任务，每任务20次评估。
    - **跨实体转移**（图5，表4）：机器人到机器人（5种非目标手臂）、人类到机器人（2个任务）。
    - **鲁棒性挑战**（表5）：5个复杂真实任务，对比单独训练的DP。
    - **消融实验**（表6，图6）：6种变体，分析深度、布局、运动各组件的贡献，以及混合采样策略。
- **充分性与客观性**：
    - **充分**：覆盖了低数据、高数据、仿真、真实、跨实体、复杂场景，与大量基线进行比较。
    - **公平性**：低数据对比主要与同设置方法比较；外部数据方法单独列出。消融设计清晰，基于同一基准。
    - **客观**：使用标准LIBERO协议，报告多次种子平均值，统计可靠性较高。

#### 6. 论文的主要结论与发现
1.  **显著低数据性能**：在LIBERO上每任务仅10演示，SSI-Policy平均成功率**80.42%**，比最强先前方法（ATM-DP）提升**近15%**，与使用50演示及外部数据的方法竞争。
2.  **互补性**：消融实验（表6）证明，几何（深度）和运动（轨迹）在统一接口中提供**互补**增益，单独使用某个信号效果差很多。
3.  **跨实体泛化**：
    - 机器人到机器人：使用非目标手臂视频预训练运动预测器，再结合少量目标机器人演示，表现逼近全数据基线。
    - 人类到机器人：仅使用人类手部视频（无动作）训练SSI，可达到40-45%成功率；结合少量机器人数据后，可超越纯机器人训练结果（表4）。
4.  **鲁棒性**：在13个真实任务（含复杂变形、接触、长序列）上，SSI-Policy明显优于单任务Diffusion Policy（54% vs 29%）。
5.  **SSI作为主要信息载体**：仅使用SSI和本体感知（无RGB）的策略保留约98%的性能，证明SSI捕获了大部分任务相关信息。

#### 7. 优点（方法或实验设计的亮点）
1.  **统一而解耦的表示**：SSI将几何、布局、运动融合进一个**机器人无关且可在无动作视频下预训练**的接口，实现了感知与控制的有效解耦。
2.  **数据高效**：通过预训练SSI感知模块，下游策略能够从**极少数演示**（10个）中学习，并达到甚至超越需要更多数据的同类方法。
3.  **跨实体零样本/少样本转移**：SSI设计天然支持跨不同形态的迁移（机器人至机器人、人类至机器人），无需重新标注或复杂适配。
4.  **实验完备且客观**：包含仿真和真实任务、大量基线对比、详细消融及跨实体场景，结果统计严谨，提升了说服力。

#### 8. 不足与局限
1.  **感知瓶颈**：主要失败源于**感知模块噪声**（对象检测错误、深度时序不一致、变形物体抓取困难）。SSI策略的可靠性高度依赖于上游感知的准确性。
2.  **缺乏时序一致性**：单目深度或检测输出的**逐帧不稳定**会导致运动预测抖动，影响平滑控制。
3.  **域适应限制**：仿真到真实的视觉域差距需手动调整提示（如“tomato soup can”需改为“green-orange soup can”），增加了部署复杂性。
4.  **未涉及触觉反馈**：对于紧密接触、柔体操作等需要力觉的场景，论文指出未来需要融合触觉信息以提高鲁棒性。
5.  **计算资源需求**：训练和测试仍需多块A800 GPU，对于资源受限的研究者可能存在门槛。

（完）
