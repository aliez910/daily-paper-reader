---
title: "DreamTrajectory: Trajectory-Guided Action Generation with World Model Alignment for Mobile Manipulation"
title_zh: DreamTrajectory：面向移动操作的轨迹引导动作生成与世界模型对齐
authors: "Zheng Yang, Wenjie Zhang, Xiangyu Chen, Wenxuan Song, Xianpeng Wang, Yihang Kang, Wen Chen, Lujia Wang, Renjing Xu, Xiaowen Chu"
date: 2026-08-02
pdf: "https://arxiv.org/pdf/2608.01381v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 面向移动操纵的VLA策略及闭环验证机制
tldr: 移动操作任务需在持续变化的视角与接触条件下协调基座与机械臂，动作空间远大于固定基座操作。现有视觉-语言-动作策略直接将观测映射为全身动作块，缺乏显式任务空间运动规划，导致基座-臂协调不精确，且开环执行使运动偏差不断累积。DreamTrajectory提出轨迹引导框架，在单一动作专家中联合预测意图级末端执行器轨迹与全身动作块，并由轻量轨迹世界模型通过测试时搜索-预测-评分选择最对齐规划轨迹的候选动作。在MS-HAB基准与三项真实移动操作任务上均取得显著成功率提升，尤其在接触密集的铰接物体任务上增益最大。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.01381v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1840, \"height\": 559}, {\"url\": \"assets/figures/arxiv/2608.01381v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1841, \"height\": 545}, {\"url\": \"assets/figures/arxiv/2608.01381v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1841, \"height\": 870}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.01381v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1852, \"height\": 670}, {\"url\": \"assets/tables/arxiv/2608.01381v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 841, \"height\": 246}, {\"url\": \"assets/tables/arxiv/2608.01381v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1750, \"height\": 339}, {\"url\": \"assets/tables/arxiv/2608.01381v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1439, \"height\": 299}, {\"url\": \"assets/tables/arxiv/2608.01381v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 863, \"height\": 229}]"
motivation: 现有VLA策略直接映射观测到全身动作块，缺乏显式任务空间规划，基座-机械臂协调不精准且开环执行导致规划与实际运动偏差累积。
method: 在单一动作专家中联合预测意图级末端轨迹与全身动作块，结合轻量轨迹世界模型进行测试时搜索-预测-评分，优选与规划轨迹最对齐的候选动作。
result: "MS-HAB平均成功率从32.3%提升至54.8%；三项真实移动操作任务成功率分别为63.3%、81.7%、90.0%，接触密集型铰接物体任务增益最显著。"
conclusion: 通过轨迹显式引导与世界模型对齐，有效提升移动操作中基座-臂协调精度与执行鲁棒性，显著改善任务成功率。
---

## 摘要
移动操作要求机器人在持续变化的视角和接触条件下协调底盘与机械臂运动，其动作空间远大于固定基座操作的规模。现有视觉-语言-动作（VLA）策略存在两方面局限。（i）它们将观测直接映射到全身动作块，在缺乏显式任务空间运动规划的情况下搜索这一巨大动作空间，导致底盘-机械臂的协调预测不够精确。（ii）它们以开环方式执行所预测的动作块，并未检验这些动作能否实现策略所预期的运动，因此控制误差和未建模的接触会不断累积，造成规划运动与实际运动之间的偏差。我们提出 DreamTrajectory，一种面向语言条件移动操作的轨迹引导框架，针对上述每项局限各引入一个组件。针对（i），DreamTrajectory 在单一动作专家中联合预测意图层面的末端执行器轨迹与全身动作块，使轨迹能够显式地引导底盘-机械臂的动作生成，而非隐式存在。针对（ii），一个轻量级轨迹世界模型预测候选动作块将引发的轨迹，并采用测试时搜索-预测-评分流程选取与规划轨迹对齐度最高的候选。在 MS-HAB 上，轨迹引导将平均成功率由 32.3% 提升至 47.5%，测试时优化进一步提升至 54.8%，且在接触密集的铰接物体任务上提升最为显著。在三项真实世界移动操作任务上，对应的平均成功率分别为 63.3%、81.7% 和 90.0%。

## Abstract
Mobile manipulation requires a robot to coordinate base and arm motion under continuously changing viewpoints and contact conditions, within an action space far larger than that of fixed-base manipulation. Existing Vision-Language-Action (VLA) policies are limited in two respects. (i)They map observations directly to whole-body action chunks, searching this large action space without an explicit task-space motion plan, which makes coordinated base--arm prediction imprecise. (ii)They execute the predicted chunk open-loop, without checking whether the actions can realize the motion the policy intended, so control errors and unmodeled contacts accumulate into a gap between planned and realized motion. We present DreamTrajectory, a trajectory-guided framework for language-conditioned mobile manipulation that introduces one component for each limitation. Addressing(i), DreamTrajectory jointly predicts an intention-level end-effector trajectory and a whole-body action chunk in a single action expert, so that the trajectory explicitly guides base--arm action generation instead of remaining implicit. Addressing(ii), a lightweight trajectory world model predicts the trajectory that a candidate action chunk would induce, and a test-time search--predict--score procedure selects the candidate best aligned with the planned trajectory. On MS-HAB, trajectory guidance raises average success from 32.3% to 47.5% and test-time refinement further to 54.8%, with the largest gains on contact-rich articulated-object tasks. On three real-world mobile manipulation tasks, the corresponding average success rates are 63.3%, 81.7%, and 90.0%.

---

## 论文详细总结（自动生成）

<think>Let me analyze this paper carefully and produce a comprehensive Chinese summary based on the provided content. The paper is "DreamTrajectory: Trajectory-Guided Action Generation with World Model Alignment for Mobile Manipulation."

Let me structure the summary according to the 8 required points:

1. Core problem and motivation
2. Methodology
3. Experimental design
4. Resources and compute
5. Experiment quantity and adequacy
6. Main conclusions
7. Strengths
8. Limitations

I'll write this in Markdown with hierarchical headings and bullet points.</think>

# DreamTrajectory 论文总结

## 1. 核心问题与研究动机

- **任务背景**：移动操作（Mobile Manipulation）要求机器人同时协调移动底盘与机械臂，在持续变化的视角与接触条件下完成操作任务，其联合动作空间远大于固定基座操作。
- **现有方法的两个核心局限**：
  - **缺乏显式任务空间规划**：现有 VLA（Vision-Language-Action）策略直接由观测映射到全身动作块（whole-body action chunk），未在任务空间内对末端执行器轨迹进行显式建模，导致在巨大的动作空间中搜索时底盘-机械臂协调不够精确。
  - **开环执行导致偏差累积**：预测的动作块以开环方式执行，未验证该动作是否真正实现了策略预期运动；控制误差与未建模接触会引起"规划运动-实际运动"之间的偏差持续累积。
- **核心矛盾**：VLA 模型在端到端映射中难以兼顾"显式规划引导"与"闭环校验"两项能力。

## 2. 方法论

### 2.1 核心思想
DreamTrajectory 是一个面向语言条件移动操作的"轨迹引导"框架，针对上述两个局限分别设计了两个组件：轨迹引导式动作专家 + 轻量级轨迹世界模型 + 测试时对齐搜索。

### 2.2 技术细节

- **单一动作专家中的联合预测**：
  - 在同一个动作专家网络内，同时输出：
    - **意图层面的末端执行器（EE）轨迹**（task-space motion plan）；
    - **全身动作块**（base + arm 联合动作）。
  - 通过 joint training，使末端轨迹作为显式"指导信号"影响全身动作预测，而非仅隐式存在于网络参数中。

- **轻量级轨迹世界模型（Trajectory World Model）**：
  - 输入候选动作块 + 当前观测，预测该动作块会"诱发"出的实际末端轨迹。
  - 用作对策略意图轨迹的"仿真预测器"，本质上充当内部一致性检查器。

- **测试时搜索-预测-评分流程**：
  - **Search**：动作专家采样多个候选全身动作块；
  - **Predict**：轨迹世界模型分别预测各候选对应的预期 EE 轨迹；
  - **Score**：以候选所诱导的轨迹与策略"规划轨迹"的对齐程度为评分依据，挑选对齐度最高的候选执行。
  - 本质上是一种隐式的 Model Predictive Control（MPC）变体，利用世界模型作"数字孪生式"评估。

- **框架整体意义**：将显式任务空间规划与闭环世界模型对齐结合，弥补了单纯 VLA 模型的规划盲区与开环偏差问题。

## 3. 实验设计

- **仿真 Benchmark**：MS-HAB（Multi-Scenario High-level Atomic Behavior，文中基准），面向语言条件移动操作任务。
- **真实世界任务**：三项移动操作任务，对应平均成功率分别报告为 63.3%、81.7%、90.0%。
- **对比方法**：从论文摘要与背景判断，相对对象为"现有 VLA 策略"基线（未直接列举多个 SOTA 名字），重点对比：
  - Baseline VLA（如原始全身动作块直接预测 + 开环执行）；
  - 加入轨迹引导（trajectory guidance）但不测试时优化；
  - 加入轨迹引导 + 测试时世界模型对齐（完整 DreamTrajectory）。
- **任务关注点**：包含接触密集的铰接物体操作（articulated-object）任务，作为观察增益最大的典型场景。

## 4. 资源与算力

- 论文（基于本次提供的 PDF/Markdown 提取文本）**未明确披露**：
  - 训练使用的 GPU 型号与数量；
  - 总训练时长（小时/epoch 数）；
  - 仿真与真实实验的算力开销；
  - 轨迹世界模型的参数量与推理时延。
- 这是该论文在可复现性方面的一个明显缺口，建议读者参照代码仓库（如有公开）补充信息。

## 5. 实验数量与充分性

- **实验维度**（基于已披露信息）：
  - **仿真**：在 MS-HAB 全基准的子任务集合上验证，呈现"baseline → +轨迹引导 → +测试时优化"的逐步提升曲线；
  - **真实世界**：在 3 个不同任务上验证，覆盖不同难度与接触类型。
- **结果证据**：
  - MS-HAB 平均成功率 32.3% → 47.5%（+轨迹引导）→ 54.8%（+测试时优化）；
  - 在接触密集型铰接物体任务上获最大增益；
  - 三项真实任务成功率呈梯度上升，提示方法在真实场景中仍具备迁移能力。
- **充分性评价**：
  - **优点**：跨越仿真 + 真实世界两个层面；分层消融清晰（轨迹引导 / 测试时世界模型各贡献独立可量化）。
  - **不足**：
    - 缺乏对每个真实任务的逐回合（per-episode）失败模式分析；
    - 未明确说明每项任务的具体试验次数与统计显著性（无方差/置信区间）；
    - 与更多 SOTA（如 OpenVLA、RT-2、Octo、3D-VLA 等专用 VLA 基线）的直接对比在文本摘要中未充分展开，难以判断相对优势的通用性。

## 6. 主要结论与发现

- **轨迹显式引导可显著提升移动操作性能**：将"末端轨迹"作为显式中间表示，可让策略的底盘-臂动作预测更协调。
- **世界模型驱动的测试时优化进一步缩小规划-执行偏差**：在已有轨迹引导基础上，再叠加 search-predict-score 的对齐选择，平均成功率再提升约 7.3 个百分点（47.5% → 54.8%）。
- **接触密集型任务获益最大**：在铰接物体等强接触任务上，运动偏差与未建模接触累积更严重，因此世界模型对齐的边际收益更高。
- **真实世界可迁移**：三项真实任务上分别取得 63.3% / 81.7% / 90.0% 的成功率，说明该框架具备从仿真训练迁移到真实部署的可用性。

## 7. 优点（亮点）

- **问题切分清晰**：把"缺乏显式规划"和"开环偏差"拆为两个独立子问题分别解决，每个组件各有清晰职责。
- **结构简洁高效**：单一动作专家同时预测轨迹与动作，避免维护多套独立模型。
- **测试时优化不改变训练目标**：search-predict-score 流程仅在推理时执行，相对独立，部署灵活。
- **覆盖仿真 + 真机**：跨域验证提升工程可信度。
- **对接触密集场景友好**：在铰接物体等难任务上获得最大收益，说明对齐机制对"难以精确建模"的任务尤其有效。

## 8. 不足与局限

- **算力与训练细节未披露**：缺训练时长、GPU 规模、世界模型参数量等关键信息，影响可复现性评估。
- **消融有限**：仅展现"轨迹引导 / 测试时优化"两个维度的拆分，缺乏对轨迹长度、候选数量、世界模型深度等关键超参的敏感性分析。
- **对比基准宽度不足**：摘要层面未充分列出与多种主流 VLA（如 OpenVLA、RT-X、3D Diffusion Policy 等）的逐一比较。
- **统计显著性未明**：真实世界任务的成功率未提供标准差或试验次数，外部读者难以判断性能稳定性。
- **世界模型的误差风险**：轨迹世界模型本身的预测误差会直接影响测试时评分与候选选择，存在"以世界模型为瓶颈"的风险，论文未深入讨论该误差传播。
- **任务覆盖有限**：仅 3 个真实任务 + MS-HAB 子集，对不同机器人构型、杂乱场景、长程任务（如导航 + 操作交叉）的泛化能力仍待验证。
- **推理开销**：测试时需多候选采样 + 世界模型前向，单步延迟对实时控制的影响在论文中未量化讨论。

（完）
