---
title: "Dexterity-BEV: Aligning 3D World and Actions for Generalizable Robot Policies Learning"
title_zh: Dexterity-BEV：对齐3D世界与动作以实现可泛化机器人策略学习
authors: "Huayi Zhou, Wei Gao, Dekun Lu, Ruiji Liu, Zhanqi Zhang, Ziyang Zhang, Jian Chen, Wenlve Zhou, Sheng Xu, Shumin Li, Kangyi Guo, Shichen Xu, Zixin Huang, Yongyi Su, Kui Jia"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.02274v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 通过顶点图和谱对齐3D世界和动作以实现通用机器人策略
tldr: 现有端到端机器人操作策略依赖2D视觉模型，忽略3D空间本质且缺乏输入输出对齐。本文提出Dexterity-BEV，通过引入对齐顶点图与顶点频谱将2D输入提升至3D，并构建规范BEV帧统一表达视觉与动作，实现视角不变性。同时开发数据处理与时间对齐pipeline，显著减轻空间时间错位，提升策略泛化性与操作一致性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.02274v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1414, \"height\": 812}, {\"url\": \"assets/figures/arxiv/2606.02274v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1437, \"height\": 561}, {\"url\": \"assets/figures/arxiv/2606.02274v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1439, \"height\": 299}, {\"url\": \"assets/figures/arxiv/2606.02274v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 507, \"height\": 339}, {\"url\": \"assets/figures/arxiv/2606.02274v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 972, \"height\": 500}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.02274v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1401, \"height\": 268}, {\"url\": \"assets/tables/arxiv/2606.02274v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 899, \"height\": 248}, {\"url\": \"assets/tables/arxiv/2606.02274v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 427, \"height\": 633}]"
motivation: 解决2D VLM操作策略忽略3D本质及跨场景空间对齐缺失的问题。
method: 引入对齐顶点图和顶点频谱的3D表示，通过规范BEV帧对齐视觉输入与机器人动作，并构建数据时间对齐pipeline。
result: 有效缓解空间-时间错位，在真实世界操作任务中提升策略泛化性和一致性。
conclusion: Dex-BEV通过3D对齐框架实现了更通用灵巧的机器人操作学习。
---

## 摘要
端到端操作策略结合大规模预训练的视觉语言模型（VLMs）展现了实现通用且灵巧的机器人操作的潜力。然而，它们继承了2D基础模型的两个关键局限：1）依赖2D RGB输入，忽略了操作本质上3D的特性；2）输入输出空间之间以及不同机器人本体、相机设置和轨迹数据集之间缺乏空间3D对齐。本文提出了一系列贡献来解决这些问题。首先，我们引入了对齐顶点图和顶点频谱——一种像素级3D表示，利用相机标定和可选深度将2D视觉输入提升至3D。这种新颖的输入表示将3D感知与2D大型VLM的泛化能力相结合。然后，我们提出通过将每个相机视角的像素级3D信息和机器人动作表达到一个共享坐标系，来对齐操作策略的输入和输出。基于此，我们指定了一个规范的鸟瞰图（BEV）对齐框架，并创新性地提出构建BEV图像，生成一种对相机姿态变化鲁棒的视角不变表示。为了支持大规模训练和评估，我们开发了一个全面的数据处理流水线来执行此类对齐；我们还引入了一种新颖的时间对齐方案，用于处理不同机器人、人类操作员和数据集中的轨迹。这些贡献共同缓解了输入和输出的时空错位问题，提高了真实世界操作的一致性和泛化能力。预训练检查点、源代码和数据处理流水线可在 https://hnuzhy.github.io/projects/Dex-BEV 获取。

## Abstract
End-to-end manipulation policies, combined with web-scale pretrained Vision-Language Models (VLMs), show the promise for generalizable and dexterous robotic manipulation. However, they inherit two key limitations from 2D foundation models: 1) the reliance on 2D RGB inputs that ignores the intrinsically 3D nature of manipulation; and 2) the lack of spatial 3D alignment between input-output spaces as well as across diverse robot embodiments, camera setups, and trajectory datasets. In this paper, we present a series of contributions to address these issues. First, we introduce aligned vertex map and vertex spectrum -- a pixel-wise 3D representation that elevates 2D visual inputs to 3D, using camera calibration and optional depth. This novel input representation marries 3D awareness with the generalization of 2D large VLMs. Then, we propose to align the inputs and outputs of manipulation policies by expressing per-pixel 3D information of each camera view and robot actions to a shared coordinate. Based on this, we designate a canonical Bird's-Eye-View (BEV) alignment frame and innovatively propose to construct BEV images, producing a view-invariant representation robust to camera pose variations. To enable training and evaluation at scale, we develop a comprehensive data processing pipeline to perform such alignments; we also introduce a novel temporal alignment scheme for trajectories across diverse robots, human operators, and datasets. These contributions collectively mitigate input and output spatial-temporal misalignments, improving the consistency and generalization for real-world manipulation. Pretrained checkpoint, source code and data processing pipeline are available in https://hnuzhy.github.io/projects/Dex-BEV.

---

## 论文详细总结（自动生成）

# 论文总结：Dexterity-BEV: Aligning 3D World and Actions for Generalizable Robot Policies Learning

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有的端到端机器人操作策略（如 Vision-Language-Action Models, VLAs）严重依赖 2D RGB 图像输入，忽视了操作本质上的 3D 特性；同时，输入（多视角图像）与输出（关节角度/末端执行器位姿）之间以及不同机器人本体、相机设置、数据集之间缺乏空间 3D 对齐，导致策略泛化能力不足。
- **研究动机**：结合 2D 大规模预训练 VLM 的泛化能力与 3D 空间感知，通过统一的输入输出空间对齐来提升策略在不同本体、视角和场景下的鲁棒性和可迁移性。
- **整体目标**：提出 Dexterity-BEV (Dex-BEV) 框架，将 2D 视觉输入提升至 3D，并实现多视角观测、机器人本体感知和动作输出在共享坐标系（规范 BEV 帧）中的对齐，从而构建更通用、更一致的机器人操作学习范式。

## 2. 论文提出的方法论

- **核心思想**：
  - 利用相机标定（内参、外参）和可选深度图，将 2D RGB 特征与 3D 几何信息融合，产生像素级对齐的 3D 表示（对齐顶点图、顶点频谱）。
  - 指定一个规范 BEV 帧作为共享坐标，将所有相机视图的顶点图、机器人 proprioception 和动作都表达在该帧下，实现输入输出空间对齐。
  - 为无深度传感器的情况设计顶点频谱（Vertex Spectrum），通过离散深度假设投影，保持 3D 感知能力。
  - 构建合成 BEV 图像（鸟瞰图），提供视角不变性，对相机姿态变化更鲁棒。
  - 设计数据预处理流水线，对异构数据集进行 3D 空间对齐（手动 GUI + ICP + 基础模型）和跨轨迹时间对齐（归一化末端速度）。

- **关键技术细节**：
  - **对齐顶点图（Aligned Vertex Map）**：给定深度图 \(D_{t,i}\) 和相机内参 \(K_i\)，将像素 \((u,v)\) 反投影到相机坐标系顶点 \(P_{\text{camera}_i}(u,v) = K_i^{-1}[u,v,1]^T D_{t,i}(u,v)\)。然后通过外参 \(T_{t,i}\) 和共享对齐帧 \(T_{\text{align}_t}\) 变换到全局一致的 3D 顶点：\(P_{\text{aligned}_i} = T_{\text{align}_t}^{-1} T_{t,i} P_{\text{camera}_i}\)。该顶点图保持与原始 RGB 像素对齐，可编码 3D 位置嵌入并与 RGB 特征相加。
  - **顶点频谱（Vertex Spectrum）**：对于无深度图像的相机，采样 \(M\) 个深度假设 \(d_j = d_{\min} + (d_{\max}-d_{\min}) \cdot \frac{j(j+1)}{M(M+1)}\)，每个深度假设对应一个 3D 点，生成体素坐标网格，经轻量编码器产生 2D 位置嵌入。
  - **BEV 帧与 BEV 图像构建**：BEV 帧可选机器人基坐标系或工作空间底部中心。将多相机齐次点云通过正射投影到 BEV 平面，生成 RGB 鸟瞰图及其对应的像素级 3D 顶点图。
  - **网络架构**：多视角的 RGB-3D 融合特征 + BEV 图像特征 + 语言指令输入到 VLM 骨干网络，输出多模态表示后经 Flow Matching 动作专家预测未来 \(M\) 步动作。动作表示为统一 BEV 帧下的 SE(3) 位姿。
  - **数据对齐流水线**：
    - 3D 空间对齐：统一相机内/外参格式，通过手动 3D GUI 匹配、ICP 注册、DepthAnything V3 估计深度，缺失深度用 FoundationStereo 合成；高精度 URDF 模型注册到观测空间，统一 TCP 标架约定（平行爪尖、多指腕部），通过正向运动学计算统一 SE(3) 位姿。
    - 时间对齐：假设多数操作任务为准静态，归一化末端效应器速度至标准值，重新计算轨迹关键点物理时间，消除因不同机器人或遥操作者造成的速度差异。

- **关键公式**：反投影公式（式 2）、对齐变换公式（式 4）、深度假设 LID 采样（式 5）、Flow Matching 损失（式 1）。

## 3. 实验设计

- **模拟基准**：
  - LIBERO：4 个任务套件（Spatial, Object, Goal, Long），官方设置以及修改版（随机化相机位姿、机器人基座和场景布局）。
  - RoboTwin-2.0：50 个任务，包含随机化环境。
  - 对比方法：π0、X-VLA，以及 2D 消融（去除所有 3D 输入且使用 X-VLA 约定表达 SE(3)）。
  - 评价指标：成功率（success rate）。
- **真实世界实验**：
  - 四种不同的双臂平台：Agilex bimanual、DexForce W1 humanoid（灵巧手）、DexForce W1（平行夹爪）、DexForce A1 semi-humanoid。
  - 五个长时域任务：Fold Mailer Box (Agilex)、Fold Cloth (Agilex)、Scoop Popcorn (W1*) 、Handover Book (W1) 、Fold Cloth (A1)。
  - 对比方法 π0、X-VLA，每个任务 30 次试验，报告成功率。
  - 定性展示对未见颜色/大小/刚度的衣物折叠、动态干扰下的抓取、人机交互传递等零样本泛化。

## 4. 资源与算力

- 论文中 **未明确说明 GPU 型号、数量、训练时长** 等算力资源信息。仅提到预训练检查点、源代码和数据处理流水线开源，但未提供训练计算量细节。需要指出这一点。

## 5. 实验数量与充分性

- **模拟实验**：两个 benchmark（LIBERO 和 RoboTwin-2.0），每个 benchmark 包括多个任务子集（LIBERO 4 个套件，RoboTwin 2.0 整体平均）。此外，设计了修改版 LIBERO 实验（随机化相机和场景布局），对比了 2D 消融和两个 SOTA 基线。附带了训练损失曲线（图 4）比较模型收敛。
- **真实实验**：5 个不同任务，每个 30 轮，对比两个强基线，报告成功率绝对数值。定性展示了零样本泛化案例。
- **消融成分**：通过去除所有 3D 输入和 3D 对齐的 2D 消融，验证了 3D 表示和对齐的有效性。
- **充分性与客观公平性**：
  - 实验覆盖了不同本体（单臂、双臂、人形）、不同传感器设置（有无深度）、不同场景（仿真与真实），泛化测试充分。
  - 对比方法选取当时较新的 VLA 模型（π0, X-VLA），并注明使用官方或报告结果，公平性较好。
  - 但未提供多次运行的标准差或置信区间，对随机性的量化不足；真实世界试验次数（30）适中但未提及随机种子控制。
  - 修改版 LIBERO 实验设计了强烈的相机/场景扰动，挑战性高，结果有说服力。

## 6. 论文的主要结论与发现

- Dex-BEV 在模拟 benchmark 上与 SOTA 相当（LIBERO 几乎持平），在 RoboTwin-2.0 上显著提升，且能实现单一 checkpoint 跨不同本体泛化。
- 2D 消融性能大幅下降，证明 3D 输入和对齐是核心贡献。
- 在修改版 LIBERO（随机相机和场景布局）中，基线方法几乎失败（<10%），Dex-BEV 仍保持约 90% 成功率，显示强大的视角和场景不变性。
- 真实世界 5 个长时域双臂任务上，Dex-BEV 成功率显著优于 π0 和 X-VLA（10-30% 绝对提升），并展示出对未见变形体、动态干扰和人机交互的鲁棒泛化。
- 结论：通过 3D 空间对齐和 BEV 表示，模型学到了底层 3D 空间机械原理而非记忆 2D 模式，从而实现了更好的泛化。

## 7. 优点

- **方法创新**：
  - 将自动驾驶领域的 BEV 表示、顶点频谱等概念引入机器人 VLA，与 2D VLM 结合，实现 3D 感知与 2D 泛化的双赢。
  - 提出统一坐标帧对齐输入输出，消除因本体、相机、帧约定造成的分布差异，降低模型学习负担。
  - 设计完整数据处理流水线，覆盖空间对齐（手动+自动+基础模型）和时间对齐（速度归一化），具有实用工程价值。
- **实验全面**：
  - 覆盖仿真和真实、多种本体、多种任务类型（折叠、抓取、传递、舀取）。
  - 强扰动下的对比实验凸显视角不变性。
  - 公开代码和数据流水线，可复现性强。

## 8. 不足与局限

- **依赖相机标定**：Dex-BEV 强烈依赖准确的相机内/外参，在无标定或频繁变动的非结构化环境中难以直接部署。文中亦承认这一局限，并建议未来探索无标定 BEV 学习方法或改进 3D 重建基础模型。
- **深度信息依赖**：对多相机深度图的可用性有要求；虽然顶点频谱缓解了 RGB-only 情况，但仍需深度范围先验，且 M 个深度假设增加计算量。
- **算力资源未公开**：未报告训练所需 GPU 型号、数量、时长，不利于其他研究者评估复现成本。
- **实验统计细节不足**：真实实验仅报告成功率，未提供方差、置信区间或统计检验，对结果稳定性的量化较弱。
- **时间对齐假设局限**：假设任务为准静态（速度可缩放），对于动态任务（如投掷、击打）可能不适用。
- **基线覆盖有限**：对比方法均为 2024-2026 年的代表性 VLA，但未包括更多近期模型（如 OCTO、RDT-1B 等），也未对比基于点云/体素的纯 3D 方法（如 3D-VLA），无法全面说明方法的领先性。
- **应用限制**：目前的 BEV 帧依赖机器人基座或工作台中心，对于移动操作或全身操作可能需要更灵活的对齐机制。

（完）
