---
title: Generalizable Dense Reward for Long-Horizon Robotic Tasks
title_zh: 长程机器人任务的可泛化密集奖励
authors: "Silong Yong, Stephen Sheng, Carl Qi, Xiaojie Wang, Evan Sheehan, Anurag Shivaprasad, Yaqi Xie, Katia Sycara, Yesh Dattatreya"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00055v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 利用 VLM 提供的稠密奖励改进基于模仿学习的基础策略
tldr: 针对机器人基础模型在长程任务中因分布偏移和误差累积导致的性能下降问题，本文提出VLLR框架。该框架结合了基于LLM/VLM的任务进度识别（外部奖励）和基于策略自确定性的内在奖励。通过LLM分解任务并利用VLM初始化价值函数，结合PPO微调，在无需人工奖励工程的情况下，显著提升了机器人在复杂任务中的成功率和泛化能力。
selection_source: fresh_fetch
motivation: 现有的机器人模仿学习模型在长程任务中表现不佳，且强化学习微调通常依赖繁琐且难以泛化的人工奖励设计。
method: 提出VLLR框架，利用LLM分解任务、VLM评估进度以初始化价值函数，并结合策略自确定性提供每步的内在奖励指导。
result: "在CHORES基准测试中，VLLR比预训练策略成功率提升高达56%，在分布内和分布外任务上均优于现有SOTA强化学习微调方法。"
conclusion: VLLR通过结合视觉语言模型的任务理解与策略自身的确定性，为长程机器人任务提供了一种无需人工干预的高泛化性稠密奖励方案。
---

## 摘要
现有的机器人基础策略主要通过大规模模仿学习进行训练。虽然这些模型展示了强大的能力，但由于分布偏移和误差累积，它们在处理长程（long-horizon）任务时往往面临挑战。虽然强化学习（RL）可以微调这些模型，但在没有手动奖励工程的情况下，它难以在多样化的任务中取得良好效果。我们提出了 VLLR，这是一个密集奖励框架，结合了：（1）来自大语言模型（LLM）和视觉语言模型（VLM）的用于任务进度识别的外在奖励，以及（2）基于策略自我确定性（self-certainty）的内在奖励。VLLR 利用 LLM 将任务分解为可验证的子任务，并使用 VLM 估计进度以在简短的预热阶段初始化价值函数，从而避免了完整训练过程中高昂的推理成本；而自我确定性则在整个 PPO 微调过程中提供逐步的内在指导。消融研究揭示了互补的优势：基于 VLM 的价值初始化主要提高了任务完成效率，而自我确定性主要提升了成功率，特别是在分布外（out-of-distribution）任务上。在涵盖移动操作和导航的 CHORES 基准测试中，VLLR 相比预训练策略实现了高达 56% 的绝对成功率提升，在分布内任务上相比最先进的 RL 微调方法提升了高达 5%，在分布外任务上提升了高达 10%，且无需任何手动奖励工程。更多可视化结果见 https://silongyong.github.io/vllr_project_page/。

## Abstract
Existing robotic foundation policies are trained primarily via large-scale imitation learning. While such models demonstrate strong capabilities, they often struggle with long-horizon tasks due to distribution shift and error accumulation. While reinforcement learning (RL) can finetune these models, it cannot work well across diverse tasks without manual reward engineering. We propose VLLR, a dense reward framework combining (1) an extrinsic reward from Large Language Models (LLMs) and Vision-Language Models (VLMs) for task progress recognition, and (2) an intrinsic reward based on policy self-certainty. VLLR uses LLMs to decompose tasks into verifiable subtasks and then VLMs to estimate progress to initialize the value function for a brief warm-up phase, avoiding prohibitive inference cost during full training; and self-certainty provides per-step intrinsic guidance throughout PPO finetuning. Ablation studies reveal complementary benefits: VLM-based value initialization primarily improves task completion efficiency, while self-certainty primarily enhances success rates, particularly on out-of-distribution tasks. On the CHORES benchmark covering mobile manipulation and navigation, VLLR achieves up to 56% absolute success rate gains over the pretrained policy, up to 5% gains over state-of-the-art RL finetuning methods on in-distribution tasks, and up to $10\%$ gains on out-of-distribution tasks, all without manual reward engineering. Additional visualizations can be found in https://silongyong.github.io/vllr_project_page/

---

## 论文详细总结（自动生成）

以下是对论文《Generalizable Dense Reward for Long-Horizon Robotic Tasks》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：现有的机器人基础策略（Foundation Policies）主要依赖大规模模仿学习（IL），但在处理**长程任务**（Long-Horizon Tasks）时，容易因误差累积和分布偏移导致失败。
*   **核心挑战**：强化学习（RL）虽能微调模型，但面临“奖励设计难题”。稀疏奖励（仅在任务结束时提供）导致信用分配困难；而人工设计的密集奖励既脆弱又难以泛化到不同任务。
*   **整体含义**：本文旨在开发一种无需人工干预、具备通用性的密集奖励框架，通过结合大模型（LLM/VLM）的语义理解能力与策略自身的内在确定性，提升机器人在复杂长程任务中的成功率和效率。

### 2. 方法论：核心思想与关键技术
论文提出了 **VLLR (Vision-Language-Long-horizon Reward)** 框架，其核心由三个部分和两个阶段组成：

*   **核心思想**：将奖励建模视为揭示“潜在任务层级”的过程，结合外部语义监督（子目标进度）和内在动作引导（策略自确定性）。
*   **关键技术细节**：
    1.  **外部奖励（LLM+VLM）**：
        *   **任务分解**：利用 LLM（如 Claude-3.7）结合环境场景图（Scene Graph），将自然语言指令分解为有序的子目标序列。
        *   **进度估计**：利用 VLM（如 Nova Pro）对比当前与前一帧观察，估计任务进度 $p_t$。
        *   **噪声过滤**：提出一种“早熟饱和预防算法”（Algo 1），通过中值滤波消除 VLM 幻觉产生的虚假进度峰值。
    2.  **内在奖励（策略自确定性）**：
        *   **公式**：$R_{SC} = -\frac{1}{|A|} \sum_{i=1}^{|A|} \log(|A| \cdot \pi_\theta(i))$。该指标衡量动作分布的集中程度。
        *   **逻辑**：当策略的内在世界模型与当前观察一致时，动作分布更“尖锐”，这可作为任务进展的代理信号。
    3.  **两阶段优化流程**：
        *   **阶段 I（价值初始化）**：仅在最初的 200k 步中使用 VLM 奖励来预热价值函数（Critic），避免了全过程调用 VLM 带来的高昂推理成本。
        *   **阶段 II（策略微调）**：使用 PPO 算法，结合内在奖励 $R_{SC}$ 和稀疏的任务成功奖励进行训练。

### 3. 实验设计
*   **数据集/场景**：使用 **CHORES** 基准测试，基于 ProcThor 模拟器（包含约 19 万间房屋）。
*   **任务类型**：
    *   **分布内（ID）任务**：物体导航（ObjNav）、抓取（Pick-up）、取物（Fetch）、房间访问（RoomVisit）。
    *   **分布外（OOD）任务**：属性关联导航（ObjNavRel）、功能性导航（ObjNavAff）。
*   **对比方法**：
    *   **IL 基础模型**：SPOC。
    *   **SOTA RL 微调**：FLaRe。
    *   **其他 RL 基准**：PIRLNav、JSRL、Poliformer、SPOC++。
*   **评价指标**：成功率（Success Rate）和路径长度加权成功率（SEL）。

### 4. 资源与算力
*   **训练时长/步数**：
    *   价值函数初始化：200k 步。
    *   总训练步数：导航类任务 20M 步，涉及精细操作的任务（如 Fetch, Pick-up）及 OOD 任务为 50M 步。
*   **模型使用**：任务分解使用 Claude-3.7-Sonnet，进度估计使用 Amazon Nova Pro（选其原因是推理延迟低）。
*   **硬件说明**：文中**未明确说明**具体的 GPU 型号和数量，但强调了通过两阶段设计降低了对 VLM 推理算力的依赖。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 类分布内任务和 2 类分布外任务，并在超过 1.7 万个未见过的测试环境中进行评估。
*   **消融实验**：针对“仅稀疏奖励”、“仅自确定性奖励”、“仅 VLM 奖励”以及“完整 VLLR”进行了对比。
*   **充分性评价**：实验设计较为充分。通过跨任务（导航 vs 操作）和跨分布（ID vs OOD）的对比，验证了方法的通用性。消融实验清晰地展示了 VLM 负责提升效率、自确定性负责提升成功率的互补关系。

### 6. 主要结论与发现
*   **性能飞跃**：VLLR 相比纯模仿学习策略，成功率提升高达 **56%**；相比最先进的 RL 微调方法（FLaRe），在 ID 任务上提升 **5%**，在 OOD 任务上提升 **10%**。
*   **效率提升**：VLM 提供的语义引导显著提高了任务完成的效率（SEL 指标提升明显）。
*   **泛化能力**：即使在预训练数据中未出现的任务（如寻找“最大的苹果”），VLLR 也能通过 LLM 的推理能力提供有效的奖励引导。

### 7. 优点
*   **自动化与通用性**：完全摆脱了对人工编写奖励函数或特权环境信息的依赖。
*   **计算友好**：通过“价值初始化”策略，巧妙解决了大模型在线推理延迟高、成本贵的问题。
*   **鲁棒性设计**：针对 VLM 常见的识别错误和幻觉，设计了专门的滤波算法，确保了奖励信号的稳定性。

### 8. 不足与局限
*   **动作空间限制**：目前主要在离散动作空间（Discrete Action Space）上验证，尚未在连续控制任务中进行测试。
*   **VLM 依赖性**：奖励质量高度依赖于所选 VLM 的感知能力，若 VLM 无法识别特定物体，外部奖励将失效。
*   **应用范围**：实验仅限于模拟环境（CHORES/ProcThor），尚未展示在真实物理机器人上的迁移效果。

（完）
