---
title: "Open-Loop Planning, Closed-Loop Verification: Speculative Verification for VLA"
title_zh: 开环规划，闭环验证：面向 VLA 的投机验证
authors: "Zihua Wang, Zhitao Lin, Ruibo Li, Yu Zhang, Xu Yang, Siya Mi, Xiu-Shen Wei"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02965v1"
tags: ["query:rob-il"]
score: 10.0
evidence: 操纵任务中VLA模型的闭环验证
tldr: 针对视觉-语言-动作（VLA）模型推理成本高且开环执行鲁棒性差的问题，本文提出SV-VLA框架。该框架利用大型VLA模型作为低频宏观规划器生成动作块，并引入轻量级验证器进行高频闭环监控。验证器根据当前观测和规划上下文判断是否需要重新规划，从而在保持高效推理的同时，显著提升了机器人在动态环境中的执行可靠性。
selection_source: fresh_fetch
motivation: 现有的VLA模型动作分块技术虽能降低计算开销，但因缺乏闭环反馈，难以应对环境变化和误差累积。
method: 提出一种投机验证框架，由重型VLA模型生成动作序列，并由轻量级验证器实时比对计划动作与闭环参考动作，仅在必要时触发重规划。
result: 实验证明SV-VLA在动态环境中兼具动作分块的高效性和闭环控制的鲁棒性。
conclusion: SV-VLA通过结合长程规划与轻量级在线验证，为实现高效且可靠的具身智能控制提供了一种新方案。
---

## 摘要
视觉-语言-动作（VLA）模型作为具身控制的大型基础模型，在操作任务中展现出强大的性能。然而，其高性能是以高昂的推理成本为代价的。为了提高效率，近期的方法采用了动作分块（action chunking）技术，即预测一系列未来动作以进行开环执行。尽管开环执行在减少计算量方面行之有效，但由于缺乏闭环反馈，它对环境变化十分敏感，且容易产生误差累积。为了解决这一局限性，我们提出了面向 VLA 控制的投机验证（SV-VLA）框架，该框架将高效的开环长程规划与轻量级的闭环在线验证相结合。具体而言，SV-VLA 使用重型 VLA 作为低频宏观规划器来生成动作块及规划上下文，同时利用轻量级验证器根据最新观测结果持续监控执行过程。在当前观测和规划上下文的共同约束下，验证器将已规划的动作与闭环参考动作进行对比，并仅在必要时触发重新规划。实验表明，SV-VLA 结合了分块预测的高效性与闭环控制的鲁棒性，能够在动态环境中实现高效且可靠的基于 VLA 的控制。代码见：https://github.com/edsad122/SV-VLA。

## Abstract
Vision-Language-Action (VLA) models, as large foundation models for embodied control, have shown strong performance in manipulation tasks. However, their performance comes at high inference cost. To improve efficiency, recent methods adopt action chunking, which predicts a sequence of future actions for open-loop execution. Although effective for reducing computation, open-loop execution is sensitive to environmental changes and prone to error accumulation due to the lack of close-loop feedback. To address this limitation, we propose Speculative Verification for VLA Control (SV-VLA), a framework that combines efficient open-loop long-horizon planning with lightweight closed-loop online verification. Specifically, SV-VLA uses a heavy VLA as a low-frequency macro-planner to generate an action chunk together with a planning context, while a lightweight verifier continuously monitors execution based on the latest observations. Conditioned on both the current observation and the planning context, the verifier compares the planned action against a closed-loop reference action and triggers replanning only when necessary. Experiments demonstrate that SV-VLA combines the efficiency of chunked prediction with the robustness of closed-loop control, enabling efficient and reliable VLA-based control in dynamic environments. Code is available: https://github.com/edsad122/SV-VLA.

---

## 论文详细总结（自动生成）

以下是对论文《Open-Loop Planning, Closed-Loop Verification: Speculative Verification for VLA》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉-语言-动作（VLA）模型虽然强大，但推理延迟高，难以实现高频闭环控制。
*   **研究背景**：
    *   **动作分块（Action Chunking）**：通过一次推理预测多个动作来提高效率，但属于“开环执行”，无法应对环境扰动，且存在误差累积。
    *   **投机采样（Speculative Decoding）**：在NLP中有效，但在机器人领域失效，因为验证未来动作需要尚未发生的“未来观测”。
*   **研究动机**：开发一种既能享受动作分块的高效性，又能具备闭环控制鲁棒性的框架，实现“昂贵但低频的规划”与“廉价但高频的验证”相结合。

### 2. 方法论：SV-VLA 框架
核心思想是将控制过程解耦为**宏观规划器**和**轻量级验证器**。
*   **宏观规划器（重型 VLA）**：在规划边界（如任务开始或触发重规划时）调用。输入当前观测、指令和状态，输出一个动作块（如 64 个动作）以及一个**规划上下文特征（Context Feature）**。
*   **轻量级验证器（轻量级模块）**：以控制频率运行。
    *   **特征融合**：利用轻量级视觉骨干网络（如 ViT-Tiny）提取当前实时观测特征，并与宏观规划器提供的上下文特征融合。
    *   **参考动作预测**：预测一个“参考动作”，用于评估原计划动作的有效性。
*   **基于偏差的重规划机制（Deviation-based Replanning）**：
    *   计算当前计划执行的动作与验证器预测的参考动作之间的 L1 距离。
    *   **判定逻辑**：若偏差低于阈值 $\tau$，继续执行原计划；若偏差超过 $\tau$，判定为执行漂移或环境改变，立即丢弃剩余动作块，触发重型 VLA 进行重规划。

### 3. 实验设计
*   **数据集/场景**：使用 **LIBERO** 仿真基准测试，包含三个子任务集：LIBERO-Goal（目标）、LIBERO-Spatial（空间关系）、LIBERO-Object（物体类别）。
*   **基准模型（Backbone）**：以 OpenVLA-OFT（分块大小 $K=64$）作为重型 VLA。
*   **对比方法**：
    1.  **BASE (K=8)**：短分块开环基准（较准但慢）。
    2.  **BASE (K=64)**：长分块开环基准（快但易失败）。
    3.  **Speculative Decoding**：传统的投机采样方法。
*   **评估指标**：任务成功率、推理加速比、每回合重规划次数。

### 4. 资源与算力
*   **训练算力**：使用了 **4 台 NVIDIA A100 GPU** 进行训练。
*   **评估算力**：在 **1 台 NVIDIA V100 GPU** 上进行在线测试，以确保推理延迟评估的公平性。
*   **超参数**：AdamW 优化器，学习率 $8 \times 10^{-4}$，Batch Size 为 64。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 LIBERO 的三大任务集，进行了大量的在线 Rollout 测试。
*   **消融实验**：非常充分，分别验证了“规划上下文特征”、“实时观测输入”、“重规划机制”以及“偏差阈值 $\tau$”对性能的影响。
*   **客观性**：通过对比不同分块长度的基准模型，客观展示了 SV-VLA 在速度和精度之间的折中能力。实验结果显示，SV-VLA 在保持较高加速比的同时，将平均成功率从 79.5% 提升至 90.9%。

### 6. 主要结论与发现
*   **性能提升**：SV-VLA 显著优于长分块开环模型，成功率提升了 11.4%，且比短分块模型快 2.17 倍。
*   **验证器的作用**：轻量级验证器能有效识别执行失败（如抓取落空），并及时触发重规划，避免了开环执行中的“盲目”操作。
*   **效率平衡**：验证器的推理成本极低（约 0.081s），远低于重型 VLA（约 1.373s），证明了“低频规划+高频验证”的经济性。

### 7. 优点与亮点
*   **架构解耦**：设计巧妙，解决了机器人领域无法直接应用投机采样的难题。
*   **即插即用**：重型 VLA 和视觉编码器在训练验证器时保持冻结，易于适配现有的预训练 VLA 模型。
*   **灵活性**：通过调节阈值 $\tau$，用户可以根据任务对鲁棒性的需求灵活调整推理频率。

### 8. 不足与局限
*   **阈值敏感性**：目前使用固定阈值 $\tau$，但在不同复杂度的任务中，最优阈值可能不同，缺乏自适应调整机制。
*   **纠错能力有限**：验证器目前仅用于“触发重规划”，而不能直接进行微小的局部动作修正（Local Correction）。
*   **环境局限**：实验主要在仿真环境（LIBERO）中完成，尚未在具有更复杂动力学和光影变化的真实物理世界中进行大规模验证。

（完）
