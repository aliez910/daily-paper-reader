---
title: Learning Structured Robot Policies from Vision-Language Models via Synthetic Neuro-Symbolic Supervision
title_zh: 通过合成神经符号监督从视觉语言模型中学习结构化机器人策略
authors: "Alessandro Adami, Tommaso Tubaldo, Marco Todescato, Ruggero Carli, Pietro Falco"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02812v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 通过视觉语言模型将多模态观测映射到机器人行为
tldr: 本研究针对端到端视觉语言模型在机器人策略中缺乏透明度的问题，提出了一种神经符号化方法。通过自动化的合成数据生成流程，训练VLM从视觉观察和自然语言指令中直接合成可执行的行为树策略。该方法结合了基础模型的高维感知能力与符号控制的可解释性，并在真实机器人实验中验证了其从合成数据到物理系统的有效迁移能力。
selection_source: fresh_fetch
motivation: 旨在解决当前端到端机器人策略不透明、难以分析的问题，通过引入结构化表示提升系统的可解释性与安全性。
method: 提出一种神经符号框架，利用自动化流水线生成合成的多模态数据集，训练VLM根据视觉和语言输入合成可执行的行为树策略。
result: 在两种机器人机械臂上的实验证明，完全基于合成数据训练的结构化策略能够成功迁移并应用于真实物理环境。
conclusion: 基础模型可被适配以生成可解释的结构化策略，为多模态机器人决策提供了一种优于传统黑盒模型的新方案。
---

## 摘要
视觉语言模型（VLM）最近在将多模态观测映射到机器人行为方面展现出了强大的能力。然而，目前大多数方法依赖于端到端的视觉运动策略，这些策略仍然是不透明且难以分析的，限制了它们在安全关键型机器人应用中的使用。相比之下，经典的机器人系统通常依赖于结构化的策略表示，这些表示提供了可解释性、模块化和反应式执行。本研究探讨了如何使基础模型专门化，以生成基于多模态感知的结构化机器人策略，从而架起高维学习与符号控制之间的桥梁。我们提出了一种神经符号方法，其中 VLM 根据视觉观测、自然语言指令和结构化系统规范合成可执行的行为树（Behavior Tree）策略。为了在无需人工标注的情况下实现可扩展的监督，我们引入了一个自动化流水线，该流水线生成了一个合成多模态数据集，其中包含领域随机化的场景以及由基础模型生成的指令-策略示例。在两台机器人机械臂上进行的真实世界实验表明，完全从合成监督中学习到的结构化策略可以成功迁移到物理系统。结果表明，基础模型可以经过调整以产生可解释且结构化的机器人策略，为多模态机器人决策提供了一种替代不透明端到端方法的新方案。

## Abstract
Vision-language models (VLMs) have recently demonstrated strong capabilities in mapping multimodal observations to robot behaviors. However, most current approaches rely on end-to-end visuomotor policies that remain opaque and difficult to analyze, limiting their use in safety-critical robotic applications. In contrast, classical robotic systems often rely on structured policy representations that provide interpretability, modularity, and reactive execution. This work investigates how foundation models can be specialized to generate structured robot policies grounded in multimodal perception, bridging high-dimensional learning and symbolic control. We propose a neuro-symbolic approach in which a VLM synthesizes executable Behavior Tree policies from visual observations, natural language instructions, and structured system specifications. To enable scalable supervision without manual annotation, we introduce an automated pipeline that generates a synthetic multimodal dataset of domain-randomized scenes paired with instruction-policy examples produced by a foundation model. Real-world experiments on two robotic manipulators show that structured policies learned entirely from synthetic supervision transfer successfully to physical systems. The results indicate that foundation models can be adapted to produce interpretable and structured robot policies, providing an alternative to opaque end-to-end approaches for multimodal robot decision making.

---

## 论文详细总结（自动生成）

以下是对论文《Learning Structured Robot Policies from Vision-Language Models via Synthetic Neuro-Symbolic Supervision》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的视觉-语言-动作模型（VLA）大多采用端到端的黑盒架构，输出低级控制指令，导致决策过程不可解释、难以分析且难以施加安全约束，不适用于安全关键型机器人任务。
*   **研究动机**：
    *   **弥合差距**：结合视觉语言模型（VLM）的高维感知能力与行为树（Behavior Trees, BTs）的模块化、可解释性和反应式执行优势。
    *   **解决标注瓶颈**：通过自动化流水线生成合成数据，解决机器人领域高质量、大规模标注数据匮乏的问题。
    *   **神经符号集成**：探索如何让基础模型内化机器人策略的结构化先验，生成可执行的符号化策略而非直接的运动指令。

### 2. 方法论
*   **核心思想**：提出一种神经符号监督框架，利用大型基础模型作为“教师”，在合成环境下生成“指令-行为树”对，随后微调轻量级VLM（Pixtral-12B）直接从图像和指令合成JSON格式的行为树。
*   **关键技术细节**：
    *   **合成数据流水线**：使用 MuJoCo 物理引擎生成10,000个领域随机化的桌面场景，提取对象位姿作为符号元数据。
    *   **自动化标注**：利用 Gemini 3 Flash 模型根据场景元数据生成自然语言指令及对应的结构化行为树（JSON格式）。
    *   **结构化先验约束**：在生成目标中强制执行“反应式守护”（如在动作前检查条件）、“空间偏移计算”（自动计算抓取前的接近点位姿）和“扁平化层级约束”（防止逻辑过深导致执行错误）。
    *   **原子基元分解**：将复杂任务分解为 `MovePose`、`OpenGripper`、`is_grasped` 等原子动作和条件检查，提高可解释性和鲁棒性。

### 3. 实验设计
*   **数据集/场景**：
    *   **训练集**：10,000个完全合成的 MuJoCo 桌面场景。
    *   **测试场景**：包含真实世界的桌面环境，涉及不同颜色、形状的几何体。
*   **Benchmark 与对比方法**：
    *   **对比模型**：GPT-5 和 Gemini 3 Flash（在 Zero-shot 和 One-shot 模式下）。
    *   **消融实验**：针对 LoRA 秩（Rank）、微调模块（仅 Attention 或包含 MLP）、上下文窗口长度、学习率、数据集规模等进行了17组变体实验。
*   **验证平台**：在 Franka Emika Panda（7自由度）和 UR5e（6自由度）两款真实机械臂上进行跨平台验证。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A40 GPU (48 GiB VRAM)**。
*   **训练细节**：
    *   采用 8-bit 量化优化器（Paged AdamW）和 Bfloat16 精度。
    *   使用 LoRA 适配器进行微调。
    *   **时长**：对于10,000个样本，训练时长约为 **15.12 小时**。
    *   **上下文限制**：最大上下文窗口设为 4,400 tokens 以防止显存溢出。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 **17组消融实验**，系统地分析了超参数对 JSON 语法正确率（JVR）、任务成功率（TSR）和逻辑深度的影响。
    *   在真实机器人上进行了多轮测试，验证了从合成数据到真实环境的 **Zero-shot 迁移**能力。
*   **充分性与公平性**：实验覆盖了从语法有效性到物理执行成功率的多个维度。通过对比不同参数规模和提示词策略（Zero-shot vs One-shot），客观展示了微调模型在特定结构化任务上的优势。

### 6. 主要结论与发现
*   **合成监督有效性**：完全在合成数据上训练的模型能够成功迁移到真实物理系统，证明了“逻辑迁移”优于单纯的“视觉迁移”。
*   **结构化先验的内化**：通过微调，12B 规模的模型可以内化复杂的机器人语法约束（如 z-offset 计算），在无需 One-shot 示例的情况下达到甚至超过 GPT-5 等超大规模模型的表现。
*   **硬件无关性**：生成的符号化行为树作为抽象层，使得同一套策略无需修改即可在不同运动学特性的机器人（Panda 和 UR5e）上运行。

### 7. 优点
*   **高可解释性**：输出的行为树每一步逻辑都清晰可见，便于安全审计和运行时监控。
*   **低成本扩展**：无需人工演示或标注，通过物理引擎和教师模型即可无限生成训练数据。
*   **反应式执行**：相比线性动作序列，行为树具备天然的闭环反馈能力，能处理环境扰动。
*   **部署友好**：微调后的 12B 模型可在边缘设备上运行，且输出格式严格遵循 JSON 规范。

### 8. 不足与局限
*   **语义泛化边界**：在处理未见过的复杂语义任务（如“交换两个物体位置”）时，模型会出现“模式倒退”，无法自主推导出需要中间缓冲位的复杂逻辑。
*   **功能接地差距**：模型对新工具（如吸盘）的物理特性缺乏零样本理解，虽然能学会新词汇，但不知道吸取前必须先接触物体。
*   **依赖感知后端**：目前假设对象位姿由外部感知系统提供，尚未实现完全端到端的像素级符号接地。

（完）
