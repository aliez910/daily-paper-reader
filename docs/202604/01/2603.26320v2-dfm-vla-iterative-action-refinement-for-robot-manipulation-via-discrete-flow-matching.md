---
title: "DFM-VLA: Iterative Action Refinement for Robot Manipulation via Discrete Flow Matching"
title_zh: DFM-VLA：通过离散流匹配实现机器人操作的迭代动作细化
authors: "Jiayi Chen, Wenxuan Song, Shuai Chen, Jingbo Wang, Zhijun Li, Haoang Li"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26320v2"
tags: ["query:rob-il"]
score: 10.0
evidence: 用于机器人操纵的视觉-语言-动作 (VLA) 模型
tldr: 针对现有视觉-语言-动作（VLA）模型在离散动作生成中无法纠正早期错误的问题，本文提出了DFM-VLA。该模型利用离散流匹配技术，通过建模动作Token级的概率速度场，实现了对完整动作序列的迭代优化。结合两阶段解码策略，DFM-VLA在CALVIN和LIBERO等基准测试及真实场景中表现优异，显著提升了机器人操作的成功率和鲁棒性，同时保持了较高的推理效率。
selection_source: fresh_fetch
motivation: 现有的自回归或离散扩散VLA模型在生成动作Token后无法对其进行修正，导致早期生成的错误会严重影响最终的操作性能。
method: 提出一种基于离散流匹配的VLA框架，通过建模概率速度场来迭代更新动作序列，并采用包含迭代细化与确定性验证的两阶段解码策略。
result: "在CALVIN任务中实现4.44的平均成功长度，在LIBERO中达到95.7%的成功率，性能显著优于自回归和扩散模型等强基准。"
conclusion: 离散流匹配技术通过迭代细化动作Token，为提升机器人操作任务的准确性和鲁棒性提供了一种高效且极具潜力的解决方案。
---

## 摘要
采用离散分词方案对动作进行编码的视觉-语言-动作（VLA）模型正越来越多地被应用于机器人操作，但现有的解码范式仍存在根本性的局限。无论是通过自回归 VLA 进行顺序解码，还是通过离散扩散 VLA 进行并行解码，一旦生成一个 token，它通常就是固定的，无法在后续迭代中进行修改，因此早期的 token 错误无法在后期得到有效纠正。我们提出了 DFM-VLA，一种用于动作 token 迭代细化的离散流匹配 VLA。DFM-VLA 建模了一个 token 级的概率速度场，在细化迭代过程中动态更新整个动作序列。我们研究了两种构建速度场的方法：辅助速度头公式化和动作嵌入引导公式化。我们的框架进一步采用了一种两阶段解码策略，包括一个迭代细化阶段，随后是用于稳定收敛的确定性验证。在 CALVIN、LIBERO 和真实世界操作任务上的大量实验表明，DFM-VLA 在保持高推理效率的同时，在操作性能上始终优于强大的自回归、离散扩散和连续扩散基准模型。特别是，DFM-VLA 在 CALVIN 上实现了 4.44 的平均成功长度，在 LIBERO 上实现了 95.7% 的平均成功率，突显了通过离散流匹配进行动作细化在机器人操作中的价值。项目主页见 https://chris1220313648.github.io/DFM-VLA/。

## Abstract
Vision--Language--Action (VLA) models that encode actions using a discrete tokenization scheme are increasingly adopted for robotic manipulation, but existing decoding paradigms remain fundamentally limited. Whether actions are decoded sequentially by autoregressive VLAs or in parallel by discrete diffusion VLAs, once a token is generated, it is typically fixed and cannot be revised in subsequent iterations, so early token errors cannot be effectively corrected later. We propose DFM-VLA, a discrete flow matching VLA for iterative refinement of action tokens. DFM-VLA~models a token-level probability velocity field that dynamically updates the full action sequence across refinement iterations. We investigate two ways to construct the velocity field: an auxiliary velocity-head formulation and an action-embedding-guided formulation. Our framework further adopts a two-stage decoding strategy with an iterative refinement stage followed by deterministic validation for stable convergence. Extensive experiments on CALVIN, LIBERO, and real-world manipulation tasks show that DFM-VLA consistently outperforms strong autoregressive, discrete diffusion, and continuous diffusion baselines in manipulation performance while retaining high inference efficiency. In particular, DFM-VLA achieves an average success length of 4.44 on CALVIN and an average success rate of 95.7\% on LIBERO, highlighting the value of action refinement via discrete flow matching for robotic manipulation. Our project is available https://chris1220313648.github.io/DFM-VLA/

---

## 论文详细总结（自动生成）

### DFM-VLA：通过离散流匹配实现机器人操作的迭代动作细化

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对当前视觉-语言-动作（VLA）模型在离散动作生成中的**“不可逆承诺”（Irreversible Commitment）**问题展开研究。
*   **背景**：现有的离散 VLA 模型主要分为自回归（AR）和离散扩散（DD）两类。AR 模型按顺序生成 Token，一旦生成便无法更改；DD 模型虽然支持并行，但在早期迭代中产生的错误 Token 在后续步骤中也难以被有效修正。
*   **核心问题**：在复杂的机器人操纵任务中，早期的动作预测错误会随时间累积，导致任务失败。
*   **研究目标**：引入离散流匹配（Discrete Flow Matching）机制，允许模型在推理过程中对整个动作序列进行多次迭代和“撤回”修正，从而提升动作生成的准确性和鲁棒性。

#### 2. 方法论：核心思想与关键技术
DFM-VLA 的核心是将动作生成建模为一个在离散空间上的概率流过程。
*   **核心思想**：通过建模一个 Token 级的**概率速度场（Probability Velocity Field）**，描述 Token 如何随时间从噪声分布演变为目标动作分布。
*   **关键技术细节**：
    *   **速度场构建**：探索了两种方案：
        1.  **辅助速度头（Auxiliary Velocity Head）**：通过额外的预测头从隐藏状态直接预测 Token 替换的速度。
        2.  **动作嵌入引导（Action-Embedding-Guided）**：利用动作 Token 在嵌入空间中的语义距离定义概率路径，推导出动力学最优速度。实验证明此方案收敛更快、性能更强。
    *   **两阶段解码策略**：
        1.  **迭代细化阶段**：利用欧拉离散化进行随机跳转采样，允许模型在全序列范围内修正不确定的 Token。
        2.  **确定性验证阶段**：在最后几步切换为贪婪解码，确保动作序列稳定收敛。
    *   **自适应 KV 缓存**：针对迭代解码的特性，通过余弦相似度动态更新动作部分的 KV 缓存，显著降低推理延迟。

#### 3. 实验设计
论文在仿真环境和真实世界中进行了广泛评估：
*   **数据集/场景**：
    *   **CALVIN**：评估长程（Long-horizon）语言条件下的操纵能力，采用 ABCD→D 设置。
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，测试跨任务的泛化性。
    *   **真实世界**：使用双臂 AgileX 机器人，执行“协作抬锅”、“蔬菜入锅”、“方块放置”等任务。
*   **Benchmark 与对比方法**：
    *   对比了自回归基准（如 RT-1, UniVLA, OpenVLA）、离散扩散基准（如 Dream-VLA, π0-FAST）以及连续扩散模型（如 RDT）。

#### 4. 资源与算力
*   **硬件**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练时长/规模**：
    *   仿真基准（CALVIN/LIBERO）：训练 20k 至 32k 步。
    *   真实世界任务：每个任务训练 5k 步。
    *   批量大小（Batch Size）为 8。

#### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **多维度评估**：涵盖了长程任务成功率、跨任务泛化率、真实场景成功率。
*   **消融实验**：详细探讨了调度参数（$c, \alpha$）、两阶段解码的步数分配（$T_{fine}$ vs $T_{val}$）、速度场构建方式（Head vs Embed）以及数据规模（10%, 50%, 100%）对性能的影响。
*   **效率分析**：对比了不同解码策略下的推理速度，验证了自适应缓存的有效性。
*   **客观性**：通过在相同骨干网络和数据下对比 AR、DD 和 DFM 范式，确保了比较的公平性。

#### 6. 主要结论与发现
*   **性能领先**：DFM-VLA 在 CALVIN 上实现了 **4.44** 的平均成功长度，在 LIBERO 上达到了 **95.7%** 的平均成功率，均刷新了 SOTA 记录。
*   **修正能力的价值**：实验证实，允许全序列 Token 修正（而非仅填充掩码）能显著提升动作的一致性和准确性。
*   **数据效率**：在低数据量（10% 数据）情况下，DFM-VLA 的表现远超自回归模型，显示出更强的学习效率。
*   **真实世界鲁棒性**：在需要双臂协作的复杂任务中，DFM-VLA 的成功率（77.5%）显著优于连续扩散模型 RDT（65.0%）。

#### 7. 优点
*   **创新性**：首次将离散流匹配引入 VLA 领域，解决了离散动作生成的“不可逆”痛点。
*   **统一性**：保持了视觉、语言、动作在离散 Token 空间内的统一，充分利用了预训练 VLM 的语义能力。
*   **高效性**：通过两阶段解码和自适应缓存，在提升性能的同时，推理速度比传统自回归模型快 2.4 倍。

#### 8. 不足与局限
*   **迭代开销**：尽管比自回归快，但相比于单步生成的策略，DFM 仍需要多次迭代（如 16 步），在极高频控制场景下可能存在压力。
*   **超参数敏感性**：虽然论文给出了推荐配置，但概率路径的调度参数（$c, \alpha$）在不同分布的数据集上可能仍需微调。
*   **依赖骨干网络**：性能在很大程度上受限于底层预训练视觉语言模型的质量。

（完）
