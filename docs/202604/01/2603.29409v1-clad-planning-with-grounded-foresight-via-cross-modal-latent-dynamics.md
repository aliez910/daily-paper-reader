---
title: "CLaD: Planning with Grounded Foresight via Cross-Modal Latent Dynamics"
title_zh: CLaD：通过跨模态潜空间动力学实现具有具身预见性的规划
authors: "Andrew Jeong, Jaemin Kim, Sebin Lee, Sung-Eui Yoon"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29409v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 用于操纵中视觉到动作规划的跨模态潜空间动力学
tldr: "针对机器人操作中运动学与语义转换耦合的问题，本文提出CLaD框架。该框架通过非对称交叉注意力机制建模本体感受与语义状态的联合演化，利用自监督学习预测具有落地感的潜空间预见（foresight）。通过将预测的预见信息与观测结合来引导扩散策略生成动作。在LIBERO-LONG基准测试中，CLaD以极少的参数量实现了94.7%的成功率，性能媲美大型视觉-语言-动作模型（VLA）。"
selection_source: fresh_fetch
motivation: 现有机器人规划方法往往在语义或潜空间中独立进行，缺乏对运动学和语义转换之间耦合关系的显式对齐。
method: 提出一种跨模态潜空间动力学模型，利用非对称交叉注意力让运动学转换查询语义转换，并通过自监督目标预测受观测约束的潜空间预见。
result: "在LIBERO-LONG基准上达到了94.7%的成功率，在参数量显著减少的情况下表现出与大型VLA模型相当的竞争力。"
conclusion: CLaD证明了通过显式建模跨模态动力学并结合扩散策略，可以在轻量化模型上实现高效且精准的长程机器人操作规划。
---

## 摘要
机器人操作涉及运动学和语义转换，这些转换通过底层动作内在耦合。然而，现有方法通常在语义空间或潜空间中进行规划，而没有显式地对齐这些跨模态转换。为此，我们提出了 CLaD 框架，该框架通过非对称交叉注意力机制建模本体感受和语义状态在动作下的共同演化，允许运动学转换查询语义转换。CLaD 通过带有 EMA 目标编码器和辅助重构损失的自监督目标来预测具身的潜空间预见，在防止表示崩溃的同时将预测锚定到可观测状态。预测的预见性结果与观测值进行调制，以作为扩散策略生成动作的条件。在 LIBERO-LONG 基准测试中，CLaD 实现了 94.7% 的成功率，在参数量显著减少的情况下，其性能可与大型视觉-语言-动作（VLA）模型相媲美。

## Abstract
Robotic manipulation involves kinematic and semantic transitions that are inherently coupled via underlying actions. However, existing approaches plan within either semantic or latent space without explicitly aligning these cross-modal transitions. To address this, we propose CLaD, a framework that models how proprioceptive and semantic states jointly evolve under actions through asymmetric cross-attention that allows kinematic transitions to query semantic ones. CLaD predicts grounded latent foresights via self-supervised objectives with EMA target encoders and auxiliary reconstruction losses, preventing representation collapse while anchoring predictions to observable states. Predicted foresights are modulated with observations to condition a diffusion policy for action generation. On LIBERO-LONG benchmark, CLaD achieves 94.7\% success rate, competitive with large VLAs with significantly fewer parameters.

---

## 论文详细总结（自动生成）

这是一份关于论文《CLaD: Planning with Grounded Foresight via Cross-Modal Latent Dynamics》的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：在机器人操作中，本体感受（机器人运动）和语义转换（视觉场景变化）是高度耦合的。现有的规划方法要么生成计算昂贵的语义伪影（如子目标图像或文本），要么在缺乏跨模态理解的单一模态潜空间中规划，这容易导致轨迹在物理或逻辑上不一致。
*   **研究背景**：为了实现高效且准确的长程（Long-horizon）任务规划，需要一种既能捕捉跨模态关联，又能保持计算轻量化的动力学模型。

### 2. 方法论
CLaD 提出了一种两阶段框架，核心思想是**建模跨模态转换的协同演化**：
*   **跨模态潜空间动力学（CLaD）**：
    *   **转换嵌入**：不仅编码当前状态，还结合过去状态和动作序列，提取本体感受和语义的转换表示。
    *   **非对称交叉注意力**：这是关键创新。使用本体感受转换作为查询（Query），去查询语义转换（Key/Value）。这种设计让模型能够通过机器人的运动上下文来解释场景变化。
*   **具身潜空间预见（Grounded Latent Foresight）**：
    *   **自监督预测**：利用 EMA（指数移动平均）目标编码器生成稳定的预测目标，防止潜空间表示崩溃。
    *   **双重锚定机制**：通过潜空间预测损失（$L_{latent}$）和辅助重构损失（$L_{recon}$）确保预测结果锚定在可观测的物理量上，避免表示漂移。
*   **扩散策略控制**：
    *   将预测的未来潜空间状态（预见）作为条件，通过 FiLM（特征线性调制）层与当前观测融合，引导扩散策略生成动作序列。

### 3. 实验设计
*   **基准测试**：主要在 **LIBERO-LONG** 基准上进行，包含 10 个需要多步顺序推理的长程操作任务。此外在补充材料中测试了 LIBERO-Spatial、Object 和 Goal。
*   **对比方法**：
    *   **语义规划类**：SuSIE（图像编辑扩散模型）、Seer（预测逆动力学）。
    *   **潜空间规划类**：LBP（潜空间后向规划）。
    *   **大型 VLA 模型**：OpenVLA (7B)、$\pi_0$ (3.3B)、$\pi_{0.5}$ (3.3B)。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 4090 GPU**。
*   **训练时长**：
    *   第一阶段（动力学模型）：约 2 小时（25K steps）。
    *   第二阶段（扩散策略）：约 20 小时（200K steps）。
    *   总计约 22 小时。
*   **模型规模**：总参数量 **0.66B**（其中 VLM 0.1B，CLaD 0.33B，策略网络 0.23B）。
*   **推理效率**：推理频率达 **25Hz**，显存占用仅 **4GB**。

### 5. 实验数量与充分性
*   **实验规模**：针对 10 个长程任务进行了评估，每项任务进行 20 至 50 次测试（Rollouts）。
*   **消融实验**：非常充分，涵盖了：
    *   模态贡献分析（证明了跨模态优于单模态）。
    *   重构损失 $L_{recon}$ 的必要性（证明了其对防止表示漂移的关键作用）。
    *   交叉注意力设计（对比了对称与非对称结构）。
    *   动作掩码策略的影响。
*   **客观性**：通过与参数量大得多的 SOTA 模型（如 7B 的 OpenVLA）在相同基准下对比，证明了其参数效率。

### 6. 主要结论与发现
*   **性能卓越**：CLaD 以 0.66B 的参数量实现了 **94.7%** 的成功率，超过了 7B 的 OpenVLA（93.8%）和 3.3B 的 $\pi_{0.5}$（93.2%）。
*   **跨模态耦合的重要性**：实验表明，仅靠本体感受预见会严重损害性能，而结合语义预见能显著提升长程任务的稳定性。
*   **归纳偏置有效性**：非对称交叉注意力（运动查询语义）被证明是提取任务相关动力学最有效的归纳偏置。

### 7. 优点
*   **参数高效性**：用不到 1/10 的参数量达到了超大规模 VLA 模型的性能。
*   **计算低开销**：避免了生成高分辨率图像或文本的计算负担，适合实时部署。
*   **落地感强**：通过辅助重构损失，确保了潜空间预测不会脱离实际物理观测。
*   **鲁棒性**：在处理感知模糊（如相似物体）的任务中表现优于其他潜空间规划方法。

### 8. 不足与局限
*   **精细感知局限**：紧凑的潜空间表示可能无法完全捕捉极度精细的操作细节（如任务 9 中两个锅的精确对齐，成功率相对较低）。
*   **泛化能力上限**：虽然在长程规划上很强，但在需要大量先验知识的零样本泛化（如 LIBERO-Object/Goal）上，仍略逊于经过海量数据预训练的巨型 VLA 模型。
*   **训练流程**：两阶段训练虽然解耦了规划与控制，但仍需一定的训练时间，未来可探索在大规模异构数据集上的预训练。

（完）
