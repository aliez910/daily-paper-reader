---
title: Efficient Camera Pose Augmentation for View Generalization in Robotic Policy Learning
title_zh: 机器人策略学习中面向视角泛化的高效相机位姿增强
authors: "Sen Wang, Huaiyi Dong, Jingyi Tian, Jiayi Li, Zhuo Yang, Tongtong Cao, Anlin Chen, Shuang Wu, Le Wang, Sanping Zhou"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29192v1"
tags: ["query:rob-il"]
score: 8.0
evidence: 利用3D高斯泼溅进行视觉运动策略学习以实现视角泛化
tldr: 针对2D视觉运动策略在未知视角下泛化能力差的问题，本文提出GenSplat框架。该框架利用前馈式3D高斯泼溅（3DGS）技术，从稀疏且未校准的输入中快速重建高质量3D场景。通过3D先验蒸馏策略确保几何稳定性，并利用生成的合成视角进行数据增强，使机器人策略能够基于底层3D结构进行决策，显著提升了在剧烈空间扰动下的执行鲁棒性。
selection_source: fresh_fetch
motivation: 现有的2D视觉策略过度依赖静态观测，导致其在面对未见过的相机视角时难以保持动作输出的一致性。
method: 提出一种基于置换等变架构的前馈式3DGS框架，并引入3D先验蒸馏策略以防止几何坍缩，从而实现高效的视角合成与增强。
result: 通过在训练中引入多样化的合成视角，该方法强制策略学习底层3D结构，在严重空间扰动下的表现远超基准模型。
conclusion: 高效的3D场景重建与视角增强技术能显著增强机器人策略的视角泛化能力，确保其在复杂环境下的稳健运行。
---

## 摘要
当前主流的以 2D 为中心的视觉运动策略在面对新视角泛化时表现出显著缺陷，因为它们对静态观测的依赖阻碍了在未见视角下实现连贯的动作映射。针对这一问题，我们提出了 GenSplat，这是一个前馈 3D 高斯泼溅（3D Gaussian Splatting）框架，通过新视角渲染来促进具有视角泛化能力的策略学习。GenSplat 采用置换等变架构，通过单次前向传递即可从稀疏且未校准的输入中重建出高保真 3D 场景。为了确保结构完整性，我们设计了一种 3D 先验蒸馏策略来正则化 3DGS 优化，从而防止纯光度监督中常见的几何崩溃。通过从这些稳定的 3D 表示中渲染多样化的合成视角，我们在训练过程中系统地增强了观测流形。这种增强迫使策略将其决策建立在底层 3D 结构之上，从而确保在基线方法性能严重下降的剧烈空间扰动下仍能实现稳健执行。

## Abstract
Prevailing 2D-centric visuomotor policies exhibit a pronounced deficiency in novel view generalization, as their reliance on static observations hinders consistent action mapping across unseen views. In response, we introduce GenSplat, a feed-forward 3D Gaussian Splatting framework that facilitates view-generalized policy learning through novel view rendering. GenSplat employs a permutation-equivariant architecture to reconstruct high-fidelity 3D scenes from sparse, uncalibrated inputs in a single forward pass. To ensure structural integrity, we design a 3D-prior distillation strategy that regularizes the 3DGS optimization, preventing the geometric collapse typical of purely photometric supervision. By rendering diverse synthetic views from these stable 3D representations, we systematically augment the observational manifold during training. This augmentation forces the policy to ground its decisions in underlying 3D structures, thereby ensuring robust execution under severe spatial perturbations where baselines severely degrade.

---

## 论文详细总结（自动生成）

### 论文总结：GenSplat - 机器人策略学习中面向视角泛化的高效相机位姿增强

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的视觉运动策略（Visuomotor Policies）高度依赖于训练时的静态相机视角。一旦相机发生微小的位姿变动（SE(3) 扰动），策略的成功率就会大幅下降。
*   **研究动机**：为了解决视角泛化问题，传统方法需要人工重新采集大量不同视角的演示数据，成本极高。
*   **整体含义**：本文提出 **GenSplat** 框架，利用前馈式 3D 高斯泼溅（3DGS）技术，从极少数且未校准的观测图像中快速重建 3D 场景，并通过渲染合成的新视角来增强训练数据集，从而在不增加人工采集成本的前提下，显著提升机器人策略对相机位姿变化的鲁棒性。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：将 3D 重建视为一个前馈推理过程，结合 3D 基础模型的几何先验，生成物理一致性强、几何结构稳定的新视角图像。
*   **关键技术细节**：
    *   **置换等变架构（Permutation-Equivariant Architecture）**：采用 Transformer 架构处理稀疏输入，确保对输入图像的顺序不敏感，增强了对未校准相机设置的适应性。
    *   **3D 先验蒸馏（3D-Prior Distillation）**：从预训练的 3D 基础模型（如 $\pi^3$）中提取几何知识。通过回归每像素的 **3D 点图（Point Map）** 而非简单的深度图，来约束 3DGS 的优化，防止在稀疏视角下出现几何坍缩或“漂浮物”伪影。
    *   **位姿无关的预训练**：通过蒸馏相机位姿先验，实现相对仿射不变的变换监督，解决了机器人数据集通常缺乏精确相机外参的问题。
    *   **损失函数**：结合了光度损失（$L_1$ + LPIPS）、深度对齐损失以及 3D 先验损失（点图损失 $L_{3D}$ + 相机位姿损失 $L_{cam}$）。

#### 3. 实验设计
*   **数据集与场景**：
    *   **预训练**：使用大规模野外机器人数据集 **DROID**（约 27.1 万张图像）。
    *   **实机评估**：在 Franka Research 3 机械臂上进行，涵盖 6 个任务（抓取香蕉、堆叠方块、插入圆环、放置罐子、盘中放块、堆叠杯子）。
*   **Benchmark 与对比方法**：
    *   **扩散模型类（Diffusion-based）**：ZeroNVS, VISTA, SEVA。
    *   **高斯泼溅类（Gaussian-based）**：InstantSplat, NoPoSplat, AnySplat。
    *   **策略基准**：$\pi_0$ (VLA 模型) 和 Diffusion Policy。
*   **评估协议**：设置了 Small、Medium、Large 三种等级的相机位姿扰动（旋转最高达 30°，平移最高达 6cm）。

#### 4. 资源与算力
*   **训练细节**：训练迭代 30k 次，Batch Size 为 16，使用余弦退火学习率（峰值 $2e^{-4}$）。
*   **算力优化**：启用了 FlashAttention、梯度检查点（Gradient Checkpointing）和 bfloat16 精度以减少显存占用。
*   **具体硬件**：文中**未明确说明**训练所用的具体 GPU 型号和数量，但提到推理/合成速度为 **0.14s/帧**，远快于扩散模型（如 SEVA 需 50s）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   每个任务收集 100 组专家演示。
    *   实机评估中，每个任务在不同扰动等级下进行 30-50 次测试。
    *   进行了消融实验（验证置换等变架构和 3D 先验的作用）和数据效率实验（验证合成视角数量 $N$ 的影响）。
*   **充分性评价**：实验设计较为充分，涵盖了从底层渲染质量（PSNR/LPIPS）到高层策略成功率的全方位对比。通过多任务、多扰动等级的实机测试，客观地证明了该方法在真实环境中的有效性。

#### 6. 主要结论与发现
*   **性能提升**：在剧烈（Large）扰动下，GenSplat 增强后的策略成功率提升显著（如 Diffusion Policy 提升了 56% 的相对性能）。
*   **几何一致性至关重要**：扩散模型生成的新视角虽然视觉尚可，但由于缺乏严格的几何约束，会导致策略性能下降；而 GenSplat 通过 3DGS 保证了物理一致性。
*   **数据饱和点**：研究发现，每个原始轨迹合成约 3 个新视角即可达到性能饱和，进一步增加视角带来的边际收益递减。

#### 7. 优点
*   **高效性**：前馈推理架构避免了每场景耗时的优化过程，适合大规模数据增强。
*   **无需校准**：能够处理未校准的相机输入，极大地降低了在不同机器人平台上部署的门槛。
*   **高质量渲染**：引入 3D 先验蒸馏，有效解决了传统前馈 3DGS 在稀疏视角下的几何崩溃问题。

#### 8. 不足与局限
*   **动态场景限制**：目前主要针对静态工作空间重建，对于手眼相机（Wrist Camera）在动作过程中的动态遮挡和快速运动几何恢复仍有挑战。
*   **依赖基础模型**：GenSplat 的性能在很大程度上取决于所使用的 3D 几何基础模型的质量。
*   **应用范围**：实验主要集中在桌面级操作任务，对于更大空间尺度或更复杂背景的泛化能力尚待验证。

（完）
