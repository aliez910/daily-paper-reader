---
title: Can Explicit Physical Feasibility Benefit VLA Learning? An Empirical Study
title_zh: 显式物理可行性是否能使 VLA 学习受益？一项实证研究
authors: "Yubai Wei, Chen Wu, Hashem Haghbayan"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17896v1"
tags: ["query:rob-il"]
score: 9.5
evidence: 通过大规模模仿学习训练的VLA模型，并引入物理可行性监督
tldr: 现有的视觉-语言-动作（VLA）模型主要依赖模仿学习，缺乏对物理约束（如避障和运动学可行性）的显式监督。本文探讨了引入显式物理可行性监督是否能为VLA策略提供结构化引导。研究者提出了一种基于几何的可行性目标，并将其整合到扩散式VLA策略的训练中。实验表明，该方法在避障操作任务中显著提升了物理可靠性、任务成功率及低数据量下的学习效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17896v1/fig-001.webp\", \"caption\": \"Fig. 1. Illustration on learning VLA with explicit physical feasibility supervision. The policy is trained to match demonstrated action chunks via LMSE. We additionally map predicted actions through forward kinematics and compute signed distances to the obstacle, producing a feasibility loss Lgeo. The combined objective is Ltotal = LMSE+λLgeo. Obstacle geometry and kinematic computations are used only during training; at inference the policy acts solely on RGB observations and language.\", \"page\": 1, \"index\": 1, \"width\": 512, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2604.17896v1/fig-002.webp\", \"caption\": \"TABLE III EFFECT OF TRAINING DATA SIZE UNDER LARGE PERTURBATIONS (δ=0.10, λ=1.0). THE BENEFIT OF FEASIBILITY SUPERVISION IS LARGEST IN THE LOW-DATA REGIME AND DIMINISHES AS DATA INCREASES.\", \"page\": 7, \"index\": 2, \"width\": 529, \"height\": 247}, {\"url\": \"assets/figures/arxiv/2604.17896v1/fig-003.webp\", \"caption\": \"TABLE IV ABLATION OF SUPERVISION STRENGTH (δ, λ) UNDER LARGE PERTURBATIONS (40 TRAINING EPISODES). MODERATE SUPERVISION ACHIEVES THE BEST BALANCE IN SSR, WHILE OVERLY STRONG SUPERVISION INTRODUCES A SAFETY–ACCURACY TRADE-OFF.\", \"page\": 7, \"index\": 3, \"width\": 529, \"height\": 271}]"
motivation: 现有的VLA模型仅通过模仿学习隐式推断物理约束，导致在复杂几何环境下的动作可靠性不足。
method: 提出一种基于几何的显式物理可行性监督目标，并将其集成到扩散式VLA策略的训练过程中。
result: 实验证明，显式可行性监督不仅提高了机器人的避障能力和任务成功率，还增强了在少量数据下的学习效率。
conclusion: 显式物理可行性信号能有效补充模仿学习，是开发更可靠、更高效的VLA策略的潜力方向。
---

## 摘要
视觉-语言-动作（VLA）模型将多模态输入直接映射到机器人动作，通常通过大规模模仿学习进行训练。虽然这种范式表现出了强大的性能，但现有的 VLA 训练过程并未显式监督硬性物理约束，如避障或运动学可行性。因此，物理可行行为背后的几何结构只能从演示中隐式推断。在本文中，我们研究了引入显式可行性监督是否能为 VLA 策略提供有效的结构化引导。我们制定了一个简单的基于几何的可行性目标，并将其集成到基于扩散的 VLA 策略的训练阶段。为了系统地评估这一想法，我们使用避障操纵作为几何相关物理可行性的受控探针。实验结果表明，通过可行性监督增强 VLA 训练可以提高物理可靠性和整体任务性能，同时还能提高低数据量情况下的学习效率。这些发现表明，显式可行性信号可以有效补充基于模仿的 VLA 学习，突显了其在开发更可靠的 VLA 策略方面的潜力。

## Abstract
Vision-Language-Action (VLA) models map multimodal inputs directly to robot actions and are typically trained through large-scale imitation learning. While this paradigm has shown strong performance, prevailing VLA training procedures do not explicitly supervise hard physical constraints such as obstacle avoidance or kinematic feasibility. As a result, the geometric structure underlying physically feasible behavior must be inferred only implicitly from demonstrations. In this paper, we study whether introducing explicit feasibility supervision can provide effective structured guidance for VLA policies. We formulate a simple geometry-grounded feasibility objective and integrate it into the training stage of a diffusion-based VLA policy. To evaluate this idea systematically, we use obstacle-aware manipulation as a controlled probe of geometry-dependent physical feasibility. Empirical results show that augmenting VLA training with feasibility supervision improves both physical reliability and overall task performance, while also enhancing learning efficiency in the low-data regime. These findings indicate that explicit feasibility signals can effectively complement imitation-based VLA learning, highlighting their potential for developing more reliable VLA policies.

---

## 论文详细总结（自动生成）

这篇论文《Can Explicit Physical Feasibility Benefit VLA Learning? An Empirical Study》探讨了在视觉-语言-动作（VLA）模型中引入显式物理约束监督的效果。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 VLA 模型主要依赖大规模模仿学习（Imitation Learning），通过最小化预测动作与专家演示之间的均方误差（MSE）来学习。然而，这种方式仅能让模型“隐式”地从数据中推断物理约束（如避障、运动学限制）。
*   **研究动机**：在现实部署中，机器人必须满足硬性的物理约束。作者试图探究：如果我们在训练中显式地加入物理可行性（Physical Feasibility）监督信号，是否能提升 VLA 策略的性能、可靠性以及数据效率？

### 2. 方法论
*   **核心思想**：在扩散策略（Diffusion Policy）的训练目标中，除了标准的模仿损失（$L_{MSE}$）外，增加一个基于几何的可行性损失项（$L_{geo}$）。
*   **关键技术细节**：
    *   **正向运动学（FK）映射**：将模型预测的关节空间动作序列通过可微的正向运动学函数映射到工作空间中的机器人连杆位姿。
    *   **有向距离函数（SDF）计算**：将障碍物建模为有向包围盒（OBB），计算机器人关键点与障碍物表面之间的有向距离。
    *   **几何可行性损失**：采用平方铰链损失（Squared Hinge Loss）。当机器人与障碍物的距离小于设定的安全阈值 $\delta$ 时，产生惩罚。
    *   **训练公式**：$L_{total} = L_{MSE} + \lambda L_{geo}$。其中 $\lambda$ 是调节物理监督强度的权重。
*   **推理阶段**：该方法仅在训练时需要障碍物几何信息和运动学模型，**推理时仅需 RGB 图像和语言指令**，不增加部署负担。

### 3. 实验设计
*   **场景与任务**：在 NVIDIA Isaac Sim 仿真环境中设计了“近距离避障到达（Close-obstacle Reaching）”任务。机器人（Franka 手臂）需根据指令绕过障碍物触碰目标。
*   **数据集**：通过合成管道生成了 120 个演示片段，包含 3 视角 RGB 图像、语言指令和专家轨迹。
*   **基准模型（Benchmark）**：以目前最大的开源扩散基础模型 **RDT-1B**（12亿参数）作为骨干网络。
*   **对比方法**：
    1.  **MSE (Baseline)**：仅使用标准模仿学习训练。
    2.  **MSE + Feasibility (Ours)**：结合了显式物理可行性监督的训练。
*   **评估指标**：安全成功率（SSR，同时满足到达精度和避障间距）、最小间距（$d_{min}$）和目标距离（$d_{tgt}$）。

### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA A100 GPU** 进行全参数微调。
*   **训练时长**：
    *   40 个片段：约 8 小时。
    *   80 个片段：约 15 小时。
    *   120 个片段：约 23 小时。
*   **训练参数**：学习率 $1 \times 10^{-4}$，训练 2000 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对不同数据量（40, 80, 120 片段）进行了对比，验证数据效率。
    *   针对障碍物位置进行了“小扰动”（分布内）和“大扰动”（分布外/局部泛化）测试。
    *   进行了消融实验，测试了不同安全阈值 $\delta$ 和损失权重 $\lambda$ 的组合。
*   **充分性评价**：实验设计较为系统，使用了先进的基础模型作为底座，增强了结论的说服力。但在任务多样性上稍显单一（仅限于避障到达任务）。

### 6. 主要结论与发现
*   **性能提升**：显式可行性监督显著提高了安全成功率，且模型在提升安全性的同时，也意外地提升了到达目标的精度。
*   **低数据效率**：在极少量数据（40 个片段）下，该方法的优势最明显，甚至能超过使用更多数据训练的基准模型。
*   **监督强度平衡**：适度的物理监督效果最好；过强的监督会导致模型过于保守，从而在安全性和任务完成度之间产生权衡（Trade-off）。

### 7. 优点
*   **训练简单且推理高效**：通过可微几何计算将物理先验引入神经网络，且不改变推理流程。
*   **结构化引导**：证明了物理先验可以作为模仿学习的有效补充，帮助模型在视觉输入与物理空间之间建立更好的关联。
*   **开源基础模型验证**：在 RDT-1B 这样的大规模模型上验证了有效性，具有较强的参考价值。

### 8. 不足与局限
*   **任务覆盖面窄**：实验仅集中在单臂避障到达任务，未涉及双臂协作、复杂操作或多障碍物环境。
*   **仿真与现实差距**：所有实验均在 Isaac Sim 中完成，尚未在真实物理机器人上验证其泛化能力。
*   **约束定义简单**：目前仅考虑了静态障碍物避障，未涉及自碰撞、关节限位或动力学约束（如速度/加速度限制）。
*   **依赖几何信息**：虽然推理不需要，但训练时仍需准确的障碍物 OBB 信息，这在复杂现实场景的标注中可能存在挑战。

（完）
