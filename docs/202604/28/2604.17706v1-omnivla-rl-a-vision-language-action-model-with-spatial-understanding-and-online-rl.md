---
title: "OmniVLA-RL: A Vision-Language-Action Model with Spatial Understanding and Online RL"
title_zh: OmniVLA-RL：一种具有空间理解和在线强化学习能力的视觉-语言-动作模型
authors: "Haoxiang Jie, Yaoyuan Yan, Xiangyu Wei, Kailin Wang, Hongjie Yan, Zhiyou Heng, Daocheng Chen"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17706v1"
tags: ["query:rob-il"]
score: 10.0
evidence: 具有空间理解和强化学习能力的VLA模型，用于复杂操纵基准
tldr: 针对具身智能中视觉-语言-动作（VLA）模型在空间感知、多模态融合及强化学习稳定性方面的不足，本文提出OmniVLA-RL。该模型采用Mix-of-Transformers架构整合推理、空间与动作专家，并引入Flow-GSPO算法，通过将流匹配重构为随机微分方程并结合分组分段策略优化，显著提升了动作精度与训练鲁棒性，在LIBERO等基准测试中达到领先水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17706v1/fig-001.webp\", \"caption\": \"Figure 4: Comparison of training success rates on the LIBERO-Plus multi-task benchmark. Flow-GSPO exhibits superior convergence speed and higher final success rates compared to PPO and GRPO baselines.\", \"page\": 11, \"index\": 1, \"width\": 954, \"height\": 580}, {\"url\": \"assets/figures/arxiv/2604.17706v1/fig-002.webp\", \"caption\": \"Figure 2: Block-wise Causal Attention mask of OmniVLA-RL. Tokens from the Spatial and Reasoning Experts form an omni-visible prefix with full bidirectional attention among themselves (dark blocks), enabling rich cross-modal alignment between spatial and semantic representations. Action tokens form a causal suffix: each action chunk can attend to the entire prefix but is restricted to attending only to preceding action tokens within the suffix (lower-triangular pattern). Prefix tokens are blocked from attending to action tokens (white blocks), preventing stochastic denoising noise from contaminating scene understanding.\", \"page\": 7, \"index\": 2, \"width\": 392, \"height\": 353}, {\"url\": \"assets/figures/arxiv/2604.17706v1/fig-003.webp\", \"caption\": \"Table 1: Performance comparison on the LIBERO benchmark. Bold indicates best performance.\", \"page\": 10, \"index\": 3, \"width\": 968, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2604.17706v1/fig-004.webp\", \"caption\": \"Figure 1: Overall architecture of OmniVLA-RL. The VLA model adopts a Mixture-of-Transformers (MoT) backbone shared across three experts. The Reasoning Expert (centre) encodes multi-view RGB observations via a Vision Encoder and task instructions via a Text Encoder, producing semantic and linguistic tokens. The Spatial Expert (left) extracts fine-grained 3D structural features from multi-view scenes using a Spatial Encoder, and a lightweight Spatial Decoder is appended as an auxiliary supervision head during training. The Action Expert (right) generates action trajectories autoregressively conditioned on the fused spatial-semantic representations. All three experts share the same Transformer layers and interact via the proposed Block-wise Causal Attention, which treats spatial and semantic tokens as an omni-visible prefix while enforcing causal constraints on action tokens. The online RL module (Flow-GSPO) fine-tunes the entire model by optimising action-block-level policy rewards through stochastic flow matching.\", \"page\": 5, \"index\": 4, \"width\": 869, \"height\": 310}]"
motivation: 现有的VLA模型在精确空间感知、多模态融合效率以及强化学习训练稳定性方面存在明显局限。
method: 提出Mix-of-Transformers架构以协同推理、空间和动作专家，并结合Flow-GSPO算法优化动作生成与策略训练。
result: 在LIBERO和LIBERO-Plus基准测试中，OmniVLA-RL的表现显著优于现有的最先进方法。
conclusion: OmniVLA-RL通过创新的架构设计和强化学习算法，有效克服了当前VLA模型的核心局限性，提升了具身智能任务的执行效果。
---

## 摘要
视觉-语言-动作（VLA）模型代表了具身智能领域的一种范式转变，但现有框架往往面临空间感知不精确、多模态融合欠佳以及强化学习不稳定的挑战。为了弥补这些差距，我们提出了 OmniVLA-RL，这是一种利用混合 Transformer（MoT）设计来协同整合推理、空间和动作专家的创新架构。此外，我们引入了 Flow-GSPO，它将流匹配（flow matching）重新表述为随机微分方程（SDE）过程，并将其与分组分段策略优化（GSPO）相结合，以增强动作精度和训练鲁棒性。在 LIBERO 和 LIBERO-Plus 基准测试上的广泛评估表明，OmniVLA-RL 显著优于现有最先进的方法，有效克服了当前 VLA 模型的根本局限性。

## Abstract
Visual-Language-Action (VLA) models represent a paradigm shift in embodied AI, yet existing frameworks often struggle with imprecise spatial perception, suboptimal multimodal fusion, and instability in reinforcement learning. To bridge these gaps, we propose OmniVLA-RL, a novel architecture that leverages a Mix-of-Transformers (MoT) design to synergistically integrate reasoning, spatial, and action experts. Furthermore, we introduce Flow-GSPO, which reformulates flow matching as a Stochastic Differential Equation (SDE) process and integrates it with Group Segmented Policy Optimization (GSPO) to enhance action precision and training robustness. Extensive evaluations on the LIBERO and LIBERO-Plus benchmarks demonstrate that OmniVLA-RL significantly outperforms state-of-the-art methods, effectively overcoming the fundamental limitations of current VLA models.

---

## 论文详细总结（自动生成）

这是一份关于论文《OmniVLA-RL: A Vision-Language-Action Model with Spatial Understanding and Online RL》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在具身智能任务中存在三大痛点：**空间感知不精确**（难以处理3D位置和尺寸）、**多模态融合效率低**（简单的早期或后期融合未触及大模型核心）、以及**强化学习（RL）训练不稳定**（如PPO过于复杂，GRPO在处理动作序列时易崩溃）。
*   **研究背景**：机器人操作任务需要极高的3D空间精度，而通用视觉语言模型（VLM）往往缺乏这种细粒度的几何理解能力。同时，如何将生成式动作模型（如流匹配）与在线强化学习高效结合也是当前的研究前沿。

### 2. 方法论
OmniVLA-RL 提出了一个统一的框架，核心包含以下关键技术：
*   **混合 Transformer（MoT）架构**：
    *   **推理专家（Reasoning Expert）**：基于预训练 VLM（如 PaLiGemma），负责指令理解和全局语义提取。
    *   **空间专家（Spatial Expert）**：利用 VGGT 编码器提取细粒度 3D 结构特征，并通过辅助监督头（重建点云、相机参数等）进行训练。
    *   **动作专家（Action Expert）**：基于条件流匹配（CFM）生成连续动作轨迹。
*   **块状因果注意力（Block-wise Causal Attention）**：设计了一种特殊的掩码机制。空间和语义 Token 作为“前缀”是全向可见的，而动作 Token 作为“后缀”遵循因果律，且防止动作生成的随机噪声干扰感知层。
*   **Flow-GSPO 算法**：
    *   **随机流匹配**：通过 Fokker-Planck 方程将确定性的 ODE 过程转换为随机微分方程（SDE），引入随机性以支持 RL 探索。
    *   **分组分段策略优化（GSPO）**：将动作块（Action Block）作为最小优化单元，而非 Token 级别，解决了 GRPO 在序列采样中的不稳定性。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO 基准测试**：包含四个子集（Spatial, Object, Goal, Long），评估空间推理、物体操作、目标达成和长程决策。
    *   **LIBERO-Plus**：更具挑战性的扩展版，涉及多阶段操作（如打开抽屉并取出特定物体）。
*   **对比方法**：
    *   **VLA 基准**：Diffusion Policy, Octo, OpenVLA, SpatialVLA, CoT-VLA, $\pi_0$, $\pi_{0.5}$, F1 等。
    *   **RL 算法对比**：PPO 和 GRPO。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到了使用 AdamW 优化器，学习率为 $1 \times 10^{-5}$，RL 阶段每 10 步刷新一次 Rollout Buffer，总共进行 200 次 RL 更新步骤。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 LIBERO 的 4 个子集上进行了全面测试。
    *   在 LIBERO-Plus 上进行了收敛速度和成功率的对比实验。
    *   进行了针对“空间专家”组件和不同 RL 算法（PPO/GRPO/Flow-GSPO）的消融实验。
*   **充分性评价**：实验设计较为充分，涵盖了从基础模仿学习到在线强化学习的性能演进。通过与多种 SOTA（最先进）模型的对比，客观地展示了模型在空间精度和训练稳定性上的优势。

### 6. 主要结论与发现
*   **性能领先**：OmniVLA-RL 在 LIBERO 平均成功率达到 **97.6%**，显著优于 $\pi_{0.5}$ 和 OpenVLA 等模型。
*   **RL 稳定性**：Flow-GSPO 在收敛速度和最终成功率上均优于 PPO 和 GRPO，尤其在长程任务中表现出更强的鲁棒性。
*   **空间专家的必要性**：消融实验显示，移除空间专家会导致性能下降 8.3%，证明了细粒度几何特征对高精度操作至关重要。

### 7. 优点与亮点
*   **架构创新**：MoT 架构实现了感知、推理与动作的深度解耦与高效融合，避免了传统早期/后期融合的局限。
*   **理论突破**：成功将流匹配（Flow Matching）与 GSPO 结合，通过 SDE 转换解决了生成式动作模型在 RL 训练中的梯度回传和探索问题。
*   **注意力机制**：Block-wise Causal Attention 有效隔离了感知特征与动作噪声，保证了环境理解的纯净性。

### 8. 不足与局限
*   **Sim-to-Real 验证缺失**：实验主要在仿真环境（LIBERO）中完成，尚未在真实物理机器人上进行大规模验证，存在“仿真到现实”的迁移风险。
*   **缺乏世界模型**：虽然动作生成很强，但模型目前缺乏一个专门的“世界模型”来预测环境状态转移，这可能限制其在极长程、非结构化环境中的规划能力。
*   **硬件细节不明**：缺乏算力消耗的具体数据，难以评估该模型在实际部署时的资源效率。

（完）
