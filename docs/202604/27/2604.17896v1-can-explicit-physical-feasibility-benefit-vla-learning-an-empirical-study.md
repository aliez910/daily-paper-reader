---
title: Can Explicit Physical Feasibility Benefit VLA Learning? An Empirical Study
title_zh: 显式物理可行性是否能使 VLA 学习受益？一项实证研究
authors: "Yubai Wei, Chen Wu, Hashem Haghbayan"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17896v1"
tags: ["query:rob-il"]
score: 10.0
evidence: 通过大规模模仿学习训练并考虑物理可行性的VLA模型
tldr: 本文探讨了显式物理可行性监督对视觉-语言-动作（VLA）模型学习的影响。针对现有VLA模型仅通过模仿学习隐式推断物理约束的问题，研究者提出了一种基于几何的可行性目标函数，并将其整合进扩散式VLA策略的训练中。通过障碍物感知操作任务的评估，证明了该方法能显著提高物理可靠性、任务成功率及低数据环境下的学习效率，为构建更可靠的机器人策略提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17896v1/fig-001.webp\", \"caption\": \"Fig. 1. Illustration on learning VLA with explicit physical feasibility supervision. The policy is trained to match demonstrated action chunks via LMSE. We additionally map predicted actions through forward kinematics and compute signed distances to the obstacle, producing a feasibility loss Lgeo. The combined objective is Ltotal = LMSE+λLgeo. Obstacle geometry and kinematic computations are used only during training; at inference the policy acts solely on RGB observations and language.\", \"page\": 1, \"index\": 1, \"width\": 512, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2604.17896v1/fig-002.webp\", \"caption\": \"TABLE III EFFECT OF TRAINING DATA SIZE UNDER LARGE PERTURBATIONS (δ=0.10, λ=1.0). THE BENEFIT OF FEASIBILITY SUPERVISION IS LARGEST IN THE LOW-DATA REGIME AND DIMINISHES AS DATA INCREASES.\", \"page\": 7, \"index\": 2, \"width\": 529, \"height\": 247}, {\"url\": \"assets/figures/arxiv/2604.17896v1/fig-003.webp\", \"caption\": \"TABLE IV ABLATION OF SUPERVISION STRENGTH (δ, λ) UNDER LARGE PERTURBATIONS (40 TRAINING EPISODES). MODERATE SUPERVISION ACHIEVES THE BEST BALANCE IN SSR, WHILE OVERLY STRONG SUPERVISION INTRODUCES A SAFETY–ACCURACY TRADE-OFF.\", \"page\": 7, \"index\": 3, \"width\": 529, \"height\": 271}]"
motivation: 现有的VLA模型通常缺乏对避障或运动学可行性等硬性物理约束的显式监督，导致其难以准确掌握物理行为的几何结构。
method: 制定了一个简单的基于几何的可行性目标，并将其作为辅助监督信号集成到基于扩散的VLA策略训练阶段。
result: 实验结果显示，引入可行性监督不仅增强了物理可靠性和任务表现，还显著提升了模型在数据稀缺情况下的学习效率。
conclusion: 显式物理可行性信号可以有效补充基于模仿的VLA学习，对于开发更具鲁棒性和可靠性的机器人策略具有重要价值。
---

## 摘要
视觉-语言-动作（VLA）模型将多模态输入直接映射到机器人动作，通常通过大规模模仿学习进行训练。虽然这种范式表现出了强大的性能，但现有的 VLA 训练程序并未显式监督诸如避障或运动学可行性等硬性物理约束。因此，物理可行行为背后的几何结构只能从演示中隐式推断。在本文中，我们研究了引入显式可行性监督是否能为 VLA 策略提供有效的结构化指导。我们制定了一个简单的基于几何的可行性目标，并将其集成到基于扩散的 VLA 策略的训练阶段。为了系统地评估这一想法，我们使用避障操纵作为几何相关物理可行性的受控探针。实验结果表明，通过可行性监督增强 VLA 训练不仅提高了物理可靠性和整体任务性能，还提升了低数据量情况下的学习效率。这些发现表明，显式可行性信号可以有效补充基于模仿的 VLA 学习，凸显了其在开发更可靠 VLA 策略方面的潜力。

## Abstract
Vision-Language-Action (VLA) models map multimodal inputs directly to robot actions and are typically trained through large-scale imitation learning. While this paradigm has shown strong performance, prevailing VLA training procedures do not explicitly supervise hard physical constraints such as obstacle avoidance or kinematic feasibility. As a result, the geometric structure underlying physically feasible behavior must be inferred only implicitly from demonstrations. In this paper, we study whether introducing explicit feasibility supervision can provide effective structured guidance for VLA policies. We formulate a simple geometry-grounded feasibility objective and integrate it into the training stage of a diffusion-based VLA policy. To evaluate this idea systematically, we use obstacle-aware manipulation as a controlled probe of geometry-dependent physical feasibility. Empirical results show that augmenting VLA training with feasibility supervision improves both physical reliability and overall task performance, while also enhancing learning efficiency in the low-data regime. These findings indicate that explicit feasibility signals can effectively complement imitation-based VLA learning, highlighting their potential for developing more reliable VLA policies.

---

## 论文详细总结（自动生成）

这是一份关于论文《Can Explicit Physical Feasibility Benefit VLA Learning? An Empirical Study》的结构化深度分析报告：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的视觉-语言-动作（VLA）模型主要依赖大规模模仿学习（Imitation Learning），将多模态输入直接映射为动作。然而，这种范式通常缺乏对硬性物理约束（如避障、运动学可行性）的显式监督，模型必须从演示数据中隐式地推断几何结构。
*   **核心问题**：在 VLA 训练中引入**显式的物理可行性监督信号**，是否能提供有效的结构化引导，从而提升模型的可靠性和学习效率？

### 2. 方法论：核心思想与技术细节
论文提出在基于扩散（Diffusion-based）的 VLA 策略训练阶段，引入一个辅助的几何可行性目标函数。
*   **核心思想**：利用训练时可获得的机器人运动学模型和障碍物几何信息，对模型预测的动作序列进行实时物理评估，并通过反向传播引导模型避开非法状态。
*   **关键技术流程**：
    1.  **正向运动学映射（FK）**：将 VLA 预测的关节空间动作序列 $\hat{a}$ 映射到工作空间中机器人各连杆的位姿。
    2.  **有向距离计算（SDF）**：将障碍物建模为导向包围盒（OBB），计算机器人关键点到 OBB 表面的有向距离 $d$。正值表示安全，负值表示穿透。
    3.  **几何可行性损失 ($L_{geo}$)**：采用平方铰链损失（Squared Hinge Loss）。当清除距离小于预设的安全裕度 $\delta$ 时，激活惩罚：
        $L_{geo} = \text{Avg} [ \max(0, \delta - d)^2 ]$
    4.  **联合优化**：总损失函数为 $L_{total} = L_{MSE} + \lambda L_{geo}$。
*   **推理特性**：该几何监督仅在训练时使用。在推理阶段，策略依然只依赖 RGB 图像和语言指令，不增加额外的感知或计算开销。

### 3. 实验设计
*   **场景与任务**：在 NVIDIA Isaac Sim 仿真环境中设计了“近距离避障到达（Close-obstacle Reaching）”任务。机器人需根据指令（如“绕过绿色障碍物抓取黑色物体”）到达目标。
*   **数据集**：通过合成管道生成了 120 个高质量演示片段，包含三视角 RGB 图像、语言指令及避障轨迹。
*   **Benchmark 与对比方法**：
    *   **基准方法 (MSE)**：标准的基于扩散的模仿学习（仅使用 MSE 损失）。
    *   **提出方法 (MSE+Feasibility)**：在 MSE 基础上加入物理可行性监督。
*   **评估指标**：安全成功率（SSR），综合考虑了最小避障距离 ($d_{min}$) 和最终到达精度 ($dtgt$)。

### 4. 资源与算力
*   **模型架构**：采用 **RDT-1B**（12亿参数的扩散基础模型）作为骨干网络。
*   **硬件配置**：使用 **2 张 NVIDIA A100 GPU** 进行全参数微调。
*   **训练时长**：
    *   40 个片段：约 8 小时。
    *   80 个片段：约 15 小时。
    *   120 个片段：约 23 小时。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **数据效率实验**：对比了 40、80、120 个训练片段下的表现。
    *   **鲁棒性测试**：在“小扰动”（分布内）和“大扰动”（分布外障碍物位置）两种设置下进行评估。
    *   **消融实验**：对安全裕度 $\delta$ 和损失权重 $\lambda$ 进行了多组组合测试。
*   **充分性评价**：实验设计较为**充分且客观**。作者通过引入“反事实轨迹生成”确保了测试场景具有挑战性，并使用了聚类自助法（Clustered Bootstrap）计算 95% 置信区间，增强了结果的统计可靠性。

### 6. 主要结论与发现
*   **性能提升**：引入可行性监督显著提高了安全成功率（SSR），在某些指标上比纯模仿学习提升了一倍以上。
*   **协同效应**：物理监督不仅提升了安全性（避障），还意外地**提升了到达精度**。这表明几何信号帮助模型更好地理解了视觉场景与物理空间的关系。
*   **低数据效率**：在极低数据量（40 个片段）下，该方法的优势最明显，甚至能超过使用 120 个片段训练的基准模型。
*   **强度平衡**：监督强度（$\delta, \lambda$）需适中。过强的监督会导致策略过于保守，从而降低任务成功率。

### 7. 优点与亮点
*   **即插即用**：该方法作为一种训练时的“归纳偏置”，不需要改变模型推理架构。
*   **弥补 IL 短板**：有效解决了模仿学习在面对未见过的障碍物配置时容易发生碰撞的问题。
*   **理论与实践结合**：将经典的机器人几何约束（SDF、FK）成功集成到现代大参数 VLA 模型的端到端训练中。

### 8. 不足与局限
*   **场景单一**：实验仅限于单臂避障到达任务，尚未在更复杂的长程任务或多物体交互中验证。
*   **几何简化**：将障碍物简化为 OBB（包围盒）可能无法处理形状极度复杂的物体。
*   **缺乏真机验证**：所有实验均在仿真环境（Isaac Sim）中完成，现实世界中的感知噪声和动力学差异可能影响该信号的有效性。
*   **依赖先验**：训练时需要准确的机器人 CAD 模型（用于 FK）和障碍物位置信息，这在某些无监督或开放世界场景下可能难以获取。

（完）
