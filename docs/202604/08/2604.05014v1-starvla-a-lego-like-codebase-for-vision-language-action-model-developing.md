---
title: "StarVLA: A Lego-like Codebase for Vision-Language-Action Model Developing"
title_zh: StarVLA：一种用于视觉-语言-动作模型开发的乐高式代码库
authors: StarVLA Community
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05014v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 用于视觉-语言-动作（VLA）模型开发和通用智能体的代码库
tldr: StarVLA是一个开源的视觉-语言-动作（VLA）模型开发框架，旨在解决当前具身智能研究中架构碎片化、代码库不兼容和评估协议不统一的挑战。它采用模块化的“骨干网络-动作头”设计，支持Qwen-VL、Cosmos等多种模型与动作解码器的灵活组合。通过集成跨具身学习策略和统一的评估接口（如LIBERO、SimplerEnv），StarVLA显著降低了VLA模型的开发与复现门槛，并在多个基准测试中展现出卓越的性能。
selection_source: fresh_fetch
motivation: 针对VLA研究中因架构不兼容和评估标准不一导致的复现困难及对比不公等碎片化问题。
method: 构建了一个类似乐高的模块化框架，支持独立更换骨干网络与动作头，并提供统一的训练策略和多基准评估接口。
result: StarVLA提供的训练方案在多个主流基准测试中均达到或超过了现有先进方法的表现。
conclusion: StarVLA是目前最全面的开源VLA框架之一，能有效推动具身智能领域的算法原型设计与标准化研究。
---

## 摘要
构建通用具身智能体需要整合感知、语言理解和动作，这些是基于多模态基础模型（包括视觉-语言模型和世界模型的最新进展）的视觉-语言-动作（VLA）方法所解决的核心能力。尽管取得了快速进展，但 VLA 方法在不兼容的架构、代码库和评估协议中仍然处于碎片化状态，阻碍了有原则的比较和可复现性。我们提出了 StarVLA，一个用于 VLA 研究的开源代码库。StarVLA 从三个方面应对这些挑战。首先，它提供了一种模块化的“主干网络-动作头”架构，支持 VLM 主干（如 Qwen-VL）和世界模型主干（如 Cosmos），以及代表性的动作解码范式，所有这些都在一个共享的抽象层下，其中主干和动作头可以各自独立更换。其次，它提供了可重用的训练策略，包括跨具身学习和多模态协同训练，这些策略在支持的范式中具有一致的适用性。第三，它通过统一的评估接口集成了主要的基准测试，包括 LIBERO、SimplerEnv、RoboTwin 2.0、RoboCasa-GR1 和 BEHAVIOR-1K，支持仿真和真实机器人部署。StarVLA 还提供了简单、完全可复现的单基准训练方案，尽管数据工程极少，但在使用 VLM 和世界模型主干的多个基准测试中，已经达到或超过了之前的方法。据我们所知，StarVLA 是目前最全面的开源 VLA 框架之一，我们期望它能降低复现现有方法和原型化新方法的门槛。StarVLA 正在积极维护和扩展；我们将随着项目的发展更新本报告。代码和文档可在 https://github.com/starVLA/starVLA 获取。

## Abstract
Building generalist embodied agents requires integrating perception, language understanding, and action, which are core capabilities addressed by Vision-Language-Action (VLA) approaches based on multimodal foundation models, including recent advances in vision-language models and world models. Despite rapid progress, VLA methods remain fragmented across incompatible architectures, codebases, and evaluation protocols, hindering principled comparison and reproducibility. We present StarVLA, an open-source codebase for VLA research. StarVLA addresses these challenges in three aspects. First, it provides a modular backbone--action-head architecture that supports both VLM backbones (e.g., Qwen-VL) and world-model backbones (e.g., Cosmos) alongside representative action-decoding paradigms, all under a shared abstraction in which backbone and action head can each be swapped independently. Second, it provides reusable training strategies, including cross-embodiment learning and multimodal co-training, that apply consistently across supported paradigms. Third, it integrates major benchmarks, including LIBERO, SimplerEnv, RoboTwin~2.0, RoboCasa-GR1, and BEHAVIOR-1K, through a unified evaluation interface that supports both simulation and real-robot deployment. StarVLA also ships simple, fully reproducible single-benchmark training recipes that, despite minimal data engineering, already match or surpass prior methods on multiple benchmarks with both VLM and world-model backbones. To our best knowledge, StarVLA is one of the most comprehensive open-source VLA frameworks available, and we expect it to lower the barrier for reproducing existing methods and prototyping new ones. StarVLA is being actively maintained and expanded; we will update this report as the project evolves. The code and documentation are available at https://github.com/starVLA/starVLA.

---

## 论文详细总结（自动生成）

以下是对论文《StarVLA: A Lego-like Codebase for Vision-Language-Action Model Developing》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：具身智能（Embodied AI）领域正向通用智能体演进，视觉-语言-动作（VLA）模型成为核心。然而，当前 VLA 研究存在严重的**碎片化**问题：
    *   **架构不兼容**：不同方法（如基于 VLM 的自回归 vs. 基于世界模型的扩散模型）代码库独立。
    *   **系统耦合**：模型架构、数据处理和训练流程紧密耦合，难以复现或更换组件。
    *   **评估不统一**：各研究使用的基准测试和协议不一致，难以进行公平的横向对比。
*   **核心目标**：打造一个“乐高式”的开源代码库，通过统一的抽象层和模块化设计，降低 VLA 模型的开发、复现和对比门槛。

### 2. 方法论
*   **核心思想：骨干网络-动作头（Backbone-Action-Head）解耦**。
    *   将 VLA 模型拆分为两个独立模块：负责感知和推理的 **VL Backbone**（如 Qwen-VL, Cosmos）和负责将表征映射为指令的 **Action Head**。
*   **关键技术细节**：
    *   **统一 I/O 接口**：训练和推理均直接处理原始环境观测（图像、文本），避免训练/测试分布不一致。
    *   **四种代表性范式**：
        1.  **StarVLA-FAST**：自回归动作标记化（Action Tokenization）。
        2.  **StarVLA-OFT**：并行回归（Parallel Regression），使用轻量级 MLP。
        3.  **StarVLA-$\pi$**：基于流匹配（Flow-matching）的去噪动作生成。
        4.  **StarVLA-GR00T**：双系统设计（系统 1 快速动作，系统 2 慢速推理）。
    *   **训练策略**：支持行为克隆（BC）监督学习、多目标协同训练（保持 VLM 的推理能力）以及跨具身（Cross-embodiment）混合数据训练。

### 3. 实验设计
*   **数据集与场景**：集成了 5+ 个主流基准测试：
    *   **LIBERO**：130 个桌面操作任务。
    *   **SimplerEnv**：模拟真实机器人（WidowX, Google Robot）的仿真环境。
    *   **RoboTwin 2.0**：双臂协作任务。
    *   **RoboCasa-GR1**：类人机器人家庭场景任务。
    *   **BEHAVIOR-1K**：大规模人类日常活动模拟。
    *   **CALVIN**：长程语言条件操作。
*   **对比方法**：包括 OpenVLA, $\pi_0$, $\pi_{0.5}$, GR00T-N1.5/1.6, RT-1/2, CogACT 等当前最先进（SOTA）的方法。
*   **实验设置**：分为“专家模型”（单基准训练）和“通用模型”（跨基准/跨具身联合训练）。

### 4. 资源与算力
*   **GPU 型号与数量**：
    *   **单节点训练**：使用 8 张 A100 (80GB) GPU。
    *   **多节点扩展**：实验展示了从 8 张扩展到 **256 张 A100** 的效率表现。
    *   **效率对比**：文中还提到了在 H200 GPU 上的推理/训练延迟对比。
*   **训练时长**：LIBERO 基准通常训练 30K 步（约 10 个 epoch），RoboTwin 2.0 训练 150K 步。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，涵盖了多个不同架构（VLM vs. 世界模型）、不同动作头、不同基准测试的组合。
*   **充分性与客观性**：
    *   **消融实验**：对比了不同 VL Backbone（如 Qwen3-VL-4B vs. Cosmos-Predict2-2B）对性能的影响。
    *   **公平性**：坚持使用各基准测试的官方评估协议，且在极少数据工程（无重度增强）的情况下建立基准，确保了结果的纯净和可参考性。
    *   **重复性**：每个设置运行多次（如 SimplerEnv 运行 5 次）并报告均值，以应对仿真环境的方差。

### 6. 主要结论与发现
*   **模块化有效性**：证明了 Backbone 和 Action Head 可以独立更换而不损失性能，甚至在某些基准上超过了原作者的实现。
*   **数据效率**：StarVLA 方案在 LIBERO 等基准上仅需较少的训练轮数即可达到 SOTA。
*   **协同训练的重要性**：通过 VLM 数据协同训练，可以有效防止模型在学习动作时“遗忘”空间推理和语言理解能力。
*   **通用模型潜力**：联合训练的“全能模型”在复杂任务（如 RoboCasa）上的表现甚至优于专门针对该任务训练的“专家模型”。

### 7. 优点
*   **高度模块化**：像乐高一样灵活，极大方便了研究人员测试新的模型组件。
*   **统一接口**：Server-Client 架构解决了不同仿真器依赖冲突的问题，支持无缝切换仿真与实机部署。
*   **开源贡献**：提供了目前最全面的 VLA 开发框架和可复现的训练脚本（Recipes）。

### 8. 不足与局限
*   **强化学习集成**：虽然计划支持 RL 微调，但在当前版本（v1.0）中仍处于集成阶段，尚未完全开放。
*   **算力门槛**：尽管提供了轻量级配置，但大规模跨具身预训练仍需要极高的算力资源（如 256x A100）。
*   **实机数据偏差**：虽然支持实机部署，但论文主要实验仍集中在仿真环境，真实世界的泛化性仍需更多实机验证。

（完）
