---
title: "A1: A Fully Transparent Open-Source, Adaptive and Efficient Truncated Vision-Language-Action Model"
title_zh: A1：一个完全透明、开源、自适应且高效的截断式视觉-语言-动作模型
authors: "Kaidong Zhang, Jian Zhang, Rongtao Xu, Yu Sun, Shuoshuo Xue, Youpeng Wen, Xiaoyu Guo, Minghao Guo, Weijia Liufu, Liu Zihou, Kangyi Ji, Yangsong Zhang, Jiarun Zhu, Jingzhi Liu, Zihang Li, Ruiyi Chen, Meng Cao, Jingming Zhang, Shen Zhao, Xiaojun Chang, Feng Zheng, Ivan Laptev, Xiaodan Liang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05672v1"
tags: ["query:rob-il"]
score: 10.0
evidence: 用于开放世界机器人操纵的高效VLA模型
tldr: "A1是一个开源透明的视觉-语言-动作（VLA）模型框架，旨在解决现有模型推理成本高、延迟大的问题。它利用预训练VLM的先验知识，并引入预算感知的自适应推理方案，包括基于动作一致性的提前终止机制和跨层截断流匹配技术。在多个仿真和真实机器人测试中，A1在显著降低计算成本（最高减少76.6%）的同时，实现了超越pi0等基准模型的SOTA成功率，为低成本实时机器人控制提供了新方案。"
selection_source: fresh_fetch
motivation: 针对现有VLA模型因庞大的骨干网络和迭代式动作生成头导致的推理延迟高、计算成本昂贵且难以在普通硬件实时部署的问题。
method: 引入了预算感知的自适应推理方案，通过监测中间层动作一致性触发提前终止，并利用跨层截断流匹配技术实现去噪过程的预热启动。
result: "在RoboChallenge中以29.00%的成功率超越了pi0和RDT-1B，同时将推理延迟降低了高达72%，骨干网络计算量减少了76.6%。"
conclusion: A1通过全栈开源和高效的自适应推理机制，证明了在保持高性能操作的同时，可以大幅提升VLA模型的推理效率和部署可行性。
---

## 摘要
视觉-语言-动作（VLA）模型已成为开放世界机器人操控的一种强大范式，但其实际部署往往受限于成本：十亿级参数规模的 VLM 主干网络以及基于迭代扩散/流（diffusion/flow-based）的动作头会产生高延迟和高计算量，使得在通用硬件上进行实时控制变得昂贵。我们提出了 A1，这是一个完全开源且透明的 VLA 框架，旨在不牺牲操控成功率的情况下实现低成本、高吞吐量的推理。我们的方法利用预训练的 VLM 为动作生成提供隐式的 affordance 先验。我们发布了完整的训练栈（训练代码、数据/数据处理流水线、中间检查点和评估脚本），以实现端到端的复现。除了仅优化 VLM 之外，A1 还通过引入一种预算感知的自适应推理方案来针对整个推理流水线，该方案能够同时加速主干网络和动作头。具体而言，我们通过监测 VLM 中间层的动作一致性来触发提前终止，并提出了层间截断流匹配（Inter-Layer Truncated Flow Matching），该方法可以在不同层之间热启动去噪过程，从而以显著更少的有效去噪迭代次数实现精确的动作。在仿真基准测试（LIBERO、VLABench）和真实机器人（Franka、AgiBot）上，A1 在显著降低推理成本的同时（例如，流匹配推理的每回合延迟降低高达 72%，主干网络计算量减少高达 76.6% 且性能下降极小），实现了最先进的成功率。在 RoboChallenge 上，A1 达到了 29.00% 的平均成功率，优于包括 pi0 (28.33%)、X-VLA (21.33%) 和 RDT-1B (15.00%) 在内的基准模型。

## Abstract
Vision--Language--Action (VLA) models have emerged as a powerful paradigm for open-world robot manipulation, but their practical deployment is often constrained by \emph{cost}: billion-scale VLM backbones and iterative diffusion/flow-based action heads incur high latency and compute, making real-time control expensive on commodity hardware. We present A1, a fully open-source and transparent VLA framework designed for low-cost, high-throughput inference without sacrificing manipulation success; Our approach leverages pretrained VLMs that provide implicit affordance priors for action generation. We release the full training stack (training code, data/data-processing pipeline, intermediate checkpoints, and evaluation scripts) to enable end-to-end reproducibility. Beyond optimizing the VLM alone, A1 targets the full inference pipeline by introducing a budget-aware adaptive inference scheme that jointly accelerates the backbone and the \emph{action head}. Specifically, we monitor action consistency across intermediate VLM layers to trigger early termination, and propose Inter-Layer Truncated Flow Matching that warm-starts denoising across layers, enabling accurate actions with substantially fewer effective denoising iterations. Across simulation benchmarks (LIBERO, VLABench) and real robots (Franka, AgiBot), A1 achieves state-of-the-art success rates while significantly reducing inference cost (e.g., up to 72% lower per-episode latency for flow-matching inference and up to 76.6% backbone computation reduction with minor performance degradation). On RoboChallenge, A1 achieves an average success rate of 29.00%, outperforming baselines including pi0(28.33%), X-VLA (21.33%), and RDT-1B (15.00%).

---

## 论文详细总结（自动生成）

这篇论文介绍了 **A1**，一个旨在解决视觉-语言-动作（VLA）模型推理成本高昂问题的开源框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：现有的 VLA 模型（如 OpenVLA, $\pi_0$）虽然在开放世界操控中表现出色，但其**推理成本极高**。主要瓶颈在于：
    1.  **庞大的主干网络**：通常基于数十亿参数的 VLM，每步推理都要完整运行所有层。
    2.  **迭代式动作头**：基于扩散（Diffusion）或流匹配（Flow Matching）的动作头通常需要 10-20 次迭代去噪，成为新的计算瓶颈。
*   **研究动机**：作者观察到动作生成存在冗余性（相邻层输出相似）和轨迹收敛性（流匹配在前几步即可锁定大致方向）。因此，A1 的目标是在不牺牲成功率的前提下，实现**低延迟、高吞吐量**的实时控制，并保持全栈开源透明。

### 2. 方法论：核心思想与关键技术
A1 结合了高效的 VLM 主干（基于 Molmo-7B）和两种可选动作头（MLP 或 Flow Matching），其核心加速技术包括：
*   **预算感知的自适应推理（Adaptive Inference）**：
    *   **提前终止机制**：在 VLM 的中间层计算动作预测，并进行“动作一致性测试”。如果当前层与前一层的动作差异 $\Delta^i_t$ 小于预设阈值 $\eta_i$，则提前退出，无需运行后续 VLM 层。
    *   **阈值校准**：通过在训练集上离线运行，根据期望的计算预算（如指数分布或高斯分布）自动校准每一层的退出阈值。
*   **层间截断流匹配（Inter-Layer Truncated Flow Matching, ITFM）**：
    *   **核心创新**：为了避免动作头在每一层都进行完整的去噪迭代，ITFM 在每个 VLM 层仅运行极少数去噪步（如 $\delta=2$）。
    *   **热启动（Warm-start）**：下一层的去噪过程不再从随机噪声开始，而是从上一层输出的预测结果开始初始化。这使得去噪过程可以跨 VLM 层累积，大幅减少了总迭代次数。
*   **训练策略**：采用两阶段训练（大规模多机器人预训练 + 任务特定微调），并引入多退出（Multi-exit）监督，确保中间层也能生成有效的动作。

### 3. 实验设计
*   **数据集/场景**：
    *   **预训练**：整合了 DROID, AgiBot, RoboCOIN, RoboMind, GM-100, RoboChallenge 等开源数据，外加 15,951 条自研机器人轨迹。
    *   **仿真基准**：LIBERO（长程操控）、VLABench（视觉语言理解）。
    *   **真实机器人**：在 Franka, AgiBot, WuJie-Arm, Dobot-Arm 四个平台上测试了 7 类任务（如擦桌子、摆放水果、叠方块等）。
*   **对比方法**：对比了 $\pi_0$, $\pi_{0.5}$, OpenVLA, RDT-1B, X-VLA, Octo, SmolVLA 等主流模型。
*   **Benchmark**：重点评估了 **RoboChallenge (Table30)**，这是一个包含 30 个真实机器人任务的大型基准。

### 4. 资源与算力
*   **推理算力分析**：文中详细对比了 GFLOPs。例如，Molmo-7B 完整推理约 11,074 GFLOPs；A1-FM 在自适应推理下，主干网络计算量可减少高达 **76.6%**。
*   **训练算力**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长，但提到了预训练步数为 200K，微调步数为 50K-100K，Batch Size 为 32-1024 不等。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验覆盖了 LIBERO 的四个子集和 VLABench 的四个任务。
    *   真实世界实验在 4 种不同形态的机器人上进行，每个任务测试 10 次，总计收集了超过 3,000 条轨迹。
    *   RoboChallenge 包含 30 个任务的对比。
*   **充分性与公平性**：实验设计较为全面，包含了消融实验（验证提前终止和热启动的有效性）和零样本泛化实验（LIBERO-Plus）。对比基准涵盖了目前最先进的开源和半开源模型，结果具有较高的说服力。

### 6. 主要结论与发现
*   **性能领先**：在 RoboChallenge 上，A1 以 29.00% 的成功率超越了 $\pi_0$ (28.33%) 和 RDT-1B (15.00%)。
*   **效率极高**：通过 ITFM 技术，推理延迟降低了 **72%**；在仅保留约 23% 计算量的情况下，LIBERO 成功率仅下降约 4%。
*   **计算分配**：可视化显示，模型在执行简单动作（如移动）时倾向于从前几层（第 3-5 层）退出，而在执行关键复杂动作（如打开炉灶）时会运行到更深层（第 17-25 层）。

### 7. 优点（亮点）
*   **全栈透明开源**：不仅发布权重，还发布了完整的训练代码、数据处理流水线和评估脚本，极大地促进了社区复现。
*   **联合优化方案**：不同于以往只优化 VLM 或只优化动作头的研究，A1 实现了主干网络和动作头的**协同加速**。
*   **热启动去噪**：提出的层间截断流匹配（ITFM）是一个非常巧妙的设计，解决了扩散类动作头在自适应推理中的额外开销问题。

### 8. 不足与局限
*   **数据依赖**：预训练仍依赖于带标签的机器人数据，未来需探索如何利用无标签的人类视频数据。
*   **误差累积**：目前主要基于模仿学习，在长程任务中仍存在累积误差导致的精度下降。
*   **硬件延迟**：虽然模型推理变快了，但云端服务器与本地机器人之间的网络延迟仍是实时控制中的一个实际障碍。
*   **训练算力细节缺失**：缺乏具体的训练资源消耗说明，对于想要复现的开发者来说，难以预估成本。

（完）
