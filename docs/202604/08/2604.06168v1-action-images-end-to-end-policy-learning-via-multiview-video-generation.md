---
title: "Action Images: End-to-End Policy Learning via Multiview Video Generation"
title_zh: 动作图像：通过多视角视频生成的端到端策略学习
authors: "Haoyu Zhen, Zixian Gao, Qiao Sun, Yilin Zhao, Yuncong Yang, Yilun Du, Tsun-Hsuan Wang, Yi-Ling Qiao, Chuang Gan"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06168v1"
tags: ["query:rob-il"]
score: 10.0
evidence: 通过视频生成的端到端策略学习统一视觉动作模型
tldr: Action Images 提出了一种将机器人策略学习转化为多视图视频生成的统一世界动作模型。该方法不使用传统的低维动作 token，而是将 7 自由度动作转化为像素级、可解释的“动作图像”，使预训练视频模型能直接作为零样本策略。该模型在 RLBench 和真实场景中表现优异，实现了视频与动作的联合生成，显著提升了跨环境的迁移能力和生成质量。
selection_source: fresh_fetch
motivation: 现有模型依赖独立的动作模块或非像素对齐表示，限制了对预训练视频模型知识的利用及跨视图的泛化能力。
method: 将机器人动作编码为多视图像素级“动作图像”，利用视频生成骨干网络统一处理策略执行、视频预测和动作标注。
result: 在 RLBench 和真实世界测试中，该模型实现了领先的零样本成功率，并优于现有的视频空间世界模型。
conclusion: 将动作转化为可解释的像素级图像是提升机器人策略学习效果和实现多任务统一建模的有效途径。
---

## 摘要
世界动作模型 (WAMs) 已成为机器人策略学习的一个极具前景的方向，因为它们可以利用强大的视频骨干网络来建模未来状态。然而，现有方法通常依赖于独立的动作模块，或者使用非像素对齐 (pixel-grounded) 的动作表示，这使得难以充分利用视频模型的预训练知识，并限制了在不同视角和环境之间的迁移。在这项工作中，我们提出了 Action Images，这是一种统一的世界动作模型，将策略学习表述为多视角视频生成。我们没有将控制编码为低维 token，而是将 7 自由度 (7-DoF) 机器人动作转化为可解释的动作图像：即在 2D 像素中对齐并显式跟踪机械臂运动的多视角动作视频。这种像素对齐的动作表示允许视频骨干网络本身充当零样本策略，而无需独立的策略头或动作模块。除了控制之外，同一个统一模型还支持在共享表示下的视频-动作联合生成、动作条件视频生成以及动作标注。在 RLBench 和真实世界评估中，我们的模型实现了最强的零样本成功率，并比之前的视频空间世界模型提高了视频-动作联合生成的质量，这表明可解释的动作图像是策略学习的一条可行路径。

## Abstract
World action models (WAMs) have emerged as a promising direction for robot policy learning, as they can leverage powerful video backbones to model the future states. However, existing approaches often rely on separate action modules, or use action representations that are not pixel-grounded, making it difficult to fully exploit the pretrained knowledge of video models and limiting transfer across viewpoints and environments. In this work, we present Action Images, a unified world action model that formulates policy learning as multiview video generation. Instead of encoding control as low-dimensional tokens, we translate 7-DoF robot actions into interpretable action images: multi-view action videos that are grounded in 2D pixels and explicitly track robot-arm motion. This pixel-grounded action representation allows the video backbone itself to act as a zero-shot policy, without a separate policy head or action module. Beyond control, the same unified model supports video-action joint generation, action-conditioned video generation, and action labeling under a shared representation. On RLBench and real-world evaluations, our model achieves the strongest zero-shot success rates and improves video-action joint generation quality over prior video-space world models, suggesting that interpretable action images are a promising route to policy learning.

---

## 论文详细总结（自动生成）

以下是对论文《Action Images: End-to-End Policy Learning via Multiview Video Generation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的机器人世界模型（World Models）虽然在视频预测上表现出色，但在转化为**策略泛化（Policy Generalization）**时存在巨大鸿沟。
*   **研究动机**：
    *   **表示不一致**：传统方法将 7 自由度（7-DoF）动作编码为低维 Token 或使用独立的策略头，这导致视频模型的预训练知识（如物理规律、空间关系）无法直接服务于控制。
    *   **缺乏像素对齐**：动作与图像空间脱节，使得模型在跨视角、跨环境迁移时表现脆弱。
*   **整体含义**：本文提出将策略学习完全等同于**多视角视频生成**，通过将动作转化为“像素对齐”的图像，使视频骨干网络无需修改即可直接输出可执行的控制指令。

### 2. 方法论
*   **核心思想**：将机器人动作（Action）转化为可解释的**动作图像（Action Images）**，并与原始观察视频在同一视频空间内进行联合建模。
*   **关键技术细节**：
    1.  **动作转图像（Action-to-Image）**：
        *   将 7-DoF 动作（位置、姿态、夹爪）转换为 3 个语义 3D 点：位置点（Position）、法线点（Normal）和向上点（Up）。
        *   利用相机参数将 3D 点投影到 2D 像素平面。
        *   **通道编码**：R 通道渲染位置点的高斯热图；G 通道渲染法线点；B 通道渲染向上点，并在低响应区域嵌入夹爪开合度的二进制信号。
    2.  **动作解码（Action Decoding）**：
        *   采用**射线投射（Ray Casting）**技术：从主视图热图中心发射射线，在侧视图热图中寻找最佳匹配点，从而将 2D 热图还原为精确的 3D 动作向量。
    3.  **统一训练框架**：
        *   基于预训练的 **Wan 2.2** 视频生成模型。
        *   **多掩码策略（Masking Strategy）**：通过随机遮盖视频或动作 Token，使模型同时具备“联合生成”、“动作条件视频生成”和“视频转动作标注”三种能力。
        *   **相机控制**：引入 Plücker 嵌入来注入相机姿态信息，确保多视角一致性。

### 3. 实验设计
*   **数据集**：
    *   **RLBench**：提供精确的动作和相机标注（仿真环境）。
    *   **DROID**：大规模真实世界机械臂操作数据。
    *   **BridgeV2**：用于增强视频生成的真实感。
*   **Benchmark 与对比方法**：
    *   **对比方法**：包括扩散策略（MV-Policy）、视觉语言动作模型（$\pi_{0.5}$、MolmoAct）以及最新的世界模型（TesserAct、Cosmos-Policy）。
    *   **评估场景**：涵盖仿真（RLBench）和真实世界（xArm 机器人）的零样本（Zero-shot）任务，如“放杯子”、“捡玩具”、“关抽屉”等。

### 4. 资源与算力
*   **训练算力**：使用了 **32 台 NVIDIA A100 GPU**。
*   **训练细节**：采用 DeepSpeed ZeRO 优化器，bfloat16 混合精度，训练时长为 100,000 个优化步数（Steps）。
*   **推理优化**：在 8 台 H100 上通过并行化、缓存（Caching）和 `torch.compile` 优化，推理速度最高可达 **71 FPS**（相比原始扩散生成的缓慢速度有显著提升）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了零样本策略评估（仿真+真实世界）。
    *   进行了在域（In-domain）任务的性能对比。
    *   评估了视频与动作的联合生成质量（PSNR, SSIM, FVD 等指标）。
    *   进行了消融实验，验证了增加“动作头”对精度的提升作用。
*   **充分性评价**：实验设计较为全面，不仅关注成功率，还评估了生成视频的物理真实性。对比了从传统 IL 到最新 VLA 的多种基准，客观地展示了像素对齐表示在泛化性上的优势。

### 6. 主要结论与发现
*   **像素对齐是关键**：将动作表示为图像，使模型能够利用视频骨干网络的空间推理能力，显著提升了零样本迁移成功率。
*   **多视角优势**：多视角动作图像解决了单视图下的深度歧义，使 3D 动作还原更精确。
*   **统一建模**：同一个模型可以同时完成预测未来和执行动作，且两者在语义上是高度一致的。

### 7. 优点
*   **创新性强**：打破了动作必须是低维向量的固有思维，将控制问题转化为视觉跟踪和预测问题。
*   **零样本能力出色**：在未见过的物体和环境下，表现优于需要额外策略头的世界模型。
*   **多功能集成**：无需修改架构即可实现动作标注和条件生成，具有极高的灵活性。

### 8. 不足与局限
*   **实时性瓶颈**：尽管有推理优化，但基于大型扩散模型的生成过程仍比轻量级策略（如 MLP）慢，可能限制高频闭环控制的应用。
*   **数据依赖**：该方法高度依赖精确的相机内参和外参，在相机参数未知的野外数据上表现受限。
*   **开环风险**：论文主要展示了开环（Open-loop）生成的轨迹执行，在面对动态干扰时的闭环纠错能力尚需更多验证。

（完）
