---
title: "BiCoord: A Bimanual Manipulation Benchmark towards Long-Horizon Spatial-Temporal Coordination"
title_zh: BiCoord：面向长程时空协同的双臂操作基准
authors: "Xingyu Peng, Chen Gao, Liankai Jin, Annan Li, Si Liu"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05831v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 用于长程协调任务的双臂操纵基准
tldr: BiCoord是一个针对长程、紧密协作双臂操作的全新基准测试。针对现有基准任务周期短、协作松散的问题，BiCoord设计了需要持续双臂依赖和动态角色交换的多阶段任务，并提出了一套从时间、空间及时空维度量化评估协作能力的指标。实验表明，当前主流操作策略在处理高耦合长程任务时仍面临巨大挑战，该基准为研究协调感知型机器人学习提供了重要基础。
selection_source: fresh_fetch
motivation: 现有双臂操作基准测试大多任务周期短且协作松散，难以捕捉现实世界中复杂的双臂时空耦合特性。
method: 引入了包含多样化长程任务的BiCoord基准，并设计了涵盖时间、空间和时空维度的定量协作评估指标。
result: 实验证明DP、RDT、Pi0和OpenVLA-OFT等代表性策略在处理长周期、高耦合的双臂任务时表现挣扎。
conclusion: BiCoord揭示了实现长程紧密协作任务的根本挑战，为未来协调感知型机器人操作研究提供了基准和方向。
---

## 摘要
双臂操作，即协同使用两个机械臂来完成任务，对于在机器人领域实现人类水平的灵巧性至关重要。最近的仿真基准（如 RoboTwin 和 RLBench2）推动了双臂操作的数据驱动学习。然而，现有任务多为短程且仅为松散协同，未能捕捉到现实世界双臂行为中固有的时空耦合。为了填补这一空白，我们推出了 BiCoord，这是一个针对长程且紧密协同双臂操作的基准。具体而言，BiCoord 包含多种任务，这些任务需要在多个子目标之间保持持续的臂间依赖和动态角色交换。此外，我们提出了一套定量指标，从时间、空间和时空维度评估协同性，从而实现对双臂协作的系统性衡量。实验结果表明，代表性的操作策略（如 DP、RDT、Pi0 和 OpenVLA-OFT）在处理长时且高度耦合的任务时表现挣扎，揭示了实现长程紧密协同任务的根本挑战。我们希望 BiCoord 能够为研究长程协作操作奠定基础，并启发未来关于协同感知机器人学习的研究。所有数据集、代码和补充材料均可在 https://buaa-colalab.github.io/BiCoord/ 获取。

## Abstract
Bimanual manipulation, i.e., the coordinated use of two robotic arms to complete tasks, is essential for achieving human-level dexterity in robotics. Recent simulation benchmarks, e.g., RoboTwin and RLBench2, have advanced data-driven learning for bimanual manipulation. However, existing tasks are short-horizon and only loosely coordinated, failing to capture the spatial-temporal coupling inherent in real-world bimanual behaviors. To address this gap, we introduce BiCoord, a benchmark for long-horizon and tightly coordinated bimanual manipulation. Specifically, BiCoord comprises diverse tasks that require continuous inter-arm dependency and dynamic role exchange across multiple sub-goals. Also, we propose a suite of quantitative metrics that evaluate coordination from temporal, spatial, and spatial-temporal perspectives, enabling systematic measurement of bimanual cooperation. Experimental results show that representative manipulation policies, e.g., DP, RDT, Pi0, and OpenVLA-OFT, struggle with long-duration and highly coupled tasks, revealing fundamental challenges in achieving long-horizon and tight coordination tasks. We hope BiCoord can serve as a foundation for studying long-horizon cooperative manipulation and inspire future research on coordination-aware robotic learning. All datasets, codes and supplements could be found at https://buaa-colalab.github.io/BiCoord/.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **BiCoord**，一个专门为双臂协作机器人设计的长程（Long-Horizon）且具有紧密时空协同（Spatial-Temporal Coordination）特性的操作基准测试。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的双臂操作基准（如 RoboTwin, RLBench2）存在两个主要缺陷：
    1.  **任务周期短（Short-horizon）**：通常仅包含简单的“抓取-放置”动作，缺乏复杂的多阶段推理。
    2.  **协作松散（Loosely coordinated）**：双臂往往独立运行或仅在时间上交替动作，缺乏现实世界中（如烹饪、组装）那种紧密的时空耦合。
*   **研究动机**：人类的双臂操作具有高度的**相位耦合**（Phased coupling）、**时空约束**（Spatial-temporal constraints）和**预测性协调**（Predictive coordination）。论文旨在填补这一空白，提供一个能模拟真实世界复杂协作逻辑的仿真基准。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：通过设计具有多阶段子目标、高频率双臂交互的任务，并引入一套全新的量化指标来衡量“协同度”。
*   **关键技术细节**：
    *   **任务生成管线**：采用“任务设计 -> 动作代码生成（利用 LLM 代理生成 API 调用代码） -> 人机回环验证 -> 轨迹生成与自动标注”的流程。
    *   **时空协同指标**：
        *   **MRD/ARD**：最小/平均相对距离，衡量双臂在空间上的接近程度。
        *   **SMT/SMP**：同步运动时间/百分比，衡量双臂在时间上的并行度。
        *   **STI（时空积分，核心创新）**：通过计算不同距离阈值下的同步运动百分比积分，综合量化双臂在时间和空间上的耦合程度。
    *   **阶段化标注**：每个任务被划分为多个阶段（Stage），并附带子目标描述和双臂行为标注，支持细粒度的评估。

### 3. 实验设计
*   **数据集/场景**：基于 RoboTwin 2.0 仿真环境，包含 18 个复杂任务（如：烹饪、拼图、从塔底抽取方块、交换锅具等）。
*   **对比方法（Baselines）**：
    *   **Diffusion Policy (DP)**：基于扩散过程的视觉运动策略。
    *   **RDT-1B**：双臂操作的扩散基础模型。
    *   **Pi0**：基于流匹配（Flow Matching）的视觉-语言-动作（VLA）模型。
    *   **OpenVLA-OFT**：经过优化的开源 VLA 模型。
*   **评估设置**：分为单任务学习（Single-task）和多任务学习（Multi-task）两种设置。

### 4. 资源与算力
*   **算力设备**：实验在 **NVIDIA A800 (40GB) GPU** 上运行。
*   **训练细节**：
    *   多任务模型从官方权重开始微调，共进行 **50,000 步**训练。
    *   总 Batch Size 为 **32**。
    *   单任务模型参考了 RoboTwin 2.0 的标准配置进行训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖 18 个任务，每个任务评估 100 次 Rollouts。
    *   对比了 4 种主流的机器人学习架构。
    *   进行了单任务 vs 多任务的对比实验。
*   **充分性与公平性**：实验设计较为充分，引入了 **SSR（阶段成功率）** 和 **TL（轨迹长度）** 等多维指标。通过排除 SR 为 0 的任务来计算平均轨迹长度，确保了对比的客观性。

### 6. 主要结论与发现
*   **VLA 模型占优**：得益于大规模预训练知识，Pi0 和 RDT 等 VLA 模型在处理复杂任务时表现优于传统的 DP。
*   **DP 效率更高**：虽然 DP 在泛化性上稍逊，但在成功完成任务时，其轨迹长度通常比 VLA 模型短（更高效），且动作更稳定。
*   **推理能力不足**：当前模型在初始条件改变（如方块颜色、顺序变化）时，容易出现抓取错误，显示出模仿学习的局限性。
*   **精准对齐挑战**：所有模型在需要极高精度的任务（如将方块从一个碗倒入另一个碗）中表现都很差，成功率接近 0。
*   **长程衰减**：随着任务阶段增加，所有模型的成功率均显著下降。

### 7. 优点与亮点
*   **指标创新**：提出的 **STI 指标** 填补了双臂协作量化评价的空白，能有效区分“真协作”与“伪并行”。
*   **任务复杂度高**：BiCoord 的任务平均包含 4.27 个阶段，轨迹长度和物体数量远超现有基准，更具挑战性。
*   **细粒度评估**：阶段化的标注和 SSR 指标允许研究者分析模型具体是在哪一步失败的，而不仅仅是看最终结果。

### 8. 不足与局限
*   **仿真与现实差距（Sim-to-Real）**：论文完全基于仿真环境，未在真实物理机器人上验证策略的迁移效果。
*   **模型推理瓶颈**：实验显示当前最先进的模型在面对 BiCoord 的长程逻辑时仍显吃力，说明基准难度极高，现有的模仿学习方法可能需要结合强化学习或高层规划。
*   **应用限制**：目前主要针对刚性物体操作，未涉及双臂操作中常见的柔性物体（如布料、绳索）的复杂交互。

（完）
