---
title: "AnySlot: Goal-Conditioned Vision-Language-Action Policies for Zero-Shot Slot-Level Placement"
title_zh: AnySlot：用于零样本插槽级放置的目标条件化视觉-语言-动作策略
authors: "Zhaofeng Hu, Sifan Zhou, Qinbo Zhang, Rongtao Xu, Qi Su, Ci-Jyun Liang"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10432v2"
tags: ["query:rob-il"]
score: 10.0
evidence: 用于通用机器人操纵的目标条件视觉-语言-动作 (VLA) 策略
tldr: AnySlot是一个旨在解决零样本槽位级放置任务的VLA框架。针对现有模型在复杂指令下精度不足的问题，该框架通过引入显式空间视觉目标（场景标记）作为中间表示，将任务解耦为高层语义选择与底层目标条件控制。此外，研究者还推出了包含九类任务的SlotBench基准。实验表明，AnySlot在零样本场景下的语义准确性和空间鲁棒性显著优于现有基线模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10432v2/fig-001.webp\", \"caption\": \"Fig. 2: AnySlot overview. We formulate slot-level placement as goal-conditioned control. High-level goal construction uses the Nano-Banana image generator to render a blue-sphere goal from the language prompt, lifting it to a view-consistent multi-view overlay via depth and camera calibration. Low-level control uses a goal-conditioned VLA policy (π0.5) with a PaliGemma-3B backbone and action expert, mapping goalaugmented multi-view observations to continuous actions At.\", \"page\": 6, \"index\": 1, \"width\": 646, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2604.10432v2/fig-002.webp\", \"caption\": \"Fig. 1: Overview of flat (a), modular (b), and (c) our goal-conditioned policy.\", \"page\": 2, \"index\": 2, \"width\": 644, \"height\": 241}, {\"url\": \"assets/figures/arxiv/2604.10432v2/fig-003.webp\", \"caption\": \"Fig. 4: Comparison between AnySlot and a VLM-based method. AnySlot accurately grounds the target slot and executes successful placement, while the VLM-based method mislocalizes the target and fails.\", \"page\": 10, \"index\": 3, \"width\": 675, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2604.10432v2/fig-004.webp\", \"caption\": \"Table 2: Ablation on high-level grounding module. Report Coarse Accuracy (CA, %), Instruction Accuracy (IA, %) and Latency (s). † means inference performed on a single H200 GPU. The yellow rows denote the module used by AnySlot.\", \"page\": 13, \"index\": 4, \"width\": 702, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2604.10432v2/fig-005.webp\", \"caption\": \"Table 1: Performance comparison of different methods on the SlotBench benchmark. Each entry reports SR (Success Rate; higher is better, %) and IA (Instruction Accuracy; higher is better, %). The yellow rows denote our proposed AnySlot, while the blue rows indicate the oracle upper bound.\", \"page\": 12, \"index\": 5, \"width\": 690, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2604.10432v2/fig-006.webp\", \"caption\": \"Fig. 3: SlotBench. A slot-level benchmark with nine challenges for spatial reasoning and language grounding in manipulation.\", \"page\": 9, \"index\": 6, \"width\": 716, \"height\": 532}]"
motivation: 现有的单体VLA策略难以在复杂的组合语言指令下实现亚厘米级的精确物体放置。
method: 提出一种层次化设计，先将语言指令转化为显式的视觉场景标记，再由目标条件VLA策略执行该视觉目标。
result: 在新开发的SlotBench基准测试中，AnySlot在零样本槽位级放置任务上的表现大幅领先于传统的VLA和模块化方法。
conclusion: 通过将高层槽位定位与底层动作执行解耦，AnySlot显著增强了机器人处理高精度空间推理任务的能力。
---

## 摘要
视觉-语言-动作（VLA）策略已成为通用机器人操控的一种多功能范式。然而，在组合式语言指令下进行精确的物体放置，对于现代单体式 VLA 策略来说仍然是一个重大挑战。插槽级任务既需要可靠的插槽定位（grounding），也需要亚厘米级的执行精度。为此，我们提出了 AnySlot，这是一个通过引入显式空间视觉目标作为语言定位与控制之间的中间表示，从而降低组合复杂性的框架。AnySlot 通过生成场景标记（scene marker）将语言转化为显式视觉目标，随后利用目标条件化 VLA 策略执行该目标。这种分层设计有效地将高层插槽选择与底层执行解耦，确保了语义准确性和空间鲁棒性。此外，考虑到目前缺乏针对此类高精度任务的基准测试，我们推出了 SlotBench，这是一个包含九个任务类别的综合仿真基准，专门用于评估插槽级放置中的结构化空间推理。大量实验表明，AnySlot 在零样本插槽级放置方面显著优于扁平化 VLA 基线和之前的模块化定位方法。

## Abstract
Vision-Language-Action (VLA) policies have emerged as a versatile paradigm for generalist robotic manipulation. However, precise object placement under compositional language instructions remains a major challenge for modern monolithic VLA policies. Slot-level tasks require both reliable slot grounding and sub-centimeter execution accuracy. To this end, we propose AnySlot, a framework that reduces compositional complexity by introducing an explicit spatial visual goal as an intermediate representation between language grounding and control. AnySlot turns language into an explicit visual goal by generating a scene marker, then executes this goal with a goal-conditioned VLA policy. This hierarchical design effectively decouples high-level slot selection from low-level execution, ensuring both semantic accuracy and spatial robustness. Furthermore, recognizing the lack of existing benchmarks for such precision-demanding tasks, we introduce SlotBench, a comprehensive simulation benchmark featuring nine task categories tailored to evaluate structured spatial reasoning in slot-level placement. Extensive experiments show that AnySlot significantly outperforms flat VLA baselines and previous modular grounding methods in zero-shot slot-level placement.

---

## 论文详细总结（自动生成）

### AnySlot: 零样本插槽级放置的目标条件化 VLA 策略总结

#### 1. 论文的核心问题与整体含义
论文关注的是机器人操控中一个极具挑战性的任务：**插槽级放置（Slot-Level Placement）**。
*   **研究动机**：现有的视觉-语言-动作（VLA）策略在处理复杂、组合式的语言指令（如“放在左数第二个且最大的格子里”）时，往往难以兼顾**语义理解的准确性**与**执行的亚厘米级精度**。
*   **核心问题**：扁平化（Flat）的端到端模型将推理与控制耦合，导致空间定位模糊；而传统的模块化方法通常输出单一坐标，丢失了格子的几何边界信息，且对感知噪声敏感。

#### 2. 论文提出的方法论：AnySlot
AnySlot 采用了一种**两阶段的分层架构**，核心思想是将语言指令转化为显式的**空间视觉目标（Visual Goal）**作为中间表示。

*   **第一阶段：高层目标构建（High-level Goal Construction）**
    *   利用预训练的图像生成模型（如 Nano-Banana）在机器人头部相机的视图中，根据指令在目标插槽处渲染一个“蓝色球体”标记。
    *   通过深度图和相机内参将该标记反投影到 3D 空间，确定世界坐标系下的锚点。
    *   将该 3D 锚点重新投影到所有相机流（如手腕相机）中，生成视角一致的视觉覆盖层（Overlay）。
*   **第二阶段：底层目标条件策略（Low-level Goal-Conditioned Policy）**
    *   使用基于 PaliGemma-3B 主干和流匹配（Flow-matching）动作专家的 $\pi_{0.5}$ 模型。
    *   策略输入包含带有视觉标记的多视图图像，指令被固定为简单的“移动物体到蓝色球体处”，从而将复杂的语义推理从控制层解耦。

#### 3. 实验设计
*   **基准测试（SlotBench）**：论文推出了专门的仿真基准 SlotBench，包含 9 类任务：序数推理、尺寸比较、垂直推理、距离推理、组合关系、逻辑否定、模糊语言、功能性推理（Affordance）和常识推理。
*   **对比方法**：
    *   **扁平化 VLA**：Diffusion Policy, OpenVLA, $\pi_0, \pi_{0.5}$。
    *   **模块化方法**：RoboPoint, HAMSTER, AnyPlace。
    *   **Oracle**：由人类专家提供完美视觉目标的上限基准。
*   **评估指标**：成功率（SR）、指令准确率（IA，定位误差 < 0.02m）、粗略准确率（CA）。

#### 4. 资源与算力
*   **硬件**：实验使用了单台 **NVIDIA H200 GPU**。
*   **训练细节**：底层策略 $\pi_{0.5}$ 在合成数据集 $D_{syn}$ 上进行了全参数微调，训练步数为 **20,000 步**，Batch Size 为 **64**。
*   **推理延迟**：高层生成模块的推理时间约为 11-16 秒（取决于使用的生成模型），这在实时性要求极高的场景下是一个考虑因素。

#### 5. 实验数量与充分性
*   **实验规模**：每项任务进行 50 次试验，涵盖了 9 类不同的推理挑战。
*   **消融实验**：非常充分。作者对比了多种高层模型（Molmo, GPT-5.3, Gemini 3.1, Nano-Banana 等）和不同的底层策略（Diffusion Policy, OpenVLA-OFT 等），并验证了合成数据集对训练的重要性。
*   **客观性**：通过引入 Oracle 基准明确了性能瓶颈所在（主要是高层推理而非底层控制），实验设计较为公平。

#### 6. 论文的主要结论与发现
*   **解耦的优越性**：AnySlot 在 SlotBench 上的平均成功率接近 **90%**，远超所有扁平化 VLA 基线（几乎为 0%）。
*   **视觉目标 vs 坐标回归**：图像生成模型生成的视觉标记比 VLM 输出的坐标更精确，且能提供更好的几何上下文，显著提升了亚厘米级任务的成功率。
*   **零样本能力**：AnySlot 展现了极强的零样本迁移能力，能够处理训练集中未见过的复杂插槽布局和语言组合。

#### 7. 优点（亮点）
*   **创新的中间表示**：将“图像编辑/生成”引入机器人感知链路，把抽象的语言定位转化为直观的像素级目标。
*   **SlotBench 基准**：填补了机器人领域缺乏高精度空间推理放置任务基准的空白。
*   **多视角一致性**：通过 3D 锚点投影解决了多摄像头系统中的目标对齐问题。

#### 8. 不足与局限
*   **推理延迟**：依赖大型图像生成模型导致首帧响应时间较长（>10秒），难以应对动态环境。
*   **依赖深度信息**：第一阶段的 3D 重建高度依赖深度相机的精度，在反光或透明物体表面可能失效。
*   **应用限制**：目前主要针对“放置”这一特定动作，对于更复杂的长程任务（Long-horizon）或需要频繁调整策略的任务，该框架的灵活性尚待验证。

（完）
