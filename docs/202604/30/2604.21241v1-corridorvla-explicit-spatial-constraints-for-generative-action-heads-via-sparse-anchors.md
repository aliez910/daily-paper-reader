---
title: "CorridorVLA: Explicit Spatial Constraints for Generative Action Heads via Sparse Anchors"
title_zh: CorridorVLA：通过稀疏锚点为生成式动作头提供显式空间约束
authors: "Dachong Li, ZhuangZhuang Chen, Jin Zhang, Jianqiang Li"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21241v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 操纵基准测试中VLA动作生成的显式空间约束
tldr: 针对视觉-语言-动作（VLA）模型中空间引导通常隐含在潜特征中的问题，本文提出CorridorVLA。该方法通过预测稀疏空间锚点（增量物理变化）来构建显式的容差区域（走廊），为生成式动作头提供直接约束。在LIBERO-Plus基准测试中，CorridorVLA显著提升了SmolVLA和GR00T的成功率，证明了动作对齐的物理线索能有效增强生成式策略的性能与可解释性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21241v1/fig-001.webp\", \"caption\": \"Fig. 2. Framework. (A) The backbone predicts a small set of future key spatial increments, while the action output is augmented with the corresponding end-effector displacement fields. These key increments are then used to constrain action generation, requiring only a few additional prediction slots with minimal changes to the original VLA pipeline. (B) Spatial-change guidance provides a simple prior: manipulation trajectories tend to evolve smoothly, so sparse key increments can offer a safe, structured signal that reduces unstructured exploration under stochastic generation.\", \"page\": 3, \"index\": 1, \"width\": 512, \"height\": 410}]"
motivation: 现有的VLA模型主要依赖隐含的潜特征进行空间引导，缺乏对动作生成的显式物理约束。
method: 通过预测稀疏空间锚点定义动作走廊，并对偏离该区域的生成轨迹施加纠正梯度。
result: "在LIBERO-Plus基准上，该方法将SmolVLA和GR00T的成功率提升了3.4%至12.4%，其中GR00T-Corr变体达到了83.21%。"
conclusion: 动作对齐的物理线索能为生成式策略提供直接且可解释的约束，是视觉或潜特征引导的重要补充。
---

## 摘要
视觉-语言-动作（VLA）模型通常使用中间表示来连接多模态输入与连续控制，然而空间引导往往通过潜特征隐式注入。我们提出了 CorridorVLA，它将稀疏空间锚点预测为增量物理变化（例如 Δ-位置），并利用这些锚点在动作生成的训练目标中施加显式的容差区域。这些锚点定义了一个“走廊”，用于引导流匹配（flow-matching）动作头：如果轨迹所隐含的空间演化超出了该走廊，则会接收到修正梯度，同时允许接触和执行噪声引起的轻微偏差。在更具挑战性的 LIBERO-Plus 基准测试中，CorridorVLA 在 SmolVLA 和 GR00T 上均取得了持续的提升，相比相应的基线模型，成功率提高了 3.4% 至 12.4%；值得注意的是，我们的 GR00T-Corr 变体达到了 83.21% 的成功率。这些结果表明，与动作对齐的物理线索可以为生成式动作策略提供直接且可解释的约束，从而补充了以视觉或潜形式编码的空间引导。代码可在 https://github.com/corridorVLA 获取。

## Abstract
Vision--Language--Action (VLA) models often use intermediate representations to connect multimodal inputs with continuous control, yet spatial guidance is often injected implicitly through latent features. We propose $CorridorVLA$, which predicts sparse spatial anchors as incremental physical changes (e.g., $Δ$-positions) and uses them to impose an explicit tolerance region in the training objective for action generation. The anchors define a corridor that guides a flow-matching action head: trajectories whose implied spatial evolution falls outside it receive corrective gradients, while minor deviations from contacts and execution noise are permitted. On the more challenging LIBERO-Plus benchmark, CorridorVLA yields consistent gains across both SmolVLA and GR00T, improving success rate by $3.4\%$--$12.4\%$ over the corresponding baselines; notably, our GR00T-Corr variant reaches a success rate of $83.21\%$. These results indicate that action-aligned physical cues can provide direct and interpretable constraints for generative action policies, complementing spatial guidance encoded in visual or latent forms. Code is available at https://github.com/corridorVLA.

---

## 论文详细总结（自动生成）

以下是对论文《CorridorVLA: Explicit Spatial Constraints for Generative Action Heads via Sparse Anchors》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在连接多模态输入与连续控制时，空间引导信息通常是以隐式的方式（如通过潜特征或视觉特征）注入的。这种方式缺乏对动作生成的直接物理约束，导致模型在复杂操纵任务中容易产生不合理的轨迹。
*   **研究动机**：作者探索是否可以将空间引导表达为**直接的、文本风格的物理量**（如末端执行器的位移），并利用这些量在目标函数层面为动作生成施加显式的约束（即“走廊”约束），从而提高生成轨迹的物理合理性和可解释性。

### 2. 方法论
CorridorVLA 的核心思想是在 VLA 骨干网络和生成式动作头之间建立一种显式的空间耦合。
*   **稀疏锚点预测**：在 VLM 骨干网络中引入少量的可学习槽位（Slots），预测未来 $K$ 个时间点的末端执行器（EE）增量位置（$\Delta$-positions）。相比绝对位置，增量位置对视点和偏移更鲁棒。
*   **动作走廊约束（Corridor Constraint）**：
    *   **走廊缓冲（Buffer）**：定义一个容差区域（走廊）。如果生成的轨迹在锚点处的偏差超过阈值 $\delta$，则施加纠正梯度；如果在走廊内，则允许轻微的执行噪声。
    *   **走廊内一致性（Consistency）**：在轨迹进入走廊后，通过阶段性累积进度损失进一步精细化轨迹。
*   **流匹配（Flow Matching）集成**：将上述约束集成到流匹配动作头中。通过噪声感知权重（Noise-aware weighting），在去噪过程接近数据流形时（即 $t \to 0$ 时）加大空间约束的权重。
*   **锚点选择**：使用 Ramer–Douglas–Peucker (RDP) 算法和动态规划从原始轨迹中提取最具代表性的几何关键点作为锚点。

### 3. 实验设计
*   **数据集与场景**：主要在 **LIBERO**（包含 Long, Goal, Object, Spatial 四个子集）和更具挑战性的 **LIBERO-Plus** 基准测试上进行评估。
*   **对比方法**：
    *   **基线模型**：SmolVLA (0.45B) 和 GR00T。
    *   **其他 SOTA 模型**：$\pi_0$ (3.3B), OpenVLA-OFT, NORA, UniVLA, GraspVLA 等。
*   **评估指标**：任务成功率（Success Rate）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **效率提示**：文中提到 SmolVLA 是一个“负担得起且高效”的模型，且 CorridorVLA 仅增加了极少量的预测 Token（如 $K=3$），因此推理开销几乎没有增加。实验基于 LeRobot 框架实现。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流 VLA 骨干（SmolVLA, GR00T）上验证了通用性。
    *   在 LIBERO 和 LIBERO-Plus 两个基准上进行了大规模测试。
    *   **消融实验**：非常详尽，涵盖了走廊损失组件（Buffer vs Consistency）、锚点目标类型（绝对位置 vs 增量位置）、锚点选择策略（均匀采样 vs RDP）以及骨干网络的交互方式（因果掩码 vs 双向注意力）。
*   **充分性评价**：实验设计较为充分且客观。通过在不同规模和架构的模型上取得一致提升，证明了该方法的有效性。

### 6. 论文的主要结论
*   **性能提升**：CorridorVLA 在 LIBERO-Plus 上将 SmolVLA 的成功率提升了 **12.4%**，将 GR00T 的成功率提升了 **7.98%**（达到 83.21%）。
*   **物理约束的价值**：显式的物理锚点比隐式的潜特征引导更有效，尤其是在长程任务中，能显著减少随机生成过程中的无序探索。
*   **增量表示优于绝对表示**：预测 $\Delta$-positions 相比绝对坐标能更好地捕捉控制的本质并应对环境变化。

### 7. 优点与亮点
*   **轻量且高效**：几乎不增加模型参数量和推理延迟，仅通过修改损失函数和增加少量 Token 即可实现显著提升。
*   **可解释性**：通过预测物理锚点，开发者可以直观地观察模型预期的运动路径，增加了黑盒 VLA 模型的可解释性。
*   **即插即用**：该方法不依赖于特定的 VLM 架构，在 SmolVLA 和 GR00T 上的成功迁移证明了其普适性。

### 8. 不足与局限
*   **缺乏真机实验**：所有实验均在仿真环境（LIBERO）中完成，尚未在真实机器人平台上验证其对现实世界噪声和物理接触的鲁棒性。
*   **对比范围有限**：未与同样使用空间引导但基于图像/视频生成的重型模型（如 ReconVLA）进行直接的性能与计算成本对比。
*   **超参数敏感性**：走廊宽度 $\delta$ 和损失权重 $\lambda$ 的选择可能需要针对不同机器人硬件进行精细调优。

（完）
