---
title: Disentangled Point Diffusion for Precise Object Placement
title_zh: 用于精确物体放置的解耦点扩散模型
authors: "Lyuxing He, Eric Cai, Shobhit Aggarwal, Jianjun Wang, David Held"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11793v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 通过示教学习实现精准机器人放置
tldr: 针对机器人操作中端到端策略在泛化性和精度上的不足，本文提出TAX-DPD框架。该框架采用分层解耦的点云扩散模型，通过稠密高斯混合模型（GMM）预测全局放置先验，并利用解耦点云扩散模块进行局部几何推理。实验证明，该方法在放置精度、多模态覆盖及对新几何形状的泛化能力上达到了SOTA水平，且适用于刚性及非刚性物体。
selection_source: fresh_fetch
motivation: 现有的端到端机器人操作策略在处理新物体几何形状和实现高精度放置方面存在局限。
method: 提出一种分层框架，结合稠密高斯混合模型预测全局位置，并利用解耦的点云扩散模块分别处理物体几何与放置位姿。
result: 在仿真和真实世界的高精度工业插拔及布料悬挂任务中，该方法在精度和泛化性上均优于现有的SE(3)扩散等方法。
conclusion: 解耦的点云扩散机制能显著提升物体放置的精确度与鲁棒性，为复杂机器人操作任务提供了有效的模块化解决方案。
---

## 摘要
机器人操作领域的最新进展突显了从演示中学习（learning from demonstration）的有效性。然而，尽管端到端策略在表达能力和灵活性方面表现出色，但在泛化到新物体几何形状以及达到高精度方面仍面临挑战。另一种以物体为中心的方法将任务建模为预测目标物体的放置位姿，从而实现了问题的模块化分解。基于这种目标预测范式，我们提出了 TAX-DPD，这是一个分层的解耦点扩散框架，在放置精度、多模态覆盖以及对物体几何形状和场景配置变化的泛化能力方面均达到了最先进的性能。我们通过一种新型的前馈稠密高斯混合模型（GMM）对全局场景级放置进行建模，该模型生成了全局放置的空间稠密先验；随后，我们通过一种新型的解耦点云扩散模块对局部物体级配置进行建模，该模块分别对物体几何形状和放置坐标系进行扩散，从而实现精确的局部几何推理。有趣的是，我们证明了即使在刚性物体放置的背景下，我们的点云扩散方法也比之前基于 SE(3) 扩散的方法具有显著更高的准确性。我们在仿真环境中的一系列挑战性任务以及现实世界中的高精度工业插拔任务上验证了我们的方法。此外，我们还展示了仿真环境下的挂衣服任务结果，表明我们的框架可以进一步放宽对物体刚性的假设。

## Abstract
Recent advances in robotic manipulation have highlighted the effectiveness of learning from demonstration. However, while end-to-end policies excel in expressivity and flexibility, they struggle both in generalizing to novel object geometries and in attaining a high degree of precision. An alternative, object-centric approach frames the task as predicting the placement pose of the target object, providing a modular decomposition of the problem. Building on this goal-prediction paradigm, we propose TAX-DPD, a hierarchical, disentangled point diffusion framework that achieves state-of-the-art performance in placement precision, multi-modal coverage, and generalization to variations in object geometries and scene configurations. We model global scene-level placements through a novel feed-forward Dense Gaussian Mixture Model (GMM) that yields a spatially dense prior over global placements; we then model the local object-level configuration through a novel disentangled point cloud diffusion module that separately diffuses the object geometry and the placement frame, enabling precise local geometric reasoning. Interestingly, we demonstrate that our point cloud diffusion achieves substantially higher accuracy than a prior approach based on SE(3)-diffusion, even in the context of rigid object placement. We validate our approach across a suite of challenging tasks in simulation and in the real-world on high-precision industrial insertion tasks. Furthermore, we present results on a cloth-hanging task in simulation, indicating that our framework can further relax assumptions on object rigidity.

---

## 论文详细总结（自动生成）

这是一份关于论文《Disentangled Point Diffusion for Precise Object Placement》的结构化深度总结：

### 1. 核心问题与整体含义
该论文针对机器人操作中的**精确物体放置（Precise Object Placement）**问题展开研究。
*   **研究动机**：现有的端到端（End-to-End）策略虽然灵活，但在处理低容差任务（如工业插拔）和泛化到新物体几何形状时精度不足。
*   **核心痛点**：传统方法多依赖 SE(3) 位姿预测，但对于几何形状多变的物体，很难定义统一的参考坐标系。
*   **整体含义**：论文提出了一种名为 **TAX-DPD** 的分层框架，通过将目标预测分解为“全局位置初始化”和“局部几何解耦扩散”，实现了毫米级的放置精度，并能有效处理多模态（多个可行放置点）和非刚性物体。

### 2. 方法论
TAX-DPD 采用两阶段分层架构：
*   **全局放置初始化（Global Placement Initialization）**：
    *   使用前馈神经网络预测**稠密高斯混合模型（Dense GMM）**。
    *   为场景中的每个点预测一个高斯分布（权重和残差），从而在复杂场景中捕捉多个潜在的放置位置（多模态覆盖）。
*   **局部配置精化（Local Configuration Refinement）**：
    *   **核心思想**：在局部坐标系下，通过**解耦点扩散（Disentangled Point Diffusion）**同时预测物体的形状（Shape）和坐标系（Frame）。
    *   **技术细节**：将目标点云 $P^*_O$ 分解为均值中心化的形状 $\phi$ 和中心位移 $\rho$。扩散模型分别对这两部分进行去噪。
    *   **关键组件**：
        *   **重构嵌入（Reconstruction Embedding）**：将当前去噪的物体与场景点云结合编码，增强几何推理。
        *   **变形嵌入（Deformation Embedding）**：显式建模初始形状与去噪形状之间的位移。
        *   **旋转噪声（Rotation Noise）**：在训练中引入随机旋转扰动，提升模型对位姿变化的鲁棒性。
*   **刚性对齐**：对于刚性物体，最终通过 RANSAC-SVD 算法从预测的点云中恢复 SE(3) 变换。

### 3. 实验设计
*   **仿真场景**：
    *   **RPDiff 基准测试**：包括挂杯子（Mug/Rack）、插书（Book/Shelf）、堆叠罐子（Can/Cabinet）等 5 类任务。
    *   **DEDO 任务**：非刚性布料挂架任务，验证对变形物体的处理能力。
*   **真实世界实验**：
    *   使用 NIST 组装任务板，进行高精度工业连接器插拔（Waterproof, DSUB-25, SSD 连接器）。
*   **对比方法（Benchmarks）**：
    *   **RPDiff**：基于 SE(3) 扩散的领先方法。
    *   **TAX3D**：之前的点云扩散方法。
    *   **TAX-Pose**：基于软对应关系的位姿估计方法。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练参数**：提到了 Batch Size 为 16，学习率为 1e-4，使用了 5 层 Diffusion Transformer (DiT) 块，隐藏层维度为 128，扩散步数为 100 步。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验涵盖了 5 种不同难度的任务，每种任务进行 100 次测试。
    *   真实世界实验针对 3 种工业连接器进行了 80 次独立测试（每种 20 次）。
    *   进行了详尽的**消融实验**（共 8 组变体），验证了 GMM、解耦扩散、各类嵌入模块及旋转噪声的必要性。
*   **充分性评价**：实验设计非常充分且客观。通过对比“点云扩散”与“SE(3) 扩散”在相同架构下的表现，有力地证明了点云表示在处理几何泛化时的优越性。

### 6. 主要结论与发现
*   **精度突破**：TAX-DPD 在 RPDiff 任务上达到了 97% 的平均成功率，远超 RPDiff 原作的 88%。
*   **几何泛化**：点云扩散比 SE(3) 扩散更能适应未见过的物体几何形状，因为点云能直接推理底层几何特征。
*   **多模态能力**：Dense GMM 能够有效覆盖场景中所有可行的放置点，避免了局部最优。
*   **非刚性支持**：该框架无需修改即可直接应用于布料等非刚性物体的目标预测。

### 7. 优点
*   **分层解耦设计**：巧妙地解决了全局多模态搜索与局部高精度精化之间的尺度冲突。
*   **表示法创新**：证明了在精确放置任务中，点云生成比直接预测位姿矩阵更具鲁棒性。
*   **工业级精度**：在真实世界中实现了毫米级的插拔精度，具有极强的实用价值。

### 8. 不足与局限
*   **分割依赖**：假设目标物体已从场景中完美分割，这在复杂堆叠或遮挡严重的真实场景中可能难以实现。
*   **实时性挑战**：作为扩散模型，推理时需要多次迭代去噪，可能无法满足极高频率的动态调整需求。
*   **非刚性物体跟踪**：虽然能预测非刚性目标，但在真实世界中对非刚性物体的实时 3D 跟踪和闭环控制仍是难题（论文也提到将其留作未来工作）。

（完）
