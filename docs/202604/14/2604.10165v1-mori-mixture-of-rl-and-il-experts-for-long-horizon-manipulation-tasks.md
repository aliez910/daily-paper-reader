---
title: "MoRI: Mixture of RL and IL Experts for Long-Horizon Manipulation Tasks"
title_zh: MoRI：用于长程操纵任务的强化学习与模仿学习专家混合模型
authors: "Yaohang Xu, Lianjie Ma, Gewei Zuo, Wentao Zhang, Han Ding, Lijun Zhu"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10165v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 混合强化学习与模仿学习专家处理长程操纵任务
tldr: "针对长程机器人操纵任务中模仿学习（IL）易产生复合误差和强化学习（RL）采样效率低的问题，本文提出MoRI框架。该方法通过动作方差动态切换IL与RL专家，结合离线预训练与在线微调，并引入IL正则化以减少人工干预。实验表明，MoRI在多项复杂任务中实现了97.5%的成功率，显著提升了收敛速度并降低了干预成本。"
selection_source: fresh_fetch
motivation: 旨在解决IL在长程任务中的分布偏移问题以及RL在复杂操纵中采样效率低下且人工干预成本高昂的挑战。
method: 提出一种混合专家系统，根据动作方差动态切换IL与RL专家，并结合离线预训练、在线微调及IL正则化策略。
result: "在四项真实世界任务中，MoRI仅需2-5小时微调即可达到97.5%的平均成功率，且比基准RL算法减少了85.8%的人工干预。"
conclusion: MoRI通过动态融合IL与RL的优势，为实现高效、安全且低干预的复杂机器人操纵任务提供了一种有效的解决方案。
---

## 摘要
强化学习 (RL) 和模仿学习 (IL) 是机器人操纵中策略获取的标准框架。虽然 IL 能够高效地推导策略，但它面临着复合误差和分布偏移的问题。相反，RL 有助于自主探索，但往往受限于低样本效率和高昂的试错成本。由于现有的混合方法在处理复杂任务时通常表现不佳，我们提出了强化学习与模仿学习专家混合模型 (MoRI)。该系统根据专家动作的方差在 IL 和 RL 专家之间动态切换，以处理粗略运动和精细操纵。MoRI 采用离线预训练阶段，随后进行在线微调以加速收敛。为了保持探索安全性并最大限度地减少人工干预，该系统对 RL 组件应用了基于 IL 的正则化。在四个复杂的现实世界任务中的评估表明，MoRI 在 2 到 5 小时的微调内实现了 97.5% 的平均成功率。与基准 RL 算法相比，MoRI 减少了 85.8% 的人工干预，并将收敛时间缩短了 21%，证明了其在机器人操纵领域的强大能力。

## Abstract
Reinforcement Learning (RL) and Imitation Learning (IL) are the standard frameworks for policy acquisition in manipulation. While IL offers efficient policy derivation, it suffers from compounding errors and distribution shift. Conversely, RL facilitates autonomous exploration but is frequently hindered by low sample efficiency and the high cost of trial and error. Since existing hybrid methods often struggle with complex tasks, we introduce Mixture of RL and IL Experts (MoRI). This system dynamically switches between IL and RL experts based on the variance of expert actions to handle coarse movements and fine-grained manipulations. MoRI employs an offline pre-training stage followed by online fine-tuning to accelerate convergence. To maintain exploration safety and minimize human intervention, the system applies IL-based regularization to the RL component. Evaluation across four complex real-world tasks shows that MoRI achieves an average success rate of 97.5% within 2 to 5 hours of fine-tuning. Compared to baseline RL algorithms, MoRI reduces human intervention by 85.8% and shortens convergence time by 21%, demonstrating its capability in robotic manipulation.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MoRI**（强化学习与模仿学习专家混合模型）的框架，旨在解决长程机器人操纵任务中单一学习范式的局限性。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：机器人操纵任务通常包含“粗粒度运动”（如接近物体）和“细粒度接触操纵”（如精准插入）。
*   **核心矛盾**：
    *   **模仿学习 (IL)**：虽能高效获取基础策略，但存在复合误差和分布偏移问题，难以应对高精度要求。
    *   **强化学习 (RL)**：具备自主探索和鲁棒性，但在真实世界中采样效率极低，且频繁的试错会导致高昂的人工干预成本和硬件风险。
*   **研究目标**：开发一种能够动态结合 IL 的高效性和 RL 的鲁棒性的框架，以实现长程任务的高效、安全学习。

### 2. 方法论
MoRI 采用了基于 **混合专家 (MoE)** 架构的动态调度机制，核心流程分为两个阶段：
*   **核心思想**：利用**动作方差**作为专家置信度的指标。将确定性高的粗略运动分配给 IL 专家，将不确定性高、需精细调整的接触任务分配给 RL 专家。
*   **关键技术细节**：
    1.  **第一阶段：离线预训练**：使用少量（约20条）人类演示数据。通过 MSE 损失训练 BC（行为克隆）策略，并利用 AWAC（优势加权 Actor-Critic）算法初始化 RL 策略，使两者初步对齐人类策略。
    2.  **第二阶段：在线微调**：
        *   **门控网络 (Gating Network)**：根据状态输出 IL 和 RL 专家的权重。优化目标包含加权方差最小化、负载均衡和专家专业化损失。
        *   **IL 正则化**：在 RL 的 Actor 损失函数中引入 IL 策略的约束（MSE 距离），确保 RL 的探索不会偏离合理区域，提高安全性。
        *   **离散 BC 专家 (DBC)**：专门用于夹持器（Gripper）控制，相比传统的 DQN，DBC 在处理长程任务中的开关动作时更稳定、收敛更快。
    3.  **硬切换机制**：在每个时间步，系统根据门控权重直接选择 IL 或 RL 专家的动作输出。

### 3. 实验设计
*   **实验场景**：在 Franka Research 3 机械臂上进行了四项复杂的真实世界任务：
    1.  **抽屉收纳**：开抽屉、抓块、放入、关抽屉。
    2.  **盒装毛巾**：掀盖、放毛巾、盖盖。
    3.  **双插座插入**：将两组不同引脚的插头插入精确插槽。
    4.  **毛巾双折**：涉及多点接触的力控折叠。
*   **对比基准 (Benchmarks)**：
    *   **ConRFT**：一种现有的视觉-语言-动作（VLA）模型强化微调方法。
    *   **独立专家**：纯 BC 策略和纯 RL 策略。
    *   **消融实验**：移除 IL 正则化的版本、使用 DQN 代替 DBC 的版本。

### 4. 资源与算力
*   **硬件**：使用单块 **NVIDIA RTX 4090 GPU** 进行训练。
*   **训练时长**：在线微调阶段每个任务耗时约 **2 到 5 小时**。
*   **数据量**：仅需 20 条初始演示数据。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种不同类型的操纵任务，每项任务均进行了 20 次独立测试以计算成功率。
*   **充分性评价**：
    *   **对比充分**：不仅对比了最先进的混合方法（ConRFT），还通过与单一专家的对比证明了 MoE 架构的必要性。
    *   **消融深入**：针对 IL 正则化对动作平滑性的影响、DBC 对长程任务稳定性的影响进行了详细分析。
    *   **客观性**：实验在真实物理环境下完成，而非仅在仿真中，具有较高的说服力。

### 6. 主要结论与发现
*   **性能卓越**：MoRI 在四项任务中达到了 **97.5%** 的平均成功率。
*   **效率提升**：相比基准 RL 算法，收敛速度提升了 **21%**。
*   **人工成本降低**：人工干预次数大幅减少了 **85.8%**，演示数据在缓存中的占比仅为 7%，而自主成功样本量增加了 111.7%。
*   **调度逻辑**：实验证实，系统在执行固定动作（如开关抽屉）时倾向于调用 IL，而在处理随机性强的抓取和插入时倾向于调用 RL。

### 7. 优点
*   **动态互补**：通过方差感知实现了 IL 和 RL 的无缝切换，解决了 IL 无法纠错和 RL 探索盲目的问题。
*   **安全性高**：IL 正则化有效地约束了 RL 的探索空间，使其在真实机器人上训练时更加安全。
*   **实用性强**：训练时间短（几小时量级），对人类演示数据的依赖度低，适合实际部署。

### 8. 不足与局限
*   **泛化能力限制**：虽然在特定任务内表现优异，但在面对剧烈的环境扰动时，自主恢复能力仍有提升空间。
*   **缺乏高层推理**：目前主要依赖底层感知，未集成视觉语言模型（VLM）进行高层逻辑推理，难以处理更复杂的非结构化指令。
*   **人工依赖**：虽然干预减少，但初期仍需人类提供演示和奖励函数的分类器训练数据。

（完）
