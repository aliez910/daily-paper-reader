---
title: "AnySlot: Goal-Conditioned Vision-Language-Action Policies for Zero-Shot Slot-Level Placement"
title_zh: AnySlot：用于零样本插槽级放置的目标条件化视觉-语言-动作策略
authors: "Zhaofeng Hu, Sifan Zhou, Qinbo Zhang, Rongtao Xu, Qi Su, Ci-Jyun Liang"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10432v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 用于通用机器人操纵的目标条件VLA策略
tldr: AnySlot是一个针对零样本槽位级放置任务的VLA框架，旨在解决现有模型在复杂指令下精度不足的问题。它通过引入显式空间视觉目标（场景标记）作为中间表示，将任务解耦为高层语义定位与底层精准执行，并配套推出了SlotBench基准测试。实验表明，AnySlot在零样本场景下的放置精度和鲁棒性显著优于现有基准模型。
selection_source: fresh_fetch
motivation: 现有的VLA策略在处理需要亚厘米级精度和复杂语言理解的槽位放置任务时，难以同时保证语义定位的准确性和动作执行的精确度。
method: AnySlot通过将语言指令转化为显式的视觉场景标记作为中间目标，并利用目标条件化的VLA策略实现高层选择与底层控制的有效解耦。
result: 在包含九类任务的SlotBench基准测试中，AnySlot在零样本槽位级放置任务上的表现大幅领先于传统的扁平化VLA模型和模块化定位方法。
conclusion: 将复杂的组合指令解耦为显式视觉目标引导的层次化架构，是提升机器人策略在精密操作任务中泛化能力和准确性的有效途径。
---

## 摘要
视觉-语言-动作（VLA）策略已成为通用机器人操控的一种多功能范式。然而，在组合式语言指令下进行精确的物体放置，对于现代单体式 VLA 策略而言仍是一个重大挑战。插槽级任务既需要可靠的插槽定位（grounding），也需要亚厘米级的执行精度。为此，我们提出了 AnySlot，该框架通过引入显式的空间视觉目标作为语言定位与控制之间的中间表示，降低了组合复杂度。AnySlot 通过生成场景标记（scene marker）将语言转化为显式的视觉目标，随后利用目标条件化 VLA 策略执行该目标。这种分层设计有效地将高层插槽选择与底层执行解耦，确保了语义准确性和空间鲁棒性。此外，考虑到现有基准测试缺乏此类对精度要求极高的任务，我们推出了 SlotBench，这是一个包含九个任务类别的综合仿真基准，专门用于评估插槽级放置中的结构化空间推理。大量实验表明，AnySlot 在零样本插槽级放置任务中显著优于扁平化 VLA 基线模型和以往的模块化定位方法。

## Abstract
Vision-Language-Action (VLA) policies have emerged as a versatile paradigm for generalist robotic manipulation. However, precise object placement under compositional language instructions remains a major challenge for modern monolithic VLA policies. Slot-level tasks require both reliable slot grounding and sub-centimeter execution accuracy. To this end, we propose AnySlot, a framework that reduces compositional complexity by introducing an explicit spatial visual goal as an intermediate representation between language grounding and control. AnySlot turns language into an explicit visual goal by generating a scene marker, then executes this goal with a goal-conditioned VLA policy. This hierarchical design effectively decouples high-level slot selection from low-level execution, ensuring both semantic accuracy and spatial robustness. Furthermore, recognizing the lack of existing benchmarks for such precision-demanding tasks, we introduce SlotBench, a comprehensive simulation benchmark featuring nine task categories tailored to evaluate structured spatial reasoning in slot-level placement. Extensive experiments show that AnySlot significantly outperforms flat VLA baselines and previous modular grounding methods in zero-shot slot-level placement.

---

## 论文详细总结（自动生成）

这是一份关于论文《AnySlot: Goal-Conditioned Vision-Language-Action Policies for Zero-Shot Slot-Level Placement》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是机器人操作中一个极具挑战性的任务：**插槽级精准放置（Slot-Level Placement）**。
*   **研究动机**：现有的视觉-语言-动作（VLA）策略在处理粗粒度的“取放”任务时表现良好，但在面对需要亚厘米级精度和复杂空间推理（如“放在左起第二个格子里”或“避开最不稳定的区域”）的插槽任务时，往往表现不佳。
*   **核心矛盾**：
    *   **扁平化（Flat）策略**：将语言直接映射到动作，难以在处理复杂组合指令的同时保持几何精度。
    *   **模块化（Modular）策略**：通常依赖视觉语言模型（VLM）预测坐标，但坐标预测往往过于粗糙且缺乏几何上下文。
*   **整体含义**：AnySlot 旨在通过引入“显式空间视觉目标”作为中间表示，解耦高层语义推理与底层精准控制。

### 2. 方法论：核心思想与关键技术
AnySlot 采用两阶段分层架构：
*   **核心思想**：将语言指令转化为图像中的一个**显式视觉标记（如蓝色球体）**，然后让底层策略去“追踪”并放置到该标记处。
*   **关键技术细节**：
    1.  **高层目标构建（Stage 1）**：
        *   使用现成的图像生成模型（如 Nano-Banana）在主摄像头视图中根据指令生成一个带有蓝色球体标记的引导图。
        *   利用深度图和相机标定，将该标记中心点反投影到 3D 世界空间，形成一个“世界锚点”。
        *   将该锚点重新投影到所有摄像头流（如手眼相机、侧向相机）中，生成视图一致的视觉目标叠加层。
    2.  **底层目标条件化策略（Stage 2）**：
        *   采用连续生成式 VLA 策略（基于 $\pi_{0.5}$，使用 PaliGemma-3B 骨干网络）。
        *   策略输入为带有视觉标记的多视图图像，指令被固定为简单的“移动物体到蓝色球体引导的插槽”。
        *   通过这种方式，底层策略只需专注于视觉引导下的精准对齐，而无需理解复杂的组合语言。

### 3. 实验设计
*   **基准测试（SlotBench）**：论文推出了专门的仿真基准，包含 9 类任务：序数推理、尺寸比较、高度推理、距离推理、组合关系、逻辑否定、模糊语言、适宜性推理和常识推理。
*   **数据集**：
    *   **合成数据集 ($D_{syn}$)**：在 SAPIEN 仿真器中收集，包含带有视觉标记的抓取和放置轨迹，用于训练底层策略。
    *   **标准数据集 ($D_{std}$)**：仅含语言指令，用于对比实验。
*   **对比方法**：
    *   **扁平化 VLA**：Diffusion Policy, OpenVLA, $\pi_0, \pi_{0.5}$。
    *   **模块化方法**：RoboPoint, HAMSTER, AnyPlace。
    *   **Oracle**：由人类专家提供完美的高层目标作为性能上限。

### 4. 资源与算力
*   **硬件**：使用了单台 **NVIDIA H200 GPU**。
*   **训练细节**：对 $\pi_{0.5}$ 进行了全参数微调，训练步数为 **20,000 步**，Batch Size 为 **64**。
*   **推理延迟**：文中提到图像生成模型（如 Nano-Banana 2）的推理延迟约为 11 秒，这在实时性要求极高的场景下是一个考虑因素。

### 5. 实验数量与充分性
*   **实验规模**：每项任务进行 50 次试验，覆盖了 9 大类任务和至少 45 种独特的场景布局。
*   **消融实验**：
    *   对比了不同的高层定位模块（各种 VLM 与图像生成器的对比）。
    *   对比了不同的底层策略架构（Diffusion Policy vs VLA）。
    *   验证了合成数据集对训练的重要性。
*   **充分性评价**：实验设计非常全面，不仅在仿真中验证了复杂的逻辑推理能力，还在真实世界（UR10e 机器人）中验证了目标重构的几何一致性。对比基线涵盖了当前最前沿的 VLA 模型，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **解耦的优越性**：AnySlot 在 SlotBench 上的平均成功率接近 **90%**，而传统的扁平化 VLA 模型在绝大多数复杂推理任务中几乎完全失败。
*   **视觉目标的精度**：图像生成模型生成的视觉标记比 VLM 预测的坐标具有更高的空间保真度和语义对齐度。
*   **泛化能力**：AnySlot 展现了强大的零样本泛化能力，能够处理训练中未见过的插槽布局和复杂的组合指令。

### 7. 优点（亮点）
*   **创新的中间表示**：将抽象的语言转化为具象的视觉标记，极大地降低了底层策略的学习难度。
*   **视图一致性**：通过 3D 锚点投影技术，解决了多摄像头系统中视觉引导的一致性问题。
*   **高质量基准**：SlotBench 填补了机器人操作领域缺乏精细化、逻辑化插槽放置评估工具的空白。

### 8. 不足与局限
*   **推理延迟**：依赖外部图像生成模型会导致显著的初始延迟（约 11 秒），难以应对动态变化的环境。
*   **误差累积**：如果高层生成模型错误地放置了标记，底层策略无法自我纠正（尽管实验显示生成模型已足够强大）。
*   **应用限制**：目前主要针对静态插槽放置任务，对于需要频繁交互或长程规划的任务，该框架的适用性尚待验证。

（完）
