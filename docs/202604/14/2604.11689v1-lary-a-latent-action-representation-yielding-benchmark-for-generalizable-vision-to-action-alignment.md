---
title: "LARY: A Latent Action Representation Yielding Benchmark for Generalizable Vision-to-Action Alignment"
title_zh: LARY：一个用于可泛化视觉到动作对齐的潜在动作表示生成基准
authors: "Dujun Nie, Fengjiao Chen, Qi Lv, Jun Kuang, Xiaoyu Li, Xuezhi Cao, Xunliang Cai"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11689v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 评估视觉到动作对齐中潜动作表示的基准测试
tldr: 针对视觉-语言-动作（VLA）模型中显式动作数据短缺的问题，本文提出了LARY基准，用于评估从视频中提取的潜动作表示在语义理解和机器人控制方面的能力。通过包含百万级视频和多模态轨迹的大规模数据集，研究发现通用视觉基础模型在动作对齐上优于专门的具身模型，且潜空间比像素空间更适合物理动作对齐。
selection_source: fresh_fetch
motivation: 旨在解决如何从大规模无标注人类视频中提取有效的潜动作表示，以克服VLA模型训练中动作数据匮乏的挑战。
method: 提出了LARY基准框架，通过涵盖151类动作、百万级视频及多具身运动轨迹的大规模数据集，统一评估高层语义动作和底层机器人控制。
result: 实验表明通用视觉基础模型在动作表征上优于专门的具身模型，且基于潜空间的视觉表征比像素空间更契合物理动作空间。
conclusion: 研究强调了语义级抽象在视觉到动作映射中的核心作用，证明了通用视觉知识对物理控制具有内在的促进价值。
---

## 摘要
虽然显式动作数据的短缺限制了视觉-语言-动作（VLA）模型，但人类动作视频提供了一个可扩展但未标注的数据源。利用大规模人类视频数据集的一个关键挑战在于将视觉信号转化为独立于本体的表示，即潜在动作（latent actions）。然而，潜在动作表示从视觉观测中推导出鲁棒控制的能力尚未得到严格评估。我们提出了 LARY（Latent Action Representation Yielding）基准，这是一个统一的框架，用于在高级语义动作（做什么）和低级机器人控制（怎么做）两个层面上评估潜在动作表示。该精心策划的数据集包含超过 100 万个视频（1,000 小时），涵盖 151 个动作类别，以及跨越不同具身智能体和环境的 62 万个图像对和 59.5 万条运动轨迹。我们的实验揭示了两个关键见解：（i）在没有任何动作监督的情况下训练的通用视觉基础模型，其表现始终优于专门的具身潜在动作模型；（ii）基于潜空间的视觉空间在本质上比基于像素的空间能更好地与物理动作空间对齐。这些结果表明，通用视觉表示固有地编码了用于物理控制的动作相关知识，并且语义层面的抽象是比像素级重建更有效的视觉到动作路径。

## Abstract
While the shortage of explicit action data limits Vision-Language-Action (VLA) models, human action videos offer a scalable yet unlabeled data source. A critical challenge in utilizing large-scale human video datasets lies in transforming visual signals into ontology-independent representations, known as latent actions. However, the capacity of latent action representation to derive robust control from visual observations has yet to be rigorously evaluated. We introduce the Latent Action Representation Yielding (LARY) Benchmark, a unified framework for evaluating latent action representations on both high-level semantic actions (what to do) and low-level robotic control (how to do). The comprehensively curated dataset encompasses over one million videos (1,000 hours) spanning 151 action categories, alongside 620K image pairs and 595K motion trajectories across diverse embodiments and environments. Our experiments reveal two crucial insights: (i) General visual foundation models, trained without any action supervision, consistently outperform specialized embodied latent action models. (ii) Latent-based visual space is fundamentally better aligned to physical action space than pixel-based space. These results suggest that general visual representations inherently encode action-relevant knowledge for physical control, and that semantic-level abstraction serves as a fundamentally more effective pathway from vision to action than pixel-level reconstruction.

---

## 论文详细总结（自动生成）

### LARY：一个用于可泛化视觉到动作对齐的潜在动作表示生成基准

#### 1. 核心问题与整体含义
视觉-语言-动作（VLA）模型的发展受限于高质量、带动作标注的机器人数据稀缺。虽然互联网上存在海量的人类动作视频，但这些视频缺乏显式的控制信号。研究者尝试通过“潜在动作（Latent Actions）”模型将视觉变化转化为离散或连续的表征，但目前缺乏一个统一、严格的基准来评估这些表征的质量。
**LARY 基准**旨在解决这一问题，通过解耦表征学习与下游策略执行，直接评估潜在动作表征在**高层语义理解（做什么）**和**底层物理控制（怎么做）**两个维度上的有效性。

#### 2. 方法论
LARY 提出了一个分层的评估框架和自动化数据处理流程：
*   **核心思想**：将潜在动作表征（$z$）通过两个任务进行探测：
    *   **语义动作分类（$f_{sem}: Z \to C$）**：评估表征是否包含动作类别信息。
    *   **物理执行回归（$f_{dyn}: Z \to A$）**：评估表征是否能重建末端执行器的运动轨迹。
*   **关键技术细节**：
    *   **自动化数据引擎**：利用大模型（如豆包-1.5-pro-vision）对异构视频数据集进行自动切分、语义对齐和视角过滤，构建了统一的动作分类体系。
    *   **分层探测协议**：涵盖运动学层面的原子原语（如“向上移动”）和任务层面的复合行为（如“倒水”、“折叠”）。
    *   **通用潜在动作模型（General LAMs）**：提出将 LAPA 等训练范式嫁接到冻结的通用视觉编码器（如 DINOv3, SigLIP 2）上，利用视觉先验增强动作表征。

#### 3. 实验设计
*   **数据集与场景**：
    *   **分类任务**：包含 120 万个视频剪辑，涵盖 151 个动作类别。数据源自 LIBERO、Ego4D、Something-Something v2、EPIC-KITCHENS、AgiBotWorld-Beta 等。
    *   **回归任务**：包含 59.5 万条运动轨迹。涉及 CALVIN、VLABench、RoboCOIN 等，涵盖 11 种机器人平台（单臂、双臂、半身人型）。
*   **对比方法**：评估了 11 个模型，分为四类：
    1.  **具身 LAM**：LAPA, UniVLA, villa-X。
    2.  **通用语义编码器**：DINOv3, V-JEPA 2。
    3.  **生成式像素编码器**：Wan2.2 VAE, FLUX.2-dev VAE。
    4.  **通用 LAM（本文提出）**：基于 DINOv2/v3, SigLIP 2, MAGVIT2 的混合模型。

#### 4. 资源与算力
*   **算力设备**：使用了 **8 台 NVIDIA H800 (141GB) GPU**。
*   **训练细节**：通用 LAM 变体在包含 200 万帧的内部数据集上训练了 **100,000 个步数（steps）**。输入图像分辨率统一为 224x224。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的跨模型对比（11 个模型）、跨领域评估（人类 vs 机器人、自我中心 vs 他我中心视角）。
*   **消融实验**：针对量化瓶颈（Codebook 大小）、序列长度、潜空间维度以及采样步长（Stride）进行了详细的消融研究。
*   **客观性与公平性**：通过冻结编码器权重、使用统一的 4 层 Attentive Probe 分类器和 MLP 回归器，确保了不同表征之间的比较是在相同“解码能力”前提下进行的，具有较高的客观性和公平性。

#### 6. 主要结论与发现
*   **通用视觉模型胜出**：在没有任何动作监督的情况下，通用的视觉基础模型（如 V-JEPA 2, DINOv3）在动作分类和回归任务上均显著优于专门的具身潜在动作模型。
*   **潜空间优于像素空间**：基于潜空间的视觉表征比基于像素的表征（如视频生成模型的 VAE）能更好地与物理动作空间对齐。
*   **语义抽象是关键路径**：实验证明，从视觉到动作的有效路径在于语义层面的抽象，而非像素级的重建。
*   **表征坍缩风险**：专门的具身模型由于数据规模小或约束过早，容易出现表征质量受限的问题。

#### 7. 优点
*   **解耦评估**：首次将潜在动作表征的质量与下游策略性能解耦，提供了更纯粹的表征评估手段。
*   **规模宏大**：构建了目前已知规模最大、覆盖面最广（151 类动作、11 种具身）的潜在动作评估基准。
*   **自动化流程**：开发的自动化数据引擎解决了具身智能领域长期存在的标注不一致和数据孤岛问题。

#### 8. 不足与局限
*   **机器人动作偏见**：通用 LAM 虽然在人类动作上表现优异，但在特定机器人动作上的表现仍有提升空间，存在“人类中心”的偏见。
*   **硬件依赖**：虽然评估了多种具身，但对于极高自由度（如灵巧手细粒度操作）的逆动力学建模仍是一个挑战。
*   **应用限制**：目前主要关注表征质量，尚未在大规模闭环部署中验证这些表征对最终任务成功率的直接贡献。

（完）
