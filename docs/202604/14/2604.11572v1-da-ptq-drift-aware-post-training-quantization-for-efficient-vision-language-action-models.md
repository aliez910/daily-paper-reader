---
title: "DA-PTQ: Drift-Aware Post-Training Quantization for Efficient Vision-Language-Action Models"
title_zh: DA-PTQ：面向高效视觉-语言-动作模型的漂移感知后训练量化
authors: "Siyuan Xu, Tianshi Wang, Fengling Li, Lei Zhu, Heng Tao Shen"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11572v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 视觉-语言-动作模型在机器人控制中的高效部署
tldr: 针对具身智能中视觉-语言-动作模型（VLA）部署受限的问题，本文提出了一种漂移感知的训练后量化方法（DA-PTQ）。研究发现量化误差在序列控制中会累积导致运动漂移，为此设计了跨空间表示补偿和运动驱动的混合精度分配策略。该方法在低比特设置下显著减少了运动轨迹偏差，使VLA能在资源受限的机器人平台上高效运行，且性能接近全精度模型。
selection_source: fresh_fetch
motivation: 传统的训练后量化直接应用于VLA时，量化扰动会在序列决策中累积并放大，导致严重的运动轨迹漂移和性能下降。
method: 提出了DA-PTQ框架，通过跨空间表示补偿缓解多模态与动作空间间的失真，并利用运动驱动的混合精度分配策略最小化轨迹误差。
result: 实验证明DA-PTQ在低比特量化下显著降低了运动学漂移，实现了与全精度模型相当的控制性能。
conclusion: DA-PTQ为在资源受限的机器人平台上高效部署高性能VLA模型提供了一种有效的量化解决方案。
---

## 摘要
视觉-语言-动作模型 (VLAs) 在具身智能领域展现出巨大潜力，但由于其对内存和计算的高需求，在资源受限的机器人上部署仍面临挑战。虽然后训练量化 (PTQ) 提供了一种高效的解决方案，但直接将 PTQ 应用于 VLAs 往往会导致在序列控制过程中出现严重的性能下降。我们发现时间误差累积是一个关键因素，即视觉-语言到动作接口处的量化扰动被逐渐放大，导致执行轨迹中出现运动学漂移。为了解决这一问题，我们提出了漂移感知后训练量化 (DA-PTQ)，它将量化建模为序列决策过程中的漂移感知优化问题。DA-PTQ 包含两个组件：(1) 跨空间表示补偿，旨在减轻多模态表示与动作空间之间的结构化失真，以提高动作一致性；(2) 运动驱动的混合精度分配，通过最小化轨迹级运动误差来分配位宽。大量实验表明，DA-PTQ 显著减少了运动学漂移，并在低比特设置下实现了与全精度模型相当的性能，从而使 VLAs 在资源受限的机器人平台上进行实际部署成为可能。

## Abstract
Vision-Language-Action models (VLAs) have demonstrated strong potential for embodied AI, yet their deployment on resource-limited robots remains challenging due to high memory and computational demands. While Post-Training Quantization (PTQ) provides an efficient solution, directly applying PTQ to VLAs often results in severe performance degradation during sequential control. We identify temporal error accumulation as a key factor, where quantization perturbations at the vision-language-to-action interface are progressively amplified, leading to kinematic drift in executed trajectories. To address this issue, we propose Drift-Aware Post-Training Quantization (DA-PTQ), which formulates quantization as a drift-aware optimization problem over sequential decision processes. DA-PTQ consists of two components: (1) Cross-Space Representation Compensation, which mitigates structured distortions between multimodal representations and action space to improve action consistency, and (2) Motion-Driven Mixed-Precision Allocation, which assigns bit-widths by minimizing trajectory-level motion errors. Extensive experiments show that DA-PTQ significantly reduces kinematic drift and achieves comparable performance to full-precision models under low-bit settings, enabling practical deployment of VLAs on resource-limited robotic platforms.

---

## 论文详细总结（自动生成）

这是一份关于论文《DA-PTQ: Drift-Aware Post-Training Quantization for Efficient Vision-Language-Action Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉-语言-动作模型（VLA）虽然在具身智能中表现出色，但其巨大的参数量（如 7B+）导致在资源受限的机器人平台上部署困难。
*   **研究动机**：传统的后训练量化（PTQ）方法在处理 VLA 时，往往会导致严重的性能下降。作者发现，这是因为量化误差在序列决策过程中会随时间累积并被机器人动力学放大，产生**运动学漂移（Kinematic Drift）**。
*   **整体含义**：本文旨在开发一种无需重新训练的量化框架，通过显式建模和抑制运动学漂移，实现 VLA 模型的高效、稳定部署。

### 2. 方法论
DA-PTQ 将量化视为一个“漂移感知”的优化问题，包含三个阶段：
*   **漂移剖析（Drift Profiling）**：通过全精度模型的前向传播收集激活统计数据，并利用**结构化雅可比矩阵（Structural Jacobian）**估计不同动作维度（如平移、旋转）对长程轨迹漂移的影响权重。
*   **跨空间表示补偿（CSRC）**：
    *   针对量化引起的感知-动作接口处的分布失真，采用分层补偿方案。
    *   利用仿射变换和低秩更新（基于 SVD）来对齐量化后与全精度模型的激活分布。
    *   这些补偿参数最终被“折叠”进量化后的权重中，**不增加推理开销**。
*   **运动驱动的混合精度分配（DA-MPA）**：
    *   根据漂移敏感度得分对模型层进行排序。
    *   将最敏感的层（如前 30%）保留为高精度（BF16），而将其余层压缩为低比特（如 W4），以在压缩率和控制稳定性之间取得平衡。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **SimplerEnv**：标准化的机器人操纵模拟基准。
    *   **WidowX 机器人**：用于评估域内（In-domain）任务性能（如放勺子、放胡萝卜等）。
    *   **Google Robot**：用于评估跨域（Cross-domain）零样本泛化能力。
    *   **校准集**：从 BridgeData V2 中提取的 512 条代表性轨迹。
*   **Benchmark 与对比方法**：
    *   **CogACT (FP)**：全精度基准模型。
    *   **VLA-Cache**：基于缓存的推理加速方法。
    *   **QuantVLA**：现有的 VLA 特定 PTQ 方法（基于静态敏感度指标）。

### 4. 资源与算力
*   **硬件环境**：实验部署在 **NVIDIA RTX 5090 GPU** 上。
*   **算力消耗**：由于是 PTQ 方法，该框架**无需重新训练或微调**。校准过程仅涉及前向传播和轻量级的梯度累积，计算成本极低。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了两个不同的机器人实体（WidowX 和 Google Robot）。
    *   测试了多种操纵任务（视觉匹配、变体聚合等）。
    *   进行了详细的**消融实验**，分别验证了 CSRC 和 DA-MPA 的有效性。
*   **充分性评价**：实验设计较为充分。通过跨域测试验证了量化参数的泛化性，通过与最新基准（如 QuantVLA）的对比证明了其优越性。实验结果包含了成功率、内存减少比例和推理加速比，评价维度客观全面。

### 6. 主要结论与发现
*   **性能保持**：在 W4A8 量化设置下，DA-PTQ 在 WidowX 上的平均成功率达到 48.9%（全精度为 51.3%），显著优于 QuantVLA（43.5%）。
*   **效率提升**：实现了约 **42.5% 的内存减少**和 **54.8% 的推理加速**。
*   **漂移抑制**：定性轨迹分析显示，DA-PTQ 生成的动作曲线更加平滑，有效消除了低比特量化常引发的高频振荡和运动漂移。

### 7. 优点（亮点）
*   **漂移感知视角**：首次将机器人运动学中的雅可比矩阵引入量化敏感度分析，抓住了具身智能模型量化的核心痛点。
*   **零推理开销**：通过权重折叠技术，在提升精度的同时不增加任何运行时的计算负担。
*   **强泛化性**：仅需少量校准数据即可在不同机器人实体和任务上保持稳定性能。

### 8. 不足与局限
*   **运动学简化**：雅可比矩阵的推导基于简化的平面虚拟链模型，可能无法完全捕捉复杂、非线性或存在频繁物理接触（如摩擦、碰撞）的动态场景。
*   **校准分布依赖**：虽然表现出一定的泛化性，但校准过程仍假设了固定的数据分布，在极端环境变化下的鲁棒性有待进一步验证。
*   **架构限制**：目前主要针对基于扩散（Diffusion-based）的 VLA 架构进行优化，对其他动作生成头（如自回归 Token 预测）的适用性未做深入探讨。

（完）
