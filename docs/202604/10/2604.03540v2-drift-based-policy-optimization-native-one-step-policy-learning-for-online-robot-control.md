---
title: "Drift-Based Policy Optimization: Native One-Step Policy Learning for Online Robot Control"
title_zh: 基于漂移的策略优化：面向在线机器人控制的原生单步策略学习
authors: "Yuxuan Gao, Yedong Shen, Shiqi Zhang, Wenhao Yu, Yifan Duan, Jia pan, Jiajia Wu, Jiajun Deng, Yanyong Zhang"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03540v2"
tags: ["query:rob-il"]
score: 9.0
evidence: 用于高频闭环控制的一步生成式策略
tldr: 针对多步生成式策略在机器人控制中推理速度慢、难以实现高频闭环控制的问题，本文提出了一种原生单步生成式策略框架。该框架包含漂移基策略（DBP）和漂移基策略优化（DBPO），通过将迭代优化过程内化到模型参数中，实现了在保持多峰动作建模能力的同时，仅需单步推理即可完成动作生成。实验证明该方法在离线模仿学习和在线强化学习中均表现优异，推理速度提升达100倍，并成功应用于105.2 Hz的实机双臂机器人控制。
selection_source: fresh_fetch
motivation: 多步生成式策略（如扩散模型）因推理时需要多次迭代去噪，导致计算开销巨大，难以满足高频闭环控制和在线强化学习的需求。
method: 提出漂移基策略（DBP）利用定点漂移目标将迭代细化过程内化为单步生成，并结合DBPO框架实现稳定的在线强化学习更新。
result: DBP在保持多峰建模能力的同时，推理速度比扩散策略快100倍，且在多个基准测试和实机双臂机器人任务中达到或超过了多步策略的性能。
conclusion: 该研究通过将细化过程从推理阶段转移到训练阶段，成功实现了高效、高性能的原生单步机器人控制策略。
---

## 摘要
虽然多步生成式策略通过建模多模态动作分布在机器人操作中取得了强劲性能，但它们在推理时需要多步迭代去噪。因此，生成每个动作都需要数十到数百次网络函数评估（NFEs），这使得它们在高频闭环控制和在线强化学习（RL）中成本高昂。为了解决这一局限性，我们提出了一种原生单步生成式策略的两阶段框架，将细化过程从推理阶段转移到训练阶段。首先，我们引入了基于漂移的策略（DBP），它利用不动点漂移目标将迭代细化内化到模型参数中，从而在设计上构建出单步生成式骨干网络，同时保留了多模态动作建模能力。其次，我们开发了基于漂移的策略优化（DBPO），这是一种在线强化学习框架，为预训练的骨干网络配备了兼容的随机接口，在不牺牲单步部署特性的情况下实现了稳定的同策略（on-policy）更新。广泛的实验证明了所提框架在离线模仿学习、在线微调和真实世界控制场景中的有效性。DBP 在推理速度提升高达 100 倍的同时，性能达到或超过了多步扩散策略。在具有挑战性的操作基准测试中，它也始终优于现有的单步基准模型。此外，DBPO 在在线设置中实现了有效且稳定的策略改进。在真实双臂机器人上的实验证明了 105.2 Hz 的可靠高频控制。

## Abstract
Although multi-step generative policies achieve strong performance in robotic manipulation by modeling multimodal action distributions, they require multi-step iterative denoising at inference time. Each action therefore needs tens to hundreds of network function evaluations (NFEs), making them costly for high-frequency closed-loop control and online reinforcement learning (RL). To address this limitation, we propose a two-stage framework for native one-step generative policies that shifts refinement from inference to training. First, we introduce the Drift-Based Policy (DBP), which leverages fixed-point drifting objectives to internalize iterative refinement into the model parameters, yielding a one-step generative backbone by design while preserving multimodal action modeling capacity. Second, we develop Drift-Based Policy Optimization (DBPO), an online RL framework that equips the pretrained backbone with a compatible stochastic interface, enabling stable on-policy updates without sacrificing the one-step deployment property. Extensive experiments demonstrate the effectiveness of the proposed framework across offline imitation learning, online fine-tuning, and real-world control scenarios. DBP matches or exceeds the performance of multi-step diffusion policies while achieving up to $100\times$ faster inference. It also consistently outperforms existing one-step baselines on challenging manipulation benchmarks. Moreover, DBPO enables effective and stable policy improvement in online settings. Experiments on a real-world dual-arm robot demonstrate reliable high-frequency control at 105.2 Hz.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为**基于漂移的策略优化（Drift-Based Policy Optimization, DBPO）**的新型机器人控制框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：多步生成式策略（如扩散策略 Diffusion Policy）虽然在处理机器人操作中的多模态动作分布（即同一观察下有多种有效动作）方面表现优异，但其推理过程依赖于数十次甚至上百次的迭代去噪（NFE）。
*   **负面影响**：高昂的推理延迟限制了其在**高频闭环控制**（需要快速响应）和**在线强化学习**（需要大量交互采样）中的应用。
*   **研究目标**：开发一种“原生单步”（Native One-Step）生成式策略，既能保留多模态建模能力，又能在单次网络前向传播中完成动作生成，从而实现极低延迟的实时控制。

### 2. 方法论
该论文提出了一个两阶段框架，将细化过程从“推理时迭代”转移到“训练时内化”：

*   **阶段一：基于漂移的策略（DBP）**
    *   **核心思想**：借鉴“漂移模型”（Drifting Models）原理。在训练阶段，构建一个漂移场（Drift Field），通过吸引力（向专家演示靠近）和排斥力（防止模式崩溃/多模态重叠）来调整生成的动作分布。
    *   **技术细节**：模型学习一个不动点回归目标。训练时，多个假设动作（Hypotheses）相互作用并向专家锚点漂移；训练完成后，这些复杂的修正动力学被“内化”到模型参数中。
    *   **原生单步**：推理时不再需要迭代，只需一次前向计算即可从噪声中生成高质量动作块。

*   **阶段二：基于漂移的策略优化（DBPO）**
    *   **核心思想**：为单步骨干网络配备随机接口，使其支持在线强化学习（RL）。
    *   **关键技术**：
        *   **随机适配器**：在预训练的确定性骨干上添加高斯噪声头，支持精确的对数似然计算。
        *   **PPO 更新**：利用 PPO 算法进行同策略更新，仅针对执行的动作前缀计算似然，确保与闭环控制一致。
        *   **锚点正则化（Anchor Loss）**：防止在线微调时策略偏离预训练的有效流形，保证训练稳定性。

### 3. 实验设计
*   **数据集与场景**：
    1.  **Diffusion Policy 标准库**：包含 Push-T、BlockPush、RoboMimic、Kitchen 等 12 个任务。
    2.  **点云操作基准**：Adroit（手部操作）和 Meta-World（通用操作），共 37 个任务。
    3.  **在线 RL 任务**：RoboMimic 图像任务和 D4RL 运动任务。
    4.  **真实世界**：使用双臂 UR5 机器人完成 Lift（提升）、Can（罐子操作）和 Bimanual Transport（双臂运输）。
*   **对比方法（Benchmarks）**：
    *   **多步策略**：Diffusion Policy (100 NFE), DP3 (10 NFE)。
    *   **单步/加速策略**：MP1, OMP, FlowPolicy, AdaFlow, ReinFlow, DMPO 等。

### 4. 资源与算力
*   **算力设备**：训练使用了 **8 张 NVIDIA RTX 3090 GPU**。
*   **训练时长**：在 Adroit 任务上训练 3000 个 epoch，在 Meta-World 上训练 1000 个 epoch。
*   **推理设备**：真实世界部署仅需 **1 张 RTX 3090 GPU**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2D 图像到 3D 点云、从仿真到实机、从离线模仿到在线强化学习的全方位测试。
*   **消融实验**：详细探讨了假设数量 $G$、温度参数 $R$、损失计算模式（块模式 vs 步进模式）以及锚点正则化的影响。
*   **客观性**：实验在多个随机种子下运行并报告了均值和标准差，对比基准均采用了相同的骨干网络（如 U-Net）以确保公平性。整体实验设计非常充分且具有说服力。

### 6. 主要结论与发现
*   **性能卓越**：DBP 在推理速度提升 **100 倍**（从 100 NFE 降至 1 NFE）的情况下，性能达到甚至超过了多步扩散策略（平均得分从 0.79 提升至 0.83）。
*   **单步 SOTA**：在 37 个点云任务中，DBP 达到了 **88.4%** 的平均成功率，显著优于之前的单步领先方法 OMP (82.3%)。
*   **在线提升**：DBPO 证明了可以通过在线交互进一步突破离线模仿学习的性能天花板。
*   **实机可行性**：在真实双臂机器人上实现了 **105.2 Hz** 的超高频控制，成功率达 75%。

### 7. 优点
*   **推理效率极高**：彻底解决了生成式策略在实时控制中的延迟瓶颈。
*   **无需蒸馏**：与需要预训练教师模型进行蒸馏的方法不同，DBP 是“原生”单步学习，简化了训练流程。
*   **多模态保持**：通过漂移场机制，在单步生成中依然保留了强大的多模态建模能力。
*   **通用性强**：支持图像、点云等多种输入模态，且能无缝衔接在线 RL。

### 8. 不足与局限
*   **动作空间扩展性**：在“块模式”下，内存消耗随动作块长度和动作维度呈平方级增长，处理超长时界任务可能存在压力。
*   **环境随机性**：虽然能处理动作多模态，但在极端随机或高度动态的环境中，单步生成可能不如多步迭代那样具有鲁棒的纠错能力。
*   **超参数敏感性**：漂移场中的温度参数 $R$ 和假设数量 $G$ 对不同任务的敏感度不同，可能需要针对性调优。

（完）
