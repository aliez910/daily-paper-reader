---
title: "Drift-Based Policy Optimization: Native One-Step Policy Learning for Online Robot Control"
title_zh: 基于漂移的策略优化：面向在线机器人控制的原生单步策略学习
authors: "Yuxuan Gao, Yedong Shen, Shiqi Zhang, Wenhao Yu, Yifan Duan, Jia pan, Jiajia Wu, Jiajun Deng, Yanyong Zhang"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03540v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 用于机器人操纵的一步生成式策略
tldr: 针对多步生成式策略在机器人控制中推理速度慢的问题，本文提出了一种原生单步生成策略框架。通过漂移策略（DBP）将迭代优化内化至模型参数，并结合漂移策略优化（DBPO）实现稳定的在线强化学习。该方法在保持多模态建模能力的同时，推理速度提升达100倍，在离线模仿学习和在线微调中均表现优异，支持高频闭环控制。
selection_source: fresh_fetch
motivation: 现有的多步生成式策略推理开销大，难以满足高频闭环控制和在线强化学习的实时性需求。
method: 提出DBP利用定点漂移目标实现单步生成，并开发DBPO框架通过兼容的随机接口进行稳定的在线策略更新。
result: 在多个基准测试中，DBP性能媲美或超越多步扩散策略，推理速度提升百倍，并在真机上实现105.2 Hz的高频控制。
conclusion: 该框架成功解决了生成式策略在实时机器人控制中的效率瓶颈，为高效、稳定的在线机器人学习提供了新方案。
---

## 摘要
尽管多步生成式策略通过建模多模态动作分布在机器人操作中取得了卓越性能，但它们在推理时需要多步迭代去噪。因此，生成每个动作都需要数十到数百次网络函数评估（NFEs），这使得它们在面对高频闭环控制和在线强化学习（RL）时成本过高。为了解决这一局限性，我们提出了一个针对原生单步生成式策略的两阶段框架，将细化过程从推理阶段转移到训练阶段。首先，我们引入了基于漂移的策略（DBP），该策略利用不动点漂移目标将迭代细化过程内化到模型参数中，从而在设计上实现单步生成式骨干网络，同时保留多模态动作建模能力。其次，我们开发了基于漂移的策略优化（DBPO），这是一个在线强化学习框架，它为预训练的骨干网络配备了兼容的随机接口，在不牺牲单步部署特性的前提下实现了稳定的同策略（on-policy）更新。大量实验证明了所提框架在离线模仿学习、在线微调和真实世界控制场景中的有效性。DBP 在达到或超过多步扩散策略性能的同时，实现了高达 100 倍的推理加速。此外，它在挑战性的操作基准测试中始终优于现有的单步基准模型。DBPO 还在在线设置中实现了有效且稳定的策略改进。在真实世界双臂机器人上的实验展示了 105.2 Hz 的可靠高频控制。

## Abstract
Although multi-step generative policies achieve strong performance in robotic manipulation by modeling multimodal action distributions, they require multi-step iterative denoising at inference time. Each action therefore needs tens to hundreds of network function evaluations (NFEs), making them costly for high-frequency closed-loop control and online reinforcement learning (RL). To address this limitation, we propose a two-stage framework for native one-step generative policies that shifts refinement from inference to training. First, we introduce the Drift-Based Policy (DBP), which leverages fixed-point drifting objectives to internalize iterative refinement into the model parameters, yielding a one-step generative backbone by design while preserving multimodal action modeling capacity. Second, we develop Drift-Based Policy Optimization (DBPO), an online RL framework that equips the pretrained backbone with a compatible stochastic interface, enabling stable on-policy updates without sacrificing the one-step deployment property. Extensive experiments demonstrate the effectiveness of the proposed framework across offline imitation learning, online fine-tuning, and real-world control scenarios. DBP matches or exceeds the performance of multi-step diffusion policies while achieving up to $100\times$ faster inference. It also consistently outperforms existing one-step baselines on challenging manipulation benchmarks. Moreover, DBPO enables effective and stable policy improvement in online settings. Experiments on a real-world dual-arm robot demonstrate reliable high-frequency control at 105.2 Hz.

---

## 论文详细总结（自动生成）

### 论文总结：基于漂移的策略优化（DBPO）

#### 1. 核心问题与研究动机
*   **核心问题**：目前主流的生成式机器人策略（如扩散策略 Diffusion Policy）虽然能很好地处理多模态动作分布，但推理时需要数十次甚至上百次的迭代去噪（NFE），导致推理延迟高，难以满足高频闭环控制（如 >100Hz）和在线强化学习（RL）的需求。
*   **研究背景**：现有的单步生成方案通常依赖于对预训练多步模型的蒸馏（Distillation）或复杂的辅助约束（如方向对齐、瞬时速度约束），缺乏一种原生（Native）且高效的单步生成框架。

#### 2. 方法论
*   **核心思想**：提出“漂移策略”（Drift-Based Policy, DBP），将迭代细化过程从**推理阶段**转移到**训练阶段**。通过在训练时让模型内化（Internalize）修正动力学，使得推理时仅需一次前向传播即可生成高质量动作。
*   **关键技术细节**：
    *   **DBP（离线阶段）**：基于漂移模型（Drifting Models）原理，在动作空间构建漂移场。训练目标包含对专家样本的“吸引”和对模型生成样本的“排斥”，通过不动点回归（Fixed-point Regression）让网络参数直接学习到最终的分布映射。
    *   **DBPO（在线阶段）**：为 DBP 骨干网络添加一个轻量级的随机适配器（Stochastic Adapter）。
    *   **精确似然计算**：利用 PPO 算法进行同策略更新。通过固定潜在变量 $z$，实现了无需对 $z$ 进行边缘化即可计算精确的动作似然值，从而支持稳定的在线微调。
    *   **锚点正则化（Anchor Regularization）**：引入锚点损失，防止在线更新时策略偏离预训练的分布太远，确保训练稳定性。

#### 3. 实验设计
*   **数据集与场景**：
    *   **扩散策略基准测试**：Push-T、BlockPush、RoboMimic、Kitchen（共12个任务）。
    *   **点云操纵基准**：Adroit 和 Meta-World（共37个任务，涵盖简单到极难）。
    *   **在线 RL 任务**：RoboMimic 操纵任务和 D4RL 运动任务。
    *   **真实世界**：双臂 UR5 机器人，执行 Lift（提起）、Can（拨罐）、Transport（双臂协作运输）。
*   **对比方法**：
    *   **多步模型**：Diffusion Policy (100 NFE), DP3 (10 NFE)。
    *   **单步模型**：MP1, OMP, FlowPolicy, AdaFlow, CP 等。
    *   **在线 RL 基准**：ReinFlow, DMPO。

#### 4. 资源与算力
*   **算力设备**：使用了 8 台 NVIDIA RTX 3090 GPU 进行大规模仿真训练。
*   **训练时长**：在 Adroit 任务上训练 3000 个 epoch，在 Meta-World 上训练 1000 个 epoch。
*   **推理设备**：真机部署使用单张 RTX 3090 GPU，端到端延迟仅 9.5ms。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从低维状态到图像、再到 3D 点云的多种模态；从离线模仿学习到在线强化学习；从仿真环境到真实物理系统。
*   **消融实验**：针对锚点正则化、温度参数设置等关键组件进行了消融研究。
*   **客观性**：在对比实验中，作者特意匹配了与基准模型相同的网络架构（如 U-Net），以确保性能提升源于算法改进而非模型容量增加。实验覆盖面广，数据量充足，具有较强的说服力。

#### 6. 主要结论与发现
*   **性能与效率双赢**：DBP 在 1-NFE 下的性能媲美或超过了 100-NFE 的扩散策略，推理速度提升达 100 倍。
*   **单步 SOTA**：在 37 个点云操纵任务中，DBP 达到了 88.4% 的平均成功率，显著优于之前的单步领先方法 OMP (82.3%)。
*   **在线提升显著**：DBPO 证明了即使在单步约束下，通过在线 RL 也能进一步突破离线模仿学习的性能天花板。
*   **真机可行性**：在双臂机器人上实现了 105.2 Hz 的高频控制，成功率达 75%。

#### 7. 优点
*   **原生单步设计**：无需复杂的蒸馏过程，从训练目标层面解决了推理效率问题。
*   **多模态兼容性**：能够无缝处理图像、点云等异构传感器输入。
*   **数学完备性**：提供了收敛性保证的理论视角，并解决了生成式策略在 PPO 更新时的似然计算难题。

#### 8. 不足与局限
*   **任务复杂度限制**：目前的评估主要集中在桌面操纵和基础运动任务，对于长程（Long-horizon）或涉及复杂接触动力学的任务仍有待验证。
*   **样本效率**：虽然在线微调效果好，但在“从零开始”学习（Learning from scratch）的场景下，其样本效率可能仍有提升空间。
*   **真机失败模式**：在真机实验中，抓取滑动和双臂同步冲突仍是主要的失败原因，说明策略在极端物理交互下的鲁棒性仍需加强。

（完）
