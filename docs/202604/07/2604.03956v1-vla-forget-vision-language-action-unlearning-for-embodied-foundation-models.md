---
title: "VLA-Forget: Vision-Language-Action Unlearning for Embodied Foundation Models"
title_zh: VLA-Forget：具身基础模型的视觉-语言-动作遗忘
authors: "Ravi Ranjan, Agoritsa Polyzou"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.03956v1"
tags: ["query:rob-il"]
score: 10.0
evidence: 操纵任务中视觉-语言-动作 (VLA) 模型的遗忘挑战
tldr: 针对具身智能大模型（VLA）中不安全或隐私行为的移除挑战，本文提出 VLA-Forget 框架。该框架通过混合遗忘策略，协同优化视觉编码器、跨模态投影器和语言骨干网，在确保目标行为被彻底遗忘的同时，最大限度地保留了模型的感知、推理和动作控制能力，解决了传统方法在多模态分布知识遗忘上的局限性。
selection_source: fresh_fetch
motivation: 现有的 VLA 模型在部署时面临安全和隐私风险，且其知识分布在多个模块中，导致传统的单一模态遗忘方法效果不佳。
method: 提出一种混合遗忘框架，结合了感知层的比例感知选择性编辑与推理/动作层的层选择性遗忘，通过分阶段更新视觉、投影和 Transformer 模块来平衡遗忘与性能。
result: 实验表明，该方法在遗忘效率、感知特异性和任务成功率保留方面均优于基准模型，并显著降低了量化后的恢复风险。
conclusion: VLA-Forget 为具身智能模型提供了一种高效且能保持通用能力的遗忘方案，是实现安全可靠机器人操控的重要一步。
---

## 摘要
视觉-语言-动作（VLA）模型正作为机器人操作的具身基础模型兴起，但其部署引入了新的遗忘（unlearning）挑战：即如何在不降低感知、语言对齐（language grounding）和动作控制能力的前提下，移除不安全、错误或隐私敏感的行为。在 OpenVLA 风格的策略中，行为是通过融合视觉编码器、跨模态投影器和预测标记化机器人动作的语言主干网络生成的，因此不良知识可能分布在感知、对齐和推理/动作层中，而非局限于单个模块。因此，仅应用于视觉堆栈或仅应用于语言主干的部分遗忘通常是不够的，而为独立视觉或语言模型设计的传统遗忘基准方法在具身场景中可能会留下遗忘残留，或导致不必要的效用损失。我们提出了 VLA-Forget，这是一个混合遗忘框架，它结合了针对感知和跨模态特异性的比例感知选择性编辑，以及针对保持效用的遗忘的层选择性推理/动作遗忘。VLA-Forget 通过对视觉编码器、投影器和上层动作生成 Transformer 块进行阶段性更新，共同优化三个目标：定向遗忘、感知保留和推理维持。在遗忘集行为探测和保留任务评估中，相较于强遗忘基准，VLA-Forget 将遗忘效能提高了 10%，感知特异性保留提高了 22%，推理和任务成功率维持提高了 9%，并将量化后恢复降低了 55%。

## Abstract
Vision-language-action (VLA) models are emerging as embodied foundation models for robotic manipulation, but their deployment introduces a new unlearning challenge: removing unsafe, spurious, or privacy-sensitive behaviors without degrading perception, language grounding, and action control. In OpenVLA-style policies, behavior is produced through a fused visual encoder, a cross-modal projector, and a language backbone that predicts tokenized robot actions, so undesirable knowledge can be distributed across perception, alignment, and reasoning/action layers rather than confined to a single module. Consequently, partial unlearning applied only to the vision stack or only to the language backbone is often insufficient, while conventional unlearning baselines designed for standalone vision or language models may leave residual forgetting or incur unnecessary utility loss in embodied settings. We propose VLA-Forget, a hybrid unlearning framework that combines ratio-aware selective editing for perception and cross-modal specificity with layer-selective reasoning/action unlearning for utility-preserving forgetting. VLA-Forget jointly optimizes three objectives: targeted forgetting, perceptual preservation, and reasoning retention, through staged updates over the visual encoder, projector, and upper action-generating transformer blocks. Across forget-set behavior probes and retain-task evaluations, VLA-Forget improves forgetting efficacy by 10%, preserves perceptual specificity by 22%, retains reasoning and task success by 9%, and reduces post-quantization recovery by 55% relative to strong unlearning baselines.

---

## 论文详细总结（自动生成）

### 论文总结：VLA-Forget —— 具身基础模型的视觉-语言-动作遗忘框架

#### 1. 核心问题与整体含义（研究动机和背景）
随着视觉-语言-动作（VLA）模型（如 OpenVLA）成为机器人操控的核心，如何安全地“擦除”模型中不当的知识变得至关重要。这些不当知识包括：**不安全或危险的动作行为、隐私敏感的演示数据、以及虚假的关联（如因背景颜色误判指令）**。
传统的遗忘技术（Machine Unlearning）多针对单一模态（纯文本或纯图像），而 VLA 模型的知识耦合在视觉编码器、跨模态投影器和语言/动作主干网中。仅修改单一模块往往无法彻底根除错误行为，或会导致模型整体操控能力的剧烈下降。本文旨在提出一种能够平衡“彻底遗忘”与“效用保留”的具身模型专用遗忘框架。

#### 2. 方法论：核心思想与关键技术
**VLA-Forget** 采用了一种**分阶段、组件感知（Component-aware）**的混合遗忘策略：

*   **核心思想**：将遗忘过程分解为感知、对齐和推理三个层面，通过选择性地编辑对目标行为贡献最大的参数层，同时利用蒸馏和锚定损失保护无关任务的性能。
*   **关键技术细节**：
    *   **混合选择性定位**：
        *   **感知与投影层选择**：使用“比例感知分数”（Ratio-aware score），筛选出对遗忘集梯度大、对保留集干扰小的视觉/投影层。
        *   **推理/动作层选择**：在 LLM 主干网中，基于“显著性比例”（Significance ratio）识别与动作生成最相关的 Transformer 块。
    *   **多目标优化损失函数**：
        *   **保留损失 ($L_{retain}$)**：确保模型在正常任务上的预测能力，并使用 KL 散度约束模型不偏离原始版本。
        *   **遗忘损失 ($L_{forget}$)**：通过梯度上升抑制目标行为。
        *   **不匹配损失 ($L_{mismatch}$)**：增大遗忘样本与原始输出的分布差异，防止“浅层遗忘”。
        *   **感知保留损失 ($L_{feat}$)**：通过特征蒸馏保护视觉编码器的通用感知能力。
    *   **算法流程**：采用 **Adapter-first（如 LoRA）** 更新。分为三个阶段：1. 移除视觉触发特征；2. 阻断跨模态错误绑定；3. 抑制残余的动作先验。使用 **PCGrad（梯度手术）** 解决不同目标间的梯度冲突。

#### 3. 实验设计
*   **数据集/场景**：
    *   **Open X-Embodiment (OXE)**：真实机器人数据集，用于评估复杂现实场景下的遗忘效果。
    *   **lerobot/pusht_image**：受控的合成基准环境，用于快速迭代和消融实验。
*   **对比基准（Baselines）**：
    *   **SSD**（选择性突触抑制）、**SalUn**（基于显著性的遗忘）：视觉侧遗忘代表。
    *   **GA**（梯度上升）、**NPO**（负偏好优化）：语言侧遗忘代表。
*   **评估指标**：包括遗忘动作损失（FC）、保留效用得分（RC）、任务成功率（TSR）和安全违规率（SVR）等 6 项指标。

#### 4. 资源与算力
*   **硬件需求**：论文在附录中提到实验可以在**单块现代 GPU**（Single modern GPU）上运行。
*   **训练效率**：由于采用了参数高效的 LoRA 微调而非全参数更新，且设计了早停机制（Early stopping），该方法具有较高的计算效率。
*   **具体时长**：文中未明确给出总训练小时数，但提到了每个阶段的更新步数（如 60-140 步不等）。

#### 5. 实验数量与充分性
*   **实验规模**：在 OpenVLA-7B 和 pi0fast-base 两个主流模型上进行了验证。
*   **消融实验**：非常充分。涵盖了不同组件（视觉 vs 投影 vs 语言）的组合影响、各项损失函数的贡献、学习率敏感性以及 PCGrad 的必要性。
*   **鲁棒性测试**：特别测试了**量化（8-bit, 4-bit）**后的遗忘稳定性，这是针对实际部署环境的客观考量。
*   **公平性**：所有对比方法均在相同的优化预算和数据集划分下进行，使用了 5 个随机种子取平均值。

#### 6. 主要结论与发现
*   **协同效应**：单一模块的遗忘是不够的。全框架（视觉+投影+语言）在遗忘效能上比基准提高 10%，同时感知特异性保留提升 22%。
*   **效用保留**：VLA-Forget 在移除有害行为的同时，对正常任务成功率（TSR）的损害最小（比强基准高出 9%）。
*   **量化鲁棒性**：传统方法在模型量化后容易出现“遗忘失效”，而 VLA-Forget 将量化后的恢复风险降低了 55%。

#### 7. 优点（亮点）
*   **多模态深度融合**：首次系统性地探讨了具身模型中知识分布在不同模态组件间的遗忘难题。
*   **部署友好**：基于 LoRA 的设计使得遗忘后的模型易于回滚、合并或作为补丁分发。
*   **安全性导向**：引入了“矛盾探测”（Contradiction probes）和安全违规率评估，更符合机器人实际运行的安全需求。

#### 8. 不足与局限
*   **近似遗忘**：该方法仍属于近似遗忘，无法从数学上提供“完全擦除”的理论保证。
*   **数据依赖**：遗忘效果高度依赖于“遗忘集”和“保留集”的构建质量，若数据划分存在偏差，可能影响泛化性。
*   **短时界限制**：实验主要集中在短时（Short-horizon）操控任务，对于长程规划（Long-horizon）任务中的隐性错误行为移除尚待验证。
*   **调参开销**：虽然是分阶段更新，但涉及多个超参数（如各阶段步数、损失权重），在不同架构上可能需要重新微调。

（完）
