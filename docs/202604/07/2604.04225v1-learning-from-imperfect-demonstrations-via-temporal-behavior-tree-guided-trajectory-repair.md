---
title: Learning from Imperfect Demonstrations via Temporal Behavior Tree-Guided Trajectory Repair
title_zh: 通过时序行为树引导的轨迹修复从不完美演示中学习
authors: "Aniruddh G. Puranic, Sebastian Schirmer, John S. Baras, Calin Belta"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04225v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 修复用于模仿学习的次优轨迹
tldr: 针对机器人模仿学习中演示数据往往存在噪声或次优的问题，本文提出了一种基于时序行为树（TBT）的轨迹修复框架。该方法利用结合了信号时序逻辑与行为树语义的TBT对不满足规范的演示进行修正，使其符合逻辑约束。修复后的轨迹用于提取势函数以优化强化学习的奖励信号，从而在无需运动学模型的情况下引导智能体学习。实验证明该方法在离散和连续任务中均能显著提升学习效率。
selection_source: fresh_fetch
motivation: 现实中的机器人演示数据通常是不完美的（有噪声或次优），直接用于模仿学习或强化学习效果较差。
method: 提出时序行为树（TBT）框架，通过模型驱动的修复算法修正违规轨迹，并利用修复后的数据进行奖励塑形。
result: 在离散网格导航及连续单/多智能体到达-避障任务中，该框架有效提升了数据效率和任务成功率。
conclusion: 该研究证明了结合形式化方法与强化学习可以有效利用低质量演示数据，实现高效且逻辑一致的机器人策略学习。
---

## 摘要
从演示中学习机器人控制策略是一种强大的范式，然而现实世界的数据往往是次优的、有噪声的或存在其他缺陷，这给模仿学习和强化学习带来了重大挑战。在这项工作中，我们提出了一个形式化框架，利用时序行为树（TBT）——一种结合了行为树语义的信号时序逻辑（STL）扩展——在将次优轨迹用于下游策略学习之前对其进行修复。针对违反 TBT 规范的演示，一种基于模型的修复算法会修正轨迹段以满足形式化约束，从而产生一个既逻辑一致又具有可解释性的数据集。修复后的轨迹随后被用于提取势函数，以塑造强化学习的奖励信号，在不需要了解智能体运动学模型的情况下，引导智能体走向状态空间中与任务一致的区域。我们在离散网格世界导航以及连续单智能体和多智能体到达-规避任务中证明了该框架的有效性，突显了其在无法假设存在高质量演示的场景下进行高效数据机器人学习的潜力。

## Abstract
Learning robot control policies from demonstrations is a powerful paradigm, yet real-world data is often suboptimal, noisy, or otherwise imperfect, posing significant challenges for imitation and reinforcement learning. In this work, we present a formal framework that leverages Temporal Behavior Trees (TBT), an extension of Signal Temporal Logic (STL) with Behavior Tree semantics, to repair suboptimal trajectories prior to their use in downstream policy learning. Given demonstrations that violate a TBT specification, a model-based repair algorithm corrects trajectory segments to satisfy the formal constraints, yielding a dataset that is both logically consistent and interpretable. The repaired trajectories are then used to extract potential functions that shape the reward signal for reinforcement learning, guiding the agent toward task-consistent regions of the state space without requiring knowledge of the agent's kinematic model. We demonstrate the effectiveness of this framework on discrete grid-world navigation and continuous single and multi-agent reach-avoid tasks, highlighting its potential for data-efficient robot learning in settings where high-quality demonstrations cannot be assumed.

---

## 论文详细总结（自动生成）

以下是对论文《Learning from Imperfect Demonstrations via Temporal Behavior Tree-Guided Trajectory Repair》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：从演示中学习（LfD）是机器人领域的重要范式，但现实中的演示数据往往是“不完美”的（包含噪声、次优操作、违反安全约束或任务逻辑）。
*   **核心问题**：如何有效地利用这些不完美的演示数据，而不是简单地丢弃它们或进行繁琐的人工筛选？
*   **整体含义**：本文提出了一个结合形式化方法与强化学习（RL）的框架。通过**时序行为树（TBT）**对违规轨迹进行逻辑修复，并利用修复后的轨迹生成势函数来引导强化学习，从而在无需高质量演示和精确运动学模型的情况下实现高效的策略学习。

### 2. 方法论
该框架包含三个主要阶段：
*   **轨迹修复（TBT Trace Repair）**：
    *   使用 TBT（结合了信号时序逻辑 STL 的表达能力和行为树的层次结构）定义任务规范。
    *   将修复问题建模为混合整数线性规划（MILP）。给定一个近似的运动学模型和违规轨迹，算法寻找一条在满足 TBT 约束的前提下，与原始轨迹距离最近的修复轨迹。
    *   为了提高效率，引入了基于“地标（Landmarks）”的策略，将复杂的 MILP 简化为线性规划（LP）。
*   **奖励发现（Reward Discovery）**：
    *   从修复后的轨迹集中提取两种势函数：**邻近势函数（$\Phi_{prox}$）**奖励智能体靠近修复轨迹中的状态；**安全势函数（$\Phi_{safe}$）**惩罚偏离修复轨迹过远的智能体。
    *   利用基于势函数的奖励塑形（Reward Shaping）理论，将这些函数转化为密集的奖励信号。
*   **策略学习（Policy Learning）**：
    *   使用标准强化学习算法（如 PPO）进行训练。奖励函数由环境奖励（基于 TBT 在线监测器的鲁棒性值）和上述塑形奖励组成。
    *   **关键技术细节**：势函数的计算仅依赖于位姿空间（Pose Space），因此奖励信号与智能体的具体运动学模型（如 Ackermann 或 Unicycle）解耦。

### 3. 实验设计
*   **实验场景**：
    1.  **离散网格世界（Grid-World）**：10x10 随机障碍物地图，任务是到达目标并避障。
    2.  **连续单智能体导航（Safety-Gymnasium）**：使用 Ackermann 运动学的机器人，在包含硬约束（障碍物）和软约束（紫色区域）的环境中导航。
    3.  **连续多智能体导航**：两个使用 Unicycle 运动学的机器人协作到达两个目标点。
*   **基准方法（Benchmarks）**：
    *   稀疏奖励的 PPO。
    *   专家设计的密集奖励 PPO。
    *   Safe-PPO（专门用于约束优化的 PPO 变体）。
*   **数据来源**：收集 50 条由训练早期的 RL 智能体生成的、包含违规行为的次优演示。

### 4. 资源与算力
*   **硬件配置**：Intel Core Ultra 9 285K (24核) 处理器，64GB RAM，**NVIDIA RTX 5090 GPU**。
*   **软件工具**：使用 Gurobi 优化器求解轨迹修复问题，使用 Stable-Baselines3 实现 RL 训练。
*   **耗时情况**：网格世界修复耗时 < 1秒；连续任务修复耗时 < 3秒。RL 训练步数在 $10^4$ 到 $5 \times 10^5$ 之间。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了离散/连续、单智能体/多智能体、不同运动学模型等多种维度。
*   **充分性评价**：实验设计较为充分。作者在 100 个测试回合中记录了统计数据，并对比了成功率、成本率（违规频率）和收敛速度。
*   **客观性**：通过在不同复杂度的地图上进行对比，证明了该方法在稀疏奖励环境下的优越性。同时，验证了修复算法使用的模型（Dubins）与实际训练模型（Ackermann/Unicycle）不一致时的泛化能力，体现了实验的客观性。

### 6. 主要结论与发现
*   **性能提升**：该框架在所有任务中均达到或超过了专家设计奖励的性能，且收敛速度更快。
*   **安全性增强**：在多智能体任务中，该方法比专家奖励基准的障碍物碰撞率降低了 **47%**。
*   **模型无关性**：证明了从修复轨迹中提取的势函数具有跨平台适用性，无需针对特定机器人的运动学重新设计奖励。
*   **逻辑一致性**：修复后的轨迹不仅提供了数值引导，还确保了逻辑上的正确性。

### 7. 优点
*   **形式化保证**：利用 TBT 确保了修复轨迹的可验证性和逻辑严密性。
*   **数据效率**：能够变废为宝，将原本无法使用的违规演示转化为高质量的引导信号。
*   **解耦设计**：奖励塑形与智能体动力学解耦，极大地增强了框架的通用性。
*   **计算优化**：通过 KD-Tree 加速最近邻查询，显著降低了 RL 训练时的计算开销。

### 8. 不足与局限
*   **离线限制**：轨迹修复是离线进行的，且依赖于环境的近似模型，无法直接处理完全未知的动态环境。
*   **配置敏感性**：势函数是基于特定环境配置提取的。在障碍物和目标位置高度随机化的场景下，如果修复轨迹覆盖不足，引导效果可能会打折扣。
*   **计算复杂度**：虽然引入了地标法简化，但对于极高维度的系统，求解 MILP 仍可能面临扩展性挑战。
*   **模型依赖**：修复过程仍需一个线性化的近似运动学模型，这在某些复杂非线性系统中可能难以获得。

（完）
