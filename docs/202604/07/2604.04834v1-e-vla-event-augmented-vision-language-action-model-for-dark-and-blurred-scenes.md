---
title: "E-VLA: Event-Augmented Vision-Language-Action Model for Dark and Blurred Scenes"
title_zh: E-VLA：面向黑暗和模糊场景的事件增强型视觉-语言-动作模型
authors: "Jiajun Zhai, Hao Shi, Shangwei Guo, Kailun Yang, Kaiwei Wang"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04834v1"
tags: ["query:rob-il"]
score: 10.0
evidence: 用于操纵任务的事件增强型视觉-语言-动作（VLA）模型
tldr: 针对传统VLA模型在低光照、运动模糊等极端环境下感知失效的问题，本文提出了E-VLA框架。该框架通过引入事件相机捕捉的运动和结构线索，增强了模型在恶劣视觉条件下的操纵鲁棒性。研究团队构建了开源遥操作平台并采集了首个同步的RGB-事件-动作数据集，提出了多种轻量级事件融合策略。实验表明，E-VLA在极暗和模糊场景下的任务成功率显著提升，为构建全天候具身智能系统提供了有效路径。
selection_source: fresh_fetch
motivation: 传统VLA模型在低光照和运动模糊等极端环境下感知能力脆弱，限制了机器人在复杂现实场景中的可靠应用。
method: 提出E-VLA框架，通过轻量级适配器或参数无关的融合策略，将事件相机的动态线索直接整合进预训练的VLA模型中。
result: "在20 lux低光环境下，任务成功率从0%最高提升至90%；在严重运动模糊下，分类任务成功率从5%提升至32.5%。"
conclusion: 研究证明了事件驱动感知能显著增强VLA模型的鲁棒性，为开发超越传统帧成像限制的具身智能系统奠定了基础。
---

## 摘要
机器人视觉-语言-动作（VLA）模型在开放式操作中具有良好的泛化能力，但在极端低光、运动模糊和黑电平裁剪等感知阶段退化情况下，其感知能力非常脆弱。我们提出了 E-VLA，这是一个事件增强型 VLA 框架，可在传统的基于帧的视觉变得不可靠时提高操作的鲁棒性。E-VLA 并非从事件中重建图像，而是直接利用事件流中的运动和结构线索，以在不利条件下保持语义感知和感知-动作的一致性。我们使用 DAVIS346 事件相机构建了一个开源遥操作平台，并收集了一个涵盖多种任务和光照设置的真实世界同步 RGB-事件-动作操作数据集。我们还提出了轻量级且与预训练模型兼容的事件集成策略，并研究了用于稳定部署的事件分窗和融合方法。实验表明，即使是简单的无参数融合（即在 RGB 图像上叠加累积的事件图），也能显著提高在黑暗和严重模糊场景下的鲁棒性：在 20 lux 的“拾取-放置”任务中，成功率从 0%（仅图像）提高到 60%（叠加融合），并使用我们的事件适配器进一步提高到 90%；在严重的运动模糊（1000 毫秒曝光）下，“拾取-放置”任务的成功率从 0% 提高到 20-25%，而“分类”任务从 5% 提高到 32.5%。总之，E-VLA 提供了系统性的证据，证明事件驱动的感知可以有效地集成到 VLA 模型中，为超越传统基于帧成像的鲁棒具身智能指明了方向。代码和数据集将在 https://github.com/JJayzee/E-VLA 上发布。

## Abstract
Robotic Vision-Language-Action (VLA) models generalize well for open-ended manipulation, but their perception is fragile under sensing-stage degradations such as extreme low light, motion blur, and black clipping. We present E-VLA, an event-augmented VLA framework that improves manipulation robustness when conventional frame-based vision becomes unreliable. Instead of reconstructing images from events, E-VLA directly leverages motion and structural cues in event streams to preserve semantic perception and perception-action consistency under adverse conditions. We build an open-source teleoperation platform with a DAVIS346 event camera and collect a real-world synchronized RGB-event-action manipulation dataset across diverse tasks and illumination settings. We also propose lightweight, pretrained-compatible event integration strategies and study event windowing and fusion for stable deployment. Experiments show that even a simple parameter-free fusion, i.e., overlaying accumulated event maps onto RGB images, could substantially improve robustness in dark and blur-heavy scenes: on Pick-Place at 20 lux, success increases from 0% (image-only) to 60% with overlay fusion and to 90% with our event adapter; under severe motion blur (1000 ms exposure), Pick-Place improves from 0% to 20-25%, and Sorting from 5% to 32.5%. Overall, E-VLA provides systematic evidence that event-driven perception can be effectively integrated into VLA models, pointing toward robust embodied intelligence beyond conventional frame-based imaging. Code and dataset will be available at https://github.com/JJayzee/E-VLA.

---

## 论文详细总结（自动生成）

这是一份关于论文《E-VLA: Event-Augmented Vision-Language-Action Model for Dark and Blurred Scenes》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型（如 RT-2, OpenVLA）高度依赖传统的 RGB 帧图像。在**极端低光（暗光）**、**运动模糊**或**黑电平裁剪（图像全黑）**等恶劣感知条件下，基于帧的视觉编码器会彻底失效，导致机器人操作崩溃。
*   **研究动机**：事件相机（Event Camera）具有高动态范围和微秒级时间分辨率，能捕捉亮度变化而非绝对强度。本文旨在探索如何将事件流集成到预训练的 VLA 模型中，以弥补传统相机在极端环境下的物理缺陷，提升具身智能的鲁棒性。

### 2. 方法论
E-VLA 框架的核心思想是利用事件流提供的运动和结构线索，通过轻量级且兼容预训练模型的方式增强感知。
*   **事件表示与分窗**：
    *   **Recent-count Windowing**：放弃传统的固定时长分窗，采用“最近 N 个事件”的计数分窗。这解决了机器人手臂运动速度变化导致事件率不稳定的问题，确保了感知与动作的稳定耦合。
    *   **累积表示**：将事件窗口累积成类似图像的 2D 帧（灰度图或伪彩色图），以便与预训练的视觉编码器（如 SigLIP）兼容。
*   **两种融合策略**：
    *   **Overlay-based Fusion（叠加融合）**：无参数策略。直接将累积的事件图叠加在 RGB 图像上。这种方法简单高效，不改变模型结构。
    *   **Hierarchical Event Adapter（分层事件适配器）**：参数高效策略。引入一个轻量级的 ViT 分支处理事件，并将其特征通过 MLP 注入到主视觉编码器的中间层（第 3, 6, 9, 12 层）。
*   **训练流程**：采用两阶段策略。先冻结 VLM 主干，只训练动作专家（Action Expert）；再联合微调事件适配器和动作专家。为了防止模型过度依赖 RGB，训练中加入了图像随机丢弃（Image Dropout）。

### 3. 实验设计
*   **数据集**：构建了首个同步的 **RGB-Event-Action 操纵数据集**。包含 Pick-Place（拾取放置）、Sorting（分类）、Stacking（堆叠）三个任务，涵盖 200、100、75、40 lux 等多种光照水平，共 724 个演示视频（约 34 万帧）。
*   **硬件平台**：基于开源 SO100 机械臂，手腕处安装 DAVIS346 事件相机（346x260 分辨率）。
*   **对比基准（Baselines）**：
    *   **纯图像模型**：SmolVLA。
    *   **图像增强方法**：RetinexNet, Retinexformer, EvLight（试图通过算法修复暗光图像）。
    *   **事件重建方法**：E2VID（将事件还原为图像再输入 VLA）。
*   **测试场景**：梯度低光（低至 20 lux 及以下）、不同程度的运动模糊（曝光时间从 100ms 到 1000ms）。

### 4. 资源与算力
*   **训练算力**：使用单张 **NVIDIA A800 GPU** 进行训练。
*   **训练时长**：总计进行 20k 次迭代（适配器训练 10k + 联合微调 10k）。
*   **部署算力**：推理部署在 **NVIDIA AGX Orin** 嵌入式平台上，证明了模型的轻量化和实时性。
*   **参数量**：事件适配器仅引入约 13.3M 额外参数（不到总参数量的 3%）。

### 5. 实验数量与充分性
*   **实验规模**：进行了多维度测试，包括 6 种光照梯度、6 种模糊程度、3 类操纵任务。
*   **消融实验**：详细探讨了事件窗口大小（500 到 4000 事件）、分窗策略（时间 vs 计数）、训练策略（是否共享权重、Dropout 比例）以及事件表示方法（Voxel Grid vs Time Surface）。
*   **充分性评价**：实验设计非常系统，不仅对比了 SOTA 图像增强算法，还测试了 OOD（分布外）泛化能力（即在正常光照下训练，在未见过的暗光下测试），客观证明了事件相机的硬件级优势。

### 6. 主要结论与发现
*   **极端环境下的压倒性优势**：在 20 lux（近乎全黑）环境下，纯图像模型成功率为 0%，而 E-VLA 适配器版本保持了 **90%** 的成功率。
*   **抗模糊能力**：在 1000ms 极端曝光导致的严重模糊下，E-VLA 仍能提供有效的空间引导，成功率远高于基准模型。
*   **泛化性**：即使不使用低光数据训练，引入事件分支也能显著提升模型对光照变化的适应力。
*   **事件计数优于时长**：在机器人频繁启停的操作中，基于事件计数的窗口比基于固定时长的窗口更稳定。

### 7. 优点
*   **硬件与算法结合**：从物理感知层面解决 VLA 的脆弱性，而非单纯依赖数据增强。
*   **轻量化设计**：适配器方案极小，且 Overlay 方案甚至实现了零参数增加的性能飞跃。
*   **开源贡献**：提供了首个此类多模态数据集和硬件参考设计，对具身智能社区价值巨大。

### 8. 不足与局限
*   **颜色语义缺失**：事件相机不记录绝对颜色，在需要精细颜色分辨的任务（如 Sorting）中，如果 RGB 完全失效，仅靠事件无法区分物体颜色。
*   **遮挡问题**：手腕相机在抓取物体时存在视野遮挡，这是单目视觉的固有缺陷，事件相机也无法完全消除。
*   **被动感知**：目前的事件生成依赖于机械臂自身的运动，如果环境和手臂都静止，事件流将消失，可能导致感知中断。

（完）
