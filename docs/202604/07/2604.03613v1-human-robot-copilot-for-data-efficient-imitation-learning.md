---
title: Human-Robot Copilot for Data-Efficient Imitation Learning
title_zh: 面向数据高效型模仿学习的人机协作驾驶系统
authors: "Rui Yan, Zaitian Gongye, Lars Paulsen, Xuxin Cheng, Xiaolong Wang"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03613v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 针对特定任务技能的人机协作模仿学习
tldr: 针对机器人模仿学习中数据效率低和策略在分布外状态表现差的问题，本文提出了一种名为“人机副驾驶”的框架。该框架基于HG-DAgger范式，通过引入缩放因子实现灵巧的远程操作，并保持对多种机械臂的兼容性。实验证明，该方法在相同演示次数下显著提升了任务性能，并因其间歇性干预机制使得数据采集过程更加高效省时。
selection_source: fresh_fetch
motivation: 现有的交互式模仿学习方法难以在精细控制的灵巧性与跨平台的通用性之间取得平衡，导致受限数据下的策略鲁棒性不足。
method: 提出一种人机协作框架，利用缩放因子增强远程操作的灵巧性，并支持多种机械臂结构的按需人工干预。
result: 实验结果显示，该框架在相同数量的演示轨迹下实现了更高的任务成功率，且数据采集过程更加高效。
conclusion: 人机副驾驶框架通过优化人工干预机制，有效提升了模仿学习的数据效率和策略在复杂环境下的泛化能力。
---

## 摘要
通过远程操作收集人类演示是教授机器人特定任务技能的一种常用方法。然而，当可用的演示数量有限时，由于复合误差或环境随机性，策略容易进入分布外（OOD）状态。现有的交互式模仿学习或人机在环方法试图通过遵循人类门控 DAgger (HG-DAgger) 范式来解决这一问题，该方法通过在策略执行期间进行选择性的人类干预来增强演示。尽管如此，这些方法难以平衡灵巧性和通用性：它们要么提供细粒度的修正但受限于特定的运动学结构，要么以牺牲精确控制为代价来实现通用性。为了克服这一局限性，我们提出了人机协作驾驶（Human-Robot Copilot）框架，该框架可以利用缩放因子进行灵巧的远程操作，同时保持与各种工业和研究机械臂的兼容性。实验结果表明，在相同数量的演示轨迹下，我们的框架实现了更高的性能。此外，由于仅需间歇性地进行纠正性干预，整个数据收集过程更加高效且耗时更短。

## Abstract
Collecting human demonstrations via teleoperation is a common approach for teaching robots task-specific skills. However, when only a limited number of demonstrations are available, policies are prone to entering out-of-distribution (OOD) states due to compounding errors or environmental stochasticity. Existing interactive imitation learning or human-in-the-loop methods try to address this issue by following the Human-Gated DAgger (HG-DAgger) paradigm, an approach that augments demonstrations through selective human intervention during policy execution. Nevertheless, these approaches struggle to balance dexterity and generality: they either provide fine-grained corrections but are limited to specific kinematic structures, or achieve generality at the cost of precise control. To overcome this limitation, we propose the Human-Robot Copilot framework that can leverage a scaling factor for dexterous teleoperation while maintaining compatibility with a wide range of industrial and research manipulators. Experimental results demonstrate that our framework achieves higher performance with the same number of demonstration trajectories. Moreover, since corrective interventions are required only intermittently, the overall data collection process is more efficient and less time-consuming.

---

## 论文详细总结（自动生成）

以下是对论文《Human-Robot Copilot for Data-Efficient Imitation Learning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：在模仿学习（IL）中，由于复合误差和环境随机性，机器人策略容易进入“分布外”（OOD）状态导致失败。现有的“人机在环”（Human-in-the-loop）方法在**灵巧性**（精细控制能力）与**通用性**（适配不同机器人硬件）之间难以平衡。
*   **研究动机**：
    *   传统的离线学习效率低，无法针对性解决失败场景。
    *   现有的远程操作设备（如VR、SpaceMouse）缺乏直观的绝对位置参考，或仅限于同构机器人（Leader和Follower必须完全一致），限制了应用范围。
    *   需要一种低成本、跨平台且支持高精度修正的交互式学习框架。

### 2. 方法论
该论文提出了 **Human-Robot Copilot** 框架，包含硬件设计、控制架构和学习流水线：
*   **异构远程操作（Heterogeneous Teleoperation）**：
    *   使用低成本（约$1,000）的3D打印Leader设备，通过逆运动学（IK）解算将Leader末端位姿映射到Follower机器人，实现跨形态控制。
    *   **双向控制逻辑**：在机器人自主运行策略时，Leader设备会实时同步Follower的状态。这确保了人类在干预瞬间，Leader与Follower的位姿是完全对齐的，实现了无缝切换。
*   **可调缩放因子（Scaling Factor）**：
    *   引入公式 $x_f = \alpha(x_l - c_l) + c_t$。通过调整 $\alpha$，人类可以放大或缩小操作空间。
    *   **大 $\alpha$**：用于大范围快速移动；**小 $\alpha$**：用于高精度对准（如毫米级插拔）。
*   **片段式微调（Clip-Based Fine-tuning）**：
    *   人类仅在策略失败时介入并录制“修正片段”（Data Clips），而非重新录制整个轨迹。
    *   将这些针对性片段与原始数据混合，对轻量化 ACT（Action Chunking Transformer）策略进行快速微调。

### 3. 实验设计
*   **实验场景**：
    *   **仿真环境**：Robomimic 基准测试中的 PickPlace（抓取放置）和 NutAssembly（螺母装配）。
    *   **现实世界**：高度随机的“方块分类”任务和高精度的“汉诺塔插拔”任务（容差仅2mm）。
*   **基准测试（Benchmark）与对比方法**：
    *   **Base Policy**：仅使用初始演示数据的离线模仿学习。
    *   **Keyboard/VR Policy**：使用键盘（增量控制）或VR控制器作为干预手段的对比组。
    *   **评估指标**：任务成功率（分阶段评估）和数据采集总耗时。

### 4. 资源与算力
*   **硬件成本**：Leader设备总成本约 $1,000。
*   **训练时长**：
    *   基础策略训练约 **40分钟**。
    *   加入干预数据后的微调训练少于 **10分钟**。
*   **算力说明**：文中未明确标注具体的 GPU 型号和数量，但提到使用了轻量化 ResNet-18 作为视觉骨干网络，以支持快速迭代。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了仿真和实测，每项任务进行 50 次测试（Rollouts）。
*   **充分性**：
    *   对比了不同干预工具（键盘、VR、Copilot）的效率。
    *   分析了缩放因子 $\alpha$ 对末端轨迹 RMS 误差的影响。
    *   **公平性**：在对比实验中，确保了总演示轨迹数量一致，证明了“高质量修正片段”比“盲目增加完整轨迹”更有效。

### 6. 主要结论与发现
*   **性能提升**：在相同数据量下，Copilot 框架显著提高了任务成功率，尤其是在高精度任务中。
*   **效率极高**：收集修正片段的时间远低于收集完整轨迹的时间，且能直接覆盖策略的薄弱环节（OOD状态）。
*   **缩放因子的价值**：实验证明，较小的缩放因子（如 $\alpha=0.5$）能显著减少人类操作时的抖动和超调，使收集到的演示数据更平滑、更易于学习。

### 7. 优点
*   **低成本与开源**：硬件方案可负担且开源，利于社区复现。
*   **跨平台兼容性**：不要求 Leader 和 Follower 结构相同，具有极强的通用性。
*   **双向同步机制**：解决了人机切换时的位姿跳变问题，提升了干预的直观性。
*   **数据效率**：通过针对性补丁（Clips）而非全量数据解决 OOD 问题。

### 8. 不足与局限
*   **固定缩放因子**：目前 $\alpha$ 需要手动预设，尚未实现根据任务阶段自动调整的自适应缩放。
*   **多模态限制**：由于 ACT 策略在处理多模态数据（人类不同的操作风格）时存在局限，干预动作必须尽量贴合现有策略逻辑，否则可能导致训练不稳定。
*   **干预时机依赖**：系统性能仍高度依赖于人类操作员判断“何时介入”的经验。

（完）
