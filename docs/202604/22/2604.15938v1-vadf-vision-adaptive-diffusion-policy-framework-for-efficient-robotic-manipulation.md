---
title: "VADF: Vision-Adaptive Diffusion Policy Framework for Efficient Robotic Manipulation"
title_zh: VADF：用于高效机器人操作的视觉自适应扩散策略框架
authors: "Xinglei Yu, Zhenyang Liu, Shufeng Nan, Simo Wu, Yanwei Fu"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15938v1"
tags: ["query:rob-il"]
score: 10.0
evidence: 用于高效机器人操纵的视觉自适应扩散策略框架。
tldr: VADF是一个视觉驱动的双重自适应扩散策略框架，旨在解决机器人操作中训练收敛慢和推理效率低的问题。训练时通过自适应损失网络进行难样本挖掘以加速收敛；推理时利用分层视觉任务分割器根据任务复杂度动态调整噪声步数。该框架具有模型无关性，能显著提升训练效率并降低推理开销，实现更高效的机器人操控。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15938v1/fig-001.webp\", \"caption\": \"Fig. 1: VADF: Vision-Adaptive Diffusion Framework. VADF introduces a dualadaptive mechanism for both optimization and deployment. ALN : Accelerates convergence by identifying high-loss regions and prioritizing hard negatives. HVTS: Leverages zero-shot task decomposition to dynamically schedule denoising budgets (Nd, Na) based on stage complexity, ensuring high precision with reduced latency.\", \"page\": 2, \"index\": 1, \"width\": 698, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2604.15938v1/fig-002.webp\", \"caption\": \"Fig. 2: End-to-end framework of VADF. Training phase (ALN): Expert demonstration action sequences A0 are corrupted with controlled noise to generate At−1, At. A learnable time sampler produces temporal encodings p(t|A0) for the denoising model to predict noise ϵ(t), with adaptive loss computation and backpropagation enabling efficient policy learning. Inference phase (HVTS): High-level instructions and scene observations are processed by VLM-based task planners and stage classifiers to generate structured task decomposition and dynamic scheduling parameters, enabling adaptive execution across diverse robotic manipulation scenarios.\", \"page\": 5, \"index\": 2, \"width\": 642, \"height\": 489}]"
motivation: 现有的扩散策略因均匀采样和缺乏样本难度感知，导致训练收敛缓慢且推理时常因计算量大而超时。
method: 提出包含ALN（用于实时量化样本难度并引导加权采样）和HVTS（用于根据视觉输入动态调整推理步数）的视觉自适应框架。
result: 该框架显著减少了训练收敛步数，并在推理过程中大幅降低了计算开销，同时提高了早期成功率。
conclusion: VADF通过在训练和推理阶段引入视觉自适应机制，为高效、实时的机器人扩散策略提供了一种通用且有效的解决方案。
---

## 摘要
扩散策略正成为机器人操作的主流，但由于均匀采样和缺乏样本难度感知，面临着难负样本类别不平衡的问题，导致训练收敛缓慢且推理时频繁出现超时失败。我们提出了 VADF（视觉自适应扩散策略框架），这是一种视觉驱动的双重自适应框架，可显著减少收敛步数并在推理中实现早期成功，其模型无关的设计使其能够无缝集成到任何扩散策略架构中。在训练期间，我们引入了自适应损失网络（ALN），这是一种基于 MLP 的轻量级损失预测器，可实时量化每步样本的难度。在难负样本挖掘的指导下，它进行加权采样以优先处理高损失区域，从而实现自适应权重更新和更快的收敛。在推理阶段，我们设计了分层视觉任务分割器（HVTS），它根据视觉输入将高级任务指令分解为多阶段的低级子指令。它通过为简单动作分配较短的噪声调度和较长的直接执行序列，为复杂动作分配较长的噪声步数和较短的执行序列，从而将动作序列自适应地分割为简单和复杂的子任务，进而大幅减少计算开销并显著提高早期成功率。

## Abstract
Diffusion policies are becoming mainstream in robotic manipulation but suffer from hard negative class imbalance due to uniform sampling and lack of sample difficulty awareness, leading to slow training convergence and frequent inference timeout failures. We propose VADF (Vision-Adaptive Diffusion Policy Framework), a vision-driven dual-adaptive framework that significantly reduces convergence steps and achieves early success in inference, with model-agnostic design enabling seamless integration into any diffusion policy architecture. During training, we introduce Adaptive Loss Network (ALN), a lightweight MLP-based loss predictor that quantifies per-step sample difficulty in real time. Guided by hard negative mining, it performs weighted sampling to prioritize high-loss regions, enabling adaptive weight updates and faster convergence. In inference, we design the Hierarchical Vision Task Segmenter (HVTS), which decomposes high-level task instructions into multi-stage low-level sub-instructions based on visual input. It adaptively segments action sequences into simple and complex subtasks by assigning shorter noise schedules with longer direct execution sequences to simple actions, and longer noise steps with shorter execution sequences to complex ones, thereby dramatically reducing computational overhead and significantly improving the early success rate.

---

## 论文详细总结（自动生成）

### VADF：用于高效机器人操作的视觉自适应扩散策略框架论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
扩散策略（Diffusion Policy）在机器人操作中表现出色，但存在两个主要瓶颈：
*   **训练效率低**：传统方法采用均匀采样，无法区分简单样本和具有挑战性的“难负样本”（Hard Negatives），导致在低信息区域浪费大量计算资源，收敛缓慢。
*   **推理开销大且僵化**：现有的推理过程通常采用固定的去噪步数（$N_d$）和动作预测步长（$N_a$），忽略了任务阶段的异质性。例如，简单的接近动作和复杂的精准插入动作被分配了相同的计算资源，导致简单任务延迟过高，而复杂任务精度不足。
**VADF** 旨在通过视觉驱动的双重自适应机制，在不改变底层模型架构的前提下，实现训练加速和推理优化。

#### 2. 方法论：核心思想与关键技术
VADF 提出了两个模型无关（Model-agnostic）的插件模块：

*   **自适应损失网络（ALN - 训练阶段）**：
    *   **核心思想**：通过实时量化样本难度，优先训练高损失区域。
    *   **技术细节**：引入一个轻量级的 MLP 作为损失预测器。它利用**重要性采样（Importance Sampling）**原理，将固定的均匀时间步采样替换为可学习的分布 $\pi_\phi(k)$。
    *   **难样本挖掘**：维护一个轨迹权重向量，根据实时重构误差动态更新。高损失的轨迹和关键时间步会被赋予更高的采样概率，从而加速模型对复杂动作逻辑的掌握。

*   **分层视觉任务分割器（HVTS - 推理阶段）**：
    *   **核心思想**：利用视觉语言模型（VLM）进行零样本（Zero-shot）任务分解，动态分配计算预算。
    *   **技术细节**：
        1.  **任务分解**：使用 VLM（如 Qwen2-VL）将高级指令分解为多个语义阶段（如“接近”、“对齐”、“抓取”）。
        2.  **动态调度**：为简单阶段分配较少的去噪步数（$N_d$）和较长的执行步长（$N_a$）以提升速度；为复杂阶段分配较多的去噪步数和较短的执行步长以提升精度。
        3.  **实时分类**：在执行过程中，VLM 根据实时视觉反馈识别当前所处阶段，动态切换推理参数。

#### 3. 实验设计
*   **数据集与场景**：
    *   **Robomimic**：包含 Lift, Can, Square, Transport, ToolHang 等 3D 操作任务。
    *   **Push-T**：经典的 2D 形状推送任务。
    *   **Kitchen**：长程（Long-horizon）多任务顺序操作环境。
    *   **Adroit**：极具挑战性的 24 自由度灵巧手操作任务（Door, Hammer, Pen）。
*   **Benchmark 与对比方法**：
    *   对比了原始的 **Diffusion Policy**（基于 CNN 和 Transformer 版本）。
    *   对比了 **IBC**（隐式行为克隆）、**DP3**（3D 扩散策略）以及 **DDIM** 加速采样算法。
*   **真实世界验证**：在 ARX5 机械臂上进行了杯子堆叠、苹果入微波炉等任务的实机测试。

#### 4. 资源与算力
*   **硬件**：训练使用 **NVIDIA RTX A6000** GPU（48GB 显存）；实机推理使用 **NVIDIA RTX 5880 Ada** GPU。
*   **训练时长**：根据任务复杂度，训练持续 600 到 3500 个 Epoch（约 2万至 12万次梯度更新）。
*   **参数设置**：Batch size 为 32；VLM 使用 Qwen2-VL-7B-Instruct。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2D 到 3D、从简单抓取到高维度灵巧操作的多种任务，实验设置较为全面。
*   **统计严谨性**：所有实验均在 **3 个随机种子**下运行，每个任务在 50 个初始条件下进行评估，并报告了平均值和标准差。
*   **消融实验**：专门针对 ALN 和 HVTS 进行了消融分析，验证了两个模块各自的贡献以及协同效应。
*   **公平性**：在相同的骨干网络和环境设置下与基准模型对比，证明了其作为插件的有效性。

#### 6. 主要结论与发现
*   **性能提升**：VADF 在所有基准测试中均优于原始扩散策略，尤其在 Adroit 灵巧手任务中，成功率提升了 10% 以上。
*   **收敛加速**：ALN 显著减少了达到峰值性能所需的训练步数，且训练过程更加稳定（方差更小）。
*   **推理效率**：HVTS 在保持甚至提升成功率的同时，实现了高达 **2.46倍** 的推理加速（结合 DDIM 时）。
*   **早期成功率**：通过动态调整，模型在任务初期的表现更稳健，大幅降低了因超时导致的失败。

#### 7. 优点
*   **模型无关性**：作为即插即用的框架，无需修改原有的扩散模型架构，也无需额外的专家标注或预训练阶段。
*   **计算感知**：首次在扩散策略中引入了“可变努力”范式，使机器人能像人类一样根据任务难度分配注意力。
*   **零样本能力**：利用现成的 VLM 进行任务理解，具备很强的通用性和语义解释性。

#### 8. 不足与局限
*   **VLM 依赖性**：HVTS 的效果高度依赖于底层视觉语言模型的推理能力，若 VLM 发生误判，可能导致错误的参数调度。
*   **感知延迟**：虽然减少了去噪计算，但引入 VLM 进行阶段识别本身会带来一定的计算开销（文中通过非逐帧识别来缓解）。
*   **超参数敏感性**：阶段划分的数量 $K$ 以及 $N_d, N_a$ 的取值范围仍需一定的经验调试。

（完）
