---
title: "SIM1: Physics-Aligned Simulator as Zero-Shot Data Scaler in Deformable Worlds"
title_zh: SIM1：可变形世界中作为零样本数据扩展器的物理对齐模拟器
authors: "Yunsong Zhou, Hangxu Liu, Xuekun Jiang, Xing Shen, Yuanzhen Zhou, Hui Wang, Baole Fang, Yang Tian, Mulin Yu, Qiaojun Yu, Li Ma, Hengjie Li, Hanqing Wang, Jia Zeng, Jiangmiao Pang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08544v2"
tags: ["query:rob-il"]
score: 8.0
evidence: 柔性物体操纵的数据扩展引擎
tldr: "针对柔性物体操作中数据获取难、仿真与现实不匹配的问题，本文提出SIM1，一个物理对齐的real-to-sim-to-real数据引擎。它通过将现实场景数字化为度量一致的孪生场景，并利用弹性建模校准动力学，结合扩散模型生成高质量合成轨迹。实验证明，仅用合成数据训练的策略在真实世界中达到了90%的零样本成功率，且数据效率比真实数据高15倍，显著提升了泛化能力。"
selection_source: fresh_fetch
motivation: 传统的仿真到现实方法因缺乏物理对齐，难以处理柔性物体复杂的几何形状和动力学变化。
method: 提出SIM1引擎，通过场景数字化、弹性动力学校准及基于扩散模型的轨迹生成与过滤，将少量演示扩展为大规模高质量合成数据。
result: 15的数据比例下达到了与真实数据相当的性能。
conclusion: 物理对齐的仿真可以作为柔性物体操作的可扩展监督手段，是实现高效机器人策略学习的有效途径。
---

## 摘要
可变形物体的机器人操作是具身学习中一个数据密集型的领域，其中形状、接触和拓扑结构的共同演化方式远超刚体的多变性。尽管模拟技术有望缓解现实世界数据获取的成本压力，但主流的“模拟到现实”（sim-to-real）流程仍根植于刚体抽象，导致几何形状不匹配、软体动力学脆弱，以及不适用于布料交互的运动原语。我们认为，模拟之所以失败，并非因为它是合成的，而是因为它是未接地的。为了解决这一问题，我们推出了 SIM1，这是一个物理对齐的“现实-模拟-现实”（real-to-sim-to-real）数据引擎，旨在将模拟接地于物理世界。在给定有限演示的情况下，该系统将场景数字化为度量一致的孪生体，通过弹性建模校准可变形动力学，并通过带有质量过滤的基于扩散的轨迹生成来扩展行为。该流程将稀疏的观测转化为具有接近演示保真度的大规模合成监督信号。实验表明，在 1:15 的等效比例下，纯合成数据训练的策略达到了与真实数据基准相当的水平，同时在现实世界部署中实现了 90% 的零样本成功率和 50% 的泛化增益。这些结果验证了物理对齐模拟作为可变形操作的可扩展监督手段的有效性，并为高效数据策略学习提供了一条切实可行的路径。

## Abstract
Robotic manipulation with deformable objects represents a data-intensive regime in embodied learning, where shape, contact, and topology co-evolve in ways that far exceed the variability of rigids. Although simulation promises relief from the cost of real-world data acquisition, prevailing sim-to-real pipelines remain rooted in rigid-body abstractions, producing mismatched geometry, fragile soft dynamics, and motion primitives poorly suited for cloth interaction. We posit that simulation fails not for being synthetic, but for being ungrounded. To address this, we introduce SIM1, a physics-aligned real-to-sim-to-real data engine that grounds simulation in the physical world. Given limited demonstrations, the system digitizes scenes into metric-consistent twins, calibrates deformable dynamics through elastic modeling, and expands behaviors via diffusion-based trajectory generation with quality filtering. This pipeline transforms sparse observations into scaled synthetic supervision with near-demonstration fidelity. Experiments show that policies trained on purely synthetic data achieve parity with real-data baselines at a 1:15 equivalence ratio, while delivering 90% zero-shot success and 50% generalization gains in real-world deployment. These results validate physics-aligned simulation as scalable supervision for deformable manipulation and a practical pathway for data-efficient policy learning.

---

## 论文详细总结（自动生成）

以下是对论文《SIM1: Physics-Aligned Simulator as Zero-Shot Data Scaler in Deformable Worlds》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：柔性物体（如衣物）的操作是机器人领域的难点，其形状、接触和拓扑结构的演化极其复杂，对训练数据量有极高需求。
*   **核心痛点**：
    1.  **现实数据昂贵**：采集大规模真实世界柔性物体操作数据成本极高。
    2.  **仿真与现实鸿沟（Sim-to-Real Gap）**：传统仿真器多基于刚体抽象，导致几何不匹配、软体动力学不稳定，且缺乏适用于布料交互的运动原语。
*   **核心思想**：提出 **SIM1**，一个物理对齐的 **Real-to-Sim-to-Real (R2S2R)** 数据引擎。其核心理念是“模拟的失败不在于它是合成的，而在于它未与现实接地（ungrounded）”。通过将仿真环境在几何、动力学和运动三个维度与现实对齐，使合成数据能直接用于真实机器人的零样本（Zero-Shot）部署。

### 2. 方法论
SIM1 框架包含三个关键阶段：
*   **SIM1-Scene（场景数字化）**：
    *   利用高精度 3D 扫描（EinScan Rigil Pro）获取衣物的度量一致（Metric-accurate）网格和纹理。
    *   通过 URDF 导入机器人资产，确保运动学和工作空间与现实完全一致。
*   **SIM1-Sim（动力学对齐）**：
    *   **稳定求解器**：开发了基于增强顶点块下降（AVBD）的求解器，引入了**应变约束（Strain Constraint）**。公式为 $C(e) = \|e_i - e_j\| - (1 + \xi)l_0 \le 0$，当边缘拉伸超过阈值 $\xi$ 时，注入虚拟弹性力以加速收敛，防止布料在交互中出现非物理的过度拉伸。
    *   **参数校准**：通过双向同步基础设施，由专家对比现实与仿真的视觉反馈，迭代调整杨氏模量、摩擦力等物理参数。
*   **SIM1-DataGen（结构化操作合成）**：
    *   **轨迹分解**：将专家演示分解为“交互段”和“移动段”。
    *   **扩散模型生成**：利用条件扩散强制（Diffusion Forcing）模型补全移动段，生成类人的平滑过渡轨迹。
    *   **质量过滤**：使用基于状态的统计规则和基于 Transformer 的视频判别器过滤掉穿模或失败的合成轨迹。

### 3. 实验设计
*   **实验场景**：使用 ARX ACONE 双臂机器人进行 T 恤折叠（主任务）、毛巾翻转、短裤折叠等任务。
*   **数据集/Benchmark**：
    *   采集了 1,000 条真实轨迹作为基准。
    *   在仿真中生成了 10k 规模的合成数据集。
*   **对比方法**：
    *   **Real Data**：纯真实数据训练。
    *   **Sim Teleoperated**：仿真环境下的遥操作数据。
    *   **Sim Generated**：SIM1 自动生成的合成数据。
    *   **模型底座**：在 $\pi_0$ 和 $\pi_{0.5}$ 视觉语言动作模型（VLA）上进行验证。
*   **评估维度**：域内成功率、空间泛化（平移/旋转）、纹理泛化、光照泛化、视角泛化。

### 4. 资源与算力
*   **硬件环境**：
    *   **仿真运行**：单台 RTX 4090 工作站。
    *   **大规模生成**：配备 8x NVIDIA RTX 4090 GPU 的服务器。
*   **效率数据**：
    *   仿真运行速度约为 15 FPS。
    *   单 GPU 渲染一条轨迹平均耗时 16.2 分钟。
    *   **成本对比**：仿真生成单条轨迹成本约为 0.10 美元，比现实采集（2.71 美元）便宜约 27 倍，吞吐量提升 6.8 倍。

### 5. 实验数量与充分性
*   **实验规模**：
    *   真实世界部署测试超过 30 次/配置。
    *   涵盖了从 T 恤到 Polo 衫、短裤、毛巾等多种柔性物体。
*   **消融实验**：针对轨迹分解、扩散生成、稳定求解器等核心组件进行了详细的消融对比，验证了各模块对成功率的贡献。
*   **客观性**：实验设置了“从零训练（From Scratch）”对比组，排除了预训练模型先验知识的干扰，证明了性能提升确实源于合成数据。

### 6. 主要结论与发现
*   **零样本成功**：纯合成数据训练的策略在真实 T 恤折叠任务中达到了 **90%** 的成功率。
*   **数据等效比**：在达到相同性能时，1 条真实演示约等效于 **15 条** SIM1 合成轨迹。
*   **泛化增益**：相比真实数据，合成数据由于具备更高的环境多样性，在空间、纹理、光照等分布偏移下提供了超过 **50%** 的泛化性能提升。
*   **扩展性**：随着合成数据规模增加，模型性能持续增长，且未表现出明显的饱和迹象。

### 7. 优点
*   **物理接地**：通过 AVBD 求解器解决了软体仿真中常见的“过度拉伸”和“数值不稳定”问题，使仿真动力学具有极高的真实度。
*   **端到端流程**：实现了从扫描建模到自动轨迹生成、过滤、渲染的完整闭环。
*   **高效扩展**：证明了在柔性物体领域，高质量仿真数据可以作为真实数据的有效替代和补充。

### 8. 不足与局限
*   **自动化程度限制**：物理参数（如不同布料的摩擦、弹性）的校准仍需专家进行一定程度的手动调优，尚未实现完全自动化的参数辨识。
*   **任务复杂度**：虽然展示了长程折叠任务，但对于更极端的拓扑变化（如打结、穿衣）尚未深入探讨。
*   **求解器开销**：虽然 AVBD 求解器在 GPU 上运行，但对于极高分辨率的网格，实时性仍有提升空间。

（完）
