---
title: "V-CAGE: Vision-Closed-Loop Agentic Generation Engine for Robotic Manipulation"
title_zh: V-CAGE：用于机器人操作的视觉闭环智能体生成引擎
authors: "Yaru Liu, Ao-bo Wang, Nanyang Ye"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09036v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 用于机器人数据合成的视觉闭环智能生成
tldr: "V-CAGE是一个用于机器人操作数据合成的智能体框架，旨在解决现有VLA模型训练数据缺乏语义一致性和物理可行性的问题。该框架通过修复引导的场景构建实现上下文感知的布局规划，确保目标可达；并引入基于VLM的闭环验证机制作为视觉评论员，严格过滤无效轨迹。此外，它采用感知驱动的压缩算法降低了90%以上的存储成本。V-CAGE实现了端到端的数据合成自动化，为大规模、高质量机器人数据集的生成提供了高效、可扩展的方案。"
selection_source: fresh_fetch
motivation: 现有机器人场景生成方法缺乏上下文感知，导致生成的环境常出现物理不可达或语义不合理的问题，阻碍了VLA模型的规模化训练。
method: 提出了一种集成修复引导场景构建、VLM闭环验证机制和感知驱动视频压缩技术的智能体化数据合成引擎。
result: 该系统能够自动合成语义结构化且运动学可达的高质量数据，并在不损失训练效能的前提下大幅减少了存储占用。
conclusion: V-CAGE通过将语义布局规划与视觉自验证相结合，为构建大规模、高保真且物理一致的机器人操作数据集提供了自动化闭环方案。
---

## 摘要
扩展视觉-语言-动作（VLA）模型需要大规模的数据集，这些数据集必须在语义上连贯且在物理上可行。然而，现有的场景生成方法通常缺乏上下文感知能力，难以合成嵌入丰富语义信息的高保真环境，经常导致目标位置不可达，从而使任务过早失败。我们提出了 V-CAGE（视觉闭环智能体生成引擎），这是一个用于自主机器人数据合成的智能体框架。与传统的脚本化流水线不同，V-CAGE 作为一个具身智能体系统运行，利用基础模型将高层语义推理与底层物理交互联系起来。具体而言，我们引入了修复引导的场景构建（Inpainting-Guided Scene Construction），以系统地安排上下文感知布局，确保生成的场景在语义上结构化且在运动学上可达。为了确保轨迹的正确性，我们将功能元数据与基于视觉语言模型（VLM）的闭环验证机制相结合，充当视觉评论者（visual critic），严格过滤掉隐性失败并切断错误传播链。最后，为了克服大规模视频数据集的存储瓶颈，我们实现了一种感知驱动的压缩算法，在不损害下游 VLA 训练效果的前提下，实现了超过 90% 的文件大小缩减。通过集中语义布局规划和视觉自我验证，V-CAGE 实现了端到端流水线的自动化，从而能够高度可扩展地合成多样化、高质量的机器人操作数据集。

## Abstract
Scaling Vision-Language-Action (VLA) models requires massive datasets that are both semantically coherent and physically feasible. However, existing scene generation methods often lack context-awareness, making it difficult to synthesize high-fidelity environments embedded with rich semantic information, frequently resulting in unreachable target positions that cause tasks to fail prematurely. We present V-CAGE (Vision-Closed-loop Agentic Generation Engine), an agentic framework for autonomous robotic data synthesis. Unlike traditional scripted pipelines, V-CAGE operates as an embodied agentic system, leveraging foundation models to bridge high-level semantic reasoning with low-level physical interaction. Specifically, we introduce Inpainting-Guided Scene Construction to systematically arrange context-aware layouts, ensuring that the generated scenes are both semantically structured and kinematically reachable. To ensure trajectory correctness, we integrate functional metadata with a Vision-Language Model based closed-loop verification mechanism, acting as a visual critic to rigorously filter out silent failures and sever the error propagation chain. Finally, to overcome the storage bottleneck of massive video datasets, we implement a perceptually-driven compression algorithm that achieves over 90\% filesize reduction without compromising downstream VLA training efficacy. By centralizing semantic layout planning and visual self-verification, V-CAGE automates the end-to-end pipeline, enabling the highly scalable synthesis of diverse, high-quality robotic manipulation datasets.

---

## 论文详细总结（自动生成）

### V-CAGE：用于机器人操作的视觉闭环智能体生成引擎论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：扩展视觉-语言-动作（VLA）模型需要海量、语义连贯且物理可行的专家轨迹数据。
*   **现有挑战**：
    *   **真实数据昂贵**：在现实世界收集多步交互数据成本极高，且难以覆盖长尾场景。
    *   **传统仿真局限**：现有的自动化仿真流水线多为“盲目”脚本，缺乏智能体推理能力。
    *   **几何与语义冲突**：随机放置物体常导致物体重叠或目标位置不可达。
    *   **隐性失败（Silent Failures）**：在长程任务中，微小的执行错误（如抓取打滑）会随时间累积，导致后续轨迹全部失效。
*   **核心目标**：开发一个名为 V-CAGE 的智能体框架，通过视觉闭环验证和上下文感知的场景构建，实现高质量、长程机器人操作数据的自动化合成。

#### 2. 方法论：核心思想与关键技术
V-CAGE 将数据合成过程从“脚本自动化”转向“智能体化仿真”，核心组件包括：
*   **修复引导的场景构建 (IGSC)**：
    1.  **资产选择与初步放置**：LLM 根据指令选择物体，并利用 AABB 碰撞检测进行初步无碰撞放置。
    2.  **布局规划与图像修复**：LLM 生成语义布局描述，利用修复模型（Nano Banana）生成目标布局图像。
    3.  **坐标恢复**：利用 Grounding DINO 和 DINOv2 进行特征匹配，将图像中的像素位置映射回物理世界坐标。
    4.  **碰撞优化**：通过最小化包含碰撞惩罚、位移惩罚和边界约束的复合代价函数（L-BFGS-B 算法）来精细调整物体位置。
*   **基于模板的子任务搜索**：利用预定义的代码模板（如抓取、放置），由 LLM 填充执行逻辑，将复杂任务分解为可验证的原子动作。
*   **基于 VLM 的闭环验证**：引入 Gemini 3 作为“视觉评论员”，对每个子任务的执行结果进行视觉检查。只有通过验证的轨迹才会被保留，从而切断错误传播链。
*   **动作感知感知压缩**：基于人类视觉感知指标（ColorVideoVDP），识别关键帧并动态调整压缩率（CRF），在保持 VLA 训练效能的同时减少 90% 以上的存储空间。

#### 3. 实验设计
*   **实验场景与任务**：
    *   **合成任务**：设计了四个复杂的长程任务：自助结账（AutoCheckout）、面包打包（PackBreads）、文具整理（PackStationery）、柜子分类（SortToCabinet）。
    *   **消融场景**：在浴室、橱柜、收银台、厨房、办公桌等多种环境下测试场景生成的有效性。
*   **Benchmark 与对比方法**：
    *   **基准模型**：使用 $\pi_{0.5}$ VLA 模型作为评估对象。
    *   **对比实验**：对比了随机放置（Random）与 IGSC 方案；对比了开环生成（无 VLM 验证）与 V-CAGE 闭环生成。
    *   **Sim-to-Real**：在 ALOHA-AgileX 硬件平台上进行实机验证，对比仅使用真实数据与“真实+仿真”混合数据的表现。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及训练总时长。
*   **模型调用**：提到了使用 Gemini 3 作为视觉验证器，以及 DINOv2 和 Grounding DINO 等视觉基础模型，这些通常需要高性能 GPU 支持。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   每个合成任务生成了 100 条专家轨迹。
    *   实机验证进行了 20 次试验。
    *   消融实验覆盖了 5 类场景，涉及数十个子任务。
*   **充分性评价**：实验设计较为全面，涵盖了从场景生成质量、数据纯度验证到下游模型训练效果及跨域（Sim-to-Real）迁移能力的评估。通过消融实验客观地证明了 IGSC 和 VLM 验证模块的必要性。

#### 6. 主要结论与发现
*   **数据质量**：VLM 闭环验证将数据纯度（正确率）提升至 100%，有效消除了“隐性失败”。
*   **训练效果**： fine-tuning 后的 $\pi_{0.5}$ 模型在复杂任务上实现了从 0% 到最高 100% 的成功率提升。
*   **Sim-to-Real 增益**：加入仿真数据后，实机任务成功率从 20% 提升至 55%，证明了合成数据的高保真度和泛化价值。
*   **压缩效率**：感知驱动的压缩算法在减少 93% 存储空间的情况下，对模型训练效果几乎没有负面影响。

#### 7. 优点（亮点）
*   **智能体化架构**：将数据生成视为智能体行为，具备语义推理和自我纠错能力。
*   **闭环验证机制**：解决了仿真数据中长期存在的“轨迹污染”问题，确保了因果一致性。
*   **存储优化**：创新的感知压缩技术解决了大规模视频数据集的存储瓶颈，具有很强的工程实用性。
*   **端到端自动化**：从自然语言指令到最终压缩的专家轨迹，实现了全流程自动化。

#### 8. 不足与局限
*   **计算成本**：基于拒绝采样（Rejection Sampling）的验证机制在任务极其复杂时，可能会因为成功率低而导致采样效率低下，计算开销较大。
*   **物理限制**：目前主要针对刚性物体操作，对于柔性物体（如布料）或流体的动态交互支持有限。
*   **实时性**：由于涉及多次 LLM 和 VLM 调用，生成单条高质量轨迹的时间成本可能较高。

（完）
