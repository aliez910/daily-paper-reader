---
title: "StreamingVLA: Streaming Vision-Language-Action Model with Action Flow Matching and Adaptive Early Observation"
title_zh: StreamingVLA：结合动作流匹配与自适应早期观测的流式视觉-语言-动作模型
authors: "Yiran Shi, Dongqi Guo, Tianchen Zhao, Feng Gao, Liangzhi Shi, Chao Yu, ZhiJian Mo, Qihua Xiao, XiaoShuai Peng, Qingmin Liao, Yu Wang"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28565v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 用于机器人控制任务的视觉-语言-动作 (VLA) 模型
tldr: 针对视觉-语言-动作（VLA）模型在边缘设备部署时因顺序执行导致的延迟和停顿问题，本文提出了StreamingVLA。该模型通过引入动作流匹配（Action Flow Matching）和自适应早期观测机制，实现了观测、生成与执行阶段的异步并行流式处理。在保持性能的同时，该方法显著提升了执行流畅度，实现了2.4倍的延迟加速，并将执行停顿减少了6.5倍。
selection_source: fresh_fetch
motivation: 传统的VLA模型因观测、生成和执行阶段必须顺序进行，导致系统频繁停顿且延迟较高，难以满足实时部署需求。
method: 采用动作流匹配替代动作分块以重叠生成与执行延迟，并设计动作显著性感知的自适应观测机制来重叠执行与观测延迟。
result: 在不损失性能的前提下，StreamingVLA实现了2.4倍的延迟加速，并将执行停顿频率降低了6.5倍。
conclusion: 通过将VLA各阶段异步并行化，StreamingVLA有效解决了实时机器人控制中的效率与流畅性瓶颈。
---

## 摘要
视觉-语言-动作（VLA）模型在自然语言驱动的感知与控制方面展现出了卓越的性能。然而，VLA 模型的高计算成本带来了显著的效率挑战，特别是在实际部署中资源受限的边缘平台。此外，由于 VLA 的不同阶段（观测、动作生成和执行）必须顺序进行，且需等待前一阶段完成，系统面临频繁的停顿和高延迟。为了解决这一问题，我们进行了系统分析以识别实现快速且流畅生成的挑战，并提出以“流式”方式使 VLA 具备跨阶段异步并行的能力。首先，我们消除了对动作分块（action chunking）的依赖，并采用了动作流匹配（action flow matching），该方法学习动作流的轨迹，而非对分块动作进行去噪，从而实现了动作生成与执行延迟的重叠。其次，我们设计了一种动作显著性感知的自适应观测机制，从而实现了执行与观测延迟的重叠。在不牺牲性能的前提下，StreamingVLA 实现了显著的加速并提高了执行的流畅度。实验结果显示，它实现了 2.4 倍的延迟加速，并将执行停顿减少了 6.5 倍。

## Abstract
Vision-language-action (VLA) models have demonstrated exceptional performance in natural language-driven perception and control. However, the high computational cost of VLA models poses significant efficiency challenges, particularly for resource-constrained edge platforms in real-world deployments. However, since different stages of VLA (observation, action generation and execution) must proceed sequentially, and wait for the completion of the preceding stage, the system suffers from frequent halting and high latency. To address this, We conduct a systematic analysis to identify the challenges for fast and fluent generation, and propose enabling VLAs with the ability to asynchronously parallelize across VLA stages in a "streaming" manner. First, we eliminate the reliance on action chunking and adopt action flow matching, which learns the trajectory of action flows rather than denoising chunk-wise actions. It overlaps the latency of action generation and execution. Second, we design an action saliency-aware adaptive observation mechanism, thereby overlapping the latency of execution and observation. Without sacrificing performance, StreamingVLA achieves substantial speedup and improves the fluency of execution. It achieves a 2.4 $\times$ latency speedup and reduces execution halting by 6.5 $\times$.

---

## 论文详细总结（自动生成）

以下是对论文《StreamingVLA: Streaming Vision-Language-Action Model with Action Flow Matching and Adaptive Early Observation》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在部署时存在**高延迟**和**执行停顿（Stuttering）**问题。
*   **研究背景**：传统的 VLA 模型（如 $\pi_0$ 系列）通常采用“动作分块（Action Chunking）”技术。尽管这能提高效率，但观测（Observation）、动作生成（Generation）和动作执行（Execution）三个阶段仍是串行同步的。机器人必须等待模型完成前一阶段的推理才能开始执行，导致在两次执行之间存在明显的停顿时间（$T_{halt}$），使得动作不连贯且缓慢。
*   **整体含义**：本文旨在通过“流式（Streaming）”架构，实现 VLA 各阶段的异步并行化，从而在不损失性能的前提下，提升机器人控制的实时性和流畅度。

### 2. 方法论
StreamingVLA 提出了两个核心技术来重叠（Overlap）不同阶段的延迟：

*   **动作流匹配（Action Flow Matching, AFM）**：
    *   **核心思想**：放弃传统的动作分块去噪（Diffusion），转而采用基于状态建模的流匹配。
    *   **技术细节**：将动作建模为特征空间中状态的更新，而非绝对值。模型预测速度场 $v_\theta$，通过公式 $x_{t+1} = x_t + v_\theta(x_t, t|o)\Delta t$ 更新状态。
    *   **优势**：这种方式支持“逐个动作”生成，使得动作生成可以与动作执行并行，消除了等待整个动作块生成的延迟。
*   **自适应早期观测（Adaptive Early Observation, AEO）**：
    *   **核心思想**：在当前动作序列尚未执行完时，提前启动下一轮的 VLM 观测处理。
    *   **关键技术**：引入“动作显著性（Action Saliency）”概念。利用一个轻量级的 Transformer 预测器来评估剩余动作对环境改变的程度。
    *   **流程**：如果预测的显著性低（环境变化小），则执行早期观测；如果显著性高（如正在开门或放置物体），则延迟观测以确保感知准确性。这实现了执行阶段与观测阶段的重叠。

### 3. 实验设计
*   **数据集与场景**：使用了 **LIBERO** 仿真基准测试，包含四个子任务集：LIBERO-Spatial（空间关系）、LIBERO-Object（物体操作）、LIBERO-Goal（目标达成）和 LIBERO-Long（长程任务）。
*   **基准模型（Backbone）**：以 $\pi_{0.5}$ 模型为基础进行改进。
*   **对比方法**：
    *   **RTC (Real-Time Chunking)**：实时分块执行。
    *   **SmolVLA**：一种轻量级 VLA 优化方法。
    *   **VLASH**：通过微调缓解早期观测失准的方法。
    *   **Temporal Ensembling**：时间集成平滑方法。
*   **评估指标**：成功率（Success Rate）、单步动作耗时（$T_{action}$）、停顿间隙（$T_{halt}$）。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA A800-SXM4-80GB GPU**。
*   **训练时长**：
    *   主模型训练：60,000 次迭代，耗时约 **10-20 GPU 小时**。
    *   自适应预测器训练：54,000 次迭代，耗时约 **3-4 GPU 小时**。
*   **推理开销**：预测器带来的额外计算开销仅占模型推理时间的 5% 左右。

### 5. 实验数量与充分性
*   **实验规模**：在 LIBERO 的 40 个不同任务上进行了评估，每个任务进行 **50 次 Rollouts**（总计 2000 次测试迭代），样本量充足。
*   **消融实验**：针对动作流匹配（AFM）、归一化修正（NM）、状态对齐（SA）以及不同的早期观测策略（Naive vs. Random vs. Adaptive）进行了详细的对比实验。
*   **真实世界验证**：在 Franka Panda 机器人臂上进行了实机部署，验证了从仿真到现实的迁移能力。
*   **评价**：实验设计客观、公平，对比了当前最前沿的加速方案，且消融实验完整地证明了各模块的贡献。

### 6. 主要结论与发现
*   **性能保持**：StreamingVLA 在大幅加速的同时，成功率几乎没有下降（平均 94.9% vs. 基准 95.1%）。
*   **显著加速**：实现了 **2.4 倍** 的端到端延迟加速。
*   **流畅度提升**：将执行过程中的停顿时间减少了 **6.5 倍**（从约 230ms 降至 36ms），实现了肉眼可见的流畅动作。
*   **自适应的重要性**：实验证明，无差别的早期观测会导致性能大幅下降，而基于显著性的自适应策略是保持高成功率的关键。

### 7. 优点
*   **系统性分析**：论文对 VLA 运行时的瓶颈进行了深入的量化分析，找出了优化的核心目标。
*   **异步架构创新**：不同于以往只关注模型压缩的研究，本文从系统调度角度出发，通过异步并行化解决了硬件利用率低的问题。
*   **即插即用**：提出的自适应观测预测器非常轻量，可以较容易地集成到其他 VLA 架构中。

### 8. 不足与局限
*   **架构依赖**：该方法目前主要针对类似 $\pi_0$ 这种结合了 VLM 和扩散/流匹配专家的架构，对于纯自回归（Autoregressive）生成的 VLA 模型（如 RT-2）的适用性尚未讨论。
*   **阈值敏感性**：自适应观测中的显著性阈值（$\eta$）可能需要针对不同环境进行手动微调，以平衡速度与精度。
*   **任务复杂度**：虽然在 LIBERO 上表现优异，但在更复杂、动态变化更剧烈的开放世界场景下，预测器的准确性可能面临挑战。

（完）
