---
title: "Flow Motion Policy: Manipulator Motion Planning with Flow Matching Models"
title_zh: 流运动策略：基于流匹配模型的机械臂运动规划
authors: "Davood Soleymanzadeh, Xiao Liang, Minghui Zheng"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07084v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 机器人操纵器的端到端神经运动规划器
tldr: 针对现有端到端神经运动规划器生成路径单一且缺乏推理优化的问题，本文提出 Flow Motion Policy (FMP)。该方法利用流匹配（Flow Matching）生成模型捕捉规划数据集的多模态分布，实现开环端到端规划。通过推理时的 Best-of-N 采样，FMP 能生成多个候选路径并筛选无碰撞解，显著提升了机械臂在复杂环境下的规划成功率和效率。
selection_source: fresh_fetch
motivation: 现有的神经运动规划器通常只能生成单一路径，无法有效利用生成模型的随机性进行推理优化。
method: 采用流匹配模型构建随机生成策略，通过对可行路径分布建模实现推理时的多路径采样与碰撞评估。
result: 实验表明，FMP 在规划成功率和效率上均优于传统的采样算法及现有的神经运动规划方法。
conclusion: 随机生成策略结合推理时优化能有效提升端到端运动规划的性能，为复杂环境下的机器人控制提供了新思路。
---

## 摘要
最近，开环端到端神经运动规划器被提出，用于改进机器人机械臂的运动规划。这些方法能够直接根据传感器观测进行规划，而无需在规划过程中依赖特权碰撞检测器。然而，许多现有方法在不同运行中针对给定工作空间仅生成单一路径，且未利用其开环结构进行推理时优化。为解决这一局限性，我们引入了流运动策略（Flow Motion Policy），这是一种用于机器人机械臂的开环端到端神经运动规划器，它利用流匹配方法的随机生成公式来捕捉规划数据集固有的多模态性。通过对可行路径的分布进行建模，流运动策略实现了高效的推理时 $N$ 选 1（best-of-$N$）采样。该方法生成多个端到端候选路径，在规划后评估其碰撞状态，并执行第一个无碰撞的解。我们将流运动策略与具有代表性的基于采样的运动规划方法和神经运动规划方法进行了基准测试。评估结果表明，流运动策略提高了规划成功率和效率，突显了随机生成策略在端到端运动规划和推理时优化中的有效性。实验评估视频可通过此链接获取。

## Abstract
Open-loop end-to-end neural motion planners have recently been proposed to improve motion planning for robotic manipulators. These methods enable planning directly from sensor observations without relying on a privileged collision checker during planning. However, many existing methods generate only a single path for a given workspace across different runs, and do not leverage their open-loop structure for inference-time optimization. To address this limitation, we introduce Flow Motion Policy, an open-loop, end-to-end neural motion planner for robotic manipulators that leverages the stochastic generative formulation of flow matching methods to capture the inherent multi-modality of planning datasets. By modeling a distribution over feasible paths, Flow Motion Policy enables efficient inference-time best-of-$N$ sampling. The method generates multiple end-to-end candidate paths, evaluates their collision status after planning, and executes the first collision-free solution. We benchmark the Flow Motion Policy against representative sampling-based and neural motion planning methods. Evaluation results demonstrate that Flow Motion Policy improves planning success and efficiency, highlighting the effectiveness of stochastic generative policies for end-to-end motion planning and inference-time optimization. Experimental evaluation videos are available via this \href{https://zh.engr.tamu.edu/wp-content/uploads/sites/310/2026/03/FMP-Website.mp4}{link}.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Flow Motion Policy (FMP)** 的新型机器人运动规划框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的端到端神经运动规划器（Neural Motion Planners）大多是**确定性**的，即对于给定的环境和目标，只能生成单一路径。这导致它们无法处理运动规划中固有的多模态性（即到达目标有多种可行路径），也无法在推理阶段通过多次采样来优化结果。
*   **研究背景**：传统的采样算法（如 RRT）虽然具有随机性，但碰撞检测（Collision Checking）占据了 90% 以上的计算时间，效率低下。而现有的神经规划器虽然速度快，但缺乏灵活性，一旦生成的唯一路径发生碰撞，规划就会失败。
*   **研究动机**：利用**流匹配（Flow Matching）**生成模型的随机性，构建一个能够生成多种候选路径的开环规划器，并通过推理时的“$N$ 选 1”（Best-of-$N$）采样策略来平衡规划速度与成功率。

### 2. 方法论
*   **核心思想**：将运动规划建模为一个条件生成任务。给定当前状态、目标状态和环境点云，模型学习可行路径增量的分布。
*   **关键技术细节**：
    *   **流匹配（Flow Matching）**：相比于传统的扩散模型（Diffusion），流匹配通过构建连续正规化流（CNF），在噪声分布和真实数据分布之间进行线性插值，训练更稳定且推理效率更高。
    *   **网络架构**：
        *   **感知层**：使用 PointNet++ 提取机器人自身和环境障碍物的点云特征。
        *   **融合层**：采用 Transformer Encoder 将机器人配置、目标配置和点云特征进行全局注意力融合。
        *   **生成头**：使用轻量级的 MLP 作为流匹配头，预测向量场以生成动作序列。
    *   **算法流程**：
        1.  **训练阶段**：使用专家规划器（cuRobo）生成的大规模数据集，通过监督学习训练流匹配模型。
        2.  **推理阶段**：从高斯噪声中并行采样 $N$ 个候选路径。
        3.  **Best-of-N 优化**：利用并行碰撞检测器评估这 $N$ 条路径，执行第一条无碰撞的路径。

### 3. 实验设计
*   **实验场景**：包括桌面（TableTop）、方块（Box）、箱子（Bins）和货架（Shelf）等多种复杂环境。
*   **Benchmark（基准测试）**：
    *   **传统采样算法**：Bi-RRT, BIT*。
    *   **神经启发式采样器**：MPNets, SIMPNet, GAIDE。
    *   **端到端神经规划器**：PerFACT, Neural MP（基于 GMM 模型）。
*   **对比维度**：规划成功率（Success Rate）和平均规划时间（Planning Time）。

### 4. 资源与算力
*   **硬件设备**：实验在一台配备 **NVIDIA RTX 4080 GPU** 的工作站上运行。
*   **算力支持**：部分研究得到了德州农工大学（Texas A&M）高性能研究计算资源的支持。
*   **训练细节**：论文未明确给出具体的训练总时长，但提到模型参数量仅为 **1.4M**，远小于 Neural MP 的 20M，具有极高的轻量化特性。

### 5. 实验数量与充分性
*   **实验规模**：在多个仿真任务中进行了大量测试，并进行了 **30 组真实的 UR5e 机械臂实验**。
*   **消融实验**：非常充分。作者对比了不同的生成头架构（MLP, U-Net, Transformer, DiT）、不同的生成框架（流匹配 vs 扩散模型）、推理步数以及采样数量 $N$ 对性能的影响。
*   **客观性**：通过与多种 SOTA 方法在相同任务下对比，并提供了真实环境的视频演示，实验结果具有较强的说服力和公平性。

### 6. 主要结论与发现
*   **性能优越**：FMP 在保持极短规划时间（约 0.4-0.8s）的同时，成功率显著高于确定性神经规划器。
*   **流匹配的优势**：相比于基于 GMM（高斯混合模型）的 Neural MP，流匹配能更好地捕捉复杂的多模态分布，生成的路径质量更高。
*   **推理优化的必要性**：Best-of-N 采样显著提升了成功率（例如在真实实验中从 33.4% 提升至 86.7%），证明了随机生成策略在处理复杂约束时的有效性。

### 7. 亮点
*   **高效性**：通过轻量级 MLP 头和并行碰撞检测，实现了极快的推理速度。
*   **多模态建模**：成功将流匹配引入运动规划，解决了端到端模型生成路径单一的问题。
*   **端到端闭环**：模型直接处理原始点云，无需人工设计的特征，泛化能力较强。

### 8. 不足与局限
*   **静态环境假设**：目前的模型主要针对静态障碍物，未显式考虑动态障碍物的实时避障。
*   **路径平滑度**：生成的路径有时缺乏局部最优性或平滑度，可能需要额外的后处理（如轨迹平滑）。
*   **泛化极限**：在极度复杂的货架场景中，如果训练数据覆盖不足，模型生成的候选路径多样性会下降，导致 Best-of-N 策略失效。

（完）
