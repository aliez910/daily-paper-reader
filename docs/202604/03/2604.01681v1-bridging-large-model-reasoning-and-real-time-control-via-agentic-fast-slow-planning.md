---
title: Bridging Large-Model Reasoning and Real-Time Control via Agentic Fast-Slow Planning
title_zh: 通过智能体快慢规划连接大模型推理与实时控制
authors: "Jiayi Chen, Shuai Wang, Guangxu Zhu, Chengzhong Xu"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01681v1"
tags: ["query:rob-il"]
score: 8.0
evidence: 通过VLM将语义意图映射到实时控制
tldr: "本研究针对大模型在自动驾驶实时控制中难以兼顾推理深度与响应速度的问题，提出了“智能体快慢规划”（AFSP）分层框架。该框架通过“感知到决策”桥梁将场景压缩为拓扑结构并由云端LLM生成指令，再通过“决策到轨迹”桥梁利用语义引导的A*算法和智能参数微调模块生成可执行路径，最后由MPC实时跟踪。该方法有效解耦了高层语义推理与底层物理控制，在提升系统鲁棒性的同时显著降低了控制偏差。"
selection_source: fresh_fetch
motivation: 现有方法在将大模型语义意图映射到实时控制时，面临轨迹生成脆弱、验证困难及高延迟等挑战。
method: "提出一种解耦感知、推理、规划与控制的分层框架，利用VLM/LLM生成语义指令，并通过语义引导的A*算法和智能细化模块转化为控制轨迹。"
result: "在CARLA实验中，该框架将横向偏差降低了高达45%，任务完成时间缩短了12%以上，表现出更强的鲁棒性。"
conclusion: 该研究成功构建了连接大模型推理与实时控制的桥梁，证明了快慢分层规划在复杂自动驾驶任务中的有效性。
---

## 摘要
大型基础模型为自主系统提供了强大的推理能力，但将语义意图映射到可靠的实时控制仍具挑战。现有方法要么 (i) 让大语言模型 (LLM) 直接生成轨迹——这种方式脆弱、难以验证且易产生延迟；要么 (ii) 在线调整模型预测控制 (MPC) 的目标——这混合了慢速深思熟虑与快速控制，模糊了接口界限。我们提出了智能体快慢规划（Agentic Fast-Slow Planning），这是一个在自然时间尺度上解耦感知、推理、规划和控制的分层框架。该框架包含两个桥梁：Perception2Decision 使用车载视觉语言模型 (VLM) 检测器将场景压缩为以自我为中心的拓扑结构，然后通过云端 LLM 决策器将其映射为符号化驾驶指令，在保持可解释性的同时减少了带宽和延迟；Decision2Trajectory 将指令转换为可执行路径，其中语义引导的 A* 算法将源自语言的软成本嵌入经典搜索中，使解偏向可行轨迹，而智能体细化模块则利用反馈和记忆来调整规划器的超参数。最后，MPC 实时跟踪轨迹，并针对困难情况提供可选的云端引导参考。在 CARLA 中的实验表明，与纯 MPC 和 A* 引导的 MPC 基准相比，智能体快慢规划提高了扰动下的鲁棒性，横向偏差减少了高达 45%，完成时间缩短了 12% 以上。代码可在 https://github.com/cjychenjiayi/icra2026_AFSP 获取。

## Abstract
Large foundation models enable powerful reasoning for autonomous systems, but mapping semantic intent to reliable real-time control remains challenging. Existing approaches either (i) let Large Language Models (LLMs) generate trajectories directly - brittle, hard to verify, and latency-prone - or (ii) adjust Model Predictive Control (MPC) objectives online - mixing slow deliberation with fast control and blurring interfaces. We propose Agentic Fast-Slow Planning, a hierarchical framework that decouples perception, reasoning, planning, and control across natural timescales. The framework contains two bridges. Perception2Decision compresses scenes into ego-centric topologies using an on-vehicle Vision-Language Model (VLM) detector, then maps them to symbolic driving directives in the cloud with an LLM decision maker - reducing bandwidth and delay while preserving interpretability. Decision2Trajectory converts directives into executable paths: Semantic-Guided A* embeds language-derived soft costs into classical search to bias solutions toward feasible trajectories, while an Agentic Refinement Module adapts planner hyperparameters using feedback and memory. Finally, MPC tracks the trajectories in real time, with optional cloud-guided references for difficult cases. Experiments in CARLA show that Agentic Fast-Slow Planning improves robustness under perturbations, reducing lateral deviation by up to 45% and completion time by over 12% compared to pure MPC and an A*-guided MPC baseline. Code is available at https://github.com/cjychenjiayi/icra2026_AFSP.

---

## 论文详细总结（自动生成）

以下是对论文《Bridging Large-Model Reasoning and Real-Time Control via Agentic Fast-Slow Planning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何将大语言模型（LLM）强大的语义推理能力转化为自动驾驶中可靠、实时的物理控制。
*   **研究动机**：
    *   **现有方法缺陷**：直接用LLM生成轨迹（端到端）过于脆弱且难以验证；而通过语言在线调整MPC参数的方法则模糊了“慢速推理”与“快速控制”的界限，导致接口混乱。
    *   **时间尺度不匹配**：感知、推理、规划和控制在物理世界中运行的频率完全不同，缺乏一个能够解耦这些模块并保持语义一致性的分层框架。

### 2. 方法论：智能体快慢规划（AFSP）
该框架受人类认知的“双过程理论”启发，将系统分为三个层级：
*   **Perception2Decision（感知到决策桥梁）**：
    *   **车载端**：使用轻量化视觉语言模型（VLM）作为拓扑检测器，将高维图像压缩为以自我为中心的“拓扑图”（包含物体类别、距离、方位等），极大降低了上传云端的带宽需求。
    *   **云端**：LLM决策器接收拓扑图，通过思维链（CoT）输出符号化的驾驶指令（如“左转”、“保持”）和驾驶风格。
*   **Decision2Trajectory（决策到轨迹桥梁）**：
    *   **语义引导 A* 算法**：将LLM的指令转化为 A* 搜索中的“软成本”（Soft Costs）。如果路径符合指令则奖励，不符合则惩罚。这种设计既能引导搜索，又能在指令错误时依靠几何约束保证安全。
    *   **智能体细化模块（Agentic Refinement）**：利用LLM作为智能体，根据规划器的反馈（如是否发生振荡、是否靠近障碍物）自动微调 A* 的超参数，替代了繁琐的人工调参。
*   **实时控制层**：
    *   **云端引导 MPC**：底层控制器实时跟踪 A* 生成的参考轨迹。在复杂场景下，控制器会优先选择云端优化的全局参考路径。

### 3. 实验设计
*   **实验环境**：CARLA 自动驾驶模拟器（涵盖 Town2-7 和 Town10HD 场景）。
*   **数据集**：构建了一个包含 20 万帧图像的 CARLA 数据集，用于 VLM 的微调和评估。
*   **Benchmark（基准方法）**：
    1.  **纯 MPC**：仅依靠局部避障逻辑。
    2.  **A*-MPC**：经典 A* 引导的 MPC（无语义引导）。
*   **测试场景**：设置了三种地图扰动场景（包括地图平移偏移、障碍物半径膨胀等），以测试系统在感知/地图误差下的鲁棒性。

### 4. 资源与算力
*   **硬件使用**：使用了 **4 块 NVIDIA A100 GPU** 进行 VLM 的微调。
*   **训练细节**：VLM（基于 InternVL-2B）训练了 12 个 epoch，学习率为 $8 \times 10^{-5}$。
*   **推理模型**：云端决策使用了 DeepSeek-V3 接口，VLM 部署使用了 Qwen2-VL 框架。

### 5. 实验数量与充分性
*   **感知实验**：在 1000 帧测试集上对比了三种 VLM 微调策略（仅视觉、全参数、两阶段微调）。
*   **决策实验**：对比了 200 帧场景下 VLM 与 LLM 的决策一致性及延迟。
*   **闭环控制实验**：在 CARLA 中针对 3 种不同扰动程度的场景，每种方法重复运行 **10 次**以减少随机性误差。
*   **充分性评价**：实验设计较为全面，不仅验证了单个模块（VLM、A*）的性能，还通过闭环仿真证明了整个分层框架在应对地图偏移等“长尾”扰动时的优越性。

### 6. 主要结论与发现
*   **性能提升**：相比于纯 MPC，AFSP 将**横向偏差降低了约 45%**，任务**完成时间缩短了 12% 以上**。
*   **鲁棒性增强**：语义引导的 A* 能够纠正因地图偏移导致的路径偏差，使车辆在扰动下仍能保持符合意图的行驶路径。
*   **效率优化**：通过拓扑图压缩，云端推理的延迟显著低于直接处理图像，且 LLM 决策质量与 VLM 相当。
*   **自动化调参**：智能体细化模块能有效找到最优 A* 参数，避免了不同场景下的人工干预。

### 7. 优点
*   **架构解耦**：清晰地分离了慢速语义推理（云端）与快速物理控制（本地），解决了大模型延迟与实时性要求的矛盾。
*   **软约束机制**：语义引导 A* 不将 LLM 指令视为硬性规定，而是作为成本函数，增强了系统对 LLM 幻觉或错误指令的容错能力。
*   **通信友好**：拓扑图序列化的方法为车云协同自动驾驶提供了一种极低带宽的解决方案。

### 8. 不足与局限
*   **云端依赖性**：虽然优化了带宽，但系统仍高度依赖云端连接。若发生网络中断，本地降级策略（如仅靠 MPC）的性能表现未在文中深入探讨。
*   **场景复杂度限制**：实验主要在 CARLA 模拟器中进行，真实世界中极其复杂的动态交通流（如非结构化道路、行人密集区）可能对拓扑图的表达能力提出更高挑战。
*   **实时决策频率**：LLM 推理仍需数秒（约 4s），这意味着系统无法应对瞬息万变的突发状况，仍需依赖底层 MPC 的瞬时反应。

（完）
