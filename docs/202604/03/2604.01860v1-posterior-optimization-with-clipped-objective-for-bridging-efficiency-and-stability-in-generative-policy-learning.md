---
title: Posterior Optimization with Clipped Objective for Bridging Efficiency and Stability in Generative Policy Learning
title_zh: 采用截断目标的后验优化：衔接生成式策略学习中的效率与稳定性
authors: "Yuhui Chen, Haoran Li, Zhennan Jiang, Yuxing Qin, Yuxuan Wan, Weiheng Liu, Dongbin Zhao"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01860v1"
tags: ["query:rob-il"]
score: 8.0
evidence: 通过离线到在线强化学习实现机器人操纵的生成式策略学习
tldr: 针对生成式机器人策略在强化学习微调中存在的稳定性差和采样效率低的问题，本文提出了POCO框架。该框架将策略改进建模为后验推理问题，通过期望最大化过程将奖励加权的隐式后验蒸馏至策略中，无需似然估计。POCO采用离线到在线的范式，利用预训练先验引导探索，并具有模型无关性，能有效微调大型VLA模型。实验证明其在多项仿真和真实任务中表现优异，显著提升了成功率并防止了策略崩溃。
selection_source: fresh_fetch
motivation: 解决生成式策略在强化学习微调过程中面临的训练不稳定、采样效率低下以及容易发生策略崩溃的挑战。
method: 提出一种基于剪切目标的后验优化框架，通过EM算法将奖励加权的后验分布蒸馏到动作块策略中，并结合离线先验进行在线探索。
result: "在7个仿真基准和4个真实世界任务中超越了现有SOTA方法，并在真实任务中达到了96.7%的成功率。"
conclusion: POCO为生成式策略提供了一种高效且稳定的微调方案，能够无缝扩展至大型视觉-语言-动作模型。
---

## 摘要
表达能力强的生成模型通过捕获长时程轨迹上的复杂多模态动作分布，显著推动了机器人操纵领域的发展。然而，由于不稳定性和样本效率低下，通过强化学习（RL）微调这些策略仍然面临挑战。我们提出了采用截断目标的后验优化（POCO），这是一个原则性的强化学习框架，它将策略改进公式化为针对时间动作块（temporal action chunks）定制的后验推理问题。通过期望最大化（EM）过程，POCO 在无需似然估计的情况下，将奖励加权的隐式后验蒸馏到策略中。此外，POCO 采用了一种从离线到在线的范式，将在线探索锚定在预训练先验上，其模型无关的设计使其能够在不修改架构的情况下微调大型视觉-语言-动作（VLA）模型。在 7 个仿真基准测试和 4 个接触丰富的真实世界任务中的评估表明，POCO 有效防止了灾难性的策略崩溃，性能优于现有的 SOTA 基准方法，并在真实世界任务中实现了 96.7% 的成功率。视频可在项目网站 https://cccedric.github.io/poco/ 获取。

## Abstract
Expressive generative models have advanced robotic manipulation by capturing complex, multi-modal action distributions over temporally extended trajectories. However, fine-tuning these policies via RL remains challenging due to instability and sample inefficiency. We introduce Posterior Optimization with Clipped Objective (POCO), a principled RL framework that formulates policy improvement as a posterior inference problem tailored for temporal action chunks. Through an Expectation-Maximization procedure, POCO distills a reward-weighted implicit posterior into the policy without likelihood estimation. Furthermore, POCO adopts an offline-to-online paradigm that anchors online exploration to pre-trained priors, and its model-agnostic design scales to fine-tune large VLA models without architectural modifications. Evaluations across 7 simulation benchmarks and 4 contact-rich real-world tasks demonstrate that POCO prevents catastrophic policy collapse, outperforms SOTA baselines, and achieves a 96.7% success rate on real-world tasks. Videos are available at our project website https://cccedric.github.io/poco/.

---

## 论文详细总结（自动生成）

以下是对论文《Posterior Optimization with Clipped Objective for Bridging Efficiency and Stability in Generative Policy Learning》（POCO）的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：生成式模型（如扩散模型、流匹配模型及大型 VLA 模型）在处理机器人多模态动作分布方面表现出色，但通过强化学习（RL）对其进行微调面临“稳定性-效率”两难困境。
    *   **离策（Off-policy）方法**：采样效率高，但直接回传噪声较大的 Q 梯度会导致策略在离群（OOD）状态下崩溃，破坏预训练先验。
    *   **在策（On-policy）方法**：通过信任区域保证稳定，但在连续控制任务中采样效率极低，难以应用于现实世界。
*   **核心目标**：提出一种既能利用离线数据保证效率，又能通过后验推理机制确保训练稳定性的强化学习框架，特别针对“时间动作块”（Action Chunking）进行优化。

### 2. 方法论
*   **核心思想**：将策略改进视为一个**概率后验推理问题**。当前策略作为先验，Q 值作为证据，通过期望最大化（EM）过程迭代蒸馏出更好的动作分布。
*   **关键技术细节**：
    *   **动作块后验（Posterior over Action Chunks）**：不同于单步推理，POCO 在动作序列（Chunk）层面构建后验，利用块级 Q 函数评估整个序列的期望回报，缓解长程信用分配问题。
    *   **隐式 E 步（Implicit E-step）**：无需显式似然估计。从当前策略采样 $N$ 个候选动作块，根据其 Q 值计算重要性权重 $w_j = \exp(Q/\eta)$。
    *   **带截断目标的 M 步（M-step with Clipped Objective）**：为了支持似然无关（Likelihood-free）的模型（如流匹配），将 KL 散度约束转化为加权监督损失。引入**截断机制（Clipped Objective）**，限制单次更新的几何变形幅度，防止策略流形崩溃。
    *   **离线到在线范式**：先进行行为克隆（BC）预训练，在线微调初期冻结策略并进行 **Critic 预热**，以获得稳定的价值估计。

### 3. 实验设计
*   **场景与数据集**：
    *   **仿真基准**：包含 7 个任务。来自 OGBench（scene, puzzle-3x3, cube-double/triple）和 RoboMimic（lift, can, square）。
    *   **真实世界**：4 个接触密集型任务（Pick Cube, Route Cable, Insert USB, Assemble SSD）以及 2 个 VLA 扩展任务。
*   **对比方法（Benchmark）**：
    *   **离策/高效类**：RLPD, QC, FQL。
    *   **在策/稳定类**：DPPO, ReinFlow。
    *   **其他**：DSRL（噪声引导）、BC/SFT（纯监督学习）。

### 4. 资源与算力
*   **算力设备**：使用了高性能云服务器，配备 **8 台 NVIDIA H20 GPU** 处理计算密集型的策略训练与推理。
*   **系统架构**：采用分布式架构，云端负责模型更新，本地计算机负责机器人硬件接口与数据流传输。
*   **控制频率**：硬件执行频率为 15Hz。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **仿真实验**：每个任务进行 5 个随机种子的重复实验，每步评估 50 次。
    *   **真实世界**：每个任务进行 30 次独立测试。
    *   **消融实验**：针对截断阈值 $\zeta$ 和后验引导比例 $\beta$ 进行了详细的参数敏感性分析。
*   **充分性评价**：实验覆盖了从简单抓取到复杂装配（如 SSD 插入）、从低维 MLP 到大规模 VLA 模型（$\pi_{0.5}$, GR00T N1.6），对比基准涵盖了近年主流的生成式 RL 算法，实验设计客观且具有说服力。

### 6. 主要结论与发现
*   **性能卓越**：POCO 在所有仿真任务中均达到或超越了 SOTA 方法的采样效率和最终成功率。
*   **稳定性强**：在离线到在线的切换过程中，POCO 有效避免了其他方法常见的“性能骤降”现象，成功保留了预训练先验。
*   **真实世界表现**：在 4 个极具挑战性的真实任务中实现了 **96.7% 的平均成功率**，尤其在 Assemble SSD 任务中，将成功率从 BC 的 26.7% 提升至 96.7%。
*   **可扩展性**：证明了该方法可以无缝应用于微调数十亿参数的 VLA 模型，显著提升其在特定下游任务中的表现。

### 7. 优点
*   **似然无关性**：不需要计算复杂的生成模型似然值，使其能兼容流匹配（Flow Matching）等先进架构。
*   **截断机制创新**：通过简单的损失截断解决了生成式策略在 RL 微调中容易发生的“流形崩溃”问题。
*   **动作块优化**：针对机器人控制的动作块特性进行了理论推导，保证了动作的平滑性和时序一致性。

### 8. 不足与局限
*   **奖励依赖**：真实世界实验仍依赖人工通过按键提供稀疏奖励（Sparse Reward），自动化程度受限。
*   **探索策略**：目前主要依赖随机采样进行探索，对于极度复杂的长程任务，可能需要更结构化的探索机制。
*   **预热需求**：算法需要一个 Critic 预热阶段，这在一定程度上增加了训练流程的复杂性。

（完）
