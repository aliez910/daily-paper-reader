---
title: "AnchorVLA: Anchored Diffusion for Efficient End-to-End Mobile Manipulation"
title_zh: AnchorVLA：用于高效端到端移动操作的锚定扩散模型
authors: "Jia Syuen Lim, Zhizhen Zhang, Peter Bohm, Brendan Tidd, Zi Huang, Yadan Luo"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01567v1"
tags: ["query:rob-il"]
score: 10.0
evidence: 用于高效端到端移动操纵的扩散VLA策略
tldr: 针对移动操作中扩散策略推理慢和动作分块导致的漂移问题，本文提出 AnchorVLA。它结合了轻量级 VLA 主干和锚定扩散动作头，通过在锚定轨迹附近进行局部去噪来减少推理开销，并引入残差修正模块实现高频自纠偏。该方法在保持多模态动作生成的同时，显著提升了闭环控制的效率和鲁棒性。
selection_source: fresh_fetch
motivation: 传统的扩散策略在移动操作中面临推理延迟高和动作分块导致的累积漂移挑战。
method: 提出一种锚定扩散机制，在锚定轨迹附近进行截断去噪，并结合轻量级残差模块进行实时动作修正。
result: 在多种移动操作任务中，AnchorVLA 在面对干扰和分布偏移时表现出更高的成功率、稳定性和更低的推理延迟。
conclusion: 通过局部去噪和实时纠偏，AnchorVLA 有效平衡了多模态动作建模与高效闭环控制的需求。
---

## 摘要
移动操作中的一个核心挑战是在执行过程中保持反应性的同时，保留多种合理的动作模型。在杂乱场景中，抓取瓶子通常有多种有效方式。鲁棒的行为取决于在场景演变时保留这种动作多样性并保持反应性。扩散策略（Diffusion policies）因其能够建模多模态动作分布而非坍缩至单一解而备受关注。但在实践中，完整的迭代去噪在控制阶段开销巨大。动作分块（Action chunking）虽有助于分摊推理成本，但也会导致部分开环行为，使得微小误差累积成漂移。我们提出了 AnchorVLA，这是一种基于扩散的移动操作 VLA 策略。其核心见解是：当采样从合理候选解流形附近开始时，无需进行深度去噪即可恢复多模态的有效动作。AnchorVLA 将轻量级 VLA 适配主干网络与锚定扩散动作头相结合，利用截断扩散调度在锚定轨迹周围进行局部去噪。这在保留多模态动作生成能力的同时，降低了闭环控制的推理成本。关键在于，为缓解分块诱发的漂移，我们通过轻量级残差修正模块引入了测试时自校正机制，在执行过程中进行高频的逐步调整。在多种移动操作任务中，AnchorVLA 在保持低延迟推理的同时，显著提升了在扰动和分布偏移下的成功率与稳定性。源代码已开源至 https://github.com/jason-lim26/AnchorVLA。

## Abstract
A central challenge in mobile manipulation is preserving multiple plausible action models while remaining reactive during execution. A bottle in a cluttered scene can often be approached and grasped in multiple valid ways. Robust behavior depends on preserving this action diversity while remaining reactive as the scene evolves. Diffusion policies are appealing because they model multimodal action distributions rather than collapsing to one solution. But in practice, full iterative denoising is costly at control time. Action chunking helps amortize inference, yet it also creates partially open-loop behavior, allowing small mismatches to accumulate into drift. We present AnchorVLA, a diffusion-based VLA policy for mobile manipulation built on the core insight that when sampling begins near a plausible solution manifold, extensive denoising is unnecessary to recover multimodal, valid actions. AnchorVLA combines a lightweight VLA adaptation backbone with an anchored diffusion action head, which denoises locally around anchor trajectories using a truncated diffusion schedule. This retains multimodal action generation while reducing inference cost for closed-loop control. Crucially, to mitigate chunking-induced drift, we introduce a test-time self-correction mechanism via a lightweight residual correction module that makes high-frequency, per-step adjustments during rollout. Across diverse mobile manipulation tasks, AnchorVLA improves success and stability under disturbances and distribution shifts while maintaining low-latency inference. The source code is made available at https://github.com/jason-lim26/AnchorVLA.

---

## 论文详细总结（自动生成）

### AnchorVLA：用于高效端到端移动操作的锚定扩散模型 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
移动操作（Mobile Manipulation）要求机器人同时协调移动底座和机械臂，在非结构化环境中完成任务。其核心挑战在于：
*   **多模态性（Multimodality）：** 同一指令（如“抓取苹果”）存在多种有效的执行路径（不同的接近角度、底座姿态等）。传统的 L1 回归模型容易产生“众数平均”效应，导致生成的动作在物理上不可行。
*   **推理延迟与漂移：** 扩散策略（Diffusion Policy）虽能建模多模态，但全迭代去噪推理慢。采用动作分块（Action Chunking）虽能提速，但会导致部分开环行为，使微小的执行误差累积成严重的轨迹漂移。
*   **计算开销：** 在移动设备上运行大型视觉-语言-动作（VLA）模型面临严峻的算力约束。

#### 2. 方法论：核心思想与关键技术
AnchorVLA 提出了一种结合“锚定扩散”与“实时修正”的架构：
*   **锚定轨迹词典（Trajectory Vocabulary）：** 通过对专家演示数据进行 K-Means 聚类，提取出 $M$ 个代表性的动作轨迹作为“锚点（Anchors）”。
*   **锚定截断扩散（Anchored Truncated Diffusion）：** 
    *   不同于从纯高斯噪声开始去噪，模型从预定义的锚点附近开始采样。
    *   采用截断调度（Truncated Schedule），仅需极少数去噪步骤（如 5-10 步）即可生成高质量动作，显著降低推理延迟。
*   **锚定评分头（Anchor Scoring Head）：** 扩散头同时对多个锚点进行局部去噪，评分头根据当前环境上下文 $x_t$ 为这些候选轨迹打分，选择最匹配的一条执行。
*   **残差修正模块（Residual Correction Module）：** 这是一个轻量级的 MLP，在动作块执行期间，根据每一帧的实时观测对预定义的宏观动作进行微调（Micro-adjustments），以补偿开环执行带来的漂移。

#### 3. 实验设计
*   **实验场景：**
    *   **仿真：** 使用 ManiSkill-HAB 基准测试，包含 6 类任务（苹果/碗的拾取与放置、冰箱开门、抽屉关闭）。
    *   **现实世界：** 部署在 Unitree Go2 四足机器人（配备 SO101 机械臂）上，测试“开抽屉”和“提包”任务。四足机器人的震动为实时修正模块提供了极佳的压力测试环境。
*   **对比基准（Benchmarks）：**
    *   **基础模型：** ACT (Action Chunking with Transformers), DP (Diffusion Policy)。
    *   **3D/大模型：** DP3 (3D Diffusion Policy), $\pi_0$, RDT (Robotics Diffusion Transformer)。
    *   **SOTA 移动操作模型：** AC-DiT (Adaptive Coordination Diffusion Transformer)。

#### 4. 资源与算力
*   **模型规格：** 使用 Qwen-2.5 (0.5B) 作为 VLA 主干网络。
*   **训练细节：** 采用 LoRA 微调（Rank=64），学习率为 $2 \times 10^{-4}$，Batch Size 为 16，训练步数为 100k。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号和数量，但提到通过动作分块（H=5）相比逐帧查询（H=1）可减少约 80% 的计算量（从 641.4 TFLOPs 降至 128.3 TFLOPs）。

#### 5. 实验数量与充分性
*   **实验规模：** 仿真实验中每个任务进行 3 次独立试验，每次试验包含 100 个未见过的场景（共 1800+ 次仿真测试）；现实世界中每个任务进行 10 次试验。
*   **消融实验：** 详细探讨了锚定先验的影响（不同去噪步数下的表现）、残差修正模块的增益、以及动作分块长度（H）对性能与计算效率的权衡。
*   **评价：** 实验设计涵盖了从仿真到现实的迁移，对比了当前最先进的多种基线模型，消融实验逻辑严密，能够客观证明各组件的有效性。

#### 6. 主要结论与发现
*   **性能领先：** AnchorVLA 在 ManiSkill-HAB 上达到 64.0% 的平均成功率，优于所有基线模型（包括使用点云输入的模型）。
*   **高效性：** 借助锚定先验，仅需 5 步去噪即可达到传统扩散模型 50 步的效果，推理频率提升至 89.8 Hz。
*   **鲁棒性：** 残差修正模块将任务成功率提升了约 9.3%，有效解决了长动作块执行时的漂移问题。
*   **计算节省：** 在保持高性能的同时，通过增加动作块长度，可大幅降低对大模型主干的查询频率。

#### 7. 优点与亮点
*   **创新性结合：** 巧妙地将扩散模型的表达能力与锚定机制的效率结合，解决了 VLA 模型在移动端部署的实时性难题。
*   **双层控制架构：** 宏观规划（扩散头）与微观修正（残差模块）的分离，既保证了多模态建模，又增强了闭环控制的鲁棒性。
*   **现实部署价值：** 在四足机器人上的成功部署证明了该方法处理复杂动态（如底座震动）的能力。

#### 8. 不足与局限
*   **数据依赖：** 锚点词典依赖于离线专家演示的聚类，如果测试场景与训练数据分布差异极大（OOD），锚点可能失效。
*   **缺乏在线学习：** 目前主要基于模仿学习，模型不具备在执行失败后通过强化学习进行在线自我恢复或持续进化的能力。
*   **硬件限制：** 现实实验的任务复杂度受限于机械臂的物理触达范围和四足底座的运动学约束。

（完）
