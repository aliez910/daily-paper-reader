---
title: Boosting Vision-Language-Action Finetuning with Feasible Action Neighborhood Prior
title_zh: 利用可行动作邻域先验提升视觉-语言-动作模型微调
authors: "Haochen Niu, Kanyu Zhang, Shuyu Yin, Qinghai Guo, Peilin Liu, Fei Wen"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01570v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 利用可行动作邻域先验提升机器人操纵的VLA微调
tldr: 针对机器人操作中存在“可行动作邻域（FAN）”而非单一正确动作的特性，本文提出了一种FAN引导的正则化方法。该方法通过引入高斯先验，使模型输出分布在动作方向和幅度上保持局部平滑与单峰性，从而克服了传统VLA训练忽略动作容差的问题。实验证明，该方法在强化微调和监督微调中均显著提升了样本效率及在分布外场景的泛化能力。
selection_source: fresh_fetch
motivation: 现有的VLA训练直接沿用语言模型范式，忽略了机器人操作中多个相近动作均可导致相同结果的“可行动作邻域”特性，导致泛化性差。
method: 引入一种FAN引导的正则化项，利用高斯先验约束模型输出分布，使其在首选动作周围呈现局部平滑和单峰特性。
result: 在多种微调场景下，该方法显著提高了模型的样本利用率，并增强了在分布内及分布外任务中的操作成功率。
conclusion: 通过对齐物理操作中的动作容差，FAN引导的正则化为实现高效且泛化性强的VLA模型适配提供了一种原则性且实用的方法。
---

## 摘要
在现实世界的机器人操作中，状态通常允许一个近乎等效动作的邻域。也就是说，对于每个状态，存在一个可行动作邻域（FAN）而非单一的正确动作，在该邻域内的运动所产生的进展是难以区分的。然而，现有的 VLA 训练方法直接继承自语言设置，未能利用 FAN 特性，从而导致泛化能力差和样本效率低。为解决这一局限性，我们引入了一种 FAN 引导的正则化器，通过塑造模型的输出分布使其与 FAN 的几何结构对齐。具体而言，我们引入了一个高斯先验，旨在促进围绕首选方向和幅度的局部平滑且单峰的预测。在强化微调（RFT）和监督微调（SFT）的大量实验中，我们的方法在样本效率以及分布内和分布外（OOD）场景的成功率方面均取得了显著提升。通过与物理操作固有的动作容差相对齐，FAN 引导的正则化为实现高样本效率且具泛化性的 VLA 适配提供了一种有原则且实用的方法。

## Abstract
In real-world robotic manipulation, states typically admit a neighborhood of near-equivalent actions. That is, for each state, there exist a feasible action neighborhood (FAN) rather than a single correct action, within which motions yield indistinguishable progress. However, prevalent VLA training methodologies are directly inherited from linguistic settings and do not exploit the FAN property, thus leading to poor generalization and low sample efficiency. To address this limitation, we introduce a FAN-guided regularizer that shapes the model's output distribution to align with the geometry of FAN. Concretely, we introduce a Gaussian prior that promotes locally smooth and unimodal predictions around the preferred direction and magnitude. In extensive experiments across both reinforced finetuning (RFT) and supervised finetuning (SFT), our method achieves significant improvement in sample efficiency, and success rate in both in-distribution and out-of-distribution (OOD) scenarios. By aligning with the intrinsic action tolerance of physical manipulation, FAN-guided regularization provides a principled and practical method for sample-efficient, and generalizable VLA adaptation.

---

## 论文详细总结（自动生成）

以下是对论文《Boosting Vision-Language-Action Finetuning with Feasible Action Neighborhood Prior》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型微调方法（如 SFT 和 RFT）直接沿用了语言模型的训练范式（如交叉熵损失或 PPO），将动作视为离散的 Token。
*   **研究动机**：在物理机器人操作中，动作具有**固有容差（Tolerance）**。对于某一状态，通常存在一个“可行动作邻域（Feasible Action Neighborhood, FAN）”，即在该邻域内的多个相近动作都能产生几乎相同的任务进展。
*   **现状局限**：传统方法追求“绝对正确”，导致模型概率分布过于尖锐（Spiky），容易在小样本微调时产生过拟合，且在强化学习微调中样本效率极低，缺乏对分布外（OOD）场景的泛化能力。

### 2. 方法论
*   **核心思想**：引入一种 **FAN 引导的正则化器**，通过塑造模型的输出分布，使其符合物理动作空间的几何特性（局部平滑、单峰性、连续性）。
*   **关键技术细节**：
    *   **FAN 定义**：基于 Q 函数定义，即与最优动作 $a^*$ 相比，Q 值损失在阈值 $\delta$ 以内的连通动作集合。
    *   **正则化项**：计算模型当前策略 $\pi$ 与目标高斯分布 $\mathcal{N}(\mu, \Sigma)$ 之间的 KL 散度。其中均值 $\mu$ 为模型预测的最优动作。
    *   **针对 SFT 的适配 (FAN-SFT)**：使用**动态协方差** $\Sigma$（由策略自身的方差决定），在保证监督信号稳定的同时，鼓励分布向高斯形状靠拢。
    *   **针对 RFT 的适配 (FAN-PPO)**：在 PPO 的目标函数中加入该正则项，并使用**固定协方差**以维持训练稳定性。
*   **理论支撑**：论文证明了在该正则化下，更新后的策略是原策略、目标高斯分布与指数化 Q 值的几何插值，实现了“加权容差”而非“排他正确”。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **ManiSkill**：使用 `PutOnPlate` 任务，包含 25 种多样的拾取与放置原语。
    *   **LIBERO**：涵盖 `LIBERO-Spatial`、`Object`、`Goal`、`Long` 四个子集。
    *   **真实世界实验**：使用 7 自由度 JAKA 机械臂进行物体放置任务。
*   **Benchmark 与对比方法**：
    *   **基准模型**：OpenVLA（单步动作预测）和 OpenVLA-OFT（动作块预测）。
    *   **对比算法**：标准 SFT、标准 PPO（RL4VLA）、标签平滑（Label Smoothing）以及熵最大化（Entropy Maximization）。
*   **评估维度**：分布内（In-Distribution）成功率、15 种分布外（OOD）扰动（视觉、语义、执行层面的扰动）、样本效率（收敛速度）。

### 4. 资源与算力
*   **硬件设备**：使用了 NVIDIA **A100 (80GB)** GPU。
*   **算力分配**：
    *   **SFT 实验**：根据模型不同，使用了 2 到 4 块 A100。
    *   **RFT 实验**：利用 ManiSkill 的 GPU 并行仿真特性，使用 1 块 A100 进行强化学习训练。
*   **训练规模**：SFT 训练步数在 20K-60K 之间；RFT 训练约 390-650 个 Episode。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了两种主流 VLA 架构（OpenVLA 和 OpenVLA-OFT）。
    *   在 ManiSkill 上进行了 15 种不同类型的 OOD 鲁棒性测试。
    *   进行了不同数据规模（1.6K 到 16K）下的消融实验。
    *   提供了详细的超参数敏感性分析（针对正则化系数 $\alpha$ 和高斯标准差 $\sigma$）。
*   **充分性评价**：实验设计非常充分且客观。通过仿真环境的大规模测试与真实机器人的交叉验证，证明了方法在不同范式（SFT/RFT）下的普适性。

### 6. 主要结论与发现
*   **性能提升**：在 SFT 中，FAN 正则化使 OOD 成功率平均提升了 5.2%；在 RFT 中，OOD 成功率提升超过 6%，部分任务提升达 10% 以上。
*   **样本效率**：在 RFT 过程中，达到相同成功率所需的训练步数仅为基准方法的 **1/3**，显著加速了收敛。
*   **泛化性**：模型不再死记硬背训练集中的精确坐标，而是学会了在目标位置周围的合理区域内进行操作，对空间偏移和视觉噪声表现出极强的韧性。

### 7. 优点
*   **原理简洁且通用**：无需修改模型架构或自回归解码方案，仅通过损失函数改造即可实现，易于集成到现有 VLA 框架中。
*   **物理对齐**：深刻洞察了语言 Token 与物理动作之间的本质区别，将“动作容差”这一物理特性引入了训练目标。
*   **多范式适用**：同时解决了 SFT 的过拟合问题和 RFT 的样本效率低下问题。

### 8. 不足与局限
*   **超参数依赖**：正则化系数 $\alpha$ 和目标分布的标准差 $\sigma$ 对结果有一定影响，过大的 $\alpha$ 可能导致训练崩溃。
*   **高斯先验的局限性**：虽然高斯分布能很好地模拟局部平滑性，但对于某些具有强多峰特性的复杂动作空间（例如需要绕过障碍物的路径选择），简单的单峰高斯先验可能不够精确。
*   **应用限制**：目前主要在拾取、放置等相对基础的操作任务上验证，对于更长程、涉及复杂接触动力学的任务（如拧螺丝、折叠衣物）尚需进一步验证。

（完）
