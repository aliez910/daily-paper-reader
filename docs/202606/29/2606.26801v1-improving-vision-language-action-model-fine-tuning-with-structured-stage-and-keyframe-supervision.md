---
title: Improving Vision-Language-Action Model Fine-Tuning with Structured Stage and Keyframe Supervision
title_zh: 通过结构化阶段和关键帧监督改进视觉-语言-动作模型微调
authors: "Yuan Xu, Yixiang Chen, Kai Wang, Jiabing Yang, Peiyan Li, Qisen Ma, Yan Huang, Liang Wang"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26801v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 通过结构阶段和关键帧监督改进VLA模型在模仿学习中的微调
tldr: "VLA模型在长时程操纵微调中对所有时间步均等监督，忽视操纵阶段和关键帧转换的结构化学习，导致失败集中在夹具事件处。StaKe框架自动从演示夹具状态中提取阶段分类和关键帧预测辅助信号，添加轻量网络头在训练中增强表征，不改变基础架构和推理流程。在双臂仿真和单臂Franka真实任务中成功率分别相对提升14%和56%，尤其长时程多阶段任务改善更显著。消融和特征可视化验证了组件设计，表明结构化监督即插即用、无需人工标注，是提升VLA长时程操纵微调性能的通用有效策略。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26801v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 830, \"height\": 347, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26801v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 873, \"height\": 389, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26801v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1293, \"height\": 616, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26801v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1734, \"height\": 886, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26801v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1752, \"height\": 792, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26801v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1113, \"height\": 528, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26801v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 895, \"height\": 345, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26801v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 590, \"height\": 259, \"label\": \"Table\"}]"
motivation: 现有VLA模型微调对所有时间步均等动作监督，缺乏对操纵阶段和关键帧转换的结构化学习，导致失败集中于挑战性的夹具事件附近，限制了长时程任务性能。
method: 提出StaKe框架，自动从演示夹具状态中提取阶段分类和关键帧预测两个辅助监督信号，通过添加轻量训练头在微调中增强相关表征，不改变原策略架构和推理流程。
result: "双臂仿真和单臂真实机器人任务成功率分别相对提升14%和56%，长时程任务改善更显著，消融验证有效。"
conclusion: 结构化监督是提升VLA模型长时程操纵微调性能的有效通用策略，且即插即用、无需额外标注。
---

## 摘要
视觉-语言-动作（VLA）模型在可泛化的机器人操作方面展现出强大潜力。然而，在微调过程中，动作监督平等地应用于所有时间步，缺乏关于机器人当前处于哪个操作阶段或下一个夹爪事件目标应该是什么的结构化监督。这导致失败集中在具有挑战性的夹爪事件转变周围。为了解决这个问题，我们提出了StaKe，一个插件式辅助监督框架，它自动从演示的夹爪状态中推导出两个互补的信号，无需手动标注：一个阶段分类器，用于识别当前的操作阶段；以及一个关键帧预测器，用于估计下一个夹爪转变处的目标关节动作。两者都建模为轻量级的辅助头，在训练期间丰富学习到的表示，同时保持基础VLA策略架构和推理循环不变。在双手模拟和单臂Franka真实机器人任务上的实验表明，StaKe持续提高了成功率（分别相对提升14%和56%），在涉及更多夹爪事件转变的更长周期任务上改进更大。消融研究验证了每个设计选择，定性分析确认了学习到的表示忠实地跟踪了操作阶段。这些结果表明，结构化监督是一种有效且通用的策略，用于在长周期操作中增强VLA微调。项目网站：https://hi-yuanxu.github.io/StaKe-Web/

## Abstract
Vision-Language-Action (VLA) models have shown strong potential for generalizable robotic manipulation. During fine-tuning, however, action supervision applies equally across all timesteps, without structured supervision on which manipulation stage the robot is in or what the next gripper-event target should be. This causes failures to concentrate around challenging gripper-event transitions. To address this, we propose StaKe, a plug-in auxiliary supervision framework that automatically derives two complementary signals from demonstration gripper states without manual annotation: a stage classifier that identifies the current manipulation stage, and a keyframe predictor that estimates the target joint action at the next gripper transition. Both are modeled as lightweight auxiliary heads that enrich the learned representations during training, while leaving the base VLA policy architecture and inference loop unchanged. Experiments on bimanual simulation and single-arm Franka real-robot tasks show that StaKe consistently improves success rates (relative gains of 14% and 56%, respectively), with larger improvements on longer-horizon tasks that involve more gripper-event transitions. Ablation studies validate each design choice, and qualitative analysis confirms that the learned representations faithfully track manipulation stages. These results indicate that structured supervision is an effective and general strategy for enhancing VLA fine-tuning in long-horizon manipulation. Project website: https://hi-yuanxu.github.io/StaKe-Web/

---

## 论文详细总结（自动生成）

# 论文结构化作总结：Improving Vision-Language-Action Model Fine-Tuning with Structured Stage and Keyframe Supervision

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：现有的 VLA（Vision-Language-Action）模型在微调过程中对所有时间步使用均等的动作损失（连续动作损失），缺乏对操作阶段（如自由运动 vs. 接触技能）和关键夹爪事件（grasp / release）的结构化监督。这种处理方式导致失败集中发生在夹爪状态变化的阶段边界附近（如图 1 所示，分为 motion-fail 和 skill-fail）。
- **背景**：VLA 模型（如 π0、π0.5）在机器人操作中展现出很强的泛化能力，但在长时程任务中，均匀的损失函数无法有效引导模型关注关键过渡时刻。现有方法虽然利用了关键帧或技能结构，但通常需要手动标注或修改架构。
- **研究意义**：提出一种即插即用、无需人工标注的结构化辅助监督方法，通过自动提取的阶段标签和关键帧目标信号，提升 VLA 模型在长时程操作微调中的表现，同时保持原有推理流程不变。

## 2. 提出的方法论：核心思想与关键技术细节
- **核心思想**：在 VLA 微调中添加两个辅助监督信号（阶段分类和关键帧预测），两者均从演示的夹爪状态自动推导，以轻量级辅助头形式在训练过程中发挥作用，不改变基础架构和推理流程。
- **关键技术细节**：
  - **阶段标签提取（Stage Label Extraction）**：
    - 基于夹爪开/闭状态识别封闭区间（任一夹爪闭合即为 skill 阶段），并对区间前后扩展 K 帧（默认 K=25），其余帧标记为 motion 阶段。
    - 使用二值交叉熵损失训练阶段分类头（MLP）。
  - **关键帧目标提取（Keyframe Target Extraction）**：
    - 检测所有夹爪状态切换帧（单/双臂的开关事件合并排序），记录此时关节配置作为关键帧；并补充终点帧。
    - 为每个时间步分配最近的下一个夹爪切换帧的关节配置作为目标（用于 L1 损失训练关键帧预测头）。
  - **模型架构**：
    - 基于 π0.5 架构，在 VLM 骨干输入序列中添加两个可学习查询 token（stage & keyframe），经骨干上下文编码后分别送入轻量 MLP 头。
    - 总损失：L_total = L_policy + λ_s L_stage + λ_k L_kf（λ_s = λ_k = 0.01）。
    - 推理时仅使用动作专家（flow matching 去噪），辅助头不参与。
- **算法流程**（文字说明）：
  1. 对每条演示轨迹，算法 1 根据夹爪闭合区间（扩展 K 帧）输出运动/技能阶段标签 s_t。
  2. 算法 2 扫描夹爪状态变化帧，生成所有关键帧（含终点帧），然后为每个时间步 t 指定最近未来关键帧的关节配置 q_kf_t。
  3. 训练时与标准 policy loss 联合优化；测试时仅使用原动作生成流程。

## 3. 实验设计
- **仿真场景**：RoboTwin 2.0 基准（基于 SAPIEN 模拟器），包含 10 个双手操作任务，按关键帧数分为 2KF（6 个）、3KF（1 个）、4KF（2 个）、5KF（1 个）。每项任务使用 50 条演示，语言指令从任务专用指令池随机采样。
- **真实机器人场景**：Franka Research 3 单臂 + 平行夹爪，ZED 2i 前视 RGB 相机。4 个桌面任务（Mug on Plate, Stack Bowls, Bowl in Drawer, Two Mugs in Basket），关键帧数 2~4。每项任务 50 条遥操作演示，多任务联合训练，每任务测试 10 次（共 40 次评估）。
- **对比方法**：
  - 主要基线：π0.5（相同架构、训练设置，无辅助损失）。
  - 外部基准：Diffusion Policy (DP)、ACT、RDT-1B（结果引用自官方 RoboTwin 2.0 基准）。
- **训练设置**：全模型后训练（VLM 主干 + 动作专家），4×NVIDIA H20 GPU，batch size 64，学习率 5e-5，20,000 步，动作块长度 H=50。

## 4. 资源与算力
- **明确提及的算力**：使用 4 块 NVIDIA H20 GPU（型号明确），训练 20,000 步，batch size 64，学习率 5e-5。
- **未说明信息**：单次训练总时长（小时/天）未给出；也未报告推理时的计算开销（虽然声称计算开销可忽略，但无具体数值）；真实机器人数据采集成本未量化。

## 5. 实验数量与充分性
- **实验数量**：
  - 仿真：10 个任务各 100 episode（共 1000 次评估），覆盖不同关键帧复杂度。
  - 真实机器人：4 个任务各 10 次（共 40 次），并报告每个关键帧的累积成功率。
  - 消融：在 4 个代表性任务（Stack Blocks, Place Burger, Lift Pot, Click Alarmclock）上进行，包括移除每个辅助损失、替换关键帧目标为固定 t+H、替换查询 token 为最后一个 VL 前缀 token、不同 expansion margin K（0/15/20/25/30）。
  - 定性分析：3 个任务（2KF, 4KF, 5KF）的 per-action-chunk stage/keyframe 预测可视化。
- **充分性与公平性**：
  - 对比基线全面：包括当前主流方法 DP、ACT、RDT-1B，以及原始 π0.5（同一训练配方）。
  - 消融实验系统：验证了每个组件的必要性，且替代方案（固定目标、无专用 token）性能下降，证明设计合理。
  - 超参数消融（K 值）显示在 K=20~30 范围内稳定，选择合理。
  - 真实实验虽然在每任务 10 次上执行，但结合累积关键帧成功率和跨 4 个任务的一致性，仍具说服力。
  - 不足：消融仅在 4 个仿真任务上，未在更多任务或真实场景上验证；真实实验 trial 数较少（10 次），可能存在随机波动；未提供置信区间或统计假设检验。

## 6. 主要结论与发现
- **成功率提升**：StaKe 在仿真 10 个任务上平均成功率 59.8%，优于 π0.5（52.4%）、RDT-1B（49.7%）、DP（42.9%）、ACT（42.7%），在 7/10 任务上达到最佳或并列最佳。
- **长时程任务增益更显著**：在 4KF 和 5KF 任务上，相对 π0.5 提升 8~12 个百分点（如 Handover Block 55→63%，Place Burger 64→76%，Stack Blocks Two 42→46%）。
- **组件互补**：单独移除阶段监督或关键帧监督均导致性能下降，两者共同作用效果最好。
- **事件驱动关键帧优于固定视野**：使用下一个夹爪切换事件作为目标效果显著优于使用 t+H 固定目标。
- **专用查询 token 优势**：为辅助头分配可学习 token 优于复用最后一个 VL 前缀 token。
- **预测对齐**：定性分析显示阶段分类灵活切换（甚至在夹爪闭合前已预测 skill），关键帧误差始终较低。
- **真实机器人一致性**：在所有 4 个任务上最终成功率均高于 π0.5（平均 62.5% vs. 40.0%），后期关键帧成功率差异更明显。

## 7. 优点
- **无需人工标注**：阶段标签和关键帧目标完全从演示夹爪状态自动提取，可扩展至任意包含夹爪事件的抓取放置任务。
- **插件式、非侵入**：辅助头仅训练时激活，不改变 VLA 主干和推理流程，易于与未来架构或训练策略结合。
- **轻量高效**：仅增加两个可学习 token 和两个 MLP 头，计算开销可忽略（如作者所述）。
- **泛化性验证充分**：同时覆盖仿真双臂和真实单臂场景，任务从简单到复杂（2~5 KF），并且证明长时程任务收益更大。
- **详尽的消融和可视化**：从组件必要性、目标设计、token 形式到超参数敏感性，全面验证了方法设计；定性分析揭示了预测行为的内在解释性。

## 8. 不足与局限
- **适用场景有限**：阶段和关键帧定义依赖二进制夹爪开/闭事件，因此方法自然针对抓取放置类操作，不直接扩展到非抓取操作（如推、旋转）或灵巧手操作。
- **仿真到真实差距未深入讨论**：虽然做了真实实验，但未详细分析从仿真迁移到真实的困难或领域随机化策略。
- **实验统计严谨性不足**：真实实验每任务仅 10 次测试，无置信区间或多次重复实验；仿真虽 100 次，但未报告标准差或显著性检验。
- **消融范围可扩展**：消融仅在 4 个仿真任务上，未在更多任务或真实场景中验证；超参数 K 的消融也只在 2 个任务上。
- **训练资源细节不完整**：虽然给出了 GPU 数量和步数，但未提供训练总时长，难以直接复现或估算成本。
- **未探索推理时利用辅助表示**：当前辅助头仅用于训练，未用于测试时增强决策（如作为先验或检查点）；未来工作可能探索。
- **依赖夹爪事件的质量**：如果演示中夹爪状态噪声大或属于非抓取操作，自动提取可能不准确。

（完）
