---
title: "Decoupling Semantics and Geometric Grounding: Spatial Visual Prompts for Language-Conditioned Imitation Learning"
title_zh: 解耦语义与几何基础：面向语言条件模仿学习的空间视觉提示
authors: "Yanzhe Tang, Xinyu Shao, Yuxuan Hu, Siyu Chen, Bowen Yang, Yajun Gao, Tongtong Cao, Xiu Li, Long Zeng"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25360v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 利用空间视觉提示的解耦架构用于语言条件模仿学习
tldr: 端到端视觉语言动作模型在机器人操作中因耦合语义与空间控制，导致数据有限时模仿学习的目标辨别困难。本文提出解耦的空间视觉提示模仿学习架构，利用视觉语言基础模型解析指令生成零样本几何掩码作为空间视觉提示，并通过轻量直接特征融合注入动作生成器。这种方法在低数据条件下提供稳定梯度引导，在高度模糊任务中成功率从百分之二十四提升至百分之三十九点五，标准基准达到百分之六十七点八，真实世界实验也验证了其数据效率和鲁棒性。该工作为语言条件下的机器人精准操作提供了有效解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25360v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 513}, {\"url\": \"assets/figures/arxiv/2606.25360v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1817, \"height\": 754}, {\"url\": \"assets/figures/arxiv/2606.25360v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 813, \"height\": 833}, {\"url\": \"assets/figures/arxiv/2606.25360v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 899, \"height\": 872}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25360v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 908, \"height\": 822}, {\"url\": \"assets/tables/arxiv/2606.25360v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 908, \"height\": 808}, {\"url\": \"assets/tables/arxiv/2606.25360v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 907, \"height\": 868}, {\"url\": \"assets/tables/arxiv/2606.25360v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 907, \"height\": 294}]"
motivation: 现有端到端视觉语言动作模型在数据有限的模仿学习中因耦合语义推理与空间控制而导致目标辨别准确性受限。
method: 提出空间视觉提示模仿学习解耦架构，利用视觉语言基础模型解析指令生成零样本几何掩码作为空间视觉提示，并通过轻量直接特征融合注入连续动作生成器。
result: 在仅五十至一百条演示下，高度模糊语言任务成功率从百分之二十四提升至百分之三十九点五，标准基准达百分之六十七点八，真实实验验证数据效率与鲁棒性。
conclusion: 本文提出的解耦空间视觉提示方法显著提升语言条件模仿学习的数据效率与目标辨别准确率，为机器人操作提供有效范式。
---

## 摘要
尽管端到端的视觉-语言-动作（VLA）模型在机器人操作中展现出前景，但其整体范式将语义推理与空间控制固有地耦合在一起。这造成了严重的对齐瓶颈，限制了在数据受限的模仿学习中对精确目标的消歧。为了克服这一点，我们提出了SVP-IL，一种解耦架构，从动作生成循环中显式提取空间视觉基础。通过利用视觉-语言基础模型，我们将指令解析为零样本几何掩码，将语言翻译为显式的空间视觉提示（SVP）。这些先验知识通过轻量级的直接特征级融合机制注入到连续动作生成器中。这种整合提供了明确且未受污染的空间梯度指引，同时确保在低数据量下高度稳定的优化。大量实验表明，SVP-IL显著优于最先进的VLA和纯视觉运动基线。在仅有50到100个示范样本的训练下，SVP-IL在高歧义语言条件任务上的平均成功率从24.0%提升到39.5%，在标准基准上达到67.8%。在真实世界的机器人实验中进一步验证了其在非结构化物理环境中的鲁棒性和数据效率。

## Abstract
While end-to-end Vision-Language-Action (VLA) models show promise in robotic manipulation, their monolithic paradigm inherently couples semantic reasoning and spatial control. This creates a severe alignment bottleneck, limiting precise target disambiguation in data-constrained imitation learning. To overcome this, we propose SVP-IL, a decoupled architecture that explicitly extracts spatial visual grounding from the action generation loop. By leveraging vision-language foundation models, we parse instructions into zero-shot geometric masks, translating language into explicit Spatial Visual Prompts (SVP). These priors are injected into a continuous action generator via a lightweight direct feature-level fusion mechanism. This integration provides explicit and uncorrupted spatial gradient guidance while ensuring highly stable optimization under low-data regimes. Extensive experiments demonstrate that SVP-IL significantly outperforms state-of-the-art VLAs and pure visuomotor baselines. Trained on as few as 50 to 100 demonstrations, SVP-IL improves average success rates on highly ambiguous language-conditioned tasks from 24.0% to 39.5%, achieving 67.8% on standard benchmarks. Real-world robotic experiments further validate its robustness and data efficiency in unstructured physical environments.

---

## 论文详细总结（自动生成）

## 论文核心分析与总结：SVP-IL

### 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：现有的端到端 Vision-Language-Action (VLA) 模型在机器人操作中将语义推理与空间控制紧密耦合，造成严重的“语义-空间对齐瓶颈”。在数据量有限的模仿学习场景下，模型难以从少量演示中隐式学会将抽象语言指令精确映射到连续空间动作，导致目标辨别失败（如抓取相近干扰物）和空间精度不足。
- **研究背景**：模仿学习（IL）在单任务场景中已取得显著进展，但扩展到语言条件多目标操作时，传统方法（如将语言通过 cross-attention 或 FiLM 条件化）效率低下且数据需求巨大。端到端 VLA 模型（如 π0、OpenVLA）虽统一了流程，却在严格语义遵循上表现脆弱，尤其当场景中多个物体视觉外观相似时。作者提出应将语义推理与空间几何基础显式解耦，降低策略的认知负荷。

### 2. 方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：采用解耦感知-控制架构，将目标消歧从动作生成循环中提取出来，通过视觉语言基础模型将语言指令转译为显式的空间视觉提示（SVP），然后将这些几何先验以直接特征级融合的方式注入到连续的视觉运动策略中。
- **关键技术细节**：
    - **两阶段空间提示提取**：
        - 语义解析：使用 LLM 从自然语言指令 `l` 中提取目标物体名称 `c`：`c = LLM(l)`
        - 零样本掩码生成：利用 SAM 3 模型（以图像 `I_t` 和类别 `c` 为 prompt）生成二值几何掩码 `M_t`：`M_t = SAM3(I_t, c)`
    - **直接特征级融合机制**：
        - 设计一个轻量侧路 CNN（Side-CNN）作为提示流 `Φ_prompt`，将稀疏掩码编码为与骨干中间层维度匹配的密集特征图 `F_prompt^{(i)}`。
        - 在视觉编码器（如 ResNet-18）的第 `i` 层直接对该层原始特征 `Φ_rgb^{(i)}(I_t)` 与提示特征进行逐元素相加（非门控或注意力）：`F^{(i)} = Φ_rgb^{(i)}(I_t) + Φ_prompt^{(i)}(M_t; θ_prompt)`。
        - 保留原始文本嵌入 `l` 作为全局条件，与融合后的视觉特征和本体感受拼合后输入扩散策略（DP）的噪声预测网络。
    - **SVP-IL 实例化**：将上述注入模块集成到 Diffusion Policy（DP）中，以 ResNet-18 为视觉编码器。
- **公式/算法流程**：详见 Section III-C：`a_t ∼ π_ϕ(·| o_{t−k:t}, M_{t−k:t}, l)`；其中 `M_t` 即为具象的空间视觉提示（SVP）。

### 3. 实验设计：使用的数据集/场景、基准和对比方法

- **模拟环境**：基于 RoboTwin 2.0 基准，包含 9 个典型双机械臂任务（如调整瓶子、提锅、摇瓶等）和 6 个自定义高歧义语言条件任务（如“将[特定物体]放入篮子”）。提供 Clean（干净背景）和 Randomized（杂纹背景、动态光照、密集干扰物）两种设置。
- **真实世界场景**：在物理 Aloha-AgileX 双机械臂平台上设置 3 个任务：Table Setting（按颜色堆叠碗碟）、Desktop Cleaning（按语义区分同色物体）、Bimanual Sorting（根据目标空间位置选择正确机械臂）。
- **对比方法**：
    - 纯运动策略：ACT (Action Chunking with Transformers)、DP (Diffusion Policy)
    - 端到端 VLA：π0（全参数微调）
- **训练数据**：标准任务每种 50 个演示，自定义任务每种 100 个演示。

### 4. 资源与算力

- **文中未明确说明**使用的 GPU 型号、数量及训练时长等计算资源细节。仅描述所有模型采用相同配置进行训练，且训练数据量较小（50 或 100 个演示），暗示可复现性较强但未提供能耗或硬件要求。

### 5. 实验数量与充分性

- **实验数量**：
    - 主实验表 I：9 标准任务 + 6 自定义任务，每个方法至少 1 次评估（成功率）。
    - 鲁棒性实验表 II：DP 和 SVP-IL 在 Clean 与 Random 两种训练/测试协议下的对比。
    - 消融实验表 III：对比 Pixel-Overlay、Early-Concat 与 SVP-IL（直接特征级融合）。
    - 真实世界实验表 IV：3 个任务 × 3 种方法 × 20 次试验。
- **充分性与公平性**：
    - 覆盖了标准基准、自定义情景、视觉分布偏移、消融和物理部署，实验设计系统全面。
    - 所有基线均使用相同的训练配置，π0 采用官方推荐的全参数微调以保证公平。
    - 在低数据限制下进行对比，突出了数据效率优势。
    - 但未提供多次运行的标准差或置信区间，也未说明随机种子，限制了统计显著性评估。总体而言，实验具有较好的客观性和覆盖面。

### 6. 论文的主要结论与发现

- SVP-IL 在低数据量下显著超越 SOTA VLA（π0）和纯运动基线（ACT、DP），在标准基准上提升至 **67.8%**（π0 为 53.7%），在高度歧义的自定义任务上从 **24.0% 提升至 39.5%**。
- 直接特征级融合（逐元素相加）严格优于图像级叠加或通道拼接，避免了预训练骨干的领域偏移，提供稳定空间梯度引导。
- 显式分离语义与空间基础，避免了端到端模型的对齐瓶颈；域随机化（Domain Randomization）不能替代几何先验带来的强归纳偏置。
- 真实世界实验验证了 SVP-IL 在密集杂乱的物理环境中的鲁棒性和精确性，平均成功率 60.0% 显著高于 π0（31.7%）和 DP（28.3%）。

### 7. 优点

- **解耦架构设计**：将语义推理委托给基础模型，策略只需关注空间执行，大幅降低模型复杂度。
- **轻量融合机制**：直接特征级逐元素相加无需额外可学习门控，在低数据下不会引入过拟合风险，同时保留骨干网络训练好的纹理特征。
- **数据高效**：仅需 50–100 个演示即可显著改善歧义任务性能，对实际应用具有重要意义。
- **可插拔性**：SVP 模块可集成到不同的连续动作生成器（如 DP）中，框架通用性强。
- **算法透明**：掩码提供显式几何信息，增强可解释性并利于调试。

### 8. 不足与局限

- **上游模型依赖**：SVP 质量受限于 SAM 3 对透明/高反光/严重遮挡物体的分割能力，可能产生噪声掩码降低策略精度。
- **指令类型限制**：当前需要显式、物体为中心的指令；对抽象意图（如“清理桌子”）隐含的目标推断尚未支持。
- **计算开销**：SAM 3 推理带来额外延迟，影响控制频率，不适合高速动态环境；作者提及可考虑替换为轻量分割模型进行优化。
- **实验细节缺失**：未报告方差、置信区间和随机种子，结果偶然性无法完全排除；未提供算力消耗和训练时间，不利于复现比较。
- **真实场景测试范围**：物理实验仅在静态、密集杂乱的场景下进行，缺少对动态干扰、快速移动目标的评估。
- **潜在偏差风险**：自定义任务仅基于 RoboTwin 环境修改，可能无法完全覆盖真实世界的所有歧义挑战。

（完）
