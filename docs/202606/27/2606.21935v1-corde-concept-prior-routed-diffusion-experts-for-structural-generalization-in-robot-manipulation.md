---
title: "CoRDE: Concept-Prior Routed Diffusion Experts for Structural Generalization in Robot Manipulation"
title_zh: "CoRDE: 概念先验路由扩散专家用于机器人操作中的结构泛化"
authors: "Haidong Huang, Xixin Zhao, Yaohua Zhou, Jiayu Song, Jiayi Zhang, Jun Ma, Haiyue Zhu, Xiaocong Li"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21935v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 基于概念先验的扩散专家路由在机器人操纵中实现结构泛化
tldr: 扩散模型在机器人模仿学习中擅长多模态动作建模，但面对多任务长时域场景时，整体式网络缺乏结构泛化能力，语义子阶段间梯度冲突严重。现有MoE方法存在路由崩溃与参数膨胀问题。为此提出CoRDE，利用冻结概念编码器提取语义先验，通过变分推理和熵控制实现鲁棒路由，并借助LoRA构建参数高效专家池。理论证明低秩适应下生成保真度有界。实验表明，CoRDE有效缓解路由崩溃，形成语义对齐的专家分配，动作质量和增量学习效率显著提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21935v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1653, \"height\": 871}, {\"url\": \"assets/figures/arxiv/2606.21935v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1860, \"height\": 942}, {\"url\": \"assets/figures/arxiv/2606.21935v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 902, \"height\": 379}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21935v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1746, \"height\": 403}, {\"url\": \"assets/tables/arxiv/2606.21935v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 661, \"height\": 181}, {\"url\": \"assets/tables/arxiv/2606.21935v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1485, \"height\": 372}]"
motivation: 扩散模型在多任务长时域操作中结构泛化不足，且MoE方法面临路由崩溃与参数膨胀问题。
method: CoRDE采用冻结概念编码器提取语义分布，通过变分后验和熵控制引导路由，并利用LoRA实现参数高效专家池。
result: 实验证实CoRDE系统性减少路由崩溃，形成语义对齐的专家分配，在动作质量和增量学习上优于基线。
conclusion: CoRDE的结构引导变分蒸馏框架有效结合先验与扩散过程，为机器人操作提供了可扩展的结构泛化方案。
---

## 摘要
扩散模型在捕捉机器人模仿学习中的多模态动作分布方面表现出色。然而，在多任务和长期时间跨度场景中，单一架构缺乏结构泛化能力，会在不同语义子阶段之间产生梯度冲突。虽然纯数据驱动的混合专家（MoE）方法引入了分工，但它们经常触发路由崩溃，并且实例化完整规模的专家会导致参数爆炸和较高的扩展成本。为了解决这些问题，我们提出了概念先验路由扩散专家（CoRDE），一种结构引导的变分蒸馏框架。CoRDE从冻结的概念编码器中提取语义分布，通过可学习的软映射矩阵引导变分后验责任。该机制引入了一个熵控制的责任推断过程，在可靠语义预测下鼓励自信的路由，同时保留随机扩散项以保持行为多样性。为了解决参数膨胀问题，CoRDE使用低秩适应（LoRA）在共享的冻结骨干网络上构建参数高效的专家池。理论分析表明，混合分数差异受到责任加权的局部专家误差的约束，支持在低秩专家适应下的高保真生成。实证评估证实，与现有基线相比，CoRDE系统地减少了路由崩溃，形成了鲁棒的、语义对齐的专家分配，同时实现了优越的动作质量和增量学习效率。

## Abstract
Diffusion models excel at capturing multi-modal action distributions in robot imitation learning. However, in multi-task and long-horizon scenarios, monolithic architectures lack structural generalization capabilities, suffering from gradient conflicts between distinct semantic sub-stages. While pure data-driven Mixture-of-Experts (MoE) methods introduce labor division, they frequently trigger routing collapse, and instantiating full-scale experts causes parameter explosion and high expansion costs. To address these issues, we propose Concept-prior Routed Diffusion Experts (CoRDE), a structure-guided variational distillation framework. CoRDE extracts semantic distributions from a frozen concept encoder to guide the variational posterior responsibility via a learnable soft mapping matrix. This mechanism introduces an entropy-controlled responsibility inference process that encourages confident routing under reliable semantic predictions while preserving the stochastic diffusion term for behavioral diversity. To overcome parameter inflation, CoRDE employs a parameter-efficient expert pool using Low-Rank Adaptation (LoRA) on a shared frozen backbone. Theoretical analysis shows that the mixture score discrepancy is bounded by responsibility-weighted local expert errors, supporting high-fidelity generation under low-rank expert adaptation. Empirical evaluations confirm that, compared to existing baselines, CoRDE systematically reduces routing collapse, forming robust, semantically aligned expert allocations while achieving superior action quality and incremental learning efficiency.

---

## 论文详细总结（自动生成）

# CoRDE: 概念先验路由扩散专家用于机器人操作中的结构泛化 —— 详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：扩散模型在机器人模仿学习中能够捕捉多模态动作分布，但面对**多任务长时域**场景时，**整体式（monolithic）网络**强制用单一参数集拟合不同语义子阶段（如低频空间导航 vs 高频精细操作），导致**梯度冲突**和**灾难性干扰**，缺乏结构泛化能力。
- **现有方法局限**：
  - 纯数据驱动的**混合专家（MoE）** 虽然引入分工，但路由机制依赖数据似然，在多模态噪声数据下极易陷入**路由崩溃（routing collapse）**（赢者通吃局部最优），且人为加负载均衡损失会扭曲物理任务的自然分布。
  - 实例化全规模专家导致**参数爆炸**，计算成本高。
  - 专家分配缺乏**可解释的语义边界**，易产生路由抖动和动作混淆。
- **核心目标**：构建一个**结构引导的变分蒸馏框架**，融合高层语义概念先验与扩散生成能力，在不破坏动作多样性的前提下实现稳定、语义对齐的专家路由，并控制参数规模。

## 2. 论文提出的方法论

### 核心思想
- 利用**冻结的概念编码器**从多模态观测中提取稳定的语义分布 \(p_\phi(c|o)\)。
- 通过**可学习的软映射矩阵** \(P_{CE}(z|c)\) 将语义投影为**结构先验** \(p_{\text{struct}}(z|o) = \sum_c p_\phi(c|o) P_{CE}(z|c)\)。
- 在变分蒸馏中，用结构先验与行为证据（基于教师分数误差）通过**几何平均**得到最优变分后验责任 \(q^*(z|o, a_0)\)，实现**双熵动力学**：宏观路由熵最小化（稳定性）与随机扩散项全秩方差保持（多样性）。
- 构建**参数高效的LoRA专家池**：共享冻结的扩散骨干，每个专家仅训练注入的低秩适配器（LoRA），避免参数膨胀。

### 关键技术细节

1. **结构引导的责任推断**：
   - 行为证据 \(r(z|o,a_0) = q_\psi(z|o) \exp(-\|u_T - u^{(z)}_\theta\|_2^2)\)，归一化得 \(\bar{r}\)。
   - 变分优化目标：\(\min_{\tilde{q}} \mathbb{E}[\text{KL}(\tilde{q}\|\bar{r}) + \lambda(o) \text{KL}(\tilde{q}\|p_{\text{struct}})]\)。
   - 闭式解：\(q^* = \frac{1}{Z} \bar{r}^{\frac{1}{1+\lambda}} p_{\text{struct}}^{\frac{\lambda}{1+\lambda}}\)，其中\(\lambda(o) = \lambda_{\max}\gamma(o)\)，\(\gamma(o)\)为概念置信度。
   - 理性：语义模糊时退化为数据驱动，语义清晰时强化结构先验。

2. **LoRA参数化及理论保证**：
   - 专家权重 \(W^{(z)} = W_0 + B^{(z)}A^{(z)}\)，秩\(r \ll d\)。
   - **定理2**：混合分数场 \(\tilde{u} = \sum_z q^*(z) u^{(z)}_\theta\) 的全局\(L_2\)误差被责任加权的局部误差上界约束，保证低秩近似的高保真生成。
   - **多样性保持**：反向SDE中的Wiener过程始终注入全秩各向同性噪声，与LoRA改动的确定性漂移项解耦，不会因低秩参数丢失行为多样性。

3. **路由解耦与EM软对齐**：
   - 路由网络 \(q_\psi(z|o)\) 仅通过**KL散度**与责任后验匹配，不参与分数匹配优化，避免梯度冲突。
   - 软映射矩阵 \(P_{CE}\) 通过**期望最大化（EM）风格**更新：基于小批量软分配计数 \(N_{c,z}\)，经Dirichlet平滑和指数移动平均（EMA）演化，无需反向传播。

4. **训练与推理流程**：
   - **Phase I（离线蒸馏）**：提取概念先验 → 计算责任后验（E步） → 更新LoRA专家、路由网络和映射矩阵（M步）。
   - **Phase II（在线执行）**：语义提取 → 状态条件路由（含结构偏置\(\beta\log p_{\text{struct}}\)） → 顶部k专家激活 → SDE去噪生成动作序列。

## 3. 实验设计

### 数据集与基准

| 基准 | 场景/任务 | 主要评估指标 |
|------|-----------|--------------|
| **LIBERO** | L-Spatial, L-Object, L-Goal, L-Long（多任务长时域） | 任务成功率，宏平均 |
| **D3IL** | Avoiding, Aligning, Pushing, Stacking-1/2, Sorting (Image), Stacking (Image)（高度多模态人类演示） | 任务成功率、任务熵（0-1）、推理时间 |

### 对比方法
- **Diffusion Policy (Teacher)**：原始预训练教师策略。
- **Distill-MoE (Evidence-only)**：VDD方法，仅依赖数据似然路由。
- **CoRDE (HiMaCon encoder)**：使用自监督概念编码器 [17]。
- **CoRDE (AutoCGP encoder)**：使用无监督发现的概念 [16]。

**公平性控制**：所有蒸馏学生方法共享相同参数预算、架构（相同冻结骨干、相同专家数量K、相同LoRA配置、相同训练步数）。唯一变量是E步中是否使用结构先验。概念编码器均无需人类标注。

## 4. 资源与算力

- **文中未明确说明**所用的GPU型号、数量、训练时长、显存等具体算力信息。仅提到使用预训练教师和学生模型进行蒸馏，未报告计算开销细节。

## 5. 实验数量与充分性

- **LIBERO**：在4个套件上评估，共4组主要结果（图2）。
- **D3IL**：在7个任务（含状态和图像）上评估任务成功率和任务熵（表I），并报告推理时间（表II，仅Pushing任务）。共7组成功率+7组任务熵+1组推理时间。
- **消融实验**（表III）：在4种变体（w/o PCE、w/o adaptive λ、evidence-only、router w/o responsibility supervision）上分别报告LIBERO平均成功率、D3IL成功率和任务熵、路由诊断指标（H_route, H_post, PPL）。共4组消融结果。
- **路由诊断**：额外分析了专家使用平衡度（PPL）、路由不确定性等。
- **多模态定性**：在D3IL上展示可视化（图3）说明方法保留多模态轨迹。
- **充分性与公平性**：实验设计较为完整，控制变量严格（参数预算、架构相同），消融覆盖关键组件（软映射、自适应λ、证据驱动、路由器监督）。但**缺乏真实机器人实验**和**跨分布泛化**测试。总体在模拟基准上较充分。

## 6. 主要结论与发现

1. **性能提升**：在LIBERO上，CoRDE在所有4个套件上超过教师和Distill-MoE，尤其在L-Long和L-Goal（长期多阶段任务）上提升最大，验证结构先验能稳定路由、减少长程错误累积。
2. **多样性保持**：D3IL任务熵接近原教师DP且显著高于确定性基线，证明低秩LoRA与SDE随机项结合能有效保留多模态行为覆盖。
3. **推理加速**：蒸馏后单步生成（NFE=1）将推理时间从55.62ms（DP，NFE=64）降至2.63ms，降幅20倍以上，且无需全参数专家副本。
4. **路由稳定性**：消融表明，移除PCE或自适应λ会降低任务熵和路由诊断指标；无责任监督的路由器导致严重专家不平衡（PPL≈1.1，近似崩溃）。CoRDE的结构引导机制系统性消除了路由崩溃。
5. **免标注**：使用HiMaCon或AutoCGP等无标签概念编码器均能取得稳定增益，说明语义结构可自组织获得。

## 7. 优点：方法或实验设计上的亮点

- **创新性**：首次将**结构化的语义概念先验**系统性地引入扩散MoE蒸馏，通过变分几何平均融合先验与证据，理论形式优美。
- **理论严谨性**：为混合分数近似误差提供了上界（定理2），从理论上支持低秩专家的高保真生成。
- **参数高效性**：利用LoRA在共享骨干上构建专家池，破解参数膨胀问题，同时利用SDE反向过程保持生成多样性，思想巧妙。
- **路由稳定性**：通过解耦路由网络（仅拟合后验，不参与分数匹配）和EM软对齐，避免了梯度冲突，且使专家分配具有语义可解释性。
- **实验设计公平**：严格控制蒸馏方法的参数预算和架构，独立变量仅为结构先验，结论可信度高。
- **诊断指标新颖**：引入H_route、H_post、PPL等路由诊断指标，能明确区分专家结构化分工与路径崩溃。
- **兼容性**：支持多种无监督概念编码器（HiMaCon、AutoCGP），不依赖人工标注，实际部署灵活。

## 8. 不足与局限

- **缺乏真实实验验证**：所有评估均在模拟器（LIBERO、D3IL的仿真环境）上进行，未在实物机器人平台上测试，真实世界的噪声、延迟、分布偏移可能影响性能。
- **概念编码器依赖**：框架效果受概念质量影响。若概念编码器本身不稳定（如罕见场景下语义模糊），可能引入错误先验。论文虽设计了自适应λ回退机制，但未在严重概念错误场景下测试鲁棒性。
- **分布外泛化未探讨**：仅评估了训练任务内性能，未涉及零样本泛化或任务增量学习场景下的专家扩展与遗忘问题，尽管声称“增量学习效率”，但实验未直接验证。
- **计算资源细节缺失**：未报告训练成本（GPU型号、时长、参数量），不利于公平比较计算开销。LoRA专家数量K的选择未进行敏感性分析。
- **推理时间测量范围有限**：表II仅报告了Pushing任务上NFE=1的情形，未展示其他任务或不同NFE下的全面推理时间对比。
- **多样性指标局限**：任务熵（0-1）仅反映输出分布覆盖，但没有定量分析轨迹质量或动作成功率与多样性之间的trade-off曲线。
- **会议论文篇幅**：仅8页，部分细节（如EM更新收敛性证明、SDE求解器选择）未充分展开。

（完）
