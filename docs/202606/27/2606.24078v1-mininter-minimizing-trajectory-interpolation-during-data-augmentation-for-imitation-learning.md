---
title: "MinInter: Minimizing Trajectory Interpolation During Data Augmentation for Imitation Learning"
title_zh: MinInter：在模仿学习的数据增强中最小化轨迹插值
authors: "Qingyang Wang, Xingang Liu, Changwei Yao, Zikai Ouyang, Junwei Liu, Haibo Lu, Wei Zhang"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24078v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 通过最小化插值进行轨迹选择以改进模仿学习
tldr: 模仿学习受限于高质量演示数据的获取成本。现有轨迹级数据增强方法重组演示片段来增加数据，但会引入非专家插值段降低数据质量。本文提出MinInter方法，为每个初始状态选择需要最少插值的源演示以生成完整轨迹，从而最小化非专家段。在MimicGen基准12个操作任务26个变体上，MinInter一致提升数据生成成功率和下游策略成功率，尤其在接触丰富、长视界和高方差任务上表现突出。虽然概念简单，MinInter超越复杂框架SkillGen，凸显了最小化插值在数据增强中的价值。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24078v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1818, \"height\": 995, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24078v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1819, \"height\": 816, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24078v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 780, \"height\": 764, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24078v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 779, \"height\": 762, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24078v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 793, \"height\": 517, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24078v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1630, \"height\": 697, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24078v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 914, \"height\": 660, \"label\": \"Table\"}]"
motivation: 现有轨迹级数据增强方法重组演示时引入非专家插值段，降低了合成数据的质量，从而限制了下游策略的性能。
method: MinInter方法为每个初始配置从源演示中选择需要最少插值的那一个来生成完整轨迹，从而最小化非专家段的引入。
result: 在MimicGen基准12个操作任务26个变体上，MinInter一致提升了数据生成成功率和策略成功率，接触丰富、长视界任务提升最大。
conclusion: MinInter通过最小化插值简单而有效地提升数据增强质量，证明了减少非专家段对模仿学习的价值。
---

## 摘要
模仿学习使机器人能够从示范中获取复杂的操作技能，但其有效性受到收集高质量数据成本的限制。轨迹级数据增强方法通过在不同的初始状态下重新组合专家示范来缓解这一挑战。然而，这些方法通常在不连接的部分之间插入插值或其他非专家过渡段，而这些非专家段可能会降低生成数据的质量。本文介绍了MinInter（最小化插值），一种有效的轨迹选择方法，对于每个采样的初始配置，它选择需要最少插值来形成完整轨迹的源示范。通过在数据生成过程中显式地最小化插值，MinInter生成了更高质量的合成示范，同时与现有数据生成框架保持兼容。在MimicGen基准的12个操作任务及其26个变体上的实验表明，MinInter持续提高了数据生成成功率和策略成功率，在接触密集、长视域和高方差设置中提升最大。与最近的SkillGen框架相比，MinInter尽管概念简单，但获得了更高的策略成功率，强调了插值最小化对数据增强的价值。

## Abstract
Imitation learning enables robots to acquire complex manipulation skills from demonstrations, but its effectiveness is limited by the cost of collecting high-quality data. Trajectory-level data augmentation methods alleviate this challenge by recombining expert demonstrations under varied initial states. However, such methods typically insert interpolations or other non-expert transition segments between disjoint parts, and such non-expert segments could reduce the quality of the generated data. This paper introduces Minimizing Interpolation (MinInter), an effective trajectory selection method that, for each sampled initial configuration, chooses the source demonstration requiring the least interpolation to form a complete trajectory. By explicitly minimizing interpolations during data generation, MinInter produces higher-quality synthetic demonstrations while remaining compatible with existing data generation frameworks. Experiments on 12 manipulation tasks with 26 variants from the MimicGen benchmark show that MinInter consistently improves both data generation success rates and policy success rates, with the largest gains on contact-rich, long-horizon and high-variance settings. Compared to the recent SkillGen framework, MinInter achieves higher policy success rates despite its conceptual simplicity, underscoring the value of interpolation minimization for data augmentation.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：模仿学习依赖大规模高质量演示数据，但采集成本极高。现有轨迹级数据增强方法（如 MimicGen）通过重组专家演示、在不同初始状态下生成新轨迹来缓解数据稀缺，但重组时通常需在片段间插入线性插值等非专家过渡段，导致合成轨迹中出现不自然的运动，降低数据质量，进而限制下游策略性能。
- **研究动机**：现有方法未直接应对过度插值对数据质量的负面影响。本文提出通过显式最小化插值来提升数据增强质量，使生成数据更接近专家示范。
- **整体含义**：MinInter 在现有数据生成框架中引入简单的轨迹选择机制，为每个初始状态从源演示集中挑选需要最少插值的那一条，从而在不增加额外模型复杂度的情况下，显著提升数据生成成功率和下游模仿学习策略成功率，验证了“减少非专家过渡段”对于模仿学习数据增强的价值。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：对于每个随机采样的初始物体配置，分别用所有源演示生成候选轨迹（转换各物体中心段并插入插值连接），计算每条候选轨迹的总插值量，选择总插值最小的轨迹作为输出，再检查任务成功条件，成功则加入训练集。
- **插值度量**：定义两姿态 $E_1, E_2$ 之间的插值距离：
  \[
  \delta(E_1, E_2) = (1 - w) \|p_1 - p_2\|_{2} + w \|R_1^{-1} R_2 - I\|_{F}
  \]
  其中 $w$ 平衡平移和旋转贡献，实验取 $w=0.5$。
- **总插值**：对于含 $n$ 个片段的轨迹 $\tau$，总插值为
  \[
  \text{Inter}(\tau) = \sum_{j=0}^{n-1} \delta(E^{j}_{H_j,\text{transf}}, E^{j+1}_{1,\text{transf}})
  \]
  即所有相邻片段端点（首尾过渡处）的插值距离之和，初始端 $E^{0}_{H_0,\text{transf}}$ 为初始末端执行器位姿。
- **选择策略**：
  \[
  \tau^{*} = \arg\min_{\tau \in T_{\text{exp}}} \text{Inter}(\Gamma(\tau, s_0))
  \]
  其中 $\Gamma(\tau, s_0)$ 表示将源演示 $\tau$ 转换到初始状态 $s_0$ 后形成的完整轨迹。该轨迹通过保留末端执行器与中心物体的相对位姿变换得到，片段间用线性插值连接。
- **算法流程**：
  1. 采样初始物体配置；
  2. 对每个源演示，按式（3）转换各片段，并在相邻片段间插入线性插值（位置和方向）；
  3. 按式（4–5）计算该候选轨迹的总插值；
  4. 选择总插值最小的源演示对应的轨迹；
  5. 检查该轨迹是否满足任务成功条件，若成功则存入数据集，否则丢弃并重新采样初始配置。

整个过程与现有数据生成框架完全兼容，仅增加一步轨迹选择，无需学习额外模型。

## 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **数据集与场景**：采用 MimicGen 公开基准中的 **12 个操作任务**，共 **26 个变体**，涵盖基础操作（Stack, Stack Three）、接触密集任务（Square, Threading, Coffee, Three Piece Assembly, Hammer Cleanup, Mug Cleanup）和长视域任务（Kitchen, Nut Assembly, Pick Place, Coffee Preparation）。每个任务下分三种初始状态分布（D0, D1, D2），难度递增。环境基于 MuJoCo/robosuite。
- **数据生成**：每个任务变体用 10 条专家演示作为源数据，生成 1000 条成功轨迹用于训练。
- **下游策略**：使用 BC-RNN 策略架构，输入 RGB 图像（侧视+腕部相机）、末端执行器位姿和夹爪状态，训练 1200 个 epoch，每 20 epoch 用 50 次 rollout 评估，取最佳检查点，报告 3 个随机种子（101, 102, 103）的平均成功率。
- **对比方法**：
  - 基线：MimicGen 原始数据生成方法（不进行最小插值选择）；
  - 对比 SkillGen（在其报告的任务子集上直接比较数据）；
  - 消融：插值权重 $w$ 的选取实验。
- **公平性**：所有方法共用相同的源数据、训练和评估代码，保证实验条件完全一致。基线数据、SkillGen 结果均取自原文或附录。

## 4. 资源与算力

- 文中**未明确说明**使用的 GPU 型号、数量及训练时长。仅提及每个策略训练 1200 epochs，但未给出具体硬件配置或总计算时间。
- 基于 MimicGen 和 BC-RNN 的标准设置，可推测使用单卡或少量 GPU（如 NVIDIA RTX 系列），但无法从原文确认算力投入。此为报告中的信息缺失，不影响方法有效性，但可能影响复现的可操作性。

## 5. 实验数量与充分性

- **实验数量**：共 12 个任务 × 26 个变体，每个变体 3 个随机种子，总计 >70 个独立训练-评估实验。此外包含数据生成成功率统计、插值减少量化、按难度分组分析、消融研究（权重 $w$）等，实验覆盖面广。
- **充分性**：
  - 覆盖了从简单到复杂、接触密集、长视域等多种任务类型，且包含三种难度分布，较好地检验了方法的泛化性。
  - 对比 SkillGen 时在其报告子集上进行，结果直接可比，验证了 MinInter 的简洁有效性。
  - 同时报告数据生成成功率（+12.8%）和下游策略成功率（+4.8%），从两个层面说明提升，逻辑完整。
  - 消融实验（$w=0.5$ 最优）也提供了超参数选择的依据。
- **公平性**：与基线使用相同数据、训练代码和评估协议；与 SkillGen 对比时数据来源一致，保证客观。

## 6. 论文的主要结论与发现

- MinInter 在 **26/26 个任务变体上一致减少生成轨迹中的插值**，数据生成成功率平均提升 **+12.8%**，在困难任务（如 Hammer Cleanup D0 提升 +50.1%，Nut Assembly D0 提升 +31.1%）上尤其显著。
- 下游策略成功率在 **21/26 变体上提升或持平（均以 100% 封顶）**，平均提升 **+4.8%**，最大提升出现在 Nut Assembly D0（+20.7%）。
- 按难度分组分析显示：**提升幅度随难度增大而增大**（D0→D2 递增），且在**接触密集和长视域任务上提升高于基础任务**（CR/LH >7% vs Basic <1%）。
- 与 SkillGen 相比（在其 4 个可比较任务上），MinInter 实现 **+4.9%** 平均提升，高于 SkillGen 的 +3.8%，**以更简单的方法取得了更优结果**，突出了最小化插值的核心价值。
- 总体结论：**减少数据生成中的非专家插值**可以显著提升合成数据质量，进而增强模仿学习效果，且无需额外复杂模块。

## 7. 优点：方法或实验设计上的亮点

- **理念简洁有效**：仅通过选择最小插值的源演示即可获得收益，无需训练生成模型、运动规划器或额外策略，易于理解与集成。
- **与现有框架高度兼容**：MinInter 作为数据生成阶段的筛选步骤，可直接插入 MimicGen、DexMimicGen 等流水线，无需改动后续训练流程。
- **插值度量合理**：同时考虑平移（欧氏距离）和旋转（Frobenius 范数），并通过权重平衡二者，通过消融选定 $w=0.5$，方法透明。
- **双重评估维度**：同时报告数据生成成功率和策略成功率，验证了插值减少→数据质量提高→策略性能提升的因果链条。
- **实验覆盖全面**：涵盖 12 个任务、26 个变体、三种难度、与 SkillGen 的可比对比，以及按任务类型/难度的分组分析，结论稳健有力。
- **开源与可重复性**：基于 MimicGen 开源基准，使用公开数据与代码，确保结果可复现。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **仅模拟环境**：所有实验在 MuJoCo/robosuite 模拟器中进行，未在真实机器人上验证。未能确认 MinInter 在真实场景、物理扰动、感知噪声下的有效性。
- **对基本任务提升有限**：在已经达到 100% 或接近 100% 的任务（如 Stack D0）上无改善空间，因此平均提升被弱化；但符合预期，不构成方法缺陷。
- **单一源选择**：MinInter 仅从单个源演示中取全部片段构建轨迹，未探索跨源组合片段可能带来的更低插值。虽然简化了问题，但可能错失更优轨迹。
- **插值权重固定**：$w=0.5$ 在实验中表现最佳，但可能在不同任务（特别是旋转主导或平移主导的任务）中非最优，缺乏自适应机制。
- **未讨论多样性风险**：最小化插值可能导致过度倾向某些源演示，降低生成轨迹的多样性，可能影响策略的泛化性。论文未对此进行量化分析。
- **算力信息缺失**：未报告 GPU 型号、数量、训练时间，不利于其他研究者估算复现成本。
- **对比 SkillGen 的任务子集较小**：仅涵盖 4 个任务的可比数据，虽然提升明显但统计显著性可能受样本量限制。更多任务上的直接对比会更理想。

（完）
