---
title: "RE4: Transformation-aware Imitation of Object Interactions Using Manipulation Modes"
title_zh: RE4：基于操作模式的物体交互变换感知模仿
authors: "Arsh Chawla, Rahul Shome"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24403v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 提出一个利用操作模式的变换感知模仿学习框架用于物体交互
tldr: 对象交互模仿学习中端到端方法虽然性能提升但缺乏可解释性，本文RE4框架利用操纵模式进行变换感知模仿，兼顾高性能与可解释性。具体包含四个步骤：首先利用演示数据自监督训练轻量位姿估计器，然后基于操纵模式进行演示检索与变换，再在模式约束下重规划，最后执行变换后的演示。在Push-T和Robomimic的基于状态与图像基准上，RE4在稀疏数据和低样本条件下表现鲁棒，尤其在对抗性设置中显著优于现有方法。RE4证明了简单可解释的构建块可以学习高效操作技能，同时保持高性能与透明度，为可解释模仿学习提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24403v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 629, \"height\": 349}, {\"url\": \"assets/figures/arxiv/2606.24403v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1367, \"height\": 253}, {\"url\": \"assets/figures/arxiv/2606.24403v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1103, \"height\": 562}, {\"url\": \"assets/figures/arxiv/2606.24403v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 591, \"height\": 343}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24403v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 647, \"height\": 223}, {\"url\": \"assets/tables/arxiv/2606.24403v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1238, \"height\": 298}]"
motivation: 现有对象交互模仿学习或牺牲可解释性或性能不足，本文利用操纵理论设计兼具高性能与可解释性的框架。
method: 通过自监督轻量训练估计对象位姿，基于操纵模式进行检索、变换、重规划并执行演示。
result: 在Push-T和Robomimic基准上，RE4在稀疏数据和低样本条件下表现鲁棒，尤其对抗性设置中显著领先。
conclusion: RE4证明简单可解释的构建块能高效学习操作技能，同时保持高性能和可解释性。
---

## 摘要
物体交互任务一直是模仿学习进展的焦点。以扩散模型和基于流的变体为主的端到端方法在性能上取得了飞跃，但牺牲了可解释性。以物体为中心和姿态信息驱动的变体在操作任务的示教学习中发挥了作用。在本文中，我们重新审视了几个现代物体交互模仿学习基准，旨在构建一个框架，重新利用操作的原则理论，同时保持性能和可解释性。对于图像观察，我们提出了一种轻量级训练方法，用于目标物体的无模型姿态估计，该方法利用模仿学习可用的示教数据进行自监督学习。这些信息随后用于指导操作模式感知的示教检索、模式感知变换、连接到检索点同时保持模式约束的重规划步骤，最后执行变换后的示教。这些构成了所提出的RE4框架的四个关键步骤，并在Push-T和Robomimic中基于状态和图像的基准上进行了评估。一个评估基于图像的Push-T稀疏数据区域的对抗性基准展示了其鲁棒性，低数据机制实验的结果进一步支持了这一点。当前工作展示了使用简单可解释的构建块来学习操作技能的前景。

## Abstract
Object interaction tasks have been a focus of advances in imitation learning. End-to-end methods, dominated by diffusion and flow-based variants have shown leaps in performance while sacrificing interpretability. Object-centric and pose-informed variants have had a role in learning from demonstration in manipulation tasks. In this paper, we revisit a few modern imitation learning benchmarks for object interactions, with the aim of composing a framework that repurposes principled theories of manipulation, preserving both performance and interpretability. For image observations, lightweight training is proposed for model-free pose estimation of the target object, using self-supervision over the demonstration data available for imitation learning. This information is then used to inform a manipulation mode-aware retrieval of a demonstration, a mode-aware transformation, a replan step that connects to the retrieval point while preserving mode constraints, and finally rolling out the transformed demonstration. These compose four key steps of the proposed RE4 framework, evaluated over state-based and image-based benchmarks in Push-T and Robomimic. An adversarial benchmark that evaluates sparse data regions of image-based Push-T showcases the robustness, further bolstered by indications from low-data regime experiments. The current work shows promise in using simple interpretable building blocks to learn manipulation skills.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **问题**：物体交互模仿学习（imitation learning）中，以扩散模型、流匹配为代表的端到端方法性能优异，但缺乏可解释性、训练计算成本高、对演示数据量敏感。
- **动机**：重新利用操作理论中关于**操作模式**（manipulation modes）的原则——如 transit（自由移动）、transfer（刚性抓取移动）——来构建一个**可解释、轻量**的模仿学习框架。
- **整体含义**：提出 RE4（Retrieve, Reframe, Replan, Replay）框架，将操作模式感知引入演示检索、变换、重规划与执行，在保持性能的同时提供完全的可解释性（能追溯到具体演示帧），并在稀疏数据和低数据场景下展现出鲁棒性。

### 2. 论文提出的方法论
- **核心思想**  
  以**目标物体位姿**为锚点，利用**操作模式**（transit/transfer）指导模仿策略的四个阶段：
  1. **Retrieve（检索）**：基于当前物体位姿与末端位姿，在演示集中按模式滤波后检索最近邻演示帧。
  2. **Reframe（重新定向）**：将检索到的演示动作块变换到当前物体坐标系下，根据模式不同采用不同变换规则（transit 时物体世界固定，transfer 时物体随动）。
  3. **Replan（重规划）**：从当前末端位置到Reframe起点，在保持模式约束（如 transit 下物体不动）的前提下进行运动规划（如 RRT），得到连接路径。
  4. **Replay（重放）**：依次执行重规划路径和变换后的演示动作块。

- **自监督位姿估计（POSE ESTIMATION）**  
  对于图像观测，无需 CAD 模型或真值位姿，仅利用演示数据训练一个轻量 ResNet-18 编码器，通过绝对回归、同演示相对一致性和跨演示锚点一致性损失学习目标物体位姿，专门服务于模仿任务。

- **算法流程**（Algorithm 1 伪代码核心）  
  ```
  循环直到任务成功或步数超限：
    获取观测 O 和状态 x
    推断操作模式 m（如 gripper state 是否为抓取）
    估计物体位姿 o
    查询 q = <x, o>
    检索最近邻演示帧 j*
    根据模式变换得到 x_near 和动作块
    规划从当前 x 到 x_near 的路径
    执行规划路径 + 变换后的动作块
  ```

### 3. 实验设计
- **数据集/场景**  
  - **Push-T**：平面 SE(2) 推动 T 形块到目标位姿，206 条演示。  
  - **Robomimic**：6-DoF 操作任务 **Lift**、**Can**、**Square**，各 200 条演示。  
  每个任务均有**state-based**（使用真值位姿）和**image-based**（使用估计位姿）两种观测形式。

- **Benchmark 对比方法**  
  - Diffusion Policy（DDPM‑100 步、DDIM‑10 步）、Streaming Flow Policy (SFP)、Geometry‑aware Imitation Policy (GPI)  
  - 消融版本 **RE2**（仅 Retrieve + Replay，无 Reframe/Replan）

- **评价指标**  
  - Push-T：最大覆盖率（IoU）  
  - Robomimic：任务成功率

- **额外稀疏数据实验**  
  - **稀疏观测**：挑选 75 个与演示集距离最远的初始条件，对比不同最大步数下的覆盖率。  
  - **稀疏演示**：仅使用前 50 条演示训练/检索，对比全数据（206）场景。

### 4. 资源与算力
- 文中明确指出使用单张 **NVIDIA GeForce RTX 4090（24 GB）** 进行所有训练。
- **训练时间对比**（Table 3）：  
  - RE4 在 Robomimic 图像任务上仅需 **3～20 分钟**（轻量位姿估计训练），在 Push-T 上无需训练（位姿用闭式解法）。  
  - 相比之下，Diffusion Policy 需 **89～489 分钟**，SFP 需 **9～86 分钟**，GPI 需 **0～31 分钟**。
- **其他资源**：无更多细节（如 CPU 内存、存储等）。

### 5. 实验数量与充分性
- 主要结果（Table 1）报告了 **100 个独立种子**的均值，覆盖所有任务与观测形式，统计可靠性较好。
- 消融实验（RE2 vs RE4）清晰验证了 Reframe 和 Replan 的必要性。
- 稀疏数据实验：
  - 稀疏观测：在 **75 个初始条件 × 10 个步数预算** 下评估，呈现覆盖率-时间曲线。
  - 稀疏演示：**100 个种子**，比较 50 vs 206 条演示。
- **充分性**：实验设计覆盖了标准设置与两种稀疏场景，对比方法来自官方实现，参数按原文设定，公平性较好。但 RE4 在个别任务（Can state）上稍低于扩散基线，但整体竞争力强。未在真实机器人上验证，是文末指出的局限。

### 6. 论文的主要结论与发现
- RE4 在几乎所有 benchmark 上**匹配或超越**先进基线，尤其是在**图像输入**设置下提升明显（Push-T image 覆盖率 0.883 vs 扩散最佳 0.826）。
- **稀疏数据场景**下 RE4 降级更温和，在稀疏观测中更早到达高覆盖率，在稀疏演示中保持更高性能，体现了鲁棒性。
- 消融证实 **Reframe 和 Replan 是框架的关键步骤**，去掉后性能大幅下降。
- 仅需轻量训练（或无需训练）的位姿估计配合模式感知操作足以完成物体交互模仿，同时提供完全的可解释性（可追溯至特定演示帧和模式段）。

### 7. 优点
- **高度可解释**：每个动作可溯源到具体演示帧和操作模式。
- **训练极其轻量**：仅需训练一个小型位姿估计网络（分钟级），甚至无需训练（Push-T 用闭式解法）。
- **数据高效**：在稀疏演示下性能优越，对数据量不敏感。
- **模块化设计**：各步骤（检索、变换、规划、重放）独立且可替换。
- **鲁棒性**：对抗性稀疏初始条件下表现突出，优于扩散策略。

### 8. 不足与局限
- **位姿估计误差传导**：检索和变换均依赖位姿，估计误差累积可能影响性能，尤其在非抓取（non‑prehensile）任务中更困难。
- **当前范围有限**：仅考虑单物体接触操作，未涉及多物体、可变形物体或非接触交互。
- **缺少真实机器人验证**：所有实验在模拟器中进行，实际机器人部署的泛化性未知。
- **部分任务性能未领先**：在 Can state 设置下略低于扩散策略（0.95 vs 0.98）；但文中指出了统计波动。
- **位姿估计通用性未充分证明**：自监督方法仅在 Robomimic 的固定场景中验证，对更复杂物体或未见过场景可能失效。

（完）
