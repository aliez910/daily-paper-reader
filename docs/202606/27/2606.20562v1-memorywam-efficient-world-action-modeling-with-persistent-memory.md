---
title: "MemoryWAM: Efficient World Action Modeling with Persistent Memory"
title_zh: "MemoryWAM: 具有持久内存的高效世界动作建模"
authors: "Sizhe Yang, Juncheng Mu, Tianming Wei, Chenhao Lu, Xiaofan Li, Linning Xu, Zhengrong Xue, Zhecheng Yuan, Dahua Lin, Jiangmiao Pang, Huazhe Xu"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20562v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 带持久记忆的世界动作模型用于机器人操纵
tldr: 现有世界动作模型在高效推理与长程记忆之间存在权衡：基于有限窗口的方法在非马尔可夫环境中表现不佳，而保留完整历史的方法则导致计算开销随序列长度增长。MemoryWAM采用混合记忆设计，结合近期帧、事件边界锚定帧和压缩gist tokens，通过定制注意力机制同时检索详细短期上下文和压缩长期上下文。在长视野、依赖记忆的机器人操作任务中，MemoryWAM在模拟和真实世界中均优于强视觉语言动作模型和世界动作模型基线，同时显著降低推理延迟和GPU内存使用。该方法为机器人操作提供了一种高效且能应对非马尔可夫环境的建模范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20562v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1417, \"height\": 529}, {\"url\": \"assets/figures/arxiv/2606.20562v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1415, \"height\": 587}, {\"url\": \"assets/figures/arxiv/2606.20562v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 717, \"height\": 695}, {\"url\": \"assets/figures/arxiv/2606.20562v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1424, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2606.20562v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 831, \"height\": 360}, {\"url\": \"assets/figures/arxiv/2606.20562v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 733, \"height\": 838}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20562v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1096, \"height\": 521}, {\"url\": \"assets/tables/arxiv/2606.20562v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 618, \"height\": 147}, {\"url\": \"assets/tables/arxiv/2606.20562v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1445, \"height\": 215}]"
motivation: 现有世界动作模型在高效推理与处理非马尔可夫环境之间难以兼顾，需要一种能高效利用长程记忆的方法。
method: 提出混合记忆设计，融合近期帧、事件边界锚定帧和压缩gist tokens，并设计定制注意力机制以检索短期和长期上下文。
result: 在长视野依赖记忆的操纵任务中，MemoryWAM优于VLA和WAM基线，推理延迟和GPU内存更低。
conclusion: MemoryWAM实现了高效且长程记忆的世界动作建模，为机器人操作提供了一种计算有效的解决方案。
---

## 摘要
现实世界中的鲁棒机器人操作不仅需要理解当前观测，还需要记忆和动力学建模。世界动作模型（WAMs）通过联合建模视觉前瞻和动作，并以当前和历史观测为条件，具备了这些能力，使其成为机器人操作的一个有前景的范式。然而，现有的WAM面临一个基本的权衡：具有高效推理的方法通常仅以最近观测的有界窗口为条件，因此在非马尔可夫环境中表现不佳；而保留长历史记录的方法则会随着序列长度大幅增加时间和空间成本。为了应对这一挑战，我们引入了MemoryWAM，一种具有高效持久内存的世界动作模型。MemoryWAM采用了一种混合内存设计，结合了最近帧、事件边界锚帧和总结长程历史的紧凑摘要标记。一种定制的注意力机制能够同时检索详细的短期上下文和压缩的长期上下文，支持依赖记忆的决策制定，同时降低推理延迟和GPU内存使用。在仿真和现实世界的长期、依赖记忆的操作任务中，MemoryWAM优于强大的视觉-语言-动作（VLA）和WAM基线，同时保持了良好的计算效率。

## Abstract
Robust robotic manipulation in the real world requires not only an understanding of the current observation, but also memory and dynamics modeling. World action models (WAMs) possess these capabilities by jointly modeling visual foresight and actions conditioned on both current and historical observations, making them a promising paradigm for robotic manipulation. However, existing WAMs face a fundamental trade-off: methods with efficient inference typically condition only on a bounded window of recent observations and therefore struggle in non-Markovian environments, whereas methods that preserve long histories incur time and space costs that grow substantially with sequence length. To address this challenge, we introduce MemoryWAM, a world action model with efficient persistent memory. MemoryWAM uses a hybrid memory design that combines recent frames, event-boundary anchor frames, and compact gist tokens that summarize long-range history. A tailored attention mechanism enables retrieval of both detailed short-term context and compressed long-term context, supporting memory-dependent decision-making with reduced inference latency and GPU memory usage. Across long-horizon, memory-dependent manipulation tasks in both simulation and the real world, MemoryWAM outperforms strong vision-language-action (VLA) and WAM baselines while maintaining favorable computational efficiency.

---

## 论文详细总结（自动生成）

以下是根据论文内容生成的结构化详细总结：

- **1. 论文的核心问题与整体含义（研究动机和背景）**
  - **核心问题**：现有世界动作模型（World Action Models, WAMs）在高效推理和长程记忆保持之间面临根本权衡。基于固定大小滑动窗口的方法推理效率高，但无法处理非马尔可夫环境（即任务关键信息落在当前窗口外）；而保留完整历史KV缓存的方法可以维持长程上下文，但其延迟和GPU内存消耗随轨迹长度线性增长，难以扩展至长时任务。
  - **整体含义**：本文提出MemoryWAM，一种具有高效持久记忆的世界动作模型。通过模仿人类记忆的互补系统（短期记忆、事件边界记忆、长时抽象记忆），在几乎不增加推理成本的前提下实现非马尔可夫决策，为长期机器人操作提供计算可行的建模范式。

- **2. 论文提出的方法论：核心思想、关键技术细节**
  - **核心思想**：采用**混合记忆（Hybrid Memory）** 设计，结合三种互补的记忆形式：
    - **短期记忆**：固定大小滑动窗口（最近的4帧），保留高保真局部上下文用于即时控制。
    - **事件边界记忆（Anchor Frames）**：保留任务开始时锚定帧的全部视觉token（前2帧），因为初始场景常含关键任务信息且之后可能被遮挡或移出窗口。
    - **Gist记忆**：使用少量可学习的gist token（每帧8个，视觉token共120个，压缩比15×）压缩长程历史。后续帧不再保留全部视觉token，仅保留gist token的KV缓存，使长程缓存复杂度从O(NL)降至O(NM)（M≪L）。
  - **关键技术细节**：
    - **架构**：由视频DiT（预训练Wan2.2-TI2V-5B，约5B参数）和动作DiT（约1B参数）通过Mixture-of-Transformers方式组织。训练时视频DiT通过视频预测提供动力学监督，推理时跳过视频生成，只通过一次前向更新视频侧KV cache，动作DiT基于cache进行去噪输出动作块。
    - **注意力机制**：定制注意力掩码（图3）：当前帧和动作token可以完整访问短期窗口和锚帧的全部token，但对历史帧仅访问其对应的gist token，从而大幅降低计算量。
    - **训练**：采用连续流匹配（flow-matching），视频和动作分支的噪声调度分别使用shifted logit-normal分布（shift=5.0和1.0）。训练时模拟推理时的混合记忆注意力掩码，并使用自强制（self-forcing）策略防止teacher forcing过拟合。
  - **流程**：在每个时间步，编码当前观测获得视频latent，通过视频DiT更新cache（短期和gist token的KV），然后动作DiT以噪声动作token为查询，与混合记忆cache交叉注意力，去噪后输出16步动作块。执行动作后，采样4个子帧构成下一个条件帧。

- **3. 实验设计：数据集/场景、benchmark、对比方法**
  - **模拟实验**：使用**RMBench**基准，包含9个长期、依赖记忆的双臂操纵任务（如Observe and Pick Up, Rearrange Blocks, Press Button等），每个任务提供50个专家演示，评估100次rollout的成功率。
  - **真实实验**：设计两个任务：
    - **Shell Game**：机器人需在人类交换杯子后，识别并拾起盖住小方块的杯子（20次试验）。
    - **Look and Press**：机器人需观察桌上数字，根据数字按压左右按钮相应次数，最后按压后部按钮结束（20次试验）。
  - **对比方法**：
    - VLA基线：π0.5（直接图像到动作映射）。
    - 有界窗口WAM：FastWAM（仅依赖最近观测窗口）。
    - 全历史WAM：LingBot-VA（保留完整历史KV cache）。
    - 额外记忆机制对比：Full Attention, Test-Time Training (TTT), Recurrent Neural Networks (RNNs)（基于单层修改）。
  - **消融实验**：在Cover Blocks和Press Button两个任务上，分别移除anchor frames、gist tokens、sliding window，并与full attention和完整MemoryWAM对比。

- **4. 资源与算力**
  - **训练**：使用8张GPU（未明确型号），per-GPU batch size为1，总参数量约6B（视频DiT 5B + 动作DiT 1B）。采用bfloat16混合精度、FSDP（完全分片数据并行）、激活检查点，优化器为AdamW（学习率2e-4）。未说明训练步数或时长。
  - **推理**：真实实验部署于单张NVIDIA RTX 4090，控制频率10 Hz，块间延迟约0.3秒。动作去噪步数：模拟50步，真实10步。
  - **注意**：论文未明确给出总训练时间，但在附录提到部分任务使用了RoboTwin数据集预训练或动作历史以实现公平对比。

- **5. 实验数量与充分性**
  - **数量**：模拟9任务各100 rollout（共900次测试），真实共40次试验（每个任务20次）；消融实验在2个任务上各100 rollout；记忆机制比较在单任务上。此外进行了注意力层维度的延迟/内存测量。
  - **充分性与公平性**：
    - 对比方法覆盖了主流范式（VLA、有界WAM、全历史WAM）。
    - 与LingBot-VA对比时，在部分任务（Swap T, Observe and Pick Up）通过增加动作历史和预训练来确保公平（附录A.1说明）：不加任何额外trick时MemoryWAM仍优于LingBot-VA（如Observe and Pick Up 5% vs 3%）。
    - 消融实验系统验证了每个记忆组件的贡献。
    - 不足：真实任务仅2个，每个仅20次试验，统计显著性有限；基准RMBench的演示数据量较小（50个），可能限制了方法的泛化能力评估。

- **6. 论文的主要结论与发现**
  - **主要结论**：MemoryWAM在长期、依赖记忆的操纵任务上，成功率平均达83.0%（RMBench），显著优于无记忆方法（约10%）和有界窗口WAM（约6%），并超越全历史WAM（LingBot-VA 78.2%）。
  - **关键发现**：
    - 全历史缓存并非最优：保留全部历史可能引入冗余信息（Full Attention在部分任务上低于混合记忆，如Cover Blocks 96% vs 98%）。
    - Gist tokens是维持长程记忆的关键（去掉gist tokens导致Press Button成功率从87%降至5%）。
    - 混合记忆的压缩比有效（15×），在1,600帧轨迹下仍比TTT/RNN更高效（图4）。
    - 人类启发的记忆结构（短期+事件边界+抽象）对机器人非马尔可夫决策非常有效，且带来了计算收益。

- **7. 优点**
  - **方法论创新**：首次将类人混合记忆系统引入世界动作模型，在不牺牲性能的前提下大幅降低计算开销（时间/空间复杂度从O(N)降至O(N/d)）。
  - **性能与效率兼顾**：在模拟和真实环境中均超越强基线，且推理延迟和GPU内存显著低于全历史方法；与TTT/RNN等常复杂度方法相比，在长轨迹上仍然更快、内存更省。
  - **系统性的记忆研究**：不仅提出方法，还设计了全面的记忆机制对比（Full Attention, TTT, RNN, Hybrid），提供了latency、memory、success rate三维度的量化比较。
  - **训练策略实用**：利用视频预测作为训练监督，但推理时完全跳过生成，结合self-forcing、混合注意力掩码等trick，缩小了train-test gap。
  - **真实场景验证**：在真实机器人平台上成功部署，实现10Hz控制频率，验证了方法的实际可用性。

- **8. 不足与局限**
  - **语义理解有限**：论文明确指出，MemoryWAM继承视频扩散模型的局限性，缺乏高级语义理解和推理能力，在涉及复杂语言指令或抽象推理的任务上可能不够。
  - **数据需求**：实验基于50-100个演示，对于新任务的泛化性还需更多验证；预训练视频模型（Wan2.2）依赖较大，可能不易移植到其他视觉编码器。
  - **场景规模**：模拟仅使用RMBench（9任务），真实仅2个专门设计的记忆任务，任务多样性有限，难以全面评估在更广泛操作场景（如可变形物体、意外干扰等）下的表现。
  - **推理实时性**：虽然比全历史方法快，但块间仍有~0.3s延迟，对于需要更高频率反馈的任务可能有风险。
  - **混合记忆超参固定**：窗口大小、gist数量、锚帧数量等未做深入调优，这些超参在不同任务上可能影响效果。
  - **未来方向**：作者建议结合双系统架构（如Hi Robot）或统一多模态模型以增强语义能力，表明当前方法在语言理解和组合推理上仍有提升空间。

（完）
