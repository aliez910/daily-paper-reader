---
title: Decoupled Object-Centric Video Understanding for Generating Robotic Manipulation Commands
title_zh: 用于生成机器人操作指令的解耦式以物体为中心的视频理解
authors: "Thanh Nguyen Canh, Thanh-Tuan Tran, Haolan Zhang, Ziyan Gao, Xiem HoangVan, Nak Young Chong"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16470v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 从演示视频生成操作命令的对象中心视频理解
tldr: "针对机器人操作命令生成中因无法识别功能对象导致命令模糊的问题，提出解耦对象中心的视频理解框架，将动作识别与对象识别分离。利用TSM高效分类动作，通过轨迹角色分类、模糊检测和重叠最小化的对象选择算法定位关键对象，再用VLM零样本识别。在修改的Something-Something V2数据集上，动作分类准确率86.79%，命令生成BLEU-4对新物体提升143.9%，显著优于基线和通用VLM。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16470v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1705, \"height\": 679, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16470v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 836, \"height\": 993, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16470v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1757, \"height\": 1374, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16470v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 798, \"height\": 114, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16470v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 894, \"height\": 751, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16470v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 828, \"height\": 174, \"label\": \"Table\"}]"
motivation: 现有视频指令生成方法难以准确识别动作中的关键对象，导致命令歧义。
method: 解耦动作与对象识别：TSM分类动作，对象选择算法提取关键对象，VLM识别类别。
result: "动作分类准确率86.79%，命令生成BLEU-4在标准及新对象上达0.337/0.261，提升显著。"
conclusion: 提出模块化对象中心框架，有效生成精确、无歧义的机器人操作命令。
---

## 摘要
将视频演示转换为可执行的机器人指令仍然具有挑战性，因为现有方法往往无法识别出演示动作中哪些物体是功能相关的。因此，它们可能生成语言上可行但操作上模糊的指令。我们提出了一种以物体为中心的视频理解框架，该框架将动作识别与物体识别解耦，从而生成精确且无语法约束的操作指令。我们的方法整合了用于高效时空动作分类的时间移位模块（TSM）以及一种新颖的物体选择（Object Selection）算法，该算法通过基于轨迹的角色分类、模糊检测和重叠最小化来识别任务相关物体。随后，所选物体由视觉-语言模型（VLM）进行处理，以实现稳健的类别识别和零样本泛化。在修改后的Something-Something V2数据集上评估，我们的方法在动作分类准确率达到86.79%，在标准物体上的BLEU-4分数为0.337，在新颖物体上为0.261。这些结果分别比最强任务特定基线提高了80.2%和143.9%。在METEOR和CIDEr指标上观察到了更大的提升，在新颖物体上分别达到157.9%和171.7%。在所有语义指标上，我们的方法始终优于任务特定方法，并且与大型通用视觉-语言模型相比具有竞争力甚至超越，同时保持了模块化的以物体为中心的设计。

## Abstract
Translating video demonstrations into executable robot commands remains challenging because existing methods often fail to identify which objects are functionally involved in the demonstrated action. As a result, they may generate commands that are linguistically plausible but operationally ambiguous. We propose an object-centric video understanding framework that decouples action recognition from object identification to generate precise, grammar-free manipulation commands. Our approach integrates Temporal Shift Modules (TSM) for efficient spatio-temporal action classification with a novel \textbf{Object Selection} algorithm that identifies task-relevant objects through trajectory-based role classification, blur detection, and overlap minimization. The selected objects are then processed by Vision-Language Models (VLMs) for robust category recognition and zero-shot generalization. Evaluated on a modified Something-Something V2 dataset, our method achieves 86.79\% action classification accuracy and BLEU-4 scores of 0.337 on standard objects and 0.261 on novel objects. These results improve over the strongest task-specific baseline by 80.2\% and 143.9\%, respectively. Larger gains are observed in METEOR and CIDEr, reaching 157.9\% and 171.7\% on novel objects. Across all semantic metrics, our approach consistently outperforms task-specific methods and remains competitive with, or surpasses, large general-purpose VLMs while retaining a modular, object-centric design.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有的视频到机器人指令生成方法在理解演示视频时，无法准确识别出**哪些物体是执行动作时所涉及的功能相关物体**，导致生成的指令虽然在语言上合理，但在操作上存在歧义（例如，动作正确但物体引用错误）。
- **研究动机**：机器人操作指令生成需要在细粒度上同时明确“执行什么动作”和“操作哪个物体”，而通用视频理解方法缺乏物体级别的精度，任务特定方法（如V2CNet、Watch-and-Act）也难以在杂乱场景或遮挡下可靠地区分功能物体与无关物体。
- **整体含义**：本文提出将**动作识别与物体识别解耦**的以物体为中心的视频理解框架，旨在生成精确、无语法约束的操作指令，为机器人执行提供清晰且稳健的接口。

### 2. 论文提出的方法论：核心思想、关键技术细节及算法流程

- **核心思想**：构建**双分支解耦架构**，分别处理动作分类和物体识别，最后将结果拼接为结构化命令（动作⊕物体）。动作分支专注于时空特征聚合，物体分支则通过运动轨迹、质量过滤和VLM实现任务相关物体的定位与类别识别。

- **关键技术细节**：
    1. **动作表示分支 (ARB)**：
        - 输入：下采样后的视频帧序列 $\tilde{F}$。
        - 骨干网络：ResNet-50 + **时间移位模块 (TSM)**，实现跨帧信息交换而不增加参数量。TSM将部分通道沿时间维度移位（公式3），并采用残差分支移位策略保持主路径不变。
        - 分类：经时序池化得到视频表示 $z$，送入线性分类器+softmax，使用交叉熵损失训练（公式4），输出10种操作基元（如 pick、put、push等）。
    2. **物体感知分支 (OAB)**：
        - **关键帧提取**：基于帧间差异的显著性分数（公式6），通过阈值和Top-M选择变化剧烈的帧（公式5、7），避免模糊或静止帧。
        - **检测与跟踪**：使用HOI检测器识别与人手交互的物体，并进行跨帧跟踪，生成候选物体集 $O$。
        - **物体选择算法**（核心贡献）：
            - **阶段一：功能角色分类**：计算每个物体的总位移 $D(o)$（公式8上）和垂直变化方差 $V(o)$（公式8下），依据阈值 $\tau_D, \tau_V$ 划分到**可拾取**（$P_1$，位移大、垂直变化大）或**可放置**（$P_2$，相对静止）类别（公式9），主要针对转移动作（pick、move、put等）。
            - **阶段二：实例质量选择**：在每个类别内，计算模糊评分（拉普拉斯响应方差）和手部重叠IoU，通过加权组合选择最清晰且遮挡最小的实例（公式10）。
        - **VLM识别**：对所选物体裁剪图使用固定提示“What object is shown in this image? Respond with only the object name.”，由VLM（LLaVA）输出类别标签，实现零样本泛化。
    3. **命令生成**：最终命令 $S = A \oplus I$，采用无语法格式（如“put apple into bowl”），强调动作与物体核心信息。

### 3. 实验设计：数据集、场景、benchmark及对比方法

- **数据集**：
    - **修改的 Something-Something V2**：选取10种与机器人操作基元对应的动作类别，训练集118,562视频，测试集12,480视频。
    - **自录制视频**：135个视频，包含多样背景、光照和物体布置，用于评估真实世界泛化。
    - **物体集**：
        - **标准物体**：12个类别（训练中见过，如苹果、瓶、锅等）。
        - **新颖物体**：9个类别（训练中未见，零样本测试，如胡萝卜、葡萄、香蕉等）。

- **Benchmark**：动作分类（Top-1/Top-5准确率）和命令生成质量（BLEU-1~4、ROUGE-L、METEOR、CIDEr）。

- **对比方法**：
    - 任务特定方法：Video2Command、V2CNet、Watch-and-Act。
    - 通用大型VLM：ChatGPT-4o-mini、Qwen2.5-VL-3B、NVILA-8B、LLaVA-NeXT7B、Intern2.5VL-4B、Intern3.5VL-4B、DeepseekVL7B。所有基线VLM均被严格提示输出与本文一致的语法自由格式。

### 4. 资源与算力

- 文中未明确说明使用的 GPU 型号、数量及训练总时长，但在 **实现细节** 中给出了训练配置：
    - 动作分支使用 ResNet-50 + TSM，训练 50 个 epoch，SGD 优化器（momentum 0.9），初始学习率 0.01，余弦退火，batch size 64。
    - 物体分支的 HOI 检测器采用文献 [11] 的预训练模型，VLM（LLaVA）使用默认参数，未涉及额外训练。
- 整体计算资源需求相对适中（因 TSM 参数高效、VLM 仅用于推理），但具体开销未量化报告。

### 5. 实验数量与充分性

- **实验组数**：
    - 动作分类实验（表1）：基准测试集（Top-1 86.79% / Top-5 94.55%）和自录制视频（Top-1 68.15% / Top-5 94.81%）。
    - 命令生成实验（表2a/b）：分别在标准物体集和新颖物体集上与 10 种基线方法全面对比，涵盖 BLEU-1~4、ROUGE-L、METEOR、CIDEr 指标。
    - 消融实验（表3）：针对三个组件（关键帧提取、物体选择、VLM vs CNN）进行逐项剔除，报告标准/新颖物体上的 BLEU-4 损失。
    - 定性分析（图2、图3）：展示6种动作的示例输出及下游机器人执行结果。
- **充分性与公平性**：
    - 实验覆盖了标准、新颖、自录制多种场景，既测试了识别能力也验证了零样本泛化。
    - 对比方法包含任务特定和通用 VLM 两条路线，且要求所有 VLM 输出相同格式，保证了比较公平性。
    - 消融实验清晰验证了每个模块的贡献，尤其 VLM 替换为 CNN 时新颖物体性能下降 45.6%，有力地说明了 VLM 在零样本中的作用。
    - 总体实验设计系统、客观，结论可信。

### 6. 论文的主要结论与发现

- 所提出的解耦框架在动作分类上达到 **86.79% Top-1 准确率**，在命令生成上：
    - **标准物体**：BLEU-4 = 0.337，比最强任务特定基线 Watch-and-Act 提升 **80.2%**。
    - **新颖物体**：BLEU-4 = 0.261，比 Watch-and-Act 提升 **143.9%**；METEOR 和 CIDEr 分别提升 **157.9%** 和 **171.7%**。
    - 在所有语义指标上（BLEU、ROUGE-L、METEOR、CIDEr）均一致优于任务特定方法，并与通用大型 VLM 竞争甚至超越（尤其在高阶语义指标上）。
- **VLM 的零样本能力**是关键：当替换为 CNN 分类器时，新颖物体 BLEU-4 下降 45.6%；而直接对全帧使用 VLM 的基线（如 LLaVA-NeXT7B）效果较差（新颖 BLEU-4 仅 0.047）。
- **物体选择与关键帧提取**均至关重要：去除物体选择导致标准物体 BLEU-4 下降 29.7%；去除关键帧提取下降 11.6%。

### 7. 优点：方法或实验设计上的亮点

- **解耦设计的合理性**：动作识别需时序聚合，物体识别需局部细粒度分析，两者分离可各自选择最合适的技术（TSM + VLM），且便于独立优化与评估。
- **新颖的物体选择算法**：结合轨迹角色分类（位移+垂直方差）和质量过滤（模糊+重叠），无需复杂学习即可从杂乱交互中甄别功能物体，尤其适用于转移动作。
- **零样本泛化能力**：通过 VLM 对裁剪后的物体图识别，无需针对新物体重新训练，极大提升了框架在实际应用中的适应范围。
- **命令格式实用**：采用无语法约束的动作-物体组合，直接匹配机器人执行所需接口，避免完整句子产生的歧义。
- **实验对比全面**：既对比了专用小模型，也对比了多个最新通用 VLM，并统一输出格式，结果客观且有说服力。

### 8. 不足与局限

- **物体选择算法适用范围有限**：仅可靠处理最多两个功能物体交互的场景，当三个及以上物体同时出现时性能下降；严重手部遮挡或持续遮挡会影响轨迹估计。
- **自录制视频性能下降**：Top-1 准确率从 86.79% 降至 68.15%，表明存在域迁移问题（背景、视角差异），泛化能力仍有提升空间。
- **资源与速度未讨论**：未报告推理速度或实时性，也未评估 VLM 带来的计算/时间开销，对于真实机器人应用（需要低延迟）可能构成限制。
- **依赖外部模块质量**：物体检测与跟踪依赖 HOI 检测器 [11] 的预训练模型，其错误会传播至后续选择与识别。
- **实验未覆盖更多机器人操作场景**：仅在修改的 Something-Something V2 上验证，动作类别仅 10 种，未涉及更复杂或长程操作，局限性明确。
- **因果方向未探讨**：虽然解耦了动作与物体，但未分析两者之间的因果依赖关系（如动作本身可能暗示物体类型），可能丢失部分上下文线索。

（完）
