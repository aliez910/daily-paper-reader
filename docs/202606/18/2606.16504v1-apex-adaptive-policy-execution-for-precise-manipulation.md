---
title: "APEX: Adaptive Policy Execution for Precise Manipulation"
title_zh: APEX：面向精确操作的适应性策略执行
authors: "Mengfei Zhao, Chenxi Jiang, Tuo An, Jindou Jia, Jianfei Yang"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16504v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 自适应执行弥合模仿学习中的执行差距
tldr: "现代模仿学习策略（如visuomotor和VLA）输出高层动作参考，但缺乏底层控制动态感知，导致执行偏差。APEX作为即插即用框架插入策略与控制器之间，重构动态可行参考并依据低层状态反馈自适应调整，具有收敛保证。实验表明，APEX在演示重放中减少41.2%的跟踪误差，在四类策略上提升4.8-25.8个百分点的操作成功率。该方法将策略和控制器均视为黑盒，无需侵入性修改即可弥合执行差距，为精确操作提供高效补丁方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16504v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1698, \"height\": 975, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16504v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1564, \"height\": 601, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16504v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1722, \"height\": 650, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16504v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1721, \"height\": 477, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16504v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1427, \"height\": 793, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16504v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1699, \"height\": 391, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16504v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1455, \"height\": 1168, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16504v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1445, \"height\": 766, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16504v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1447, \"height\": 882, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16504v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1507, \"height\": 354, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16504v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 375, \"height\": 465, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16504v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 823, \"height\": 676, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16504v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1719, \"height\": 221, \"label\": \"Table\"}]"
motivation: 现有模仿学习策略与低层控制器之间存在执行差距，且先前解决方案需对策略或控制器进行侵入性修改，不适用于两者均为黑盒的常见场景。
method: APEX是即插即用框架，通过在策略与控制器间重构动态可行参考并依据低层状态反馈自适应调整来弥合执行差距，具有收敛保证。
result: "APEX在演示重放中减少41.2%的跟踪误差，并在四类策略上提升4.8-25.8个百分点的操作成功率。"
conclusion: APEX是一种非侵入式的即插即用方案，将策略和控制器视为黑盒，有效弥合执行差距，提升精确操作性能。
---

## 摘要
现代模仿学习方法，包括视觉运动策略和视觉-语言-动作（VLA）策略，通常输出高层动作参考，由低级控制器执行。然而，缺乏高阶参考信号，加上策略在训练过程中对低级控制动态缺乏感知，不可避免地导致执行差距。因此，实际动作与策略命令动作存在系统性偏差，对精度敏感的操作产生关键影响。先前的工作要么修改策略架构，要么修改低级控制器，两者都需要对预训练策略或封装控制器进行侵入式更改。这自然引发一个问题：当策略和控制器都被视为不可访问的黑盒时，我们能否弥合执行差距？我们提出适应性策略执行（APEX）框架，这是一个插入策略和控制器之间的即插即用框架，从策略输出重建动态可行的参考，并在测试时根据低级状态反馈进行自适应，具有可证明的收敛保证。广泛的经验研究表明，APEX在演示回放中将控制器引起的跟踪误差减少了41.2%，并在四种视觉运动策略和VLA策略类别上，将操作成功率提高了4.8至25.8个百分点。

## Abstract
Modern imitation learning methods, including visuomotor and Vision-Language-Action (VLA) policies, typically output high-level action references that are executed by low-level controllers. However, the absence of higher-order reference signals, together with the policy's lack of awareness of the underlying low-level control dynamics during training, inevitably induces an execution gap. As a result, realized actions deviate systematically from policy-commanded ones, with a critical impact on precision-sensitive manipulation. Prior work either modifies the policy architecture or the low-level controller, both requiring intrusive changes to the pretrained policy or packaged controller. This raises a natural question: when the policy and controller are both treated as inaccessible black boxes, can we bridge the execution gap? We propose Adaptive Policy Execution (APEX), a plug-and-play framework inserted between the policy and the controller that reconstructs a dynamically feasible reference from policy outputs and adapts at test-time according to low-level state feedback, with a provable convergence guarantee. Extensive empirical studies show that APEX reduces controller-induced tracking error by 41.2% on demonstration replay and improves manipulation success by 4.8--25.8 percentage points across four visuomotor and VLA policy classes.

---

## 论文详细总结（自动生成）

```markdown
### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现代模仿学习策略（如视觉运动策略和 VLA 模型）通常输出高层动作参考（例如关节角度或末端执行器位姿），然后由底层控制器（如 PD 控制器）跟踪执行。然而，由于策略输出缺乏速度、加速度等高阶信号，且训练过程中未考虑底层控制动态，导致测试时实际执行的动作与策略指令之间存在系统性偏差，即 **执行差距（Execution Gap）**。在精度敏感的操作任务中，即使微小的跟踪误差也会导致任务失败。
- **背景**：现有解决方案要么修改策略架构（如输出 B 样条轨迹或扭矩），要么修改底层控制器（如学习残差校正），都需要对预训练策略或封装控制器进行侵入式修改，在实际部署中往往不可行（策略和控制器均为黑盒）。
- **研究动机**：受生物运动控制中快速感觉运动反馈回路的启发，论文提出在策略和控制器之间插入一个在线校正层，在不改动任何一方的前提下弥合执行差距。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：APEX（Adaptive Policy Execution）是一个即插即用的测试时执行适配器。它通过动态滤波器从策略输出重建平滑且动态可行的参考轨迹，提供控制器所需的高阶信号；并通过在线自适应算法根据实时跟踪误差更新校正系数，补偿未知的系统动力学和控制器参数。
- **关键技术细节**：
    1.  **动态参考重建**：利用自适应被动滤波器从原始策略动作 \(a_{\pi}\) 生成平滑参考 \(\bar{a}\) 及其一阶和二阶导数，避免直接差分引入噪声。
        \[
        \dot{y} = -K_1 y - K_1 e_a, \quad \dot{\bar{a}} = -K_2 y
        \]
        其中 \(e_a = a_{\pi} - a\) 为跟踪误差，\(K_1, K_2 > 0\) 为滤波器增益。
    2.  **测试时自适应校正**：构建校正命令 \(a_r\)，包含基于参考轨迹的前馈项和基于跟踪误差的反馈项：
        \[
        a_r = \bar{a} + w[\hat{J}\ddot{\bar{a}} + \alpha e_a] + w_d \dot{\bar{a}}
        \]
        其中未知参数 \(w, w_d, J\)（对应 \(1/K_p, K_d/K_p\) 和惯性）通过在线自适应律更新：
        \[
        \dot{\hat{w}} = -\Gamma_w s [\hat{J}\ddot{\bar{a}} + \alpha e_a], \quad \dot{\hat{w}}_d = -\Gamma_{w_d} s \dot{\bar{a}}, \quad \dot{\hat{J}} = -\Gamma_J s \ddot{\bar{a}}
        \]
        其中 \(s = \dot{a} - \dot{\bar{a}}\) 为滑动变量，\(\Gamma\) 为学习率。
    3.  **稳定性保证**：定理 1 证明，在有限扰动下，APEX 能保证所有闭环信号有界，且跟踪误差收敛到以扰动界为半径的残差集内。

- **算法流程**：
    - 输入：策略参考 \(a_{\pi}\)，机器人状态反馈 \(a, \dot{a}\)
    - 步骤 1：通过滤波器计算 \(\bar{a}, \dot{\bar{a}}, \ddot{\bar{a}}\)
    - 步骤 2：根据跟踪误差更新自适应参数 \(\hat{w}, \hat{w}_d, \hat{J}\)
    - 步骤 3：生成校正命令 \(a_r\) 发送给底层控制器
    - 步骤 4：重复以上步骤，实现在线自适应。

### 3. 实验设计：数据集/场景、Benchmark、对比方法

- **数据集与场景**：
    - **模拟环境**：ManiSkill（PushCube, PickCube, PlaceSphere, PegInsertionSide），LIBERO Spatial（3 个拾取放置任务）。
    - **真机环境**：UR5 机械臂 + Robotiq 夹爪，任务包括 PushBox, PickCube, PegInsertion（径向间隙 3mm）。
- **Benchmark**：以任务成功率和动作跟踪误差（关节级或末端执行器级）作为评估指标。在专家演示回放场景中，使用专家轨迹作为参考，隔离策略预测误差。
- **对比方法**：主要对比 **Baseline**（直接将策略输出或专家参考送入底层控制器执行，无 APEX 校正）。文中还比较了 **固定参数校正**（无在线自适应）来验证自适应机制的有效性。未与其他执行差距修正方法（如时序集成残差学习）进行直接横向对比，但相关工作章节有提及。

### 4. 资源与算力

- 论文明确提及真机实验使用一台配备 **NVIDIA RTX 3090** 的工控机进行策略推理和 APEX 校正计算。
- **训练阶段**：策略（ACT, Diffusion Policy, Flow Matching）使用公开数据或自行采集的 60 个演示进行训练，但未详细说明训练所需的 GPU 数量、时长或具体算力配置。仅提及策略本身为通用方法，未涉及 APEX 的训练开销（APEX 无需额外训练）。

### 5. 实验数量与充分性

- **专家回放（隔离执行差距）**：4 个模拟任务，APEX 减少 41.2% 跟踪误差，成功率从 62.5% 提升至 81.3%。
- **ACT 策略（全控制器网格）**：4 个任务 × 12 组 \(K_p \times K_d\) 参数 = **48 组设置**，每组 100 回合，共约 4800 回合，APEX 在 40 组中提升成功率，3 组下降，5 组不变。
- **跨策略比较（ManiSkill）**：3 种策略（ACT, Diffusion Policy, Flow Matching）在 4 个任务上，表 1 报告了成功率，共 12 组对比，观察到了 4.8–25.8 pp 的提升。
- **VLA 策略（π0.5 on LIBERO）**：3 个任务 × 4 组控制器比例增益，每个设置 20 回合，成功率从 72.1% 提升至 97.9%。
- **真机验证（UR5）**：2 种策略（FM, DP）在 3 个任务上，各 20 回合，共 12 组实验，插入任务提升最显著（FM 从 20% 至 35%）。
- **消融实验**：
    - 固定校正 vs 自适应：验证自适应更新的必要性（自适应优于固定最优）。
    - 物理扰动鲁棒性（关节摩擦系数、转子电枢等效惯量）：APEX 在较大扰动范围内保持高成功率。
- **充分性与客观性**：实验覆盖了确定性策略（ACT）和生成式策略（Diffusion, Flow Matching），以及 VLA 模型（π0.5）；包含了模拟和真机验证；控制器参数进行了系统扫描。但不足在于未与现有的执行差异修正方法（如测试时平滑、残差学习）进行直接比较，且真机实验样本量相对较小（每设置 20 回合），统计显著性可进一步加强。

### 6. 论文的主要结论与发现

1.  **执行差距主要来自底层控制器**：即使使用专家演示轨迹作为参考，不匹配的 PD 控制器也会产生显著跟踪误差，导致任务失败，而 APEX 能有效减小该误差。
2.  **APEX 通用性强**：在 ACT、Diffusion Policy、Flow Matching 和 π0.5 四类策略上均能稳定提升成功率，且不依赖特定策略架构。
3.  **自适应优于固定补偿**：在线自适应比任何固定校正系数都更有效，能够动态适应不同的控制器参数和物理扰动。
4.  **理论保证与实际效果一致**：定理 1 的收敛性在实验中得到验证，跟踪误差被限制在有界范围内。
5.  **精度敏感任务增益最大**：在接触密集的 PegInsertion 任务（3mm 间隙）中，APEX 带来的成功率提升最为明显。

### 7. 优点：方法或实验设计上的亮点

- **方法创新性**：APEX 将策略和控制器均视为不可修改的黑盒，仅在其间插入轻量级在线校正层，高度适配工业场景，实用性强。
- **理论基础扎实**：提供了 Lyapunov 稳定性分析，证明跟踪误差的收敛性，增强了方法可信度。
- **实验设计严密**：
    - 通过“专家回放”实验巧妙分离了策略预测误差和控制器执行误差，确认了执行差距的存在性与严重性。
    - 对控制器参数（\(K_p, K_d\)）进行系统网格扫描，验证了 APEX 在不同控制器配置下的鲁棒性，避免过拟合某一特定设置。
    - 跨多种策略类别、模拟器和真实平台验证，增强了结论的泛化性。
- **消融充分**：自适应 vs 固定校正、物理参数扰动等消融实验有力支撑了核心设计选择。
- **工程友好**：APEX 只需访问策略输出的参考信号和机器人的状态反馈（位置、速度），实现简单，计算开销低。

### 8. 不足与局限

- **增益手工调参**：APEX 包含多个自适应增益（\(\Gamma_w, \Gamma_{w_d}, \Gamma_J\) 和滤波器增益 \(K_1, K_2\)），目前需要人工调整，论文计划未来实现自动调参。
- **扰动项被忽略**：公式推导中省略了集总扰动 \(d(t)\)，导致收敛到有界残差集而非零误差，可能限制极度高精度任务的表现。
- **控制器类型有限**：仅针对 PD 控制器设计和验证，未扩展至更复杂的控制器（如阻抗控制、MPC），限制了在其他控制栈上的直接适用性。
- **对比基线不够全面**：未与现有的测试时校正方法（如动作块后处理、残差学习）进行实验对比，无法量化 APEX 相对于这些方案的额外收益。
- **实机实验规模较小**：每个任务-策略组合仅 20 次试次，可能不足以捕捉性能差异的统计显著性，且任务种类仅三种，泛化性需更多验证。
- **假设条件**：理论分析假设策略参考在分析间隔内固定，但实际中策略输出可能频繁变化，可能导致间隔内收敛不足，但实验表明有限时间误差已显著减小。

（完）
```
