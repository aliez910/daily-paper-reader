---
title: "Decoupling Semantics and Geometric Grounding: Spatial Visual Prompts for Language-Conditioned Imitation Learning"
title_zh: 解耦语义与几何基础：用于语言条件模仿学习的空间视觉提示
authors: "Yanzhe Tang, Xinyu Shao, Yuxuan Hu, Siyu Chen, Bowen Yang, Yajun Gao, Tongtong Cao, Xiu Li, Long Zeng"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25360v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 使用空间视觉提示的语言条件模仿学习解耦架构
tldr: "当前模仿学习中端到端的视觉-语言-动作模型将语义推理与空间控制耦合，引发目标歧义对齐瓶颈，造成数据效率低下。SVP-IL解耦架构借助视觉语言基础模型，将语言指令解析为零样本几何掩码作为空间视觉提示，并通过轻量级直接特征融合机制注入连续动作生成器，由此提供明确无偏的空间梯度指导。在仅50至100条演示训练下，SVP-IL在高度歧义的语言条件任务上平均成功率从24.0%提升至39.5%，标准基准达到67.8%，显著超越现有视觉-语言-动作模型及纯视觉运动基线。真实世界机器人实验进一步验证了其在非结构化环境中的稳健性与数据高效性，证明了解耦架构在语言条件模仿学习中的有效性与潜力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25360v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 513}, {\"url\": \"assets/figures/arxiv/2606.25360v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1817, \"height\": 754}, {\"url\": \"assets/figures/arxiv/2606.25360v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 813, \"height\": 833}, {\"url\": \"assets/figures/arxiv/2606.25360v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 899, \"height\": 872}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25360v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 908, \"height\": 822}, {\"url\": \"assets/tables/arxiv/2606.25360v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 908, \"height\": 808}, {\"url\": \"assets/tables/arxiv/2606.25360v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 907, \"height\": 868}, {\"url\": \"assets/tables/arxiv/2606.25360v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 907, \"height\": 294}]"
motivation: 解决端到端VLA模型中语义与空间耦合导致的对齐瓶颈，实现数据高效的模仿学习。
method: 利用视觉语言模型生成零样本几何掩码作为空间视觉提示，通过直接特征融合注入连续动作生成器，解耦语义与几何。
result: "在50-100演示下，SVP-IL在歧义任务上平均成功率从24.0%提升至39.5%，标准基准达67.8%。"
conclusion: SVP-IL以解耦架构实现数据高效且鲁棒的机器人操作，验证了解耦在语言条件模仿学习中的有效性。
---

## 摘要
尽管端到端视觉-语言-动作（VLA）模型在机器人操作中展现出潜力，但其单一范式本质上将语义推理与空间控制耦合在一起。这造成了严重的对齐瓶颈，限制了在数据受限的模仿学习中对目标的精确消歧。为了解决这一问题，我们提出了SVP-IL，一种解耦架构，明确地从动作生成循环中提取空间视觉基础。通过利用视觉-语言基础模型，我们将指令解析为零样本几何掩码，将语言转化为显式的空间视觉提示（SVP）。这些先验信息通过轻量级的直接特征级融合机制注入到连续动作生成器中。这种集成提供了明确且无污染的空间梯度指导，同时确保在低数据场景下的高度稳定优化。大量实验表明，SVP-IL显著优于最先进的VLA模型和纯视觉运动基线。在仅使用50到100个演示样本训练的情况下，SVP-IL在高度模糊的语言条件任务上将平均成功率从24.0%提升至39.5%，在标准基准上达到67.8%。真实世界的机器人实验进一步验证了其在非结构化物理环境中的鲁棒性和数据效率。

## Abstract
While end-to-end Vision-Language-Action (VLA) models show promise in robotic manipulation, their monolithic paradigm inherently couples semantic reasoning and spatial control. This creates a severe alignment bottleneck, limiting precise target disambiguation in data-constrained imitation learning. To overcome this, we propose SVP-IL, a decoupled architecture that explicitly extracts spatial visual grounding from the action generation loop. By leveraging vision-language foundation models, we parse instructions into zero-shot geometric masks, translating language into explicit Spatial Visual Prompts (SVP). These priors are injected into a continuous action generator via a lightweight direct feature-level fusion mechanism. This integration provides explicit and uncorrupted spatial gradient guidance while ensuring highly stable optimization under low-data regimes. Extensive experiments demonstrate that SVP-IL significantly outperforms state-of-the-art VLAs and pure visuomotor baselines. Trained on as few as 50 to 100 demonstrations, SVP-IL improves average success rates on highly ambiguous language-conditioned tasks from 24.0% to 39.5%, achieving 67.8% on standard benchmarks. Real-world robotic experiments further validate its robustness and data efficiency in unstructured physical environments.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：端到端的视觉-语言-动作（VLA）模型将语义推理和空间控制耦合在一个单一网络中，导致**语义-空间对齐瓶颈**。在数据有限的模仿学习场景下，模型难以将抽象的语言指令（如“拿红色的苹果”）准确对应到图像中具体目标的像素位置，尤其在多物体、视觉相似或杂乱场景中，容易发生目标误判或抓取不精。
- **背景**：纯模仿学习方法（如 ACT、扩散策略）在单一任务中表现好，但面对语言条件任务需要隐式学习跨模态映射，数据效率极低；VLA 模型如 π0 虽引入大规模预训练，但语义指导能力仍然脆弱。现有提升空间感知的方法（如 3D 点云、热力图提示）往往计算复杂或需显式 3D 表示。
- **研究动机**：通过**解耦语义推理与几何定位**，将“理解指令”的任务交给专用的基础模型（LLM + SAM），再将该空间结果以适当方式注入动作策略，从而降低策略的学习负担，实现在极少演示下的精准目标消歧。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：设计一种解耦的感知-控制架构（SVP-IL），把“从语言到目标空间区域”的推理从连续动作生成回路中剥离出来，先利用视觉-语言基础模型生成抽象的**空间视觉提示（Spatial Visual Prompt, SVP）**，即可用的二进制几何掩码，再将这些提示通过特殊的融合机制注入到模仿学习策略（基于 Diffusion Policy）中。
- **关键技术细节**：
  - **两阶段 SVP 生成**：
    1. 使用大语言模型（LLM）解析语言指令 `l`，提取目标物体名称 `c`。
    2. 使用开放词汇分割模型 **SAM 3** 对当前 RGB 图像 `I_t` 和 `c` 进行零样本分割，输出二进制掩码 `M_t ∈ {0,1}^(H×W)`（即空间视觉提示）。
  - **提示注入模块**：避免常见的图像级叠加（如叠加掩码到 RGB 上或通道拼接）带来的域偏移和预训练权重破坏问题，提出**中间特征级直接融合**机制：
    - 主视觉编码器（如 ResNet-18）不变，保持其预训练的纹理特征提取能力。
    - 另设一个轻量侧路卷积网络（Side-CNN）将二进制掩码编码为与主编码器中间层同形状的特征图。
    - 在主编码器的某层（如对应尺寸层），将两个特征图通过 **逐元素相加**（element-wise addition）融合。这视为一种刚性结构偏置，提供强大而稳定的空间梯度引导，且无需额外可学习的门控，在低数据下不易过拟合。
  - **整体策略实例化**：
    - 动作生成器采用 **Diffusion Policy（DP）**，其条件包括融合后的视觉特征、本体感知状态 `q_t` 以及原始语言嵌入 `l`。
    - 最终输出的动作序列由其 1D U-Net 预测，并利用动作分块（action chunking）实现平滑执行。
- **算法流程（文字说明）**：
  1. 离线：利用 LLM 从指令中提取一次目标名称 `c`。
  2. 在每一时刻 t：获取 RGB 图像 → 使用 SAM 3 和 `c` 生成掩码 `M_t` → 掩码经 Side-CNN 得到提示特征 → 与主编码器中间层特征逐元素相加 → 融合后的特征与 proprioception、语言嵌入共同控制扩散策略 → 输出动作块。

## 3. 实验设计：数据集/场景、benchmark、对比方法

- **模拟环境**：RoboTwin 2.0 高保真双臂操作基准，包含 9 个典型任务（如 Adjust Bottle、Lift Pot、Click Bell）和 6 个**自定义语言条件任务**（如“将指定物体放入篮中”），用于显式测试语义消歧能力。评估时设有 **Clean（干净背景）** 和 **Randomized（杂乱背景/光照变化）** 两种视觉分布。
- **对比方法**：
  - 纯模仿学习基线：**ACT**（动作分块 Transformer）、**DP**（扩散策略）。
  - 端到端 VLA 基线：**π0**（基于流模型的最新 VLA 模型，全程微调）。
- **训练数据量**：标准任务 50 个演示/每个任务；自定义任务 100 个演示/每个任务。
- **真实世界实验**：部署于 **Aloha-AgileX 双臂机器人**，评测三个密集杂乱任务：**Table Setting**（颜色区分）、**Desktop Cleaning**（相似颜色不同语义）、**Bimanual Sorting**（根据空间位置选臂）。每任务 20 次试验，与 DP（无语言）和 π0 对比。
- **额外对比**：评估域随机化（DR）是否可替代 SVP：训练 DP 和 SVP-IL 在 Random 环境演示下的表现。

## 4. 资源与算力

- **文中未明确说明**所使用的 GPU 型号、数量或具体训练时长。仅提及“所有模型使用相同的训练配置”以及 π0 采用全参数微调。未提及硬件资源细节。
- 可能基于常见的分布式训练环境（如单卡或多卡 GPU），但无法从提供文本中得知算力投入。

## 5. 实验数量与充分性

- **实验组数**：
  - 模拟环境：9 标准任务 + 6 自定义任务，每个对 DP、ACT、π0、SVP-IL 分别评测（表 I）。
  - 域迁移实验：对比 DP 与 SVP-IL 在 Clean 和 Random 训练/评估组合下的成功率（表 II）。
  - 消融实验：对比三种融合方案（Pixel-Overlay, Early-Concat, 直接特征级融合）在基本任务和自定义任务上的效果（表 III）。
  - 真实实验：3 个任务 × DP、π0、SVP-IL 各 20 次试验 = 180 次试验（表 IV）。
- **充分性与公平性**：
  - 覆盖了标准基准和针对语义消歧的困难场景，并在不同视觉域下验证鲁棒性；同时包含了消融和实物验证，实验较全面。
  - 论文强调所有方法采用相同训练配置、相同演示数量，使对比相对公平。评价指标为成功率，定义清晰（需准确抓取+摆放）。
  - 不足之处：虽然任务多样，但模拟和真实环境都仅限于桌面双臂操作场景，未涉及更复杂的移动操作或室外环境。且针对消歧任务主要依赖单一 RGB 视图，可能受限于遮挡。

## 6. 论文的主要结论与发现

- **解耦架构更优**：将语义提取从动作策略中剥离，利用基础模型产生空间提示，显著提升了低数据条件下的目标消歧能力。SVP-IL 在标准任务上平均成功率 67.8%（vs. π0 53.7%），在自定义语义任务上 39.5%（vs. π0 24.0%）。
- **特征级融合优于图像级融合**：直接逐元素相加融合掩码特征到中间层，比图像叠加或通道拼接提升了约 15-20 个百分点，且不破坏预训练纹理先验。
- **数据高效**：仅需 50～100 条演示即可看到显著提升，且域迁移下仍优于传统域随机化训练的纯 DP。
- **真实世界有效**：在物理机器人的杂乱场景中达到 60% 平均成功率，大幅超越 DP（28.3%）和 π0（31.7%），体现了实际部署潜力。

## 7. 优点：方法或实验设计上的亮点

- **方法论亮点**：
  - 提出**解耦式感知-控制框架**，直接利用 SAM 3 等开放词汇模型的零样本能力，避免为每个任务重复训练视觉语义模块。
  - **直接特征级融合**简单而有效，既保留了预训练骨干特征，又避免引入额外可学习参数导致过拟合，在低数据下稳定性强。
  - 兼容现有动作生成器（如 Diffusion Policy），易于作为通用模块集成。
- **实验设计亮点**：
  - **自定义语义困难任务**直接暴露了端到端模型的弱点，很好地验证了方法的必要性。
  - 同时评估了在域随机化的表现，且与 DR 比较，证明解耦比单纯的数据增强更有效。
  - 包含了真实世界的平台迁移和不同机器人（Aloha-AgileX），体现了 Sim-to-Real 能力和跨硬件潜力。

## 8. 不足与局限

- **方法局限**：
  - **受上游模型精度限制**：SAM 3 对高反光、透明、严重遮挡物体可能生成不稳定掩码，导致策略空间精度下降。
  - **依赖显式目标指令**：无法处理抽象意图（如“整理桌子”），需要明确的物体名称才能初始化提示。
  - **计算开销**：使用 SAM 3 作为提示器带来了额外推理延迟，虽然通过下采样和分块缓解，但不太适用于高度动态的任务；可通过替换轻量分割模型优化。
- **实验覆盖局限**：
  - 仅在桌面双臂操作场景验证，未评估单臂或移动操作；物体类别和复杂度有限。
  - 未提及训练/推理的时间开销对比，也未系统分析不同 LLM（如简单规则 vs. 大模型）对最终效果的影响。
  - 真实实验样本数（每任务 20 次）相对不大，且可能存在未被列举的失败模式（如物体材质干扰影响分割）。
- **公平性风险**：使用的 SAM 3 和 LLM 直接生成掩码，而基线方法（如 π0）需要内部学习语义定位，这种“信息不对等”虽然是有意解耦的结果，但需要承认在提示提取步骤使用了额外的基础模型能力。

（完）
