---
title: "CoRDE: Concept-Prior Routed Diffusion Experts for Structural Generalization in Robot Manipulation"
title_zh: CoRDE：基于概念先验的路由扩散专家实现机器人操作中的结构泛化
authors: "Haidong Huang, Xixin Zhao, Yaohua Zhou, Jiayu Song, Jiayi Zhang, Jun Ma, Haiyue Zhu, Xiaocong Li"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21935v1"
tags: ["query:rob-il"]
score: 9.0
evidence: 基于概念先验的扩散专家用于操纵模仿学习
tldr: 扩散模型在多任务和长期机器人操作中缺乏结构泛化，子阶段梯度冲突严重；混合专家(MoE)方法常导致路由崩溃和参数爆炸。本文提出Concept-prior Routed Diffusion Experts (CoRDE)，一种结构引导的变分蒸馏框架，从冻结概念编码器提取语义分布，通过可学习软映射引导变分后验，并引入熵控制机制确保自信路由的同时保留行为多样性。此外，采用LoRA构建参数高效专家池避免膨胀。实验表明，CoRDE有效减少路由崩溃，形成语义对齐的专家分配，动作质量与增量学习效率均优于基线。该工作为机器人操作结构泛化提供了新视角。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21935v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1653, \"height\": 871, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21935v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1860, \"height\": 942, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21935v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 902, \"height\": 379, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21935v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1746, \"height\": 403, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21935v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 661, \"height\": 181, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21935v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1485, \"height\": 372, \"label\": \"Table\"}]"
motivation: 现有扩散模型泛化不足且MoE方法存在路由崩溃和参数爆炸问题。
method: 提出CoRDE，利用冻结概念编码器引导变分后验，通过软映射和熵控制实现路由，并采用LoRA高效专家池。
result: 有效减少路由崩溃，形成语义对齐的专家分配，提升动作质量和增量学习效率。
conclusion: CoRDE为机器人操作结构泛化提供了高效、高保真的解决方案。
---

## 摘要
扩散模型在机器人模仿学习中擅长捕捉多模态动作分布。然而，在多任务和长时域场景中，单一架构缺乏结构泛化能力，面临不同语义子阶段间的梯度冲突。纯数据驱动的混合专家（MoE）方法虽然引入了分工，但常引发路由崩溃，且实例化全规模专家会导致参数爆炸和扩展成本高昂。为解决这些问题，我们提出基于概念先验的路由扩散专家（CoRDE），一种结构引导的变分蒸馏框架。CoRDE从冻结的概念编码器中提取语义分布，通过可学习的软映射矩阵指导变分后验责任。该机制引入熵控制的责任推理过程，在可靠语义预测下鼓励自信路由，同时保留随机扩散项以保证行为多样性。为克服参数膨胀，CoRDE在共享冻结骨干网络上采用低秩自适应（LoRA）构建参数高效的专家池。理论分析表明，混合分数差异受责任加权的局部专家误差约束，支持在低秩专家自适应下实现高保真生成。实验评估证实，与现有基线相比，CoRDE系统性地减少了路由崩溃，形成了鲁棒、语义对齐的专家分配，同时实现了更优的动作质量和增量学习效率。

## Abstract
Diffusion models excel at capturing multi-modal action distributions in robot imitation learning. However, in multi-task and long-horizon scenarios, monolithic architectures lack structural generalization capabilities, suffering from gradient conflicts between distinct semantic sub-stages. While pure data-driven Mixture-of-Experts (MoE) methods introduce labor division, they frequently trigger routing collapse, and instantiating full-scale experts causes parameter explosion and high expansion costs. To address these issues, we propose Concept-prior Routed Diffusion Experts (CoRDE), a structure-guided variational distillation framework. CoRDE extracts semantic distributions from a frozen concept encoder to guide the variational posterior responsibility via a learnable soft mapping matrix. This mechanism introduces an entropy-controlled responsibility inference process that encourages confident routing under reliable semantic predictions while preserving the stochastic diffusion term for behavioral diversity. To overcome parameter inflation, CoRDE employs a parameter-efficient expert pool using Low-Rank Adaptation (LoRA) on a shared frozen backbone. Theoretical analysis shows that the mixture score discrepancy is bounded by responsibility-weighted local expert errors, supporting high-fidelity generation under low-rank expert adaptation. Empirical evaluations confirm that, compared to existing baselines, CoRDE systematically reduces routing collapse, forming robust, semantically aligned expert allocations while achieving superior action quality and incremental learning efficiency.

---

## 论文详细总结（自动生成）

# CoRDE：基于概念先验的路由扩散专家实现机器人操作中的结构泛化 —— 论文总结

## 1. 核心问题与整体含义（研究动机与背景）

扩散模型在机器人模仿学习中表现出强大的多模态动作分布建模能力，但在多任务、长时域场景下，单一架构（monolithic policy）存在严重的**结构泛化不足**问题：不同语义子阶段（如“导航”与“精细操作”）对应的动作模式频率差异极大，迫使单个网络同时拟合这些异质行为时引发**梯度冲突**和**灾难性干扰**。为缓解此问题，研究者将扩散策略蒸馏为**混合专家（MoE）**模型，但现有纯数据驱动的 MoE 方法依赖数据似然路由，容易导致**路由崩溃**（winner-takes-all 局部最优）以及**参数爆炸**（全尺寸专家拷贝）。因此，本文的核心问题是：**如何在保持多模态行为多样性的前提下，实现语义可解释、参数高效的专家分配，从而提升多任务机器人操作的结构泛化能力**。

## 2. 方法论：核心思想、关键技术细节与算法流程

### 核心思想
CoRDE 提出一种**结构引导的变分蒸馏框架**，将预训练的扩散策略（teacher）蒸馏为一组参数高效的 LoRA 专家（student）。其创新在于：利用**冻结的概念编码器**提取高层语义分布，通过**可学习的软映射矩阵**构造结构先验，在变分后验推理中融合语义先验与行为证据，从而得到稳定、语义对齐的专家分配，同时保留 SDE 中的随机扩散项以确保行为多样性。

### 关键技术细节与公式
- **结构先验构建**：冻结的概念编码器输出语义分布 \(p_{\phi}(c|o)\)，通过软映射矩阵 \(P_{CE}(z|c)\) 得到结构先验 \(p_{\text{struct}}(z|o) = \sum_c p_{\phi}(c|o)P_{CE}(z|c)\)。
- **变分后验推理**：联合语义先验与行为证据 \(r(z|o,a_0)\)（基于教师分数场与专家分数场的差异），求解最优变分后验责任：
  \[
  q^*(z|o,a_0) = \frac{1}{Z} \bigl(r(z|o,a_0)\bigr)^{\frac{1}{1+\lambda(o)}} \bigl(p_{\text{struct}}(z|o)\bigr)^{\frac{\lambda(o)}{1+\lambda(o)}}
  \]
  其中 \(\lambda(o)\) 根据概念置信度自适应调节，实现**双熵守恒**：低路由熵（高确信路由）与全秩扩散方差（多样性保持）。
- **LoRA 参数高效专家池**：共享冻结的扩散 Transformer 主干，每个专家仅在其线性投影层注入 LoRA 增量 \(W^{(z)} = W_0 + B^{(z)}A^{(z)}\)。理论证明（定理 2）混合分数场误差被责任加权的局部专家误差所界定：
  \[
  \|u_T - \tilde{u}\|_2^2 \le \sum_{z} q^*(z)\|u_T - u^{(z)}_\theta\|_2^2
  \]
  同时，SDE 中的维纳过程项确保了行为多样性不受低秩约束影响。
- **EM 风格对齐**：路由器通过拟合后验责任进行稳定训练（KL 散度）；软映射矩阵通过 Dirichlet 平滑计数与指数移动平均迭代更新，建立可解释的概念-专家多对多映射。

### 算法流程（文字描述）
- **训练阶段（Phase I, Algorithm 1）**：从数据集中采样，冻结概念编码器提取语义；计算结构先验；采样噪声与时间步，计算局部分数误差作为行为证据；按公式计算后验责任（E-step）；然后进行三个 M-step：1）更新 LoRA 专家（最小化加权的分数匹配损失）；2）更新路由器（最小化 KL 散度）；3）更新软映射矩阵（基于软计数和 EMA）。
- **推理阶段（Phase II, Algorithm 2）**：给定观测，提取语义先验，计算结构先验；将结构先验作为偏置注入路由器 logits；激活 top-k 专家；通过 SDE 迭代去噪生成动作序列。

## 3. 实验设计

### 数据集与基准场景
- **LIBERO 基准**：包含四个标准套件（L-Spatial, L-Object, L-Goal, L-Long），用于评估多任务长时域操作的成功率与结构泛化。
- **D3IL 基准**：包含 7 个任务（Avoiding, Aligning, Pushing, Stacking-1, Stacking-2 基于状态；Sorting, Stacking 基于图像），每个任务提供高度多模态的人类演示，用于评估行为多样性（Task Entropy）和推理效率。

### 对比方法
- Diffusion Policy (Teacher)：原扩散策略。
- Distill-MoE (Evidence-only)：VDD 方法，仅依赖行为证据的蒸馏 MoE，无结构先验。
- CoRDE (HiMaCon 编码器) & CoRDE (AutoCGP 编码器)：本文方法，分别使用两种无监督概念编码器。所有蒸馏模型共享相同的骨干、专家数、LoRA 配置和训练步骤，控制参数预算公平。

### 消融实验
在 CoRDE (AutoCGP) 基础上依次移除：
- w/o PCE（移除软映射，改为恒等映射）
- w/o adaptive λ(o)（固定 λ 值）
- Evidence-only (λ=0)
- Router w/o responsibility supervision（路由器不拟合后验责任）
报告了 LIBERO 平均成功率、D3IL 成功率和任务熵，以及路由诊断指标（Hroute, Hpost, PPL）。

## 4. 资源与算力

**文中未明确说明**所使用的 GPU 型号、数量、训练时长等具体硬件与算力配置。仅提及所有蒸馏模型共享相同的参数预算和架构，但未列出计算资源细节。因此无法据此判断计算开销水平。

## 5. 实验数量与充分性

- **实验数量**：在 LIBERO 四个套件上报告了成功率（图 2 含柱状图比较）；在 D3IL 七个任务上报告了成功率与任务熵（表 I）；报告了推理时间对比（表 II）；进行了完整的消融实验（表 III）。总量较为丰富。
- **充分性**：设计了公平的参数控制（所有蒸馏模型使用相同骨干、专家数、LoRA 配置）；对比了两种概念编码器（HiMaCon 和 AutoCGP），排除了对人工标注的依赖；消融实验覆盖了关键设计（PCE、自适应 λ、责任监督），并采用路由诊断指标区分结构化专门化与崩溃。实验设计整体客观、公平。

## 6. 主要结论与发现

- **性能提升**：CoRDE 在 LIBERO 所有套件上一致优于 monolithic DP 和 evidence-only Distill-MoE，尤其在具有强多阶段结构的长时域场景（L-Long, L-Goal）中提升最显著。
- **防止路由崩溃**：结构先验显著降低了后验熵和路由熵，同时保持专家平衡使用（PPL 适中），避免了崩溃。
- **保持多样性**：在 D3IL 上，CoRDE 的任务熵接近原 DP 教师，远高于确定性基线，验证了 SDE 扩散项对行为多样性的保护。
- **参数高效**：LoRA 专家池在保持高保真动作生成的同时，单步推理仅需 2.63ms（NFE=1），相比教师 DP（55.62ms）加速一个数量级。
- **消融证实**：移除 PCE、自适应 λ 或责任监督均导致性能下降和路由质量恶化，验证了各关键部件的必要性。

## 7. 优点

- **结构引导路由**：利用冻结概念编码器提供稳定语义先验，避免纯数据驱动的路由崩溃，且不需要人工标注概念。
- **理论保障**：严格推导了最优后验、路由熵界、混合分数误差界，保证低秩专家下生成保真度。
- **参数高效且多样性保留**：LoRA 在减少参数的同时，利用扩散 SDE 的随机项维持行为多样性，理论证明两者解耦。
- **软对齐 EM 算法**：建立可解释的概念-专家多对多映射，避免硬指派带来的决策突变。
- **实验设计严谨**：控制参数预算、对比两种概念提取器、提供多维度诊断指标（熵、PPL）。

## 8. 不足与局限

- **实验覆盖**：仅在仿真环境（LIBERO, D3IL）上验证，未进行真实世界机器人实验；任务多样性有限（主要为桌面操作）。
- **算力信息缺失**：未报告训练所需的 GPU 型号、数量及时间，影响可重复性评估。
- **概念编码器依赖**：性能部分依赖预先训练的概念编码器质量，若概念编码器在分布外场景下不稳定，可能退化。
- **在线适应考虑不足**：论文主要关注离线蒸馏，未探讨在线更新或分布偏移下的自适应能力。
- **消融未完全覆盖**：如未讨论专家数量 K 对路由的影响，以及不同 LoRA 秩的敏感性分析。

（完）
